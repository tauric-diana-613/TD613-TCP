name: Retired Amari Vercel repair scaffold

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by the one-shot Vercel budget finalizer."
