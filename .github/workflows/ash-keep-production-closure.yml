name: Ash Keep Production Closure

on:
  pull_request:
    paths:
      - "app/dome-world/ash-keep.html"
      - "app/dome-world/ash-keep.js"
      - "app/dome-world/ash-keep-worker.js"
      - "app/dome-world/fixtures/ash-keep-demo.json"
      - "app/dome-world/docs/ASH_KEEP_V1_PRODUCTION_DEMO_RECEIPT.md"
      - "app/engine/ash-keep-*.js"
      - "app/aperture/release.json"
      - "app/aperture/release.js"
      - "tests/fixtures/aperture-release.json"
      - "scripts/ash-keep-local-closure-server.mjs"
      - "scripts/ash-keep-production-probe.mjs"
      - "scripts/run-ash-keep-production-probe.mjs"
      - "scripts/publish-ash-keep-observer-status.mjs"
      - "scripts/assert-ash-keep-release-posture.mjs"
      - "tests/ash-keep-*.test.mjs"
      - ".github/workflows/ash-keep-production-closure.yml"
  push:
    branches: [main]
    paths:
      - "app/dome-world/ash-keep.html"
      - "app/dome-world/ash-keep.js"
      - "app/dome-world/ash-keep-worker.js"
      - "app/dome-world/fixtures/ash-keep-demo.json"
      - "app/dome-world/docs/ASH_KEEP_V1_PRODUCTION_DEMO_RECEIPT.md"
      - "app/engine/ash-keep-*.js"
      - "app/aperture/release.json"
      - "app/aperture/release.js"
      - "tests/fixtures/aperture-release.json"
      - "scripts/ash-keep-local-closure-server.mjs"
      - "scripts/ash-keep-production-probe.mjs"
      - "scripts/run-ash-keep-production-probe.mjs"
      - "scripts/publish-ash-keep-observer-status.mjs"
      - "scripts/assert-ash-keep-release-posture.mjs"
      - "tests/ash-keep-*.test.mjs"
      - ".github/workflows/ash-keep-production-closure.yml"
  workflow_run:
    workflows: ["Test and deploy static app"]
    types: [completed]
  workflow_dispatch:
    inputs:
      base_url:
        description: "Optional deployed base URL for a non-promoting observation"
        required: false
        default: ""
        type: string
      confirm_deployed_probe:
        description: "Type RUN_DEPLOYED_OBSERVATION to run against base_url"
        required: false
        default: "HOLD"
        type: string

permissions:
  contents: read
  statuses: write

jobs:
  local-closure-validation:
    if: github.event_name != 'workflow_run'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install repository dependencies
        run: npm ci
      - name: Validate probe, fixture runner, status publisher, posture verifier, and server syntax
        run: |
          node --check scripts/ash-keep-production-probe.mjs
          node --check scripts/run-ash-keep-production-probe.mjs
          node --check scripts/publish-ash-keep-observer-status.mjs
          node --check scripts/assert-ash-keep-release-posture.mjs
          node --check scripts/ash-keep-local-closure-server.mjs
      - name: Validate production closure contract
        run: node tests/ash-keep-production-closure-contract.test.mjs
      - name: Enforce release promotion gate
        run: node tests/ash-keep-production-promotion-gate.test.mjs
      - name: Verify current Ash Keep release posture
        run: node scripts/assert-ash-keep-release-posture.mjs
      - name: Rebuild maintained Aperture download artifact
        run: npm run aperture:export-downloads
      - name: Preserve Ash Keep component regressions
        run: npm run test:ash-keep
      - name: Install Chromium probe runtime
        run: |
          npm install --no-save --package-lock=false playwright@1.53.2
          npx playwright install --with-deps chromium
      - name: Start bounded local Ash Keep runtime
        run: |
          set -euo pipefail
          mkdir -p artifacts
          node scripts/ash-keep-local-closure-server.mjs 6130 > artifacts/ash-keep-local-server.log 2>&1 &
          echo $! > artifacts/ash-keep-local-server.pid
          for attempt in $(seq 1 30); do
            if curl -fsS --max-time 2 http://127.0.0.1:6130/__ash_keep_closure/readiness > artifacts/ash-keep-local-readiness.json; then
              cat artifacts/ash-keep-local-readiness.json
              exit 0
            fi
            sleep 1
          done
          cat artifacts/ash-keep-local-server.log || true
          exit 1
      - name: Run bounded local closure observation
        env:
          TD613_BASE_URL: http://127.0.0.1:6130
          TD613_ARTIFACT_DIR: artifacts/ash-keep-production-closure
          TD613_PROBE_RUNTIME_DIR: artifacts/ash-keep-probe-runtime
        run: node scripts/run-ash-keep-production-probe.mjs
      - name: Preserve closure evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ash-keep-production-closure-evidence
          path: |
            scripts/ash-keep-production-probe.mjs
            scripts/run-ash-keep-production-probe.mjs
            scripts/publish-ash-keep-observer-status.mjs
            scripts/assert-ash-keep-release-posture.mjs
            artifacts/ash-keep-local-server.log
            artifacts/ash-keep-local-readiness.json
            artifacts/ash-keep-probe-runtime/
            artifacts/ash-keep-production-closure/
          if-no-files-found: warn
          retention-days: 14
      - name: Stop bounded local runtime
        if: always()
        run: |
          if [ -f artifacts/ash-keep-local-server.pid ]; then
            kill "$(cat artifacts/ash-keep-local-server.pid)" 2>/dev/null || true
          fi

  deployed-observation:
    if: >-
      (github.event_name == 'workflow_dispatch' && inputs.confirm_deployed_probe == 'RUN_DEPLOYED_OBSERVATION' && inputs.base_url != '')
      ||
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main')
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      TD613_DEPLOYED_BASE_URL: ${{ github.event_name == 'workflow_run' && 'https://td613.com' || inputs.base_url }}
      TD613_OBSERVED_COMMIT: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_sha || github.sha }}
      TD613_UPSTREAM_WORKFLOW_RUN_ID: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.id || '' }}
      TD613_OBSERVER_RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
      TD613_OBSERVER_STATUS_CONTEXT: Ash Keep Deployed Observation
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.TD613_OBSERVED_COMMIT }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Publish observer pending status
        env:
          GITHUB_TOKEN: ${{ github.token }}
          TD613_OBSERVER_STATUS_STATE: pending
          TD613_OBSERVER_STATUS_DESCRIPTION: Ash Keep deployed observation is running
          TD613_OBSERVER_STATUS_RECEIPT_PATH: artifacts/ash-keep-deployed-observation/observer-status-pending.json
        run: node scripts/publish-ash-keep-observer-status.mjs
      - name: Install repository dependencies
        run: npm ci
      - name: Wait for deployed Ash Keep route
        run: |
          set -euo pipefail
          mkdir -p artifacts/ash-keep-deployed-observation
          route="${TD613_DEPLOYED_BASE_URL%/}/dome-world/ash-keep.html"
          for attempt in $(seq 1 60); do
            body="$(curl -fsSL --max-time 10 "$route" || true)"
            if printf '%s' "$body" | grep -q 'TD613 Ash Keep'; then
              printf '%s' "$body" > artifacts/ash-keep-deployed-observation/deployed-route.html
              printf '{"route":"%s","attempt":%s,"status":"READY"}\n' "$route" "$attempt" > artifacts/ash-keep-deployed-observation/deployment-readiness.json
              exit 0
            fi
            sleep 5
          done
          printf '{"route":"%s","status":"HOLD_DEPLOYED_ROUTE_UNAVAILABLE"}\n' "$route" > artifacts/ash-keep-deployed-observation/deployment-readiness.json
          exit 1
      - name: Record deployment observer context
        env:
          TD613_OBSERVER_RUN_ID: ${{ github.run_id }}
          TD613_OBSERVER_RUN_ATTEMPT: ${{ github.run_attempt }}
          TD613_TRIGGER_EVENT: ${{ github.event_name }}
        run: |
          node <<'NODE'
          const fs = require('node:fs');
          const crypto = require('node:crypto');
          const context = {
            schema: 'td613.ash-keep.deployment-observer-context/v0.1',
            observed_base_url: process.env.TD613_DEPLOYED_BASE_URL,
            observed_runtime_commit: process.env.TD613_OBSERVED_COMMIT,
            upstream_deployment_workflow_run_id: process.env.TD613_UPSTREAM_WORKFLOW_RUN_ID || null,
            observer_workflow_run_id: process.env.TD613_OBSERVER_RUN_ID,
            observer_workflow_run_attempt: process.env.TD613_OBSERVER_RUN_ATTEMPT,
            observer_run_url: process.env.TD613_OBSERVER_RUN_URL,
            observer_status_context: process.env.TD613_OBSERVER_STATUS_CONTEXT,
            trigger_event: process.env.TD613_TRIGGER_EVENT,
            source_status: 'DEPLOYED_OBSERVATION',
            promotion_authorized: false
          };
          const body = `${JSON.stringify(context, null, 2)}\n`;
          context.context_sha256 = `sha256:${crypto.createHash('sha256').update(body).digest('hex')}`;
          fs.writeFileSync(
            'artifacts/ash-keep-deployed-observation/deployment-observer-context.json',
            `${JSON.stringify(context, null, 2)}\n`
          );
          NODE
      - name: Install Chromium probe runtime
        run: |
          npm install --no-save --package-lock=false playwright@1.53.2
          npx playwright install --with-deps chromium
      - name: Observe deployed Ash Keep without promotion
        env:
          TD613_BASE_URL: ${{ env.TD613_DEPLOYED_BASE_URL }}
          TD613_ARTIFACT_DIR: artifacts/ash-keep-deployed-observation
          TD613_PROBE_RUNTIME_DIR: artifacts/ash-keep-probe-runtime
        run: node scripts/run-ash-keep-production-probe.mjs
      - name: Validate deployed observation class
        run: |
          node <<'NODE'
          const fs = require('node:fs');
          const report = JSON.parse(fs.readFileSync('artifacts/ash-keep-deployed-observation/ash-keep-production-closure.json', 'utf8'));
          if (report.status !== 'PASS') throw new Error(`Deployed observation did not pass: ${report.status}`);
          if (report.source_status !== 'DEPLOYED_OBSERVATION') throw new Error(`Unexpected source status: ${report.source_status}`);
          if (report.promotion_authorized !== false) throw new Error('Deployed observation attempted to authorize promotion.');
          if (report.network?.non_read_requests?.length) throw new Error('Deployed observation emitted non-read requests.');
          if (report.network?.recipient_transport_requests?.length) throw new Error('Deployed observation emitted recipient transport.');
          NODE
      - name: Verify checked-out release posture
        if: always()
        env:
          TD613_RELEASE_POSTURE_RECEIPT_PATH: artifacts/ash-keep-deployed-observation/release-posture-verification.json
        run: node scripts/assert-ash-keep-release-posture.mjs
      - name: Publish observer success status
        if: success()
        env:
          GITHUB_TOKEN: ${{ github.token }}
          TD613_OBSERVER_STATUS_STATE: success
          TD613_OBSERVER_STATUS_DESCRIPTION: Ash Keep deployed observation passed; posture preserved
          TD613_OBSERVER_STATUS_RECEIPT_PATH: artifacts/ash-keep-deployed-observation/observer-status-success.json
        run: node scripts/publish-ash-keep-observer-status.mjs
      - name: Publish observer failure status
        if: failure()
        env:
          GITHUB_TOKEN: ${{ github.token }}
          TD613_OBSERVER_STATUS_STATE: failure
          TD613_OBSERVER_STATUS_DESCRIPTION: Ash Keep deployed observation held for repair
          TD613_OBSERVER_STATUS_RECEIPT_PATH: artifacts/ash-keep-deployed-observation/observer-status-failure.json
        run: node scripts/publish-ash-keep-observer-status.mjs
      - name: Preserve deployed observation and status receipts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ash-keep-deployed-observation-evidence
          path: |
            scripts/ash-keep-production-probe.mjs
            scripts/run-ash-keep-production-probe.mjs
            scripts/publish-ash-keep-observer-status.mjs
            scripts/assert-ash-keep-release-posture.mjs
            artifacts/ash-keep-probe-runtime/
            artifacts/ash-keep-deployed-observation/
          if-no-files-found: error
          retention-days: 30
      - name: Reconcile terminal failure status
        if: failure()
        env:
          GITHUB_TOKEN: ${{ github.token }}
          TD613_OBSERVER_STATUS_STATE: failure
          TD613_OBSERVER_STATUS_DESCRIPTION: Ash Keep deployed observation or evidence preservation failed
          TD613_OBSERVER_STATUS_RECEIPT_PATH: artifacts/ash-keep-deployed-observation/observer-status-failure-reconciled.json
        run: node scripts/publish-ash-keep-observer-status.mjs
