name: Hush Current Surface Coherence

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'app/hush.html'
      - 'app/hush.js'
      - 'app/adversarial-bench.html'
      - 'app/hush-compare-layout-custody.js'
      - 'app/hush-layout-topology-guard.js'
      - 'app/hush-strict-held-diagnostic-popup.js'
      - 'app/hush-source-layout-policy.js'
      - 'scripts/hush-test-suite.txt'
      - 'vercel.json'
      - 'tests/hush-current-surface-coherence.test.mjs'
      - 'tests/hush-layout-topology-guard.test.mjs'
      - 'tests/hush-strict-held-diagnostic-popup.test.mjs'
      - '.github/workflows/hush-current-surface-coherence.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  current-surface-coherence:
    name: Hush current surface coherence
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - run: node tests/hush-current-surface-coherence.test.mjs
      - run: node tests/hush-layout-topology-guard.test.mjs
      - run: node tests/hush-strict-held-diagnostic-popup.test.mjs
