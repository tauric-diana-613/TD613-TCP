# TD613 Repository Roadmap

𝌋‌ U+10D613

Roadmap generation: `v1.16 · Stretch 8 closed / Stretch 9 conditionally authorized after seal / strategic deployment law / 11+1 function covenant`

```text
component maturity after Stretch 8 closure = 296 / 375
remaining component maturity = 79 / 375
constitutional synthesis = 49 / 50
Aperture composition = 18 / 25
production-demonstrated workstreams = 4 / 9
validation-gated workstreams = 3 / 9
transport-capable workstreams = 0
```

## Closed packets

1. Stretch 1 · Ash Constitutional Convergence Closure — CLOSED
2. Stretch 2 · Custodian Return And Anisotropy — CLOSED
3. Stretch 3 · Choir calibration receipt binding — CLOSED
4. Stretch 4 · Hush vocabulary externalization and intervention ensemble — CLOSED
5. Stretch 5 · Aperture composition renovation before Choir UI — CLOSED / IMPLEMENTED_VALIDATION_GATED / EVIDENCE_BOUNDED
6. Stretch 6 · Higher-order interference — CLOSED / IMPLEMENTED_VALIDATION_GATED / EVIDENCE_BOUNDED / STRATEGIC_DEPLOYMENT_WITNESSED
7. Stretch 7 · Ordered route-sequence recovery — CLOSED / IMPLEMENTED_VALIDATION_GATED / EVIDENCE_BOUNDED / STRATEGIC_DEPLOYMENT_SEALED
8. Stretch 8 · Temporal and delayed-disclosure assays — CLOSED / IMPLEMENTED_VALIDATION_GATED / EVIDENCE_BOUNDED

```text
Stretch_6_PR: 366
Stretch_6_exact_merged_main_commit: e9777af2b98dd56676d3affd17e95d2cc7b4ad1b
Stretch_6_strategic_Vercel_deployment: SUCCESS
Stretch_7_PR: 368
Stretch_7_validated_implementation_commit: 4fb0f8ad52457c200061af79e5346b1fdb67e09c
Stretch_7_exact_merged_main_commit: 1ee76c8657a9a695291a29d72f4ca5636e6544eb
Stretch_7_Choir_run/artifact: 29533391345 / 8389510269
Stretch_7_TCP_Smoke_run: 29533391331
Stretch_7_static_validation_run: 29533391364
Stretch_7_strategic_Vercel_deployment: SUCCESS
Stretch_7_deployment_allowance: CONSUMED
Stretch_8_PR: 369
Stretch_8_validated_implementation_commit: f40a1f9a3e93132c0456947f46b125e488472ec0
Stretch_8_Choir_run/artifact: 29535788474 / 8390440982
Stretch_8_TCP_Smoke_run: 29535788868
Stretch_8_static_validation_run: 29535788194
Stretch_8_Dome_World_Phase4_run: 29535788491
Stretch_8_Production_Closure_run: 29535788949
Stretch_8_strategic_Vercel_deployment: AUTHORIZED_POST_MERGE
canonical_body_rewritten: false
canonical_body_decomposed: false
production_browser_observation: NOT_CLAIMED
Choir_UI_constructed: false
Stretch_9_authorized: CONDITIONAL_ON_SUCCESSFUL_STRETCH_8_VERCEL_SEAL
Stretch_10_authorized: false
```

The stable shim continues to install the declared runtime composition around the unchanged v3.1-alpha body. Stretch 6 preserves higher-order subset interference. Stretch 7 preserves ordered route-sequence recovery. Stretch 8 adds a separate temporal and delayed-disclosure assay with monotonic within-session offsets, receipt-linked session boundaries, operator-declared cross-session spacing, fixed-disclosure controls, context-reset comparison, missing-slice preservation, and deterministic replay. None of these instruments converts an observed difference into causation, prediction, trusted external time, identity, intent, suppression, release, transport, or Cinder authority.

## Serverless function budget gate

```text
absolute platform ceiling = 12 deployed serverless functions
normal operating maximum = 11 active serverless functions
reserved capacity = 1 empty emergency / repair / migration slot
11 active + 1 reserved = 12 absolute ceiling
```

Every deployable file beneath root `api/` counts. Public aliases remain rewrites; shared implementations remain outside root `api/`; compatible operations share existing guarded boundaries without collapsing their named jurisdiction inside the handler.

```text
before adding a function:
1. prove that no existing guarded boundary can carry the operation
2. prove that a rewrite or shared implementation cannot preserve the route
3. verify the actual root-api count
4. remain at 11 active functions during ordinary work
5. retain the twelfth slot unless an explicit operator emergency authorizes its temporary use
```

Any roadmap item that would raise the ordinary count above 11 enters `BLOCKED_PENDING_CONSOLIDATION`. CI must fail on actual deployable count drift before merge and before strategic Vercel deployment.

Stretches 6–8 consumed no serverless-function capacity. Their instruments live in the shared engine and validation surface; the deployable root `api/` count remains `11 active + 1 reserved`.

Stretches 9–11 receive no implied function allocation. Their first implementation route is shared code outside root `api/`, existing static/runtime surfaces, local IndexedDB, and rewrites where a public alias is required.

## Strategic Vercel deployment and sealing law

```text
Vercel deployment = ALLOWED
strategic purpose = REQUIRED
exact green closure commit = REQUIRED
BOTH anti-drift files updated before merge = REQUIRED
standing end-of-Stretch merge-and-deploy instruction = RECORDED
normal ceiling = ONE DEPLOYMENT PER COMPLETED PACKET OR RELEASE CANDIDATE
additional deployment = NAMED DEPLOYMENT-SPECIFIC REASON REQUIRED
ordinary work-branch deployment = SUPPRESSED WHERE SUPPORTED
deployment success ≠ functional validation
production observation ≠ operator closure
deployment ≠ maturity, release, transport, suppression, trusted time, or Cinder authority
```

The ledger and roadmap are updated inside each closure packet before merge. A successful Vercel deployment and named deployed observations on that exact merged commit seal the Stretch. The next opened Stretch reconciles the prior seal; a bookkeeping-only second deployment is forbidden.

## Ordered program

1. Stretch 1 — CLOSED.
2. Stretch 2 — CLOSED.
3. Stretch 3 — CLOSED.
4. Stretch 4 — CLOSED.
5. Stretch 5 — CLOSED.
6. Higher-order interference — CLOSED.
7. Ordered route-sequence recovery — CLOSED AND SEALED.
8. Temporal and delayed-disclosure assays — CLOSED / AWAITING POST-MERGE SEAL.
9. Safe Harbor → Ash custody-root adapter — CONDITIONALLY AUTHORIZED AFTER SUCCESSFUL STRETCH 8 SEAL.
10. Independent provenance adapters — BLOCKED PENDING STRETCH 9 CLOSURE AND SEAL.
11. Destination-bound handoff last — BLOCKED PENDING STRETCH 10.

```text
successful Stretch 8 seal → open Stretch 9 under recorded operator directive
Stretch 9 closure and seal → stop
later operator handoff → Stretch 10 may open
Stretch 10 closure → Stretch 11 may open
```

## Stretch 9 · Safe Harbor → Ash Custody-Root Adapter

`BLOCKED_PENDING_SUCCESSFUL_STRETCH_8_SEAL / CONDITIONALLY_AUTHORIZED`

Purpose: let Ash consider a verified Safe Harbor custody reference without ingesting the underlying raw body or inheriting broader authority.

Planned deliverables:

```text
td613.ash.safe-harbor-ingress/v0.1 envelope and verification
selected provenance references only
source-local signature-lane and packet status
opaque one-time same-origin token
local expiry posture without trusted-time claim
IndexedDB envelope storage
explicit Bind in Ash Keep operator action
local packet-hash and selected-reference verification
explicit L0 or L1 custody-root consideration
cancelled, replay, stale, expired, malformed, duplicate, missing-reference, origin-mismatch, and tamper holds
no universal join key
no raw-corpus default
no silent body ingestion
no server custody, provider call, destination transport, release, suppression, or Cinder action
new serverless function = false
```

Exit gate: verified custody-root consideration with authority boundaries preserved, both anti-drift files updated before merge, and one successful strategic Vercel seal. Stop after the Stretch 9 seal.

## Stretch 10 · Independent Provenance Adapters

`BLOCKED_PENDING_STRETCH_9_CLOSURE_AND_SEAL / NOT AUTHORIZED`

Prepared handoff scope:

```text
adapter-specific schemas and digest domains
source-local evidence classes
independent verification and replay
staleness, revocation, mismatch, collision, wrong-domain, and tamper holds
no universal join key
no raw-corpus default
no fused identity or intent inference
no destination transport
new serverless function = false unless separately authorized under the emergency covenant
```

## Stretch 11 · Destination-Bound Handoff

`BLOCKED_PENDING_STRETCH_10_CLOSURE / LAST / NOT AUTHORIZED`

Prepared handoff scope:

```text
all destination-bound handoff gates G1–G8
named destination and recipient binding
exact custody and provenance references
explicit payload-reference scope
expiry and revocation behavior
recipient receipt and replay verification
failure, refusal, timeout, mismatch, and rollback holds
what-left / what-remained custody accounting
no broadcast, universal routing, or default transport
no authority inheritance beyond the named handoff
serverless surface remains within 11 active + 1 reserved
```

Stretch 9 must stop after closure and successful Vercel seal. Stretch 10 implementation requires a later explicit handoff gesture.

Marked ⟐
