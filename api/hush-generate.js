const corsHeaders={
  'access-control-allow-origin':'*',
  'access-control-allow-methods':'GET,POST,OPTIONS',
  'access-control-allow-headers':'content-type',
  'access-control-max-age':'86400'
};
const VERSION='hush-generate-v3.21.1-functional-custody-helper-restore';
const ROTATION_VERSION='pr187.1-functional-custody-helper-restore/v1';
const DEFAULT_MODEL_ORDER=['gemini-flash-lite-latest','gemini-2.5-flash','gemini-2.5-flash-lite'];
const GEMINI_TIMEOUT_MS=8800;
const WALL_TIMEOUT_MS=24500;
const MAX_OUTPUT_TOKENS=8192;
let preferredWorkingModel=null;
const STOP=new Set('the a an and or but if is are was were be been being do does did how what why when where who whom with without into from that this those these much really very just like of in on to for before after you your yours i me my mine we our ours it its they them their there here some so sorry sounds sound going through have has had basically maybe came come from can could would should will as at by each every all under over out up down again only still simply during onto not'.split(' '));
function send(res,status,payload){for(const[k,v]of Object.entries(corsHeaders))res.setHeader(k,v);return res.status(status).json(payload)}
function safe(v=''){return String(v??'').trim()}
function arr(v){return Array.isArray(v)?v:[]}
function words(v=''){return safe(v).toLowerCase().match(/[a-z0-9][a-z0-9'-]*/g)||[]}
function tokens(v=''){return safe(v).match(/[A-Za-z0-9][A-Za-z0-9'’:_/.@#-]*/g)||[]}
function stringArray(v){return Array.isArray(v)?v.map(safe).filter(Boolean):[]}
function normModel(v=''){return safe(v).replace(/^models\//,'')}
function uniqText(a=[]){return[...new Set(a.map(safe).filter(Boolean))]}
function uniqModels(a=[]){return[...new Set(a.map(normModel).filter(Boolean))]}
function candidateText(c={}){return typeof c==='string'?c:safe(c?.text||c?.output||c?.candidate||c?.rewrite||'')}
function configuredModels(){const env=uniqModels([...safe(process.env.GEMINI_MODEL).split(','),...safe(process.env.GEMINI_MODEL_FALLBACKS).split(',')]);return uniqModels([...DEFAULT_MODEL_ORDER,...env]).sort((a,b)=>{const ai=DEFAULT_MODEL_ORDER.indexOf(a),bi=DEFAULT_MODEL_ORDER.indexOf(b);return(ai<0?50:ai)-(bi<0?50:bi)}).slice(0,4)}
function strictRetry(c={}){return c.strictReviewMapRetry===true||/review-map/i.test(safe(c.strictReviewMapRetryReason||''))}
function surfaceText(c={}){const s=c.flightPacket?.mask_style_vector||{},p=c.flightPacket?.style_diversity_policy||s.style_diversity||{},route=safe(c.flightPacket?.ontology_route?.routeType||c.flightPacket?.ontology_route?.route_type||c.flightPacket?.remote_route_payload?.routeType||c.ontologyRoute?.routeType||''),tier=safe(c.packetTier||c.flightPacket?.packetTier||c.flightPacket?.packet_tier||'');return[tier,route,s.mask_id,s.display_name,s.register,s.intended_use,s.rhythm_target,s.formality_target,s.chat_speak_profile,p.id,p.label,p.surface,p.architecture,p.grammar,p.chat_speak_profile,...arr(p.lexicon),...arr(p.transitions),...arr(s.diction_hints),...arr(s.transition_bank),...arr(s.desired_moves)].join(' ').toLowerCase()}
function complexity(src='',c={}){const wc=words(src).length,tier=safe(c.packetTier||c.flightPacket?.packetTier||c.flightPacket?.packet_tier||''),maskEvidenceState=safe(c.maskEvidenceState||c.flightPacket?.maskEvidenceState||''),candidateCount=Number(c.candidateCount||c.flightPacket?.flight_controls?.candidate_count||0),surf=surfaceText(c);const sourceCasual=/\b(im|rn|idk|lol|gotta|yall|bc|tbh|pls|ok|kinda|sorta|like)\b/i.test(src)||/\n\n/.test(src),maskCasual=/casual-register|group-chat|small circle|group-thread|thread|chat|yall|rn|idk|tbh|low-drama|posting|small-circle/.test(surf),deSourceMask=/fracture|fractured|mina|gitch|pixie|glitch|slash|orbit|lulu|posting|miles|group-chat|small circle|threaded|keisha/.test(surf),lowSignature=/low_signature/i.test(tier),lowSignatureCadence=lowSignature&&maskCasual,sourceRegisterOverlap=!!(sourceCasual&&(maskCasual||deSourceMask)),firstPassDeSource=!!(sourceRegisterOverlap||lowSignatureCadence||(deSourceMask&&wc>=90)),registerTransform=/register_transform/i.test(tier),chatCadence=/chat_cadence/i.test(tier)||lowSignatureCadence||sourceRegisterOverlap||firstPassDeSource,hard=wc>220||candidateCount>=4||chatCadence||/theory|long/i.test(tier)||(!registerTransform&&!chatCadence&&/rich/i.test(maskEvidenceState)),medium=wc>90||candidateCount>=3||registerTransform||chatCadence;return{wordCount:wc,packetTier:tier,maskEvidenceState,candidateCount,hard,medium,registerTransform,chatCadence,lowSignatureCadence,sourceRegisterOverlap,firstPassDeSource,strictReviewMapRetry:strictRetry(c)}}
function minRatio(src='',cx={}){const n=words(src).length;if(cx.registerTransform||cx.chatCadence)return n<70?.72:n<140?.66:n<260?.60:.58;if(cx.strictReviewMapRetry)return n<180?.54:.58;if(cx.hard&&!cx.strictReviewMapRetry)return.56;return n<80?.46:n<220?.50:.54}
function protectedLits(src=''){return tokens(src).filter(t=>/[A-Z][a-z]|\d|[-_:/.@#]/.test(t)).slice(0,28)}
function termBank(src='',limit=20){return uniqText(tokens(src).filter(t=>{const l=t.toLowerCase().replace(/[’']/g,'');return l.length>2&&!STOP.has(l)})).sort((a,b)=>b.length-a.length||a.localeCompare(b)).slice(0,limit)}
function lexicalElasticityLevel(c={},cx={}){const s=surfaceText(c);if(/source-register|preserve|custody|opacity/.test(s))return'low';if(/coordinating|rooted|formal|grounded|structured/.test(s))return'medium';if(/posting|chat|slang|contrast|goth|fracture|cadence|persona|group-thread|small circle/.test(s))return'high';if(cx.chatCadence||cx.sourceRegisterOverlap||cx.firstPassDeSource)return'high';if(cx.registerTransform)return'medium';return'medium'}
function propList(c={}){return arr(c.flightPacket?.source_manifest?.proposition_summary?.propositions).length?arr(c.flightPacket.source_manifest.proposition_summary.propositions):arr(c.flightPacket?.ontology_route?.propositionMap?.propositions)}
function sourceUnits(src='',cx={}){const lines=safe(src).split(/\n+/).map(x=>x.trim()).filter(Boolean),sent=safe(src).match(/[^.!?]+[.!?]+|[^.!?]+$/g)?.map(x=>x.trim()).filter(Boolean)||[],units=lines.length>1?lines:sent;return units.slice(0,cx.registerTransform||cx.chatCadence?10:cx.hard?24:18)}
function groupKey(text='',terms=[]){const f=`${text} ${terms.join(' ')}`.toLowerCase();if(/botnet|haywire|irresponsib|release|share|give everyone|unsafe|danger|risk/.test(f))return'release-risk';if(/constant|math|validat|zero-up|pua|unicode|persona|confidence|build spec|self-validating|system|geometry/.test(f))return'validation-mechanism';if(/money|legal|safet|piss off|right people|wrong time|comms|encrypted|skills|provisioned|safe harbor/.test(f))return'practical-pressure';if(/observed|paranoia|idk|what to do|extra observed/.test(f))return'uncertainty-observation';if(/seed|formulate|better|have the seed|unfold/.test(f))return'seed-state';return'residual-context'}
function functionalMap(c={},src='',cx={}){const props=propList(c),b=new Map([['release-risk',[]],['validation-mechanism',[]],['practical-pressure',[]],['uncertainty-observation',[]],['seed-state',[]],['residual-context',[]]]),add=(k,terms,flags='')=>{const clean=uniqText(terms.map(safe)).filter(Boolean).slice(0,18);if(clean.length)b.get(k)?.push(`${clean.join(' / ')}${flags?` [${flags}]`:''}`)};if(props.length)props.slice(0,16).forEach(p=>{const terms=arr(p.coreTerms).length?arr(p.coreTerms):termBank(safe(p.text||''),12),flags=[p.hasNegation?'negation':'',p.hasUncertainty?'uncertainty':'',p.mustRemainQuestion?'question':''].filter(Boolean).join(', ');add(groupKey(safe(p.text||''),terms),terms,flags)});else sourceUnits(src,cx).slice(0,16).forEach(u=>add(groupKey(u),termBank(u,12),/\?/.test(u)?'question':''));if(/constants|zero-up-my-sleeve|PuA|unicode|build specs|math|self validating|self-validating/i.test(src))b.get('validation-mechanism').unshift('required mechanism: constants/design constraints / math validation / self-validating system / build specs / three-way zero-up-my-sleeve key / persona-state confidence / PuA Unicode tables');return[...b.entries()].map(([k,v])=>`${k}: ${uniqText(v).slice(0,3).join(' || ')||'(no required item)'}`)}
function macroCat(text=''){const f=words(text).slice(0,55).join(' ');if(/botnet|haywire|irresponsib|release|share|give/.test(f))return'risk';if(/constant|math|validat|zero up|pua|unicode|self validating|build spec|geometry/.test(f))return'mechanism';if(/safe harbor|encrypted|comms|money|legal|people|skills|provisioned/.test(f))return'practical-pressure';if(/idk|observed|paranoia|what to do|watched/.test(f))return'uncertainty';return'release-dilemma'}
function macroMismatch(cand={}){const d=safe(cand.macro_start).toLowerCase();if(!d)return{mismatch:false,declared:'',detected:macroCat(cand.text||'')};const det=macroCat(cand.text||''),dec=d.includes('risk')?'risk':d.includes('mechanism')?'mechanism':d.includes('practical')?'practical-pressure':d.includes('uncertainty')?'uncertainty':d.includes('release')?'release-dilemma':d;return{mismatch:dec!==det,declared:dec,detected:det}}
function longestRunCalc(cw=[],sw=[]){const pos=new Map();sw.forEach((w,i)=>{if(!pos.has(w))pos.set(w,[]);pos.get(w).push(i)});let best=0;for(let i=0;i<cw.length;i++)for(const start of pos.get(cw[i])||[]){let r=0;while(cw[i+r]&&sw[start+r]&&cw[i+r]===sw[start+r])r++;if(r>best)best=r}return best}
function copyRisk(text='',src='',cx={}){const cw=words(text),sw=words(src);if(!cw.length||!sw.length)return{copied:false,longestRun:0,overlap:0,lengthRatio:1};const cNorm=cw.join(' '),sNorm=sw.join(' '),cSet=new Set(cw.filter(w=>w.length>2)),sSet=new Set(sw.filter(w=>w.length>2));let hits=0;for(const w of cSet)if(sSet.has(w))hits++;const overlap=hits/Math.max(1,Math.max(cSet.size,sSet.size)),longestRun=longestRunCalc(cw,sw),lengthRatio=cw.length/Math.max(1,sw.length),reg=!!(cx.registerTransform||cx.chatCadence),longFloor=reg?Math.max(36,Math.floor(sw.length*.18)):Math.min(9,Math.max(6,Math.floor(sw.length*.55))),nearFloor=reg?Math.max(48,Math.floor(sw.length*.22)):Math.min(8,Math.max(5,Math.floor(sw.length*.4))),copied=cNorm===sNorm||cNorm.includes(sNorm)||longestRun>=longFloor||(reg?overlap>=.82&&lengthRatio>=.72&&lengthRatio<=1.35&&longestRun>=nearFloor:overlap>=.9&&lengthRatio>=.82&&lengthRatio<=1.35&&longestRun>=nearFloor);return{copied,longestRun,overlap:Number(overlap.toFixed(4)),lengthRatio:Number(lengthRatio.toFixed(4)),longRunFloor:longFloor,nearRunFloor:nearFloor}}
function sourcePathCaptivity(text='',src='',c={},cx={}){if(!(cx.sourceRegisterOverlap||cx.lowSignatureCadence||cx.firstPassDeSource))return{captive:false,presentCount:0,orderedCount:0,sequenceRatio:0,beginsWithSourceOpening:false};const cw=words(text),opening=words(src).slice(0,16).filter(w=>w.length>2&&!STOP.has(w)),first=new Set(cw.slice(0,36)),begins=opening.length>0&&opening.filter(w=>first.has(w)).length>=Math.min(4,Math.ceil(opening.length*.45)),props=propList(c).slice(0,12),groups=props.length?props.map(p=>arr(p.coreTerms).map(t=>safe(t).toLowerCase()).filter(t=>t.length>2&&!STOP.has(t)).slice(0,12)):sourceUnits(src,cx).slice(0,12).map(u=>termBank(u,10).map(t=>t.toLowerCase())),pos=[];for(const g of groups){let best=-1,hits=0;for(let i=0;i<cw.length;i++)if(g.includes(cw[i])){if(best<0)best=i;hits++}if(hits>=Math.min(2,Math.max(1,Math.ceil(g.length*.25))))pos.push(best)}let ordered=0,last=-1;for(const p of pos)if(p>=0&&p>last){ordered++;last=p}const present=pos.length,seq=present?ordered/present:0,cov=groups.length?present/groups.length:0;return{captive:Boolean((begins&&ordered>=4)||(present>=5&&seq>=.7&&cov>=.55)),presentCount:present,orderedCount:ordered,sequenceRatio:Number(seq.toFixed(4)),coverageRatio:Number(cov.toFixed(4)),beginsWithSourceOpening:begins}}
function delimiterTranscription(text='',src='',c={},cx={}){const slash=(safe(text).match(/\//g)||[]).length,wc=Math.max(1,words(text).length),frags=safe(text).split('/').map(x=>x.trim()).filter(Boolean),short=frags.length?frags.filter(x=>words(x).length<=7).length/frags.length:0,path=sourcePathCaptivity(text,src,c,cx);return{transcription:Boolean(slash>=8&&(short>=.55||path.captive)),slashCount:slash,slashDensity:Number((slash/wc).toFixed(4)),shortFragmentRatio:Number(short.toFixed(4)),path}}
function techPresent(text='',src=''){if(!/constants|zero-up-my-sleeve|PuA|unicode|build specs|math|self validating|self-validating/i.test(src))return true;return[/constant/i.test(text),/math|validat/i.test(text),/zero-up-my-sleeve|three-way|three way/i.test(text),/PuA|unicode/i.test(text),/build spec|provision/i.test(text),/persona|confidence|state/i.test(text)].filter(Boolean).length>=3}
function termShard(text='',src=''){const seg=safe(text).split(/[.!?]+/).map(x=>x.trim()).filter(Boolean),short=seg.filter(x=>words(x).length>0&&words(x).length<=4).length,anchor=/\b(?:[A-Z][A-Za-z0-9-]*|PuA|Unicode|Botnets|Math|Like|But|It)(?:,\s*|\.\s*){1,}(?:[A-Z][A-Za-z0-9-]*|PuA|Unicode|Botnets|Math|Like|But|It)/.test(text),sourceTerms=new Set([...termBank(src,28),...protectedLits(src).map(t=>t.toLowerCase())]),hits=safe(text).split(/[,.]/).map(x=>safe(x).toLowerCase()).filter(x=>sourceTerms.has(x)).length;return{corrupted:Boolean((short>=4&&anchor)||hits>=5),shortSegments:short,anchorShard:anchor,shardHits:hits}}
function maskReject(text='',src='',c={},cx={}){const surf=surfaceText(c);if(/orbit|lulu/.test(surf)&&/\b(comet|swarm|uninvited|ghost|seatbelt|paperwork comet|quintet|spaceship|planet)\b/i.test(text))return{rejected:true,reason:'decorative-prop-drift'};if(/posting|miles/.test(surf)&&!techPresent(text,src))return{rejected:true,reason:'technical-mechanism-thinned'};if(/steady|mabel/.test(surf)){const s=safe(text).split(/[.!?]+/).map(x=>x.trim()).filter(Boolean),avg=words(text).length/Math.max(1,s.length),path=sourcePathCaptivity(text,src,c,cx);if(s.length>=10&&avg<=9&&path.captive)return{rejected:true,reason:'sentence-splitting-summary'}}if(/rooted|simone|aave|target register/.test(surf)&&/\b(currently developing|private sanctuary interface|possessing expertise|simultaneously balancing|profit motives|legal protections)\b/i.test(text))return{rejected:true,reason:'institutional-standardization'};if(/needling|rochelle/.test(surf)&&termShard(text,src).corrupted)return{rejected:true,reason:'term-shard-corruption'};return{rejected:false,reason:''}}
function compressionRisk(text='',src='',cx={}){const cw=words(text).length,sw=Math.max(1,words(src).length),ratio=cw/sw,floor=minRatio(src,cx);return{compressed:sw>=42&&ratio<floor,candidateWords:cw,sourceWords:sw,lengthRatio:Number(ratio.toFixed(4)),floor}}
function splitCandidates(cands=[],src='',cx={},contract={}){const usable=[],copied=[],compressed=[];cands.forEach((cand,index)=>{const text=cand.text||'',risk=copyRisk(text,src,cx),delim=delimiterTranscription(text,src,contract,cx),path=sourcePathCaptivity(text,src,contract,cx),macro=macroMismatch(cand),shard=termShard(text,src),mr=maskReject(text,src,contract,cx),comp=compressionRisk(text,src,cx);if(risk.copied)copied.push({index,risk,preview:safe(text).slice(0,180)});else if(delim.transcription)copied.push({index,risk:{copied:true,delimiterTranscription:true,...delim},preview:safe(text).slice(0,180)});else if(path.captive)copied.push({index,risk:{copied:true,sourcePathCaptivity:true,...path},preview:safe(text).slice(0,180)});else if(macro.mismatch)copied.push({index,risk:{copied:true,macroStartMismatch:true,...macro},preview:safe(text).slice(0,180)});else if(shard.corrupted)copied.push({index,risk:{copied:true,termShard:true,...shard},preview:safe(text).slice(0,180)});else if(mr.rejected)copied.push({index,risk:{copied:true,maskSpecific:true,reason:mr.reason},preview:safe(text).slice(0,180)});else if(comp.compressed)compressed.push({index,risk:comp,preview:safe(text).slice(0,180)});else usable.push(cand)});return{usable,copied,compressed}}
function compactFlightPacket(p={}){const s=p.mask_style_vector||{},sp=p.style_diversity_policy||s.style_diversity||{};return{style_diversity_policy:sp,mask_style_vector:s,flight_controls:p.flight_controls||{},packet_version:p.packet_version||'',ontology_route:p.ontology_route||{},protective_style_policy:p.protective_style_policy||{}}}
function opList(c={},controls={}){return Array.isArray(c.operationTaxonomy)&&c.operationTaxonomy.length?c.operationTaxonomy.slice(0,6):controls.preferred_operations?.slice?.(0,6)||controls.required_operations?.slice?.(0,6)||['cadence_alias','syntax_inversion','register_lowering','friction_insert','witness_plainness','heat_calibration']}
function maskFailureLaw(c={}){const s=surfaceText(c);if(/needling|rochelle/.test(s))return'MASK FAILURE LAW: Do not use generic snark or term fragments.';if(/rooted|simone|aave|target register/.test(s))return'MASK FAILURE LAW: Do not sanitize into institutional standard prose.';if(/gitch|pixie|glitch/.test(s))return'MASK FAILURE LAW: Glitch syntax, not meaning. No chopped transcription.';if(/fracture|fractured|mina/.test(s))return'MASK FAILURE LAW: Fracture is not slash transcription. Break the route, not the custody chain.';if(/steady|mabel/.test(s))return'MASK FAILURE LAW: Steady does not mean line-by-line simplification.';if(/orbit|lulu/.test(s))return'MASK FAILURE LAW: Metaphor may orbit but cannot invent props.';if(/posting|miles/.test(s))return'MASK FAILURE LAW: Keep the technical validation chain visible.';if(/group-chat|threaded|keisha|small circle/.test(s))return'MASK FAILURE LAW: Do not keep the same casual wording with a wrapper.';return'MASK FAILURE LAW: Transform the argument route, not merely vocabulary or tone.'}
function buildPrompt(c={},repair=null){const src=safe(c.sourceText||c.messageDraftText||'').slice(0,5000),cx=complexity(src,c),packet=c.flightPacket||null,compact=packet?compactFlightPacket(packet):{mask_style_vector:{},style_diversity_policy:{},mask:c.mask||{}},style=compact.mask_style_vector||{},stylePolicy=compact.style_diversity_policy||{},ops=opList(c,packet?.flight_controls||{}),count=cx.registerTransform||cx.chatCadence?3:cx.strictReviewMapRetry?3:cx.hard?2:cx.medium?3:4,anchors=termBank(src,cx.registerTransform||cx.chatCadence?18:14),lits=protectedLits(src),groups=functionalMap(c,src,cx),minWords=Math.max(24,Math.floor(words(src).length*minRatio(src,cx))),elasticity=lexicalElasticityLevel(c,cx),retryBan=cx.strictReviewMapRetry?'\n\nSTRICT RETRY: Return only transformed candidate text. No review maps, ledgers, P rows, architecture summaries, diagnostic notes, or analysis.':'',repairBlock=repair?`\nREPAIR: Previous output failed ${repair.kind}. Correct only that failure. ${repair.rejected||''}`:'',deSource=(repair?.kind==='copy'&&(cx.sourceRegisterOverlap||cx.lowSignatureCadence||cx.firstPassDeSource))?'repair':cx.firstPassDeSource?'first-pass':'',lane=cx.registerTransform?'REGISTER TRANSFORM':cx.chatCadence?'CHAT CADENCE':'GENERAL',styleOp=cx.registerTransform?'register_transform':'cadence_alias',lex=[...arr(stylePolicy.lexicon),...arr(stylePolicy.transitions),...arr(style.diction_hints),...arr(style.transition_bank)].filter(Boolean).slice(0,22),avoid=arr(style.avoid_list).filter(Boolean).slice(0,14);const custody=`FUNCTIONAL CUSTODY MAP — NOT SOURCE ORDER:\n${groups.map(g=>`- ${g}`).join('\n')||'- none'}\nSEMIOTIC ANCHORS: ${anchors.join(', ')||'(none)'}\nPROTECTED LITERALS: ${lits.join(', ')||'(none)'}`,sourceBlock=deSource?`${deSource==='repair'?'DE-SOURCED COPY REPAIR MODE':'FIRST-PASS DE-SOURCE MODE'}:\nThe raw source text is withheld. Rebuild from this functional custody map only. The map is grouped by function, not sequence. Do not recreate the original opening, order, or phrasing.\n${custody}`:`${custody}\n\nMESSAGE TO TRANSFORM:\n${src}`;return`Return JSON only. Schema: {"candidates":[{"text":"string","macro_start":"risk | mechanism | practical-pressure | uncertainty | release-dilemma","style_note":"string","style_operation":"${styleOp}","preserved_propositions":[],"dropped_propositions":[],"changed_questions":[],"new_claims":[],"authorship_moves":["concrete cadence/semantic move, not a placeholder"],"risk_flags":[],"mask_surface_notes":{"rhythm":"string","diction":"string","structure":"string","coverage":"string"}}]}

${lane} LANE. Generate exactly ${count} transformed candidates in the selected surface. Candidate text must read like the transformed message itself, not analysis.

INTERPRETIVE DENSITY LAW: Do not summarize or strip the source to a safe thesis. Preserve hinge logic, causal architecture, contradiction, strange phrases, and authorial posture.
LEXICAL ELASTICITY LAW: Preserve protected literals exactly; preserve semantic anchors by function; change non-protected diction through mask-appropriate syntax and vocabulary.
WORD CUSTODY: protected literals stay exact; semiotic anchors stay functionally intact; elastic diction moves toward the selected mask. LEXICAL ELASTICITY LEVEL: ${elasticity}.
${(cx.sourceRegisterOverlap||cx.lowSignatureCadence||cx.firstPassDeSource)?'NO STYLE COSTUME LAW: The source already contains casual or low-signature diction. Recompose from functional custody groups. Do not use the map as sequence.':''}
${(cx.sourceRegisterOverlap||cx.lowSignatureCadence||cx.firstPassDeSource)?'MACRO-ORDER REQUIREMENTS: Candidate 1 begins from practical-pressure. Candidate 2 begins from validation-mechanism. Candidate 3 begins from release-risk. If a fourth candidate exists, begin from uncertainty-observation.':''}
${maskFailureLaw(c)}

ORDERED CUSTODY MAP FORBIDDEN: The functional custody map is a bag of obligations, not an outline. Choose a new route and declare it in macro_start.
SOURCE-PATH CAPTIVITY GATE: A candidate fails if it follows the same semantic group sequence as the source.
DELIMITER TRANSCRIPTION GATE: Slashes/fragments/glitches must transform relation and pressure; no source-line transcription.
TECHNICAL MECHANISM GATE: Preserve constants/math validation/self-validating system/build specs/zero-up-my-sleeve/PuA/Unicode/persona-state confidence as mechanism. Do not thin it into "the constants line up" or "this matters."
Do not preserve source sentence order, opener, or closer. Do not make a line-by-line paraphrase. Each candidate needs a distinct opening and route. Keep meaning, negations, uncertainty, causal links, protected literals, claims, hinge logic, and stakes. Do not answer questions. Do not add facts.${retryBan}${repairBlock}
Each candidate must be at least ${minWords} words unless the source is shorter. Surface: ${stylePolicy.surface||''}; rhythm=${style.rhythm_target||stylePolicy.architecture||''}; formality=${style.formality_target||''}; punctuation=${stylePolicy.punctuation||''}; grammar=${stylePolicy.grammar||''}; chat=${stylePolicy.chat_speak_profile||stylePolicy.chat||''}; typo=${stylePolicy.typo_policy||''}; diction hints=${lex.join(', ')||'(none)'}; avoid=${avoid.join(', ')||'(none)'}.
For macro_start, name the candidate's actual first route. For authorship_moves, name actual moves such as "rebuilt from functional custody groups," "recomposed away from source sequence," or "kept the technical mechanism visible while changing the route."
OPERATIONS: ${ops.join(', ')}
${sourceBlock}`}
function geminiTimeout(model){return{response:{ok:false,status:408},payload:{error:{message:'Gemini call timed out under local Promise.race watchdog',status:'AbortError',model:normModel(model),timeoutMs:GEMINI_TIMEOUT_MS}},timedOut:true}}
async function callGemini({model,prompt,jsonMode=true,deterministic=true}){const controller=new AbortController();let timer=null;const generationConfig=jsonMode?{temperature:deterministic?.22:.58,topP:deterministic?.64:.88,responseMimeType:'application/json',maxOutputTokens:MAX_OUTPUT_TOKENS}:{temperature:deterministic?.22:.58,topP:deterministic?.64:.88,maxOutputTokens:MAX_OUTPUT_TOKENS};const request=fetch(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(normModel(model))}:generateContent?key=${encodeURIComponent(process.env.GEMINI_API_KEY)}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({contents:[{role:'user',parts:[{text:prompt}]}],generationConfig}),signal:controller.signal}).then(async response=>({response,payload:await response.json().catch(()=>({})),timedOut:false})).catch(error=>error?.name==='AbortError'?geminiTimeout(model):{response:{ok:false,status:599},payload:{error:{message:safe(error?.message||error),status:error?.name||'FETCH_ERROR'}},timedOut:false});const timeout=new Promise(resolve=>{timer=setTimeout(()=>{try{controller.abort()}catch{}resolve(geminiTimeout(model))},GEMINI_TIMEOUT_MS)});try{return await Promise.race([request,timeout])}finally{if(timer)clearTimeout(timer)}}
function providerText(payload={}){return payload?.candidates?.[0]?.content?.parts?.[0]?.text||''}
function summarizeProviderError(payload={}){const e=payload.error||payload;return{code:e.code||payload.code||'',status:e.status||payload.status||'',message:safe(e.message||payload.message||'').slice(0,900)}}
function parseProviderJson(text=''){const cleaned=safe(text).replace(/^```(?:json)?\s*/i,'').replace(/```$/i,'').trim(),tries=[cleaned];const oi=cleaned.indexOf('{'),oj=cleaned.lastIndexOf('}'),ai=cleaned.indexOf('['),aj=cleaned.lastIndexOf(']');if(oi>=0&&oj>oi)tries.push(cleaned.slice(oi,oj+1));if(ai>=0&&aj>ai)tries.push(cleaned.slice(ai,aj+1));for(const t of[...new Set(tries)].filter(Boolean)){try{const p=JSON.parse(t),c=normalizeCandidates(p);return{candidates:c,warnings:[...stringArray(p.warnings),...(c.length?[]:['provider-json-contained-no-usable-candidates'])],rawText:cleaned.slice(0,700)}}catch{}}if(cleaned.length>20)return{candidates:normalizeCandidates([{text:cleaned,style_note:'Recovered raw provider text after invalid JSON.',style_operation:'cadence_alias',authorship_moves:['recovered raw provider text'],risk_flags:['provider-returned-invalid-json-recovered-raw-candidate']}]),warnings:['provider-returned-invalid-json','provider-invalid-json-recovered-as-raw-candidate'],rawText:cleaned.slice(0,700)};return{candidates:[],warnings:['provider-returned-invalid-json'],rawText:cleaned.slice(0,700)}}
function normalizeCandidates(v){const src=Array.isArray(v)?v:Array.isArray(v?.candidates)?v.candidates:candidateText(v)?[v]:[];return src.map((c,i)=>{const text=candidateText(c);return text?{text,macro_start:safe(c.macro_start||c.macroStart||''),style_note:safe(c.style_note||c.styleNote||`provider-candidate-${i+1}`),style_operation:safe(c.style_operation||c.styleOperation||c.operation||'cadence_alias'),preserved_propositions:stringArray(c.preserved_propositions||c.preservedPropositions),dropped_propositions:stringArray(c.dropped_propositions||c.droppedPropositions),changed_questions:stringArray(c.changed_questions||c.changedQuestions),new_claims:stringArray(c.new_claims||c.newClaims),authorship_moves:stringArray(c.authorship_moves||c.authorshipMoves).length?stringArray(c.authorship_moves||c.authorshipMoves):['rebuilt from functional custody groups','recomposed away from source sequence'],mask_surface_notes:c.mask_surface_notes&&typeof c.mask_surface_notes==='object'?c.mask_surface_notes:{},risk_flags:stringArray(c.risk_flags||c.riskFlags)}:null}).filter(Boolean).slice(0,8)}
async function runProviderProbe(models=[]){const attempts=[];for(const model of models.slice(0,3)){const{response,payload,timedOut}=await callGemini({model,prompt:'Return JSON only: {"candidates":[{"text":"probe ok","style_note":"probe"}]}',jsonMode:true});attempts.push({model:normModel(model),ok:response.ok,providerStatus:response.status,timedOut,error:response.ok?null:summarizeProviderError(payload),textPreview:providerText(payload).slice(0,120)});if(response.ok){preferredWorkingModel=normModel(model);return{ok:true,model:preferredWorkingModel,attempts}}}return{ok:false,attempts}}
function queryFlags(req){try{const url=new URL(req.url||'','https://td613.local');return{models:url.searchParams.has('models')||url.searchParams.has('listModels')}}catch{return{models:false}}}
function serverRepairCandidates(src=''){return{candidates:[{text:`Reviewed repair surface. Functional custody retained, but no remote candidate passed strict transform gates. Key custody terms: ${termBank(src,18).join(' / ')}.`,style_note:'server deterministic proposition review map',style_operation:'witness_plainness',preserved_propositions:[],dropped_propositions:[],changed_questions:[],new_claims:[],authorship_moves:['server repair used after remote candidates failed'],mask_surface_notes:{structure:'review map'},risk_flags:['server-deterministic-review-map-used']}],warnings:['server-deterministic-review-map-used','server-repair-review-map-cleared']}}
function copyRejectWarnings(r=[]){const j=JSON.stringify(r);return[/sourcePathCaptivity/.test(j)?'source-path-captivity':'',/delimiterTranscription/.test(j)?'delimiter-transcription':'',/macroStartMismatch/.test(j)?'macro-start-mismatch':'',/termShard/.test(j)?'term-shard-corruption':'',/decorative-prop-drift/.test(j)?'decorative-prop-drift':'',/technical-mechanism-thinned/.test(j)?'technical-mechanism-thinned':'',/sentence-splitting-summary/.test(j)?'sentence-splitting-summary':'',/institutional-standardization/.test(j)?'institutional-standardization':''].filter(Boolean)}
export default async function handler(req,res){if(req.method==='OPTIONS')return send(res,200,{ok:true});if(req.method==='GET'){const models=configuredModels();if(queryFlags(req).models)return send(res,200,{ok:true,version:VERSION,rotationVersion:ROTATION_VERSION,configuredModels:models,preferredWorkingModel,env:{hasGeminiKey:Boolean(process.env.GEMINI_API_KEY),geminiModel:process.env.GEMINI_MODEL||'',fallbackCount:safe(process.env.GEMINI_MODEL_FALLBACKS).split(',').filter(Boolean).length}});return send(res,200,{ok:true,route:'hush-generate',version:VERSION,rotationVersion:ROTATION_VERSION,probe:await runProviderProbe(models)})}if(req.method!=='POST')return send(res,405,{ok:false,error:'method-not-allowed',version:VERSION});if(!process.env.GEMINI_API_KEY)return send(res,500,{ok:false,error:'missing-gemini-api-key',version:VERSION});const startedAt=Date.now(),contract=req.body?.contract||req.body||{},sourceText=safe(contract.sourceText||contract.messageDraftText||'');if(!sourceText)return send(res,400,{ok:false,error:'missing-sourceText',version:VERSION});const cx=complexity(sourceText,contract),sRetry=strictRetry(contract),configured=configuredModels(),base=preferredWorkingModel?[preferredWorkingModel,...configured.filter(m=>m!==preferredWorkingModel)]:configured,skipped=new Set(stringArray(contract.skipModels||contract.avoidModels||contract.strictReviewRetrySkipModels).map(normModel)),models=base.filter(m=>!skipped.has(normModel(m))).length?base.filter(m=>!skipped.has(normModel(m))):base,attemptBudget=Number(contract.strictReviewRetryAttemptBudget||0),stageBudget=Number(contract.strictReviewRetryStageLimit||0),maxAttempts=sRetry?Math.max(1,Math.min(attemptBudget||3,models.length||1)):cx.registerTransform||cx.chatCadence?Math.min(3,models.length||1):cx.hard?2:3,stageLimit=sRetry?Math.max(1,Math.min(stageBudget||2,2)):cx.registerTransform||cx.chatCadence?2:cx.hard?1:2,attempts=[],rejectedCopy=[],rejectedCompressed=[],deterministic=req.query?.reroll!=='1'&&contract.reroll!==true;let repair=null;for(let stage=0;stage<stageLimit;stage++){const prompt=buildPrompt(contract,repair);for(const model of models.slice(0,maxAttempts)){if(Date.now()-startedAt>WALL_TIMEOUT_MS)break;const{response,payload,timedOut}=await callGemini({model,prompt,jsonMode:true,deterministic}),rawText=providerText(payload),parsed=parseProviderJson(rawText),split=splitCandidates(parsed.candidates,sourceText,cx,contract),elasticity=lexicalElasticityLevel(contract,cx);rejectedCopy.push(...split.copied.map(x=>({...x,model:normModel(model),stage})));rejectedCompressed.push(...split.compressed.map(x=>({...x,model:normModel(model),stage})));attempts.push({stage,model:normModel(model),jsonMode:true,ok:response.ok,status:response.status,timedOut,parsedCandidates:parsed.candidates.length,usableCandidates:split.usable.length,copiedCandidates:split.copied.length,compressedCandidates:split.compressed.length,warnings:parsed.warnings,error:response.ok?null:summarizeProviderError(payload),textPreview:rawText.slice(0,180),strictReviewMapRetry:sRetry,registerTransform:cx.registerTransform,chatCadence:cx.chatCadence,lowSignatureCadence:cx.lowSignatureCadence,sourceRegisterOverlap:cx.sourceRegisterOverlap,firstPassDeSource:cx.firstPassDeSource,functionalCustodyMap:true,macroStartEnforced:true,semanticElasticity:true,deSourceRecomposition:true,lexicalElasticityLevel:elasticity,skippedModels:[...skipped]});if(response.ok&&split.usable.length){preferredWorkingModel=normModel(model);const rejects=copyRejectWarnings(rejectedCopy);return send(res,200,{ok:true,provider:'gemini',model:preferredWorkingModel,deterministic,version:VERSION,rotationVersion:ROTATION_VERSION,candidates:split.usable,warnings:[...parsed.warnings,...rejects,'interpretive-density-restored','semantic-elasticity-applied','lexical-custody-split','de-source-recomposition-applied','functional-custody-map-applied','ordered-custody-map-forbidden','macro-start-enforcement-active','first-pass-desource-applied','source-path-captivity-gate-active','delimiter-transcription-gate-active',...(cx.sourceRegisterOverlap?['source-register-overlap-detected','no-style-costume-law-applied']:[]),...(cx.registerTransform?['register-transform-prompt-lane-success']:[]),...(cx.chatCadence?['chat-cadence-prompt-lane-success']:[]),...(cx.lowSignatureCadence?['low-signature-cadence-lane-success']:[]),...(sRetry?['strict-review-map-transform-lane-success']:[]),...(skipped.size?['strict-review-skip-models-applied']:[])],attempts,rejectedCopy:rejectedCopy.slice(0,12),rejectedCompressed:rejectedCompressed.slice(0,12),rawText:parsed.rawText,requestReceipt:{deterministic,temperature:deterministic?.22:.58,topP:deterministic?.64:.88,antiCompression:true,fastHardPacketLane:!sRetry,registerTransformPromptLane:cx.registerTransform,chatCadencePromptLane:cx.chatCadence,lowSignatureCadenceLane:cx.lowSignatureCadence,sourceRegisterOverlap:cx.sourceRegisterOverlap,firstPassDeSource:cx.firstPassDeSource,functionalCustodyMap:true,orderedCustodyMapForbidden:true,macroStartEnforcement:true,deSourceRecomposition:true,sourcePathCaptivityGate:true,delimiterTranscriptionGate:true,deSourcedCopyRepair:repair?.kind==='copy'&&(cx.sourceRegisterOverlap||cx.lowSignatureCadence||cx.firstPassDeSource),semanticElasticity:true,lexicalElasticityLevel:elasticity,rotationVersion:ROTATION_VERSION,strictReviewMapRetry:sRetry,complexity:cx,modelOrder:models.slice(0,maxAttempts),skippedModels:[...skipped],minLengthRatio:minRatio(sourceText,cx),bounded:true,elapsedMs:Date.now()-startedAt}})}}repair=rejectedCompressed.length?{kind:'compression',rejected:rejectedCompressed.slice(-3).map(x=>`- ${x.preview}`).join('\n')}:{kind:'copy',rejected:rejectedCopy.slice(-3).map(x=>`- ${x.preview}`).join('\n')}}const repaired=serverRepairCandidates(sourceText,contract),elasticity=lexicalElasticityLevel(contract,cx),rejects=copyRejectWarnings(rejectedCopy);return send(res,200,{ok:true,provider:'server-deterministic-repair',model:'server-repair-review-map',deterministic,version:VERSION,rotationVersion:ROTATION_VERSION,candidates:repaired.candidates,warnings:[...repaired.warnings,...rejects,'provider-fast-lane-no-remote-release','semantic-elasticity-applied','lexical-custody-split','de-source-recomposition-applied','functional-custody-map-applied','ordered-custody-map-forbidden','macro-start-enforcement-active','first-pass-desource-applied','source-path-captivity-gate-active','delimiter-transcription-gate-active',...(cx.sourceRegisterOverlap?['source-register-overlap-detected','no-style-costume-law-applied']:[]),...(cx.registerTransform?['register-transform-prompt-lane-exhausted']:[]),...(cx.chatCadence?['chat-cadence-prompt-lane-exhausted']:[]),...(cx.lowSignatureCadence?['low-signature-cadence-lane-exhausted']:[]),...(sRetry?['strict-review-map-transform-lane-exhausted']:[]),...(skipped.size?['strict-review-skip-models-applied']:[])],attempts,rejectedCopy:rejectedCopy.slice(0,12),rejectedCompressed:rejectedCompressed.slice(0,12),requestReceipt:{deterministic,temperature:deterministic?.22:.58,topP:deterministic?.64:.88,antiCompression:true,fastHardPacketLane:!sRetry,registerTransformPromptLane:cx.registerTransform,chatCadencePromptLane:cx.chatCadence,lowSignatureCadenceLane:cx.lowSignatureCadence,sourceRegisterOverlap:cx.sourceRegisterOverlap,firstPassDeSource:cx.firstPassDeSource,functionalCustodyMap:true,orderedCustodyMapForbidden:true,macroStartEnforcement:true,deSourceRecomposition:true,sourcePathCaptivityGate:true,delimiterTranscriptionGate:true,deSourcedCopyRepair:repair?.kind==='copy'&&(cx.sourceRegisterOverlap||cx.lowSignatureCadence||cx.firstPassDeSource),semanticElasticity:true,lexicalElasticityLevel:elasticity,rotationVersion:ROTATION_VERSION,strictReviewMapRetry:sRetry,complexity:cx,modelOrder:models.slice(0,maxAttempts),skippedModels:[...skipped],minLengthRatio:minRatio(sourceText,cx),bounded:true,elapsedMs:Date.now()-startedAt,reviewMapRepair:true,reviewMapRepairVersion:ROTATION_VERSION}})}