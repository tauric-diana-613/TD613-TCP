import { PROMOTED_SAMPLE_LIBRARY } from './diagnostics.js';

const defaults = {
  voiceA: '',
  voiceB: '',
  badge: 'badge.holds',
  mirror_logic: 'off',
  containment: 'on',
  sample_library: PROMOTED_SAMPLE_LIBRARY
};

export default defaults;
