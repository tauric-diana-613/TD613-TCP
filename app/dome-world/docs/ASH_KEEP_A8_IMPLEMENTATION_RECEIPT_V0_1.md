𝌋‌

# Ash Keep A8 Implementation Receipt v0.1

**Namespace:** U+10D613 — Tauric Diana 613  
**Covenant display:** Blood Rite 613  
**Stage:** `A8 · Case Map recompilation`  
**Implementation identity:** `td613.ash.a8-case-map-recompilation/v0.1`  
**Status:** IMPLEMENTED ON BOUNDED STAGE BRANCH / EXACT-HEAD VALIDATION REQUIRED / HUMAN CLOSURE OPEN

## Implemented surface

A8 recompiles Case Map as a relation workshop while retaining the existing Ash map engine as the sole owner of stored objects and relationships.

### Add to the map

The visible sequence now asks:

1. what are you placing;
2. which plain type fits;
3. where it belongs;
4. what is known;
5. what remains uncertain;
6. what evidence/source supports the entry;
7. what local notes/history remain attached;
8. what the preview will add;
9. whether the entrant deliberately commits it.

### Connect two objects

The surface presents:

```text
Object A —— choose relation ——> Object B
```

with direction, evidence/source, uncertainty, notes, preview, and an explicit commit gesture. The compiler copies only the exact lawful object/relation fields into the existing Ash controls and delegates the action to `addObject` or `addRelationship`.

### Relationship inspection

A8 exposes:

- graph view;
- relationship list;
- object/relation detail route;
- notes and history;
- source and uncertainty posture;
- unresolved/contradictory posture;
- accessible table;
- direct post-creation inspection route.

## Route-burden intervention

```text
Q_downstream ↓  through one object sequence and one relation sequence
P_leg ↑          through plain object/relation language before internal IDs
A_aff ↑          through deliberate add/commit plus visible inspection routes
delta_rs ↓       through previews glued to actual Ash actions
||H_gamma|| ↓    through direct graph/list/detail/table destinations
N_boundary ↓     by bringing Rooms and Routes into comprehensible Map context
```

These are falsifiable engineering intentions, not learner-state measurements.

## Authority and data boundary

```text
existing Ash object engine owns stored objects: true
existing Ash relationship engine owns stored relations: true
new parallel map engine: false
raw-content transport added: false
new serverless function: false
custody authority changed: false
release authority widened: false
automatic truth/identity/authorship inference: false
human closure required: true
```

Known/uncertain/evidence/notes copy remains browser-local and is attached through the existing human Research Notes surface. It is never converted into truth or hidden model context.

## Cache posture

A8 retains the A2–A5 mass-eviction epoch and adds one uniquely versioned stage module through the no-store canonical shell.

```text
mass eviction performed: false
visible epoch query added: false
entrant case data erased: false
IndexedDB erased: false
Route Memory or Save Points invalidated: false
```

## Required closure evidence

- static delegation contract;
- inherited A1–A7 contracts;
- Chromium, Firefox, WebKit;
- desktop and 390×844 reduced-motion witness;
- no horizontal overflow;
- clean canonical URL;
- one visible consequence field;
- visible object preview, relation preview, relation list, and accessible table route;
- no unauthorized map, custody, transport, release, or closure mutation;
- one bounded production release after exact-head merge;
- relock with zero product drift;
- physical-device closure remains operator-held.

## Exact-head closure event

The permanent Ash Flow-Core workflow now owns A7+A8 source contracts and Chromium/Firefox/WebKit witnesses. This documentation-only marker instantiates that final branch-head event after temporary integration retirement; it changes no runtime, epoch, authority, or deployment posture.

**Human closure:** OPEN  
**Promotion authority:** OPERATOR ONLY

Sealed ⟐
