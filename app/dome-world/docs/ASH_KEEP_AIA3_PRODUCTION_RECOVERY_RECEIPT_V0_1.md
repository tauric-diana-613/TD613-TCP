# Ash Keep AIA3 Production Recovery Receipt v0.1

Status: `IMPLEMENTED / LOCAL_BROWSER_PROVEN / MERGE_AND_PRODUCTION_HELD`

Source incident: the AIA2 presentation moved the canonical fixed `#launch` ingress into the explanatory card after boot, hid first-use guidance and exact work, retained competing inline workspace-rail ownership, and accidentally laid the membrane out as one horizontal flex row. The result matched the supplied production screenshot: flashing ingress generations followed by a hybrid, obstructed, mostly unusable page.

## Recovery boundaries

- canonical fixed ingress remains the sole first-use membrane;
- AIA3 does not mount as a visible guide until a real case exists;
- Investigation may be an explicit valid default, but the original New Case control performs the action;
- exact main/workspace rail remain inert before case creation and operable afterward;
- `?presentation=legacy` remains an independent exact rollback route;
- finite explanation never performs or certifies an Ash action;
- route switching preserves the case, draft, exact work, and governed lifecycle semantics;
- CacheStorage, same-origin service workers, and the active session pointer receive one epoch eviction;
- IndexedDB cases remain preserved;
- no telemetry, child study, release, transport, automatic closure, or station-authority transfer is authorized.

## Local candidate evidence

The recovered desktop and 390×844 mobile task journey passed in Chromium, Firefox, and WebKit. The journey proves:

1. one fixed canonical ingress;
2. usable profile and New Case controls;
3. case creation through the original Ash action;
4. compact vertical four-task guide after case creation;
5. exact workspace rail and main application availability;
6. local text-file opening without upload;
7. finite deterministic explanation with no governed semantic mutation;
8. all four AIA routes without loss of work;
9. no horizontal overflow or clipped controls;
10. case-preserving cache-transition receipt.

## Deployment posture

- merge to `main`: `HELD UNTIL ALL EXACT-HEAD CONTRACTS PASS`
- Vercel deployment: `NOT YET PERFORMED`
- production browser observation: `NOT YET PERFORMED`
- mass tester eviction: `CODED / ACTIVATES ONCE PER BROWSER AFTER DEPLOYMENT`
- Git auto-deployment: `MUST REMAIN DISABLED OUTSIDE THE BOUNDED RELEASE`

Human closure remains open. Production success may be recorded only after exact-source parity, the AIA3 Chromium/Firefox/WebKit production journey, the one-time cache eviction receipt, IndexedDB preservation, and the mandatory Vercel relock are observed.
