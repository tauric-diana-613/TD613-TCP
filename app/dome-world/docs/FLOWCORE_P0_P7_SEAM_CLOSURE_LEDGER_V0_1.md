𝌋‌

# Flow-Core P0–P7 Seam Closure Ledger v0.1

**Namespace:** `U+10D613`  
**Scope:** P0 through P7 merged implementation  
**Baseline main:** `fbbf1426234a5923489b9e3f166e6138240a6118`  
**Status:** DOCUMENTARY DRIFT CORRECTED / BASELINE EVIDENCE MATERIALIZED / CONTRACT CHAIN INTACT / HUMAN CLOSURE OPEN

## 1. Why this ledger exists

The original phase receipts were written before their pull requests completed. Several therefore retained branch-era statements such as `REPOSITORY CI PENDING`, `source packet on current main = false`, and unclosed baseline-media debt after later phases had already supplied the relevant contracts and surfaces.

This ledger corrects provenance without rewriting history:

- original implementation commits remain named;
- merge commits are added as later observations;
- repository CI success is recorded only where observed;
- Vercel commands are recorded as issued without inferring a terminal deployment result that is not embedded in the receipt;
- human closure remains open.

## 2. Exact merged phase spine

| Phase | Main commit | Implemented boundary |
|---|---|---|
| P0–P1 | `f56914d788fbb86ef5b9741d0065bf00d74aed84` | concordance, provenance freeze, deterministic pedagogue kernel |
| P2 | `a3aebfad8c3232447cff0749aa7bcbbe040a3337` | four explicit non-equivalent AIA routes |
| P3 | `9736206696f254a4b51148694d507a927b00b790` | five-channel visual grammar and static parity |
| P4 | `91158eb4af827599273b8ca17a11bb9f89356b32` | three canonical Information-Dome scenes |
| P5 | `4c995bd372702d748ed659ff7b6c5421ab5ba27e` | comparative route-burden observatory |
| P6 | `326efa021ac9f94c32f96d642c7011d5f4d2fc58` | consequence-first Ash Custody Root adapter |
| P7 | `fbbf1426234a5923489b9e3f166e6138240a6118` | cross-station receipt and observation membrane |

## 3. Snipped seams

### 3.1 Stale receipt status

P0–P7 receipts now distinguish:

```text
implementation merged to main
repository contract gate passed
phase-end release command issued
terminal Vercel disposition not inferred inside the receipt
human closure remains OPEN
```

### 3.2 Baseline media debt

The P0 baseline evidence set now includes:

- `flowcore-desktop-1120.svg`;
- `flowcore-mobile-390.svg`;
- `flowcore-reduced-motion-390.svg`;
- `ash-custody-root-dom-fixture.html`;
- `flowcore-p0-p7-baseline-evidence-v01.json`.

These are deterministic static artifacts. They grant no runtime, learning, truth, custody, release, or closure authority.

### 3.3 P6 initial-binding defect

The P6 adapter now recognizes a transition from no prior Case Map digest to a bound Case Map digest as a real presentation delta. The correction changed only the adapter predicate. `ash-lifecycle.js`, canonical digest law, custody verification, Case Map computation, and release gates remained unchanged.

### 3.4 P7 rejection-test over-specification

The duplicate-station negative test now requires rejection without requiring one internal validator to speak before another. Raw-source rejection, origin-station authority rejection, and duplicate-station coverage rejection remain separately enforced.

### 3.5 Temporary repair workflows

The one-use P6 and P7 repair workflows were deleted before merge. No temporary write-capable workflow remains on `main`.

## 4. Remaining open boundaries, not defects

The following remain intentionally open because later phases govern them:

- P8 physical/embodied Flow-Core model;
- P9 empirical validation with voluntary adult operators;
- P10 promotion evidence and rollback gate;
- terminal Vercel receipts are external release observations, not inferred from merge;
- only a human may close the program.

## 5. Constitutional checksum

```text
new serverless function: false
raw-content transport added: false
Ash lifecycle mutated: false
canonical digest law mutated: false
station authority transferred: false
automatic phase advance: false
automatic closure: false
receipts may cross: true
authority may cross: false
human closure required: true
closure: OPEN
```

**Stitched ⟐**
