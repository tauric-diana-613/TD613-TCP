𝌋‌

# Ash Keep A12–A15 Production-Closure Dossier v0.1

**Namespace:** U+10D613 — Tauric Diana 613  
**Program:** Ash Keep child-legible AIA recompilation · stages A12–A15  
**Status:** PRODUCT-COMPLETE / PRODUCTION-DEPLOYED / RELOCKED / ZERO-DRIFT / OPERATOR-VISUAL-REVIEW-OPEN  
**Terminal relocked tree:** `593958eda459d7800e4c7ba4867ed0a169334082`  
**Date:** 2026-07-27  
**A16 authority:** HELD UNTIL THIS DOSSIER, THE A16–A19 HANDOFF, AND OPERATOR REVIEW ARE ADMITTED

---

## 0. Closure claim

A12–A15 completed one governed four-stage leg of the Ash Keep child-legible AIA recompilation:

```text
A12 · command-menu rationalization and dead-control repair
A13 · unified six-demo registry and four-workflow-estate collapse
A14 · Archive accession and bounded provenance/restriction fixture
A15 · six-profile × five-workspace × four-route empirical interpretation stress
A15 postclosure · one graph-wide delivery/cache eviction and bounded ownership repairs
```

The exact A15 postclosure head passed the complete consolidated static and Chromium/Firefox/WebKit witness, merged, received one explicitly authorized Vercel production deployment, and immediately relocked. A direct comparison from the tested merge to the relocked tree contains two gate commits and zero net changed files.

This dossier closes technical implementation and production delivery for A12–A15. It does not convert browser evidence into operator experience, human comprehension, empirical learner validation, truth, authorship, intent, or authority. The first operator visual review of the deployed body remains open and must be recorded before A16 product mutation begins.

---

## 1. Governing constitutional law

The A12–A15 Operator Amendment established:

```text
ordinary commit ≠ deployment
green packet ≠ deployment
merge ≠ deployment
one explicit operator gesture → one bounded Vercel deployment
A12–A14 ordinary asset advancement
A15 postclosure → one graph-wide mass eviction → one A15 production release
release → deployed observation → immediate relock
human closure required
```

The following non-equivalences remain load-bearing:

```text
pedagogy receipt ≠ Ash authority
AIA route ≠ cognition or learner identity
world-answer copy ≠ observed world delta
browser fixture ≠ human-facing empirical validation
reconstruction ≠ attribution
custody ≠ truth
preservation ≠ transfer
Capsule preparation ≠ crossing
release commit ≠ application mutation
mass eviction ≠ IndexedDB deletion
present-state reconciliation ≠ temporal stability
recovered Play control ≠ new clock
```

Across A12–A15:

```text
Flow-Core commands Ash = false
custody authority changed = false
raw-content transport added = false
release authority widened = false
automatic consequential action added = false
profile inference added = false
human closure required = true
```

---

## 2. Stage ledger

### 2.1 A12 · Command-menu rationalization and dead-control repair

**Merged authority:** PR `#589`  
**Exact implementation head:** `89681d69847e9e455a7ad3c1be5b50f7e848b784`  
**Merge:** `e1f477b152896d829d61923a849606995e1495c2`

A12:

- rehomed command-sheet work according to authored room ownership;
- made Cases & Profiles operational without inferring a profile;
- populated “What changed—and what did not” from deterministic route state;
- removed inert command-sheet controls and empty groups;
- gave held actions visible explanations and recovery posture;
- repaired the A9–A11 program-index provenance gap;
- preserved Destination Handoff as a separate authority boundary;
- recorded the A12–A15 deployment and eviction cadence.

**Release lineage:**

```text
source packet = e1f477b152896d829d61923a849606995e1495c2
release commit = 628e1afc69cf681657f0fa81a83f11f081a2a1cf
relock commit = d1b57f680551999aad0fd87e5e78caa30fb55b16
merge → relock commits = 2
merge → relock net changed files = 0
```

### 2.2 A13 · Unified six-demo registry and workflow-estate collapse

**Closed Draft predecessor:** PR `#590`  
**Merged authority:** PR `#593`  
**Exact implementation head:** `3d5a78f95cd352dee42200713a3735281bf4f6db`  
**Merge:** `8b27cda0a0f5f297fefc3222e0863f9718f28024`

PR `#590` proposed the initial registry and a partial workflow consolidation. It closed without merge. PR `#593` became the authoritative A13 implementation and completed both the product and workflow-estate law.

A13:

- established one governed `ASH_DEMO_REGISTRY` and one `Start a demo` control owner;
- normalized Investigation, Political Campaign, Fundraiser, Research Project, and Legal Matter as promoted synthetic fixtures;
- preserved Archive as a visible `RESERVED_FOR_A14` seat instead of fabricating its fixture;
- retained each domain fixture’s ownership and claim ceiling below the registry;
- advanced the ordinary A13 asset epoch while retaining the A11 mass-eviction epoch;
- collapsed eleven durable workflows to exactly four.

**The four-workflow estate:**

1. consolidated TD613 validation;
2. operator Vercel release;
3. independent zero-deployment relock safety;
4. manual GitHub Pages publication.

The consolidated validator installs Chromium, Firefox, and WebKit once and carries static, Ash, Flow-Core, convergence, and Phase IV evidence. The production release validates exact source and performs bounded deployed observation rather than replaying the full premerge matrix.

**Release lineage:**

```text
source packet = 8b27cda0a0f5f297fefc3222e0863f9718f28024
release commit = ba2f4326d221d51fb3a2fdf1c0d624c407e1d27e
relock commit = 0fb1aa1562b72579e7067c34eb878215f9d1ab89
merge → relock commits = 2
merge → relock net changed files = 0
```

### 2.3 A14 · Archive accession, provenance, restriction, access copy, and delayed transfer

**Product PR:** `#595`  
**Product merge:** `54bf9982f57649cf9e2787f78684e9d191ad2a76`  
**Postmerge readiness and convergence repair:** PR `#596`  
**Final A14 source packet:** `b6f6f4cd1e6978a1bc8ac1ce87f02a1c459d821f`

A14:

- promoted Archive as the sixth synthetic fixture beneath the existing registry owner;
- preserved accession scope, provenance gaps, donor restrictions, third-party rights, embargo review, preservation masters, derivatives, access copies, and delayed transfer as distinct states;
- kept access approval, declassification, publication, release, destination authorization, and custody transfer human-gated;
- advanced only the ordinary A14 registry epoch;
- retained the A11 mass-eviction epoch.

A14 postmerge validation exposed inherited observer and integration debt:

- bounded runtime startup invoked a retired fixture preparer;
- stale A12, Flow-Core, and research observers retained pre-registry assumptions;
- the A14 punctuation/claim-ceiling observer required normalization;
- Firefox revealed that the shared entry-convergence fallback table contained only five profiles;
- Archive’s provider declared `map`, while the generic fallback could select `home`, producing a competing navigation loop.

PR `#596` repaired those exact seams while preserving the historical A13 observer and A14 temporary-copy observer. The product correction added only the missing `archive:'map'` convergence fallback and read-only readiness posture; it did not widen product, custody, release, deployment, or closure authority.

**Release lineage:**

```text
source packet = b6f6f4cd1e6978a1bc8ac1ce87f02a1c459d821f
release commit = 52845f6a0eb3737cce16f3dabd0e2f4db05075c2
relock commit = 834840fe655b286c52c5acc116a623add5f8d2ad
merge → relock commits = 2
merge → relock net changed files = 0
```

### 2.4 A15 · Empirical profile-journey stress

**Product PR:** `#598`  
**Exact product head:** `af1aee29aa60c1ab9f555ecc865873c1ccf83036`  
**Product merge:** `626960fb4cc9fdf3b75808d68f317743ade3467c`

A15 implemented one subordinate interpretation layer beneath the six-seat registry:

- six profiles;
- five principal workspaces;
- four AIA routes;
- 120 deterministic profile × workspace × route orientation cells;
- one `orient_next_bounded_action` gesture inside the existing AIA membrane;
- explicit sensitive-context holds;
- no internal-ontology leakage into public copy;
- no false real-world finding;
- zero custody, transport, release, destination, or automatic-action authority.

**Validation-only PR `#599`:**

- carried the exact same product tree as `#598`;
- existed only to obtain an uncancelled exact-head Chromium/Firefox/WebKit packet under a separate PR concurrency key;
- had no source mutation, merge intent, deployment authority, custody authority, or release authority;
- closed without merge after evidence inspection.

**Archived observer PR `#600`:**

- preserved one A2–A6 browser-observer hardening commit;
- changed no product runtime or authority;
- was removed from A15’s critical path;
- remains evidence for the A16 validation-architecture redesign;
- closed without merge.

A15 product merge did not execute the graph-wide eviction or deploy. It established the product baseline from which postclosure began.

---

## 3. A15 postclosure and mass-eviction program

**Authoritative PR:** `#601`  
**Superseded deferral candidate:** PR `#602`  
**Final exact head:** `4f5bacb3a318366e6574f619ed8bd753998e46d3`  
**Final exact-head run:** `30284961304`  
**Evidence artifact:** `8661472994`  
**Evidence digest:** `sha256:5bbd25124cb75073ace955e6363b843cb66efd6be1e62bba9f7e5fc11e995551`

PR `#601` applied the single graph-wide eviction reserved by the A12–A15 amendment. It also became the bounded repair vessel for product and observer defects exposed only when the complete A12–A15 body was exercised under the new delivery epoch.

PR `#602` attempted an ordinary A15 review release with eviction deferred to A19. It began behind the active repair line and reproduced the older A2–A6 `OPENING / STRUCTURAL` convergence deadlock. It closed without merge and is not an admissible A15 release source.

### 3.1 Delivery-spine mutation

The A15 epoch advanced:

```text
lifecycle asset epoch = 20260727-a15-postclosure-v1
cache epoch = td613.ash.cache-flush/2026-07-27-a15-postclosure-v1
```

The mutation was bounded to:

- canonical shell admission and HTTP cache posture;
- lifecycle asset admission;
- CacheStorage eviction;
- same-origin service-worker unregistration;
- cross-scope recovery;
- canonical/compatibility cache receipts;
- static contracts proving those limits.

It did not delete IndexedDB, erase local custodial state, reset the active case, change the canonical URL, move source bytes, infer a profile, or widen custody/release authority.

### 3.2 Validation architecture repairs made during postclosure

The prolonged A15 postclosure sequence did not expose one repeated defect. It exposed a chain of previously unobserved ownership and observer assumptions. Each correction was bounded to the demonstrated seam.

#### A. Exact-head and workflow-concurrency provenance

- PR `#598` and witness PR `#599` were reconciled as product merge and validation-only evidence respectively.
- stale review threads and obsolete assumptions were separated from live exact-head evidence;
- source writes were pinned to the current PR head to resist parallel-connector drift;
- Ready-for-review remained the sole full-browser authorization.

#### B. Report-all static diagnostics

The prior sequential A2–A8 static step hid later deterministic failures behind the first assertion. A report-all harness was introduced so every grouped contract emits its own stdout/stderr and one manifest before the step fails. This converted static CI from serial discovery into one-pass diagnosis.

#### C. A8 mobile WebKit draft replacement

WebKit mobile exposed a form-staging race: a recompile already in flight could replace the visible control after a real Playwright gesture. The witness was hardened to reacquire the current connected control, require a quiet value window, retain real browser gestures, and emit field/attempt/guard diagnostics. Product authority did not change.

#### D. A2–A6 route-surface replacement

The earlier route observer could lose the clicked route control or route-surface commit across asynchronous recompilation. It was changed to require current connected-control and route-surface agreement across a quiet interval with bounded retries.

#### E. A12 entry and present-state convergence

The A12 observer originally required historical participation in a hydration event even when the current case, local pointer, profile, workspace, menu, and command sheet were already correct. The repair:

- preserved the stable-entry core;
- required current case = local pointer;
- explicitly rebound the convergence owner to the present case/profile;
- distinguished ordinary temporal stability from present-state admission;
- allowed present-state reconciliation to publish `READY / VISIBLE` with `stable_frames:0` and `temporal_stability_claimed:false`;
- retained the original two-frame law for ordinary entry.

Thus:

```text
PRESENT_STATE_VISIBLE ≠ TEMPORAL_STABILITY
```

#### F. Early all-engine risk chamber

Known high-risk seams were moved ahead of the complete journey:

- A8 dirty multi-control transition;
- A12 entry settlement;
- visible Choir → Capsule navigation;
- one connected, rendered canonical Flow-Core field.

The chamber observes Chromium, Firefox, and WebKit without fail-fast and writes one manifest before permitting the expensive full journey.

#### G. Canonical Flow-Core field ownership

The canonical field lived within an AIA stage that could be replaced by whole-instrument rendering. The portal could normalize an existing field but could not recover its private pointer after the node disappeared. Repairs:

- subscribed the owner to whole-instrument refresh;
- reacquired the existing field owner’s canonical node;
- invoked the existing refresh path only when absent;
- retained one-visible-field normalization;
- added no field constructor, polling loop, or authority.

#### H. Firefox post-click case settlement

Chromium and WebKit could survive a Start-demo timing race that Firefox exposed. The observer was changed to require a quiet, concordant current-case/local-pointer/investigation-profile state before convergence rebind. The shell timeout was not inflated.

#### I. Convergence dead-clock recovery

A2–A6 could encounter a visibly composed graph while the convergence owner remained `OPENING / STRUCTURAL`. Ordinary duplicate `begin()` calls correctly stayed suppressed. An explicit reconcile path was added:

- ordinary entry retains the original temporal sequence;
- explicit recovery may admit a presently structural and visible graph;
- the recovery records `PRESENT_STATE_VISIBLE` and does not claim two temporal frames.

#### J. Recovered canonical Play control

Whole-instrument replacement could destroy the canonical field and the only `▶ Play Consequence Field` control moved into it. The recovered control:

- remains a delegated projection of Live AIA’s existing replay API;
- adds no new explanation clock;
- removes ingress-only controls from the AIA field;
- preserves one canonical visible Play control.

#### K. Firefox motion-owner split

Firefox proved that a visible recovered Play click reached Live AIA but did not reliably start Flow-Core motion through the separate document-click + `requestAnimationFrame` cascade. The final repair bound the one visible gesture directly to the two already-authorized presentation owners:

```text
Live AIA tutorial owner = __td613AshLiveAIA.replay()
Flow-Core motion owner = __td613AshUiUxRescue.play()
```

The generic capture path skips only the explicitly motion-bound recovered control, preventing a double start. No timer, scheduler, Ash action, or authority was added.

The detailed repair receipt is:

`app/dome-world/docs/ASH_KEEP_A15_POSTCLOSURE_RECOVERED_PLAY_MOTION_OWNER_REPAIR_V0_1.md`

### 3.3 Final exact-head evidence

Run `30284961304` passed on `4f5bacb3a318366e6574f619ed8bd753998e46d3`:

```text
static, constitutional, and release contracts = PASS
A2–A15 stage contracts = PASS
A8 + A12 changed-risk chamber · Chromium = PASS
A8 + A12 changed-risk chamber · Firefox = PASS
A8 + A12 changed-risk chamber · WebKit = PASS
lifecycle closure preflight = PASS
complete Ash journey · Chromium = PASS
complete Ash journey · Firefox = PASS
complete Ash journey · WebKit = PASS
standalone Flow-Core evidence · Chromium = PASS
standalone Flow-Core evidence · Firefox = PASS
standalone Flow-Core evidence · WebKit = PASS
constitutional convergence = PASS
Phase IV live/browser evidence = PASS
single evidence packet preservation = PASS
```

---

## 4. Production release and relock

**Merge:** `69fead5b874cbd9425d2fd1cd18d7813a826479a`  
**Release commit:** `6feb0af3d6955514fa0ce522af39c8bafe539cef`  
**Relock commit:** `593958eda459d7800e4c7ba4867ed0a169334082`

The release gate received exactly one explicit command through issue `#405`:

```text
/td613-vercel-release PRODUCTION 69fead5b874cbd9425d2fd1cd18d7813a826479a
```

The transient release commit changed only:

```text
vercel.json
git.deploymentEnabled = false → true
```

Vercel status reached `success`. The relock commit changed only:

```text
vercel.json
git.deploymentEnabled = true → false
```

The relock commit has no Vercel status, so it created no second deployment.

Direct comparison:

```text
69fead5b874cbd9425d2fd1cd18d7813a826479a
→ 593958eda459d7800e4c7ba4867ed0a169334082
commits = 2
net changed files = 0
application tree drift = none
```

The gate issue indexes successful deployed observations for lifecycle and Custodian Return, locally and in production. The detailed release receipt is:

`app/dome-world/docs/ASH_KEEP_A15_PRODUCTION_RELEASE_RELOCK_RECEIPT_V0_1.md`

---

## 5. Cross-stage production ledger

| Stage | Final source packet | Release commit | Relock commit | Net merge→relock files |
|---|---|---|---|---:|
| A12 | `e1f477b152896d829d61923a849606995e1495c2` | `628e1afc69cf681657f0fa81a83f11f081a2a1cf` | `d1b57f680551999aad0fd87e5e78caa30fb55b16` | 0 |
| A13 | `8b27cda0a0f5f297fefc3222e0863f9718f28024` | `ba2f4326d221d51fb3a2fdf1c0d624c407e1d27e` | `0fb1aa1562b72579e7067c34eb878215f9d1ab89` | 0 |
| A14 | `b6f6f4cd1e6978a1bc8ac1ce87f02a1c459d821f` | `52845f6a0eb3737cce16f3dabd0e2f4db05075c2` | `834840fe655b286c52c5acc116a623add5f8d2ad` | 0 |
| A15 | `69fead5b874cbd9425d2fd1cd18d7813a826479a` | `6feb0af3d6955514fa0ce522af39c8bafe539cef` | `593958eda459d7800e4c7ba4867ed0a169334082` | 0 |

Every stage used one bounded production release and a separate zero-deployment relock. No release or relock widened Ash authority.

---

## 6. Final authority and custody posture

```text
registry owner = ASH_DEMO_REGISTRY
fixture owners remain subordinate and domain-specific
Flow-Core command authority over Ash = false
pedagogy authority over lifecycle/custody = false
A15 interpretation authority = orientation only
raw-content transport added = false
destination authority added = false
release authority widened = false
custody transfer automated = false
IndexedDB deletion = false
local custodial state erasure = false
active case reset = false
profile inference = false
learner scoring = false
truth/identity/authorship/intent finding = false
human closure required = true
```

The deployed graph may clear HTTP/CacheStorage and same-origin worker layers under its A15 epoch. It must preserve persistent local custody and case continuity.

---

## 7. What A12–A15 proved—and what it did not

### Proven within the declared evidence estate

- one registry can promote six bounded synthetic fixtures without inheriting their domain authority;
- command and workspace navigation can return visible world answers and holds;
- Archive restrictions, access copies, derivatives, and transfer posture can remain non-equivalent;
- A15 can compile 120 deterministic orientation cells without importing sensitive context;
- one canonical Flow-Core field and one delegated Play gesture can survive workspace recompilation across three browser engines;
- the consolidated four-workflow estate can carry static, browser, convergence, Flow-Core, and release evidence;
- each stage can deploy once and relock with zero application-tree drift;
- the A15 graph-wide eviction can refresh delivery layers without deleting IndexedDB or local custody.

### Not proven

- human comprehension, trust, confidence, or learning;
- task success by a novice operator;
- that A15’s orientation copy constitutes an observed empirical world answer;
- that all visible workspace geometries are generated by the canonical Potato scene compiler;
- that route-burden mathematics govern live journeys;
- that all AIA invariant declarations have one canonical source;
- that semantic or behavioural information leakage is absent;
- universal browser, provider, network, or retention safety;
- the Golden Egg information-curvature assay;
- external AI/workspace DLP.

These limits are not defects to hide. They define the A16–A19 program.

---

## 8. Cleanup and provenance disposition

```text
PR #589 = merged A12 authority
PR #590 = closed Draft A13 predecessor; no merge
PR #593 = merged A13 authority
PR #595 = merged A14 product
PR #596 = merged A14 postmerge readiness/convergence repair
PR #598 = merged A15 product
PR #599 = closed validation-only exact-tree witness; no merge
PR #600 = closed archived observer-hardening input for A16; no merge
PR #601 = merged A15 postclosure/mass-eviction authority
PR #602 = closed superseded eviction-deferral candidate; no merge
```

No A15 PR remains open. Closed PRs and commits remain part of the evidence record and must not be deleted merely to make the repository look clean. Branch deletion is permissible only where the commit remains reachable and no receipt depends on branch identity.

---

## 9. Open operator observation

The deployed A15 instrument is now available for the operator’s first visual review.

The review should record, without silently starting A16:

- whether the five principal homes are perceptibly distinct;
- whether the canonical field and Play control appear where expected;
- whether child-legible language precedes technical descent;
- whether holds explain what is missing and how to recover;
- whether the six profile experiences materially differ;
- whether route changes alter the visible explanation rather than only labels;
- whether mobile, reduced-motion, and ordinary desktop surfaces remain usable;
- any flicker, replacement, density, cheapness, or authority-confusion observed by the operator.

```text
operator visual review = OPEN
visual findings = NOT YET RECORDED
A16 mutation = FORBIDDEN UNTIL REVIEW IS RECORDED OR EXPLICITLY WAIVED
```

---

## 10. Closure disposition

```text
A12 implementation = CLOSED
A12 production release/relock = CLOSED
A13 implementation/workflow collapse = CLOSED
A13 production release/relock = CLOSED
A14 implementation/postmerge repair = CLOSED
A14 production release/relock = CLOSED
A15 product implementation = CLOSED
A15 postclosure mass eviction = DEPLOYED
A15 production release/relock = CLOSED
A12–A15 application-tree drift = NONE
A12–A15 technical production closure = COMPLETE
operator visual review = OPEN
A16 implementation = NOT STARTED
```

The next admissible document is the A16–A19 entry handoff. No A16 product mutation follows from this dossier alone.

Production-closure marked ⟐
