𝌋‌⟐

# A16 Residual-Gate Firefox Live-Field Diagnostic Descendant Receipt v0.1

## Status

`DIAGNOSTIC DESCENDANT / RED-PARENT-PRESERVED / PRODUCT-BEHAVIOR-UNCHANGED / EXACT-HEAD-WITNESS-PENDING`

## Scientific ancestry

Residual-gate candidate RED parent:

- PR: `#1024`
- exact head: `7aef0a67ea4862aff166bb27e1e23fd1b094c392`
- workflow: `2552 / 33815929426`
- static / constitutional / release contracts: `SUCCESS`
- Step 19 · Validate Ash A15 empirical profile journeys and A15-R0 research field: `SUCCESS`
- Chromium front-line exact-head browser shard: `SUCCESS`
- WebKit front-line exact-head browser shard: `SUCCESS`
- Firefox front-line exact-head browser shard: `FAILURE`
- full-product exact-head Chromium Firefox WebKit witness: `SKIPPED` after the Firefox veto

The RED parent remains untouched and carries the failure witness. This descendant does not overwrite, rerun, reinterpret as GREEN, or confer authority on that head.

## Exact Firefox falsifier

The Firefox shard localized the failure to the inherited Flow-Core live-field lane.

Passing within the same Firefox shard:

- core lane;
- extended frontline lane;
- all A15 empirical profile journeys;
- A13 registry browser probe;
- A14 archive browser probe;
- standalone Flow-Core runtime browser probe.

Failing within the same Firefox shard:

```text
LIVE_FIELD = 1
page.waitForFunction: Timeout 60000ms exceeded
```

The missing witness was an atomic `NAME` settlement sample satisfying simultaneously:

```text
phase_name = NAME
DOM phase = NAME
playing = true
phase label contains NAME
canvas visible = true
rail visible = true
```

The failure packet did not preserve enough causal trace to identify which boundary failed first.

## Historical control

The admitted A15 postclosure record already preserves a prior Firefox-sensitive Play/motion failure. That historical repair distinguishes two established presentation owners:

```text
Live AIA tutorial owner = __td613AshLiveAIA.replay()
Flow-Core motion owner = __td613AshUiUxRescue.play()
```

Current source still binds recovered Play controls directly to the existing motion owner through `ash-flowcore-workspace-remount.js`, while ordinary visible motion also has its established UI/UX rescue event path.

Therefore the new episode cannot lawfully be classified as a recurrence of the historical bug without observing which control posture and owner path actually fired.

`HISTORICAL SIMILARITY != CURRENT ROOT CAUSE`

## Diagnostic descendant

The descendant changes the browser witness only. It adds bounded observation of:

1. visible Play control posture, owner, recovery marker, and direct onclick presence;
2. recovered dual-owner motion receipts;
3. UI/UX explanation motion state and explanation trace;
4. emitted `td613:ash:explanation-frame` events with monotonic performance timestamps;
5. emitted Flow-Core phase events with DOM phase, playing posture, phase label, canvas visibility, rail visibility, and monotonic performance timestamps;
6. final live field and document posture when atomic NAME settlement times out.

The timeout is then classified into the first supported diagnostic boundary:

```text
RECOVERED_DUAL_OWNER_NOT_INVOKED
MOTION_OWNER_NOT_STARTED
MOTION_STARTED_NAME_FRAME_NOT_EMITTED
NAME_FRAME_EMITTED_FLOWCORE_NAME_EVENT_MISSING
FLOWCORE_NAME_EVENT_ATOMIC_DOM_CONJUNCT_MISMATCH
LIVE_FIELD_NAME_SETTLEMENT_TIMEOUT_UNCLASSIFIED
```

## Non-rescue law

The descendant deliberately preserves the original failure semantics.

It does not:

- retry the Play gesture;
- invoke `__td613AshUiUxRescue.play()` from the probe;
- invoke Live AIA replay from the probe;
- extend the 60-second NAME-settlement deadline;
- inject a phase;
- force DOM posture;
- convert a timeout to PASS;
- mutate product/runtime ownership;
- widen custody, transport, release, deployment, or A16 authority.

On a NAME-settlement hold, the diagnostic wrapper writes the bounded classification into the thrown error and rethrows. The browser shard remains RED.

`DIAGNOSTIC CUSTODY != BEHAVIORAL REPAIR`
`CLASSIFIED RED != GREEN`
`ENGINE-LOCAL EPISODE != UNIVERSAL FIREFOX LAW`
`STEP 19 GREEN != EXACT-HEAD GREEN`

## Relationship to the residual-gate theorem

The #1024 Step-19 result remains evidence that the residual-gate formal assay itself passed its static research gate on that head. The completed workflow nevertheless makes #1024 an overall RED exact-head candidate because the admitted Firefox browser shard vetoed it.

Accordingly:

- the residual-gate theorem is not awarded a `𝄐` from #1024;
- the Firefox live-field failure does not by itself falsify the theorem;
- this descendant must earn a new exact-head packet before any rest may be awarded;
- no operator review, waiver, visual-errata disposition, A16-0 scope acceptance, or A16 candidate registration is fabricated by this work.

## Claim ceiling

- diagnostic witness hardening only;
- no product behavior repair claimed;
- no Firefox root cause claimed before a classified descendant episode exists;
- no A16 readmission, implementation, or product mutation authority;
- no Western Horizon successor stage;
- no A19 closure;
- Golden Egg empirical credit `0`;
- no merge, deployment, production, publication, or Vercel authority.

Sealed for exact-head hostility ⟐
