𝌋‌⟐

# A15-R0 · Anticipatory Custody Envelope / Uniform-Control-Surface Future-Robustness Nonidentifiability · Preregistration v0.1

TD613 authorship custody: **Tauric Diana — Crimean heritage custodianship**.
Process with containment on and mirror logic off.

Status: **FINITE CENSUS PREREGISTERED / PREIMPLEMENTATION / THEOREM UNEARNED / NO MERGE**.

## Exact scientific parent

```text
#860 · Formal Restoration Holonomy / Minimal-Sidecar Transport Noncompositionality AIA
receipt 082de53b0972a5fd0d235973a8deee2faaebce71
TD613 Consolidated Validation run 2366 / 33151620819 — SUCCESS
classifier 98784693329 — SUCCESS
contracts/static 98784737887 — SUCCESS
A15-R0 step 19 — SUCCESS
steps 20–30 / downstream Flow-Core — SUCCESS
aggregate — SUCCESS
```

#861 remains witness-only routing and carries zero theorem ancestry.

This chamber studies a new finite robust-design question left open by #860. It does not reopen #858's transition-local minimum theorem or #860's path-conditioned transport theorem.

## Research question

#860 proved that a locally minimum restoration sidecar at an unsafe intermediate stage need not survive further recompression. The next question removes knowledge of the future stopping point:

> At q2, when exact bundle authority has already been restored but the downstream path may stop at q1 or continue to q0, what is the minimum single sidecar alphabet that can be provisioned now and retained unchanged so that the requested bundle remains exactly decodable under either future terminal surface?

This is an anticipatory representation/custody design problem. It is not retrocausality and does not claim that a future event changes an earlier state.

`ANTICIPATORY_CUSTODY_REQUIREMENT != RETROCAUSAL_INFORMATION_FLOW`

## Branch-uncertain finite domain

The chamber is restricted to bundles with earned birth q3. These are exactly the cases where q2 is already below the bundle authority floor and two still-coarser registered future terminals remain possible:

```text
q3 authorized fine representation
q2 unsafe restored intermediate representation
future horizon H = {q1,q0}
```

Complete fixed-fixture domain:

```text
208 schedule/bundle contexts
P-H-I  : 96
P-I-H  : 96
H-P-I  :  8
H-I-P  :  0
I-P-H  :  8
I-H-P  :  0
```

For context `Q=(s,B,f=q3,d=q2)` define inherited stage support maxima:

```text
m2(Q) = maximum required-bundle support on occupied q2 fibres
m1(Q) = maximum required-bundle support on occupied q1 fibres
m0(Q) = maximum required-bundle support on occupied q0 fibres
```

#860 already gives

```text
m2 <= m1 <= m0.
```

## Candidate anticipatory-custody envelope law

Let `r_H(Q)` be the minimum finite alphabet cardinality of one sidecar produced from the already-authorized fine representation, retained unchanged after q2, and required to decode exact `B` together with whichever future terminal record in `H={q1,q0}` survives.

Candidate exact law:

```text
r_H(Q) = m0(Q).
```

### Candidate lower bound

Because q0 is an admissible future terminal and has one surviving global registered record, any future-robust sidecar must distinguish all `m0(Q)` required bundle values that cohabit that q0 fibre. Any alphabet smaller than `m0(Q)` therefore produces an explicit same-q0-record collision.

### Candidate sufficiency

Assign each distinct required bundle value in the q0 fibre an injective label from an alphabet of exactly `m0(Q)` symbols. The sidecar is derived only from the already-authorized fine bundle value. Since the label alone separates required bundle values at q0, `(q1,label)` and `(q0,label)` both decode the requested bundle exactly. No recoding or augmentation is needed after q2.

This is a finite existence/minimality result for the declared horizon. It does not claim unique coding or minimum bit length.

## Major preregistered control-surface collapse

The complete 208-context census has the following exact local q2 minimum:

```text
m2(Q) = 5  for all 208 contexts.
```

Yet the candidate future-robust minimum `r_H(Q)=m0(Q)` has twelve distinct values:

```text
future-robust alphabet : context count
5    :  4
10   :  4
15   :  8
25   :  4
30   : 16
50   :  4
75   :  8
125  : 18
150  : 16
250  : 18
375  : 36
750  : 72
TOTAL:208
```

Thus:

```text
4   contexts require no anticipatory cardinality expansion
204 contexts require a larger sidecar than the local q2 minimum
```

or exactly:

```text
3.125%  local-minimum plateau
98.076923...% anticipatory expansion
```

The fixed fixture therefore contains a uniform visible local sidecar cardinality of five that corresponds to twelve distinct future-robust custody minima spanning a factor of 150.

Candidate scars:

```text
UNIFORM_LOCAL_MINIMUM != UNIFORM_FUTURE_ROBUST_OBLIGATION
LOCAL_SIDECAR_CARDINALITY != FUTURE_ROBUST_CUSTODY_IDENTITY
CURRENT_CONTROL_SURFACE_COST != HORIZON_ROBUST_CUSTODY_COST
LOCAL_CARDINALITY_EQUALITY != FUTURE_CUSTODY_EQUIVALENCE
```

Do not translate the 150-fold alphabet-cardinality ratio into bits, entropy, Shannon capacity, mutual information, or probability.

## Preregistered robustness-premium ratio spectrum

Since all 208 local minima equal five:

```text
r_H / m2 : context count
1   :  4
2   :  4
3   :  8
5   :  4
6   : 16
10  :  4
15  :  8
25  : 18
30  : 16
50  : 18
75  : 36
150 : 72
TOTAL:208
```

Maximum cardinality difference:

```text
750 - 5 = 745
```

Maximum cardinality ratio:

```text
750 / 5 = 150
```

`CARDINALITY_RATIO != BIT_RATIO`
`CARDINALITY_RATIO != INFORMATION_GAIN_RATIO`

## Preregistered three-stage transport signatures

Across the same 208 contexts, classify the local transport profile by whether the minimum support maximum stays flat or expands on each downstream edge:

```text
q2 -> q1       q1 -> q0       count
FLAT           FLAT              4
FLAT           EXPAND           20
EXPAND         FLAT              2
EXPAND         EXPAND          182
TOTAL                           208
```

Representative controls:

```text
FLAT -> FLAT
P-H-I / {X3}
5 -> 5 -> 5

FLAT -> EXPAND
P-H-I / {FIRST_STRATUM,X3}
5 -> 5 -> 15

EXPAND -> FLAT
P-H-I / {X2,X3}
5 -> 25 -> 25

EXPAND -> EXPAND
P-H-I / {FULL_STATE}
5 -> 25 -> 125
```

The twenty `FLAT -> EXPAND` contexts are exact counterexamples to treating immediate one-step transport success as evidence of full-horizon robustness:

`ONE_STEP_TRANSPORT_SUCCESS != HORIZON_ROBUSTNESS`.

The two `EXPAND -> FLAT` contexts establish the complementary refusal:

`EARLY_TRANSPORT_RUPTURE != NECESSARILY_CONTINUED_CUSTODY_EXPANSION`.

## Preregistered complete `(m2,m1,m0)` spectrum

```text
(5,5,5)     :  4
(5,5,10)    :  4
(5,5,15)    :  8
(5,5,25)    :  2
(5,5,50)    :  2
(5,5,75)    :  4
(5,10,30)   : 16
(5,10,150)  :  8
(5,25,25)   :  2
(5,25,50)   :  2
(5,25,75)   :  4
(5,25,125)  : 18
(5,25,250)  : 18
(5,25,375)  : 36
(5,50,150)  :  8
(5,50,750)  : 72
TOTAL         208
```

## Preregistered support burden

A canonical implementation may deduplicate repeated `(schedule,bundle,stage)` profiles across the 208 contexts:

```text
208 q3-birth schedule/bundle contexts
3 stages per context: q0,q1,q2
624 unique stage profiles
6,256 occupied-fibre support evaluations
208 horizon-envelope lower-bound witnesses
26,000 target-indexed future-robust decode checks (=208*125)
208 three-stage signature checks
```

The independent hostile must rebuild this domain separately from inherited claim births, phasonic operators, global stage fibres, and recursive claim bundles. It may compare the final public certificate only after completing the independent derivation.

Paired implementation + hostile expected burden:

```text
12,512 occupied-fibre support evaluations
416 horizon-envelope lower-bound witnesses
52,000 target-indexed future-robust decode checks
416 three-stage signature checks
```

Do not sum unlike operation classes into one synthetic total.

## Hostile requirements

The future hostile must attack at least:

1. all 208 independently reconstructed q3-birth contexts;
2. the claim that every local q2 minimum equals five;
3. all twelve future-robust alphabet sizes and their exact counts;
4. all 208 lower bounds using an explicit q0 same-record collision under an alphabet of size `m0-1`;
5. all 26,000 target-indexed exact decodes for the `m0`-label construction;
6. all sixteen `(m2,m1,m0)` support triples;
7. all four transport-signature classes and their counts 4/20/2/182;
8. a `FLAT -> EXPAND` witness to reject one-step-success-as-horizon-safety;
9. an `EXPAND -> FLAT` witness to reject automatic continued expansion;
10. the false claim that equal local cardinality identifies future robust cost;
11. the false claim that future-path possibility retroactively changes q2 source state or prior authority;
12. minimum-bit/Shannon/mutual-information language;
13. cryptographic/key/authentication equivalence;
14. full-state reconstruction unless FULL_STATE is actually in the requested bundle;
15. Ash leakage of complete context/fibre/label tables;
16. collapse into #860 single-known-path jurisdiction or #858 single-transition minimum jurisdiction.

## Jurisdiction membrane

```text
#858 TRANSITION_LOCAL_MINIMUM_RESTORATION
!=
FUTURE_HORIZON_ROBUST_MINIMUM

#860 PATH_CONDITIONED_MINIMUM_TRANSPORT
!=
BRANCH_UNCERTAIN_SINGLE_SIDECAR_ENVELOPE

PRESENT_LOCAL_COST
!=
ROBUST_COST_OVER_DECLARED_FUTURE_TERMINAL_SET
```

## Candidate consequential laws

Only after implementation, independent hostile, hardening/freeze, exact-parent audit, sterile witness routing, and exact-head TD613 Consolidated Validation SUCCESS may the chamber claim:

`IN_THE_FIXED_S3_AIA_FIXTURE_EVERY_ONE_OF_THE_208_Q3_BIRTH_Q2_RESTORATION_CONTEXTS_HAS_THE_SAME_TRANSITION_LOCAL_MINIMUM_SIDECAR_CARDINALITY_FIVE_YET_THE_MINIMUM_SINGLE_SIDECAR_REQUIRED_TO_REMAIN_EXACT_UNDER_THE_BRANCH_UNCERTAIN_FUTURE_HORIZON_Q1_OR_Q0_HAS_TWELVE_DISTINCT_CARDINALITIES_FROM_FIVE_THROUGH_SEVEN_HUNDRED_FIFTY`

`FOR_THE_DECLARED_Q1_Q0_FUTURE_HORIZON_THE_EXACT_MINIMUM_FUTURE_ROBUST_SIDECAR_CARDINALITY_EQUALS_THE_Q0_REQUIRED_BUNDLE_SUPPORT_MAXIMUM_WITH_204_OF_208_CONTEXTS_REQUIRING_ANTICIPATORY_EXPANSION_BEYOND_THE_UNIFORM_LOCAL_MINIMUM_FIVE_AND_SEVENTY_TWO_CONTEXTS_REQUIRING_A_150_FOLD_CARDINALITY_EXPANSION_TO_750`

`THE_FIXED_BRANCH_UNCERTAIN_ATLAS_CONTAINS_TWENTY_CONTEXTS_WHERE_Q2_TO_Q1_LOCAL_MINIMUM_TRANSPORT_SUCCEEDS_BUT_Q1_TO_Q0_LATER_RUPTURES_ESTABLISHING_THAT_ONE_STEP_TRANSPORT_SUCCESS_DOES_NOT_IDENTIFY_HORIZON_ROBUSTNESS`

Do not call these earned from preregistration.

## Mandatory scars

```text
UNIFORM_LOCAL_MINIMUM != UNIFORM_FUTURE_ROBUST_OBLIGATION
LOCAL_SIDECAR_CARDINALITY != FUTURE_ROBUST_CUSTODY_IDENTITY
CURRENT_CONTROL_SURFACE_COST != HORIZON_ROBUST_CUSTODY_COST
LOCAL_CARDINALITY_EQUALITY != FUTURE_CUSTODY_EQUIVALENCE
ONE_STEP_TRANSPORT_SUCCESS != HORIZON_ROBUSTNESS
EARLY_TRANSPORT_RUPTURE != NECESSARILY_CONTINUED_CUSTODY_EXPANSION
ANTICIPATORY_CUSTODY_REQUIREMENT != RETROCAUSAL_INFORMATION_FLOW
DECLARED_FUTURE_PATH_SET != FUTURE_EVENT_OCCURRENCE
FUTURE_ROBUST_OVERPROVISIONING != NEW_SOURCE_INFORMATION
ROBUST_SIDECAR != SOURCE_STATE_MUTATION
CARDINALITY_RATIO != BIT_RATIO
CARDINALITY_RATIO != INFORMATION_GAIN_RATIO
UNIFORM_CARDINALITY_SURFACE != ZERO_MUTUAL_INFORMATION_THEOREM
FUTURE_HORIZON_MINIMUM != DATA_RETENTION_POLICY
FINITE_208_CONTEXT_CENSUS != ASYMPTOTIC_INFORMATION_THEOREM
WITNESS_ROUTING != SCIENTIFIC_ANCESTRY
```

No merge, deployment, publication, production, release, Vercel, source-state mutation, autonomous retention/deletion, retrocausality, Shannon/entropy/mutual-information theorem, cryptographic theorem, authentication theorem, universal robust-control theorem, universal coding theorem, Proto-Loom/A16, or #788 promotion authority follows. #718 remains alive.

**FINITE ANTICIPATORY-CUSTODY CENSUS FROZEN BEFORE IMPLEMENTATION. THEOREM UNEARNED.**

Sealed ⟐
