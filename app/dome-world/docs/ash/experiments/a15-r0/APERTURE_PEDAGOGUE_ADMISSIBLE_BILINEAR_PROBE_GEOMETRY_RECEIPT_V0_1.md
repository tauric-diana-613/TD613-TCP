𝌋

# Aperture × Pedagogue Admissible Bilinear Probe Geometry Receipt v0.1

**Schema:** `td613.a15-r0.aperture-pedagogue-admissible-bilinear-probe-geometry-receipt/v0.1`  
**PR:** #705  
**Scientific parent:** #704 receipt head `8ed7dbdca905beb3f7c9138b499da54809834fc5`  
**Status:** EVIDENTIARY CLOSURE / DRAFT STACKED / NO PROMOTION AUTHORITY

## Witness coordinate

```text
run_number = 2059
run_id = 32672157184
witnessed_head = 7766a3cd1b951b315d00c76d8d8aad87bf229f46
static_job_id = 97274527852
conclusion = success
A15-R0_static_execution = success
browser_shards = skipped
full_repository_validation = skipped
self_hosted_calibration = skipped
literal_stdout = NOT_RETRIEVED_BY_CONNECTOR
```

#705 was restored to its true #704 stacked parent before this receipt was authored.

```text
receipt_document_commit != witnessed_head
```

The later documentation timestamp does not rewrite witness time.

## Bounded result

The exact-head hostile contract survived the existing A15-R0 static gate with the preregistered action-space distinction intact.

Inherited incomplete response system:

```text
rank(A3) = 3
null direction n = [1,-1,-1,1]
```

The unrestricted trace functional

```text
h_trace = [1,0,0,1]
```

has nonzero nullspace sensitivity and would raise the augmented linear response system to rank 4. But its reshaped coefficient matrix is the rank-2 identity, so it is not realizable as one declared scalar bilinear `rTx` probe.

By contrast:

```text
Q_ADMISSIBLE_GOOD
r=[1,0], x=[1,0]
→ rank-one coefficient geometry
→ contracts current nullspace

Q_ADMISSIBLE_BLIND
r=[1,1], x=[0,1]
→ rank-one coefficient geometry
→ remains blind to current nullspace
```

The assay-local unrestricted selector chooses the inadmissible trace direction. The corrected selector filters the action set first and chooses the informative admissible probe.

Coordinate-equivalent transformation of the legal probe preserves one-bilinear realizability.

## Earned anti-equivalences

```text
distinguishable by arbitrary linear functional on vec(T)
!=
distinguishable by one admissible scalar bilinear probe

highest unrestricted information score
!=
highest realizable one-action information score

admissible action
!=
informative action

action-count cost
!=
permission to synthesize a multi-action functional at one-action cost
```

## Canonical bounded claim

```text
AN_UNRESTRICTED_LINEAR_FUNCTIONAL_ON_VEC_T_CAN_RESOLVE_THE_CURRENT_OPERATOR_NULLSPACE_WHILE_REMAINING_INADMISSIBLE_AS_ANY_SINGLE_DECLARED_BILINEAR_INPUT_READOUT_PROBE_AND_MATCHED_ACTION_COUNT_ADMISSIBLE_BILINEAR_PROBES_CAN_DIFFER_IN_NULLSPACE_CONTRACTION_IN_THE_AUTHORED_FINITE_FIXTURE
```

## Claim ceiling

Still false / unauthorized:

```text
general_optimal_experiment_design_earned = false
canonical_operator_tomography_promotion_authority = false
operator_tomography_general_theorem_earned = false
blind_tomography_earned = false
physical_tomography_earned = false
path_category_earned = false
path_transport_earned = false
holonomy_earned = false
curvature_earned = false
Berry_structure_earned = false
quantum_behavior_earned = false
Proto_Loom_earned = false
A16_reopened = false
live_Ash_mutation = false
merge_authority = false
production_authority = false
Vercel_authority = false
```

## Next bounded learning action

The trace hostile is inadmissible as one bilinear action but decomposes exactly as

```text
trace(T) = e1^T T e1 + e2^T T e2.
```

A separately preregistered chamber may therefore ask whether an arbitrary coefficient matrix of rank `k` can be represented, in the authored finite 2x2 fixture, as a cost-`k` sum of admissible rank-one bilinear probes, while distinguishing simultaneous/composite functional evaluation from a sequential program whose intermediate actions may change the observation operator.

The latter transition-sensitive interpretation is explicitly **not** earned by this receipt.

𝌋

Sealed ⟐
