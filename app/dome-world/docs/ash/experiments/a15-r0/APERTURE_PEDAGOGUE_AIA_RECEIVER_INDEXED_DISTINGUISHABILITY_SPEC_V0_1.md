𝌋‌⟐

󐘓 U+10D613

# A15-R0 · AIA Receiver-Indexed Distinguishability and Custody-Invariant Projection · v0.1

Status: **PREREGISTERED / SCIENCE CHAMBER / NO LIVE-RUNTIME CLAIM**

## Exact witnessed parent

```text
#796 receipt
7639d5b15edc57aa3d76b8669aeefed6d86c12d6
```

The parent already earns a fail-closed Dome-World constitutional source witness over bounded source signature χ.

## Architectural question

Can the same constitutionally admitted source produce receiver-dependent information surfaces while preserving exact custody provenance and zero authority widening?

This chamber defines a bounded two-receiver architecture:

```text
R_L = HOLONOMY_LOOM_TECHNICAL
R_A = ASH_KEEP_CHILD
```

For an admitted source receipt S:

```text
P_L(S) = technical Loom receiver payload + χ(S)
P_A(S) = constitutionally faithful Ash child payload + χ(S)
```

Receiver identity is first-class and changes the visible payload.

## Constitutional invariant

For every admitted source S and every admitted receiver r:

```text
custody(P_r(S)) = χ(S)
authority(P_r(S)) = all false
```

The custody witness is receiver-invariant. The payload need not be.

## Finite anisotropy fixture

Start with the canonical #796-admitted apparatus receipt S0. Construct four additional receipts S1..S4 by changing exactly one technical payload coordinate:

```text
S1: ROUTE.observable_kind
S2: TEMPORAL.observable_kind
S3: FACE_HOLONOMY.observable_kind
S4: OBSERVABILITY_ECOLOGY.observable_kind
```

No lineage, fixture, owner, research/runtime, or authority coordinate changes.

Therefore preregistered expectations are:

```text
χ(S0)=χ(S1)=...=χ(S4)
all five source audits accepted
all five source authority vectors identical and zero
```

The inherited Ash child compiler does not expose `observable_kind`, while the technical Loom receiver does.

Preregistered partition verdict:

```text
receiver-induced partition under P_A:
  one block containing {S0,S1,S2,S3,S4}

receiver-induced partition under P_L:
  five singleton blocks
```

Thus the Loom partition strictly refines the Ash partition on this finite family.

## Directional non-reconstructibility

Because

```text
P_A(S0)=P_A(S1)
```

but

```text
P_L(S0) != P_L(S1),
```

no deterministic function F can satisfy

```text
F(P_A(Si)) = P_L(Si)
```

for every member of the finite family.

This is a finite exact nonidentifiability theorem, not a computational-complexity claim.

## Custody/payload split

Both receiver projections carry exact χ, so constitutional source custody is recoverable from either receiver.

But technical `observable_kind` payload is recoverable only from the Loom receiver on this fixture.

Therefore candidate architectural law:

```text
CUSTODY_RECOVERABILITY_CAN_BE_RECEIVER_INVARIANT
WHILE
PAYLOAD_RECOVERABILITY_IS_RECEIVER_INDEXED_AND_DIRECTIONALLY_NONINVERTIBLE
```

This is the bounded meaning of anisotropic information architecture in this chamber.

## Required scars

```text
SAME_SOURCE_CUSTODY != SAME_RECEIVER_PAYLOAD
SAME_CONSTITUTIONAL_WITNESS != SAME_VISIBLE_INFORMATION
RECEIVER_INDEXED_PROJECTION != AUTHORITY_WIDENING
ASH_COLLAPSE != LOOM_COLLAPSE
CUSTODY_RECOVERABILITY != FULL_SOURCE_RECONSTRUCTIBILITY
LOSSY_CHILD_PROJECTION != LOST_PROVENANCE
DIRECTIONAL_NONIDENTIFIABILITY != SEMANTIC_INCOMMENSURABILITY
STRICT_RECEIVER_PARTITION_REFINEMENT != UNIVERSAL_RECEIVER_ORDER
```

## Mandatory hostiles

```text
change χ and call it payload anisotropy                         REJECT
change source authority and call it receiver anisotropy         REJECT
strip χ from either receiver projection                         REJECT
claim Ash reconstructs Loom technical payload                   REJECT
claim deterministic Ash->Loom inverse on finite family          REJECT
claim Loom receiver grants tomography inverse authority         REJECT
claim receiver identity changes scientific truth                REJECT
claim same χ implies same source receipt                         REJECT
claim finite two-receiver result establishes universal AIA       REJECT
undefined receiver                                               REJECT
runtime binding                                                  REJECT
#788 scientific bridge promotion                                 REJECT
```

## Candidate earned classifications

Only if exact-head constitutional witness passes:

```text
THE_BOUNDED_DOME_WORLD_RECEIVER_FAMILY_EXHIBITS_RECEIVER_INDEXED_INFORMATION_ANISOTROPY_WITH_LOOM_STRICTLY_REFINING_ASH_ON_THE_PREREGISTERED_FIVE_SOURCE_TECHNICAL_PAYLOAD_FAMILY
NO_DETERMINISTIC_ASH_TO_LOOM_RECONSTRUCTION_EXISTS_ON_THE_PREREGISTERED_FAMILY_BECAUSE_ONE_ASH_EQUIVALENCE_CLASS_CONTAINS_MULTIPLE_DISTINCT_LOOM_PAYLOADS
THE_CONSTITUTIONAL_SOURCE_SIGNATURE_IS_RECOVERABLE_EXACTLY_FROM_BOTH_RECEIVERS_WHILE_THE_FULL_TECHNICAL_PAYLOAD_IS_NOT
CUSTODY_RECOVERABILITY_AND_PAYLOAD_RECOVERABILITY_ARE_SEPARATE_ARCHITECTURAL_COORDINATES
A_BOUNDED_RECEIVER_INDEXED_ANISOTROPIC_INFORMATION_ARCHITECTURE_IS_IMPLEMENTABLE_WITH_ZERO_AUTHORITY_WIDENING
```

## Hard ceilings

```text
bounded AIA != universal information architecture theorem
receiver-indexed payload != observer-dependent physical reality
partition refinement != semantic hierarchy
technical Loom receiver != live Holonomy Loom runtime
Ash receiver != live child/user binding
custody witness != scientific truth
non-reconstructibility != encryption/security proof
no inverse on finite family != impossible inference under all priors
AIA != physical anisotropic medium
```

No merge, deployment, Vercel, publication, release, Proto-Loom/A16, live runtime, semantic equivalence, encoder authority, physical holonomy, or continuum tomography follows.

#718 remains alive.

## Stop rule

A 𝄐 is earned only if an exact-head TD613 Consolidated Validation run executes the rolling A15-R0 sentinel and this chamber's hostile assay successfully.

Sealed ⟐
