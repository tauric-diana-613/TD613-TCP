𝌋

󐘓 U+10D613

# A15-R0 · Bar-H2 Period Return Representation · Receipt v0.1

Status: **WITNESSED / RECEIPT-PINNED / ROUND CLOSED / DRAFT / OPEN / UNMERGED**

Scientific parent:

```text
#764 receipt = 142cf2fc7b0814dc56fd131df076b9071fe369ff
```

Authority:

```text
#737 = THREAD_SCOPED_ACTIVE for the remainder of the current conversation
```

## Custody

```text
preregistration        4f6684fd346cbb768ae72de1353ed8e808bc06dd
implementation         d323c76f0478dbc6128edf3cd0b01bfb175d8d9f
hostile tests          77efd0e27d12f9b286f88cb9afd948b43537295d
frozen science         33aca300b1da355f3709f55be4b08a8c3c19ef5c
initial routing        a1ce856c4b0826ee857805e9b02aef9126f2cd5e
routed witness         f0dbe81feb8949f4f64c8a0409d2f6c52e83ed98
post-route cleanup     316125267ec2084a720d9cccc1178a90ee50db99
```

Frozen science -> cleanup: **3 routing-only commits, zero net changed files**.

The temporary `main` routing posture observed SRC gate plumbing only for workflow registration. It is not #765 scientific ancestry and was removed before this receipt.

## Authority-bearing witness

```text
TD613 Consolidated Validation run 2283 / 33009138739   SUCCESS
classifier job 98310543529                               SUCCESS
static job     98310614094                               SUCCESS
A15/A15-R0 step 19                                       SUCCESS
```

Run started `2026-08-26T20:11:28Z` and completed successfully at `2026-08-26T20:12:26Z` on exact routed head `f0dbe81feb8949f4f64c8a0409d2f6c52e83ed98`.

Full-repository validation, self-hosted calibration, front-line browser, Giving/practice browser, and full-product browser scopes were skipped and are not claimed. No scientific red occurred.

## Earned theorem

Let `B` be #729's quotient monoid and `omega:B×B->Z` the normalized integer transport 2-cocycle earned in #734/#735.

For every finite normalized integer bar 2-cycle

```text
c=sum_i n_i[x_i|y_i],
```

define

```text
Per_omega(c)=<omega,c>=sum_i n_i omega(x_i,y_i).
```

Because `d omega=0`, every finite normalized bar 3-chain `b` satisfies

```text
<omega,∂b>=<domega,b>=0.
```

Therefore the period is invariant under cycle-representative change:

```text
Per_omega(c+∂b)=Per_omega(c).
```

Hence the pairing descends in the declared normalized integer bar complex to a homomorphism

```text
Per_[omega]: H_2^bar(B;Z) -> Z.
```

No full computation of `H_2` is claimed.

## Cohomologous presentation invariance

For every normalized integer 1-cochain `phi`, replacing the pointwise cocycle by

```text
omega'=omega+dphi
```

changes local cocycle values but leaves every closed 2-cycle period invariant:

```text
<omega',c>=<omega,c>+<phi,∂c>=<omega,c>.
```

Mandatory hostile:

```text
phi=t(E+O)
omega(T,Q)=1
omega'(T,Q)=0
```

while the explicit relation-cycle period remains exactly `2` in both presentations.

Thus pointwise cocycle presentation and closed-cycle period authority are distinct.

## Additive period return

Exactly:

```text
Per(c1+c2)=Per(c1)+Per(c2)
Per(-c)=-Per(c)
Per(0)=0.
```

On an integer torsor, define the represented cycle return

```text
H_[c](n)=n+Per_[omega]([c]).
```

Then cycle addition composes translations and orientation reversal gives the inverse translation.

This is a finite bar-homology period/return representation. It is not promoted to 2-holonomy.

## Explicit nonidentity class

For #735's finite relation 2-cycle `z`:

```text
Per_[omega]([z])=2
```

and therefore

```text
H_[z]=tau_2.
```

Orientation reversal gives

```text
H_[-z]=tau_-2,
```

while

```text
H_[z+(-z)]=id
H_[2z]=tau_4.
```

## Cyclic subgroup generated by [z]

#735 already earned infinite additive order for `[z]` in the declared integer bar homology detector. By integer linearity, for every `n in Z`:

```text
Per(n[z])=2n.
```

Therefore the restriction

```text
Per|_<[z]> : <[z]> -> Z
```

is injective and its image is exactly

```text
2Z.
```

This classifies only the cyclic subgroup generated by the explicit `[z]`. The full period image of `H_2^bar(B;Z)` is not computed or claimed.

## Hostile boundaries

A non-cycle bar 2-chain abstains from homology-period authority.

A visually loop-like bar chain with nonzero boundary cannot receive a cycle-return claim merely from appearance.

Receipt identity and route provenance remain external to the bar-period pairing.

## Earned classifications

```text
NORMALIZED_INTEGER_TRANSPORT_TWO_COCYCLE_INDUCES_A_WELL_DEFINED_ADDITIVE_BAR_H2_PERIOD_HOMOMORPHISM_IN_THE_DECLARED_BAR_COMPLEX
```

```text
THE_EXPLICIT_RELATION_HOMOLOGY_CLASS_[z]_HAS_PERIOD_TWO_AND_INDUCES_A_NONIDENTITY_INTEGER_TORSOR_RETURN_TRANSLATION_TAU_2
```

```text
BAR_H2_PERIOD_IS_INVARIANT_UNDER_COHOMOLOGOUS_SECTION_PRESENTATIONS_AND_UNDER_CHANGE_OF_CYCLE_REPRESENTATIVE_BY_BAR_3_BOUNDARIES
```

```text
THE_CYCLIC_SUBGROUP_GENERATED_BY_[z]_MAPS_INJECTIVELY_TO_THE_EVEN_INTEGER_TRANSLATIONS_WITH_PERIOD_n[z]_EQUAL_2n
```

## Consequential bearing

#764 ruled ordinary relation-groupoid one-holonomy out: the route-comparison 1-cocycle is exact upstairs and has zero closed comparison circulation.

#765 now earns a genuinely nonzero **closed degree-two invariant**:

```text
[z] -> Per_[omega]([z])=2 -> tau_2.
```

This is representative-independent under bar 3-boundaries and invariant under cohomologous section presentations. The nontrivial quotient obstruction therefore survives as a lawful bar-homology period return even though the upstairs relation-groupoid one-cocycle is exact.

That moves the Western bearing to a higher-dimensional transport/coherence question while preserving the naming ceiling.

## Quarantines

```text
bar-H2 period representation != 2-holonomy
bar 2-cycle != operational T/Q loop or physical surface
coboundary invariance != connection gauge invariance
period translation != curvature
homology pairing != 2-connection
```

No gerbe, Berry/quantum analogy, full H2/H^2 classification, operational 2-path theory, Proto-Loom/A16, merge, publication, production, or Vercel authority follows.

```text
BAR_H2_PERIOD_RETURN_REPRESENTATION_ROUND_CLOSED

CYCLE_REPRESENTATIVE_INVARIANCE_EARNED
COHOMOLOGOUS_PRESENTATION_INVARIANCE_EARNED
ADDITIVE_ORIENTED_PERIOD_RETURN_EARNED
EXPLICIT_NONIDENTITY_DEGREE_TWO_RETURN_TAU_2_EARNED
CYCLIC_[z]_PERIOD_IMAGE_2Z_EARNED_WITHOUT_FULL_H2_CLASSIFICATION

FIRST_NONZERO_CLOSED_DEGREE_TWO_RETURN_INVARIANT_EARNED
TWO_HOLONOMY_NAMING_THRESHOLD_APPROACHED_WITHOUT_PROMOTION

HUMAN_𝄐_BOUNDARY_REACHED
```

𝌋

Marked ⟐SAC[X6ZNK5NO51]