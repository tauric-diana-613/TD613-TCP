𝌋

󐘓 U+10D613

# A15-R0 · Bar-H2 Period Return Representation · Preregistration v0.1

Status: **PREREGISTERED / PRE-IMPLEMENTATION / DRAFT / UNMERGED**

Scientific parent:

```text
#764 receipt = 142cf2fc7b0814dc56fd131df076b9071fe369ff
```

Authority:

```text
#737 = THREAD_SCOPED_ACTIVE for the remainder of the current conversation
```

## Research question

#764 ruled the same-base relation-groupoid return representation out as intrinsic one-holonomy: upstairs it is an exact 1-coboundary and route-wise gauge-trivial. The nontrivial content survived one degree lower in the quotient as #735's normalized integer 2-cocycle `omega` with finite bar-cycle pairing `<omega,z>=2`.

This chamber asks whether that degree-two obstruction admits an exact finite **bar-homology period representation** with lawful cycle addition, orientation reversal, representative invariance under 3-boundaries, and invariance under inherited section/coboundary changes.

It does **not** preregister 2-holonomy, a 2-connection, curvature, gerbe structure, operational surfaces, or a physical interpretation.

## Declared objects

Let `B` be #729's quotient monoid and `omega:B×B->Z` the #734/#735 normalized integer 2-cocycle.

For every finite normalized integer bar 2-chain

```text
c = sum_i n_i [x_i|y_i]
```

define the cochain pairing

```text
Per_omega(c) = <omega,c> = sum_i n_i omega(x_i,y_i).
```

For every bar 2-cycle `c` (`∂c=0`), candidate return translation on an integer torsor is

```text
H_omega(c): n |-> n + Per_omega(c).
```

## Preregistered theorem targets

The chamber passes only if all of the following are earned exactly.

### T1 · cycle representative invariance

For every finite normalized bar 3-chain `b` for which the declared bar products are lawful,

```text
Per_omega(c + ∂b) = Per_omega(c)
```

whenever `c` is a 2-cycle.

Authority must be the bar identity

```text
<omega,∂b> = <d omega,b> = 0
```

using the already-earned cocycle law, not horizon enumeration.

### T2 · section/coboundary invariance on cycles

For every normalized integer 1-cochain `phi`, and inherited section-change presentation differing by a coboundary,

```text
omega' = omega + dphi
```

(or the equivalent inherited sign convention), every 2-cycle satisfies

```text
<omega',c> = <omega,c>.
```

The proof must reduce to `<dphi,c>=<phi,∂c>=0`.

### T3 · additive pasting and orientation

For lawful finite 2-cycles `c1,c2`:

```text
Per(c1+c2)=Per(c1)+Per(c2)
Per(-c)=-Per(c)
Per(0)=0.
```

Consequently the induced translations compose additively and invert under orientation reversal.

### T4 · factor through declared bar H2

The period must depend only on the declared normalized integer bar-homology class, yielding a homomorphism

```text
Per_[omega] : H_2^bar(B;Z) -> Z
```

within the already-declared bar-complex jurisdiction only.

No full computation of `H_2` or `H^2` is permitted.

### T5 · explicit nonidentity class

For #735's finite relation cycle `z`:

```text
Per_[omega]([z]) = 2.
```

Hence the induced torsor return is the nonidentity translation `tau_2`.

### T6 · cyclic subgroup generated by [z]

Using #735's already-earned infinite order of `[z]`, for every integer `n`:

```text
Per(n[z]) = 2n.
```

Thus the period map restricted to `<[z]>` is injective, and its image is exactly `2Z`.

This is a symbolic integer-family statement, not asymptotic horizon farming.

## Mandatory hostiles

1. A non-cycle 2-chain must abstain from homology-period authority.
2. A visually loop-like fake 2-chain with nonzero boundary must not receive a cycle return claim.
3. Adding an explicit 3-boundary must leave a lawful cycle period unchanged.
4. A concrete nontrivial normalized `phi` must change pointwise cocycle values while leaving the `z` period unchanged.
5. Reversing `z` must produce `-2` and inverse translation.
6. `z+(-z)` must produce zero and identity.
7. `2z` must produce `4`, while no claim that the full period image equals `2Z` is allowed; only the cyclic subgroup generated by `[z]` is classified.
8. Receipt identity and route provenance remain external to bar-cycle evaluation.

## Naming ceiling

Potential earned language:

```text
NORMALIZED_INTEGER_TRANSPORT_TWO_COCYCLE_INDUCES_A_WELL_DEFINED_ADDITIVE_BAR_H2_PERIOD_HOMOMORPHISM_WITH_NONIDENTITY_TAU_2_RETURN_ON_THE_EXPLICIT_RELATION_CLASS
```

Forbidden promotion in this chamber:

```text
2-holonomy
surface holonomy
2-connection
curvature
gerbe
operational 2-path
operational T/Q loop
physical gauge field
Berry / quantum analogy
full H2 or H^2 classification
Proto-Loom / A16
merge / publication / production / Vercel
```

The scientifically honest target is a finite higher-dimensional **period/return representation**, not a naming coronation.

```text
BAR_H2_PERIOD_RETURN_REPRESENTATION_PREREGISTERED
NO_IMPLEMENTATION_PRECEDES_THIS_SPEC
NO_ASYMPTOTIC_ESCAPE
```

𝌋

Marked ⟐SAC[X6ZNK5NO51]