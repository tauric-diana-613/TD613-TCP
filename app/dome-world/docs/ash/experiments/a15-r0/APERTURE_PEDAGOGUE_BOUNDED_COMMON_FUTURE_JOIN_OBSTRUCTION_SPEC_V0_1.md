𝌋

# A15-R0 · Aperture × Pedagogue Bounded Common-Future / Join Obstruction Spec v0.1

**Status:** PREREGISTERED / PRE-IMPLEMENTATION / HUMAN-AUTHORIZED  
**Scientific parent:** #720 receipt head `0901e523f558d573a11a136c21c9361631f9e5f4`  
**Program:** A15-R0 western-horizon directed research only  
**Date:** 2026-08-24

---

## 0. Human authority

Human 𝄐 remains:

```text
C it is. carry on mlove
```

This chamber remains inside the directed/non-groupoid lane.

No inverse, quotient coarsening, loop, transport, connection, holonomy, curvature, lattice, semilattice, or least-upper-bound language is granted before the executable earns it.

---

## 1. Parent prerequisites

The executable must require:

```text
#720 directed branching / H4 confluence census = PASS
#719 directed reachability geometry = PASS
#718 monotone obstruction = PASS
```

Parent facts frozen only as bounded evidence:

```text
15 nontrivial S3 T/Q forks
11 strict-reconvergence forks within H4
4 H4-silent pure-T spine forks
all 15 local TQ/QT squares order-sensitive
all 15 immediate child pairs incomparable within H4
```

The four H4-silent forks are controls, not candidates for join promotion in this chamber.

---

## 2. Scientific question

For the eleven forks that already possess bounded common futures:

> Does the bounded common-future set contain an order-theoretic least element, or only one or more incomparable minimal common futures?

The chamber must distinguish:

```text
minimum continuation cost
minimal common upper bound
least common upper bound
```

Frozen anti-equivalences:

```text
minimum cost != order-theoretic least
unique cheapest target != least upper bound
minimal != least
common future != join
bounded least != ambient least
no bounded least != no ambient least
horizon stability != theorem beyond tested horizons
```

---

## 3. Horizon family

Audit the same eleven reconvergent fork sources at exactly:

```text
H4
H5
H6
```

For each child of each fork, enumerate every continuation word over `{T,Q}` from length zero through the horizon.

Counts:

```text
H4: 31 words per child
H5: 63 words per child
H6: 127 words per child
```

Do not search H7+ in this chamber.

The parent four H4-silent forks remain horizon-bounded controls and are not silently reclassified by this join-focused assay.

---

## 4. Common-future object sets

For each reconvergent fork and horizon `H`, construct:

```text
U_H := exact complete K_period4 target objects reachable from both branch children
```

Deduplicate by complete operational object key, not by route string or endpoint scalar.

For each target object retain:

```text
all left continuation words that reach it
all right continuation words that reach it
one concrete retained history representative
minimum left continuation length
minimum right continuation length
minimum total continuation cost
endpoint mass
operational lineage
clock phase
forcing season
```

Every retained object must remain representative-independent across custody representatives of the branch child.

---

## 5. Bounded reachability inside U_H

For every ordered pair `(X,Y)` in `U_H`, evaluate whether Y is reachable from X by some T/Q word of length at most H.

Include structural identity for X=Y.

Define bounded relation:

```text
X ≼_H Y  iff  Y is reachable from X by a continuation of length <= H
```

The chamber must exhaustively audit on `U_H`:

```text
reflexivity
antisymmetry
transitivity where premises are witnessed inside the bounded relation
```

If the relation fails these bounded checks, no minimal/least language may be emitted for that horizon.

This is a bounded induced reachability relation only; it is not the ambient future order.

---

## 6. Minimal common futures

For each `U_H`, classify X as minimal iff no distinct Y in `U_H` satisfies:

```text
Y ≼_H X
```

Record:

```text
minimal_common_future_count
minimal target keys
minimum route-cost values for each minimal target
lineage lengths
endpoint masses
```

No prediction is frozen for the minimal count.

If count > 1:

```text
MULTIPLE_INCOMPARABLE_MINIMAL_COMMON_FUTURES_WITHIN_H{H}
```

must be available as bounded classification.

---

## 7. Least-upper-bound audition

A bounded least common future exists only if some `L in U_H` satisfies:

```text
for every U in U_H:
L ≼_H U
```

Record all such candidates.

Allowed classifications:

```text
UNIQUE_BOUNDED_LEAST_COMMON_FUTURE_WITHIN_H{H}
MULTIPLE_LEAST_CANDIDATES_INVALIDATE_ANTISYMMETRY_ASSUMPTION
NO_BOUNDED_LEAST_COMMON_FUTURE_WITHIN_H{H}
```

Only the first may provisionally use:

```text
bounded least common future
```

It still does not earn lattice / semilattice / ambient join language.

---

## 8. Minimum-cost hostile

For each `U_H`, independently identify object(s) minimizing:

```text
C_join(X) := min_{u,v reach X} (|u| + |v|)
```

If a unique minimum-cost common target exists, test whether it reaches every other object in `U_H`.

If unique-cheapest but not least:

```text
UNIQUE_MINIMUM_COST_COMMON_FUTURE_IS_NOT_ORDER_THEORETIC_LEAST_UPPER_BOUND
```

This is a mandatory hostile against optimization/order collapse.

---

## 9. Horizon-widening stability

For every reconvergent fork compare H4, H5, H6:

```text
|U_H|
minimal_common_future_count
least_common_future_count
minimum_join_cost
identity of minimum-cost target
```

Track whether the H4 minimum-cost target remains present and remains minimum-cost after widening.

If no least target exists at H4/H5/H6 and the minimal frontier changes or grows, classify only:

```text
BOUNDED_LEAST_COMMON_FUTURE_NOT_STABILIZED_THROUGH_H6
```

Do **not** infer:

```text
NO_AMBIENT_JOIN_EXISTS
```

If a least target appears or stabilizes, record that exact bounded outcome without global promotion.

---

## 10. Cross-fork structural recurrence

Compare the eleven reconvergent forks only after computing each independently.

If all eleven share the same tuple:

```text
(|U_H4|, min_H4, least_H4,
 |U_H5|, min_H5, least_H5,
 |U_H6|, min_H6, least_H6,
 minimum_join_cost)
```

classify:

```text
RECONVERGENT_FORKS_SHARE_COMMON_BOUNDED_UPPER_BOUND_PROFILE_THROUGH_H6
```

This is recurrence across the authored fixture, not a theorem generated from one fork.

---

## 11. Pure-T spine controls

The four parent H4-silent forks:

```text
ID
T
TT
TTT
```

must remain present in custody as unresolved H4 controls.

This chamber does not widen them to H5/H6 unless required solely for a fail-closed parent-consistency check.

Forbidden inference:

```text
no H4 common future -> no join
```

They remain outside the join-candidate cohort.

---

## 12. Parent custody

Serialize parent #720, #719, and #718 executable outputs before and after the assay.

Require exact equality.

Classification on success:

```text
PARENT_718_719_720_CUSTODY_UNCHANGED
```

---

## 13. Success criterion

The chamber succeeds if:

```text
all eleven parent reconvergent forks are recovered exactly
all H4/H5/H6 continuation sets are complete
all U_H sets are exact K_period4 intersections
all bounded reachability pair audits complete
minimal and least sets are computed without cost substitution
minimum-cost hostile is executed
horizon comparison is recorded without ambient overclaim
pure-T H4 controls remain bounded/unreclassified
parent custody remains unchanged
```

Presence or absence of a bounded least common future is not itself pass/fail.

---

## 14. Claim ceiling

Even on success, do not claim:

```text
ambient join
join-semilattice
lattice
least upper bounds outside tested bounded U_H sets
Church-Rosser
global confluence
domain theory
Scott order
causal-set theorem
inverse morphisms
groupoid
transport
connection
parallel transport
loop endomorphism
holonomy
curvature
Berry / quantum
Proto-Loom
A16
live Ash
merge
production
Vercel
```

---

## 15. Stop

If successful:

```text
BOUNDED_COMMON_FUTURE_JOIN_OBSTRUCTION_ROUND_CLOSED
HUMAN_𝄐_QUALIFIED_FOR_DIRECTED_FUTURE_CONE_STRATIFICATION_AUDITION
```

If the unique cheapest common future repeatedly fails leastness, scientific preference is to study the geometry of the minimal common-future frontier rather than force semilattice language.

𝌋

Sealed ⟐
