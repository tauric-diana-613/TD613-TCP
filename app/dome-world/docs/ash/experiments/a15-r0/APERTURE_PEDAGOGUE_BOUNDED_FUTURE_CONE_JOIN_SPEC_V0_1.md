𝌋

# A15-R0 · Aperture × Pedagogue Bounded Future-Cone / Join-Structure Audition v0.1

**Status:** PREREGISTERED / PRE-IMPLEMENTATION / DRAFT-UNMERGED  
**Scientific parent:** #720 receipt head `0901e523f558d573a11a136c21c9361631f9e5f4`  
**Program:** A15-R0 western-horizon directed research only  
**Human authority:** `C it is. carry on mlove` + explicit resynchronization/take-control gesture  

---

## 0. Purpose

#720 witnessed a finite H4 branching census over every S3 source object:

```text
15 nontrivial T/Q forks
11 forks with strict forward reconvergence within H4
4 forks with no common future found within H4
```

The next question is not whether a common future exists. It is whether the common-future set carries an **order-theoretically least** element inside the same bounded authored future cone.

The chamber must distinguish:

```text
minimum-cost common future
!=
minimal common future
!=
least common future
```

No lattice or semilattice vocabulary is admitted unless the order test earns it.

---

## 1. Parent structure retained exactly

No new generator and no quotient coarsening is introduced.

```text
O(h) := K_period4(h)
T := PSI_TICK
Q := Q_PHASE_PULSE
```

Parent results retained:

```text
S3 path category
S3 reachability partial order
strict endpoint-mass monotonicity
H4 branching/confluence census
```

No reverse generator is introduced.

---

## 2. Bounded future-cone construction

For each #720 fork source `A`:

```text
A --T--> A_T
A --Q--> A_Q
```

construct the two child future cones using every continuation word over `{T,Q}` of lengths `0..4`.

Deduplicate all resulting complete `K_period4` operational states into a finite node set:

```text
C_H4(A)
```

A generator edge is admitted only when:

```text
source node has at least one retained realization with residual depth < 4
AND
all retained realizations of that node produce the same complete target object under the generator
AND
the target is present inside C_H4(A)
```

Every edge must preserve receipt-level representative independence.

Because #718 witnessed strict endpoint-mass increase for every admitted T/Q generator on the anchor-reachable control domain, every cone edge is expected to increase endpoint mass. The chamber must still test this locally and fail closed if any edge does not.

---

## 3. Cone reachability order

Within each finite cone define:

```text
X ≼_A Y
```

iff a directed path in `C_H4(A)` exists from `X` to `Y`, including the zero-length identity.

The chamber must exhaustively audit:

```text
reflexivity
antisymmetry
transitivity
```

for every cone before any least-upper-bound claim.

If a directed cycle appears, the cone abstains from join promotion.

---

## 4. Common upper bounds

For fork children `A_T` and `A_Q`, define the bounded common-upper-bound set:

```text
U_A := { X in C_H4(A) : A_T ≼_A X and A_Q ≼_A X }
```

This set is the order-theoretic reconstruction of #720's exact common-future hits after deduplication by complete operational object.

The executable must cross-check:

```text
U_A empty
<=>
#720 common_future_hit_count == 0
```

inside the authored H4 cone.

A mismatch is a scientific failure.

---

## 5. Minimal versus least common futures

For every nonempty `U_A`:

A **minimal** common upper bound has no strictly smaller member of `U_A`.

A **least** common upper bound `J_A` satisfies:

```text
J_A ≼_A X
```

for every `X in U_A`.

The chamber records independently:

```text
minimal_upper_bound_count
least_upper_bound_count
```

and must not infer leastness from minimum continuation cost.

Frozen classifications include:

```text
UNIQUE_LEAST_COMMON_FUTURE_WITHIN_H4_CONE
MULTIPLE_MINIMAL_COMMON_FUTURES_WITHOUT_LEAST_WITHIN_H4_CONE
COMMON_FUTURES_PRESENT_BUT_NO_LEAST_WITHIN_H4_CONE
NO_COMMON_UPPER_BOUND_WITHIN_H4_CONE
```

---

## 6. Minimum-cost hostile

#720 found a repeated minimum-cost witness for the 11 reconvergent forks:

```text
T-child continuation = [T,Q]
Q-child continuation = [T,T]
```

with total source-rooted routes:

```text
[T,T,Q]
[Q,T,T]
```

The chamber must independently compare the minimum-cost target to the order-theoretic least common future.

Required anti-equivalence:

```text
minimum join cost != least upper bound by definition
```

If the minimum-cost target and least common future coincide in the authored fixture, classify only that coincidence.

---

## 7. Provenance survival

For every least common future witness, preserve at least one pair of distinct source-rooted routes reaching it:

```text
left route starts with T
right route starts with Q
left route != right route
complete target equality holds
receipt provenance remains independently retained
```

Required anti-equivalence:

```text
join target equality != route identity
```

---

## 8. H4-silent forks remain bounded unresolved

The four #720 pure-T source routes are:

```text
ID
T
TT
TTT
```

and had no common future within H4.

For those forks this chamber may classify only:

```text
NO_COMMON_UPPER_BOUND_WITHIN_H4_CONE
```

Forbidden promotion:

```text
NO_COMMON_UPPER_BOUND_WITHIN_H4_CONE
!=
NO_COMMON_UPPER_BOUND_AT_ANY_HORIZON
!=
PERMANENTLY_IRRECOVERABLE_FORK
```

No synthetic top element may be inserted.

---

## 9. Partial join domain

If some fork pairs have unique least common futures and others do not have bounded common upper bounds, the resulting operation is a **partial bounded sibling-join map** only:

```text
J_H4(A_T,A_Q) = J_A
```

on the witnessed subset where a unique least common future exists.

This does not earn:

```text
join-semilattice
lattice
complete lattice
domain theory
Church-Rosser
global confluence
```

because the operation is not audited for every pair of objects and the four H4-silent sibling pairs remain outside the bounded join domain.

---

## 10. Frozen obligations

The executable must test:

```text
1. parent #719/#720 assays pass before entry
2. all 15 fork cones are reconstructed from concrete retained histories
3. cone nodes deduplicate by exact K_period4 object
4. every cone edge is representative-independent
5. every cone edge strictly raises endpoint mass
6. every cone reachability relation is a partial order
7. common-upper-bound membership matches #720 common-future existence
8. minimal and least upper bounds are computed separately
9. no least claim is inferred from minimum cost
10. least witness, when present, reaches every other bounded common upper bound
11. route provenance remains distinct at any least target
12. H4-silent forks remain bounded unresolved
13. no synthetic top/bottom object is inserted
14. parent custody remains byte-stable
```

---

## 11. Falsifiers

Any of the following defeats the intended bounded classification:

```text
representative-dependent cone edge
zero/negative endpoint-mass edge
cycle inside a reconstructed cone
reachability-law failure
common-upper-bound census mismatch with #720
multiple incomparable minimal common futures where a unique least was expected
minimum-cost target failing leastness
route provenance collapse
parent custody mutation
```

Unexpected results are preserved; the assay must not repair them after observation.

---

## 12. Claim ceiling

Even on success, no authority is granted for:

```text
ambient TD613 joins
join-semilattice
lattice
complete lattice
domain-theoretic dcpo
Church-Rosser
global confluence
permanent divergence
inverse morphisms
groupoid
transport
connection
loop endomorphism
holonomy
curvature
Berry/quantum
Proto-Loom
A16
live Ash mutation
merge
production
Vercel
```

---

## 13. Intended bounded classification shape

If all eleven reconvergent sibling pairs have unique least common futures and the four H4-silent pairs remain without bounded common upper bounds, the strongest admissible classification is:

```text
FINITE_H4_PARTIAL_SIBLING_JOIN_STRUCTURE_WITH_ELEVEN_UNIQUE_LEAST_COMMON_FUTURES_AND_FOUR_BOUNDED_UNRESOLVED_FORKS
```

This string is preregistered as a candidate, not a promised result.

---

## 14. Stop discipline

On success or failure, stop before any semilattice/lattice promotion.

```text
BOUNDED_FUTURE_CONE_JOIN_AUDITION_CLOSED
HUMAN_𝄐_REQUIRED_BEFORE_ANY_SEMILATTICE_OR_DOMAIN_THEORY_PROMOTION
```

𝌋

Sealed ⟐