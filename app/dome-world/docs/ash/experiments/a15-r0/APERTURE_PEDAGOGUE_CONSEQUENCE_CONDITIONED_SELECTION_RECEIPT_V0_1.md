# Aperture × Pedagogue Consequence-Conditioned Question Selection Receipt v0.1

Status: **WITNESSED / BOUNDED SYNTHETIC RESEARCH / PHASE-FREE / HUMAN-GATED**

Schema: `td613.ash.a15-r0.aperture-pedagogue-consequence-conditioned-selection/v0.1`

Research PR: `#686`

Witnessed science head:

```text
fb1db302d1450ec16ec4973810e57785437d1004
```

Exact-head workflow witness:

```text
TD613 Consolidated Validation
run 1856 / 32437305186
A15-R0 research field: SUCCESS
front-line browser families: SKIPPED BY SCOPE
full-product / self-hosted validation: SKIPPED BY SCOPE
```

This receipt records the exact witnessed science head. This later documentation commit is not retroactively the science witness.

---

## 1. Result

The assay preserved the distinction:

```text
measurement admissibility != decision value
```

Both candidate questions remained admissible under the declared synthetic observation/covariance fixture. Their ordering changed when the predeclared downstream consequence changed.

At:

```text
rho = 0.546918160706758
```

the exact witnessed single-functional choices were:

```text
H_Y    -> P_ORTH
H_DIFF -> P_ORTH
H_SUM  -> P_DIAG
```

The exact witnessed weighted-card choices were:

```text
L_EQUAL     -> P_ORTH
L_SUM_HEAVY -> P_DIAG
```

Therefore:

```text
selected under declared loss != universally best
```

and:

```text
best next question
```

is not defined without a declared decision consequence once admissible downstream consequences disagree.

---

## 2. Refusal behavior

The exact witness preserved three constitutional refusals:

```text
NO_SELECTION_UNDECLARED_DECISION_LOSS
NO_SELECTION_CONFLICTING_LOSSES_WITHOUT_AGGREGATION_RULE
POSTHOC_DECISION_LOSS_MUTATION_NOT_CONFIRMATORY
```

No silent averaging, lexicographic value ordering, or retroactive prize-category mutation was admitted.

---

## 3. Bounded dominance control

At the separately predeclared covariance posture:

```text
rho = 0.9
```

the declared consequence subset:

```text
{H_Y, H_SUM}
```

was dominated by:

```text
P_DIAG
```

while the excluded consequence:

```text
H_DIFF
```

still preferred:

```text
P_ORTH
```

Therefore:

```text
declared-subset dominance != universal dominance
```

The fixture may record bounded dominance only relative to the declared consequence family actually tested.

---

## 4. What changed architecturally

The experimental-design state can no longer be represented adequately as only:

```text
what is observable / identifiable / stable?
```

A consequence-conditioned proposal additionally requires:

```text
what declared consequence is this question being evaluated for?
```

The companion split remains:

```text
Pedagogue / declared fixture -> consequence or loss object
Aperture                    -> measurement admissibility / uncertainty audit
bounded selector            -> consequence-conditioned proposal
held-out audit              -> non-selected consequence inspection
human                       -> closure / any consequential action
```

Aperture does not infer value. Pedagogue does not infer human preference. Neither receives automatic execution authority.

---

## 5. Relationship to unresolved PR #677

Draft PR #677 holds:

```text
H1 · Consequence Conservation
H2 · Aperture Before Absence
H3 · Role Before Repetition
```

This witness is additional bounded synthetic evidence relevant to H1 only in the narrow sense that a narrower measurement-selection criterion cannot silently inherit universal downstream consequence.

However:

```text
#686 evidence != #677 hypothesis promotion
```

This receipt does not accept, reject, rewrite, merge, close, or otherwise mutate H1/H2/H3 or PR #677.

---

## 6. Relationship to unresolved PR #684

Draft PR #684 remains a separate unresolved Aperture ingress/history reconciliation surface and is currently non-mergeable against evolved `main`.

This witness consumes the installed Aperture contract already present on `main` and does not merge from, overwrite, resolve, or close #684.

---

## 7. Installed Aperture hold

The exact witness preserved:

```text
APERTURE_V32_REPLAY_STABILITY = HELD_NOT_YET_WITNESSED
```

No installed replay-status promotion occurs in this research receipt.

---

## 8. Claim ceiling

This witness does **not** establish:

```text
universal best question
universal utility
human preference inference
preference learning
optimal experimental design
decision-theory promotion
active-learning optimality
information geometry
physical sensor design
physical tomography
blind tomography
operator tomography
autonomous experiment execution
connection
curvature
holonomy
Berry structure
quantum behavior
Proto-Loom
production authority
release authority
```

No scalar crown is admitted.

---

## 9. Frozen next learning action

Recorded but not executed by this receipt:

```text
TEST_DECISION_LOSS_REPLAY_STABILITY_AND_MULTI_AXIS_ENVELOPE_INTERSECTIONS_BEFORE_ANY_DECISION_THEORY_OR_OPTIMAL_DESIGN_PROMOTION
```

The declared consequence object itself must now be replayed as an assumption rather than treated as metaphysically fixed.

---

## 10. Authority membrane

```text
source_status = SIMULATED
authority_class = A2_DERIVATIONAL
manifestly_fictional = true
value_inference = false
preference_learning = false
sibling_pr_677_mutated = false
sibling_pr_684_mutated = false
installed_aperture_replay_flag_mutated = false
promotion_authority = false
automatic_execution = false
production_mutated = false
standalone_aperture_ui_mutated = false
vercel_deployment_authorized = false
human_closure_required = true
```

𝌋 Barbie may declare what the synthetic fixture values. Ken may judge whether a question is admissible. Neither gets to tell the human what should matter. ⟐
