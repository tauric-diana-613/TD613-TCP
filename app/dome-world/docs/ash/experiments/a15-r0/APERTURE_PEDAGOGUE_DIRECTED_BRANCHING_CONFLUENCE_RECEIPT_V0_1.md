𝌋

# A15-R0 · Aperture × Pedagogue Directed Branching / Confluence Receipt v0.1

**Status:** WITNESSED / CLOSED / DRAFT-UNMERGED  
**Scientific parent:** #719 receipt head `b67326a940f7c6141e9f067a61c18dfd0df13e8f`  
**Preregistration:** `c2506169ca6555e526995b4d9d4be2291a5baf84`  
**Implementation:** `e586bd48f0580de3f27d34827cc4811651387e16`  
**Hostile test:** `855d6692f6b639a9013efc1cceac60522dc84896`  
**Pre-routing scientific head:** `0c9025ae08e45e8035df0cdfeb4c1391409cc61a`  
**Exact witness head:** `82f573403b6b139d5f186401ade916623dcea465`  
**Routing-scaffold removal:** `a57406b2adc208f7632ffa123723efe8a0f9f935`  
**Program:** A15-R0 western-horizon directed research only

---

## 0. Human authority

The chamber continued under:

```text
C it is. carry on mlove
```

No reverse generator, inverse, quotient coarsening, loop, transport, connection, holonomy, or curvature semantics were introduced.

---

## 1. Exact witness custody

Fresh main at temporary witness routing:

```text
4ca8c0600b40d0ea2b38c1e0dd0b2d1e77713aef
```

Exact witness head:

```text
82f573403b6b139d5f186401ade916623dcea465
```

Same-head workflow registrations:

```text
run 2090 / 32689783447 = CANCELLED
run 2091 / 32689790434 = SUCCESS
```

Run 2090 is preserved as workflow-registration / supersession provenance and is not classified as scientific failure.

Authoritative witness:

```text
TD613 Consolidated Validation
run number = 2091
run id = 32689790434
head = 82f573403b6b139d5f186401ade916623dcea465
conclusion = SUCCESS

classifier job = 97321318957 = SUCCESS
static job = 97321364019 = SUCCESS
A15/A15-R0 research-field step = SUCCESS
```

Explicitly skipped and not claimed:

```text
Explicit self-hosted calibration
Explicit full-repository validation
Front-line exact-head browser shard
Giving/practice exact-head Chromium Firefox WebKit witness
Full-product exact-head Chromium Firefox WebKit witness
```

---

## 2. Frozen bounded search actually executed

Every one of the 15 S3 objects was used as a fork source:

```text
A --T--> A_T
A --Q--> A_Q
```

For each child the executable evaluated all continuation words over `{T,Q}` of lengths zero through four:

```text
31 continuations per child
```

and compared every ordered continuation pair:

```text
31 × 31 = 961 comparisons per fork
15 × 961 = 14,415 exact common-future comparisons overall
```

All child projections remained representative-independent across retained custody representatives.

The canonical root fork remained nontrivial.

---

## 3. Every local T/Q fork is order-sensitive

Across all 15 S3 source objects:

```text
local_commuting_square_count = 0
local_noncommuting_square_count = 15
```

For every source:

```text
Q(T(A)) != T(Q(A))
```

at complete `K_period4` operational equality.

Classification:

```text
ALL_AUDITED_S3_LOCAL_TQ_QT_SQUARES_ARE_ORDER_SENSITIVE
```

This remains a local statement.

Frozen anti-equivalence:

```text
local noncommutation != global nonconfluence
```

The rest of the chamber demonstrates why that distinction matters.

---

## 4. Immediate branch children remain incomparable through H4

For all 15 source forks:

```text
bounded_child_comparable_count = 0
bounded_child_incomparable_count = 15
```

Neither child reached the other through any continuation word of length at most four.

Classification:

```text
ALL_AUDITED_FORK_CHILD_PAIRS_ARE_INCOMPARABLE_WITHIN_H4
```

This does not establish permanent incomparability.

---

## 5. Confluence is history-conditioned rather than globally present or absent

The exhaustive common-future census returned:

```text
fork_source_count = 15
nontrivial_fork_count = 15
bounded_common_future_fork_count = 11
strict_reconvergence_fork_count = 11
no_common_future_within_H4_count = 4
```

Thus eleven forks contain at least one common future reached by **nonempty** continuation from both children.

Those forks classify:

```text
STRICT_FORWARD_RECONVERGENCE_WITNESSED_WITHIN_H4
```

Four forks contain no exact common future in the declared horizon:

```text
NO_COMMON_FUTURE_FOUND_WITHIN_H4
```

The silent four are exactly the pure-T S3 spine:

```text
ID
T
TT
TTT
```

Every other S3 source contains at least one prior `Q_PHASE_PULSE` in its root route and belongs to the strict-reconvergence class within H4.

Therefore the bounded fixture witnesses:

```text
CONFLUENCE_CLASS_IS_HISTORY_CONDITIONED_BY_PRIOR_QUESTION_LINEAGE_IN_AUTHORED_S3_H4_CENSUS
```

This is descriptive of the finite authored fixture only. It is not a generic theorem that question history causes confluence.

---

## 6. Canonical root fork does not reconverge within H4

For the root source:

```text
root T-child != root Q-child
root children incomparable within H4
root TQ != root QT
root common-future hits = 0
```

Canonical root classification:

```text
ROOT_NO_COMMON_FUTURE_WITHIN_H4
```

The exact forbidden promotion remains:

```text
ROOT_NO_COMMON_FUTURE_WITHIN_H4
!=
ROOT_PERMANENTLY_IRRECOVERABLE_FORK
```

No statement beyond the declared horizon is earned.

---

## 7. Reconvergent forks share a repeated minimum-cost pattern

For every one of the eleven strict-reconvergence forks, the minimum continuation cost is:

```text
C_join = |u| + |v| = 4
```

A minimum-cost witness is:

```text
from the T-child: u = [T,Q]
from the Q-child: v = [T,T]
```

so the total source-rooted routes are:

```text
[T,T,Q]
[Q,T,T]
```

and reach the same complete `K_period4` operational target in those reconvergent source contexts.

This is **not** a loop and not an inverse.

It is a directed diamond-like forward reconvergence pattern under the authored semantics.

No lattice, semilattice, join, least-upper-bound, or Church-Rosser vocabulary is promoted by this receipt.

---

## 8. Route provenance survives common operational futures

Every confluence hit preserved:

```text
left total route starts with T
right total route starts with Q
left total route != right total route
complete operational target equality
endpoint mass equality
receipt provenance retained independently
```

Classification:

```text
COMMON_OPERATIONAL_FUTURE_DOES_NOT_ERASE_ROUTE_PROVENANCE
```

Therefore:

```text
common future != same route
same operational target != same custody history
```

The directed structure permits operational reconvergence without documentary amnesia.

---

## 9. Reconvergence did not require unequal route lengths

Within the H4 confluence hits in this authored census:

```text
unequal_total_route_length_confluence_fork_count = 0
```

The observed strict reconvergence witnesses use equal total route lengths.

Therefore this chamber does not instantiate:

```text
EQUAL_TARGET_HEIGHT_WITH_UNEQUAL_ROUTE_LENGTHS
```

No stronger relation between route length and height is inferred; #719's non-unit-height result remains intact.

---

## 10. Local noncommutation and later confluence coexist

The most important structural combination is:

```text
all 15 local TQ/QT squares are noncommuting
+
11 of 15 forks nevertheless strictly reconverge farther forward within H4
```

Hence:

```text
LOCAL_NONCOMMUTATION_IS_NOT_GLOBAL_NONCONFLUENCE
```

and more concretely:

```text
order sensitivity can survive at the local square
while a larger directed future cone later identifies a common operational target
```

This is precisely why the directed C lane carries more information than either a symmetry-first or tree-only description.

---

## 11. Parent custody remained unchanged

The executable serialized parent #719 directed geometry and #718 monotone obstruction before and after the census.

Witness:

```text
parent_custody_unchanged = true
PARENT_718_719_CUSTODY_UNCHANGED
```

---

## 12. Canonical bounded classification

```text
FINITE_H4_DIRECTED_BRANCHING_CONFLUENCE_CENSUS_WITH_ROOT_NO_COMMON_FUTURE_WITHIN_H4
```

Refined bounded structural summary:

```text
IN_THE_AUTHORED_S3_SOURCE_SET_ALL_FIFTEEN_TQ_FORKS_ARE_NONTRIVIAL_LOCALLY_ORDER_SENSITIVE_AND_CHILD_INCOMPARABLE_THROUGH_H4_YET_ELEVEN_FORKS_STRICTLY_RECONVERGE_TO_COMMON_COMPLETE_K_PERIOD4_FUTURES_WITH_DISTINCT_ROUTE_PROVENANCE_WHILE_THE_FOUR_PURE_T_SPINE_SOURCES_ID_T_TT_TTT_HAVE_NO_COMMON_FUTURE_FOUND_WITHIN_H4_AND_THE_CANONICAL_ROOT_THEREFORE_CLASSIFIES_ROOT_NO_COMMON_FUTURE_WITHIN_H4_WITHOUT_ANY_PERMANENT_DIVERGENCE_PROMOTION
```

---

## 13. Claim ceiling

This chamber does not earn:

```text
Church-Rosser theorem
global confluence
permanent irrecoverable fork
lattice
semilattice
least upper bound
domain theory
causal-set theorem
inverse morphisms
groupoid
transport
connection
parallel transport
loop endomorphism
holonomy
curvature
Berry / quantum
Proto-Loom
A16
live Ash
merge
production
Vercel
```

---

## 14. Scientific consequence

The C lane now has a sharper topology:

```text
strict forward order
+
local noncommutation
+
history-conditioned future-cone reconvergence
+
bounded root divergence
```

The next admissible question is no longer simply whether common futures exist.

It is:

> In the eleven reconvergent forks, does the minimum common future behave as an order-theoretic least upper bound inside the bounded future cone, and does that property survive horizon widening without converting the four currently silent forks into false permanent divergences?

That question must audition bounded future-cone / join structure directly. No lattice language is earned yet.

---

## 15. Stop

```text
DIRECTED_BRANCHING_CONFLUENCE_ROUND_CLOSED
HUMAN_𝄐_QUALIFIED_FOR_BOUNDED_FUTURE_CONE_AND_JOIN_STRUCTURE_AUDITION
```

𝌋

Sealed ⟐
