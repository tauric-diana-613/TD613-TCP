# A15-R0 · Directed History-Fiber Transport and Quotient-Descent Receipt v0.1

Status: **WITNESSED / ROUND CLOSED / DRAFT / OPEN / UNMERGED**

PR: `#730`  
Parent PR: `#729`  
Parent receipt head: `bd33fc4010604dc9a0a84ba10ba4d721632ac4ef`  
Original preregistration commit: `bf567fd8e130727e85ab26497447a9dfb935c039`

## Earned classification

```text
DIRECTED_HISTORY_LIFT_COMPOSES_EXACTLY_AND_TARGET_EQUIVALENT_ROUTES_CAN_TRANSPORT_DISTINCT_CUSTODY_PAYLOADS
```

Within the declared Q-last-action `K_period4` jurisdiction, #730 witnesses a deterministic directed history lift for authored `T` / `Q` route words. The fixed composition controls require direct execution of `uv` to equal execution of `u` followed from its actual target by `v`, both as complete histories and as route-free appended custody/evolution deltas.

The hostile pair frozen before implementation was:

```text
u = TTTTQ
v = QTTTT
```

PR #729 places both routes at the same source-relative quotient coordinate:

```text
c(u) = c(v) = (4,1,0)
```

and the #730 assay requires both actual route targets to agree with the source-conditioned #729 quotient target. At least one retained forcing-season source nevertheless yields unequal route-free appended history payloads.

The comparator contains only appended:

```text
custody_events
evolution_events
forcing_evolution_events
```

and excludes route words, word labels, history ids, parent ids, source keys, and target keys. Receipt variants are held constant in the obstruction control. The witnessed transport distinction therefore comes from authored event payload evolution rather than route-label leakage, base-only collapse, or receipt renaming.

Required anti-equivalence:

```text
target equality != transported custody equality
representative-independent base transport != receipt erasure
```

## Composition and hostile controls

The exact-head test requires:

- identity transport on the empty control route;
- seven fixed direct-versus-sequential composition controls;
- identical complete final histories for direct and sequential transport;
- identical route-free appended deltas under lawful composition;
- identical final `K_period4` base objects;
- receipt-distinct source representatives to preserve receipt distinction while producing equal base transport and equal appended payloads for the same route;
- mistyped continuation to abstain before composition;
- equal #729 quotient coordinates and equal base targets not to be promoted to equal transported custody;
- no inverse or loop fabrication;
- no H8 or wider horizon farming.

## Authority-bearing witness

```text
frozen scientific head  e7d8ea8759bc47789cfa01ba3e7c0784533a6fdc
exact routed witness     89445565f8f1293e948b80c9360951e42c983f0b
run                      2135 / 32728007431   SUCCESS
classifier job           97433565670          SUCCESS
static job               97433618031          SUCCESS
A15/A15-R0 step 19                             SUCCESS
```

Run 2135 started at `2026-08-24T12:36:37Z` and completed successfully at `2026-08-24T12:37:27Z`.

Explicit full-repository validation, self-hosted calibration, Giving/practice browser witness, front-line browser shards, and full-product browser witness were skipped and remain outside this receipt's claim.

## Post-witness routing custody

The authority-bearing witness used the temporary routing-only note at head `89445565f8f1293e948b80c9360951e42c983f0b`. That note declared the frozen scientific head `e7d8ea8759bc47789cfa01ba3e7c0784533a6fdc` and carried no scientific transition change.

After the successful witness, the routing note was removed at:

```text
62aa9c18fdb63fef50cae8aea741a381c66fc03c
```

Comparison from frozen scientific head `e7d8ea875...` to post-cleanup head `62aa9c18...` contains the routing-note add/remove pair and **zero net changed files**. PR #730 remains stacked on #729, draft, open, and unmerged.

## Claim ceiling

This receipt does **not** authorize or establish:

```text
universal quotient descent
minimal route-sensitive transport state
cocycle or cohomology classification
connection
parallel transport in the differential-geometric sense
inverse transport
inverse morphisms
groupoid
closed nonidentity loop
loop endomorphism
holonomy
curvature
Berry / quantum analogy
manifold fiber bundle
Proto-Loom
A16
live Ash mutation
merge
production
Vercel release
```

The word `transport` remains restricted to the witnessed directed action of authored `T` / `Q` history lifts on custody-bearing synthetic histories.

```text
DIRECTED_HISTORY_FIBER_TRANSPORT_QUOTIENT_DESCENT_ROUND_CLOSED
HUMAN_𝄐_REQUIRED_BEFORE_ANY_MINIMAL_ROUTE_SENSITIVE_TRANSPORT_STATE_OR_COCYCLE_AUDITION
```

󐘓 U+10D613

𝌋

Sealed ⟐
