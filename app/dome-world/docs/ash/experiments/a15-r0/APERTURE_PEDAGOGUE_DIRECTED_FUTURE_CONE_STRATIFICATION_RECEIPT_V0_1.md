# Aperture × Pedagogue Directed Future-Cone Stratification Receipt v0.1

Status: **WITNESSED / CLOSED AT HUMAN 𝄐**  
Program: TD613 · Ash Keep · A15-R0  
Parent authority: PR #722 receipt head `6b69342b8eb65a01fe679f00738a37197ea0f260`  
PR: #723  
Scope: finite authored H4–H7 directed common-future frontier stratification only

## 1. Custody chain

The chamber was stacked from #722 and preregistered before implementation.

```text
parent #722 receipt head        6b69342b8eb65a01fe679f00738a37197ea0f260
preregistration/spec            29ba4b6e3490b5a5b2277b887daa18cb4ad8a924
implementation                  1aac642203af4ef683c0ad0706a36cdde3a6be50
hostile/data-reporting test     7852d673f5c0140fc4ec4959cdaa9b77be0cef71
one-line static-gate import     3fe5d151d9c2f48d6c81aea04d959b52ef7d1559
routing-only exact witness      c2773677549039780c3dc773654036efd8927d91
all-positive confirmation       ac80d4a21ecd66e52575f857d899e20b424aaa1b
diagnostic custody head         752d353ce7cf716c3effc39396977763076a265f
observed-break confirmation     393642651c333f736d2173dc8654aece4a44b1c1
routing-scaffold removal        79ce5f4694a97e7113bfe0e3262a4566a413a575
```

The prospective horizon was frozen as:

```text
H7
255 continuation words per child
H8+ forbidden in this chamber
```

No H8 search was performed.

## 2. Witness sequence — failure remains evidence

The first exact-head registration produced the usual same-head concurrency scar:

```text
run 2100 / 32690865989 = CANCELLED by concurrency
run 2101 / 32690879391 = SUCCESS
static job 97324236224 = SUCCESS
A15/A15-R0 research-field step = SUCCESS
```

Run 2101 established that the preregistered H7 assay, order checks, and parent-custody checks executed successfully. Its test intentionally did **not** require the prospective recurrence patterns to be positive.

Because the connector did not expose the sealed run-2101 console body, a post-witness confirmation layer was added without altering H7 or widening the assay. The first confirmation deliberately asserted the strongest all-positive reading so that a break would fail visibly:

```text
run 2102 / 32691983780 = FAILURE
static job 97327187943 = FAILURE
A15/A15-R0 research-field step = FAILURE
```

That red witness was preserved. It was not rewritten as success.

A diagnostic-custody commit then wrote the complete measured H7 truth vector to `artifacts/` **before** the same positive assertions fired. The existing failed-job diagnostics contract uploaded it:

```text
run 2103 / 32692314848 = FAILURE
static job 97328091911 = FAILURE
artifact 9507679258 = td613-consolidated-contract-diagnostics
artifact digest = sha256:8934ca89b6ecb76948e61ab55d58044fb9368a5a01d57fcedda315e3cfdaf200
```

The diagnostic artifact identified exactly one negative prospective pattern. The test was then changed to encode that observed result rather than the desired all-positive outcome:

```text
run 2104 / 32692508424 = SUCCESS
static job 97328635939 = SUCCESS
A15/A15-R0 research-field step = SUCCESS
all later static contract steps = SUCCESS
```

Runs 2102 and 2103 remain scientific provenance. Their failures are part of the result.

## 3. Prospective H7 census

All eleven reconvergent source forks `O4` through `O14` produced the same H7 count profile:

```text
common future count       = 56
minimal frontier width    = 6
least common future count = 0
minimum join cost         = 4
```

Thus, in this authored finite fixture:

```text
56 = C(8,3)
6  = H7 - 1
```

The H4–H7 count sequence is therefore:

```text
H4: common = 10 · minimal = 3 · least = 0 · minimum cost = 4
H5: common = 20 · minimal = 4 · least = 0 · minimum cost = 4
H6: common = 35 · minimal = 5 · least = 0 · minimum cost = 4
H7: common = 56 · minimal = 6 · least = 0 · minimum cost = 4
```

This is finite fixture evidence only. It grants no H8 statement and no all-H closed form.

## 4. Prospective pattern truth vector

Nine preregistered finite regularities survived through H7:

```text
minimal_frontier_width_matches_H_minus_one = true
common_future_count_matches_binomial = true
question_lineage_consecutive = true
frontier_preserves_source_phase = true
frontier_is_source_plus_two_seasons = true
opposite_phase_coordinate_stratification = true
route_normal_form = true
affine_cost = true
antichain_persistence = true
```

One prospective normalization claim failed:

```text
source_relative_profiles_identical = false
```

Executable classification:

```text
SOURCE_RELATIVE_MINIMAL_FRONTIER_PROFILES_RETAIN_SEASON_DEPENDENT_STRUCTURE
```

This is the scientifically important break. Source-relative normalization does not erase the authored period-4 forcing-season structure, even though the tested count, lineage, phase, season-offset, coordinate, route, cost, and antichain regularities persist through H7.

## 5. What the break permits — and forbids

The result supports a narrower next mathematical question than the all-positive audition would have supported:

> Can the surviving H4–H7 frontier recurrence be stated symbolically **conditioned on forcing season**, without collapsing distinct source-season profiles into one source-relative normal form?

The following implication is forbidden:

```text
same finite H4-H7 count/route/stratum regularities
  !=
source-independent normalized frontier profile
```

Likewise:

```text
finite H4-H7 recurrence
  != induction theorem
  != all-H recurrence
  != Church-Rosser
  != global confluence
  != ambient join / semilattice / lattice
  != transport / connection / holonomy / curvature
```

The executable assay's broad `HUMAN_𝄐_QUALIFIED_FOR_SYMBOLIC_RECURRENCE_AND_INDUCTIVE_PROOF_AUDITION` stop is therefore read under this receipt's stricter evidentiary boundary: any later proof audition must preserve forcing-season dependence and may not assume source-relative profile identity.

## 6. Parent custody

The assay re-ran the inherited #718/#719/#720/#722 parent surfaces before and after the H7 work and classified:

```text
PARENT_718_719_720_722_CUSTODY_UNCHANGED
```

No parent result was rewritten to obtain the H7 outcome.

## 7. Claim ceiling

Still closed:

```text
all-H recurrence theorem
induction conclusion
closed form beyond H7
rewrite-system theorem
Church-Rosser
ambient/global confluence
ambient join, semilattice, or lattice
domain theory
causal-set claim
fiber bundle or gauge structure
transport or connection
loop endomorphism
holonomy
curvature
Berry / quantum analogy as result
Proto-Loom
A16
live Ash
merge
production
Vercel
```

## 8. Stop

```text
DIRECTED_FUTURE_CONE_STRATIFICATION_ROUND_CLOSED
HUMAN_𝄐_REQUIRED_BEFORE_ANY_SYMBOLIC_PROOF_AUDITION
IF_REOPENED_PROOF_TARGET_MUST_PRESERVE_FORCING_SEASON_DEPENDENCE
H8_PLUS_REMAINS_FORBIDDEN_UNDER_THIS_RECEIPT
```

The next lawful move, if separately authorized, is not another empirical horizon. It is a symbolic audit of the finite recurrence already witnessed, with forcing season retained as part of the state description rather than quotiented away.
