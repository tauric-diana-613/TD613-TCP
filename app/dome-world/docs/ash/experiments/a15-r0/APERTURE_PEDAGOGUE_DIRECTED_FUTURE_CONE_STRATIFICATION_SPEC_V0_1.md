𝌋

# A15-R0 · Aperture × Pedagogue Directed Future-Cone Stratification Spec v0.1

**Status:** PREREGISTERED / PRE-IMPLEMENTATION / HUMAN-AUTHORIZED  
**Scientific parent:** #722 receipt head `6b69342b8eb65a01fe679f00738a37197ea0f260`  
**Program:** A15-R0 western-horizon directed research only  
**Date:** 2026-08-24

---

## 0. Human authority

Human 𝄐 remains:

```text
C it is. carry on mlove
```

This chamber continues the directed/non-groupoid lane only.

No inverse, quotient surgery, lattice, semilattice, transport, connection, holonomy, curvature, Proto-Loom, A16, live Ash, merge, production, or Vercel authority is granted.

---

## 1. Parent prerequisites

The executable must require success from:

```text
#722 bounded common-future leastness obstruction
#720 directed branching / confluence census
#719 directed reachability geometry
#718 monotone obstruction
```

Parent bounded observations:

```text
11 reconvergent forks
H4 minimal-frontier width = 3
H5 minimal-frontier width = 4
H6 minimal-frontier width = 5
H4/H5/H6 least-common-future count = 0
unique minimum-cost common future stable through H6
```

These are observations, not a recurrence law.

---

## 2. Prospective H7 extension

Before implementation freeze one new horizon:

```text
H7
```

For each of the eleven reconvergent forks enumerate all continuation words over `{T,Q}` of lengths zero through seven from each branch child:

```text
1 + 2 + 4 + 8 + 16 + 32 + 64 + 128 = 255 words per child
```

Construct exact complete-`K_period4` common-future objects and their bounded H7 reachability order using the same semantics as #722.

No H8+ search is permitted in this chamber.

H7 was selected prospectively to test whether the H4-H6 frontier pattern survives one genuinely new horizon.

---

## 3. H7 common-future and minimal-frontier census

For each reconvergent fork record:

```text
|U_H7|
minimal common-future count
least-common-future count
minimum total continuation cost
minimum-cost target identity
```

No H7 numeric result is preregistered.

If all eleven forks share the same H7 tuple, classify:

```text
RECONVERGENT_FORKS_SHARE_H7_FUTURE_CONE_PROFILE
```

Otherwise preserve the divergence.

---

## 4. Four-horizon count-pattern audition

After H7 is observed, compare H4-H7 only.

Audit the finite equalities:

```text
minimal_frontier_width(H) == H - 1
common_future_count(H) == C(H + 1, 3)
```

for each H in `{4,5,6,7}` and each reconvergent fork.

Only if all 44 horizon/fork cases satisfy an equality may emit:

```text
MINIMAL_FRONTIER_WIDTH_MATCHES_H_MINUS_ONE_THROUGH_H7
COMMON_FUTURE_COUNT_MATCHES_BINOMIAL_C_H_PLUS_1_CHOOSE_3_THROUGH_H7
```

These are finite pattern matches only.

Forbidden promotion:

```text
pattern through H7 -> formula for all H
```

No induction is performed in this chamber.

---

## 5. Minimal-frontier lineage stratification

For every minimal common future at H4-H7 record relative to the fork source:

```text
question-lineage increment
lineage-length increment
endpoint-mass gain
clock phase
forcing season
minimum continuation cost
minimum left route
minimum right route
```

Sort each frontier by question-lineage increment.

Audit whether the frontier question-lineage increments form a consecutive integer interval.

If witnessed, classify:

```text
MINIMAL_COMMON_FUTURE_FRONTIER_IS_CONSECUTIVELY_STRATIFIED_BY_QUESTION_LINEAGE_DEPTH
```

No causal interpretation is attached to the index.

---

## 6. Phase / season anchoring

For each fork and horizon audit whether every minimal target shares one common `(clock_phase, forcing_season)` pair.

Then compare that pair to the source:

```text
frontier clock phase vs source clock phase
frontier forcing season vs source forcing season advanced by two season steps
```

Allowed bounded classifications if witnessed:

```text
MINIMAL_FRONTIER_PRESERVES_SOURCE_PHASE
MINIMAL_FRONTIER_OCCUPIES_SOURCE_PLUS_TWO_FORCING_SEASON
```

No periodic-orbit or loop language follows.

---

## 7. Endpoint-coordinate stratification

Within each minimal frontier, sort by increasing question-lineage increment and compute endpoint differences between adjacent strata.

Audit whether:

```text
adjacent endpoint difference has exactly one nonzero coordinate
nonzero increment = +1
same coordinate repeats across every adjacency within that fork/horizon
```

Record the varying coordinate.

Compare it to the question-pulse coordinate of the **opposite clock phase** from the source.

If all cases agree, classify:

```text
MINIMAL_FRONTIER_STRATA_ADVANCE_ALONG_OPPOSITE_PHASE_QUESTION_COORDINATE
```

This is an authored coordinate relation, not physical orthogonality or curvature.

---

## 8. Route normal-form audition

For each minimal stratum identify minimum-length continuation words from both children.

Index strata by `k = 0,1,...` in increasing question-lineage increment.

Audit the finite route templates:

```text
left-child continuation  = Q^k T Q
right-child continuation = T Q^k T
```

where `Q^0` is empty.

Equivalently, source-rooted fork routes are:

```text
T Q^k T Q
Q T Q^k T
```

Only if every audited minimal stratum at H4-H7 matches may emit:

```text
MINIMAL_FRONTIER_ROUTE_NORMAL_FORM_MATCHES_T_QK_TQ_VS_Q_T_QK_T_THROUGH_H7
```

This is finite route syntax evidence only, not a rewrite theorem.

---

## 9. Cost / stratum relation

For each stratum index k audit:

```text
minimum total continuation cost = 2k + 4
```

If every case agrees through H7:

```text
MINIMUM_RECONVERGENCE_COST_MATCHES_AFFINE_STRATUM_INDEX_THROUGH_H7
```

Do not extrapolate beyond H7.

---

## 10. Antichain persistence

For every minimal frontier at H4-H7 require pairwise bounded incomparability under the corresponding horizon reachability relation.

Classification on success:

```text
MINIMAL_COMMON_FUTURE_FRONTIER_REMAINS_AN_ANTICHAIN_THROUGH_H7
```

Because adjacent strata can have different endpoint masses, this result must explicitly preserve:

```text
strictly different monotone height != comparability
```

The frontier antichain therefore cannot be explained by same-height coincidence alone.

---

## 11. Cross-fork normalization

Compare frontier descriptors after subtracting source-relative quantities:

```text
source endpoint mass
source question-lineage count
source season index
```

If all eleven fork profiles become identical under these relative coordinates, classify:

```text
RECONVERGENT_MINIMAL_FRONTIERS_COLLAPSE_TO_COMMON_SOURCE_RELATIVE_STRATIFICATION_PROFILE_THROUGH_H7
```

This is a bounded coordinate normalization, not gauge theory.

---

## 12. Parent custody

Serialize #722, #720, #719, and #718 executable outputs before and after the assay.

Require exact equality.

Classification:

```text
PARENT_718_719_720_722_CUSTODY_UNCHANGED
```

---

## 13. Success criterion

The assay succeeds if:

```text
all 11 reconvergent forks are recovered
H7 cone enumeration completes for both children of every fork
H7 common-future intersection is exact
H7 bounded reachability order passes required checks
minimal frontier is correctly extracted
H4-H7 stratification descriptors are computed without extrapolation
route/cost/coordinate pattern tests report witnessed truth rather than required positive outcomes
parent custody remains unchanged
```

Pattern survival is not itself pass/fail.

---

## 14. Claim ceiling

Even a maximally regular H4-H7 pattern does not earn:

```text
all-H recurrence theorem
mathematical induction conclusion
closed form beyond tested horizons
rewrite-system theorem
Church-Rosser
global confluence
ambient join / lattice / semilattice
domain theory
causal set
fiber bundle
gauge structure
transport
connection
loop endomorphism
holonomy
curvature
Berry / quantum
Proto-Loom
A16
live Ash
merge
production
Vercel
```

---

## 15. Stop

If successful:

```text
DIRECTED_FUTURE_CONE_STRATIFICATION_ROUND_CLOSED
HUMAN_𝄐_QUALIFIED_FOR_SYMBOLIC_RECURRENCE_AND_INDUCTIVE_PROOF_AUDITION
```

Scientific preference after a positive H7 pattern: stop horizon farming and ask whether the route/state transition grammar can prove the observed frontier recurrence symbolically for arbitrary finite horizon.

𝌋

Sealed ⟐
