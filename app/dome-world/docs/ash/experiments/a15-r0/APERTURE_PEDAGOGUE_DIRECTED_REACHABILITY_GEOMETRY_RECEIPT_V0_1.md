𝌋

# A15-R0 · Aperture × Pedagogue Directed Reachability Geometry Receipt v0.1

**Status:** WITNESSED / CLOSED / DRAFT-UNMERGED  
**Scientific parent:** #718 receipt head `05ae44861f1b7c1871928c9bdc8e0f730698e709`  
**Preregistration:** `67240104fb014265ade9268d340f0338a3b76291`  
**Implementation:** `a20fa097e93ec2ede6e23329e0b85989cab4c11c`  
**Hostile test:** `d91d896ba30f9a76a3da3d6b0bded0fbb1b2d621`  
**Pre-routing scientific head:** `108a15b39816bb07a2423013a2873fd9884631a4`  
**Exact witness head:** `139034f01d6d54d77064e37f41357bf6976cf534`  
**Routing-scaffold removal:** `e42061ba78ec68fa2b8fd0a1a84007c484abc2c9`  
**Program:** A15-R0 western-horizon bounded directed research only

---

## 0. Human authority

The chamber proceeded after the human selected the directed lane:

```text
C it is. carry on mlove
```

No reversible generator was introduced.

No coarser operational quotient was introduced.

The parent object remained:

```text
O(h) := K_period4(h)
```

with generators:

```text
T := PSI_TICK
Q := Q_PHASE_PULSE
```

---

## 1. Exact witness custody

Fresh main during temporary witness routing:

```text
4ca8c0600b40d0ea2b38c1e0dd0b2d1e77713aef
```

Exact witness head:

```text
139034f01d6d54d77064e37f41357bf6976cf534
```

Same-head workflow registrations:

```text
run 2086 / 32689280979 = CANCELLED
run 2087 / 32689291495 = SUCCESS
```

Run 2086 is preserved as workflow-registration / supersession provenance and is not classified as scientific failure.

Authoritative witness:

```text
TD613 Consolidated Validation
run number = 2087
run id = 32689291495
head = 139034f01d6d54d77064e37f41357bf6976cf534
conclusion = SUCCESS
```

Jobs:

```text
classifier job = 97319946489 = SUCCESS
static job = 97319981211 = SUCCESS
```

The static job's:

```text
Validate Ash A15 empirical profile journeys and A15-R0 research field
```

step completed `SUCCESS`.

Explicitly skipped and not claimed:

```text
Explicit self-hosted calibration
Explicit full-repository validation
Front-line exact-head browser shard
Giving/practice exact-head Chromium Firefox WebKit witness
Full-product exact-head Chromium Firefox WebKit witness
```

---

## 2. Three directed information layers were kept distinct

The chamber audited three separate structures:

```text
full finite S3 path category
    ↓ forget route identity
reachability relation
    ↓ retain only minimum directed path length
directed shortest-path distance
```

The assay preserved the anti-equivalences:

```text
path identity != endpoint reachability
reachability equality != route equality
directed distance equality != route equality
same height != same operational object
same height != mutual reachability
partial order != thin path category
directed quasi-metric != symmetric metric
```

No later layer was allowed to retroactively redefine the richer parent layer.

---

## 3. Reachability on S3 is a finite partial order

Define:

```text
A ≼ B  iff  at least one internal S3 arrow A -> B exists.
```

The executable exhaustively audited:

```text
reflexivity over every object
antisymmetry over every ordered object pair
transitivity over every ordered object triple for which the premise holds
```

Witness:

```text
reflexivity failures = []
antisymmetry failures = []
transitivity failures = []
```

Classification:

```text
FINITE_S3_REACHABILITY_IS_A_PARTIAL_ORDER
```

This is a finite authored S3 result only.

It is not an ambient TD613 partial-order theorem and not a causal-set theorem.

---

## 4. The parent S3 path category is thin on this audited slice

The chamber counted all internal category arrows by ordered endpoint pair.

Witnessed outcome:

```text
maximum endpoint-pair multiplicity = 1
multiple endpoint pairs = []
```

Classification:

```text
FINITE_S3_PATH_CATEGORY_IS_THIN_ON_AUDITED_SLICE
```

Therefore the preregistered route-multiplicity hostile was not instantiated in S3.

This matters because thinness was **measured**, not assumed from the existence of a partial order.

No ambient thinness theorem is earned.

---

## 5. Endpoint mass supplies strict height but not unit grading

The authored height remained:

```text
H(O) := M(O) - M(O_root)
```

where:

```text
M(O) := sum of endpoint entries.
```

Every S3 generator edge satisfied:

```text
ΔH > 0
```

so the #718 monotone obstruction and #717 finite slice are locally compatible.

However the stronger preregistered unit-grading condition failed:

```text
not every generator edge has ΔH = 1
not every internal arrow satisfies length = H(target) - H(source)
```

This follows from the declared forcing table itself: legal T steps at some forcing seasons and/or after Q_PHASE_PULSE add more than one unit of endpoint mass.

Classification:

```text
STRICT_MONOTONE_HEIGHT_ON_S3_WITHOUT_UNIT_GRADING_PROMOTION
```

Thus:

```text
path step count != endpoint-mass height gain
```

in the authored directed geometry.

This is a scientifically useful non-equivalence, not a defect.

---

## 6. Equal height produced antichain witnesses

The chamber searched distinct objects with equal authored height.

Such pairs exist.

For every audited same-height distinct pair:

```text
A does not reach B
B does not reach A
```

because any nonidentity admitted path would strictly increase endpoint mass.

Classification:

```text
SAME_HEIGHT_DISTINCT_OBJECTS_FORM_A_DIRECTED_ANTICHAIN_WITNESS
```

Immediate anti-equivalences:

```text
same scalar height != same object
same scalar height != same route class
same scalar height != mutual reachability
```

The simplest structural witness occurs already after one generator step from the root: the T-successor and Q-successor occupy the same endpoint-mass height while remaining distinct and incomparable operational objects.

---

## 7. Directed shortest-path distance earned bounded quasi-metric language

Define on S3:

```text
d→(A,B) = minimum internal arrow length A -> B
           +∞ when no internal arrow A -> B exists.
```

The executable audited:

```text
d→(A,A) = 0 for every A
all finite distances are nonnegative integers
triangle inequality over every ordered object triple
```

Witness:

```text
zero-law failures = []
finite-distance failures = []
triangle-inequality failures = []
```

Classification:

```text
FINITE_S3_EXTENDED_DIRECTED_SHORTEST_PATH_QUASIMETRIC
```

The chamber also witnessed at least one strict directional asymmetry:

```text
d→(A,B) < +∞
d→(B,A) = +∞
```

Classification:

```text
DIRECTED_DISTANCE_ASYMMETRY_WITNESSED
```

Therefore metric language remains forbidden.

```text
directed quasi-metric != symmetric metric
```

No Lawvere-enriched-category promotion is claimed.

---

## 8. Root distance and height are different coordinates

Every S3 object remained reachable from the canonical root because the slice itself was generated from root words.

But because unit grading failed:

```text
d→(root,O)
```

tracks minimum **step count**, while:

```text
H(O)
```

tracks accumulated endpoint-mass increase.

Some legal routes therefore satisfy:

```text
d→(root,O) != H(O)
```

The directed geometry now carries at least two distinct forward coordinates:

```text
combinatorial route length
monotone endpoint-mass height
```

Neither is allowed to erase the other.

---

## 9. Information-loss audit

Because the audited S3 path category is thin, there is no ordered endpoint pair containing two distinct path arrows.

Therefore:

```text
PATH_CATEGORY_THIN_DISTANCE_ROUTE_COLLAPSE_HOSTILE_NOT_INSTANTIATED
```

This does not establish that directed distance is globally route-faithful.

It only means this finite tree-like S3 slice supplies no parallel-arrow witness against it.

A future widened directed slice must rerun multiplicity before reusing this conclusion.

---

## 10. Parent custody remained unchanged

The executable serialized both parent assays before and after directed-geometry derivation:

```text
#717 finite path category
#718 monotone obstruction
```

Witness:

```text
parent_custody_unchanged = true
```

Classification:

```text
PARENT_717_718_CUSTODY_UNCHANGED
```

---

## 11. Canonical bounded classification

Because unit grading was not earned and the finite S3 path category was thin, the canonical bounded classification is:

```text
FINITE_S3_DIRECTED_REACHABILITY_PARTIAL_ORDER_WITH_STRICT_MONOTONE_HEIGHT_AND_EXTENDED_SHORTEST_PATH_QUASIMETRIC_SAME_HEIGHT_ANTICHAIN
```

Strongest bounded claim:

```text
IN_THE_AUTHORED_FINITE_S3_SLICE_EXISTENCE_OF_A_DIRECTED_PATH_DEFINES_A_PARTIAL_ORDER_ON_OPERATIONAL_OBJECTS_ENDPOINT_MASS_DIFFERENCE_SUPPLIES_A_STRICT_MONOTONE_BUT_NON_UNIT_HEIGHT_THE_INTERNAL_DIRECTED_SHORTEST_PATH_LENGTHS_SATISFY_ZERO_AND_TRIANGLE_LAWS_WITH_A_FINITE_TO_INFINITY_ASYMMETRY_WITNESS_SAME_HEIGHT_DISTINCT_OBJECTS_FORM_INCOMPARABLE_ANTICHAIN_WITNESSES_AND_THE_AUDITED_PARENT_PATH_CATEGORY_IS_THIN_ON_THIS_SLICE
```

---

## 12. Claim ceiling

This chamber does not earn:

```text
ambient TD613 partial order
causal-set theorem
generic poset representation theorem
generic Lyapunov theorem
symmetric metric geometry
Riemannian geometry
Finsler geometry
Lawvere enrichment
groupoid
inverse semantics
transport
connection
parallel transport
loop endomorphism
holonomy
curvature
Berry phase
quantum analogy
Proto-Loom
A16
live Ash
merge
production
Vercel
```

---

## 13. Scientific consequence

The C lane now has positive structure rather than merely a prohibition against inverses.

The finite S3 object world supports:

```text
a directed reachability order
+
a strict forward height
+
a directed shortest-path quasi-metric
```

while preserving:

```text
step count != height gain
same height != comparability
forward reachability != reverse reachability
```

The next scientifically admissible C-lane question is therefore not “can we make a loop?”

It is:

> When two lawful futures branch from the same operational object, under what conditions do they remain permanently divergent, become comparable later, or reconverge into a common future object without any inverse operation?

That is a directed branching / confluence problem.

---

## 14. Stop

```text
DIRECTED_REACHABILITY_GEOMETRY_ROUND_CLOSED
HUMAN_𝄐_QUALIFIED_FOR_DIRECTED_BRANCHING_AND_CONFLUENCE_AUDITION
```

𝌋

Sealed ⟐
