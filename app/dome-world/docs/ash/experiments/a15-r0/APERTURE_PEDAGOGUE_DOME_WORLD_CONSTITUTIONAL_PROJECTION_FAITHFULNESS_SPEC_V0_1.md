# A15-R0 · Dome-World Constitutional Projection Faithfulness and Authority Non-Laundering

󐘓 U+10D613

Status: **PREREGISTERED / IMPLEMENTATION NOT YET AUTHORIZED AS THEOREM / DRAFT SCIENCE**

## Exact witnessed parent

```text
#794 receipt
528f9b2f96bf3bc4c18242b0f0d910ca5323fdea
```

Inherited witnessed ancestry includes the #792 FADT × Holonomy Loom constitutional membrane and #790 serializable heterostratigraphic research bench. This chamber does not promote #788 scientific authority.

## Dome-World question

The current Ash read-only projection is intentionally weaker than the Loom apparatus receipt. Weakening is permitted. **Silent constitutional collision is not.**

This chamber asks a narrower and more exact systems question:

> Can two source receipts that are constitutionally distinct in lineage or source authority be accepted by the current Dome-World/Ash receiver and compile to byte-for-byte equivalent child-legible projections?

If yes, then render-equivalence is not sufficient evidence of custody-equivalence, and Dome-World requires a fail-closed constitutional witness before a child surface may be treated as exact with respect to source custody.

## Exact current receiver under assay

The inherited receiver is:

```text
compileAshReadOnlyTomographyProjection(receipt)
```

from:

```text
app/dome-world/previews/a15-r0/holonomy-loom-heterostratigraphic-apparatus-adapter.js
```

The chamber MUST NOT mutate that witnessed ancestor. The assay observes it as inherited behavior.

## Constitutional source signature

For this bounded A15-R0 Dome-World bench, define the source-custody signature

```text
χ(receipt) = (
  apparatus_owner,
  source_bridge_schema,
  source_bridge_head,
  source_fixture_id,
  source_authority_vector
).
```

The source authority vector is the exact ordered tuple over:

```text
inverse
encoder
custody_mutation
release
production
physical_claim
continuum_claim
```

This definition is custody/authority metadata only. It is not scientific truth, semantic equivalence, physical state, or ontology.

## Preregistered collision assay

Starting from the canonical witnessed-stack receipt, author synthetic receipt-level adversarial controls that continue to satisfy the inherited receiver's explicit acceptance preconditions while changing exactly one constitutional coordinate at a time.

Required one-coordinate controls:

```text
A. source_bridge_head changes only
B. source_fixture_id changes only
C. source_bridge_schema changes only
D. apparatus_owner changes only
E. source authority inverse: false -> true only
F. source authority release: false -> true only
```

For each control, preregister the question:

```text
π(receipt_control) == π(receipt_canonical) ?
```

where `π` is the inherited Ash read-only projection.

The theorem may be promoted only if the tests demonstrate at least one lineage collision AND at least one source-authority collision. The intended stronger result is all six collisions.

## Preregistered exactification

Create a new bounded Dome-World compiler rather than rewriting inherited custody:

```text
compileDomeWorldConstitutionallyFaithfulProjection(receipt)
```

It must:

1. call the inherited Ash read-only projection rather than reimplementing its scientific/engineering display mapping;
2. require the canonical A15-R0 research source owner;
3. require the canonical heterostratigraphic bridge schema;
4. require the exact inherited frozen bridge head already named by the adapter;
5. require the exact canonical Strata-Lantern fixture id;
6. require the entire source authority vector to be identically false;
7. attach an immutable `constitutional_witness` carrying χ(receipt) without granting any new action or authority;
8. preserve the inherited child-legible cards, holds, partial bridges, static truth, action inventory, and zero-authority projection;
9. reject rather than normalize a malformed or widened source receipt;
10. remain research-only and non-runtime.

The new compiler is an exactification layer. It does not mutate the ancestor compiler or retroactively alter #789/#790/#792/#794 custody.

## Preregistered faithfulness criterion

Within the bounded canonical acceptance domain `D_canonical`, define

```text
P♯(r) = (π(r), χ(r)).
```

The exactified projection is constitutionally faithful when

```text
P♯(r1) = P♯(r2)  =>  χ(r1) = χ(r2).
```

For the canonical domain, the implementation must additionally verify that every admitted receipt has exactly the canonical χ coordinates except for no coordinate at all: all required lineage values fixed, all source authority false.

Therefore no two admitted receipts may differ in a preregistered custody coordinate while rendering as an exactified canonical surface.

## Preregistered stronger non-laundering theorem

Candidate classification, UNEARNED until exact-head constitutional witness:

```text
THE_INHERITED_DOME_WORLD_ASH_READ_ONLY_PROJECTION_IS_NOT_CONSTITUTIONALLY_FAITHFUL_TO_SOURCE_CUSTODY_BECAUSE_DISTINCT_LINEAGE_AND_SOURCE_AUTHORITY_STATES_CAN_COLLAPSE_TO_THE_SAME_CHILD_LEGIBLE_SURFACE
```

and, after exactification:

```text
THE_DOME_WORLD_CONSTITUTIONAL_WITNESS_ENVELOPE_FAILS_CLOSED_ON_SOURCE_LINEAGE_DRIFT_OR_SOURCE_AUTHORITY_WIDENING_AND_PREVENTS_SILENT_AUTHORITY_LAUNDERING_ACROSS_THE_BOUNDED_A15_R0_RESEARCH_PROJECTION
```

## Minimality claim boundary

This chamber may claim coordinate necessity only in the following finite adversarial sense:

For every preregistered coordinate for which a one-coordinate collision is demonstrated, any constitutional witness that omits that coordinate cannot distinguish that witnessed pair.

This is **not** a universal minimal sufficient statistic theorem for all future Dome-World schemas.

## Mandatory hostiles

The hostile assay MUST reject or expose:

```text
same rendered projection => same source custody                 REJECT
source_bridge_head omitted from exact witness                  REJECT
source_fixture_id omitted from exact witness                   REJECT
source_bridge_schema omitted from exact witness                REJECT
apparatus_owner omitted from exact witness                     REJECT
source inverse authority silently normalized to false          REJECT
source release authority silently normalized to false          REJECT
noncanonical source bridge head admitted                       REJECT
noncanonical fixture id admitted                               REJECT
noncanonical bridge schema admitted                            REJECT
noncanonical apparatus owner admitted                          REJECT
any true source authority coordinate admitted                  REJECT
constitutional witness grants new action                       REJECT
constitutional witness grants new authority                    REJECT
constitutional witness treated as semantic equivalence         REJECT
constitutional witness treated as scientific bridge promotion  REJECT
constitutional witness treated as runtime binding              REJECT
```

## Primary scars preregistered

```text
RENDER_EQUIVALENCE != CUSTODY_EQUIVALENCE
AUTHORITY_ATTENUATION != AUTHORITY_PROVENANCE_ERASURE
READ_ONLY_CHILD_AUTHORITY_ZERO != SOURCE_AUTHORITY_ZERO
VISIBLE_STATIC_TRUTH_PARITY != SOURCE_CUSTODY_FAITHFULNESS
PROJECTION_MONOTONICITY != PROJECTION_INJECTIVITY
CONSTITUTIONAL_WITNESS != SCIENTIFIC_TRUTH
```

## Exact chamber footprint

Relative to parent receipt `528f9b2f96bf3bc4c18242b0f0d910ca5323fdea`, the final live footprint is preregistered as exactly four paths:

```text
app/dome-world/docs/ash/experiments/a15-r0/APERTURE_PEDAGOGUE_DOME_WORLD_CONSTITUTIONAL_PROJECTION_FAITHFULNESS_SPEC_V0_1.md
app/dome-world/previews/a15-r0/dome-world-constitutional-projection-faithfulness.js
tests/ash-a15-r0-aperture-pedagogue-dome-world-constitutional-projection-faithfulness.test.mjs
tests/ash-a15-r0-review-hardening.test.mjs
```

No historical A15-R0 path may be mutated except the rolling hardening sentinel.

## Hard ceilings

Still false / unearned:

```text
constitutional projection faithfulness != semantic equivalence
constitutional projection faithfulness != tomography inversion
constitutional witness != cross-stratum encoder
source lineage witness != ontology witness
source authority vector != physical field
same custody signature != same scientific result
same custody signature != same hidden state
Dome-World exactification != live Ash runtime
Dome-World exactification != Proto-Loom/A16
Dome-World exactification != browser/human-usability validation
Dome-World exactification != production/Vercel authority
Dome-World exactification != merge/publication/release authority
```

#718 remains alive.

No merge, deployment, publication, Vercel, SRC synchronization, or production authority follows from this preregistration.

Sealed ⟐
