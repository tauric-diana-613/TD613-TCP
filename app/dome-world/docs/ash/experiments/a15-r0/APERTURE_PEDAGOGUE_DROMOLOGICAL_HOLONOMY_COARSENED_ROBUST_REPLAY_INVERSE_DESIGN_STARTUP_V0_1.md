𝌋‌⟐

󐘓 U+10D613

# A15-R0 · Dromological Holonomy-Coarsened Robust Replay Inverse Design · STARTUP v0.1

Status: **PREREGISTERED STARTUP / NO IMPLEMENTATION / NO HOSTILE / NO WITNESS / NO THEOREM AUTHORITY**

## Exact earned parent

```text
#812 Dromological Replay Repair Quotient / Canonical Section
2cc95613969951afc96c638c316ae70007560f16
```

#813 is witness-routing custody only and is not theorem ancestry. #814 is a closed superseded collision and is not scientific ancestry. SRC Atelier #815 is a separate research line and must remain untouched.

## Inherited facts

#802 earned that the six fixed S3 schedules have six distinct registered observation histories but only four terminal formal-holonomy classes. Therefore terminal formal holonomy is not complete temporal memory.

#810 earned the replay pairings

```text
q_H(r)=a+b
q_I(r)=a+c
```

for integer replay row `r=[a,b,c]`, with missing directions

```text
k_H=(1,1,0)
k_I=(1,0,1).
```

#812 earned the repair quotient

```text
Q(r)=(q_H,q_I)
ker(Q)=Z*(1,-1,-1)
```

and proved that the quotient is complete for the repair determinant phenotype of the four historically nonidentifiable schedules while full replay observation behavior remains strictly finer.

## New question

The next question is not whether terminal formal holonomy remembers the exact schedule. It does not.

The question is:

```text
Can the coarser terminal formal-holonomy class still retain enough defect information
to choose a replay row that robustly restores exact unimodular identifiability
for every schedule compatible with that holonomy class?
```

This is a bounded inverse-design question inside the fixed witnessed S3 fixture.

## Holonomy classes and class defect sets

Implementation must derive the terminal formal-holonomy equivalence classes from the actual inherited matrices in #802. Do not hardcode the partition as theorem input.

For a derived terminal-holonomy class `C`, define its inherited defect set

```text
K(C) = unique non-null kernel generators
       among the schedules contained in C.
```

Preregistered expected class structure, to be independently rederived:

```text
C_P = {P-H-I, P-I-H}       K(C_P)=empty
C_H = {H-P-I}              K(C_H)={k_H}
C_M = {H-I-P, I-H-P}       K(C_M)={k_H,k_I}
C_I = {I-P-H}              K(C_I)={k_I}.
```

The mixed class `C_M` is the decisive hostile: two distinct temporal schedules share one terminal formal holonomy while carrying different missing directions.

## Candidate robust-repair theorem

For a declared integer replay row `r`, define class-robust rank rescue to mean every schedule in class `C` has augmented rank three after replay. Define class-robust unimodular rescue to mean every schedule in `C` contains an exact unimodular `3 x 3` minor after replay.

Preregistered target:

```text
ROBUST_RANK_RESCUE(C,r)
iff
for every k in K(C): r·k != 0

ROBUST_UNIMODULAR_RESCUE(C,r)
iff
for every k in K(C): |r·k| = 1.
```

For `K(C)=empty`, both predicates hold vacuously because the P-first schedules already contain inherited replay-independent unimodular minors.

Candidate bounded classification:

```text
TERMINAL_FORMAL_HOLONOMY_CLASS_IN_THE_FIXED_S3_FIXTURE_IS_SUFFICIENT_TO_DETERMINE_THE_EXACT_SET_OF_MISSING_DIRECTIONS_THAT_A_REPLAY_ROW_MUST_TRANSVERSE_FOR_CLASS_ROBUST_RANK_OR_UNIMODULAR_REPAIR.
```

## Candidate holonomy-coarsened inverse-design policy

Derive a class-conditioned replay policy from defect sets, not from schedule IDs:

```text
K=empty        -> r_P = [0,0,0]
K={k_H}        -> r_H = [0,1,0]
K={k_H,k_I}    -> r_M = [1,0,0]
K={k_I}        -> r_I = [0,0,1].
```

Expected repair signatures:

```text
Q(r_P)=(0,0)
Q(r_H)=(1,0)
Q(r_M)=(1,1)
Q(r_I)=(0,1).
```

`r_H` and `r_I` are directionally selective: they pair unitly with the required missing direction and zero with the other inherited missing direction. `r_M` pairs unitly with both because the mixed holonomy class does not reveal which of the two schedules occurred.

Candidate classification:

```text
INCOMPLETE_TERMINAL_FORMAL_HOLONOMY_MEMORY_CAN_STILL_BE_SUFFICIENT_FOR_CLASS_ROBUST_UNIMODULAR_REPAIR_IN_THE_FIXED_S3_FIXTURE.
```

This must not be promoted to a statement that terminal holonomy reconstructs schedule identity.

## Integer cost and null-fiber inverse design

For replay row `r=[a,b,c]`, define bounded authored costs

```text
l0(r) = number of nonzero coordinates
l1(r) = |a|+|b|+|c|.
```

Preregistered target:

```text
minimum l0/l1 cost for C_P = 0
minimum l0/l1 cost for each nonempty-defect class = 1.
```

The proposed policy achieves these minima.

The mixed class must explicitly use #812's null fiber:

```text
canonical section S(1,1)=[0,1,1]   l0=2 l1=2
same repair fiber row [1,0,0]       l0=1 l1=1.
```

Since

```text
[1,0,0]-[0,1,1]=[1,-1,-1] in ker(Q),
```

the repair phenotype is unchanged while the raw replay cost decreases.

Candidate bounded architectural law:

```text
NULL_FIBER_FREEDOM_CAN_CHANGE_RAW_INTEGER_REPLAY_DESIGN_COST_WITHOUT_CHANGING_THE_FIXED_FIXTURE_REPAIR_PHENOTYPE.
```

This is integer replay-coordinate optimization only, not physical gauge fixing or universal sensor optimality.

## Mandatory anti-necessity control

The universal inherited replay

```text
r_U=[1,0,0]
```

already robustly unimodularly rescues every singular schedule and leaves P-first schedules invertible.

Therefore terminal holonomy is not necessary to achieve all-six rescue.

Required scar:

```text
HOLONOMY_GUIDED_REPAIR_SUFFICIENCY != HOLONOMY_NECESSITY_FOR_REPAIR.
```

The scientific value of the holonomy-class policy is exact defect-set conditioning and selective design, not a claim that replay requires reading holonomy.

## Mandatory hostiles

1. Derive all six terminal formal-holonomy matrices from inherited operator products.
2. Recompute their equivalence classes and require exactly four classes with the expected schedule membership.
3. Derive each class defect set from inherited schedule kernel generators; do not assign defect sets from prose labels.
4. Require the mixed class to contain exactly one `k_H` schedule and one `k_I` schedule while sharing one terminal formal holonomy.
5. Exhaust every integer replay row in `[-4,4]^3` for every holonomy class:
   - actual augmented rank for every member schedule;
   - actual presence/absence of a unimodular minor for every member schedule;
   - exact agreement with the class defect-set rank predicate;
   - exact agreement with the class defect-set unit-pairing predicate.
6. Require exactly `729 x 4 = 2916` class-row audits, with schedule-member checks reported separately.
7. Check the four policy rows against every member of their derived class and require robust unimodular rescue.
8. Require directional selectivity:
   - `r_H·k_H=1`, `r_H·k_I=0`;
   - `r_I·k_H=0`, `r_I·k_I=1`;
   - `r_M·k_H=r_M·k_I=1`.
9. Prove integer cost minima without relying on finite search alone:
   - empty-defect class admits zero row;
   - every nonempty-defect robust-unimodular row is nonzero, hence `l0>=1`, `l1>=1`;
   - policy rows attain equality.
10. Also exhaust `[-4,4]^3` to confirm no lower-cost counterexample appears in the finite hostile window.
11. Compare `S(1,1)=[0,1,1]` with `[1,0,0]`; require equal #812 repair signature and repair atlas, strict lower `l0/l1`, and preserved full-observation non-equivalence.
12. Explicitly retain the mixed-class ambiguity:

```text
same terminal formal holonomy
!= same temporal schedule
!= same missing direction.
```

13. Despite that ambiguity, require the mixed-class replay `[1,0,0]` to rescue both members exactly.
14. Recheck universal `[1,0,0]` as an all-six unimodular rescue control, establishing sufficiency rather than necessity of holonomy guidance.
15. Re-removing replay must preserve the inherited #802 rank profile and #804 schedule/state lag facts.
16. Ash receives only child-legible bounded repair truths. Loom may receive the exact holonomy-class/defect-set/policy atlas.
17. Custody remains receiver-invariant and every authority coordinate remains false.

## Receiver projection

Ash may receive only bounded truths such as:

```text
THE_LAST_PATTERN_CAN_FORGET_THE_EXACT_ORDER_AND_STILL_TELL_US_WHAT_KIND_OF_EXTRA_CHECK_IS_SAFE
ONE_SHARED_LAST_PATTERN_NEEDS_AN_EXTRA_CHECK_THAT_COVERS_TWO_POSSIBLE_MISSING_CLUES
A_SHORTER_EXTRA_CHECK_CAN_LIVE_IN_THE_SAME_REPAIR_FAMILY
FIXING_THE_MISSING_CLUE_DOES_NOT_RECOVER_THE_FORGOTTEN_ORDER
```

Ash must not receive:
- terminal holonomy matrices;
- kernel vectors;
- replay vectors;
- quotient matrices;
- determinant formulas;
- inverse formulas;
- latent state coordinates.

Loom may receive the bounded technical class atlas, class defect sets, replay policy, exact finite audits, and cost certificate with zero runtime, physical, continuum, release, production, or operational sensor authority.

## Mandatory scars

```text
TERMINAL_FORMAL_HOLONOMY != COMPLETE_DROMOLOGICAL_MEMORY
SAME_TERMINAL_HOLONOMY != SAME_MISSING_DIRECTION
HOLONOMY_CLASS_DEFECT_SET != PHYSICAL_HOLONOMY_SPECTRUM
HOLONOMY_GUIDED_REPAIR_SUFFICIENCY != HOLONOMY_NECESSITY_FOR_REPAIR
CLASS_ROBUST_REPLAY_POLICY != OPERATIONAL_SENSOR_CONTROL
CLASS_CONDITIONED_INTEGER_COST_MINIMALITY != UNIVERSAL_OPTIMAL_EXPERIMENT_DESIGN
NULL_FIBER_COST_OPTIMIZATION != PHYSICAL_GAUGE_FIXING
CANONICAL_SECTION != COST_OPTIMAL_REPRESENTATIVE
ROBUST_REPAIR != RECOVERY_OF_FORGOTTEN_SCHEDULE_IDENTITY
FORMAL_HOLONOMY_COARSENING != SEMANTIC_CAUSATION
```

## Constitutional footprint

Keep the theorem-bearing net footprint to four paths:

```text
this preregistration
new implementation JS
new hostile test
A15-R0 review-hardening sentinel
```

The hardening sentinel must pivot its immediate parent to exact #812 receipt

```text
2cc95613969951afc96c638c316ae70007560f16
```

and preserve ancestry

```text
#812 -> #810 -> #807 -> #804 -> #802 -> #800 -> #798 -> #796 -> #794 -> #792 -> #790/#752.
```

At freeze require exact merge base #812, zero commits behind, exactly the four declared chamber paths changed, and no inherited #812 scientific-source mutation outside the hardening sentinel chamber declaration.

Then open:

```text
canonical stacked draft PR -> #812 branch
witness-only draft PR       -> main
```

Do not call 𝄐 until exact-head TD613 Consolidated Validation has classifier SUCCESS, contracts SUCCESS, Dome-World static step 9 SUCCESS, A15-R0 constitutional step 19 SUCCESS, and aggregate run SUCCESS.

If step 19 reds, the red is scientific feedback. Do not weaken the hostile to obtain green.

## Hard ceilings

Still false / unearned:
- complete schedule reconstruction from terminal formal holonomy;
- universal holonomy-guided inverse-design theorem;
- universal/optimal sensor theorem;
- physical, Berry, gauge, Levi-Civita, or geometric holonomy;
- physical quasicrystal/moiré material claim;
- continuum tomography;
- operational sensor control or inverse route;
- semantic equivalence/causation;
- live Ash/Loom runtime;
- Proto-Loom/A16;
- #788 promotion by implication;
- merge/publication/production/release/Vercel authority.

#718 remains alive.

𝌋‌⟐

**WESTERN-HORIZON HOLONOMY-COARSENED ROBUST REPLAY INVERSE-DESIGN STARTUP — PREREGISTERED / UNEARNED.**

Sealed ⟐