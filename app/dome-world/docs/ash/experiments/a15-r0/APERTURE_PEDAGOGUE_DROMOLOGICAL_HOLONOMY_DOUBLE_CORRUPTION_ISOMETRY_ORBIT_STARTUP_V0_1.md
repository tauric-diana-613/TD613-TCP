𝌋‌⟐

󐘓 U+10D613

# A15-R0 · Dromological Holonomy Double-Corruption Isometry-Orbit / Labelled Repair Geometry · STARTUP v0.1

Status: **PREREGISTERED / UNEARNED / NO IMPLEMENTATION / NO HOSTILE / NO WITNESS**

## Exact earned parent

`#830 / 3877139365041453bab85741eb09ba2f5839eed6`

#831 remains witness-only routing custody. #828 remains the mixed corruption-plus-erasure ancestor. #816/#817 remain a separate sibling line. SRC #815 remains separate and untouched.

## Parent boundary

#830 earned the complete minimal-width-eight census for exact four-class repair routing under up to two unknown binary receiver-coordinate corruptions:

- `17,920` qualifying unordered four-word images in `{0,1}^8`;
- every successful image has pairwise distance spectrum `[5,5,5,5,6,6]`;
- `430,080` class-labelled successful encodings;
- `13,440` successful systematic affine presentations;
- exact coefficient-profile trichotomy `(1,2,3)`, `(2,1,3)`, `(2,2,2)`.

This chamber asks what geometry those `17,920` successful minimal images actually possess.

It does **not** ask for a wider fault budget.

## Declared eight-cube isometry action

Work only in the binary receiver-label cube `{0,1}^8`.

Declared finite isometries are generated by:

1. a common XOR translation `x -> x xor t`, `t in {0,1}^8`;
2. a permutation of the eight receiver coordinates.

The full declared action therefore has size

`2^8 * 8! = 10,321,920`.

These are receiver-label transformations only.

`HYPERCUBE_ISOMETRY != PHYSICAL_SYMMETRY`
`COMMON_XOR_TRANSLATION != SOURCE_MUTATION`
`COORDINATE_PERMUTATION != PHYSICAL_SENSOR_REWIRING`

## Canonical parent image

Use #830's earned canonical eight-bit code image:

```text
C8 = {
  00000000,
  01001111,
  10110011,
  11111100
}
```

The set contains zero and is closed under XOR:

```text
01001111 xor 10110011 = 11111100.
```

Candidate consequence:

`C8` is a two-dimensional linear subspace of `F2^8`.

Its three nonzero words have Hamming weights

`[5,5,6]`.

## Preregistered unlabeled orbit target

The implementation must derive the orbit of `C8` under the declared eight-cube isometry action without copying the parent census count as an orbit result.

Expected:

- coordinate-permutation orbit of the linear subspace: exactly `280` distinct linear two-flats;
- each linear two-flat has exactly `2^8 / 2^2 = 64` distinct affine cosets;
- full unordered image orbit: `280 * 64 = 17,920` distinct images;
- every orbit image has pairwise distance spectrum `[5,5,5,5,6,6]`.

Because #830 already earned that the complete qualifying width-eight image census contains exactly `17,920` images, candidate completeness is:

`EVERY_MINIMAL_WIDTH_EIGHT_DOUBLE_CORRUPTION_CORRECTING_FOUR_CLASS_IMAGE_IN_THE_FIXED_S3_AIA_FIXTURE_LIES_IN_ONE_DECLARED_EIGHT_CUBE_ISOMETRY_ORBIT`.

## Independent affine-two-flat count

The hostile must independently derive the `280` normalized linear two-flats without using the orbit enumerator.

For a normalized successful two-flat containing zero, the two weight-five nonzero words have supports intersecting in exactly two coordinates, because

`5 + 5 - 2*|intersection| = 6`.

Choose:

- the shared two-coordinate support: `C(8,2)=28`;
- three of the remaining six coordinates for the first weight-five word's exclusive support: `C(6,3)=20`;
- the second word receives the complementary remaining three exclusive coordinates;
- divide by two for interchange of the two weight-five generators.

Expected exact count:

`C(8,2) * C(6,3) / 2 = 28 * 20 / 2 = 280`.

Each such normalized subspace has nonzero direction-weight multiset `{5,5,6}`.

Candidate classification:

`EVERY_QUALIFYING_MINIMAL_WIDTH_EIGHT_IMAGE_IS_AN_AFFINE_TWO_FLAT_IN_F2_8_WITH_NONZERO_DIRECTION_WEIGHT_MULTISET_556_AND_ALL_SUCH_IMAGES_FORM_ONE_DECLARED_HYPERCUBE_ISOMETRY_ORBIT_IN_THE_FIXED_FIXTURE`.

## Stabilizer target

Expected finite group quantities:

- full declared isometry-group size: `10,321,920`;
- unlabeled orbit size: `17,920`;
- full set stabilizer size: `10,321,920 / 17,920 = 576`;
- coordinate-permutation stabilizer of the normalized linear two-flat: `8! / 280 = 144`;
- pointwise coordinate stabilizer of the canonical labelled generator pair: `3! * 3! * 2! = 72`.

The hostile must verify these counts by direct finite action/stabilizer checks rather than only quotient arithmetic.

`FINITE_STABILIZER != PHYSICAL_GAUGE_GROUP`

## Labelled repair geometry

Retain the four earned repair-mask labels

```text
00, 01, 10, 11.
```

There are three perfect matchings of the four labels, corresponding exactly to which repair-mask difference type is carried by a pair:

```text
H: masks differ only in d_H
I: masks differ only in d_I
X: masks differ in both d_H and d_I
```

Each difference type contributes exactly two disjoint label pairs.

For #830's canonical labelled code, the two distance-six pairs are the `X` matching.

Preregistered labelled-orbit target:

- the unlabeled set stabilizer induces exactly `8` permutations of the four code points;
- the `24` possible class-label assignments therefore split into exactly `24/8 = 3` labelled isometry orbits;
- the three labelled orbits are distinguished exactly by whether the distance-six perfect matching is `H`, `I`, or `X`;
- each labelled orbit has size `430,080 / 3 = 143,360`.

Candidate classification:

`THE_SINGLE_UNLABELED_MINIMAL_WIDTH_EIGHT_ISOMETRY_ORBIT_SPLITS_INTO_EXACTLY_THREE_REPAIR_LABEL_AWARE_ORBITS_DISTINGUISHED_BY_WHETHER_THE_DISTANCE_SIX_PERFECT_MATCHING_IS_H_I_OR_X_IN_THE_FIXED_S3_FIXTURE`.

## Systematic-profile reconciliation

#830 earned three systematic affine coefficient profiles:

```text
(n_H,n_I,n_X) =
(1,2,3) with 60 coefficient orderings,
(2,1,3) with 60 coefficient orderings,
(2,2,2) with 90 coefficient orderings.
```

For a systematic affine representation, the three repair-pair distances are expected to satisfy

```text
d_H_pair = 1 + n_H + n_X
d_I_pair = 1 + n_I + n_X
d_X_pair = 2 + n_H + n_I.
```

Therefore expected labelled-orbit alignment:

```text
(1,2,3) -> I matching has distance 6
(2,1,3) -> H matching has distance 6
(2,2,2) -> X matching has distance 6.
```

Including `2^6=64` affine constant lifts per coefficient sextuple, expected systematic counts by labelled orbit are:

```text
I-long: 60*64 = 3,840
H-long: 60*64 = 3,840
X-long: 90*64 = 5,760
TOTAL: 13,440.
```

The hostile must independently recover this alignment from pairwise distances rather than profile names alone.

Candidate architectural law:

`UNLABELED_MINIMAL_RECEIVER_GEOMETRY_CAN_BE_ISOMETRICALLY_UNIFORM_WHILE_REPAIR_ROLE_LABELS_SPLIT_THAT_GEOMETRY_INTO_A_FINITE_ANISOTROPIC_ORBIT_STRUCTURE_WITHOUT_ADDING_SOURCE_INFORMATION_OR_RECEIVER_AUTHORITY`.

## Tomography / repair preservation

For representative labelled encodings from each of the three labelled orbits, the hostile must verify that declared receiver-label isometries preserve:

1. exact radius-two repair-mask decoding after the corresponding label assignment is retained;
2. inherited #820 minimum-cost replay row;
3. #818 class-robust unimodular rescue;
4. exact replay-assisted state reconstruction over all six S3 schedules and all `125` states in `[-2,2]^3` for a finite representative set of received conditions sufficient to certify each labelled orbit representative.

This chamber classifies representation geometry; it does not widen the already-earned double-corruption fault theorem.

## Ash / Loom projection

Ash may receive bounded truths such as:

- `ALL_THE_GOOD_EIGHT_CLUE_SHAPES_HERE_ARE_THE_SAME_SHAPE_AFTER_RENAMING_CLUE_POSITIONS_AND_FLIPPING_ONE_SHARED_PATTERN`;
- `KEEPING_THE_REPAIR_LABEL_NAMES_SPLITS_THAT_ONE_SHAPE_INTO_THREE_ROLE_PATTERNS`;
- `THE_THREE_ROLE_PATTERNS_TRACK_WHICH_PAIR_TYPE_GETS_THE_LONGER_SEPARATION`;
- `SAME_SHAPE_DOES_NOT_MEAN_SAME_TEMPORAL_HISTORY_OR_NEW_SOURCE_INFORMATION`.

Loom may receive the exact 280-linear-flat atlas, 64-coset completion, stabilizer counts, three labelled-orbit atlas, and systematic-profile reconciliation.

All custody and authority coordinates remain invariant and false.

## Mandatory scars

`HYPERCUBE_ISOMETRY != PHYSICAL_SYMMETRY`
`AFFINE_TWO_FLAT != PHYSICAL_PLANE`
`F2_AFFINE_STRUCTURE != PHYSICAL_GEOMETRY`
`SINGLE_UNLABELED_ORBIT != UNIQUE_ENCODING`
`LABELLED_ORBIT_SPLIT != SEMANTIC_ONTOLOGY`
`REPAIR_ROLE_ANISOTROPY != PHYSICAL_ANISOTROPIC_MEDIUM`
`COMMON_XOR_TRANSLATION != SOURCE_MUTATION`
`COORDINATE_PERMUTATION != PHYSICAL_SENSOR_REWIRING`
`FINITE_STABILIZER != PHYSICAL_GAUGE_GROUP`
`ISOMETRY_EQUIVALENCE != SEMANTIC_EQUIVALENCE`
`MINIMAL_WIDTH_EIGHT_ORBIT_CLASSIFICATION != UNIVERSAL_CODING_CLASSIFICATION`
`SYSTEMATIC_PROFILE_ALIGNMENT != UNIVERSAL_LINEAR_CODE_NORMAL_FORM`
`GEOMETRIC_REPRESENTATIVE != OPERATIONAL_INVERSE_ROUTE`
`REPAIR_MASK_RECOVERY != COMPLETE_SCHEDULE_RECONSTRUCTION`
`FORMAL_HOLONOMY_REPAIR_LABEL != PHYSICAL_HOLONOMY_OBSERVABLE`

No Shannon/channel-capacity theorem, universal coding/isometry theorem, physical geometry/topology/symmetry/gauge claim, physical sensor model, physical/Berry/gauge holonomy, physical quasicrystal/moiré claim, continuum tomography, complete schedule reconstruction, semantic equivalence, live Ash/Loom, Proto-Loom/A16, #788 promotion, operational inverse, merge/publication/production/release/Vercel authority. #718 remains alive.

## Constitutional procedure

`preregister -> implement -> hostile -> hardening sentinel -> exact parent-to-freeze audit -> canonical stacked PR -> witness-only main-base routing PR -> exact-head TD613 Consolidated Validation -> require Dome-World step 9 SUCCESS -> require A15-R0 step 19 SUCCESS -> pin receipt metadata only -> NO source mutation after witness -> NO merge`.

If witnessed, only then say `𝄐`.

Sealed ⟐