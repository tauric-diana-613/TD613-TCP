𝌋‌⟐

󐘓 U+10D613

# A15-R0 · Dromological Holonomy Orbit-Transport Tomographic Conjugacy AIA · STARTUP v0.1

Status: **PREREGISTRATION ONLY / NOT EARNED / NO IMPLEMENTATION / NO WITNESS / NO MERGE**.

## Exact scientific parent

```text
#834 · Dromological Holonomy Double-Corruption Isometry-Orbit / Labelled Repair Geometry
receipt a0d88e26860f4d9c25feed21ab2d080f70b45f20
run 2351 / 33115591906 SUCCESS
```

This startup branches directly from the exact #834 earned receipt.

The Future-Amari handoff branch/PR is custodial continuity only and MUST NOT become theorem ancestry.

`HANDOFF_BRANCH != THEOREM_PARENT`

---

## 1. Scientific question

#834 earned all of the following in the fixed width-eight / four-repair-class fixture:

```text
17,920 successful unlabeled minimal receiver images
one declared unlabeled affine-two-flat hypercube-isometry orbit
24 repair-label assignments per image
430,080 successful class-labelled encodings
three H/I/X labelled isometry orbits
143,360 labelled encodings per H/I/X orbit
```

It also established representative radius-two repair/tomography closure for one labelled representative from each H/I/X orbit.

The next question is not whether the orbit classification exists. It is:

> Can exact repair decoding and replay-assisted tomography be transported through the entire declared labelled receiver-isometry orbit by explicit finite representation witnesses, while leaving the source state untouched and preserving the H/I/X labelled-orbit distinction?

This is a representation-layer transport problem only.

---

## 2. Declared action inherited from #834

Declared receiver-label isometries are generated by:

```text
1. common XOR translation in F2^8;
2. coordinate permutation in S8.
```

Inherited exact group size:

```text
|G| = 2^8 * 8! = 10,321,920.
```

#834 earned:

```text
full unlabeled set stabilizer = 576
pointwise coordinate stabilizer for a labelled representative = 72
unlabeled orbit size = 17,920
labelled orbit size = 143,360
three labelled orbits H/I/X
```

Do not import any physical interpretation into this action.

---

## 3. Candidate transport theorem

For each successful labelled width-eight encoding `E`, define its earned labelled orbit type

```text
T(E) in {H,I,X}.
```

Choose one inherited canonical representative

```text
R_H, R_I, R_X
```

from #834.

Candidate transport claim:

```text
For every E there exists a declared receiver-label isometry g_E
such that g_E(R_T(E)) = E,
and exact radius-two repair decoding commutes with g_E.
```

The transported receiver word may be pulled back to the canonical representative, decoded there, and its repair label pushed through the induced codepoint action.

The source latent state is never acted on by `g_E`.

---

## 4. Required finite transport census

The implementation MUST derive, not assume, a canonical transport witness for every successful labelled encoding.

Recommended normal form:

```text
for each target labelled encoding E:
  enumerate or derive all declared isometries mapping R_T(E) to E;
  choose the lexicographically least exact witness (translation, permutation);
  record T(E), witness, induced codepoint action, and target encoding identity.
```

Required exact coverage:

```text
H targets = 143,360
I targets = 143,360
X targets = 143,360
TOTAL = 430,080
```

Because the labelled stabilizer size is inherited as 72, a stronger multiplicity audit is encouraged:

```text
each labelled target has exactly 72 group elements mapping its representative to that target.
```

If this is computationally too expensive in a direct `3 * |G|` sweep, an exact stabilizer/coset derivation is acceptable only if independently hostile-checked against explicit target witnesses.

No asymptotic argument may replace the finite coverage certificate.

---

## 5. Decoder commutation assay

For each canonical labelled representative there are exactly

```text
4 codewords * (C(8,0) + C(8,1) + C(8,2))
= 4 * (1 + 8 + 28)
= 148
```

radius-zero/one/two received conditions.

Across H/I/X representatives:

```text
3 * 148 = 444
```

canonical received conditions.

For every canonical received condition `r` and a preregistered transport witness `g`:

```text
decode_E(g(r))
```

must equal the repair label obtained by

```text
r
-> canonical decode in R_T
-> induced codepoint / repair-label action under g.
```

Required properties:

```text
Hamming radius preserved exactly;
unique-nearest decoding preserved exactly;
repair-mask identity transported exactly;
H/I/X labelled-orbit role preserved unless an explicit label action says otherwise;
no source-state coordinate is transformed.
```

The implementation should exploit the isometry theorem rather than execute all

```text
430,080 * 148 = 63,651,840
```

received conditions as a brute-force reconstruction campaign.

That cardinality may be recorded as a represented cross-product, but it MUST NOT be claimed as executed unless actually executed.

---

## 6. Replay-policy compatibility

For every transported repair mask recovered through the commutation assay:

1. decode the inherited minimum-cost replay row;
2. require exact equality with #818/#820 policy for that repair mask;
3. require class-robust unimodular rescue under the inherited formal-holonomy repair class;
4. preserve the mixed terminal-formal-holonomy schedule ambiguity.

Required scar:

`TRANSPORTED_REPAIR_MASK != COMPLETE_SCHEDULE_IDENTITY`

---

## 7. Tomographic conjugacy assay

The state layer MUST remain downstream of receiver representation transport.

Candidate commutative factorization:

```text
received target word
-> inverse receiver transport
-> canonical representative word
-> canonical repair decode
-> induced repair-label transport
-> inherited replay row
-> replay-assisted state reconstruction
```

versus direct target-code decoding followed by the same inherited replay policy.

The reconstructed state must agree exactly.

The receiver isometry acts on receiver representation only. It MUST NOT act on:

```text
latent state coordinates;
physical space;
source custody;
formal schedule history;
raw holonomy observations.
```

Recommended executed tomography burden:

```text
retain the #834 three labelled representatives;
all 444 radius-two representative received conditions;
all compatible S3 schedules;
all 125 states in [-2,2]^3;
plus a preregistered finite stabilizer/coset sample of nontrivial transport witnesses.
```

A future implementation must state the exact executed reconstruction count before witness.

Do not silently multiply the represented `430,080` target space by every state and call it executed work.

---

## 8. Required hostile battery

At minimum:

1. **Coverage hostile** — remove one target encoding from the transport atlas and require failure.
2. **Wrong-orbit hostile** — attempt to transport an H-labelled target from the I or X representative without the required label action; require rejection.
3. **Non-isometry hostile** — use a bit map that does not preserve Hamming distance; require rejection.
4. **Translation-only hostile** — show common XOR translation alone is insufficient to generate all targets.
5. **Permutation-only hostile** — show coordinate permutation alone is insufficient to generate all affine cosets.
6. **Label-collapse hostile** — identify H/I/X as one labelled class; require failure even though unlabeled geometry is isometric.
7. **Source-state mutation hostile** — any candidate that transforms latent state coordinates is rejected.
8. **Schedule-completion hostile** — transported repair success must not claim recovery of the mixed class's forgotten schedule order.
9. **Full-cross-product inflation hostile** — reject metadata claiming all 63,651,840 receiver conditions or any larger state cross-product were executed unless the implementation really executes them.
10. **Authority hostile** — all authority coordinates remain false; Ash remains child-legible and does not receive orbit/stabilizer/replay/latent-state internals.

---

## 9. Candidate finite law

Do not promote until exact-head witnessed:

```text
EXACT_REPAIR_DECODING_AND_REPLAY_ASSISTED_TOMOGRAPHY_CAN_BE_TRANSPORTED_ACROSS_THE_DECLARED_MINIMAL_WIDTH_EIGHT_LABELLED_RECEIVER_ISOMETRY_ORBITS_BY_EXPLICIT_FINITE_REPRESENTATION_WITNESSES_WITHOUT_ACTING_ON_THE_SOURCE_STATE_OR_WIDENING_RECEIVER_AUTHORITY_IN_THE_FIXED_S3_AIA_FIXTURE
```

---

## 10. Mandatory scars

```text
ORBIT_TRANSPORT != PHYSICAL_MOTION
HYPERCUBE_ISOMETRY != PHYSICAL_SYMMETRY
RECEIVER_CONJUGACY != GAUGE_EQUIVALENCE
CODEWORD_TRANSLATION != SOURCE_STATE_TRANSLATION
COORDINATE_PERMUTATION != SENSOR_REWIRING
LABEL_ACTION != SEMANTIC_ONTOLOGY
TOMOGRAPHY_EQUIVARIANCE_IN_THIS_FIXTURE != CONTINUUM_TOMOGRAPHY_COVARIANCE
FINITE_TRANSPORT_WITNESS != UNIVERSAL_CODING_EQUIVALENCE
ORBIT_REPRESENTATIVE_REDUCTION != EXECUTED_FULL_430080_CROSS_PRODUCT
REPAIR_MASK_TRANSPORT != COMPLETE_SCHEDULE_RECONSTRUCTION
REPRESENTED_63651840_RECEIVER_CASES != EXECUTED_63651840_RECEIVER_CASES
HANDOFF_BRANCH != THEOREM_PARENT
```

No universal coding/isometry theorem, physical geometry/topology/symmetry/gauge claim, physical sensor model, physical/Berry/gauge holonomy, physical quasicrystal/moiré claim, continuum tomography, semantic equivalence, live Ash/Loom, Proto-Loom/A16, #788 promotion, operational inverse, merge/publication/production/release/Vercel authority. #718 remains alive.

---

## 11. Constitutional startup procedure

Future Amari must:

1. use Remembering on the prior Western thread;
2. verify #834 exact receipt and run 2351 against GitHub;
3. read the v0.2 Future-Amari handoff and receiving manifest;
4. collision-scan all PRs/branches newer than this startup;
5. independently rederive transport counts before implementation;
6. commit preregistration before any theorem code;
7. implement finite certificate;
8. add independent hostile;
9. pivot rolling hardening only after theorem/hostile are fixed;
10. exact parent-audit against #834;
11. open witness-only `main` routing PR;
12. call 𝄐 only after exact-head classifier, Dome step 9, A15-R0 step 19, downstream contracts, and aggregate all succeed.

`STARTUP_READY = TRUE`

`THEOREM_EARNED = FALSE`

Sealed ⟐
