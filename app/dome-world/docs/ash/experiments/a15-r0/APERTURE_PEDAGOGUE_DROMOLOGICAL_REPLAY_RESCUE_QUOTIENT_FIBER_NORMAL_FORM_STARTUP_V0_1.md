𝌋‌⟐

󐘓 U+10D613

# A15-R0 · Dromological Replay-Rescue Quotient / Fiber Normal Form · STARTUP v0.1

Status: **PREREGISTERED STARTUP / NO IMPLEMENTATION / NO HOSTILE / NO WITNESS / NO THEOREM AUTHORITY**

## Exact earned parent

```text
#810 Dromological Replay Transversality / Unimodular Rescue Locus
79a6533843c4133345bec3c1e83477c621230b09
```

#811 is witness-routing custody only and is not theorem ancestry.

## Motivation inherited from #810

For a declared integer replay row

```text
r = [a,b,c] in Z^3,
```

#810 earned the fixed-fixture rescue pairings

```text
q_H(r) = a+b
q_I(r) = a+c
```

against the two witnessed missing state directions

```text
k_H=(1,1,0)
k_I=(1,0,1).
```

It also earned

```text
all-six rank rescue       iff q_H != 0 and q_I != 0
all-six unimodular rescue iff |q_H| = 1 and |q_I| = 1.
```

The next question is whether these two pairings merely predict rescue, or whether they define an exact integer quotient of replay design space for the declared rescue problem.

## Preregistered quotient map

Define

```text
Q : Z^3 -> Z^2
Q(a,b,c) = (a+b, a+c).
```

Preregistered target:

```text
ker(Q) = Z * (1,-1,-1).
```

The vector `(1,-1,-1)` is not imported as a conclusion merely because it appeared as #807's hostile replay. Implementation must rederive the primitive kernel generator independently from the two #810 inherited pairing rows, e.g. by exact integer cross product and primitive normalization.

Preregistered section:

```text
s(u,v) = [0,u,v]
```

so

```text
Q(s(u,v)) = (u,v).
```

Candidate exact decomposition:

```text
[a,b,c]
=
s(a+b,a+c)
+
a * [1,-1,-1].
```

Thus two replay rows should share the same quotient coordinate iff their difference is an integer multiple of the primitive fiber direction.

## Preregistered unimodular replay-coordinate normal form

Use the third coordinate

```text
t(r)=a.
```

Candidate coordinate transform

```text
T(r) = (q_H(r), q_I(r), t(r))
```

with matrix

```text
[1 1 0]
[1 0 1]
[1 0 0].
```

Preregistered determinant target:

```text
det(T)=+1.
```

If independently rederived, this makes `T` a unimodular change of integer coordinates, with inverse

```text
r = [t, q_H-t, q_I-t].
```

This is the algebraic source of #810's four affine unimodular rescue lines, not a physical sensor manifold.

## Candidate theorem package

If implementation and hostiles survive:

```text
THE_FIXED_S3_INTEGER_REPLAY_SPACE_ADMITS_A_UNIMODULAR_NORMAL_FORM_Z3_ISOMORPHIC_TO_Z2_CROSS_Z_WITH_COORDINATES_Q_H_Q_I_T_WHERE_THE_DECLARED_RESCUE_CLASSIFICATION_FACTORS_THROUGH_THE_Z2_QUOTIENT_AND_T_PARAMETERIZES_THE_EXACT_INTEGER_FIBER.
```

Exact-sequence form:

```text
0 -> Z --g--> Z^3 --Q--> Z^2 -> 0
```

with

```text
g(1)=(1,-1,-1)
```

and the sequence split by `s(u,v)=[0,u,v]`.

Candidate bounded architectural law:

```text
IN_THE_FIXED_S3_FIXTURE_REPLAY_IDENTITY_CAN_CONTAIN_AN_INTEGER_FIBER_COORDINATE_THAT_IS_INVISIBLE_TO_RESCUE_CLASSIFICATION_WHILE_REMAINING_VISIBLE_TO_THE_FULL_REPLAY_OBSERVATION_TRANSCRIPT.
```

## Rescue geometry in quotient coordinates

Preregistered quotient-space classification:

```text
rank-rescue quotient locus
= {(u,v) in Z^2 : u != 0 and v != 0}

unimodular-rescue quotient locus
= {(-1,-1),(-1,+1),(+1,-1),(+1,+1)}.
```

Every unimodular replay-rescue affine line from #810 should be exactly one fiber

```text
Q^-1(eps_H, eps_I)
```

for `eps_H,eps_I in {-1,+1}`.

## Mandatory hostiles

1. Derive `k_H,k_I` from the exact #810 certificate rather than hardcoding them as theorem inputs.
2. Derive the primitive fiber generator by exact cross product / gcd normalization and require exactly `(1,-1,-1)` under the declared sign convention.
3. Derive the `3 x 3` replay-coordinate transform from those inherited pairing rows plus the declared fiber coordinate `t=a`; require determinant exactly `+1`.
4. Compute its integer inverse by exact adjugate or an independently checked integer formula. No floating point.
5. Exhaustively check every replay row in `[-4,4]^3`:
   - exact `T^-1(T(r))=r`;
   - exact decomposition `r=s(Q(r))+t*g`;
   - exact parent rank/unimodular predicate agreement from `(q_H,q_I)`.

Expected replay rows:

```text
9^3 = 729.
```

6. Exhaustively check every quotient target `(u,v)` in `[-4,4]^2`:
   - `Q(s(u,v))=(u,v)`;
   - exact surjectivity on the finite target window;
   - rank-rescue predicate count should be independently obtained;
   - unimodular quotient points should be independently obtained.

Expected quotient targets:

```text
9^2 = 81.
```

7. For every base row in `[-3,3]^3` and every fiber translation `n=-4..4`, verify

```text
Q(r+n*g)=Q(r)
```

and verify the all-six rank/unimodular rescue classifications remain identical.

Expected translation checks:

```text
7^3 x 9 = 3087.
```

8. Within the finite cube `[-3,3]^3`, independently verify pairwise fiber completeness:

```text
Q(r)=Q(s)
iff
r-s is an integer multiple of g.
```

Do not replace this hostile with a symbolic assertion only.

9. Named same-fiber controls must include at least:

```text
r0=[1,0,0]
r1=[2,-1,-1]
```

with

```text
Q(r0)=Q(r1)=(1,1),
```

both exact-unimodular rescue, while their replay rows, augmented matrices, and at least one full observation transcript remain distinct.

This hostile is essential:

```text
SAME_RESCUE_QUOTIENT != SAME_REPLAY_TRANSCRIPT.
```

10. The null fiber must include

```text
Q(0,0,0)=Q(1,-1,-1)=(0,0)
```

while all four historically rank-two schedules remain unrescued.

11. One-sided quotient axes must retain the #810 family-selective failure behavior.
12. Re-removing replay must preserve the historical #802/#804 nonidentifiability facts.
13. Schedule identity remains separately inherited at `tau_schedule=2`.
14. Ash receives only bounded child-legible quotient/fiber truth. Loom may receive the exact integer normal-form atlas.
15. Custody remains receiver-invariant and every authority coordinate remains false.

## Ash projection ceiling

Ash may receive only truths such as:

```text
MANY_DIFFERENT_EXTRA_CHECKS_CAN_HAVE_THE_SAME_RESCUE_EFFECT
THE_RESCUE_EFFECT_DOES_NOT_TELL_US_WHICH_EXACT_EXTRA_CHECK_WAS_USED
FOUR_RESCUE_EFFECTS_HAVE_EXACT_INTEGER_KEYS_IN_THIS_FIXTURE
THE_FULL_OBSERVATION_CAN_STILL_TELL_APART_CHECKS_WITH_THE_SAME_RESCUE_EFFECT
```

Ash must not receive:
- quotient matrix;
- kernel/fiber vector;
- affine-fiber equations;
- replay vectors;
- inverse formulas;
- latent state coordinates.

Loom may receive the bounded exact quotient matrix, primitive fiber generator, unimodular normal form, finite quotient atlas, and hostile counts with zero runtime/production/physical authority.

## Mandatory scars

```text
SAME_RESCUE_QUOTIENT != SAME_REPLAY_ROW
SAME_RESCUE_QUOTIENT != SAME_AUGMENTED_OBSERVATION_MATRIX
SAME_RESCUE_QUOTIENT != SAME_FULL_OBSERVATION_TRANSCRIPT
RESCUE_CLASSIFICATION_FACTORING != INFORMATION_ERASURE_FROM_THE_SOURCE_RECORD
INTEGER_FIBER != PHYSICAL_GAUGE_ORBIT
SPLIT_EXACT_SEQUENCE != OPERATIONAL_INVERSE_ROUTE
UNIMODULAR_COORDINATE_CHANGE != UNIVERSAL_SENSOR_OPTIMALITY
QUOTIENT_COMPLETE_FOR_DECLARED_RESCUE_COORDINATES != SEMANTIC_EQUIVALENCE
FINITE_FIBER_CERTIFICATE != ASYMPTOTIC_RECOVERY_THEOREM
```

## Constitutional footprint

Keep the theorem-bearing net footprint to four paths:

```text
this preregistration
new implementation JS
new hostile test
A15-R0 review-hardening sentinel
```

The hardening sentinel must pivot its immediate parent to exact #810 receipt

```text
79a6533843c4133345bec3c1e83477c621230b09
```

and preserve ancestry

```text
#810 -> #807 -> #804 -> #802 -> #800 -> #798 -> #796 -> #794 -> #792 -> #790/#752.
```

At freeze require:
- exact merge base #810 receipt;
- zero commits behind;
- only the four declared chamber paths changed;
- no inherited #810 scientific-source mutation outside the hardening sentinel's chamber declaration.

Then open:

```text
canonical stacked draft PR -> #810 branch
witness-only draft PR       -> main
```

Do not call 𝄐 until exact-head TD613 Consolidated Validation has classifier SUCCESS, contracts SUCCESS, Dome-World static step 9 SUCCESS, A15-R0 constitutional step 19 SUCCESS, and aggregate run SUCCESS.

If step 19 reds, treat the red as scientific feedback. Do not weaken the hostile.

## Hard ceilings

Still false / unearned:
- universal replay theorem;
- universal quotient or sufficient-statistic theorem;
- universal/optimal sensor theorem;
- physical gauge orbit or gauge redundancy;
- physical quasicrystal/moiré claim;
- physical/Berry/gauge holonomy;
- continuum tomography;
- semantic equivalence/causation;
- live Ash/Loom runtime;
- Proto-Loom/A16;
- #788 promotion by implication;
- operational inverse route;
- merge/publication/production/release/Vercel authority.

#718 remains alive.

𝌋‌⟐

**WESTERN-HORIZON REPLAY-RESCUE QUOTIENT / FIBER NORMAL-FORM STARTUP — PREREGISTERED / UNEARNED.**

Sealed ⟐
