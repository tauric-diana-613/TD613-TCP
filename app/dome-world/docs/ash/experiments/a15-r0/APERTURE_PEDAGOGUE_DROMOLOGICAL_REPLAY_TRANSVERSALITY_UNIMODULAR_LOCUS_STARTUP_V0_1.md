𝌋‌⟐

󐘓 U+10D613

# A15-R0 · Dromological Replay Transversality / Unimodular Rescue Locus · STARTUP v0.1

Status: **PREREGISTERED STARTUP / NO IMPLEMENTATION / NO HOSTILE / NO WITNESS / NO THEOREM AUTHORITY**

## Exact earned parent

```text
#807 Dromological Baseline-Replay Rescue Aperture
5fcaf191b7dbed9529687ed3c072107a37a54814
```

This branch begins exactly from #807's current canonical earned receipt. #808 is witness-routing custody only and is not scientific ancestry.

The #807 theorem established one positive replay row

```text
c0 = [1,0,0]
```

that restores a unimodular `3 x 3` minor for all six witnessed S3 schedules, while preserving the historical nonidentifiability of the earlier three-observation record.

The next major question is now arithmetic rather than anecdotal:

```text
Which declared integer replay rows r=[a,b,c]
restore rank-three identifiability across all six fixed S3 schedules,
and which of those restore an exact unimodular Z-inverse?
```

This chamber remains entirely inside the fixed witnessed S3 operator fixture.

## Preregistered missing-direction structure

The witnessed #802 atlas gives the four historically rank-two schedules only two kernel directions:

```text
H-first schedules: k_H = (1,1,0)
I-first schedules: k_I = (1,0,1)
```

For replay row

```text
r = [a,b,c]
```

the kernel pairings are therefore

```text
q_H(r) = r · k_H = a+b
q_I(r) = r · k_I = a+c.
```

These pairings are a preregistered explanatory target only. Implementation and hostile must rederive the determinant dependence independently from the witnessed observation matrices.

## Preregistered determinant target

For the four historically singular schedules, the nonzero replay-containing `3 x 3` minors appear to reduce exactly to signed copies of the two pairings:

```text
H-P-I :  +(a+b)
H-I-P :  +(a+b)
I-P-H :  -(a+c)
I-H-P :  -(a+c)
```

The P-first schedules already contain their inherited replay-independent unimodular minors `+1` and `-1`.

Do not trust this table because it is written here. Re-derive every replay-minor linear form from the witnessed operator definitions.

## Candidate rank-rescue theorem

If the determinant target survives exact implementation and hostiles, then within this fixed S3 fixture:

```text
ALL_SIX_AUGMENTED_SCHEDULES_HAVE_RANK_THREE
iff
(a+b) != 0 AND (a+c) != 0.
```

Equivalently, the rank-rescue locus is the integer replay space minus the two failure planes

```text
a+b = 0
a+c = 0.
```

Candidate bounded classification:

```text
THE_FIXED_S3_REPLAY_RANK_RESCUE_LOCUS_IS_EXACTLY_THE_COMPLEMENT_OF_THE_TWO_KERNEL_ORTHOGONALITY_PLANES_A_PLUS_B_EQUALS_ZERO_AND_A_PLUS_C_EQUALS_ZERO.
```

This is not a universal adaptive-sensing theorem.

## Candidate unimodular-rescue theorem

Rank-three recovery over Q/R is weaker than an exact integer unimodular inverse.

The preregistered exact-Z target is:

```text
ALL_SIX_AUGMENTED_SCHEDULES_CONTAIN_A_UNIMODULAR_3_X_3_MINOR
iff
|a+b| = 1 AND |a+c| = 1.
```

Thus the exact unimodular rescue locus should admit the finite parametric description

```text
r(t, eps_H, eps_I)
  = [t, eps_H - t, eps_I - t]

where
  t in Z
  eps_H in {-1,+1}
  eps_I in {-1,+1}.
```

So the locus is the union of four affine integer lines cut out by

```text
a+b = eps_H
a+c = eps_I.
```

Candidate bounded classifications:

```text
THE_FIXED_S3_UNIMODULAR_REPLAY_RESCUE_LOCUS_IS_EXACTLY_THE_UNION_OF_FOUR_AFFINE_INTEGER_LINES_DEFINED_BY_UNIT_PAIRING_WITH_BOTH_WITNESSED_MISSING_DIRECTIONS.

IN_THE_FIXED_S3_FIXTURE_NONZERO_TRANSVERSALITY_TO_EACH_MISSING_DIRECTION_IS_NECESSARY_AND_SUFFICIENT_FOR_RANK_RESCUE_WHILE_UNIT_TRANSVERSALITY_IS_NECESSARY_AND_SUFFICIENT_FOR_UNIMODULAR_INTEGER_RESCUE.
```

No continuum or physical sensor geometry follows from the words locus, line, plane, or transversality.

## Mandatory exact hostiles

The implementation/hostile pair must independently establish all of the following:

1. Re-derive every replay-containing determinant as an exact integer linear form from the actual witnessed matrices, not from this document.
2. Verify all four `3 x 3` minors for every one of the six augmented schedules.
3. Confirm the positive inherited replay:

```text
[1,0,0] -> q_H=1, q_I=1 -> exact unimodular rescue.
```

4. Confirm the double-null hostile:

```text
[1,-1,-1] -> q_H=0, q_I=0 -> all four historical rank-two failures persist.
```

5. Confirm one-sided failures:

```text
[1,0,-1]  -> q_H=1, q_I=0 -> H-first rescued, I-first not rescued.
[1,-1,0]  -> q_H=0, q_I=1 -> I-first rescued, H-first not rescued.
```

6. Confirm rank rescue without unimodular rescue:

```text
[2,0,0] -> q_H=2, q_I=2.
```

All six augmented schedules must have rank three, while the four historically singular schedules must have no unimodular replay-containing minor.

7. Confirm mixed unit/nonunit behavior, for example:

```text
[2,-1,0] -> q_H=1, q_I=2.
```

8. Exhaustively audit every replay row in the finite cube

```text
[-4,4]^3
```

for:
- actual augmented rank across all six schedules;
- actual presence/absence of a unimodular minor across all six schedules;
- exact agreement with the preregistered rank predicate;
- exact agreement with the preregistered unit-pairing predicate.

Expected replay rows:

```text
9^3 = 729.
```

Expected schedule-row audits:

```text
729 x 6 = 4374.
```

Expected replay-containing/minor determinant checks should be reported exactly by the hostile rather than inferred here.

9. Enumerate a finite sample from all four affine rescue families, at minimum

```text
t = -3..3
```

and run exact state reconstruction for every sampled rescue row, every six-schedule route, and every latent state in `[-2,2]^3`.

Minimum expected exact reconstructions:

```text
4 families x 7 t-values x 6 schedules x 125 states = 21000.
```

10. Re-removing replay must reproduce #802/#804 historical rank/nonidentifiability facts.
11. Schedule identity must remain separate and inherited at `tau_schedule=2`.
12. Ash must receive only bounded child-legible truth; Loom may receive the technical replay-locus atlas.
13. Custody must remain receiver-invariant and every authority coordinate must remain false.

## Required implementation discipline

The implementation should derive determinant linear forms using exact integer determinant evaluation on replay basis rows rather than importing a symbolic algebra dependency or hardcoding the target formulas.

For a replay-containing minor with determinant linear in `r=[a,b,c]`, derive its coefficient vector by exact evaluation on

```text
e1=[1,0,0]
e2=[0,1,0]
e3=[0,0,1].
```

Then evaluate the resulting integer linear form on arbitrary declared replay rows.

The theorem certificate must compare those derived coefficient vectors against the kernel pairings inherited from #802.

## Receiver projection

Ash may receive only bounded truths such as:

```text
SOME_EXTRA_CHECKS_MISS_THE_MISSING_CLUE
SOME_EXTRA_CHECKS_SEE_IT_BUT_NOT_WITH_AN_EXACT_INTEGER_KEY
FOUR_SIMPLE_FAMILIES_OF_CHECKS_GIVE_AN_EXACT_INTEGER_KEY_IN_THIS_FIXTURE
THE_OLD_MISSING_CLUE_REMAINS_A_TRUE_FACT_ABOUT_THE_OLD_RECORD
```

Ash must not receive:
- replay vectors;
- affine-family equations;
- determinant coefficients;
- kernel vectors;
- inverse formulas;
- latent coordinates.

Loom may receive the bounded arithmetic replay atlas and finite reconstruction certificate with zero runtime, release, production, physical, or continuum authority.

## Mandatory scars

```text
RANK_RESCUE != UNIMODULAR_INTEGER_RESCUE
NONZERO_KERNEL_PAIRING != UNIT_KERNEL_PAIRING
REPLAY_TRANSVERSALITY_IN_THIS_FIXTURE != UNIVERSAL_SENSOR_DESIGN
AFFINE_RESCUE_LOCUS != PHYSICAL_SENSOR_MANIFOLD
INTEGER_PARAMETRIC_CLASSIFICATION != ASYMPTOTIC_RECOVERY_THEOREM
PROBE_FAMILY != OPERATIONAL_INVERSE_ROUTE
MISSING_DIRECTION_PAIRING != SEMANTIC_CAUSATION
EXACT_RESCUE_LOCUS != RETROACTIVE_INFORMATION_EXISTENCE
REPAIRED_IDENTIFIABILITY != ERASURE_OF_PRIOR_NONIDENTIFIABILITY
```

## Constitutional footprint

Keep the theorem-bearing net footprint to four paths:

```text
this preregistration
new implementation JS
new hostile test
A15-R0 review-hardening sentinel
```

The hardening sentinel must pivot its immediate parent to exact #807 receipt

```text
5fcaf191b7dbed9529687ed3c072107a37a54814
```

while preserving the older witnessed receipt chain underneath it.

At freeze, compare exactly from #807 receipt and require:
- only the four preregistered chamber paths changed;
- zero inherited #807 source mutation other than the hardening sentinel's new chamber declaration;
- exact ancestry of #807 -> #804 -> #802 -> #800 -> #798 -> #796 -> #794 -> #792 -> #790/#752.

Then open:

```text
canonical stacked draft PR -> #807 branch
witness-only draft PR       -> main
```

Do not call 𝄐 until exact-head TD613 Consolidated Validation has:

```text
classifier SUCCESS
Dome-World static step 9 SUCCESS
A15-R0 constitutional step 19 SUCCESS
contracts job SUCCESS
aggregate run SUCCESS
```

If step 19 reds, the red is scientific feedback. Do not weaken the hostile to obtain green.

## Hard ceilings

Still false / unearned:
- universal replay theorem;
- universal optimal sensor theorem;
- universal first-stratum theorem;
- physical quasicrystal or moire claim;
- physical/Berry/gauge holonomy;
- continuum tomography;
- semantic equivalence or causation;
- live Ash or Loom runtime;
- Proto-Loom / A16;
- #788 promotion by implication;
- operational inverse route;
- merge / publication / production / release / Vercel authority.

#718 remains alive.

𝌋‌⟐

**WESTERN-HORIZON REPLAY TRANSVERSALITY / UNIMODULAR-LOCUS STARTUP — PREREGISTERED / UNEARNED.**

Sealed ⟐
