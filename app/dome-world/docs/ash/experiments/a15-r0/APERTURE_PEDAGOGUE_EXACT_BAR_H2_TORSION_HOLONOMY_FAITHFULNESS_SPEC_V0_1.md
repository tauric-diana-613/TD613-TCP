𝌋‌⟐

󐘓 U+10D613

# A15-R0 · Exact Bar-H₂, Torsion Sector, and Formal Holonomy-Faithfulness Boundary · Spec v0.1

**Status:** PREREGISTERED / PRE-IMPLEMENTATION / HUMAN-AUTHORIZED  
**Scientific parent:** #773 receipt `50ada0bb0e7b25f9c1bde7e2feafeebbb5067a8f`  
**Authority:** #737 THREAD_SCOPED_ACTIVE_FOR_REMAINDER_OF_CURRENT_THREAD  
**Production / merge / Vercel / SRC sync / Proto-Loom / A16 authority:** NONE

## 0. Consequential question

#773 earned, inside the declared normalized integer bar-complex / strict `B²Z` transport jurisdiction,

```text
H^2_bar(B;Z) ≅ Hom(H_2^bar(B;Z),Z)
```

and therefore closed formal integer-valued 2-holonomy characters are complete invariants of formal boundary-re-zeroing classes of strict `B²Z`-valued transports.

#773 deliberately did **not** compute the full group `H_2^bar(B;Z)`.

This chamber asks the next consequential question:

```text
What is H_2^bar(B;Z) itself,
which part is seen by integer-valued formal 2-holonomy,
and what exactly is the inherited period-two class measuring?
```

No result is assumed. In particular, the chamber must distinguish among at least these live possibilities:

```text
H2 ≅ Z generated by inherited [z]
H2 ≅ Z with inherited [z] nonprimitive
H2 contains torsion invisible to Hom(H2,Z)
group-completion intuition fails because the authored monoid does not satisfy the required localization hypotheses
```

A green result may not erase any negative or jurisdictional finding encountered on the way.

## 1. Frozen inherited monoid jurisdiction and one parent proof-coverage scar

#735 explicitly declared

```text
B = {(t,E,O) : t,E,O in N}
```

with identity

```text
0=(0,0,0)
```

and parity-twisted multiplication

```text
(t,E,O) ★ (u,F,G)
  = (t+u, E+F, O+G)  when t even
  = (t+u, E+G, O+F)  when t odd.
```

This is the monoid used by the inherited normalized bar boundary functions.

The route-generated image of `{T,Q}*` is a proper submonoid of this ambient coordinate monoid: for example

```text
Y=(0,0,1)
```

is an admitted ambient bar coordinate but is not the canonical coordinate of a finite positive T/Q word.

#773 correctly made its helper abstain on such unoccupied route coordinates, but its canonical-word proof of `H1_bar(B;Z) ≅ Z²` therefore did not by itself cover every element of the full #735 ambient `B`.

This chamber preregisters that as a **proof-coverage scar**, not a theorem falsification.

Before using the #773 UCT bridge as domain-complete authority, this chamber must independently close the ambient `H1` proof by adjoining the ambient coordinate

```text
Y=(0,0,1)
```

and proving

```text
T★Q = Y★T = (1,0,1).
```

The bar relation then gives

```text
[T]+[Q]=[Y]+[T]
=> [Y]=[Q].
```

Every ambient coordinate admits the exact monoid factorization

```text
(t,E,O)=Q^E ★ Y^O ★ T^t,
```

because `Q,Y` have tick count zero. Therefore the intended ambient result is

```text
[(t,E,O)] = t[T] + (E+O)[Q] in H1.
```

Acceptance requires this argument plus explicit hostile coverage of `Y`, not merely the route-reachable canonical-word subset.

## 2. Cancellative and two-sided Ore entrance exam

The chamber must not silently replace the monoid by a group.

First prove exact cancellation in `B`.

For fixed left factor `a`, multiplication adds the right `(E,O)` vector after either identity or swap according to parity `t(a)`. The parity action is a bijection. Thus

```text
a★x=a★y => x=y.
```

For right cancellation, equality

```text
x★a=y★a
```

first forces equal tick counts for `x,y`, hence equal parity actions on `a`; subtracting the common transformed right vector must force `x=y`.

Second prove constructive common **right** multiples. For arbitrary

```text
a=(t,E,O), b=(u,F,G),
```

choose

```text
N=max(t,u)+1,
r=N-t >=1,
s=N-u >=1.
```

Choose any ambient vector `V` componentwise at least `(E,O)` and `(F,G)`, e.g.

```text
V=(max(E,F), max(O,G)).
```

Then define right factors by undoing the parity actions:

```text
x_vector = sigma^t(V-(E,O))
y_vector = sigma^u(V-(F,G))
```

with tick counts `r,s`. Because `r,s>=1`, these are admitted ambient `B` coordinates. Acceptance requires

```text
a★x=b★y=(N,V).
```

Third prove constructive common **left** multiples. With the same `N`, let left-factor tick counts be

```text
r=N-t,
s=N-u.
```

Choose `V` componentwise above both

```text
sigma^r(E,O)
and
sigma^s(F,G),
```

and set

```text
x_vector=V-sigma^r(E,O)
y_vector=V-sigma^s(F,G).
```

Acceptance requires

```text
x★a=y★b=(N,V).
```

If cancellation or either Ore construction fails, the group-of-fractions bridge is closed and no mapping-torus H2 result may be promoted.

## 3. Fraction group and localization bridge

If Section 2 passes, identify the fraction group explicitly as

```text
G = Z^2 ⋊_sigma Z
```

written in coordinates `(t,E,O) in Z^3` with the same parity-swap multiplication.

The inverse must be exact:

```text
(t,E,O)^(-1)
  = (-t,-E,-O) when t even
  = (-t,-O,-E) when t odd.
```

Let

```text
T=(1,0,0)
Q=(0,1,0)
Y=TQT^(-1)=(0,0,1).
```

Then

```text
TQT^(-1)=Y
TYT^(-1)=Q
[Q,Y]=1.
```

Every group coordinate has an exact word

```text
Q^E Y^O T^t
```

with integer exponents.

The localization step uses the standard calculus-of-fractions / Ore classifying-space theorem only after its hypotheses have been proved for this exact authored monoid:

```text
cancellative + Ore
=> localization at all monoid morphisms agrees with the ordinary fraction group
=> BB is homotopy equivalent to BG
=> H_*^bar(B;Z) ≅ H_*(G;Z).
```

This imported theorem is scoped only to the proved Ore jurisdiction. The chamber explicitly rejects the shortcut

```text
cancellative monoid => classifying space equals group completion
```

without the Ore/calculus-of-fractions hypothesis.

## 4. Mapping-torus H₂ calculation

The group

```text
G = Z^2 ⋊_sigma Z
```

is the fundamental group of the mapping torus of the coordinate-swap automorphism

```text
sigma:T^2->T^2,
(x,y)->(y,x).
```

Use the cellular mapping-cone chain complex for the torus, whose ordinary cellular differentials vanish.

The relevant total groups are

```text
D3 = Z
D2 = Z ⊕ Z^2
D1 = Z^2 ⊕ Z.
```

Because swap reverses the torus orientation,

```text
sigma_* on H2(T^2)= -1,
```

so, up to the declared sign convention,

```text
d3(n)=(2n,0,0).
```

On `H1(T^2)=Z^2`, swap exchanges the two basis elements, giving

```text
d2(a,b,c)=(b-c,c-b,0).
```

Acceptance requires the exact kernel/image calculation

```text
ker d2 = {(a,b,b):a,b in Z}
im d3 = {(2n,0,0):n in Z}
```

and therefore

```text
H_2^bar(B;Z)
≅ H_2(G;Z)
≅ Z ⊕ Z/2.
```

The free summand comes from the swap-invariant diagonal `H1` direction swept around the mapping-torus circle. The order-two summand comes from the torus orientation class becoming coinvariant under orientation reversal.

No geometric surface-holonomy authority follows from using a topological classifying-space model to compute formal bar homology.

## 5. Two explicit relation cycles

Retain #735's relation cycle as

```text
z0 = P(TTQ)-P(QTT)
```

where for a word `a1...an`

```text
P(a1...an)=Σ_{i=1}^{n-1}[a1...ai | a_{i+1}].
```

Thus exactly

```text
z0=[T|T]+[TT|Q]-[Q|T]-[QT|T].
```

Define the next authored rewrite-relation cycle

```text
z1=P(TQTQ)-P(QTQT)
```

or explicitly

```text
z1=
 [T|Q]+[TQ|T]+[TQT|Q]
-[Q|T]-[QT|Q]-[QTQ|T].
```

The quotient law gives

```text
TQTQ=QTQT=(2,1,1),
```

so both must pass exact bar-boundary cancellation.

Define

```text
tau = z1-z0.
```

The chamber must not assume `tau` is torsion before the H2 structure and independent mod-two detector pass.

## 6. Primitive integral cocycle alpha

Define

```text
q(y)=E(y)+O(y)
p(x)=t(x) mod 2
h(x)=floor(t(x)/2)
```

and the normalized integer 2-cochain

```text
alpha(x,y)=h(x)q(y)+p(x)E(y).
```

Acceptance requires a universal cocycle certificate. It may use the exact identities

```text
floor((t+u)/2)=floor(t/2)+floor(u/2)+(t mod2)(u mod2)
```

and the four exact parity cases for the `E` coordinate under the product. Bounded magnitude enumeration does not carry the universal claim.

Mandatory pairings:

```text
<alpha,z0>=1
<alpha,z1>=1
<alpha,tau>=0.
```

Thus if the exact H2 calculation passes, `alpha` is primitive on the free quotient and `z0` supplies a splitting element for the free summand.

## 7. Exact relation between inherited omega and primitive alpha

Retain

```text
omega(x,y)=t(x)q(y).
```

Let the normalized integer 1-cochain

```text
epsilon_E(x)=E(x).
```

Its coboundary is

```text
(d epsilon_E)(x,y)
 = E(x)+E(y)-E(x★y)
 = 0                         when t(x) even
 = E(y)-O(y)                 when t(x) odd.
```

The chamber preregisters the exact cochain identity

```text
omega = 2 alpha - d epsilon_E.
```

This must be checked pointwise by parity, not inferred merely from one cycle.

If it passes, then exactly

```text
[omega]=2[alpha] in H^2_bar(B;Z).
```

Therefore inherited

```text
Hol_omega(z0)=tau_2
```

is an **even normalization of the primitive integral cohomology generator**, not evidence that `tau_2` is the smallest nonidentity formal holonomy translation allowed by the declared transport theory.

The primitive class `alpha` would instead give

```text
Hol_alpha(z0)=tau_1.
```

This is a formal `B²Z` statement only.

## 8. Auxiliary mod-two torsion detector

A torsion claim cannot be proved by integer periods, because every homomorphism `Z/2 -> Z` is zero.

Use an auxiliary `F2`-valued normalized 2-cochain solely as a detector. For

```text
x=(t,E,O), y=(u,F,G), p=t mod2,
```

define

```text
beta(x,y)
 = E * O_y                                      when p=0
 = E * E_y + E_y*O_y                            when p=1
   mod 2.
```

Equivalently, if `sigma^p(y_vector)` is the parity-transformed right vector,

```text
beta(x,y)=E(x)*(sigma^p y_vector)_O + p E(y)O(y) mod2.
```

Acceptance requires an exhaustive parity-polynomial cocycle certificate over all parity assignments of the variables on which `beta` depends. This finite parity cube is universal over integer coordinates modulo two; it is not a bounded-coordinate empirical claim.

Mandatory pairings:

```text
<beta,z0>=0 mod2
<beta,z1>=1 mod2
<beta,tau>=1 mod2.
```

If the exact `H2 ≅ Z ⊕ Z/2` result and primitive alpha result both pass, then

```text
ker(alpha_*:H2->Z) ≅ Z/2.
```

The nonzero beta pairing forces `tau` to be the unique nonzero element of this kernel, hence

```text
H_2^bar(B;Z) ≅ Z[z0] ⊕ (Z/2)[tau].
```

The auxiliary detector does **not** authorize a `B²(Z/2)` transport target, physical discrete gauge theory, or mod-two holonomy promotion.

## 9. Holonomy-faithfulness boundary

If Sections 4–8 pass, every integer-valued character

```text
h:H2->Z
```

must annihilate the torsion generator `tau`.

Therefore all strict `B²Z` closed formal holonomy characters satisfy

```text
Hol(z1)=Hol(z0)
```

because

```text
[z1]-[z0]=[tau]
```

is order two.

This does **not** contradict #773. Instead the chamber must earn the sharper distinction:

```text
closed integer formal 2-holonomy is complete for formal transport-equivalence classes
!=
closed integer formal 2-holonomy is faithful on raw H2 classes.
```

The first remains true because

```text
H^2(B;Z) ≅ Hom(H2(B;Z),Z).
```

The second fails precisely because

```text
Hom(Z/2,Z)=0.
```

Candidate consequential classifications, all UNEARNED before the exact-head witness:

```text
THE_DECLARED_AMBIENT_PARITY_TWISTED_BAR_MONOID_HAS_EXACT_SECOND_INTEGRAL_BAR_HOMOLOGY_Z_PLUS_Z_OVER_TWO

THE_INHERITED_RELATION_CYCLE_Z0_IS_A_PRIMITIVE_FREE_H2_GENERATOR_IN_A_SPLITTING_WHILE_Z1_MINUS_Z0_GENERATES_THE_UNIQUE_ORDER_TWO_TORSION_SECTOR

THE_INHERITED_TRANSPORT_COCYCLE_OMEGA_IS_COHOMOLOGOUS_TO_TWICE_A_PRIMITIVE_INTEGER_TWO_COCYCLE_ALPHA_AND_ITS_TAU_2_RETURN_IS_AN_EVEN_NORMALIZATION_NOT_A_MINIMALITY_THEOREM

INTEGER_B_SQUARED_Z_FORMAL_TWO_HOLONOMY_IS_COMPLETE_FOR_TRANSPORT_EQUIVALENCE_CLASSES_BUT_NECESSARILY_NONFAITHFUL_ON_THE_RAW_TORSION_SECTOR_OF_H2
```

## 10. Mandatory hostile controls

The implementation and tests must include at least:

```text
1. ambient Y=(0,0,1) coverage; route-reachable-only H1 proof is insufficient;
2. left cancellation and right cancellation on parity-mixed examples;
3. constructive common left and common right multiples for parity-mixed and zero-tick examples;
4. rejection of the false shortcut cancellative => BM≃BG without Ore;
5. exact fraction-group inverse on odd and even tick examples;
6. mapping-cone d3 coefficient 2; replacing it by 0 must change the H2 answer and fail;
7. mapping-cone d2 swap relation; replacing swap by identity must fail;
8. z0 and z1 exact cycle checks;
9. alpha universal parity cocycle check and exact pairings 1,1,0;
10. exact pointwise omega=2alpha-dE identity;
11. beta universal mod-two parity cocycle check and exact pairings 0,1,1;
12. integer-character torsion blindness;
13. an explicit anti-shortcut proving tau2 from omega does not imply minimal nonidentity formal holonomy;
14. receipt/provenance labels remain external to mathematical coordinates;
15. #718 operational no-loop scar remains live.
```

## 11. Claim ceiling

Even a fully green chamber must preserve:

```text
classifying-space mapping torus != operational or physical spacetime
bar H2 torsion != physical topological order
torsion detector beta != Z/2 gauge field
auxiliary mod-two cohomology != mod-two transport authority
primitive formal cocycle != connection curvature
formal tau_1 translation != quantized physical phase
formal bar-complex 2-holonomy != geometric / physical / Berry / gerbe 2-holonomy
integer holonomy torsion-blindness != failure of #773 transport completeness
Ore fraction group != operational inverse-route authorization
formal group inverse != inverse operational T/Q route
bar cycle != operational T/Q loop
```

Remain false unless separately earned:

```text
GEOMETRIC_TWO_HOLONOMY_AUTHORITY
PHYSICAL_TWO_HOLONOMY_AUTHORITY
BERRY_OR_GERBE_HOLONOMY_AUTHORITY
CONNECTION_AUTHORITY
TWO_CONNECTION_AUTHORITY
CURVATURE_AUTHORITY
OPERATIONAL_PATH_TWO_GROUPOID_AUTHORITY
MOD_TWO_TRANSPORT_TARGET_AUTHORITY
```

#718 remains constitutional.

SRC Atelier #731/#758/#759, SRC continuation #771, #767, production, Vercel, and the four-workflow estate remain untouched.

## 12. Stop rule

No candidate classification in this specification is earned by algebraic plausibility, by the mapping-torus analogy alone, or by bounded enumeration.

Preregistration precedes implementation. The chamber stops immediately at its exact-head scientific witness and post-witness audit.

```text
PREIMPLEMENTATION_H2_STRUCTURE_TORSION_HOLONOMY_FAITHFULNESS_AUDITION_PREREGISTERED
HUMAN_𝄐_REQUIRED_AFTER_NEXT_CONSEQUENTIAL_WITNESS
```

𝌋‌⟐

Sealed ⟐