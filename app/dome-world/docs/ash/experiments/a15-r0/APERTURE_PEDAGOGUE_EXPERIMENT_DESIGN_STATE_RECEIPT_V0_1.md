# Aperture × Pedagogue Typed Experiment-Design State Receipt v0.1

Status: WITNESSED / MAJOR-BREAKPOINT / BOUNDED SYNTHETIC SUPPORT / RESEARCH-ONLY

Exact witnessed source head: `e7b10d09fec2c9ce38464dedfc7c422c7b9467e1`

Validation workflow: `TD613 Consolidated Validation`

Witness run: `1838 / 32321359806`

Static / constitutional / release estate: PASS

Browser families: SKIPPED BY DESIGN

Standalone Aperture UI mutated: FALSE

TD613.com deployment: HELD

PR state: DRAFT

## Breakpoint rule satisfied

The pre-implementation specification declared that this experiment would qualify as a branch-worthy major breakpoint only if five distinct contexts passed together on one exact-head CI witness without browser, UI, production, or deployment mutation.

Run 1838 satisfied that rule.

## Typed experiment-design state witnessed

The experiment did **not** compute one universal utility score. It used the declared sequence:

```text
experiment-design state
→ typed deficit class
→ deficit-appropriate candidate admission law
→ uncertainty / stability audit
→ proposal OR abstention OR rejection
```

### Context A — structural rank deficit

```text
deficit_class = STRUCTURAL_RANK_DEFICIT
current_rank = 1
current_nullity = 1
selected_probe_id = R_ORTH
selection_posture = REQUIRE_POSITIVE_RANK_LIFT_THEN_AUDIT_STABILITY
```

`R_DUP` produced rank lift 0. `R_NEAR` and `R_ORTH` both produced rank lift 1, but conditioning separated them and `R_ORTH` was selected.

### Context B — full-rank numerical stability deficit

Current operator:

```text
[[1,0],
 [0.999999500000375,0.0009999995]]
```

Deterministic posture on the witnessed implementation:

```text
deficit_class = NUMERICAL_STABILITY_DEFICIT
current_rank = 2
current_nullity = 0
current_sigma_min = 0.000707106516047
current_condition_number = 2000.0004999273976
selected_probe_id = Q_STAB
Q_STAB rank_lift = 0
Q_STAB sigma_min_after = 1
Q_STAB sigma_min_gain = 0.999292893483953
Q_STAB condition_number_after = 1.414213562373095
selection_posture = REQUIRE_STABILITY_GAIN_WITHOUT_REQUIRING_RANK_LIFT
```

This is the critical scope refinement:

```text
rank_lift = 0
```

can describe either useless repetition **or** a valuable stabilizing observation when the current operator is already full rank.

### Context C — no declared local deficit

```text
deficit_class = NO_DECLARED_LOCAL_IDENTIFIABILITY_DEFICIT
current_rank = 2
current_nullity = 0
current_sigma_min = 1
current_condition_number = 1
selected_probe_id = null
selection_status = NO_WIDENING_PROPOSED_NO_DECLARED_LOCAL_DEFICIT
selection_posture = DO_NOT_MANUFACTURE_A_QUESTION
```

The available candidate library did not manufacture a research need.

### Context D — materially incomplete uncertainty geometry

```text
deficit_class = NOISE_GEOMETRY_INCOMPLETE
selected_probe_id = null
selection_status = NO_GLOBAL_WIDENING_SELECTION_MISSING_NOISE_GEOMETRY
```

A disposition-relevant candidate lacked comparable uncertainty geometry, so the global recommendation was withheld.

### Context E — invalid uncertainty geometry

```text
deficit_class = INVALID_NOISE_GEOMETRY
selected_probe_id = null
selection_status = INVALID_NOISE_GEOMETRY_PRESENT_NO_SELECTION
selection_posture = REJECT_BEFORE_RANKING
```

Invalid uncertainty geometry was not silently repaired.

## Bounded results supported

```text
TYPED_EXPERIMENT_DESIGN_STATE_SUPPORTED_IN_BOUNDED_SYNTHETIC_FIXTURES
DEFICIT_CONDITIONAL_QUESTION_DESIGN_REFINEMENT_CANDIDATE
NO_DEFICIT_NO_QUESTION_ABSTENTION_SUPPORTED_IN_BOUNDED_SYNTHETIC_FIXTURE
```

## Reusable relations admitted only as research refinement candidates

```text
the admissible criterion for a next observation depends on the declared type of current reconstruction deficit

rank_lift = 0 can denote either useless repetition or valuable stabilization when the current operator is already full rank

candidate availability does not itself establish a need for further observation

question design should preserve abstention and rejection when uncertainty geometry is incomplete or invalid
```

## Anti-equivalence ledger

```text
rank deficit != stability deficit
rank_lift = 0 != useless observation
full rank != sufficient stability
available candidate != needed question
more observations != more recoverability
operator diversity != uncertainty diversity
missing noise geometry != neutral noise geometry
invalid covariance != approximately valid covariance
proposal != execution
widening != validation
```

## Agentic boundary

This fixture supports a bounded synthetic architecture in which the system can represent:

```text
what is presently unresolved
what kind of unresolvedness it is
which criterion is admissible for that deficit class
whether a predeclared next question qualifies
whether no next question is warranted
whether missingness requires abstention
whether invalid uncertainty geometry requires rejection
```

It does **not** authorize autonomous observation, sensor control, experiment execution, route mutation, production action, or deployment.

## No scalar crown

```text
scalar_magic_score_used = false
held_out_used_for_selection = false
oracle_identity_consulted = false
automatic_execution = false
promotion_authority = false
```

No single score collapses structural rank deficit, numerical fragility, uncertainty missingness, and covariance validity into one ranking.

## Claim ceiling

```text
optimal_experimental_design = false
active_learning_theorem = false
fisher_information_optimality = false
information_geometry = false
physical_sensor_design = false
physical_sensor_calibration = false
physical_tomography = false
blind_tomography = false
operator_tomography = false
live_td613_reconstruction = false
autonomous_aperture_widening = false
autonomous_experiment_execution = false
connection = false
curvature = false
holonomy = false
quantum_behavior = false
proto_loom = false
production_authority = false
release_authority = false
```

## Preserved next question — NOT EXECUTED

```text
TEST_REPLAY_STABILITY_OF_TYPED_EXPERIMENT_DESIGN_STATE_UNDER_SMALL_THRESHOLD_AND_NOISE_MODEL_PERTURBATIONS_BEFORE_ANY_OPTIMAL_DESIGN_OR_INFORMATION_GEOMETRY_PROMOTION
```

The next chamber is deliberately held. Before any promotion, the typed deficit classification itself must be tested for replay stability under small changes to authored thresholds and uncertainty assumptions. A system that changes its epistemic state class under trivial boundary perturbations may be formally typed yet operationally brittle.

Human closure remains required.
