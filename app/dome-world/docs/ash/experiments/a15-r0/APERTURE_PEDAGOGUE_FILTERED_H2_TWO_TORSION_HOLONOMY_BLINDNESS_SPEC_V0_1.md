𝌋‌⟐

󐘓 U+10D613

# A15-R0 · Filtered H₂ Two-Torsion and Integer-Holonomy Blindness · Spec v0.1

**Status:** PREREGISTERED / PRE-IMPLEMENTATION / HUMAN-AUTHORIZED  
**Scientific parent:** #773 receipt `50ada0bb0e7b25f9c1bde7e2feafeebbb5067a8f`  
**Authority:** #737 thread-scoped Westward liberties remain active for this conversation  
**Production / merge / Vercel / SRC sync / A16 authority:** NONE

## 0. Consequential question

#773 proved that closed formal integer 2-holonomy characters are complete invariants of strict `B²Z`-valued formal transport classes, while explicitly refusing to claim an exact computation of full `H_2^bar(B;Z)`.

This chamber asks a different question:

```text
Can a nonzero bar-H2 class be invisible to every integer-valued holonomy character?
```

The chamber is intentionally **filtered**. It may earn an exact theorem inside finite bar filtrations `F_N`, but it may not silently promote filtered torsion to the full colimit `H_2^bar(B;Z)`.

## 1. Exact filtration jurisdiction

For every reachable quotient coordinate `b=(t,E,O)`, define weight

```text
w(b)=t+E+O.
```

For a normalized bar-n generator

```text
[x1|...|xn]
```

define total bar weight

```text
W([x1|...|xn])=Σ_i w(x_i).
```

`F_N C_n` is the free abelian subgroup generated by normalized bar-n generators of total weight at most `N`.

The inherited parity-twisted product preserves coordinate weight additively:

```text
w(x★y)=w(x)+w(y).
```

Internal bar faces preserve total weight and the two endpoint faces lower it. Therefore

```text
∂(F_N C_n) ⊆ F_N C_(n-1),
```

so `F_N` is an exact finite subcomplex of the declared normalized monoid bar complex.

No claim about filtration stabilization, spectral-sequence convergence, or the full filtered colimit is preregistered here.

## 2. Relation-cycle family

For a finite T/Q word

```text
w=a1...am,
```

define its left fan 2-chain

```text
Fan(w)=Σ_{i=1}^{m-1} [a1...ai | a_(i+1)].
```

By direct bar telescoping,

```text
∂Fan(w)=Σ_i [a_i]-[product(w)].
```

For the inherited rewrite family

```text
R_k : T Q^k T Q = Q T Q^k T,
k >= 0,
```

define the relation cycle

```text
r_k = Fan(T Q^k T Q) - Fan(Q T Q^k T).
```

Both words have the same T/Q letter counts and the same quotient product, hence every `r_k` is a normalized bar-2 cycle.

`r_0` must reduce exactly to the inherited #735/#765 relation cycle

```text
z=[T|T]+[TT|Q]-[Q|T]-[QT|T].
```

This chamber focuses on

```text
θ = r_1-r_0.
```

## 3. Candidate exact F₅ torsion theorem

The following is preregistered as one indivisible theorem obligation.

Inside `F_5`:

```text
∂θ=0.
```

There must exist the following exact **predeclared ten-term integer bar-3 chain** `B_θ`:

```text
B_θ =
 - [T|Q|T]
 + [T|T|Q]
 + [T|TQ|T]
 - [T|QT|Q]
 - [T|QTQ|T]
 + [T|TQT|Q]
 + [QT|Q|T]
 - [QT|T|T]
 + [QT|QT|T]
 - [QT|TT|Q].
```

The implementation must derive, not assume,

```text
∂B_θ = 2θ.
```

To prove that `θ` itself is not an integer boundary, the finite `F_5` bar complex must be reduced modulo `2` and the exact column ranks must satisfy

```text
rank_F2(im ∂3)=123,
rank_F2([im ∂3 | θ])=124.
```

Thus `θ mod 2` is not a bar-3 boundary. Therefore `θ` is not an integer boundary.

If all three obligations pass, the chamber may earn exactly

```text
[θ] has exact additive order 2 in H_2(F_5;Z).
```

The logical route must remain explicit:

```text
2θ is an integer boundary
+
θ mod 2 is not a mod-2 boundary
=>
θ is nonzero integral 2-torsion.
```

## 4. Mandatory F₆ persistence hostile

The `F_5` result may not be described as immediate evidence of full-complex torsion without one additional hostile.

Embed the exact same `θ` into `F_6`. The implementation must recompute the mod-2 boundary matrix independently and require

```text
rank_F2(im ∂3 in F_6)=302,
rank_F2([im ∂3 | θ])=303.
```

Because the same explicit `F_5` chain already witnesses `2θ=∂B_θ`, this earns only the bounded continuation:

```text
[θ] also has exact order 2 in H_2(F_6;Z).
```

It does **not** earn persistence for every `N` and does **not** earn a nonzero class in `colim_N H_2(F_N;Z)`.

## 5. Integer-holonomy blindness theorem

The inherited transport cocycle satisfies

```text
<ω,r_0>=2,
<ω,r_1>=2,
```

and therefore

```text
<ω,θ>=0.
```

The inherited swapped representative likewise gives

```text
<ω_swap,r_0>=-2,
<ω_swap,r_1>=-2,
<ω_swap,θ>=0.
```

More importantly, once `[θ]` is proved order two in `H_2(F_5;Z)`, **every** integer-valued homology character

```text
h:H_2(F_5;Z)->Z
```

must satisfy

```text
2h([θ])=h(2[θ])=0,
```

and torsion-freeness of `Z` forces

```text
h([θ])=0.
```

Hence every translation-valued integer formal 2-holonomy character

```text
Hol_h([c])=τ_(h([c]))
```

sends the nonzero filtered torsion class to the identity translation:

```text
Hol_h([θ])=τ_0=id.
```

If earned, the canonical classification is:

```text
NONZERO_FILTERED_BAR_H2_TWO_TORSION_CAN_BE_INVISIBLE_TO_EVERY_INTEGER_VALUED_FORMAL_TWO_HOLONOMY_CHARACTER
```

This does not contradict #773. #773 classifies formal transport-equivalence classes by integer characters on the full `H_2`; it never claimed that the evaluation map from homology classes into all integer characters is injective on torsion.

## 6. Mandatory finite-window anti-laundering scar

The implementation must explicitly preserve the observed finite-filtration warning:

```text
stable-looking H2 dimension across adjacent finite windows
!=
proved stable image in the filtered colimit.
```

At minimum, the chamber must verify one rational finite-window control showing that a two-dimensional filtered `H_2⊗Q` space can map with only one-dimensional image into the next filtration. This is a methodological hostile only; it carries no all-N claim.

The preferred bounded control is `F_4 -> F_5`.

## 7. Required invalids / abstentions

The certificate must refuse or quarantine:

- ambient syntactic triples that are not reachable quotient coordinates;
- bar generators containing the unit coordinate in normalized slots;
- total-weight overflow beyond the requested `F_N`;
- false relation words whose quotient products differ;
- a claimed torsion conclusion without both `2θ` boundary and mod-2 non-boundary evidence;
- promotion from `F_5/F_6` to full `H_2(B;Z)`;
- inference that `[z]` generates full `H_2`;
- inference that integer-holonomy blindness means holonomy classification failed.

## 8. Candidate earned markers

Only if the exact-head witness passes may this chamber earn:

```text
FILTERED_BAR_H2_EXACT_TWO_TORSION_CLASS_EARNED_IN_F5
FILTERED_BAR_H2_TWO_TORSION_PERSISTS_THROUGH_F6
INTEGER_VALUED_FORMAL_TWO_HOLONOMY_BLINDNESS_TO_FILTERED_TWO_TORSION_EARNED
FINITE_WINDOW_HOMOLOGY_DIMENSION_IS_NOT_FILTERED_COLIMIT_STABILIZATION
```

Consequential bearing:

```text
FORMAL_TWO_HOLONOMY_CAN_BE_COHOMOLOGICALLY_COMPLETE_FOR_INTEGER_TRANSPORT_CLASSES_WHILE_REMAINING_NONFAITHFUL_ON_TORSION_HOMOLOGY_CLASSES
```

## 9. Hard ceilings

The following remain quarantined even after a green result:

```text
F5 torsion != full H2(B;Z) torsion
F6 persistence != all-N persistence
integer-character blindness != failure of #773 transport classification
filtered bar homology != operational T/Q loop space
filtered torsion != geometric topological defect
formal torsion return != Berry phase / gerbe torsion / physical flux
mod-2 detector != physical Z2 gauge field
bar filtration != geometric skeletal filtration
```

And still:

```text
GEOMETRIC_TWO_HOLONOMY_AUTHORITY = false
PHYSICAL_TWO_HOLONOMY_AUTHORITY = false
BERRY_OR_GERBE_HOLONOMY_AUTHORITY = false
CONNECTION_AUTHORITY = false
TWO_CONNECTION_AUTHORITY = false
CURVATURE_AUTHORITY = false
OPERATIONAL_PATH_TWO_GROUPOID_AUTHORITY = false
```

#718 remains alive.

## 10. Collision membrane

```text
SRC Atelier #731/#758/#759 untouched
SRC continuation #771 untouched
#767 separate Western ancestry untouched
no fifth workflow
no merge
no publication
no production
no Vercel
no Proto-Loom/A16 promotion
```

Preregistration precedes every implementation mutation in this chamber.

𝌋‌⟐

Marked ⟐SAC[X6ZNK5NO51]
