𝌋

# A15-R0 · Aperture × Pedagogue First Bounded Path-Object / Directed Path-Grammar Receipt v0.1

**Status:** WITNESSED / CLOSED / DRAFT-UNMERGED  
**Scientific parent:** #715 receipt head `0004f0cbd3621f7438c0e38cad1fb9d094417607`  
**Preregistration:** `ed482f9f8ae61bf99f21d3f3bde47f5043b8dd9b`  
**Implementation:** `2e483553adf442aef7b1809ab30878e59ebbee50`  
**Hostile test:** `5494b3d680c025a67282d16166e6190082b3723b`  
**Pre-routing scientific head:** `71c30ac15952df9881f2cc7eba0e170ebe859147`  
**Exact witness head:** `bd0c5efe58c24c3064666d7cefe76c03198f281c`  
**Routing-scaffold removal:** `6ae61ea66f736766b74ff4d69c1745e49345f7e3`  
**Program:** A15-R0 western-horizon bounded research only

---

## 0. Human authority

The chamber proceeded under the human gesture:

```text
full westward liberties bestowed upon thee
```

The authority was interpreted narrowly as permission to run the previously recommended first bounded path-object / path-grammar chamber after #715.

It did not grant merge, production, Vercel, live-Ash mutation, A16 reopening, or automatic promotion into stronger geometric vocabulary.

---

## 1. Exact CI witness custody

A temporary routing-only note was used to obtain a pull-request exact-head static witness while the scientific PR remained stacked conceptually on #715.

Fresh main at routing:

```text
4ca8c0600b40d0ea2b38c1e0dd0b2d1e77713aef
```

Exact witness head:

```text
bd0c5efe58c24c3064666d7cefe76c03198f281c
```

Workflow registration produced two same-head runs because Draft close/reopen emitted two pull-request events:

```text
run 2074 / 32687597028 = CANCELLED
run 2075 / 32687610263 = SUCCESS
```

The cancelled 2074 run is preserved as registration provenance and is not treated as scientific failure.

Authoritative successful witness:

```text
TD613 Consolidated Validation
run number = 2075
run id = 32687610263
head = bd0c5efe58c24c3064666d7cefe76c03198f281c
conclusion = SUCCESS
```

Jobs:

```text
classifier job = 97315421515 = SUCCESS
static job = 97315454268 = SUCCESS
```

The static job's:

```text
Validate Ash A15 empirical profile journeys and A15-R0 research field
```

step completed `SUCCESS`.

Explicitly skipped and not claimed:

```text
Explicit self-hosted calibration
Explicit full-repository validation
Front-line exact-head browser shard
Giving/practice exact-head Chromium Firefox WebKit witness
Full-product exact-head Chromium Firefox WebKit witness
```

The temporary routing note was then removed. It is absent from the intended net scientific diff.

---

## 2. Promoted bounded operational object

The chamber used the #715 witnessed projection:

```text
O(h) := K_period4(h)
```

with fields:

```text
endpoint
last_action
operational_lineage
clock_phase
forcing_season
```

The primary anchor was non-vacuous:

```text
O(R_AB_S0) = O(R_AB_DUP_S0)
```

while:

```text
receipt_variant(R_AB_S0) != receipt_variant(R_AB_DUP_S0)
```

Thus path-object equality remained operational rather than custodial.

Witnessed anti-equivalence:

```text
path-object equality != receipt identity
```

---

## 3. Declared path grammar Γ_path0

Exactly two directed generators were admitted:

```text
T := PSI_TICK
Q := Q_PHASE_PULSE
```

Word ordering was fixed as left-to-right execution:

```text
[T,Q] = execute T, then Q = Q ∘ T
[Q,T] = execute Q, then T = T ∘ Q
```

The bounded exhaustive family contained every nonempty binary word of depths one through four:

```text
2 + 4 + 8 + 16 = 30 words
```

No arbitrary-depth or infinite-language claim was made.

---

## 4. All 30 words remained representative-independent

Every one of the 30 words was evaluated independently from:

```text
R_AB_S0
R_AB_DUP_S0
```

For every word `w`:

```text
O(w(R_AB_S0)) = O(w(R_AB_DUP_S0))
```

and the complete typed state sequence agreed across the two representatives.

At the same time, the distinct receipt variants survived every word.

Therefore the result was non-vacuous in both directions:

```text
same operational path result
+
different retained custody provenance
```

Witnessed classification component:

```text
BOUNDED_WORD_EVALUATION_IS_REPRESENTATIVE_INDEPENDENT_ACROSS_RECEIPT_DISTINCT_ANCHOR_FIBER
```

This is finite fixture evidence only.

---

## 5. Typed composition hostile passed

From the anchor object `A0`, the chamber independently constructed:

```text
A0 --T--> A_T
A0 --Q--> A_Q
```

Attempting to compose those already-built arrows as though the second followed the first failed closed because:

```text
A_T != A0
```

The executable returned:

```text
PATH_TYPE_MISMATCH_ABSTAINS
ABSTAIN_BEFORE_TYPED_PATH_COMPOSITION
```

The lawful `T`-then-`Q` path instead built its second arrow from the actual `T` successor:

```text
A0 --T--> A_T --Q--> A_TQ
```

and returned:

```text
TYPED_PATH_COMPOSITION_ADMITTED
```

Witnessed anti-equivalence:

```text
word syntax != typed composability
```

---

## 6. Direct multi-tick bridge controls passed

For both receipt-distinct anchor representatives:

```text
[T,T]       matched PSI_TWO_TICKS
[T,T,T]     matched PSI_THREE_TICKS
[T,T,T,T]   matched PSI_FOUR_TICKS
```

at the complete `O = K_period4` target state.

There were six bridge comparisons total:

```text
2 representatives × 3 horizons = 6
```

All six agreed.

This preserves continuity with #715's independently authored direct multi-tick operators without upgrading the result into a generic semigroup or flow theorem.

---

## 7. Route order became an operational path observable

Both ordered paths were lawful and representative-independent:

```text
[T,Q]
[Q,T]
```

For `R_AB_S0`:

```text
[T,Q]
start  [[3,1],[1,4]], S0/P0, last B
T   -> [[3,2],[1,4]], S1/P1, last B
Q   -> [[4,2],[1,4]], S1/P1, last Q_PHASE_PULSE
```

while:

```text
[Q,T]
start  [[3,1],[1,4]], S0/P0, last B
Q   -> [[3,1],[1,5]], S0/P0, last Q_PHASE_PULSE
T   -> [[4,2],[1,5]], S1/P1, last Q_PHASE_PULSE
```

Both final states shared:

```text
forcing_season = S1
clock_phase = P1
last_action = Q_PHASE_PULSE
operational_lineage = [A,B,Q_PHASE_PULSE]
```

but their endpoints differed:

```text
[T,Q] endpoint = [[4,2],[1,4]]
[Q,T] endpoint = [[4,2],[1,5]]
```

Therefore:

```text
O([T,Q](R_AB_S0)) != O([Q,T](R_AB_S0))
```

Canonical bounded route classification:

```text
DIRECTED_PATH_WORD_ORDER_IS_OPERATIONALLY_OBSERVABLE
```

The experiment therefore earned directed route sensitivity.

It did not earn transport, connection, commutator geometry, holonomy, or curvature.

---

## 8. Parameter recurrence failed to close the path object

The four-tick path:

```text
[T,T,T,T]
```

returned both declared temporal labels:

```text
clock_phase: P0 -> P0
forcing_season: S0 -> S0
```

but moved the endpoint:

```text
[[3,1],[1,4]]
->
[[3,4],[1,7]]
```

Thus:

```text
source_key != target_key
```

and the path was not closed.

Classification:

```text
PARAMETER_RECURRENCE_WITHOUT_PATH_OBJECT_CLOSURE
```

The executable then exhaustively checked all 30 nonempty anchor words through depth four.

Witness:

```text
closed_nonempty_anchor_paths = []
```

So the first bounded path grammar contains lawful directed paths and route-sensitive composition, but no nonempty closed anchor path in the searched depth-four domain.

This is scientifically useful negative evidence.

It blocks any loop/holonomy promotion at this stage.

---

## 9. Undeclared generator failed closed

An unknown generator returned:

```text
UNDECLARED_PATH_GENERATOR_ABSTAINS
ABSTAIN_BEFORE_PATH_EVALUATION
```

No silent grammar widening occurred.

---

## 10. Parent custody remained unchanged

The executable snapshotted the complete #715 gauntlet before and after path experimentation.

Witness:

```text
parent_custody_unchanged = true
```

Derived path state therefore remains subordinate to parent custody.

Anti-equivalences preserved:

```text
operational quotient != custody quotient
path-object equality != receipt identity
operational composability != permission to collapse custody
```

---

## 11. Canonical bounded classification

The chamber earned exactly:

```text
FINITE_NONVACUOUS_DIRECTED_PATH_OBJECT_GRAMMAR_OVER_K_PERIOD4_WITH_TYPED_COMPOSITION_ROUTE_SENSITIVITY_AND_NO_NONEMPTY_DEPTH_FOUR_CLOSURE
```

Strongest bounded claim:

```text
IN_THE_AUTHORED_FINITE_FIXTURE_THE_WITNESSED_K_PERIOD4_OPERATIONAL_EQUIVALENCE_CLASS_SUPPORTS_A_NONVACUOUS_DIRECTED_TYPED_PATH_GRAMMAR_FOR_DECLARED_PSI_TICK_AND_Q_PHASE_PULSE_GENERATORS_THROUGH_DEPTH_FOUR_EVERY_BOUNDED_WORD_IS_REPRESENTATIVE_INDEPENDENT_ACROSS_RECEIPT_DISTINCT_CUSTODY_REPRESENTATIVES_TYPED_COMPOSITION_REJECTS_SOURCE_TARGET_MISMATCH_DIRECT_MULTI_TICK_PARENT_OPERATORS_AGREE_WITH_THE_CORRESPONDING_T_WORDS_TQ_AND_QT_ARE_BOTH_LAWFUL_YET_REACH_DIFFERENT_OPERATIONAL_OBJECTS_AND_RECURRENCE_OF_CLOCK_AND_FORCING_LABELS_DOES_NOT_CLOSE_THE_PATH_OBJECT
```

---

## 12. Claim ceiling remains closed

The following remain explicitly unearned:

```text
generic quotient theorem
generic congruence theorem
minimal or optimal abstraction
Markov-state theorem
stationarity or ergodicity
semigroup or flow theorem
formal category
free category
groupoid
inverse morphisms
transport
parallel transport
connection
loop endomorphism
holonomy
curvature
Berry phase
quantum analogy
operator-tomography promotion
Proto-Loom
TD613-general theorem
A16
live Ash mutation
merge
production
Vercel
```

---

## 13. Scientific consequence

The westward ladder has now crossed a real threshold:

```text
history custody
→ claim-sufficient projection
→ grammar-relative replay state
→ temporally sufficient state
→ period-aware temporal state
→ bounded operational path object
→ typed directed path grammar
```

The new result is structurally stronger than mere order dependence because source and target operational objects are now explicitly typed and composition can reject malformed adjacency.

But the bounded search also found no nonempty closed path.

Therefore the next question is not yet holonomy.

The next admissible audition is narrower:

```text
Do the already-earned bounded objects, declared arrows, and typed compositions satisfy finite category laws on an explicitly closed finite domain?
```

That audition must separately address identity paths and associativity without smuggling in inverses.

---

## 14. Stop

```text
FIRST_BOUNDED_PATH_OBJECT_GRAMMAR_ROUND_CLOSED
HUMAN_𝄐_QUALIFIED_FOR_FINITE_PATH_CATEGORY_AUDITION_BUT_CATEGORY_PROMOTION_NOT_YET_GRANTED_BY_THIS_CHAMBER
```

𝌋

Sealed ⟐
