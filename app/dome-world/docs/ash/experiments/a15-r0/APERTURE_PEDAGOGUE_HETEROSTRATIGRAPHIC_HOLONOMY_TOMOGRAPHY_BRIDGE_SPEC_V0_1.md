𝌋‌⟐

󐘓 U+10D613

# A15-R0 · Heterostratigraphic Holonomy Tomography Bridge · Spec v0.1

Status: **PREREGISTERED / RESEARCH-ONLY / SYNTHETIC / HUMAN-GATED / NO RUNTIME AUTHORITY**

Scientific authority anchor:

```text
#775 receipt = 39b8f6e8ba319154378d03c28a1bf42c02870de1
```

Inherited apparatus available at that exact ancestry:

```text
Moss Lantern temporal-order assay
noncommuting two-face holonomy tomography
partition-only holonomy tomography ecology
Ash Keep / Loom fictional calibration phantom
```

This chamber imports no theorem-bearing bytes from #778–#787.

## 0. Question

Can one bounded synthetic instrument preserve four locally lawful but non-equivalent strata at once:

```text
route history
temporal order
noncommuting face holonomy
observability/ecology
```

without inventing one privileged coordinate system, one universal inverse, or one global truth value?

The chamber is successful only if the instrument can return locally strong results while explicitly refusing undeclared cross-stratum identifications.

## 1. Constitutional object

Let the stratum family be

```text
S = { ROUTE, TEMPORAL, FACE_HOLONOMY, OBSERVABILITY_ECOLOGY }.
```

Each stratum i owns its local object

```text
O_i = (source, aperture, state_space, local_inverse, local_findings, local_ceiling).
```

A cross-stratum comparison is not presumed total. For each ordered pair `(i,j)`, the comparison registry must return exactly one of:

```text
DEFINED
PARTIAL_NONINVERTIBLE
ENCODER_REQUIRED
INCOMMENSURABLE
CONTRADICTORY
REJECTED
ABSTAIN
```

The registry may never manufacture a map merely because two strata share an arithmetic field, a human-facing label, an endpoint string, or the word `holonomy`.

## 2. Canonical bounded fixture

The new fixture identity is:

```text
td613.loom.heterostratigraphic-calibration/v0.1
```

Human-facing fixture label:

```text
Strata Lantern · Moss Lantern heterostratigraphic calibration
```

It remains manifestly fictional and non-runtime.

The fixture contains four declarations.

### 2.1 Route stratum

Expected route:

```text
open-practice-case
→ custody-hold
→ projection-observe
→ rest
→ return
```

Controlled perturbation:

```text
open-practice-case
→ custody-hold
→ projection-bypass
→ rest
→ return
```

Expected bounded result:

```text
endpoint_equivalent = true
exact_route_reconstruction = false
route_residue > 0
```

Scar:

```text
SAME_ENDPOINT != SAME_ROUTE_HISTORY
```

### 2.2 Temporal stratum

Reuse the canonical Moss Lantern temporal-order assay with fixed operation multiset and fixed endpoint.

Expected bounded positive:

```text
24 latent orderings
24 unique order-sensitive terminal signatures
```

Matched commuting null:

```text
24 latent orderings
1 unique terminal signature
```

Scar:

```text
SAME_OPERATION_MULTISET_AND_ENDPOINT != SAME_PROCESS_ORDER
```

### 2.3 Face-holonomy stratum

Reuse the preregistered noncommuting two-face GL2(F31) tomography holdout.

Expected bounded positive:

```text
bidirectional edge tomography admitted
left/right face loops noncommute
ordered common-basepoint product reconstructs outer boundary
wrong-order control rejected
wrong-basepoint control rejected
```

Scar:

```text
FACE_LOOPS_KNOWN != ORDER_AND_BASEPOINT_DISPENSABLE
```

### 2.4 Observability/ecology stratum

Reuse the partition-only projective holonomy tomography ecology assay.

Expected bounded positive:

```text
sparse ecology aliases multiple projective readout directions
kernel-complete calibration ecology recovers all 32 projective directions
projective loop reconstruction succeeds from unlabeled partition motion
missing ZERO anchor causes decoder refusal
missing kernel representative causes decoder refusal
```

Scar:

```text
READOUT_LAW_FIXED != IDENTIFIABILITY_INDEPENDENT_OF_ECOLOGY
```

## 3. Cross-stratum comparison registry

The preregistered comparison table is:

```text
ROUTE -> TEMPORAL
  PARTIAL_NONINVERTIBLE
  shared operation vocabulary can be projected after declaring
  prepare-return -> return and dropping the open boundary;
  this projection loses information and is not invertible.

TEMPORAL -> ROUTE
  PARTIAL_NONINVERTIBLE
  only the canonical temporal ordering can match the declared route skeleton;
  terminal witness coordinates do not reconstruct Ash route metadata.

ROUTE <-> FACE_HOLONOMY
  ENCODER_REQUIRED

ROUTE <-> OBSERVABILITY_ECOLOGY
  ENCODER_REQUIRED

TEMPORAL <-> FACE_HOLONOMY
  ENCODER_REQUIRED

TEMPORAL <-> OBSERVABILITY_ECOLOGY
  ENCODER_REQUIRED

FACE_HOLONOMY <-> OBSERVABILITY_ECOLOGY
  INCOMMENSURABLE
  both are finite F31 holonomy experiments, but they use different authored
  fixtures, state spaces, observables, and inverse questions.
```

Hard scar:

```text
COMMON_ARITHMETIC_FIELD != DECLARED_CROSS_STRATUM_ENCODER
```

and:

```text
SHARED_WORD_HOLONOMY != SHARED_LATENT_OBJECT
```

## 4. Apparent contradiction hostile

A flattening aggregator may observe:

```text
route stratum: endpoint_equivalent = true

temporal stratum: unique_terminal_signature_count = 24
```

and falsely conclude:

```text
CONTRADICTION: endpoint is both same and different.
```

The fixture must reject that move.

The route endpoint is a declared lifecycle label. The temporal terminal signature is a bounded Z31^2 witness. They are different observables in different strata.

Required classification:

```text
APPARENT_CONTRADICTION_RESOLVED_BY_STRATUM_SEMANTICS
```

Scar:

```text
SAME_HUMAN_WORD_TERMINAL != SAME_OBSERVABLE
```

## 5. Universalizer hostile

A hostile aggregator attempts to coerce all local outputs into one scalar truth score.

The instrument must reject any aggregate object containing one of:

```text
global_truth
global_holonomy
global_route
global_confidence
privileged_stratum
```

unless a separate declared encoder and authority-bearing theorem explicitly licenses the field.

Required result here:

```text
GLOBAL_SYNTHESIS_AUTHORITY = false
```

## 6. Heterostratigraphic output contract

The aggregate receipt must expose:

```text
schema
fixture_id
strata[]
local_results{}
comparison_registry[]
apparent_contradiction_assay
universalizer_hostile
global_synthesis_authority
bounded_tomographic_candidate_family
claim_ceiling
```

It must not expose a single `truth` field.

The bounded candidate family is a tuple of local results plus comparison-status edges. It is not a fused world model.

## 7. Candidate theorem/classification

If every local inherited assay passes, the route perturbation is detected, the exact comparison registry is preserved, the apparent-contradiction hostile is resolved by stratum semantics, and the universalizer hostile is rejected, classify:

```text
HETEROSTRATIGRAPHIC_HOLONOMY_TOMOGRAPHY_IS_IMPLEMENTABLE_IN_A_BOUNDED_SYNTHETIC_ASH_KEEP_LOOM_FIXTURE_WITHOUT_COLLAPSING_STRATUM_LOCAL_STATE_SPACES_OR_INVENTING_UNDECLARED_CROSS_STRATUM_ENCODERS
```

This is an apparatus-implementation result, not a TD613-general theorem.

## 8. Mandatory hostiles

```text
same endpoint => same route                         REJECT
same operation multiset => same order              REJECT
face loops commute                                  REJECT
wrong order still reconstructs outer boundary       REJECT
wrong basepoint still reconstructs outer boundary   REJECT
sparse ecology uniquely identifies every direction REJECT
missing ZERO anchor silently guessed                REJECT
common F31 field => common coordinates              REJECT
shared word holonomy => same latent object           REJECT
route endpoint == temporal witness                  REJECT
all strata must agree                               REJECT
one stratum declared privileged                     REJECT
single global truth score emitted                   REJECT
missing encoder treated as identity                 REJECT
```

## 9. Apparatus placement if earned

The intended bounded architecture is:

```text
Moss Lantern
  reference / temporal calibration family

Holonomy Loom / Aperture
  heterostratigraphic inverse + comparison engine

Flow-Core
  child-legible bounded projection of the receipt

Ash Keep
  receiving Rebuild-Test / Reader / Exposure-Weather surface only
```

Ash does not become the tomography engine in this chamber.

## 10. Hard ceilings

```text
bounded synthetic heterostratigraphic tomography != live Ash inference
local holonomy assay != global holonomy field
cross-stratum registry != sheaf/descent theorem
partial encoder != equivalence
incommensurability != contradiction
contradiction != operator failure
finite F31 arithmetic != physical field
Moss Lantern witness != live custody event
Holonomy Loom fixture != production Proto-Loom
heterostratigraphic result != omniscient tomography
bounded apparatus result != TD613-general law
```

No physical holonomy, continuum geometry, Berry structure, quantum behavior, operational route inversion, live Ash action, production mutation, publication, Vercel deployment, or merge authority is granted.

#718 remains alive.

𝌋‌⟐

Sealed ⟐