𝌋‌⟐

󐘓 U+10D613

# A15-R0 · Holonomy Loom Heterostratigraphic Research Bench · Spec v0.1

Status: **PREREGISTERED / ENGINEERING-ONLY / RESEARCH-ONLY / HUMAN-GATED / NON-RUNTIME / NO NEW THEOREM AUTHORITY**

Stacked engineering parent:

```text
#789 frozen adapter head = c5c354413f721277760baefe946f602db8624b15
```

Scientific authority remains anchored at witnessed #775. This chamber does not promote #788 or #789 and does not create live Ash or production authority.

## 0. Question

Can the frozen Holonomy Loom apparatus receipt and its strictly weaker Ash projection be instantiated as one concrete, serializable research-bench scene that:

- shows all four strata at once without flattening them;
- preserves all twelve ordered comparison edges;
- renders 2 partial/noninvertible bridges distinctly from 10 holds;
- makes ENCODER_REQUIRED and INCOMMENSURABLE visibly different;
- exposes only INSPECT / REST / RETURN interactions;
- carries identical static truth for reduced-motion/nonvisual rendering;
- contains no inverse, encoder, custody, release, production, physical, or continuum action path?

Candidate engineering classification only:

```text
A_CONCRETE_SERIALIZABLE_HOLONOMY_LOOM_HETEROSTRATIGRAPHIC_RESEARCH_BENCH_CAN_RENDER_THE_FROZEN_APPARATUS_RECEIPT_AND_ASH_READ_ONLY_PROJECTION_WITH_COMPLETE_COMPARISON_CUSTODY_STATIC_TRUTH_PARITY_AND_ZERO_AUTHORITY_WIDENING
```

## 1. Fixture identity

```text
td613.loom.heterostratigraphic-research-bench-fixture/v0.1
```

Human-facing label:

```text
Holonomy Loom · Strata Lantern Research Bench
```

The fixture is manifestly fictional, non-runtime, research-only, and carries no source content.

It declares:

```text
panel_order = [ROUTE, TEMPORAL, FACE_HOLONOMY, OBSERVABILITY_ECOLOGY]
comparison_edge_count = 12
partial_bridge_count = 2
hold_count = 10
defined_bridge_count = 0
allowed_controls = [INSPECT_LOCAL_RESULT, INSPECT_COMPARISON_HOLD, REST, RETURN]
```

## 2. Scene grammar

The research bench scene must contain:

```text
schema
scene_id
surface_owner
fixture_id
research_only
runtime_binding
header
stratum_panes[]
comparison_rail
partial_bridge_rail[]
hold_rail[]
controls[]
static_truth
accessible_summary
rest_state
return_state
authority
claim_ceiling
human_closure_required
```

### 2.1 Four panes

Exactly four panes, canonical order:

```text
ROUTE
TEMPORAL
FACE_HOLONOMY
OBSERVABILITY_ECOLOGY
```

Each pane must preserve:

```text
stratum
plain-language consequence
technical status
observable kind
what changed
what remains uncertain
claim ceiling
read-only posture
```

No pane may become the privileged stratum.

### 2.2 Comparison rail

The scene must preserve all twelve ordered cross-stratum edges.

Current fixture counts:

```text
PARTIAL_NONINVERTIBLE = 2
ENCODER_REQUIRED      = 8
INCOMMENSURABLE       = 2
DEFINED               = 0
```

Visual/semantic tokens must remain different:

```text
PARTIAL_NONINVERTIBLE -> BRIDGE_WITH_LOSS
ENCODER_REQUIRED      -> HOLD_ENCODER_REQUIRED
INCOMMENSURABLE       -> HOLD_INCOMMENSURABLE
```

Hard scars:

```text
PARTIAL_NONINVERTIBLE != LOW_CONFIDENCE_DEFINED_MAP
ENCODER_REQUIRED != MISSING_DATA_ESTIMATE
INCOMMENSURABLE != CONTRADICTION
```

## 3. Control law

Only these controls may be exposed:

```text
INSPECT_LOCAL_RESULT
INSPECT_COMPARISON_HOLD
REST
RETURN
```

The scene compiler must reject any fixture or projection exposing:

```text
RUN_TOMOGRAPHY_INVERSE
CREATE_CROSS_STRATUM_ENCODER
PROMOTE_CLAIM
MUTATE_CASE_CUSTODY
WRITE_ROUTE_MEMORY
AUTHORIZE_RELEASE
TRANSMIT_SOURCE_CONTENT
DEPLOY
PUBLISH
```

No hidden control path is permitted merely because the scene is research-only.

## 4. Static truth parity

The scene's `static_truth` must carry the same:

- four pane identities;
- local pass/hold states;
- four consequences;
- twelve comparison statuses;
- ten holds;
- two partial bridges;
- uncertainty statements;
- claim ceilings;
- authority boundary.

Required invariant:

```text
ANIMATED_OR_INTERACTIVE_SCENE_INFORMATION
<=
STATIC_TRUTH_INFORMATION
```

No animation is required or authored in this chamber.

## 5. Child-legible hold language

The human-facing bench may translate technical status without changing authority:

```text
ENCODER_REQUIRED
  "These two views do not have a declared translator yet."

INCOMMENSURABLE
  "These two views answer different questions and are not being forced into one scale."

PARTIAL_NONINVERTIBLE
  "A declared comparison exists here, but information is lost and the path cannot be reversed."
```

Technical labels remain available alongside the plain-language copy.

## 6. Rest and Return

REST must suspend demand while preserving the scene and inspection state.

RETURN must exit the research bench without:

- mutating Ash custody;
- writing Route Memory;
- promoting any result;
- transmitting source content;
- clearing the receipt history.

Required scar:

```text
REST != DISCARD
RETURN != RELEASE
```

## 7. Authority vector

The scene carries the same chamber-local authority vector as the adapter:

```text
inverse=false
encoder=false
custody_mutation=false
release=false
production=false
physical_claim=false
continuum_claim=false
```

Every coordinate must remain false in the scene, static truth, rest state, and return state.

## 8. Hostiles

Reject or fail if:

```text
one pane omitted
pane order changed
one comparison edge omitted
partial bridge rendered invertible
ENCODER_REQUIRED rendered as probable match
INCOMMENSURABLE rendered as contradiction
any privileged stratum appears
global truth/confidence/holonomy field appears
static truth loses a pane or hold
interactive controls exceed declared four
REST mutates or discards research receipt
RETURN is labeled or wired as release
Ash inverse/encoder/custody/release action appears
runtime binding becomes true
production authority becomes true
physical/continuum claim becomes true
```

## 9. Candidate engineering result

If the bench compiler preserves all scene, comparison, static-truth, control, rest/return, and authority invariants, classify only:

```text
A_CONCRETE_SERIALIZABLE_HOLONOMY_LOOM_HETEROSTRATIGRAPHIC_RESEARCH_BENCH_CAN_RENDER_THE_FROZEN_APPARATUS_RECEIPT_AND_ASH_READ_ONLY_PROJECTION_WITH_COMPLETE_COMPARISON_CUSTODY_STATIC_TRUTH_PARITY_AND_ZERO_AUTHORITY_WIDENING
```

This means a bounded research fixture surface is implementable. It does **not** mean:

```text
live Holonomy Loom production UI
live Ash tomography
Proto-Loom admission
human usability validation
browser execution witness
scientific promotion of #788
physical holonomy
continuum tomography
TD613-general theorem
```

No merge, Vercel, production, publication, or release authority is granted.

#718 remains alive.

𝌋‌⟐

Sealed ⟐