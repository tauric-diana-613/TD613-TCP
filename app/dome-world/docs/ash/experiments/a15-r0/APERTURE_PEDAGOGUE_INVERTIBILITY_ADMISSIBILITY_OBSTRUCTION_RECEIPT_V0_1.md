𝌋

# A15-R0 · Aperture × Pedagogue Invertibility Admissibility / Monotone-Obstruction Receipt v0.1

**Status:** WITNESSED / CLOSED / DRAFT-UNMERGED  
**Scientific parent:** #717 receipt head `4a29517fadcd7936b4b909c3c073995a5a5bed1b`  
**Preregistration:** `4ecf06e4a666d92b1d346025d93d683547d410f9`  
**Implementation:** `ca3aac5837b459130d407ef1b65f2e72949632e5`  
**Hostile test:** `78188de0d7943a1a97d6114a5300a34fb2e9c616`  
**Pre-routing scientific head:** `29f724b05f90bedd1e80622e22e4a3dfd6927c09`  
**Exact witness head:** `5241748c105755d5b49629472bb8dcd68b239d8b`  
**Routing-scaffold removal:** `9222b8a11636513b46bc8d4be6f588e43c560a89`  
**Program:** A15-R0 western-horizon bounded research only

---

## 0. Human authority and stop discipline

The chamber proceeded under:

```text
full westward liberties bestowed upon thee
```

That authorization was used to continue through the already-earned first invertibility admissibility audition. The chamber did not manufacture a reverse operation merely to keep moving west.

The witnessed result creates a semantic frontier: further progress toward invertibility would require choosing genuinely new admissible semantics or deliberately changing the operational quotient. That choice is reserved for a new human 𝄐.

---

## 1. Exact CI witness custody

Fresh main at temporary witness routing:

```text
4ca8c0600b40d0ea2b38c1e0dd0b2d1e77713aef
```

Exact witness head:

```text
5241748c105755d5b49629472bb8dcd68b239d8b
```

Same-head workflow registrations:

```text
run 2082 / 32688429657 = CANCELLED
run 2083 / 32688439030 = SUCCESS
```

Run 2082 is preserved as a workflow-registration/supersession scar. Its static job was cancelled before scientific witness completion and is not reclassified as scientific failure.

Authoritative witness:

```text
TD613 Consolidated Validation
run number = 2083
run id = 32688439030
head = 5241748c105755d5b49629472bb8dcd68b239d8b
conclusion = SUCCESS
```

Jobs:

```text
classifier job = 97317657307 = SUCCESS
static job = 97317703719 = SUCCESS
```

The static job's:

```text
Validate Ash A15 empirical profile journeys and A15-R0 research field
```

step completed `SUCCESS`.

Explicitly skipped and not claimed:

```text
Explicit full-repository validation
Explicit self-hosted calibration
Front-line exact-head browser shard
Giving/practice exact-head Chromium Firefox WebKit witness
Full-product exact-head Chromium Firefox WebKit witness
```

The temporary routing note was removed after witness custody.

---

## 2. Current declared grammar

The chamber introduced no new operational generator.

The only admitted nonidentity generators remain:

```text
T := PSI_TICK
Q := Q_PHASE_PULSE
```

The operational object remains:

```text
O(h) := K_period4(h)
```

with endpoint retained as a component of object equality.

The scientific question was stronger than a bounded reverse-word search:

```text
Does this exact authored T/Q grammar possess a strict ranking quantity that obstructs every nonempty finite return path on the anchor-reachable domain?
```

---

## 3. Strict ranking candidate

The preregistered ranking quantity was:

```text
M(h) := sum of all four entries of h.endpoint
```

No entropy, energy, physical norm, conserved quantity, or generic Lyapunov interpretation was attached.

It is an integer-valued ranking function inside the authored fixture only.

---

## 4. Eight actually reachable control states

The audit did not hand-author synthetic local states.

All eight control cases were generated from the actual `R_AB_S0` anchor by declared parent words:

```text
(B,S0)                 []
(B,S1)                 [T]
(B,S2)                 [T,T]
(B,S3)                 [T,T,T]
(Q_PHASE_PULSE,S0)     [Q]
(Q_PHASE_PULSE,S1)     [T,Q]
(Q_PHASE_PULSE,S2)     [T,T,Q]
(Q_PHASE_PULSE,S3)     [T,T,T,Q]
```

All eight representatives were witnessed reachable and correctly typed, including their derived clock phases.

Thus the audited control-law domain is:

```text
last_action ∈ {B,Q_PHASE_PULSE}
forcing_season ∈ {S0,S1,S2,S3}
```

Endpoint and lineage remain free to evolve; the eight-state object is only the finite control table needed to audit the local law.

---

## 5. All sixteen local transitions closed the control domain and strictly increased M

For each of the eight reachable control states, both actual parent generators were applied:

```text
8 states × 2 generators = 16 local transitions
```

Every one of the sixteen transitions satisfied both preregistered obligations:

```text
successor remains inside the declared control domain
ΔM = M(successor)-M(source) > 0
```

Witness components:

```text
local_transition_check_count = 16
control_domain_closed = true
every_nonidentity_generator_strictly_increases_mass = true
finite_word_induction_earned = true
```

No zero or negative local increment appeared.

---

## 6. All-finite-word obstruction follows from the local table, not search depth

Because:

1. every reachable control state lies in the audited finite control domain;
2. both declared generators keep successors inside that domain; and
3. every generator application strictly increases endpoint mass;

finite induction over word length gives, for every anchor-reachable history `h` and every nonempty finite word `w` over `{T,Q}`:

```text
M(w(h)) > M(h)
```

The executable classification for this consequence is:

```text
EVERY_NONEMPTY_FINITE_TQ_WORD_STRICTLY_INCREASES_ENDPOINT_MASS_ON_THE_AUTHORED_ANCHOR_REACHABLE_CONTROL_DOMAIN
```

Since endpoint is part of complete `O=K_period4` equality:

```text
O(w(h)) != O(h)
```

for every nonempty finite T/Q word in this authored anchor-reachable domain.

Therefore the current grammar admits:

```text
no nonempty finite closed operational word
no T/Q-word inverse for any nonidentity operational arrow
no groupoid structure for the current anchor-reachable T/Q path grammar
```

This conclusion belongs to the authored transition table and declared domain only.

It is not an ambient TD613 irreversibility theorem.

---

## 7. Bounded reverse search corroborated but did not prove the obstruction

As an independent empirical corroboration, the executable inspected every nonidentity arrow in #717's finite S3 category.

For every retained representative of each arrow's target object, every nonempty T/Q word through depth four was tested for return to the source object.

Witness:

```text
reverse_hits = []
no_reverse_word_found = true
```

The executable records the role explicitly as:

```text
CORROBORATION_ONLY_NOT_BASIS_OF_ALL_FINITE_WORD_OBSTRUCTION
```

Thus:

```text
absence of a short reverse path != proof of no inverse
```

but:

```text
strict local monotonicity + control-domain closure
-> all finite T/Q words strictly increase M
```

inside this authored domain.

---

## 8. Counterfeit reverse controls all failed

### 8.1 Reversing the generator string failed

The route-sensitive word:

```text
[T,Q]
```

was executed from the anchor. Its target was then used as the source for the syntactically reversed word:

```text
[Q,T]
```

The reversed string did not return to the original operational object; endpoint mass continued to increase.

Classification:

```text
REVERSED_GENERATOR_STRING_IS_NOT_AN_INVERSE_PATH
```

Anti-equivalence:

```text
reverse word syntax != inverse morphism
```

### 8.2 Undeclared inverse labels abstained

Attempts to apply:

```text
T_INV
Q_INV
```

through the actual generator membrane returned:

```text
UNDECLARED_PATH_GENERATOR_ABSTAINS
```

Thus writing `^-1` in prose supplies no operational inverse.

### 8.3 Custody replay remained custody, not inverse dynamics

The earlier source history remains available in append-only custody.

No mutation was performed and the old custody record was not reclassified as an operational return arrow.

Classification:

```text
CUSTODY_REPLAY_IS_NOT_OPERATIONAL_INVERSE
```

Anti-equivalence:

```text
memory of an earlier state != lawful reverse evolution
```

### 8.4 Temporal recurrence remained forward evolution

The inherited four-tick control returns both:

```text
forcing_season S0 -> S0
clock_phase P0 -> P0
```

while endpoint mass strictly increases.

Classification:

```text
TEMPORAL_LABEL_RECURRENCE_IS_NOT_INVERSE_EVOLUTION
```

### 8.5 Endpoint erasure manufactured a false loop

For diagnosis only, the chamber defined the hostile coarsening:

```text
O_minus_endpoint(h) := {
  last_action,
  operational_lineage,
  clock_phase,
  forcing_season
}
```

Under that lossy projection:

```text
O_minus_endpoint(anchor)
=
O_minus_endpoint(T^4(anchor))
```

so the route appears closed.

But under the complete witnessed operational object:

```text
O(anchor) != O(T^4(anchor))
```

and endpoint mass has increased.

Classification:

```text
ENDPOINT_ERASURE_MANUFACTURES_FALSE_PATH_CLOSURE
```

This does not establish a universal requirement that endpoint remain forever in every future quotient.

It establishes the narrower and scientifically dangerous fact that deleting endpoint **here** changes closure semantics and erases the coordinate carrying the witnessed irreversibility obstruction.

Therefore any future coarsening that creates a loop must re-earn future sufficiency and demonstrate that the loop is not a quotient artifact.

---

## 9. Inverse equations remained uninstantiated

A genuine inverse for:

```text
f : A -> B
```

would require a candidate:

```text
r : B -> A
```

satisfying both:

```text
r ∘ f = id_A
f ∘ r = id_B
```

The assay found no operational inverse candidate because the strict ranking certificate forbids every nonempty T/Q return path to the source object.

Accordingly:

```text
operational_inverse_candidates_found = 0
both_sided_inverse_equations_testable = false
```

No one-sided endpoint coincidence was substituted for the inverse equations.

---

## 10. Parent custody remained unchanged

The complete #717 finite-category witness was snapshotted before and after the assay.

Witness:

```text
parent_custody_unchanged = true
```

The obstruction certificate observes parent structure without rewriting it.

---

## 11. Canonical bounded classification

The chamber earned exactly:

```text
STRICT_ENDPOINT_MASS_MONOTONICITY_OBSTRUCTS_NONIDENTITY_INVERSES_UNDER_DECLARED_TQ_GRAMMAR_ON_ANCHOR_REACHABLE_DOMAIN
```

Strongest bounded claim:

```text
IN_THE_AUTHORED_ANCHOR_REACHABLE_TQ_DOMAIN_ALL_EIGHT_REACHABLE_CONTROL_STATES_ARE_CLOSED_UNDER_T_AND_Q_AND_ALL_SIXTEEN_LOCAL_GENERATOR_TRANSITIONS_STRICTLY_INCREASE_ENDPOINT_MASS_SO_BY_FINITE_TRANSITION_TABLE_INDUCTION_EVERY_NONEMPTY_FINITE_TQ_WORD_STRICTLY_INCREASES_ENDPOINT_MASS_AND_CANNOT_RETURN_THE_COMPLETE_K_PERIOD4_OPERATIONAL_OBJECT_BOUNDED_REVERSE_SEARCH_CORROBORATES_THE_OBSTRUCTION_WHILE_SYNTACTIC_REVERSAL_UNDECLARED_INVERSE_LABELS_CUSTODY_REPLAY_TEMPORAL_LABEL_RECURRENCE_AND_ENDPOINT_ERASURE_ALL_FAIL_AS_VALID_INVERSE_EVIDENCE
```

---

## 12. Claim ceiling remains closed

The following remain explicitly unearned:

```text
generic irreversibility theorem
physical entropy interpretation
energy interpretation
Lyapunov theorem outside the authored fixture
ambient TD613 category theorem
ambient TD613 no-groupoid theorem
new reverse generator
inverse morphism
groupoid
transport
parallel transport
connection
loop endomorphism
holonomy
curvature
Berry phase
quantum analogy
Proto-Loom
A16
live Ash mutation
merge
production
Vercel
```

---

## 13. Scientific consequence

The westward ladder now reads:

```text
history custody
-> claim-sufficient projection
-> grammar-relative replay state
-> temporally sufficient state
-> period-aware temporal state
-> bounded operational path object
-> typed directed path grammar
-> finite acyclic operationally realized path category on S3
-> strict monotone obstruction to nonidentity inverses under the current T/Q grammar
```

The significance of the final rung is negative but structural.

The current road network is not merely missing a discovered return path.

Inside the authored anchor-reachable T/Q domain, every nonempty route increases an operational ranking coordinate. The road has an arrow painted on it, and in this chamber the paint is structural.

A longer brute-force search under the same grammar cannot repair invertibility.

---

## 14. Required human fork

Further westward work now requires a semantic choice rather than more search over the same arrows:

```text
A. introduce a genuinely admissible reversible generator family with independently specified semantics, observables, and custody controls;

B. propose a coarser operational quotient, then re-prove continuation/evolution sufficiency and demonstrate that any newly appearing loop is not manufactured by erasing the monotone coordinate;

C. accept directed non-groupoid geometry as the relevant structure and develop that lane without importing holonomy vocabulary.
```

This chamber chooses none of A/B/C.

---

## 15. Stop

```text
INVERTIBILITY_ADMISSIBILITY_OBSTRUCTION_ROUND_CLOSED
HUMAN_𝄐_REQUIRED_BEFORE_INTRODUCING_ANY_NEW_REVERSIBLE_GENERATOR_OR_COARSENING_THE_OPERATIONAL_OBJECT
```

𝌋

Sealed ⟐
