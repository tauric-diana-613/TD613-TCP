# A15-R0 · Mod-Two Cohomology Ring: Essential Degree-Two Generators and Degree-One Cup Collapse

Status: **PREREGISTERED / SCIENCE UNEARNED PENDING CONSTITUTIONAL WITNESS**

Scientific parent:

```text
#775 receipt = 39b8f6e8ba319154378d03c28a1bf42c02870de1
```

This chamber depends only on witnessed #775 and standard group-cohomological UCT/Lyndon-Hochschild-Serre facts for

```text
1 -> Z^2 -> G=Z^2 ⋊_swap Z -> Z -> 1.
```

It does not depend on #778, #780, #781, #782, #783, or #784.

## Preregistered degree-one classes

Define normalized mod-two 1-cocycles

```text
u(t,E,O)=t mod 2
v(t,E,O)=(E+O) mod 2.
```

Witnessed #775 gives `H1(G;Z)=Z^2`, hence

```text
H^1(G;F2)=F2<u,v>.
```

Both classes lift to integral homomorphisms (`t` and `E+O`). The chamber must certify

```text
u^2=0
v^2=0.
```

For the mixed product, inherited #775 gives the pointwise integral identity

```text
omega(x,y)=t(x)(E(y)+O(y))
omega = 2 kappa - dE*.
```

Reducing mod two requires

```text
u cup v = d(E* mod 2),
```

therefore

```text
u v = 0 in H^2(G;F2).
```

Preregistered scar:

```text
ALL_DEGREE_ONE_CUP_PRODUCTS_VANISH
!=
SECOND_COHOMOLOGY_VANISHES.
```

## Preregistered degree-two basis

Inherited #775 supplies

```text
kappa(z0)=1, kappa(theta)=0
beta(z0)=0, beta(theta)=1
H2(G;Z)=Z<z0> ⊕ (Z/2)<theta>.
```

Let `kappa_bar` be `kappa mod 2`.

UCT with `H1(G;Z)=Z^2` free gives

```text
H^2(G;F2) ≅ Hom(H2(G;Z),F2) ≅ F2^2.
```

The pairing matrix on `(z0,theta)` is preregistered as

```text
              z0  theta
kappa_bar      1    0
beta           0    1
```

so

```text
H^2(G;F2)=F2<kappa_bar,beta>.
```

`kappa_bar` is the reduction of the primitive integral class. `beta` cannot lie in the image of integral reduction because every integral H^2 character annihilates order-two `theta`, while `beta(theta)=1`.

Preregistered distinction:

```text
INTEGRAL_REDUCTION_SECTOR = F2<kappa_bar>
TORSION_SENSITIVE_NONLIFTING_SECTOR = F2<beta>.
```

## Essential degree-two theorem

Since

```text
u^2=v^2=uv=0
```

and `dim_F2 H^2=2`, the entire decomposable subspace generated by degree-one cups is zero:

```text
Dec^2(H^*) = im(H^1 ⊗ H^1 -> H^2) = 0.
```

Therefore both independent H^2 classes are indecomposable and every multiplicative generating set for `H^*(G;F2)` must contain at least two degree-two generators.

Candidate classification:

```text
THE_MOD_TWO_COHOMOLOGY_ALGEBRA_IS_NOT_GENERATED_IN_DEGREE_ONE_AND_REQUIRES_TWO_ESSENTIAL_DEGREE_TWO_GENERATORS.
```

## Preregistered degree-three / multiplicative closure

Recompute from the inherited mapping-torus/group model, without borrowing #780:

```text
H3(G;Z)=0
```

because the swap acts as `-1` on `H2(Z^2;Z)=Z`, so the degree-three Wang kernel is `ker(2:Z->Z)=0`.

UCT over `F2` then gives

```text
H^3(G;F2) ≅ Ext^1_Z(H2(G;Z),F2) ≅ F2.
```

The LHS spectral sequence has only columns `p=0,1` and no possible differentials. Under restriction to the fiber `N=Z^2`:

```text
beta|_N = a cup b      (fiber top class)
kappa_bar|_N = 0
v|_N = a+b
u|_N = 0.
```

The multiplicative LHS product must certify

```text
[u][beta] != 0
[v][kappa_bar] != 0
[u][beta] = [v][kappa_bar] = Omega,
```

where `Omega` is the unique nonzero element of `H^3(G;F2)`.

The chamber need not determine `v beta` as a named cochain-level representative unless independently certified; no unstated full-ring relation may be promoted.

Consequential candidate scar:

```text
NONZERO_TOP_MOD_TWO_COHOMOLOGY
!=
TOP_CLASS_GENERATED_BY_DEGREE_ONE_CUPS.
```

The degree-three class is reached only after adjoining essential degree-two information.

## Mandatory hostiles

```text
u^2 != 0                                      REJECT
v^2 != 0                                      REJECT
uv != 0                                       REJECT
H^2(F2) one-dimensional                       REJECT
beta integral-lifts                           REJECT
kappa_bar and beta linearly dependent         REJECT
H^2 generated by H^1 cups                     REJECT
one degree-two algebra generator suffices     REJECT
H3(G;Z) nonzero                               REJECT
H^3(G;F2)=0                                   REJECT
u beta = 0                                    REJECT
v kappa_bar = 0                               REJECT
claiming an uncertified value for v beta      REJECT
mod-two cohomology ring = physical field ring REJECT
```

## Hard ceilings

```text
mod-two cohomology != physical Z2 gauge sector
essential degree-two generator != hidden physical degree of freedom
cup product != operational route composition
LHS filtration != TD613 admissibility filtration
coefficient-born class != ontology creation
classifying-space cohomology != spacetime field content
```

#718 remains alive.

𝌋‌⟐

Sealed ⟐
