𝌋

# Aperture × Pedagogue Operator-Response Reconstruction × Coordinate-Equivalence Receipt v0.1

**Schema:** `td613.a15-r0.aperture-pedagogue-operator-response-reconstruction-coordinate-equivalence-receipt/v0.1`  
**Program:** A15-R0  
**PR:** #704  
**Scientific parent:** #703 receipt head `e34993edd1e55f064aabe8d71f7be99561262466`  
**Receipt status:** EVIDENTIARY CLOSURE / DRAFT-STACKED / NO PROMOTION AUTHORITY

## 1. Custody coordinates

The bounded response-reconstruction chamber has two distinct CI witness coordinates and one later documentation coordinate.

### First witness — permanent representation-level failure

```text
run_number = 2057
run_id = 32670804353
witnessed_head = 47927c9f0ca1b31da87df2b8ff9548f924a494d6
static_job_id = 97271287193
conclusion = failure
failure_surface = A15-R0 operator-response reconstruction hostile contract
failure = JavaScript deep-strict signed-zero mismatch
actual = [0,1,-0,-1]
expected = [0,1,0,-1]
```

Run 2057 remains failed forever. It is not retroactively reclassified by the later repair or successful witness.

The failure did not alter the preregistered probe family, response values, rank/determinant targets, frozen nullspace, held-out responses, coordinate transform, partial-coordinate hostile tables, naming criteria, or claim ceilings.

### Representation repair

```text
repair_commit = b36a0e60fb5f6388e007f25c759f676c8d954378
repair = canonicalize IEEE-754 -0 to semantic 0 at computed measurement-row boundary
```

The repair changes representation only. It does not change the authored mathematical fixture.

### Re-witness — exact-head pass

```text
run_number = 2058
run_id = 32671492898
witnessed_head = 8ff4869e8cfad7b3f1138bc0977ced491f0b30aa
static_job_id = 97272928049
conclusion = success
A15-R0_static_execution = success
browser_shards = skipped
full_repository_validation = skipped
self_hosted_calibration = skipped
literal_stdout = NOT_RETRIEVED_BY_CONNECTOR
```

The existing A15-R0 static gate imported and executed the hostile contract on the exact witnessed head. The contract survived that exact-head execution. No literal stdout line is reconstructed or quoted from source as though retrieved from CI.

The witness-routing note is part of the successful witness head. #704 was restored to its true stacked parent before this receipt was authored.

## 2. Witness time != documentation time

This receipt is necessarily later than the successful CI witness.

```text
receipt_document_commit != witnessed_head
witnessed_head = 8ff4869e8cfad7b3f1138bc0977ced491f0b30aa
```

The later receipt may describe the witnessed contract and GitHub job result. It may not claim that CI executed the receipt document itself.

## 3. Bounded scientific result

Within the authored finite 2x2 bilinear-response fixture, the exact-head hostile contract checks the complete frozen N1-N10 naming-exam membrane:

```text
N1  full hidden operator reconstructed from full-rank scalar response family
N2  incomplete response family retains nontrivial operator nullspace
N3  held-out response remains unidentified before completion
N4  later independent measurement completes held-out response prediction
N5  declared coordinate clone is independently reconstructed
N6  complete coordinate transformation preserves scalar response law
N7  partial coordinate transformations are rejected
N8  training-matching open-set source is defeated by held-out response
N9  reconstruction truth / held-out answer / coordinate-transform leakage is rejected
N10 claim-ceiling membranes remain preserved
```

The assay-local naming candidate is therefore permitted only in its explicitly bounded form:

```text
BOUNDED_FINITE_OPERATOR_TOMOGRAPHY_NAMING_CRITERIA_SATISFIED_IN_AUTHORED_2X2_BILINEAR_RESPONSE_FIXTURE
```

This token is a bounded experimental naming result, not a canonical program-promotion decision.

## 4. Canonical bounded claim

```text
A_COMPLETE_HIDDEN_2X2_LINEAR_OPERATOR_CAN_BE_RECONSTRUCTED_FROM_A_FULL_RANK_MIXED_FAMILY_OF_SCALAR_BILINEAR_INPUT_READOUT_RESPONSES_IN_THE_AUTHORED_SYNTHETIC_FIXTURE_WHILE_INCOMPLETE_RESPONSE_DATA_LEAVE_A_NONTRIVIAL_OPERATOR_NULLSPACE_AND_HELDOUT_RESPONSE_AMBIGUITY_AND_A_CONSISTENT_DECLARED_COORDINATE_CHANGE_RECONSTRUCTS_A_CONJUGATE_MATRIX_WITH_IDENTICAL_TRAINING_AND_HELDOUT_SCALAR_RESPONSES
```

## 5. Anti-equivalences retained

```text
raw operator representative != coordinate-invariant scalar response law
conjugating operator alone != complete coordinate change
full response reconstruction in one authored finite fixture != general operator tomography theorem
bounded naming candidate != canonical terminology promotion
successful exact-head static witness != A16 reopening
successful static witness != browser / production / human evidence
```

## 6. Claim ceiling

The following remain false / unauthorized:

```text
canonical_operator_tomography_promotion_authority = false
operator_tomography_general_theorem_earned = false
blind_tomography_earned = false
physical_tomography_earned = false
path_category_earned = false
path_transport_earned = false
holonomy_earned = false
curvature_earned_from_this_chamber = false
Berry_structure_earned = false
quantum_behavior_earned = false
Proto_Loom_earned = false
A16_reopened = false
live_Ash_mutation = false
merge_authority = false
production_authority = false
Vercel_authority = false
```

## 7. Next bounded western question

The next experiment must not reward the naming token by assuming arbitrary linear measurements are admissible interrogation actions.

For a single declared scalar bilinear probe,

```text
z = r T x
```

the induced linear functional on `vec(T)` has separable / rank-one coefficient geometry. Therefore:

```text
distinguishable by an arbitrary linear functional on vec(T)
!=
distinguishable by one admissible declared bilinear probe
```

The next bounded chamber may test admissible bilinear probe geometry against an unrestricted-functional hostile while keeping operator-tomography promotion authority false.

𝌋

Sealed ⟐
