# A15-R0 · Parallel-Lift Fiber Return Audition · Spec v0.1

󐘓 U+10D613

Status: **PREREGISTERED / PRE-IMPLEMENTATION**

## Authority and ancestry

```text
Westward gate            #737 · THREAD_SCOPED_ACTIVATION_005
scientific parent        #761 · parallel-path defect potential and zero-circulation
parent receipt            a71c884b63e82ed81fe13d1c95ebc060e4f7d2bb
```

This chamber bridges the earlier cocycle/extension line (#733–#738) with #761's sharpened pre-holonomy bearing.

No merge, publication, production, Vercel, Proto-Loom/A16, or ontology promotion follows from this preregistration.

## Frozen question

#761 ruled the inherited-gap decomposition defect out as a nonzero-holonomy source because its oriented comparison is a globally exact path-local potential difference.

The next lawful question is narrower than holonomy:

> Can two distinct authored T/Q routes with the same #729 quotient base induce different **bijective transports of the #738 integer cocycle-extension fiber**, so that comparing the two lifts yields a nonidentity return automorphism of the common fiber?

This chamber auditions that return automorphism only.

It does **not** preregister an operational loop, a connection, a path groupoid, or holonomy.

## Frozen objects

Let

```text
B = #729 quotient monoid
E_omega = Z x B
omega(x,y)=t(x)(E(y)+O(y))
```

from #734/#738.

For a finite authored word `w` in `{T,Q}*`, let

```text
pi(w) = #729 quotient coordinate in B
P(w)  = #733 exact first-block moment
```

and define the route-lift transport between canonically identified integer fibers

```text
T_w : Z_e -> Z_pi(w)
T_w(n)=n+P(w).
```

This is the fiber-coordinate action induced by the #738 free-route splitting section `Sigma_P(w)=(P(w),w)`.

`T_w` is a bijection of integer fibers with coordinate inverse

```text
T_w^{-1}(m)=m-P(w).
```

The inverse here is an inverse **fiber bijection**. It is not an inverse authored T/Q route and does not weaken #718's operational no-loop obstruction.

For two finite authored words `u,v` with the same quotient base

```text
pi(u)=pi(v)=b,
```

define the target-fiber return comparison

```text
R_(u|v)
  = T_u o T_v^{-1} : Z_b -> Z_b

R_(u|v)(n)=n+P(u)-P(v).
```

If `pi(u)!=pi(v)`, the return comparison must abstain.

## Preregistered obligations

The chamber passes only if all obligations below hold.

### O1 · lawful fiber transport

For every finite authored word `w` accepted by the witness:

```text
T_w(n)=n+P(w)
T_w^{-1}(T_w(n))=n
T_w(T_w^{-1}(m))=m
```

for integer fiber coordinates.

This proves bijectivity of the carried integer fiber map only.

### O2 · same-base return automorphism

For every tested same-base pair `(u,v)`:

```text
R_(u|v)(n)=n+DeltaP(u,v)
DeltaP(u,v)=P(u)-P(v).
```

It must be a bijective integer translation with inverse

```text
R_(v|u).
```

### O3 · identity and reversal

Exactly:

```text
R_(u|u)=id
R_(v|u)=R_(u|v)^{-1}.
```

### O4 · compositional comparison law

Whenever `u,v,w` all share one quotient base:

```text
R_(u|v) o R_(v|w) = R_(u|w).
```

This is a comparison-composition law over one common target fiber.

It is not an operational route-composition theorem.

### O5 · inherited TTQ/QTT wound

The foundational #735/#738 pair must close exactly:

```text
u=TTQ
v=QTT
pi(u)=pi(v)=(2,1,0)
P(u)=2
P(v)=0
R_(TTQ|QTT)(n)=n+2.
```

The return must be nonidentity.

### O6 · all-finite R_k relation family

For every finite integer `k>=0`, define

```text
L_k = T Q^k T Q
R_k = Q T Q^k T.
```

The symbolic certificate must establish for all finite `k`:

```text
pi(L_k)=pi(R_k)
P(L_k)=k+2
P(R_k)=k
DeltaP(L_k,R_k)=2
```

hence

```text
R_(L_k|R_k)(n)=n+2.
```

Finite executable samples may corroborate this claim, but the all-finite authority must be symbolic and may not be horizon-farmed.

### O7 · same-base / same-P distinct-route hostile

The inherited #733 collision

```text
u=TQTQT
v=QTTTQ
```

must satisfy

```text
u!=v
pi(u)=pi(v)
P(u)=P(v)=3
R_(u|v)=id.
```

Therefore:

```text
route distinction != nonidentity fiber return
extension return != complete route provenance.
```

### O8 · different-base abstention hostile

For any pair with

```text
pi(u)!=pi(v),
```

the return constructor must produce an explicit abstention status rather than force the two fibers into a common target.

### O9 · #738 extension consistency

For finite concrete controls, the transport amount `P(w)` must agree with the integer fiber coordinate carried by the #738 free-route first-moment splitting section.

This chamber may reuse witnessed parent functions but may not recursively replay the entire ancestor assay chain.

### O10 · operational-inverse quarantine

The witness and receipt must preserve:

```text
inverse fiber bijection != inverse T/Q route
same-base parallel comparison != operational closed path
return automorphism of carried fiber != holonomy
```

#718 remains untouched.

## Mandatory symbolic derivation for R_k

Write

```text
L_k = T Q^k T Q
```

with Q-blocks

```text
(q0,q1,q2)=(0,k,1).
```

Therefore

```text
P(L_k)=0*q0+1*q1+2*q2=k+2.
```

For

```text
R_k = Q T Q^k T
```

the Q-blocks are

```text
(q0,q1,q2)=(1,k,0),
```

so

```text
P(R_k)=k.
```

#726 already established the target-preserving relation family

```text
T Q^k T Q -> Q T Q^k T.
```

#728/#729 established that equal target normal form is exactly equal quotient coordinate in the retained source-relative jurisdiction. Thus this chamber may use the inherited relation family as quotient-base equality authority without re-proving #726–#729.

The fiber-return increment is therefore exactly

```text
DeltaP=2
```

for every finite `k>=0`.

## Mandatory anti-promotions

Success does not earn:

- an operational loop;
- an inverse T/Q morphism;
- a groupoid;
- a connection;
- a fundamental group / path groupoid;
- a holonomy representation;
- curvature;
- Berry / quantum analogy;
- physical or gauge interpretation;
- complete route provenance from the integer fiber;
- full H^2 classification;
- asymptotic or infinite-word claims;
- Proto-Loom / A16;
- merge, publication, production, or Vercel.

The strongest candidate wording allowed before witness is:

```text
SAME_BASE_PARALLEL_AUTHORED_ROUTES_MAY_INDUCE_NONIDENTITY_BIJECTIVE_RETURN_TRANSLATIONS_ON_THE_INTEGER_COCYCLE_EXTENSION_FIBER
```

and, if the all-finite `R_k` obligation survives:

```text
THE_RK_TARGET_EQUIVALENCE_RELATION_FAMILY_CARRIES_A_UNIFORM_PLUS_TWO_INTEGER_FIBER_RETURN_TRANSLATION
```

Do not promote either wording to holonomy without a later chamber establishing the missing path/comparison structure.

## Stop

After exact-head witness and receipt, stop at a fresh conceptual gate before any formal comparison-groupoid, relation-cell holonomy, connection, or curvature audition.

󐘓 U+10D613

𝌋

Preregistered ⟐