# A15-R0 · Parallel-Path Defect Potential and Zero-Circulation Audition · Spec v0.1

󐘓 U+10D613

Status: **PREREGISTERED / PRE-IMPLEMENTATION / DRAFT / OPEN / UNMERGED**

## Authority and ancestry

```text
Westward gate          #737 · Activation 004
parent chamber         #760 · commuting erasure diamond gap decomposition
parent receipt         1340cbf785547454ecbe365986b88b6ec9ff3283
```

This chamber is finite. It does not enlarge a horizon and does not authorize operational-loop, holonomy, curvature, connection, groupoid, Proto-Loom/A16, merge, publication, production, or Vercel claims.

## Frozen question

#760 proved that lawful finite quotient paths with one common composite erasure may have the same terminal admissibility objects `(U,I,Gamma)` while assigning different inherited/cross-settled genealogies to that same terminal wound.

For any lawful path `alpha` in one common-composite path family and terminal value `z in Gamma`, define the inherited-gap indicator

```text
h_alpha(z) = 1 if z in H^alpha
             0 otherwise.
```

For two lawful parallel paths `alpha,beta`, audition the **oriented comparison defect**

```text
delta_(alpha,beta)(z) = h_beta(z) - h_alpha(z) in {-1,0,+1}.
```

This is a candidate finite additive comparison transport over the terminal wound coordinates. It is not preregistered as holonomy.

## Frozen all-family obligations

For every finite lawful family of paths sharing:

```text
same antecedent rows/supports
same terminal state set
same composite quotient
same terminal Gamma
```

require exactly:

```text
1. identity
   delta_(alpha,alpha)=0.

2. reversal
   delta_(beta,alpha)=-delta_(alpha,beta).

3. additive pasting / composition
   delta_(alpha,gamma)
   = delta_(alpha,beta)+delta_(beta,gamma)
   coordinatewise over Z^Gamma.

4. closed comparison circulation
   for every finite cycle alpha_0,...,alpha_n=alpha_0,
   sum_i delta_(alpha_i,alpha_(i+1)) = 0.

5. support recovery
   support(delta_(alpha,beta))
   = H^alpha symmetric-difference H^beta
   = #760 D(alpha,beta).

6. oriented gain/loss recovery
   delta=+1 exactly on H^beta\H^alpha;
   delta=-1 exactly on H^alpha\H^beta.

7. refinement orientation
   if alpha's induced partition refines beta's, then
   H^alpha subseteq H^beta and delta_(alpha,beta)>=0 coordinatewise.
```

The all-family authority is symbolic finite telescoping of path-local indicator potentials, not bounded path enumeration.

## Mandatory pasted-comparison hostile

Use the same antecedent/support wound family extending #760:

```text
X={x1,x2,x3,x4}
Z={a,b}
K_x1={a,b}
K_x2={a}
K_x3={b}
K_x4={}
terminal W={w}
Gamma={a,b}.
```

Four lawful factorizations of the same composite:

```text
A={{x1,x2},{x3,x4}}       -> H_A={b}
B={{x1,x3},{x2,x4}}       -> H_B={a}
C={{x1},{x2},{x3},{x4}}   -> H_C={}
D={{x1,x2,x3,x4}}         -> H_D={a,b}.
```

Require the exact oriented vectors in coordinate order `(a,b)`:

```text
delta_AB=(+1,-1)
delta_BD=(0,+1)
delta_AC=(0,-1)
delta_CD=(+1,+1)

delta_AD=(+1,0)
```

and both pasted routes in the comparison graph must agree:

```text
delta_AB + delta_BD
= delta_AC + delta_CD
= delta_AD.
```

Mandatory closed comparison cycle:

```text
A -> B -> D -> C -> A
```

must have exact circulation `(0,0)`.

## Mandatory anti-shortcut hostile

Pure defect cardinality must **not** be accepted as the additive transport object.

Using `A,B,C` above:

```text
|D(A,B)|=2
|D(B,C)|=1
|D(A,C)|=1
```

so ordinary integer cardinality addition fails:

```text
2+1 != 1.
```

The signed coordinate vector is therefore materially stronger than a defect count.

## Consequential decision rule

If all frozen obligations pass, classify only:

```text
PARALLEL_PATH_GAP_GENEALOGY_ADMITS_AN_EXACT_ORIENTED_INTEGER_POTENTIAL_DIFFERENCE_WITH_ADDITIVE_PASTING_AND_ZERO_CLOSED_COMPARISON_CIRCULATION
```

and:

```text
THE_760_PARALLEL_PATH_DEFECT_IS_GLOBALLY_INTEGRABLE_AS_A_DIFFERENCE_OF_PATH_LOCAL_H_INDICATORS_AND_CANNOT_BY_ITSELF_SUPPLY_NONZERO_HOLONOMY
```

This is a negative pre-holonomy bearing: a path-sensitive quantity may still be too exact/integrable to constitute holonomy.

If any identity, reversal, additive-pasting, support, refinement, or zero-circulation obligation fails, the corresponding theorem claim is falsified and must not be repaired by weakening the comparator.

## Claim ceiling

Even on success:

```text
comparison cycle != operational loop
zero comparison circulation != physical flat connection
potential difference != connection
path-sensitive decomposition != holonomy
failure to obtain holonomy here != impossibility of holonomy in a richer carried state
```

#718's operational nonloop obstruction remains binding.

No asymptotic extension. No H8 farming. No probability/entropy. No source erasure. No route provenance reconstruction. No merge/publication/production/Vercel.

## Preregistered stop

```text
PARALLEL_PATH_DEFECT_POTENTIAL_AUDITION_PENDING
HUMAN_𝄐_REQUIRED_AFTER_WITNESSED_CLASSIFICATION_BEFORE_ANY_NONINTEGRABLE_TRANSPORT_OR_RETURN_ENDOMORPHISM_AUDITION
```

󐘓 U+10D613

𝌋

Preregistered ⟐