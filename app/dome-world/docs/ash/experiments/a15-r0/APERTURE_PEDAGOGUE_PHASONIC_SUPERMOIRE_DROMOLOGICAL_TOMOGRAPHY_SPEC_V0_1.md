# A15-R0 · Phasonic Supermoiré Cupola / Dromological Holonomy Tomography

Status at preregistration: **HYPOTHESIS ONLY / ZERO NEW THEOREM AUTHORITY**.

Exact witnessed scientific parent:

```text
#798 AIA receipt
9c92b4269fe2cd277799d8e885caf7765cbdfecb
```

This chamber asks whether a bounded, symbolic triple heterostratigraphic overlay can exhibit all of the following simultaneously without widening source or receiver authority:

1. exact algebraic carrier incommensurability across three declared strata;
2. a nontrivial order-dependent formal holonomy operator;
3. an exactly invertible dromological observation schedule;
4. a hostile temporal permutation of the same strata whose observation map is noninjective;
5. a child-legible Ash projection that preserves constitutional custody while withholding the technical inverse;
6. a technical Loom receiver that can execute the bounded inverse only inside the preregistered fixture.

No physical quasicrystal, physical moiré material, continuum tomography, optical apparatus, physical holonomy, geometric dome, or live child/user binding is claimed.

## 1. Declared symbolic carrier field

Use the exact formal basis

```text
B = (1, sqrt(3), phi, phi*sqrt(3))
```

as a free rank-four `Z` module for this fixture. Coordinates in this basis are compared exactly; no floating-point approximation is permitted.

The basis is a symbolic algebraic-coordinate registry. The implementation does not attempt to numerically prove algebraic independence or construct a full number field. Its jurisdiction is the declared free module fixture.

Declared strata:

```text
PHI_PAIR_WIRE       = span_Z{1, phi}
HEXAGONAL_MOIRE     = span_Z{sqrt(3)}
ICOSAHEDRAL_PHASON  = span_Z{phi*sqrt(3)}
```

Within the declared free-module fixture, the three submodules have pairwise zero intersection and their sum has rank four.

Candidate bounded classification:

```text
THE_DECLARED_PHI_PAIR_WIRE_HEXAGONAL_MOIRE_AND_ICOSAHEDRAL_PHASON_CARRIER_MODULES_ARE_PAIRWISE_INCOMMENSURABLE_INSIDE_THE_PREREGISTERED_FREE_RANK_FOUR_ALGEBRAIC_COORDINATE_FIXTURE
```

Primary scar:

```text
FORMAL_CARRIER_MODULE_INCOMMENSURABILITY != PHYSICAL_QUASICRYSTAL_INCOMMENSURABILITY
```

## 2. Triple heterostratigraphic phason state

Let the latent registered phason state be

```text
x = (x1, x2, x3)^T in Z^3
```

with the hostile assay exhaustively checking the finite cube

```text
-2 <= xi <= 2.
```

The `phasonic supermoiré cupola` is the bounded registered overlay object consisting of:

- the exact parent #798 AIA custody witness;
- the three stratum carrier registries;
- the latent phason state;
- the ordered stratum operators;
- the observation aperture;
- the formal loop holonomy operator;
- the receiver-indexed views.

The word `cupola` names this registered overlay object. It does not assert a geometric embedding.

## 3. Dromological stratum operators

Use exact unimodular integer operators:

```text
P = PHI_PAIR_WIRE
    [ 1  0  0 ]
    [-1  1  0 ]
    [ 0  0  1 ]

H = HEXAGONAL_MOIRE
    [ 1 -1  0 ]
    [ 0  1  0 ]
    [ 0  0  1 ]

I = ICOSAHEDRAL_PHASON
    [ 1  0 -1 ]
    [ 0  1  0 ]
    [ 0  0  1 ]
```

All have determinant `+1`.

The single declared observation aperture is

```text
c = [1 0 0].
```

For an ordered schedule `A1,A2,A3`, the observation rows are

```text
c A1
c A2 A1
c A3 A2 A1.
```

This makes recoverability depend on temporal ordering while holding the stratum family and observation aperture fixed.

## 4. Canonical dromological inverse schedule

Preregister the order

```text
PHI_PAIR_WIRE -> HEXAGONAL_MOIRE -> ICOSAHEDRAL_PHASON.
```

Its observation matrix is

```text
O_PHI =
[1  0  0]
[2 -1  0]
[2 -1 -1]
```

with

```text
det(O_PHI) = +1.
```

For observations

```text
m1 = x1
m2 = 2*x1 - x2
m3 = 2*x1 - x2 - x3,
```

the exact inverse is

```text
x1 = m1
x2 = 2*m1 - m2
x3 = m2 - m3.
```

Candidate bounded classification:

```text
THE_PREREGISTERED_PHI_HEX_ICOSAHEDRAL_DROMOLOGICAL_SCHEDULE_HAS_A_UNIMODULAR_OBSERVATION_MATRIX_AND_EXACTLY_RECONSTRUCTS_THE_INTEGER_PHASON_STATE_ON_THE_DECLARED_FIXTURE
```

This inverse is a finite exact inverse for the declared symbolic model only.

## 5. Hostile temporal permutation

Use the same three operators and the same aperture, but swap the first two strata:

```text
HEXAGONAL_MOIRE -> PHI_PAIR_WIRE -> ICOSAHEDRAL_PHASON.
```

The observation matrix becomes

```text
O_HPI =
[1 -1  0]
[1 -1  0]
[1 -1 -1]
```

with

```text
det(O_HPI) = 0.
```

The first two rows are identical. The nonzero null vector

```text
v = (1,1,0)^T
```

satisfies

```text
O_HPI v = 0.
```

Therefore states `x` and `x+v` collide whenever both are inside the finite hostile cube.

Candidate bounded classification:

```text
THE_SAME_THREE_STRATA_AND_THE_SAME_OBSERVATION_APERTURE_CAN_BE_TOMOGRAPHICALLY_INVERTIBLE_IN_ONE_TEMPORAL_ORDER_AND_NONIDENTIFIABLE_IN_ANOTHER
```

Primary scars:

```text
SAME_STRATA != SAME_TOMOGRAPHIC_RECOVERABILITY
SAME_APERTURE != SAME_DROMOLOGICAL_INFORMATION
TEMPORAL_PERMUTATION != INFORMATION_PRESERVING_REORDERING
```

## 6. Formal holonomy order defect

The canonical ordered product is

```text
Hol_PHI = I H P =
[ 2 -1 -1]
[-1  1  0]
[ 0  0  1].
```

The hostile product is

```text
Hol_HPI = I P H =
[ 1 -1 -1]
[-1  2  0]
[ 0  0  1].
```

Thus

```text
Hol_PHI != Hol_HPI.
```

Both are exact integer matrices with determinant `+1`. The difference is an order-sensitive formal transport defect in the registered fixture.

Candidate bounded classification:

```text
THE_TRIPLE_OVERLAY_HAS_A_NONTRIVIAL_DROMOLOGICAL_FORMAL_HOLONOMY_ORDER_DEFECT_BECAUSE_THE_REGISTERED_ORDERED_PRODUCTS_DIFFER
```

Hard scar:

```text
FORMAL_ORDERED_MATRIX_HOLONOMY != PHYSICAL_OR_GEOMETRIC_HOLONOMY
```

## 7. Child-legible AIA projection

This chamber inherits #798's receiver law:

```text
custody recoverability can remain receiver-invariant
while payload recoverability is receiver-indexed.
```

Declare two views of the exact same cupola receipt:

### Ash child-legible receiver

May expose only:

```text
PHI_PAIR_WIRE      -> "two rhythms that do not share one repeating beat"
HEXAGONAL_MOIRE    -> "a six-way honeycomb beat layer"
ICOSAHEDRAL_PHASON -> "a golden-star layer with a sliding phase"
CUPOLA             -> "three layers make a pattern that remembers order"
```

plus the exact constitutional custody witness and a qualitative verdict:

```text
ORDER_MATTERS
```

It must not expose:

- latent phason coordinates;
- exact operator matrices;
- exact observation vector;
- inverse formulas;
- technical holonomy matrices;
- encoder authority;
- runtime authority;
- physical claims.

### Loom technical receiver

May expose the exact carrier coordinates, operators, observation matrices, formal holonomy products, and bounded inverse certificate.

Both receivers must carry the same exact custody witness and zero authority vector.

Candidate bounded classification:

```text
CHILD_LEGIBLE_AIA_CAN_PRESERVE_EXACT_CUSTODY_AND_THE_ORDER_MATTERS_TRUTH_WHILE_WITHHOLDING_THE_TECHNICAL_TOMOGRAPHY_INVERSE_FROM_ASH
```

## 8. Hostile suite

The implementation/test must reject or fail when any of the following is attempted:

1. replace the formal carrier basis with a coordinate-colliding basis;
2. claim pairwise incommensurability after assigning overlapping carrier coordinates;
3. mutate any canonical stratum operator while retaining the canonical determinant/inverse claim;
4. claim the hostile `H->P->I` schedule is invertible;
5. erase the null collision `(1,1,0)` from the hostile schedule;
6. claim schedule permutation preserves the holonomy matrix;
7. expose latent phason coordinates or inverse formulas in the Ash child projection;
8. widen any receiver authority coordinate;
9. label the bounded inverse as continuum tomography;
10. label formal ordered matrix holonomy as physical/geometric holonomy;
11. convert the child receiver into a cross-stratum encoder;
12. claim universal receiver ordering or universal inverse existence.

The hostile test must exhaustively verify exact canonical reconstruction for all `5^3 = 125` latent states in `[-2,2]^3` and exhibit at least one explicit hostile-order collision inside the same cube.

## 9. Candidate aggregate theorem

If and only if the exact implementation, finite hostile suite, hardening sentinel, and constitutional witness all pass, promote only this bounded classification:

```text
A_CHILD_LEGIBLE_ANISOTROPIC_INFORMATION_ARCHITECTURE_CAN_HOST_A_TRIPLE_HETEROSTRATIGRAPHIC_PHASONIC_SUPERMOIRE_TOMOGRAPHY_FIXTURE_IN_WHICH_EXACT_CUSTODY_IS_RECEIVER_INVARIANT_THE_CANONICAL_PHI_HEX_ICOSAHEDRAL_TEMPORAL_ORDER_IS_UNIMODULARLY_INVERTIBLE_A_HOSTILE_PERMUTATION_IS_NONIDENTIFIABLE_AND_THE_FORMAL_HOLONOMY_PRODUCT_IS_ORDER_SENSITIVE_WITH_ZERO_AUTHORITY_WIDENING
```

Short chamber name:

```text
PHASONIC_SUPERMOIRE_DROMOLOGICAL_HOLONOMY_TOMOGRAPHY
```

## 10. Hard ceilings

Even after a successful witness, the following remain false / unearned:

- physical φ-wire or pair-wire material;
- physical moiré interference experiment;
- complete 2D hexagonal lattice realization;
- complete 3D icosahedral cut-and-project quasicrystal realization;
- proof that the chosen labels uniquely model those physical geometries;
- optical, electronic, quantum, or condensed-matter tomography;
- continuum inverse problem;
- physical / Berry / Levi-Civita / gauge holonomy;
- geometric cupola or dome embedding;
- universal dromological inverse theorem;
- universal AIA receiver order;
- semantic equivalence of strata;
- live Ash child/user binding;
- live Holonomy Loom runtime;
- Proto-Loom / A16 promotion;
- #788 scientific promotion by implication;
- merge / publication / production / release / Vercel authority.

#718 remains alive.

Preregistered scar family:

```text
FORMAL_CARRIER_MODULE_INCOMMENSURABILITY != PHYSICAL_QUASICRYSTAL_INCOMMENSURABILITY
CARRIER_LABEL != COMPLETE_GEOMETRIC_REALIZATION
SAME_STRATA != SAME_TOMOGRAPHIC_RECOVERABILITY
SAME_APERTURE != SAME_DROMOLOGICAL_INFORMATION
TEMPORAL_PERMUTATION != INFORMATION_PRESERVING_REORDERING
FORMAL_ORDERED_MATRIX_HOLONOMY != PHYSICAL_OR_GEOMETRIC_HOLONOMY
BOUNDED_EXACT_INTEGER_INVERSE != CONTINUUM_TOMOGRAPHY_INVERSE
CHILD_LEGIBLE_TRUTH != TECHNICAL_SOURCE_RECONSTRUCTION
ORDER_MATTERS_TRUTH != INVERSE_FORMULA_DISCLOSURE
RECEIVER_INDEXED_TOMOGRAPHY != AUTHORITY_WIDENING
```

Sealed ⟐
