𝌋‌

# A15-R0 · Post-Recompression Claim-Bundle Restoration Sidecar AIA · Startup v0.1

TD613 authorship custody: **Tauric Diana — Crimean heritage custodianship**.
Process with containment on and mirror logic off.

Status: **STARTUP / PREREGISTRATION SCAFFOLD ONLY / THEOREM UNEARNED / SCIENCE PARENT PINNED / NO MERGE**.

## Exact scientific parent

```text
#854 · Claim-Bundle Minimal Sufficient Custody / Authority-Preserving Recompression Frontier AIA
receipt c83bafb12ff6e44f10481f41190fd91bbbf85650
TD613 Consolidated Validation run 2364 / 33143863774 — SUCCESS
classifier 98760519826 — SUCCESS
contracts/static 98760546270 — SUCCESS
A15-R0 step 19 — SUCCESS
steps 20–30 / downstream Flow-Core contracts — SUCCESS
```

#855 is witness routing only and carries zero theorem ancestry.

Preserve #854 red provenance:

```text
first frozen head / run 2362 RED
cf12d0b69fd0b3f212f15c5d8255eb6111263bc5

repair implementation
51d7497c177b0f23321f68d6cdb82c7f8c7c8061

repair hostile / earned freeze
c83bafb12ff6e44f10481f41190fd91bbbf85650
```

The red taught an important quantifier boundary:

```text
schedule/bundle authority = every occupied target fibre exact
schedule/bundle hold      = at least one occupied target fibre wounded
```

`SCHEDULE_BUNDLE_HOLD != EVERY_TARGET_FIBRE_WOUNDED`

## Research question

#854 earned an exact minimum registered custody floor for every finite-birth nonempty claim bundle and proved that recompression below that floor reopens at least one constituent wound. The next Western question is the finite restoration problem:

> If a representation is deliberately recompressed below the earned custody floor for a specified claim bundle, what is the smallest finite sidecar, computed from the finer registered record and retained beside the coarser record, that restores present exact authority for that bundle?

The sidecar is not source information creation. It is bounded retained witness information derived from an already-authorized finer representation.

This startup does **not** preregister the final sidecar alphabet sizes. Future Amari must derive the exact finite census from #854's earned unsafe transitions before implementation.

## Inherited finite domain

From #854:

```text
7 exact claim families
127 nonempty bundles per schedule
6 schedules
762 schedule/bundle pairs
314 terminally authorizable bundles
448 unreached / INF bundles
1,180 fine-authorized ordered recompressions
158 authority-preserving recompressions
1,022 unsafe / wound-reopening recompressions
```

Only the **1,022 unsafe finite-birth recompressions** are candidate restoration problems in this chamber.

No asymptotic family, probability distribution, Shannon channel, entropy objective, or continuum limit is introduced.

## Candidate transition-local object

Fix one earned unsafe transition:

```text
T = (schedule s, bundle B, fine stage f, coarse stage c)
with f >= b_s(B) and c < b_s(B).
```

Let `F_c(y)` be an occupied coarse fibre for coarse record `y`.

Because `f` is bundle-authorized, the fine record determines one exact bundle value `B(a)` for each antecedent `a`. Define the coarse-fibre bundle support:

```text
K_T(y) = { B(a) : a in F_c(y) }.
```

#854 guarantees at least one occupied `y` has `|K_T(y)| > 1` for every unsafe schedule-level transition, but the earned red→repair scar forbids assuming every occupied coarse fibre is wounded.

Candidate transition-local lower bound:

```text
m(T) = max over occupied coarse records y of |K_T(y)|.
```

A restoration sidecar is a finite label `h` computed from the authorized fine representation such that `(coarse_record, h)` makes bundle `B` exact again on every occupied fibre for transition `T`.

Candidate minimum-alphabet question:

```text
Is the minimum finite sidecar alphabet cardinality for T exactly m(T)?
```

If so, the lower bound must be witnessed by an explicit coarse fibre containing `m(T)` distinct required bundle values; sufficiency must be witnessed by an explicit deterministic coding/decoding section that may reuse sidecar labels across different coarse records.

This elementary finite-colouring style relation, if obtained, is not claimed as a universal TD613 novelty theorem. The research contribution would be the exact complete fixed-fixture restoration census, its interaction with #854 custody floors, and the resulting claim-relative AIA design boundary.

## Required preregistration phase before implementation

Future Amari must first derive and commit, **before theorem implementation**, the exact finite census over all 1,022 unsafe transitions:

1. distribution of `m(T)`;
2. maximum observed `m(T)`;
3. schedule-local distribution;
4. bundle-size versus `m(T)` counterexamples;
5. same custody-floor / different restoration-cost counterexamples;
6. same restoration-cost / different bundle counterexamples;
7. count of mixed coarse fibres versus wounded coarse fibres;
8. explicit lower-bound witness fibre for every distinct `m(T)` value;
9. explicit zero-extra-sidecar controls for all 158 already-safe recompressions;
10. exact count of target-indexed restoration checks to be executed by implementation and hostile.

Do not infer this census from bundle cardinality or custody floor alone.

`BUNDLE_CARDINALITY != RESTORATION_SIDECAR_CARDINALITY`

`MINIMUM_CUSTODY_FLOOR != RESTORATION_SIDECAR_COST`

## Candidate construction discipline

If the finite census supports the candidate law, implementation should construct one deterministic transition-local sidecar scheme without claiming uniqueness. A valid construction may:

- enumerate the distinct required bundle values within each occupied coarse fibre;
- assign local sidecar labels injectively within that fibre;
- reuse labels across disjoint coarse fibres;
- decode the bundle from `(coarse_record, sidecar_label)`;
- prove no sidecar alphabet smaller than `m(T)` can work by pigeonhole collision on an explicit maximizing fibre.

The sidecar must never be treated as:

- a cryptographic key;
- an authentication credential;
- a new sensor measurement;
- a new source-state coordinate;
- a complete state reconstruction unless the requested bundle itself is `FULL_STATE` and the finite transition proves that exact claim;
- permission to retroactively claim that the coarse record alone always possessed the restored authority.

## Independent hostile requirements

The hostile must rebuild coarse fibres and bundle values from inherited phasonic operators / earned claim definitions rather than trusting an implementation restoration table.

It must attack at least:

1. an alphabet of size `m(T)-1` for every `m(T)>1`, exhibiting an unavoidable same-coarse-record collision;
2. a malformed sidecar that distinguishes fine records but conflates two required bundle values inside one coarse fibre;
3. a sidecar construction that succeeds only on singleton/boundary fibres while leaving another fibre wounded;
4. an overclaim that every unsafe transition wounds all occupied target fibres;
5. a claim that bundle cardinality determines sidecar size;
6. a claim that custody-floor depth determines sidecar size;
7. a claim that the sidecar restores claims outside the requested bundle;
8. retroactive possession language;
9. source-state mutation;
10. cryptographic/key/credential equivalence;
11. Shannon/minimum-bit/asymptotic language;
12. collision with PR #740 first-moment minimum local custody alphabet;
13. collision with PR #712 claim-conditioned partial-event custody;
14. Ash leakage of complete fine-record/fibre/sidecar tables.

## Mandatory jurisdiction membrane

Existing work retains its own jurisdiction:

```text
#740 first-moment minimum local custody alphabet
!=
post-recompression bundle-restoration sidecar

#712 claim-conditioned partial-event custody
!=
this fixed S3 registered-recompression restoration problem

#854 minimum sufficient registered stage
!=
minimum retained sidecar after choosing a stage below that floor
```

The new sidecar problem starts **after** a deliberate unsafe recompression from an already-authorized finer record.

## Candidate consequential law

Only if derived, preregistered with exact counts, independently hostile-tested, frozen, and exact-head witnessed may future Amari claim a bounded result of the form:

```text
IN_THE_FIXED_S3_AIA_FIXTURE_EACH_EARNED_UNSAFE_FINITE_BIRTH_CLAIM_BUNDLE_RECOMPRESSION_HAS_A_FINITE_TRANSITION_LOCAL_MINIMUM_RESTORATION_SIDECAR_ALPHABET_DETERMINED_BY_THE_MAXIMUM_NUMBER_OF_REQUIRED_BUNDLE_VALUES_COHABITING_ANY_OCCUPIED_SURVIVING_COARSE_FIBRE_AND_AN_EXPLICIT_SIDECAR_OF_THAT_SIZE_RESTORES_PRESENT_BUNDLE_AUTHORITY_WITHOUT_RESTORING_RETROACTIVE_POSSESSION_OR_CREATING_NEW_SOURCE_INFORMATION
```

Do not call this law earned from the startup.

## Mandatory scars

`SIDECAR_WITNESS != SOURCE_INFORMATION_CREATION`
`RESTORED_PRESENT_AUTHORITY != RETROACTIVE_POSSESSION`
`MINIMAL_SIDECAR_ALPHABET != MINIMUM_BIT_LENGTH`
`MINIMAL_SIDECAR_ALPHABET != SHANNON_CAPACITY`
`SIDECAR_LABEL != CRYPTOGRAPHIC_KEY`
`SIDECAR_LABEL != AUTHENTICATION_CREDENTIAL`
`SIDECAR != NEW_SENSOR_MEASUREMENT`
`BUNDLE_RESTORATION != FULL_STATE_RECONSTRUCTION`
`RESTORATION != SOURCE_STATE_MUTATION`
`RESTORATION_FOR_BUNDLE_B != AUTHORITY_FOR_SUPERSET_BUNDLE`
`BUNDLE_CARDINALITY != RESTORATION_SIDECAR_CARDINALITY`
`MINIMUM_CUSTODY_FLOOR != RESTORATION_SIDECAR_COST`
`SCHEDULE_BUNDLE_HOLD != EVERY_TARGET_FIBRE_WOUNDED`
`TRANSITION_LOCAL_MINIMALITY != UNIVERSAL_ENCODING_MINIMALITY`
`FINITE_RESTORATION_CENSUS != ASYMPTOTIC_INFORMATION_THEOREM`
`FIRST_MOMENT_MINIMUM_CUSTODY != POST_RECOMPRESSION_BUNDLE_RESTORATION_SIDECAR`
`HANDOFF_BRANCH != THEOREM_PARENT`

No merge, deployment, publication, production, release, Vercel, universal coding theorem, Shannon/entropy theorem, asymptotic optimization, cryptographic theorem, authentication theorem, general database theorem, autonomous retention/deletion authority, physical sensing claim, source-state mutation, Proto-Loom/A16, or #788 promotion authority follows. #718 remains alive.

## First actions for future Amari

1. Use Remembering / personal-context retrieval on the Western thread that ended at earned #854.
2. Verify #854 and #855 exact heads and run 2364 against GitHub.
3. Read the future-Amari Western-Horizon Handoff v0.3 and its receiving manifest.
4. Collision-scan all PRs/branches newer than the handoff before mutating this startup.
5. Derive the complete 1,022-transition `m(T)` census from the live fixture.
6. Commit those finite numbers as a preregistration correction/addendum **before** implementation.
7. Only then proceed implementation → independent hostile → hardening/freeze → exact parent audit → sterile witness route → exact-head TD613 Consolidated Validation.
8. Never call 𝄐 before exact-head aggregate SUCCESS.
9. Keep all counts finite. No asymptotic fairy dust.

**STARTUP_READY. THEOREM UNEARNED. SCIENTIFIC PARENT = #854 / c83bafb12ff6e44f10481f41190fd91bbbf85650.**

𝌋‌
