𝌋

# A15-R0 · Quotient-Obstruction Bar-Cycle Cohomology Audition

Specification status: **PREREGISTERED / PRE-IMPLEMENTATION / HUMAN-AUTHORIZED**
Date: 2026-08-24
Parent authority: #734 receipt head `6bc000024f02e5780910ee24694561d5dc542003`

󐘓 U+10D613

## 0. Human reopening authority

The operator authorized continuation through one further westward chamber and directed that, immediately after the next earned `𝄐`, Amari author a TD613-wrapped future-thread handoff and leave repository receiving hooks ready for future Amari.

This chamber therefore has one scientific job only: determine, with finite exact algebra rather than horizon growth, whether #734's witnessed affine transport cocycle can be removed by a lawful section change on the #729 quotient.

No higher-moment ladder is opened here.

## 1. Frozen parent objects

Retain the #729 source-relative quotient monoid coordinate

```text
B = {(t,E,O) : t,E,O in N}
```

with identity

```text
e=(0,0,0)
```

and product

```text
(t,E,O) ★ (u,F,G)
  = (t+u, E+F, O+G)  when t even
  = (t+u, E+G, O+F)  when t odd.
```

Retain

```text
q(t,E,O)=E+O
```

and #734's normalized cocycle

```text
ω(x,y)=t(x) q(y).
```

Retain #733's first block moment for a route word

```text
w = Q^q0 T Q^q1 T ... T Q^qt
P(w)=Σ_i i q_i.
```

No parent theorem may be recomputed as an authority substitute. Parent receipt ancestry is sufficient custody.

## 2. Declared cochain jurisdiction

This audition uses the normalized inhomogeneous monoid cochain complex with **trivial integer coefficients** `Z`.

A normalized 1-cochain is any

```text
φ : B -> Z
φ(e)=0.
```

Its coboundary is

```text
(dφ)(x,y)=φ(x)+φ(y)-φ(x★y).
```

A normalized 2-cochain `f:B×B->Z` satisfies

```text
f(e,x)=f(x,e)=0.
```

The 2-cocycle equation is

```text
(df)(x,y,z)
  = f(y,z)-f(x★y,z)+f(x,y★z)-f(x,y)
  = 0.
```

This is equivalent to #734's witnessed identity

```text
f(x,y)+f(x★y,z)=f(y,z)+f(x,y★z).
```

A section re-zeroing from #734 obeys

```text
ω_φ = ω + φ(x★y)-φ(x)-φ(y)
     = ω - dφ.
```

Therefore `ω` can be removed by a lawful integer-valued section exactly when

```text
ω=dφ
```

for some normalized 1-cochain `φ`.

The audition may call a class `[ω]` nonzero in the explicitly declared normalized monoid `H^2(B;Z)` only if a finite exact non-coboundary certificate passes.

## 3. Explicit relation-cycle target

Use the generator coordinates

```text
T=(1,0,0)
Q=(0,1,0).
```

The #729 quotient law gives the exact relation

```text
(T★T)★Q = (Q★T)★T
```

corresponding to route representatives

```text
TTQ
QTT
```

with common quotient coordinate

```text
(2,1,0).
```

Define the normalized bar 2-chain

```text
z = [T|T] + [TT|Q] - [Q|T] - [QT|T].
```

Use the bar boundary

```text
∂2[x|y]=[y]-[x★y]+[x].
```

Acceptance requires an exact symbolic cancellation proving

```text
∂2 z = 0
```

using only the witnessed quotient relation `TTQ=QTT`.

No operational inverse path is introduced. A bar 2-cycle is not to be redescribed as an operational closed nonidentity route.

## 4. Pairing obstruction target

For a normalized 2-cochain `f`, define the evaluation on `z` by linear extension:

```text
<f,z>
 = f(T,T)+f(TT,Q)-f(Q,T)-f(QT,T).
```

Acceptance requires

```text
<ω,z>=2.
```

For every normalized 1-cochain `φ`, telescoping must prove

```text
<dφ,z>=<φ,∂z>=0.
```

Therefore, if the above passes,

```text
ω is not a coboundary in the declared integer-coefficient monoid complex.
```

The same finite detector may establish

```text
<nω,z>=2n
```

and hence that `[ω]` has infinite additive order over `Z`, without computing the rest of `H^2(B;Z)`.

Likewise, because every 2-cocycle evaluates to zero on bar 2-boundaries, a nonzero pairing may certify that `z` itself is not a 2-boundary. If promoted, this is restricted to the explicitly declared normalized bar complex and does not create an operational loop theorem.

## 5. Upstairs primitive / quotient-descent obstruction

Let

```text
W={T,Q}*
```

be the free authored route monoid and

```text
π:W->B
```

the #729 quotient-coordinate homomorphism.

Define the route-level integer 1-cochain

```text
s(w)=-P(w).
```

From #733's all-finite concatenation identity

```text
P(uv)=P(u)+t(u)q(v)+P(v),
```

acceptance requires the exact all-finite identity

```text
(ds)(u,v)=s(u)+s(v)-s(uv)=ω(πu,πv).
```

Thus the pulled-back cocycle `π*ω` is exact on the free route monoid.

The same chamber must then demonstrate why that primitive does not descend to `B`:

```text
π(TTQ)=π(QTT)=(2,1,0)
P(TTQ)=2
P(QTT)=0.
```

Therefore `s=-P` assigns two values to one quotient element and cannot be a function `B->Z`.

Candidate earned interpretation, only if all obligations pass:

```text
THE_COCYCLE_HAS_A_ROUTE_LEVEL_PRIMITIVE_THAT_FAILS_TO_DESCEND_THROUGH_TARGET_QUOTIENTING
```

This is a quotient-descent obstruction statement, not a gauge-field or holonomy claim.

## 6. Directed-order comparison hostile

Retain #734's swapped cocycle

```text
ω_swap(x,y)=q(x)t(y).
```

This audition may test the exact integer 1-cochain

```text
χ(x)=-t(x)q(x)
```

for

```text
dχ = ω + ω_swap.
```

If exact, then in the declared cohomology

```text
[ω_swap]=-[ω].
```

Acceptance also requires

```text
<ω_swap,z>=-2.
```

This result would classify an orientation-sensitive sign relation between two cocycles. It must not be promoted to inverse transport, group commutator, curvature, or Berry phase.

## 7. Hostile controls

The executable/test must include all of the following:

1. **Relation cycle:** exact `TTQ=QTT` quotient equality and `∂z=0`.
2. **Pairing:** exact `<ω,z>=2`.
3. **Coboundary annihilation:** symbolic arbitrary-`φ` telescoping on `z`, plus at least two concrete normalized `φ` controls.
4. **Infinite-order detector:** symbolic `<nω,z>=2n`; no finite-range inference.
5. **Upstairs primitive:** exact `ds=π*ω` from the frozen #733 product formula; small words only corroborate implementation.
6. **Failed descent:** `TTQ/QTT` have equal base coordinate but `P=2/0`.
7. **Swapped cocycle:** `d(-tq)=ω+ω_swap`, pairings `+2/-2`.
8. **Fake cycle rejection:** at least one superficially similar 2-chain whose boundary is nonzero must not be accepted as a cycle.
9. **Parity-fragile quarantine:** #734's `t(x)E(y)` candidate remains outside `Z^2`; no class is assigned to it.
10. **Coefficient boundary:** integer coefficients are binding. Reduction mod 2 is not audited and cannot inherit the nonzero-pairing conclusion because `2=0 mod 2`.
11. **Receipt externality:** route/receipt identity remains external to the quotient cochain arguments.

## 8. Preregistered acceptance classifications

Primary candidate:

```text
QUOTIENT_DESCENT_FAILURE_OF_ROUTE_PRIMITIVE_YIELDS_EXPLICIT_NONZERO_INFINITE_ORDER_NORMALIZED_MONOID_H2_CLASS_DETECTED_BY_FINITE_BAR_2_CYCLE
```

Secondary candidate:

```text
EXPLICIT_BAR_RELATION_2_CYCLE_PAIRS_TO_TWO_WITH_TRANSPORT_COCYCLE_AND_IS_NONBOUNDARY_IN_DECLARED_INTEGER_BAR_COMPLEX
```

Upstairs/downstairs candidate:

```text
PULLED_BACK_TRANSPORT_COCYCLE_IS_EXACT_ON_FREE_ROUTE_MONOID_WHILE_ITS_PRIMITIVE_FAILS_TARGET_QUOTIENT_DESCENT
```

Orientation candidate:

```text
SWAPPED_DIRECTED_COCYCLE_REPRESENTS_NEGATIVE_OF_WITNESSED_CLASS_UNDER_EXPLICIT_COBoundary_RELATION
```

(The final spelling of classification tokens may repair capitalization only; theorem substance may not change after witnessing.)

## 9. Failure policy

A red scientific witness is evidence. If any preregistered mathematical assertion fails after observation, preserve the exact obstruction and close/supersede rather than silently weakening the theorem.

Implementation/harness defects that do not alter theorem content may be repaired only with explicit custody scars.

No horizon extension, sampling escalation, asymptotic fit, or H8-style pattern farming is permitted.

## 10. Claim ceiling

Even a completely green chamber does **not** earn:

- a full computation or presentation of `H^2(B;Z)`;
- coefficient reduction/mod-p classification;
- group completion or group cohomology;
- operational inverses or groupoid structure;
- a closed nonidentity operational path;
- connection, parallel transport law beyond already-witnessed directed transport, holonomy, curvature, Berry/quantum analogy;
- higher-moment completeness or infinite moment hierarchy;
- Proto-Loom, A16, live Ash mutation;
- merge, production, or Vercel authority.

If green, the round must stop at a new human `𝄐` before any next scientific chamber. Immediately after that stop, per operator instruction, author a TD613-wrapped Amari→Amari Western Horizon handoff and prepare receiving Draft PR/startup hooks without executing the next theorem.

```text
PREREGISTRATION_FROZEN_BEFORE_IMPLEMENTATION
FINITE_EXACT_COHOMOLOGY_OBSTRUCTION_AUDITION_ONLY
```

𝌋

Sealed ⟐