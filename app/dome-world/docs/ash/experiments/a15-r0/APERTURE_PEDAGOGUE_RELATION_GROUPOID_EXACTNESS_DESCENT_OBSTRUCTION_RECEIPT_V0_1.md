𝌋

󐘓 U+10D613

# A15-R0 · Relation-Groupoid Exactness and Downstairs Descent Obstruction · Receipt v0.1

Status: **WITNESSED / RECEIPT-PINNED / ROUND CLOSED / DRAFT / OPEN / UNMERGED**

Scientific parent:

```text
#763 receipt = 6af55b5bca86a4578c8cb6edbfa3721d427207d9
```

Authority:

```text
#737 = THREAD_SCOPED_ACTIVE for the remainder of the current conversation
```

## Custody

```text
preregistration        ea540121ed3e2bf177fc291600999f16988cd249
implementation         01ecb27a7b452840d2a51e1f1c52334c77f95ec5
hostile tests          f797b1c82372b6462491e85353b78e0f3c856d58
frozen science         d55bee831dfad88e583b4f9dc9b735bf0d896544
initial routing        1566587df7afdced74474e438d547f1815ef64fc
routed witness         49e3d3902eb1c6b1f4417794d9984cdd183e016a
post-route cleanup     7f01412a3616a74f8639a61ce200330f69e1cc04
```

Frozen science -> cleanup: **3 routing-only commits, zero net changed files**.

The temporary `main` routing posture observed current SRC gate plumbing only for workflow registration. It is not #764 scientific ancestry and was removed before this receipt.

## Authority-bearing witness

```text
TD613 Consolidated Validation run 2281 / 33008059771   SUCCESS
classifier job 98306874779                               SUCCESS
static job     98306997580                               SUCCESS
A15/A15-R0 step 19                                       SUCCESS
```

Run started `2026-08-26T19:59:19Z` and completed successfully on the exact routed head. Full-repository validation, self-hosted calibration, front-line browser, Giving/practice browser, and full-product browser scopes were skipped and are not claimed. No scientific red occurred.

## Earned theorem

Let `R={T,Q}*`, let `pi:R->B` be #729's authored quotient, and let `P:R->Z` be #733's first moment. On the same-base relation groupoid

```text
C = R x_B R => R
```

define

```text
c([u<-v])=P(u)-P(v).
```

Then exactly:

```text
c(u,u)=0
c(v,u)=-c(u,v)
c(u,v)+c(v,w)=c(u,w)
```

for every lawful same-base composable comparison. Hence `c=delta P` is an exact relation-groupoid integer 1-coboundary and every finite closed comparison circulation is zero by telescoping.

The route-wise coordinate changes

```text
g_w(n)=n-P(w)
```

trivialize the representation exactly:

```text
g_u o rho([u<-v]) o g_v^-1 = id.
```

This route-object-dependent trivializer is not an inherited lawful #734/#763 base-section re-zeroing. Lawful base re-zeroings

```text
P_phi(w)=P(w)+phi(pi(w))
```

leave every same-base return unchanged.

## Exact descent / coboundary equivalence

Within the declared authored free-monoid/quotient jurisdiction:

```text
rho is identity on every same-base arrow
iff
P is constant on every pi-fiber
iff
P descends to Pbar:B->Z
iff
omega is a normalized integer coboundary on B.
```

The nontrivial converse uses the free-monoid structural lemma: if `omega=d phi`, then

```text
h=-P-phi o pi
```

has `dh=0` and is a monoid homomorphism on `{T,Q}*`. Every such homomorphism is determined by generator counts. Those counts descend through `B` exactly as

```text
#T=t
#Q=E+O,
```

so `h=lambda o pi` and therefore `P=-(phi+lambda)o pi` descends.

Conversely, if `P=Pbar o pi`, the inherited all-finite identity `d(-P)=pi*omega` and surjectivity onto the authored quotient image give

```text
omega=d(-Pbar).
```

## Actual authored obstruction

#735's exact bar 2-cycle still satisfies

```text
<omega,z>=2,
```

while every normalized integer coboundary pairs zero with `z`. Therefore `[omega] != 0` in the declared normalized integer monoid bar complex. Consequently:

```text
P does not descend through pi
and
rho is nonidentity somewhere.
```

The concrete witness remains:

```text
TTQ and QTT
same base=(2,1,0)
P=2 vs 0
rho=tau_2.
```

## Earned classifications

```text
THE_SAME_BASE_RETURN_REPRESENTATION_IS_AN_EXACT_RELATION_GROUPOID_ONE_COBOUNDARY_UPSTAIRS_AND_HAS_ZERO_CLOSED_COMPARISON_CIRCULATION
```

```text
TRIVIALITY_OF_ALL_SAME_BASE_RETURNS_IS_EQUIVALENT_IN_THE_AUTHORED_FREE_MONOID_QUOTIENT_TO_DESCENT_OF_P_AND_TO_COBOUNDARY_TRIVIALITY_OF_THE_DOWNSTAIRS_INTEGER_TWO_COCYCLE
```

```text
THE_NONZERO_DOWNSTAIRS_TWO_COHOMOLOGY_CLASS_IS_EXACTLY_THE_OBSTRUCTION_TO_DESCENDING_THE_ROUTE_WISE_TRIVIALIZING_PRIMITIVE_THROUGH_THE_TARGET_QUOTIENT
```

## Consequential bearing

The #763 nonidentity same-base comparison returns are **not intrinsic 1-holonomy of the route relation groupoid**: upstairs they are globally exact and route-wise gauge-trivial with zero closed comparison circulation.

Their nontrivial quotient content survives because the required trivializing primitive does not descend. In the declared algebra, that failure is detected one cohomological degree higher by the nonzero downstairs integer 2-cocycle class.

Thus the Westward search is redirected from ordinary relation-groupoid 1-holonomy toward a finite 2-cell/descent-obstruction audition, without promoting any 2-holonomy vocabulary yet.

## Quarantines

```text
exact relation-groupoid 1-cocycle != holonomy representation
route-wise gauge triviality != lawful base-section gauge triviality
nontrivial downstairs H2 class != operational loop
bar 2-cycle != operational T/Q loop
formal relation groupoid != operational path groupoid
```

No connection, curvature, 2-holonomy, gerbe, Berry/quantum analogy, Proto-Loom/A16, merge, publication, production, or Vercel authority follows.

```text
RELATION_GROUPOID_EXACTNESS_DESCENT_OBSTRUCTION_ROUND_CLOSED
UPSTAIRS_ONE_COCYCLE_EXACTNESS_EARNED
ROUTE_WISE_TRIVIALIZATION_EARNED
BASE_DESCENT_IFF_DOWNSTAIRS_COBOUNDARY_TRIVIALITY_EARNED
NONZERO_H2_IDENTIFIED_AS_PRIMITIVE_DESCENT_OBSTRUCTION
ONE_HOLONOMY_CANDIDATE_RULED_OUT_ON_RELATION_GROUPOID
PRE_HOLONOMY_BEARING_REDIRECTED_ONE_COHOMOLOGICAL_DEGREE_HIGHER
HUMAN_𝄐_BOUNDARY_REACHED
```

𝌋

Marked ⟐SAC[X6ZNK5NO51]
