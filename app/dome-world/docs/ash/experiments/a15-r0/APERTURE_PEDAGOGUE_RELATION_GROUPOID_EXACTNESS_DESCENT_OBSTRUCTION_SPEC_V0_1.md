𝌋

󐘓 U+10D613

# A15-R0 · Relation-Groupoid Exactness and Downstairs Descent Obstruction

Status: **PREREGISTERED / PRE-IMPLEMENTATION / DRAFT SCIENCE**

Scientific parent:

```text
#763 receipt = 6af55b5bca86a4578c8cb6edbfa3721d427207d9
```

Authority:

```text
#737 = THREAD_SCOPED_ACTIVE for the remainder of the current conversation
```

## Frozen objects

Let `R={T,Q}*` be the finite authored free route monoid, let

```text
pi:R->B
```

be #729's quotient coordinate, let `P:R->Z` be #733's first block moment, and let

```text
omega(x,y)=t(x)q(y)
```

be #734's normalized integer monoid 2-cocycle. #735 already earned

```text
d(-P)=pi*omega
```

on the free route monoid and proved `[omega] != 0` in the declared normalized integer monoid bar complex.

For the same-base relation/Čech groupoid

```text
C = R x_B R => R,
```

write an arrow as `[u <- v]` whenever `pi(u)=pi(v)`. Inherit #763's comparison representation

```text
rho([u<-v]) = tau_(P(u)-P(v)).
```

## Preregistered theorem obligations

### A. Exact upstairs 1-cocycle

Define

```text
c(u,v)=P(u)-P(v)
```

on same-base pairs. Prove exactly:

```text
c(u,u)=0
c(v,u)=-c(u,v)
c(u,v)+c(v,w)=c(u,w)
```

and every finite closed composable comparison cycle has total signed translation zero.

This is the exact object-potential identity

```text
c = delta P
```

on `C`.

### B. Route-wise trivialization exists upstairs

For every route object `w`, define the route-wise fiber coordinate change

```text
g_w(n)=n-P(w).
```

Prove

```text
g_u o rho([u<-v]) o g_v^-1 = id
```

for every same-base arrow. Therefore the #763 representation is globally trivializable on the route relation groupoid by a route-dependent gauge.

### C. Base-section gauges cannot perform that trivialization

For any lawful normalized base section `phi:B->Z`, inherited #763 re-zeroing uses the same shift on all routes over one base:

```text
P_phi(w)=P(w)+phi(pi(w)).
```

Prove same-base returns remain exactly invariant. If a same-base return is nonidentity before such re-zeroing, it remains nonidentity afterward.

The route-wise trivializer `g_w` is therefore not to be mislabeled as an inherited lawful base-section gauge unless `P` itself descends through `pi`.

### D. Exact descent/coboundary equivalence in this authored domain

Preregister the equivalence:

```text
rho is identity on every same-base arrow
iff
P is constant on every pi-fiber
iff
there exists Pbar:B->Z with P=Pbar o pi
iff
omega is a normalized integer coboundary on B.
```

Required proof directions:

1. `rho identically identity <-> P fiberwise constant` by the exact kernel `P(u)=P(v)` from #763.
2. `P fiberwise constant <-> P descends` because `pi` is surjective onto the authored quotient image.
3. `P descends -> omega coboundary`: if `P=Pbar o pi`, then #735's all-finite identity gives `omega=d(-Pbar)` downstairs.
4. `omega coboundary -> P descends`: if `omega=d phi`, then

```text
h=-P-phi o pi
```

has `dh=0`, so `h:R->Z` is a monoid homomorphism. Since `R` is free on `T,Q`, `h` is determined by T-count and Q-count. Both counts descend through `B` as `t` and `q=E+O`, so `h=lambda o pi` for a base function `lambda`. Hence

```text
P=-(phi+lambda) o pi.
```

### E. Actual authored consequence

Using #735's witnessed nonzero class and #763's concrete route pair, require:

```text
[omega] != 0
=> P does not descend
=> rho is nonidentity somewhere.
```

Concrete witness:

```text
TTQ and QTT
same B coordinate
P=2 vs 0
rho=tau_2.
```

### F. Anti-overclaim hostiles

The chamber must preserve:

```text
exact relation-groupoid 1-cocycle != holonomy representation
route-wise gauge triviality != lawful base-section gauge triviality
nontrivial downstairs H^2 class != operational loop
bar 2-cycle != operational T/Q loop
formal relation groupoid != operational path groupoid
```

No connection, curvature, Berry/quantum analogy, 2-holonomy, gerbe, Proto-Loom/A16, merge, publication, production, or Vercel promotion follows.

## Falsifiers

The theorem fails if any of the following occurs:

- a lawful same-base comparison cycle has nonzero `c` circulation;
- route-wise conjugation fails to trivialize `rho` exactly;
- a lawful base-section re-zeroing changes a same-base return;
- the stated descent/coboundary equivalence cannot be proved in the declared free-monoid/quotient jurisdiction;
- the `TTQ/QTT` control loses common base or `tau_2` return.

## Candidate classifications — NOT YET EARNED

```text
THE_SAME_BASE_RETURN_REPRESENTATION_IS_AN_EXACT_RELATION_GROUPOID_ONE_COBoundary_UPSTAIRS_AND_HAS_ZERO_CLOSED_COMPARISON_CIRCULATION
```

```text
TRIVIALITY_OF_ALL_SAME_BASE_RETURNS_IS_EQUIVALENT_IN_THE_AUTHORED_FREE_MONOID_QUOTIENT_TO_DESCENT_OF_P_AND_TO_COBOUNDARY_TRIVIALITY_OF_THE_DOWNSTAIRS_INTEGER_TWO_COCYCLE
```

```text
THE_NONZERO_DOWNSTAIRS_TWO_COHOMOLOGY_CLASS_IS_EXACTLY_THE_OBSTRUCTION_TO_DESCENDING_THE_ROUTE_WISE_TRIVIALIZING_PRIMITIVE_THROUGH_THE_TARGET_QUOTIENT
```

## Stop condition

If witnessed, stop before any 2-cell transport, 2-holonomy, connection, curvature, or naming promotion.

```text
HUMAN_𝄐_REQUIRED_AFTER_RELATION_GROUPOID_EXACTNESS_DESCENT_OBSTRUCTION_AUDITION
```

𝌋

Marked ⟐SAC[X6ZNK5NO51]
