# Aperture × Pedagogue Replay-Envelope + Held-Out Consequence Receipt v0.1

Status: **WITNESSED / BOUNDED SYNTHETIC RESEARCH / PHASE-FREE / HUMAN-GATED**

Schema: `td613.ash.a15-r0.aperture-pedagogue-replay-envelope-consequence/v0.1`

Research PR: `#686`

Witnessed science head:

```text
0428ed3807378e13b618fea2c9681fd4cab06757
```

Exact-head workflow witness:

```text
TD613 Consolidated Validation
run 1854 / 32436811883
Static, constitutional, and release contracts: SUCCESS
A15-R0 research field: SUCCESS
front-line browser shards: SKIPPED BY SCOPE
Giving/practice browser witness: SKIPPED BY SCOPE
full-product browser witness: SKIPPED BY SCOPE
```

This receipt records the exact witnessed science head. This later documentation commit is not retroactively the science witness.

---

## 1. Question

The previous replay-stability witness established:

```text
diagnostic stability != question-selection stability
```

This gauntlet asked a narrower hostile question:

> When a typed diagnostic or selected question changes at a local replay boundary, does the underlying held-out consequence landscape also contain a corresponding performance cliff?

The answer in this authored fixture is **no**.

---

## 2. Local diagnostic boundaries were mapped

### Sigma-floor boundary

Exact local boundary:

```text
sigma_min = 0.25
```

The installed local typed-deficit rule remained admitted at equality and changed class only when the declared floor moved above the observed value.

### Correlated-noise condition boundary

With a fixed identity raw operator and valid covariance:

```text
Sigma(rho) = [[1,rho],[rho,1]]
```

using the local condition-number ceiling:

```text
kappa = 10
```

the analytic boundary is:

```text
rho* = 99/101
     = 0.98019801980198
```

The exact CI witness agreed with that boundary.

Bounded conclusion:

```text
LOCAL_TYPED_DIAGNOSTIC_BOUNDARIES_MAPPED_IN_BOUNDED_SYNTHETIC_FIXTURE
```

No universal threshold or robustness radius is claimed.

---

## 3. Question-selection boundary was numerically bracketed

The previously observed `P_ORTH -> P_DIAG` switch was localized to:

```text
lower = 0.546918160676956
upper = 0.546918160736561
width = 5.9605e-11
```

with:

```text
left  -> P_ORTH
right -> P_DIAG
```

Bounded conclusion:

```text
QUESTION_SELECTION_BOUNDARY_NUMERICALLY_BRACKETED_IN_BOUNDED_SYNTHETIC_FIXTURE
```

This is a decision surface for the declared selector, not a physical phase transition or universal performance boundary.

---

## 4. Held-out consequence audit refused a universal winner

The selector did not consult held-out truth.

After selection, three predeclared held-out linear functionals were evaluated:

```text
H_Y    = [0, 1]
H_DIFF = [1,-1]
H_SUM  = [1, 1]
```

Exact witnessed preference pattern:

```text
H_Y    -> P_ORTH
H_DIFF -> P_ORTH
H_SUM  -> P_DIAG
```

Therefore:

```text
question selected by one declared criterion != universally best question
```

and:

```text
best question
```

is undefined without a declared consequence / loss / evaluation criterion.

The fixture intentionally does not aggregate these functionals into one scalar utility.

---

## 5. Candidate consequence surfaces stayed locally smooth

Across the local selection neighborhood, the underlying candidate-specific held-out variance surfaces remained smooth under the authored continuity check.

Yet the discrete selection policy changed candidate at the boundary.

The exact witnessed selected-policy consequence jump vector across the switch was:

```text
H_Y    = +0.453081839156
H_DIFF = +1.999999999667
H_SUM  = -1.093836321354
```

This does **not** mean the underlying candidate performance contains a discontinuity. The jump is produced by a discrete policy switching between two smooth candidate consequence surfaces.

Earned anti-equivalence:

```text
smooth candidate consequence surface != smooth selected-policy consequence
```

and:

```text
categorical decision boundary != physical/performance cliff
```

---

## 6. Consequence must be declared before universal superiority can be discussed

The strongest bounded lesson from this fixture is not that either question wins.

It is:

```text
measurement / question adequacy is criterion-relative once several admissible downstream consequences disagree.
```

Aperture may audit whether a proposed observation is identifiable, stable, and compatible with declared uncertainty.

Pedagogue may help name or structure the consequence that matters.

Neither may manufacture a universal utility from the mere fact that one candidate won the installed widening criterion.

This preserves the repo-level sequence:

```text
Pedagogue proposes or reframes
-> Aperture audits the admitted observation / reconstruction geometry
-> Dome-World hosts bounded assay
-> held-out consequence audits what selection did
-> human closure remains required
```

---

## 7. Relationship to unresolved sibling PR #677

Draft PR #677 currently holds the high-speculation transfer hypothesis:

```text
H1 · Consequence Conservation
```

The present result is **additional bounded synthetic evidence relevant to that hypothesis**, because the selected question cannot legitimately inherit universal downstream consequence from a narrower selection criterion.

However:

```text
#686 witness != #677 hypothesis promotion
```

This receipt does not accept, reject, rewrite, or close H1. Human closure on #677 remains intact.

The same membrane applies to H2 and H3.

---

## 8. Relationship to unresolved sibling PR #684

Draft PR #684 remains a separate Aperture ingress/history reconciliation surface and is currently non-mergeable against evolved `main`.

This receipt consumes the installed Aperture contract already present on `main` and does not merge from, overwrite, close, or resolve #684.

---

## 9. Installed Aperture promotion hold

The exact witness preserved:

```text
APERTURE_V32_REPLAY_STABILITY = HELD_NOT_YET_WITNESSED
```

Research evidence does not edit the installed replay résumé in the same breath.

Any installed promotion remains a separate human-reviewed Aperture release-lane decision.

---

## 10. Claim ceiling

This witness does **not** establish:

```text
universal robustness radius
universal best question
optimal experimental design
active-learning optimality
information geometry
Fisher-optimal design
physical sensor design
physical tomography
medical tomography
blind tomography
operator tomography
live TD613 reconstruction
autonomous observation
autonomous experiment execution
connection
curvature
holonomy
Berry structure
quantum behavior
Proto-Loom
production authority
release authority
```

No scalar crown is admitted.

---

## 11. Frozen next learning action

Recorded but not executed by this receipt:

```text
TEST_MULTI_DIMENSIONAL_REPLAY_ENVELOPE_INTERSECTIONS_AND_PREDECLARED_DECISION_LOSS_WITHOUT_OPTIMAL_DESIGN_OR_INFORMATION_GEOMETRY_PROMOTION
```

A legitimate next selector must receive an explicitly predeclared consequence / loss object rather than silently treating one mathematical widening criterion as universal value.

---

## 12. Authority membrane

```text
source_status = SIMULATED
authority_class = A2_DERIVATIONAL
manifestly_fictional = true
held_out_used_for_selection = false
no_scalar_crown = true
installed_aperture_replay_flag_mutated = false
promotion_authority = false
automatic_execution = false
production_mutated = false
standalone_aperture_ui_mutated = false
vercel_deployment_authorized = false
human_closure_required = true
```

𝌋 Ken may judge the measurement. Barbie may ask what matters. Neither gets to invent the prize category after seeing who won. ⟐
