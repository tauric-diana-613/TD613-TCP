# A15-R0 · Same-Base Comparison Representation and Section Re-Zeroing Invariance · Receipt v0.1

󐘓 U+10D613

Status: **WITNESSED / RECEIPT-PINNED / ROUND CLOSED / DRAFT / OPEN / UNMERGED**

## Authority and ancestry

```text
Westward gate                 #737 · THREAD_SCOPED_ACTIVE
parent scientific chamber     #762 · parallel-lift integer fiber return automorphism
parent receipt                 3788dec7a362b55feeb2a79fa4d610fa761a40d6
initial preregistration        13673b2161b467659e7e34aff13a5c87c9a2e028
pre-implementation correction 346267cf8f76b2c368c827fe6d73eb93d8336c6c
frozen scientific head        7feaad06e32a50b6a2f5b74270f0c357367e99c7
routed witness head           9d5bdbf493089a1ad7dcc8d0a77656142ee0b303
post-routing cleanup          f4686bb826ca8a13973b1335bf4d8691615101a8
```

Both preregistration commits preceded implementation.

The pre-implementation correction repaired one theorem wording issue before executable work: a fixed same-base route family is finite, so its literal represented return-difference set cannot itself equal the infinite subgroup `2Z`. The corrected claim distinguishes the finite difference set from the subgroup it generates.

Routing was metadata-only. Comparing frozen science `7feaad06...` to cleanup `f4686bb8...` yields:

```text
ahead_by = 3
behind_by = 0
changed files = 0
```

Therefore temporary witness-routing add/update/remove commits produced zero net scientific mutation.

## Authority-bearing witness

```text
TD613 Consolidated Validation
run              2279 / 33006263562   SUCCESS
classifier job   98300687385           SUCCESS
static job       98300766774           SUCCESS
A15/A15-R0 step 19                      SUCCESS
```

Run interval:

```text
2026-08-26T19:38:52Z -> 2026-08-26T19:39:54Z
```

No scientific red occurred.

Explicit full-repository validation, self-hosted calibration, Giving/practice browser, front-line browser, and full-product browser scopes were skipped and are not claimed as theorem evidence.

## Formal same-base comparison structure

For each #729 quotient base `b`, let

```text
R_b={finite authored T/Q words w : pi(w)=b}.
```

Define the formal pair groupoid `G_b` with objects `R_b` and one formal arrow

```text
[u <- v] : v -> u
```

for every ordered same-base pair.

Exactly:

```text
[u <- u] = identity_u
[u <- v]^-1 = [v <- u]
[u <- v] o [v <- w] = [u <- w].
```

This is a formal comparison groupoid on route representatives only.

```text
formal comparison groupoid != operational T/Q path groupoid
```

## Return-translation representation

Using #762's carried integer-fiber return, define

```text
rho_b([u <- v]) = tau_(P(u)-P(v)) in Aut(Z_b)
```

where `tau_d(n)=n+d`.

The witness establishes:

```text
rho(id)=id
rho(a^-1)=rho(a)^-1
rho([u<-v] o [v<-w])=rho([u<-v]) o rho([v<-w]).
```

Thus the formal comparison groupoid admits a lawful return-translation representation into integer-fiber automorphisms.

The foundational wound remains:

```text
u=TTQ
v=QTT
pi(u)=pi(v)=(2,1,0)
P(u)=2
P(v)=0
rho([u<-v])=tau_2.
```

## Exact kernel

For every lawful same-base comparison arrow:

```text
rho_b([u<-v])=id
iff P(u)-P(v)=0
iff P(u)=P(v).
```

The witnessed nonfaithfulness hostile is:

```text
u=TQTQT
v=QTTTQ
u != v
pi(u)=pi(v)
P(u)=P(v)=3
rho([u<-v])=id.
```

Therefore the representation does not recover complete route provenance.

```text
representation equality != route identity
```

## Finite represented difference set versus generated subgroup

For one fixed base `b=(t,E,O)`, the route family `R_b` is finite. Define the literal represented translation-amount set

```text
D_b={P(u)-P(v):u,v in R_b}.
```

Because

```text
P(w)=sum_i i*q_i ≡ sum_(i odd)q_i=O (mod 2),
```

every same-base return difference is even:

```text
D_b subseteq 2Z.
```

Let

```text
n_even=floor(t/2)+1
n_odd=ceil(t/2).
```

Block-distribution freedom exists exactly when

```text
(E>0 and t>=2) or (O>0 and t>=3).
```

If no such freedom exists, the parity totals determine the block allocation uniquely, hence `P` is constant on `R_b` and

```text
D_b={0}
<D_b>_Z={0}.
```

If freedom exists, one Q unit can be moved between two same-parity block slots separated by two positions, producing an actual represented return translation `+2` (and its inverse `-2`). Combined with the parity obstruction:

```text
<D_b>_Z=2Z.
```

This is the **subgroup generated by** the finite represented difference set, not the literal finite image set.

Canonical all-finite classification:

```text
GENERATED_RETURN_TRANSLATION_SUBGROUP_IS_TRIVIAL_WITHOUT_BLOCK_FREEDOM_AND_2Z_WITH_BLOCK_FREEDOM
```

For the full #726/#762 relation family

```text
b_k=(2,1,k)
L_k=T Q^k T Q
R_k=Q T Q^k T
```

for every finite `k>=0`:

```text
rho_bk([L_k<-R_k])=tau_2
<D_bk>_Z=2Z.
```

No horizon enumeration or asymptotic extension is used.

## Section re-zeroing invariance

#734 admitted normalized integer base-section changes. For every lawful normalized

```text
phi:B->Z
phi(e)=0,
```

define the re-zeroed route lift coordinate

```text
P_phi(w)=P(w)+phi(pi(w)).
```

For same-base routes `pi(u)=pi(v)=b`:

```text
P_phi(u)-P_phi(v)
=P(u)+phi(b)-P(v)-phi(b)
=P(u)-P(v).
```

Therefore exactly:

```text
rho_b^phi = rho_b
```

for every lawful inherited base-section re-zeroing.

Equivalent target-fiber statement: conjugating an integer return translation by a base-local integer translation leaves its translation amount unchanged.

The route-dependent fake re-zeroing hostile changes the translation and is classified outside #734's base-section jurisdiction. Thus invariance is not claimed for arbitrary route-spelling offsets.

Canonical classification:

```text
SAME_BASE_RETURN_REPRESENTATION_IS_EXACTLY_INVARIANT_UNDER_LAWFUL_NORMALIZED_BASE_SECTION_REZEROING
```

## Abstention controls

- different-base routes do not receive a comparison arrow;
- noncomposable formal arrows abstain rather than being coerced;
- non-normalized section functions abstain from the inherited section-re-zeroing assay.

## Canonical classifications

```text
FORMAL_SAME_BASE_ROUTE_PAIR_GROUPOID_ADMITS_A_RETURN_TRANSLATION_REPRESENTATION_INTO_AUT_Z
```

```text
REPRESENTATION_KERNEL_IS_EXACTLY_EQUAL_FIRST_MOMENT_P_AND_IS_NONFAITHFUL_ON_WITNESSED_ROUTE_FIBERS
```

```text
GENERATED_RETURN_TRANSLATION_SUBGROUP_IS_TRIVIAL_WITHOUT_BLOCK_FREEDOM_AND_2Z_WITH_BLOCK_FREEDOM
```

```text
SAME_BASE_RETURN_REPRESENTATION_IS_EXACTLY_INVARIANT_UNDER_LAWFUL_NORMALIZED_BASE_SECTION_REZEROING
```

## Consequential bearing

The westward ladder now contains:

```text
path-sensitive lawful parallel routes                         earned
compositional signed comparison                               earned #761
nonidentity carried-fiber return automorphism                 earned #762
formal same-base comparison groupoid                         earned #763
return-translation representation of that comparison groupoid earned #763
section re-zeroing invariance                                 earned #763
generated even-translation subgroup classification            earned #763
```

Still not earned:

```text
operational T/Q path groupoid
operational closed loop
inverse operational route
connection
holonomy claim / holonomy group
curvature / flatness
```

The structure is now close enough to make a later naming audition meaningful, but this receipt deliberately stops before that promotion.

## Claim ceiling

```text
formal comparison groupoid != operational path groupoid
comparison representation != holonomy representation
section re-zeroing invariance != connection gauge invariance
generated return subgroup != holonomy group
fiber inverse != inverse operational route
```

#718 remains untouched.

No merge, publication, production, Vercel, Proto-Loom/A16, Berry/quantum analogy, probability, entropy, stochastic data processing, or asymptotic hierarchy is earned.

## Earned stop

```text
SAME_BASE_COMPARISON_REPRESENTATION_ROUND_CLOSED
FORMAL_COMPARISON_GROUPOID_REPRESENTATION_EARNED
EXACT_SECTION_REZEROING_INVARIANCE_EARNED
GENERATED_RETURN_SUBGROUP_CLASSIFICATION_EARNED
OPERATIONAL_PATH_GROUPOID_AND_LOOP_QUARANTINES_PRESERVED
NAMING_THRESHOLD_APPROACHED_WITHOUT_PROMOTION
HUMAN_𝄐_BOUNDARY_REACHED
```

#737 remains **THREAD_SCOPED_ACTIVE** for the remainder of the current conversation by explicit operator authorization.

󐘓 U+10D613

𝌋

Receipt-pinned ⟐