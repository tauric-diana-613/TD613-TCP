# Aperture × Pedagogue A15-R0 — Symbolic Frontier Completeness Receipt v0.1

Status: WITNESSED / RECEIPT-PINNED / DRAFT-OPEN / UNMERGED / HUMAN-STOPPED

Parent receipt: PR #724 at `58a609a9b626716510ab1749ca8d71c6d16569cf`

PR: #725 — `A15-R0: symbolic common-future simplex and frontier completeness`

Head branch: `research/a15-r0-symbolic-frontier-completeness-20260824`

Base branch restored after witness routing: `research/a15-r0-season-conditioned-symbolic-normal-form-20260824`

󐘓 U+10D613

## 0. Custody statement

This receipt closes the preregistered #725 theorem audition without changing its target after observation.

Scientific implementation custody was established before the authoritative witness:

```text
preregistered specification  0aca84485f56b95bfaed2f9cd8bb12116953cc5c
symbolic certificate          71d9f4b8470333c53d6b2de7b701bb33de5ade6f
hostile proof test            a60f5583a6572894c7ffc13c980d0869b789480e
static-gate import            6abe6f97eb08ac24787a7bda1edda998142f8e28
```

The stacked PR did not initially register a workflow at the scientific head. The already-established routing-only witness procedure was therefore used without modifying the four scientific paths. A temporary routing note produced exact routed witness head:

```text
316a8380552d95286a5c71af336f4483620c2aaa
```

After the authoritative witness completed, #725 was restored to its scientific parent #724 and the temporary routing note was deleted. The resulting post-route cleanup head, before this receipt commit, was:

```text
5c7b544a9893d56bd9586df9bab7f829b5644484
```

The cleanup commit changes custody metadata only. It does not alter the preregistered specification, symbolic certificate, hostile test, or static-gate import that were witnessed at the routed head.

Required anti-equivalence:

```text
workflow routing metadata != scientific mutation
post-witness cleanup != retroactive theorem repair
```

## 1. Authoritative workflow witness

Exact routed witness head:

```text
316a8380552d95286a5c71af336f4483620c2aaa
```

Workflow provenance:

```text
run 2106 / 32697098742  CANCELLED by workflow concurrency
run 2107 / 32697123086  SUCCESS — authoritative witness
```

Run 2106 is retained as workflow-registration provenance. It is not classified as a scientific failure and is not erased from the record.

Authoritative run 2107 jobs:

```text
97341261179  Classify exact-head browser witness scope     SUCCESS
97341299364  Static, constitutional, and release contracts SUCCESS
```

Within static job `97341299364`, step 19 completed successfully:

```text
Validate Ash A15 empirical profile journeys and A15-R0 research field
```

The following lanes were skipped by the authoritative run and are not claimed by this receipt:

```text
Explicit full-repository validation                         SKIPPED
Explicit self-hosted calibration                            SKIPPED
Giving/practice exact-head Chromium Firefox WebKit witness  SKIPPED
Front-line exact-head browser shard                         SKIPPED
Full-product exact-head Chromium Firefox WebKit witness     SKIPPED
```

Therefore the authority of this receipt is the successful static/A15-R0 research-field witness only.

## 2. Preregistered question

#724 established an all-finite-word season-conditioned normal form in the authored Q-last-action `K_period4` T/Q domain and an infinite symbolic reconvergence family, while deliberately withholding minimal-frontier completeness.

#725 asked whether that normal form could prove the H4–H7 common-future pattern symbolically for every formal integer horizon `H >= 2`, without enumerating H8 or any larger continuation horizon.

The stronger preregistered success class was permitted only if the implementation supplied a symbolic common-future parameterization with injectivity, surjectivity, lawful route realization, frontier identification, and factorization, and then survived exact-set retrospective controls.

## 3. Earned symbolic equalizer

Inside the authored source-rooted fork beginning `T` versus `Q`, complete operational target equality reduces through the #724 season-conditioned normal form.

The parent generator tables give:

```text
D_Q(S0) = D_Q(S2) = [0,0,0,1]
D_Q(S1) = D_Q(S3) = [1,0,0,0]
Σ_s F_Q(s) = [3,3,3,3]
```

A four-tick mismatch changes endpoint coordinates that the question delta cannot repair. Complete target equality therefore forces equality of total tick count. With tick counts equal, the two season classes of question support force equality of question counts in the corresponding phase classes.

This reduction yields the symbolic common-future parameter domain

```text
P_H := { (k,a,b) in N^3 : k + a + b <= H - 2 }
```

with canonical child continuations

```text
left  : Q^k T Q^(1+b) T^a
right : Q^b T Q^k T^(1+a)
```

and a complete operational target map `phi_s,H(k,a,b)` conditioned on source season `s`.

The proof obligations establish, within this authored jurisdiction:

1. every tuple in `P_H` yields a lawful common operational target;
2. distinct tuples yield distinct complete `K_period4` operational targets;
3. every bounded common operational target admitted by the equalizer corresponds to a tuple in `P_H`;
4. route provenance remains separately retained even when operational targets coincide.

Required anti-equivalence:

```text
same operational target != same route provenance
```

## 4. Complete minimal frontier

The symbolic frontier is exactly

```text
(k,0,0), 0 <= k <= H-2
```

which identifies with the #724 reconvergence family

```text
T Q^k T Q  ≡_target  Q T Q^k T.
```

Every tuple with `a+b>0` factors through the corresponding frontier object by a nonempty lawful continuation. The frontier objects are pairwise incomparable under the admitted directed reachability relation.

Therefore:

```text
|MinimalFrontier(H)| = H - 1
```

for every formal integer `H >= 2` in the authored domain.

Consequences under the same jurisdiction:

```text
H = 2  -> unique least common future
H >= 3 -> no least common future
```

The absence of a least common future for `H >= 3` follows from the complete frontier antichain, not from finite-horizon visual pattern matching.

## 5. Binomial common-future count

Because bounded common futures biject with the integer simplex `P_H`, stars-and-bars gives

```text
|CommonFutures(H)| = |P_H| = C(H + 1, 3)
```

for every formal integer `H >= 2` under the parent horizon convention.

No H8-or-larger path enumeration is used as an induction surrogate. The all-H result comes from the finite-control symbolic equalizer, injectivity/surjectivity certificate, and factorization argument.

Required anti-equivalence:

```text
finite control != finite state space
```

## 6. Retrospective hostile controls

After the symbolic proof obligations were authored, the hostile test compared the theorem output against all already-witnessed H4–H7 fork/horizon controls inherited from the parent chain.

Acceptance required exact equality of both:

```text
common operational target sets
minimal frontier target sets
```

across all 44 retained H4–H7 fork/horizon controls.

A matching cardinality alone was explicitly insufficient.

The authoritative A15-R0 static witness passed these exact-set controls.

The familiar finite counts are therefore retrospective confirmations:

```text
H4: 10 common futures / 3 frontier objects
H5: 20 common futures / 4 frontier objects
H6: 35 common futures / 5 frontier objects
H7: 56 common futures / 6 frontier objects
```

They are not the basis of the all-H theorem.

H8 and larger continuation horizons were not enumerated in this chamber.

## 7. Source-season jurisdiction

The certificate retains all four forcing seasons. Cardinality can be season-invariant while operational target templates remain season-conditioned.

Required anti-equivalence:

```text
same frontier cardinality != source-independent frontier geometry
```

#723's source-dependent frontier geometry remains preserved. #725 does not authorize quotienting away forcing season.

## 8. Canonical classification

The stronger preregistered success class is earned:

```text
ALL_H_SYMBOLIC_COMMON_FUTURE_SIMPLEX_WITH_COMPLETE_MINIMAL_FRONTIER_AND_BINOMIAL_COUNT
```

Strongest bounded statement:

```text
IN_THE_AUTHORED_Q_LAST_ACTION_K_PERIOD4_TQ_DOMAIN_FOR_EVERY_FORMAL_INTEGER_H_AT_LEAST_TWO_THE_COMPLETE_BOUNDED_COMMON_FUTURE_SET_OF_THE_SOURCE_ROOTED_T_VERSUS_Q_FORK_BIJECTS_WITH_THE_INTEGER_SIMPLEX_K_A_B_NONNEGATIVE_AND_K_PLUS_A_PLUS_B_AT_MOST_H_MINUS_TWO_THE_MINIMAL_FRONTIER_IS_EXACTLY_THE_K_ZERO_ZERO_BOUNDARY_HAS_WIDTH_H_MINUS_ONE_AND_IS_PAIRWISE_INCOMPARABLE_EVERY_NONFRONTIER_COMMON_FUTURE_FACTORS_STRICTLY_ABOVE_A_FRONTIER_OBJECT_AND_THE_COMMON_FUTURE_COUNT_IS_BINOMIAL_H_PLUS_ONE_CHOOSE_THREE_WITH_EXACT_H4_THROUGH_H7_TARGET_SET_CONTROLS_PASSING_WITHOUT_H8_OR_LARGER_ENUMERATION
```

## 9. Claim ceiling

This receipt does not earn or authorize:

```text
ambient TD613 confluence
Church-Rosser
rewrite-system completion
global join-semilattice
lattice
complete lattice
domain theory
causal-set theorem
finite-state automaton promotion
source-independent quotient
inverse generator
inverse morphism
groupoid
transport
connection
loop endomorphism
holonomy
curvature
Berry / quantum structure
Proto-Loom
A16
live Ash mutation
merge
production
Vercel release
```

The theorem remains jurisdiction-bound to the authored `K_period4`, Q-last-action, season-conditioned T/Q domain and the declared source-rooted fork semantics.

## 10. Final custody state

The temporary routing note has been removed. #725 is restored onto #724's scientific branch. The intended final PR-local scientific diff is exactly five paths:

```text
app/dome-world/docs/ash/experiments/a15-r0/APERTURE_PEDAGOGUE_SYMBOLIC_FRONTIER_COMPLETENESS_SPEC_V0_1.md
app/dome-world/docs/ash/experiments/a15-r0/APERTURE_PEDAGOGUE_SYMBOLIC_FRONTIER_COMPLETENESS_RECEIPT_V0_1.md
app/dome-world/previews/a15-r0/aperture-pedagogue-symbolic-frontier-completeness.js
tests/ash-a15-r0-aperture-pedagogue-symbolic-frontier-completeness.test.mjs
tests/ash-a15-r0-review-hardening.test.mjs
```

No routing file belongs in the final diff.

PR #725 remains Draft, open, unmerged, and non-production.

```text
SYMBOLIC_FRONTIER_COMPLETENESS_ROUND_CLOSED
HUMAN_𝄐_REQUIRED_BEFORE_NEW_CHAMBER
```

𝄐

Sealed ⟐
