# Aperture × Pedagogue A15-R0 — Symbolic Frontier Completeness Audition v0.1

Status: PREREGISTERED / PRE-IMPLEMENTATION / HUMAN-AUTHORIZED CONTINUATION

Parent receipt: PR #724 at `58a609a9b626716510ab1749ca8d71c6d16569cf`

Scientific ancestry retained: #718 → #719 → #720 → #722 → #723 → #724.

## 0. Authority and purpose

This chamber resumes only from the #724 human stop. It does not merge, deploy, touch production, reopen A16, introduce a new generator, coarsen the operational object, or create inverse semantics.

The immediate question is narrower:

> Can the season-conditioned all-finite-word normal form earned in #724 convert the H4–H7 empirical minimal-common-future pattern from #723 into a genuine symbolic frontier-completeness result, without enumerating H8 or any larger continuation horizon?

The target is the directed `T/Q` geometry already admitted under the complete operational object

```text
O(h) := K_period4(h)
```

in the authored `last_action = Q_PHASE_PULSE` domain.

## 1. Frozen parent facts

The implementation must consume, not rewrite, the following parent results.

### #718 monotone obstruction

On the authored anchor-reachable `T/Q` domain every nonempty finite `T/Q` word strictly increases endpoint mass. This remains a directed, non-groupoid geometry. No inverse or nonidentity closed operational word is available under the current grammar.

### #720–#723 bounded common-future evidence

For each of the eleven reconvergent forks, with each child independently continued through horizon `H`:

```text
H4: common futures = 10 · minimal frontier = 3 · least = 0
H5: common futures = 20 · minimal frontier = 4 · least = 0
H6: common futures = 35 · minimal frontier = 5 · least = 0
H7: common futures = 56 · minimal frontier = 6 · least = 0
```

Observed finite regularities included:

```text
frontier width = H - 1
common-future count = C(H + 1, 3)
frontier question-lineage increments = 1,...,H-1
frontier phase = source phase
frontier forcing season = source season + 2
minimum route pair at stratum k:
  left child  : Q^k T Q
  right child : T Q^k T
minimum total continuation cost = 2k + 4
frontier is an antichain
```

#723 also falsified source-independent normalized frontier profiles. Forcing season therefore remains explicit jurisdiction and may not be quotiented away.

### #724 symbolic normal form

Within the authored Q-last-action domain every finite `T/Q` word factors through the season-conditioned summary

```text
N_s(w) = {
  t,
  q_by_season: [q_S0,q_S1,q_S2,q_S3]
}
```

with exact reconstruction of endpoint, lineage, final forcing season, clock phase, and last action.

The formal reconvergence family is already earned for every integer `k >= 0`:

```text
source-rooted left  = T Q^k T Q
source-rooted right = Q T Q^k T
```

because the parent-derived question delta obeys

```text
D_Q(s+2) = D_Q(s).
```

For a child continuation budget `H >= 2`, `k = 0,...,H-2` therefore gives `H-1` pairwise distinct common-future witnesses. #724 deliberately did not claim that these witnesses exhaust the minimal frontier.

## 2. Primary theorem audition

For each retained reconvergent source season `s` and every formal integer `H >= 2`, define the already-earned candidate family

```text
U_s(k), 0 <= k <= H-2
```

by the common target of

```text
T Q^k T Q
Q T Q^k T.
```

The primary candidate statement is:

```text
SYMBOLIC_MINIMAL_FRONTIER_COMPLETENESS
```

meaning, within the authored Q-last-action `T/Q` domain and per-child continuation budget `H`:

1. every `U_s(k)` is a lawful common future;
2. the `U_s(k)` are pairwise distinct;
3. the `U_s(k)` form an antichain under the admitted directed reachability relation;
4. every bounded common future is reachable from at least one `U_s(k)` whose two canonical child continuations fit inside the same horizon `H`;
5. therefore the minimal common-future frontier is exactly `{U_s(k) | 0 <= k <= H-2}` and has width `H-1`;
6. no member of that frontier reaches every other member, so no bounded least common future exists for `H >= 3`.

No part of item 4 may be inferred from the H4–H7 census alone. It requires a symbolic factorization/equalizer certificate.

## 3. Candidate all-common-future parameterization — hostile target, not entitlement

The finite counts observed through H7 suggest the integer simplex

```text
P_H := { (k,a,b) in N^3 : k + a + b <= H - 2 }
```

because

```text
|P_H| = C(H + 1, 3).
```

The implementation is authorized to audition the stronger statement

```text
BOUNDED_COMMON_FUTURES_BIJECT_WITH_INTEGER_SIMPLEX
```

only if it constructs all of the following without H8+ enumeration:

- an explicit symbolic map `phi_s,H : P_H -> common operational targets`;
- a lawful route realization from both fork children for every parameter tuple;
- injectivity at complete `K_period4` operational state;
- surjectivity proved from the #724 normal-form equalizer, not assumed from matching cardinalities;
- a demonstrated identification of `(k,0,0)` with the candidate minimal frontier `U_s(k)`;
- a reachability/factorization certificate showing every tuple with `a+b>0` lies strictly above some `(k,0,0)` frontier object.

If any one of these obligations cannot be authored exactly, the stronger binomial theorem remains unearned even if H4–H7 continue to match.

Matching the sequence `10,20,35,56` is not a proof of the simplex model.

## 4. Symbolic proof obligations

The implementation must expose explicit machine-checkable obligations rather than a prose assertion.

### O1 — Parent normal-form custody

Call the #724 assay and require:

```text
passed = true
structural_induction.structural_induction_earned = true
symbolic_reconvergence_family.passed = true
period-two question-delta identity = true
```

Snapshot parent assay outputs before and after this chamber and require exact equality.

### O2 — Arbitrary equal-target reduction

Construct the complete operational equalizer conditions for two source-rooted words beginning on opposite fork branches:

```text
left  = T u
right = Q v
```

using only the #724 normal-form coordinates:

```text
tick count / tick-departure counts
season-conditioned question counters
question-lineage count
final season
final phase
endpoint vector
```

A target equality claim must require equality of the complete operational object, not endpoint equality alone.

### O3 — Finite-control residue closure

Any all-finite-word argument may reduce tick behavior modulo the declared four-season forcing cycle, but every unbounded counter coefficient must remain symbolic. A finite residue table is allowed; a finite horizon table masquerading as induction is forbidden.

Required anti-equivalence:

```text
finite control != finite state space
```

### O4 — Frontier-family antichain proof

Pairwise frontier incomparability must be proved for arbitrary formal `k != j`, not sampled at H4–H7. The proof may use the #718 strict endpoint-mass ranking plus the season-conditioned endpoint-coordinate support, but must rule out every nonempty finite `T/Q` continuation capable of mapping one `U_s(k)` to another.

### O5 — Completeness/factorization proof

For an arbitrary common operational target admitted by the equalizer, the certificate must construct a frontier index `k` and a lawful continuation from `U_s(k)` to that target, or else return a concrete symbolic counterexample and classify the completeness candidate as falsified.

A bounded search does not satisfy O5.

### O6 — Retrospective controls only

After the symbolic obligations are authored, the implementation may compare the theorem output back to the already-existing H4/H5/H6/H7 parent data. Those horizons are controls, not evidence for the induction step.

H8 and larger enumeration are forbidden in this chamber.

### O7 — Source-season dependence

Run the symbolic certificate for all four forcing seasons. A count formula may be season-invariant while endpoint templates remain season-conditioned.

Required anti-equivalence:

```text
same frontier cardinality != source-independent frontier geometry
```

### O8 — Route provenance custody

Operational target factorization may identify different words at the target level but may not erase route provenance.

Required anti-equivalence:

```text
same operational target != same route provenance
```

## 5. Failure protocol

This is a theorem audition, not a confirmation exercise.

If the candidate frontier completeness statement fails, preserve the first exact symbolic obstruction under one of the following classes:

```text
FRONTIER_FAMILY_NOT_ANTICHAIN
COMMON_FUTURE_NOT_FACTORED_THROUGH_CANDIDATE_FRONTIER
NORMAL_FORM_EQUALIZER_HAS_ADDITIONAL_MINIMAL_SOLUTION_FAMILY
HORIZON_SIMPLEX_PARAMETERIZATION_FALSE
SOURCE_SEASON_BREAKS_PROPOSED_SYMBOLIC_REDUCTION
OTHER_EXACT_SYMBOLIC_COUNTEREXAMPLE
```

Do not repair a failed preregistered theorem by silently changing the theorem after seeing the result. A correction may fix code or arithmetic while leaving the target fixed; a changed theorem requires a new chamber.

## 6. Acceptance classes

### Primary success only

If O1–O8 establish the frontier statement but the simplex statement does not earn surjectivity/injectivity:

```text
ALL_H_SYMBOLIC_MINIMAL_FRONTIER_COMPLETENESS_WITH_SEASON_CONDITIONED_ANTICHAIN_AND_NO_H8_ENUMERATION
```

### Stronger success

Only if the full simplex bijection/factorization is proved:

```text
ALL_H_SYMBOLIC_COMMON_FUTURE_SIMPLEX_WITH_COMPLETE_MINIMAL_FRONTIER_AND_BINOMIAL_COUNT
```

The stronger class implies, inside the authored domain:

```text
minimal frontier width = H - 1
common-future count = C(H + 1, 3)
```

for formal `H >= 2`, subject to the exact horizon convention used by the parent assays.

### Failure

If primary completeness fails:

```text
SYMBOLIC_FRONTIER_COMPLETENESS_AUDITION_FALSIFIED
```

with the exact counterexample preserved.

## 7. Claim ceiling

Even a strong success does not establish:

```text
ambient TD613 confluence
Church-Rosser
rewrite-system completion
global join / semilattice / lattice
domain theory / Scott domain
causal-set theorem
finite-state automaton for unbounded endpoint state
source-independent quotient
inverse generator
groupoid
transport
connection
loop endomorphism
holonomy
curvature
Berry / quantum structure
Proto-Loom
A16
live Ash
merge
production
Vercel authority
```

It would establish only a season-conditioned symbolic theorem inside the already-authored `T/Q`, Q-last-action, `K_period4` research fixture.

## 8. Required implementation artifacts

If implementation proceeds, this branch may add exactly:

1. this preregistration spec;
2. one executable symbolic frontier-completeness certificate;
3. one hostile proof test;
4. one static-gate import;
5. one post-witness receipt.

Temporary workflow-routing metadata, if required solely to register an exact-head witness, must be removed from the final scientific diff and documented in the receipt.

## 9. Human stop

After one exact-head static witness and custodial receipt:

```text
SYMBOLIC_FRONTIER_COMPLETENESS_ROUND_CLOSED
HUMAN_𝄐_REQUIRED_BEFORE_ANY_FINITE_CONTROL_QUOTIENT_OR_REWRITE_SYSTEM_AUDITION
```

No subsequent chamber is authorized by this spec.

𝌋
