# A15-R0 · Target-Equivalence Quotient and Compositional Congruence Receipt v0.1

Status: **WITNESSED / ROUND CLOSED / DRAFT / OPEN / UNMERGED**

PR: `#729`
Parent PR: `#728`
Parent receipt head: `b08fab1ca7786a3f70c5e1816f41c1bc9f856723`
Original preregistration commit: `b7684cbf9c995cd9489ea5307a3af523d95f511b`

## Earned classification

```text
SOURCE_RELATIVE_TARGET_EQUIVALENCE_DESCENDS_TO_ASSOCIATIVE_PARITY_TWISTED_CANONICAL_ROUTE_QUOTIENT_WITH_TYPED_COMPOSITION_AND_CUSTODY_EXTERNALITY
```

Within the declared #728 source-relative finite T/Q jurisdiction, #729 witnesses the quotient coordinate

```text
c(w) = (t,E,O) in N^3
```

with parity-twisted composition

```text
(t,E,O) ★ (u,F,G)
  = (t+u, E+F, O+G)  when t is even
  = (t+u, E+G, O+F)  when t is odd.
```

The identity is `(0,0,0)`. Associativity follows from the two-element parity action on the `(E,O)` pair: `sigma^(t+u)=sigma^t o sigma^u`. The exact-head static witness also passed the declared concatenation controls, source-conditioned transition-locality controls, right-congruence certificate, typed compositional congruence certificate, canonical-representative checks, and hostile controls.

## What descends, and what does not

For the retained lawful Q-last-action sources, #728 already established that equality of `(t,E,O)` is equivalent to equality of the authored operational target. #729 establishes that this equivalence is compatible with the declared composition law strongly enough to form a canonical route quotient **when source and middle-object typing are retained**.

Source is not erased. Evaluation remains source-conditioned.

Route custody is not erased. Distinct words may occupy one quotient class while remaining distinct external ledger entries.

The quotient therefore compresses target-relevant route algebra without pretending that target equality is provenance identity.

## Hostile controls preserved

The witnessed hostile suite requires, among other controls:

- odd left tick parity must swap the right `(E,O)` contribution;
- collapsing `(E,O)` to total Q-count is rejected;
- distinct words in one quotient class remain distinct custody entries;
- the same quotient coordinate evaluated from different retained sources may produce distinct targets;
- a mismatched typed middle object abstains before composition.

## Authority-bearing witness

```text
scientific exact head   3eabd7fbf6cab5699dd91a91754ad7a5d3b1c0c2
run                     2134 / 32726679435   SUCCESS
classifier job          97429440498          SUCCESS
static job              97429491932          SUCCESS
A15/A15-R0 step 19                            SUCCESS
```

Run 2134 started at `2026-08-24T12:21:37Z` and completed at `2026-08-24T12:22:34Z`.

Explicit full-repository validation, self-hosted calibration, Giving/practice browser witness, and front-line browser shards were skipped and remain outside this receipt's claim.

## Preserved red and routing scars

The successful witness does not overwrite predecessor failures.

### Run 2124 · wrapper-comparison scar

Run `2124 / 32725332014` failed at A15-R0 step 19 because a concrete `K_period4` target was compared byte-for-byte with the same symbolic target carrying an additional `status` wrapper. The wrapper is not part of operational equality. A wrapper-only projection repair was applied in commit:

```text
2c2dff6425eb3e26244259da2ac60eab85515dc8
```

No quotient theorem term changed in that repair.

### Run 2127 · concurrency provenance

Run `2127 / 32725767354` was cancelled by same-head concurrency and carries no scientific verdict.

### Run 2128 · representative-vector scar

Run `2128 / 32725803225` reached A15-R0 step 19 and failed the aggregate obligation because the authored typed-composition control accidentally selected second representatives from different quotient classes:

```text
TQQ -> (1,0,2)
QQT -> (1,2,0)
```

The repair was separately preregistered before implementation at commit:

```text
93da317c5a5e8554f9913fd5493514ef6a80e227
```

and changed only that witness pair to distinct equal-class representatives:

```text
TTQ -> (2,1,0)
QTT -> (2,1,0)
```

The vector repair landed at:

```text
ea366fde56e3ea8f3245ba35c27fb6c1d1c28397
```

### Run 2131 · concurrency provenance

Run 2131 was cancelled by same-head concurrency and carries no scientific verdict.

### Run 2132 · path-jurisdiction hardening scar

Run `2132 / 32726447632` stopped at the historical A15-R0 path guard before the #729 assay could issue a scientific verdict. The repair preregistration had been temporarily materialized as a new file under the receipt-guarded historical experiment-doc namespace. The preregistration remains preserved in commit ancestry at `93da317c...`; the transient final-tree path was then removed without changing the science module.

Diagnostic artifact for that run was preserved by the workflow. This scar is classified as path-jurisdiction/hardening provenance, not a quotient counterexample.

### Run 2133 · concurrency provenance

Run `2133 / 32726651401` was cancelled by same-head concurrency and carries no scientific verdict.

## Post-witness cleanup

After run 2134 succeeded:

- PR #729 was restored from temporary `main` routing to parent branch `research/a15-r0-target-equivalence-completeness-20260824`;
- the temporary witness-routing note was deleted;
- no scientific executable or hostile assertion changed during cleanup;
- production and Vercel remained untouched.

Post-routing-cleanup head before this receipt was:

```text
3f9112d91091028cee60ae8d5616a2777cd5a06e
```

## Claim ceiling

This receipt does **not** authorize or establish:

```text
source erasure
custody deletion
naked untyped two-sided word congruence across source changes
inverse morphisms
groupoid structure
transport assignment
connection
loop endomorphism / loop transport
holonomy
curvature
Berry / quantum analogy
Proto-Loom
A16
live Ash mutation
merge
production
Vercel release
```

The algebra earned here is a source-relative, typed, parity-twisted canonical route quotient with external custody. Nothing downstream is silently promoted.

```text
TARGET_EQUIVALENCE_QUOTIENT_CONGRUENCE_ROUND_CLOSED
HUMAN_𝄐_REQUIRED_BEFORE_ANY_TRANSPORT_ASSIGNMENT_OR_PATH_DEPENDENT_TRANSPORT_AUDITION
```

󐘓 U+10D613

𝌋

Sealed ⟐
