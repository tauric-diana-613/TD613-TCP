𝌋

# A15-R0 · Aperture × Pedagogue Temporal-Recurrence / Phase-Aliasing Congruence Gauntlet Spec v0.1

**Status:** PREREGISTERED / PRE-IMPLEMENTATION / HUMAN-GATED  
**Scientific parent:** #714 receipt head `5e784942954a037e76abc364fabff277c64eec87`  
**Parent classification:** `FINITE_TEMPORAL_PHASE_AUGMENTATION_RESTORES_DECLARED_EXOGENOUS_EVOLUTION_CONGRUENCE_WITH_ORDER_SENSITIVE_QUESTION_TIME_INTERACTION`  
**Program:** A15-R0 western-horizon bounded research only  
**Poetic laboratory nickname:** *Grandma came back to the same clock.*

---

## 0. Human gesture and purpose

The human resumed the #714 westward research seam after the prior 𝄐.

The bounded recommendation at that seam was:

```text
one more temporal chamber
→ test recurrence / phase aliasing
→ only then reconsider first path-object promotion
```

This chamber therefore asks whether the period-two clock phase that repaired #714 remains sufficient when the exogenous environment follows a declared period-four forcing schedule.

The chamber is intentionally designed to distinguish:

```text
clock recurrence
!=
forcing-schedule recurrence
!=
endpoint recurrence
!=
full operational-state recurrence
```

No path object, category, groupoid, transport, connection, loop endomorphism, holonomy, curvature, Berry phase, quantum analogy, Proto-Loom, A16, live Ash mutation, merge, production, or Vercel authority is preregistered here.

---

## 1. Research question

#714 witnessed that

```text
K_temporal(h) = {
  endpoint,
  last_action,
  operational_lineage,
  clock_phase
}
```

is representative-independent for the declared two-phase temporal grammar.

The new question is narrower and more hostile:

> If the visible/control clock has period two while the declared exogenous forcing schedule has period four, can two histories share the same #714 temporal state yet require different lawful no-question successors? If so, does explicit period-four forcing-state augmentation restore representative-independent evolution over the bounded authored schedule?

This is a controlled model-period mismatch assay.

It is not a generic hidden-state objection.

The omitted variable is not arbitrary. It is a preregistered, deterministic, finite exogenous schedule whose projection onto the visible clock aliases two distinct forcing positions.

---

## 2. Frozen parent dependency

The assay must derive source histories from the witnessed #714 executable parent rather than retype endpoint, lineage, or receipt values.

The parent #714 gauntlet must pass before recurrence lifting begins.

The parent object must remain unchanged before and after every derived operation.

Derived recurrence state never grants permission to mutate parent custody.

---

## 3. Two nested temporal descriptions

The already-witnessed #714 clock is retained:

```text
P0
P1
```

A new finite exogenous forcing coordinate is declared:

```text
S0
S1
S2
S3
```

with deterministic advancement

```text
S0 -> S1 -> S2 -> S3 -> S0.
```

Clock phase is the parity projection of forcing season:

```text
S0 -> P0
S1 -> P1
S2 -> P0
S3 -> P1.
```

Therefore:

```text
P0 aliases {S0,S2}
P1 aliases {S1,S3}.
```

The terms `season` and `forcing_season` are synthetic assay labels only. They do not assert physical seasonality, stationarity, periodicity of real-world systems, or universal time structure.

---

## 4. Recurrence-lifted history universe

The finite recurrence universe must be derived from #714 temporal histories and contain at minimum:

```text
R_AB_S0
R_AB_DUP_S0
R_AB_S1
R_AB_S2
R_AB_S3
R_BA_S0
```

Required construction:

```text
R_AB_S0 and R_AB_DUP_S0
  derive from the receipt-distinct #714 T_AB_P0 / T_AB_DUP_P0 pair
  retain distinct receipt-level custody
  share endpoint
  share last_action
  share operational_lineage
  share clock_phase P0
  share forcing_season S0

R_AB_S0 and R_AB_S2
  derive from the same #714 operational history
  share endpoint
  share last_action
  share operational_lineage
  share clock_phase P0
  differ only in forcing_season S0 vs S2

R_AB_S1 and R_AB_S3
  share endpoint
  share last_action
  share operational_lineage
  share clock_phase P1
  differ only in forcing_season S1 vs S3
```

The same endpoint across different forcing seasons is deliberate hostile construction. It isolates schedule aliasing from endpoint difference at assay entry.

---

## 5. Candidate abstractions

Two nested candidates are frozen before implementation.

### 5.1 Parent temporal candidate

```text
K_temporal = {
  endpoint,
  last_action,
  operational_lineage,
  clock_phase
}
```

This is exactly the #714 abstraction and omits `forcing_season`.

### 5.2 Period-four candidate

```text
K_period4 = {
  endpoint,
  last_action,
  operational_lineage,
  clock_phase,
  forcing_season
}
```

Required non-vacuity condition:

```text
K_period4(R_AB_S0) = K_period4(R_AB_DUP_S0)
```

while their receipt variants remain distinct.

A successful result may not be produced by assigning one abstract state per custody object.

---

## 6. Declared period-four exogenous law Ψ₁

`PSI_TICK` advances exactly one exogenous forcing tick without appending a question action to operational lineage.

It must:

1. consume `forcing_season` and `last_action`;
2. update endpoint by the frozen deterministic delta table below;
3. preserve `last_action`;
4. advance `forcing_season` by one position;
5. recompute `clock_phase` from the new forcing season;
6. append a separate exogenous-evolution custody event;
7. preserve receipt provenance without consuming it.

The frozen one-tick delta table is:

```text
S0 + A               -> [[1,0],[0,0]]
S0 + B               -> [[0,1],[0,0]]
S0 + Q_PHASE_PULSE   -> [[1,1],[0,0]]

S1 + A               -> [[0,0],[1,0]]
S1 + B               -> [[0,0],[0,1]]
S1 + Q_PHASE_PULSE   -> [[0,0],[1,1]]

S2 + A               -> [[2,0],[0,0]]
S2 + B               -> [[0,2],[0,0]]
S2 + Q_PHASE_PULSE   -> [[2,2],[0,0]]

S3 + A               -> [[0,0],[2,0]]
S3 + B               -> [[0,0],[0,2]]
S3 + Q_PHASE_PULSE   -> [[0,0],[2,2]]
```

Thus the visible clock repeats every two ticks while the forcing amplitude distinguishes the second half of the four-position schedule.

Any undeclared last-action or forcing-season label must abstain rather than silently select a convenient delta.

---

## 7. Primary phase-aliasing hostile

The preregistered hostile pair is:

```text
R_AB_S0
R_AB_S2
```

Before evolution:

```text
K_temporal(R_AB_S0)
=
K_temporal(R_AB_S2)
```

because both expose:

```text
endpoint = [[3,1],[1,4]]
last_action = B
operational_lineage = [A,B]
clock_phase = P0.
```

But `PSI_TICK` consumes the distinct forcing season:

```text
S0 + B -> [[0,1],[0,0]]
S2 + B -> [[0,2],[0,0]].
```

Therefore the frozen prediction is:

```text
K_temporal(PSI_TICK(R_AB_S0))
!=
K_temporal(PSI_TICK(R_AB_S2)).
```

If witnessed, classify:

```text
PERIOD_TWO_CONTROL_PHASE_ALIASES_PERIOD_FOUR_EXOGENOUS_SCHEDULE
```

and earn only the bounded anti-equivalence:

```text
phase-augmented state sufficient for the earlier two-phase law
!=
state sufficient for a wider declared period-four forcing law
```

---

## 8. Period-four repair obligation

`K_period4` passes the declared schedule only if every non-singleton `K_period4` fiber is representative-independent under the frozen operations.

At minimum:

```text
R_AB_S0
R_AB_DUP_S0
```

must remain a receipt-distinct non-singleton fiber and map to one successor `K_period4` state under every declared evolution length.

Receipt identity may remain in custody.

Receipt identity may not choose the operational successor.

---

## 9. Direct finite evolution Ψ₂, Ψ₃, Ψ₄

Separate direct operations must be provided for two, three, and four forcing ticks:

```text
PSI_TWO_TICKS
PSI_THREE_TICKS
PSI_FOUR_TICKS
```

They may read the same frozen forcing table, but they must not call `PSI_TICK` internally.

For every admitted recurrence history:

```text
K_period4(PSI_TWO_TICKS(h))
=
K_period4(PSI_TICK(PSI_TICK(h)))

K_period4(PSI_THREE_TICKS(h))
=
K_period4(PSI_TICK^3(h))

K_period4(PSI_FOUR_TICKS(h))
=
K_period4(PSI_TICK^4(h)).
```

These are finite authored composition-consistency witnesses only.

They earn no semigroup, flow, generator, stationarity, periodic dynamical-system, Markov, skew-product, or recurrence theorem.

---

## 10. Recurrence anti-equivalence controls

The chamber must explicitly record two recurrence controls from `R_AB_S0`.

### 10.1 Two-tick clock recurrence

After two forcing ticks:

```text
clock_phase returns to P0
forcing_season advances S0 -> S2
endpoint changes
```

Therefore:

```text
same clock phase after two ticks
!=
same forcing position
!=
same endpoint
!=
same K_period4 state.
```

### 10.2 Four-tick schedule recurrence

After four forcing ticks:

```text
clock_phase returns to P0
forcing_season returns to S0
endpoint changes by accumulated lawful evolution
```

Therefore:

```text
same clock phase + same forcing label
!=
endpoint recurrence
!=
full K_period4-state recurrence.
```

No loop, closed path, holonomy, or curvature language may be promoted from label recurrence alone.

---

## 11. Representative-independence obligations

The chamber must evaluate both candidate abstractions against:

```text
PSI_TICK
PSI_TWO_TICKS
PSI_THREE_TICKS
PSI_FOUR_TICKS
```

Required observations:

```text
K_temporal
  -> expected to fail at least PSI_TICK due to S0/S2 aliasing

K_period4
  -> must remain representative-independent for every declared evolution length
```

If K_period4 fails any declared operation, the repair claim fails.

---

## 12. Parent-custody and abstention controls

The executable must snapshot the #714 parent result before and after the full gauntlet.

Any mutation fails the chamber.

An injected history with undeclared `last_action` must return:

```text
UNDECLARED_LAST_ACTION_ABSTAINS
ABSTAIN_BEFORE_PERIOD_FOUR_EVOLUTION
```

An injected history with undeclared `forcing_season` must return:

```text
UNDECLARED_FORCING_SEASON_ABSTAINS
ABSTAIN_BEFORE_PERIOD_FOUR_EVOLUTION
```

No default season, phase, or delta may be invented.

---

## 13. Failure taxonomy

The implementation must keep these observations distinct:

```text
PERIOD_TWO_CONTROL_PHASE_ALIASES_PERIOD_FOUR_EXOGENOUS_SCHEDULE
DECLARED_PERIOD_FOUR_STATE_FAILS_REPRESENTATIVE_INDEPENDENCE
DIRECT_AND_ITERATED_PERIOD_FOUR_EVOLUTION_DISAGREE
CLOCK_RECURRENCE_WITHOUT_FORCING_RECURRENCE
FORCING_RECURRENCE_WITHOUT_ENDPOINT_RECURRENCE
UNDECLARED_LAST_ACTION_ABSTAINS
UNDECLARED_FORCING_SEASON_ABSTAINS
PARENT_CUSTODY_MUTATED
```

The two recurrence observations are controls, not failures.

---

## 14. Claim ceiling

Even if every predicted witness passes, this chamber may earn at most a finite authored claim of the form:

```text
within the declared fixture,
a period-two control phase can alias a period-four exogenous schedule,
and adding the declared period-four forcing coordinate can restore representative-independent evolution for the tested one-through-four-tick grammar.
```

It must not claim:

- generic minimality or optimality of `K_period4`;
- a theorem that every temporal alias is repaired by a longer phase variable;
- hidden-state identification generally;
- Markov-state sufficiency;
- stationarity or ergodicity;
- a periodic-process theorem;
- semigroup, flow, generator, Lie bracket, or BCH structure;
- path object or path-category validity;
- invertibility or groupoid structure;
- transport, connection, loop endomorphism, holonomy, or curvature;
- Berry or quantum structure;
- Proto-Loom or A16 reopening;
- live Ash authority;
- merge, production, or Vercel authority.

---

## 15. Human stop

A successful chamber must stop at:

```text
TEMPORAL_RECURRENCE_PHASE_ALIASING_ROUND_CLOSED
HUMAN_𝄐_REQUIRED_BEFORE_PROMOTING_ANY_TEMPORALLY_AUGMENTED_QUOTIENT_TO_A_FIRST_BOUNDED_PATH_OBJECT_OR_PATH_GRAMMAR
```

The next chamber, if human-authorized, may ask whether the now repeatedly hostile-tested quotient can serve as a first bounded object in an explicitly typed path grammar.

That future possibility is not authorized here.

𝌋

Sealed ⟐
