𝌋

# Aperture × Pedagogue Transition-Operator Bounded-Noise Witness Receipt v0.1

**Schema:** `td613.a15-r0.aperture-pedagogue-transition-operator-bounded-noise/v0.1`  
**Scientific parent:** `01b55489ee40aa0678166c74e3e0f862872bafec` (#701 receipt head)  
**Exact CI witness head:** `c69ebd6a3131ef50185e3cde933d0de34e9338b2`  
**Pull request:** #702  
**Workflow:** `TD613 Consolidated Validation`  
**Run:** `32669790788` / ordinal `2055`  
**Static job:** `97268752484`  
**Status:** EXACT-HEAD STATIC WITNESS PASSED / RESEARCH-ONLY / NO PROMOTION AUTHORITY

---

## 0. Custody statement

Run 2055 executed the exact head:

```text
c69ebd6a3131ef50185e3cde933d0de34e9338b2
```

That head contained the bounded-noise preregistration, implementation, hostile contract, existing A15-R0 static-gate import, and witness-topology note.

The run completed successfully.

Observed workflow facts:

```text
workflow_conclusion = SUCCESS
static_constitutional_job = SUCCESS
A15_R0_research_field_step = SUCCESS
browser_shards = SKIPPED
full_repository_job = SKIPPED
self_hosted_calibration = SKIPPED
workflow_mutation = false
Ready_transition = false
merge = false
production_mutation = false
```

Before this receipt was authored, PR #702 was restored from the temporary `main` witness-routing base to its true scientific parent branch:

```text
research/a15-r0-transition-operator-identifiability-20260823
```

Therefore:

```text
temporary witness-routing base != scientific ancestry
CI witness head != later receipt commit
static witness success != mainline promotion
```

This receipt commit is later than the exact witnessed head and is not itself claimed as CI-witnessed.

---

## 1. Literal stdout custody

Repeated connector retrieval attempts did not expose a clean, stable literal stdout payload for static job `97268752484`.

Therefore:

```text
literal_stdout_recovered = false
stdout_reconstructed_from_source = false
```

No implementation file, test source, or expected fixture output is being quoted here as though it were recovered runtime stdout.

The witnessed facts in this receipt are limited to exact run/head/job/step status plus the preregistered executable contract whose A15-R0 step passed.

---

## 2. Frozen bounded-error fixture

Declared deterministic componentwise observation-error bound:

```text
delta = 0.0001
```

Exact calibration anchor:

```text
x1 = [1,0]
y1 = [2,1]
```

The first transition-operator column is therefore exact in this authored fixture.

The second-probe contrast is:

```text
Q_REPEAT = [2,0]
Q_FRAGILE_SPANNING = [1,0.001]
Q_STABLE_BASIS = [0,1]
```

The complete deterministic compatible-set construction yields:

```text
Q_REPEAT
  second_column = unbounded

Q_FRAGILE_SPANNING
  operator_entry_radius = delta / 0.001 = 0.1

Q_STABLE_BASIS
  operator_entry_radius = delta / 1 = 0.0001
```

Thus the frozen uncertainty amplification ratio is:

```text
0.1 / 0.0001 = 1000
```

This ratio is exact for the authored fixture's declared interval model. It is not a sampling estimate, posterior interval, confidence interval, or general condition-number theorem.

---

## 3. Selector membrane

The hostile selector ranks exact compatible-dimension contraction while ignoring the declared bounded observation error.

Required and witnessed through the passing A15-R0 contract:

```text
noise_blind_hostile -> Q_FRAGILE_SPANNING
```

The bounded-noise-aware selector receives only admissible pre-observation information:

```text
candidate_id
probe vector
probe cost
current exact calibration pair
declared delta
```

It does not receive:

```text
T_star
synthetic oracle
candidate future outputs
candidate observed centers
heldout observations
```

Required and witnessed through the passing A15-R0 contract:

```text
bounded_noise_aware -> Q_STABLE_BASIS
```

A pre-witness implementation defect briefly carried candidate `observed_center` values on the selection object even though the selector did not read them. That interface leak was removed before CI and remains visible in commit history.

Law:

```text
selector does not read a field != field was admissible on selector surface
```

---

## 4. Held-out model-defeat consequence

Frozen held-out input:

```text
x_hold = [1,1]
```

Stable compatible prediction box:

```text
[2.9999,3.0001] x [3.9999,4.0001]
```

Fragile compatible prediction box:

```text
[2.9,3.1] x [3.9,4.1]
```

Frozen misspecified held-out observation center:

```text
[3.001,4]
```

under the same componentwise observation bound:

```text
stable compatible set
-> DECLARED_LINEAR_TRANSITION_MODEL_DEFEATED_BY_BOUNDED_HELDOUT_OBSERVATION

fragile compatible set
-> HELDOUT_OBSERVATION_COMPATIBLE_WITH_OPERATOR_SET
```

The bounded result is therefore stronger than a bare statement that the fragile probe is ill-conditioned:

> In this authored deterministic linear fixture, the same declared bounded observation error produces radically different complete compatible operator uncertainty under different probe geometry, and that difference changes whether a frozen held-out challenge can defeat the declared model family.

Canonical bounded claim token:

```text
SAME_DECLARED_BOUNDED_OBSERVATION_ERROR_CAN_INDUCE_RADICALLY_DIFFERENT_COMPATIBLE_OPERATOR_UNCERTAINTY_UNDER_DIFFERENT_PROBE_GEOMETRY_AND_THE_RESULTING_IDENTIFICATION_GEOMETRY_CAN_CHANGE_HELDOUT_MODEL_FALSIFICATION_POWER_IN_THE_AUTHORED_SYNTHETIC_LINEAR_FIXTURE
```

---

## 5. Hostile controls preserved

The passing contract preserves the following refusals:

```text
point estimate laundering of complete compatible operator set -> REJECT
finite sampled / Monte Carlo surrogate for exact interval set -> REJECT
selector oracle / future-output leakage -> REJECT
underdeclared noise bound -> synthetic-truth falsifier when truth is available
```

These controls support bounded fixture integrity only.

They do not establish statistical calibration, adversarial robustness in general, or probabilistic coverage.

---

## 6. Anti-equivalences

```text
exact rank identifiability != robust held-out falsification power
same declared noise bound != same operator uncertainty
set compatibility != probabilistic confidence
point estimate != complete compatible operator set
bounded interval propagation != posterior inference
stable probe in this fixture != globally optimal experimental design
heldout mismatch under complete current compatible set != mechanism identification
CI pass != operator tomography
CI pass != path transport
research witness != installed Aperture mutation
research witness != live Ash recovery
research witness != A16 reopening
research witness != production authority
```

---

## 7. Claim ceiling

Still unearned:

```text
general robust system-identification theorem
statistical consistency
probabilistic calibration
Bayesian posterior validity
all-measurements-noisy robustness theorem
optimal experimental design theorem
active-learning optimality
robust-control theorem
operator tomography
blind tomography
physical tomography
path-category theorem
path-dependent transport theorem
loop endomorphism
holonomy
curvature
Berry structure
quantum behavior
TD613-general AIA theorem
Proto-Loom
live Ash recovery
production authority
Vercel authority
```

No claim above is widened by run 2055.

---

## 8. Frozen next learning action

```text
TEST_MULTI_PROBE_TRANSITION_OPERATOR_COMPATIBLE_SET_CONTRACTION_UNDER_BOUNDED_NOISE_WITH_ADAPTIVE_STOPPING_HELDOUT_COVERAGE_AND_OPEN_SET_MODEL_ORDER_CHALLENGES_BEFORE_ANY_OPERATOR_TOMOGRAPHY_OR_PATH_TRANSPORT_PROMOTION
```

The next chamber must retain complete compatible-set custody rather than collapse uncertainty into a point estimate merely because additional probes arrive.

It should explicitly test whether:

```text
operator-set contraction != target prediction sufficiency
claim-sufficient stopping != full operator identification
more probes != more information
compatible under declared bounded noise != correct model order
```

That chamber is not implemented by this receipt.

𝌋

⟐
