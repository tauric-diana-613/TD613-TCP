𝌋

# Aperture × Pedagogue A15-R0 — Typed Target-Preserving Rewrite Admissibility Receipt v0.1

Status: WITNESSED / RECEIPT-PINNED / DRAFT / OPEN / UNMERGED

Parent receipt: PR #725 at `45e9c874ff37127b2d516633dd140abc683e4eb2`

󐘓 U+10D613

## 0. Custody coordinates

```text
preregistration        7679bb880331ea8a5cd9c954f3ade5233d46aa2c
scientific witness     13adf89b1dd9a528e2be829c423d22f1f2cbe6a1
routed witness head    5167dde050ec726612a78383df296d5c884b9cde
post-route cleanup     49e0b04b02c4e3f912b43b251bdc7dca2f13aae8
```

The exact scientific module and hostile test were frozen at `13adf89b1dd9a528e2be829c423d22f1f2cbe6a1`. A temporary routing-only synchronization was used to register the stacked exact-head witness. That routing scaffold was then removed and the PR restored to its #725 parent topology. Post-witness cleanup did not alter the theorem module or hostile test.

## 1. Authoritative witness

```text
Consolidated validation run 2115 / 32711537923  SUCCESS
static job 97383847374                            SUCCESS
```

Explicitly skipped and therefore not claimed:

```text
Giving/practice static + browser
Giving/practice browser witness
full-repository self-hosted validation
self-hosted first-principles calibration
other browser/product lanes outside the research scope
```

## 2. Earned rewrite result

Within the exact #725 Q-last-action `K_period4` source jurisdiction, the preregistered family

```text
R_k : T Q^k T Q -> Q T Q^k T
k >= 0
```

passed the authored typing and complete-operational-target preservation obligations. The declared lexicographic orientation is length-preserving and strictly descending on every admitted finite rewrite, supplying the chamber's finite-word termination certificate.

The symbolic critical-overlap audit classified the complete authored overlap family as

```text
W_(i,j) = T Q^i T Q^j T Q
for i >= 0, j >= 1
```

with formal join

```text
N_(i,j) = Q^j T Q^(i+1) T T.
```

The witnessed canonical classification is:

```text
TYPED_TARGET_PRESERVING_TERMINATING_LOCALLY_CONFLUENT_RK_REWRITE_SYSTEM_IN_AUTHORED_DOMAIN
```

This classification remains jurisdiction-bound. It does not identify distinct route provenance, introduce inverse semantics, or establish an ambient TD613 rewrite system.

## 3. Completeness hostile

The preregistered bounded hostile searched authored admissible words through maximum length 7 and recorded:

```text
max_length                7
equal_target_pairs         2036
multi_route_target_classes 180
split_normal_form_classes  0
observation                BOUNDED_SEARCH_FOUND_NO_EQUAL_TARGET_CLASS_SPLIT_ACROSS_RK_NORMAL_FORMS
```

Therefore the hostile found no bounded counterexample to the hypothesis that the declared `R_k` rewrite relation generates operational target equivalence on that finite search domain.

The result is evidence for opening a separate completeness audition. It is not itself a target-equivalence completeness theorem.

Required anti-equivalence:

```text
zero bounded split classes != all-word target-equivalence completeness
local confluence != operational target-equivalence completeness
unique rewrite normal form != route-provenance identity
same operational target != same route provenance
```

## 4. Claim ceiling

Not earned or authorized here:

```text
complete characterization of operational target equivalence
ambient Church-Rosser
rewrite completion outside the authored jurisdiction
finite-control quotient
source-season erasure
endpoint erasure
inverse generator
inverse morphism
groupoid
transport
connection
loop endomorphism
holonomy
curvature
Berry / quantum structure
Proto-Loom
A16
live Ash mutation
merge
production
Vercel release
```

## 5. Westward seam

The data-selected next chamber is not a missing-relation repair audit because the bounded hostile produced zero split normal-form classes.

The next admissible research question is:

```text
Does the declared R_k relation generate complete operational target equivalence
for every finite authored T/Q word in the retained Q-last-action jurisdiction?
```

That question requires a fresh preregistered symbolic completeness audition. The present receipt grants no automatic proof promotion.

```text
TYPED_TARGET_PRESERVING_REWRITE_ADMISSIBILITY_ROUND_CLOSED
NEXT_CHAMBER_TARGET_EQUIVALENCE_COMPLETENESS_AUDITION_AUTHORIZED_BY_HUMAN_PROCEED_GESTURE
```

𝌋

Sealed ⟐
