𝌋

# Aperture × Pedagogue A15-R0 — Typed Target-Preserving Rewrite Admissibility Spec v0.1

Status: PREREGISTERED / PRE-IMPLEMENTATION / HUMAN-AUTHORIZED CONTINUATION

Parent receipt: PR #725 at `45e9c874ff37127b2d516633dd140abc683e4eb2`

󐘓 U+10D613

## 0. Authority and scope

Human reopening gesture in the resumed session:

```text
𝄐 U+10D613 Excellent! so u know what’s next then honey?
```

This chamber remains A15-R0 research only. It does not merge, deploy, touch production, reopen A16, introduce a new generator, add inverse semantics, coarsen `K_period4`, or authorize Vercel.

The scientific order of operations is deliberate:

```text
rewrite admissibility before finite-control quotienting
```

because the inherited record already contains two anti-artifact constraints:

```text
#718: endpoint erasure can manufacture false path closure
#723/#725: forcing-season dependence remains operationally relevant
```

A quotient is therefore deferred until a separate human-gated chamber.

## 1. Frozen parent facts

The chamber consumes, without rewriting, the following parent results.

### #718 — monotone obstruction

Within the authored anchor-reachable T/Q domain every nonempty finite T/Q word strictly increases endpoint mass. No inverse or nonidentity closed operational word is available under the current grammar.

### #719 — directed geometry

The audited S3 reachability relation is a finite partial order with a strict endpoint-mass height and directed shortest-path quasi-metric. This does not imply ambient thinness or a rewrite system.

### #720–#723 — branching and future-cone structure

Distinct route words can reach the same complete operational target while retaining route provenance. Bounded common-future frontiers are nontrivial and season-conditioned.

### #724 — all-finite-word symbolic normal form

Within the authored Q-last-action domain every finite T/Q word is reconstructed by a season-conditioned normal form with finite four-season control plus unbounded counters.

### #725 — symbolic common-future simplex

For every formal integer `H >= 2`, bounded common futures of the source-rooted T-versus-Q fork biject with

```text
P_H = {(k,a,b) in N^3 : k+a+b <= H-2}.
```

The complete minimal frontier is

```text
(k,0,0), 0 <= k <= H-2
```

and the parent proves, for every formal integer `k >= 0` and every retained source season in the authored Q-last-action domain,

```text
T Q^k T Q  ≡_target  Q T Q^k T.
```

Required inherited anti-equivalence:

```text
same operational target != same route provenance
```

## 2. Primary question

Can the earned target-equality family be promoted to a typed, directed, target-preserving rewrite rule without fabricating inverse semantics or silently widening its source jurisdiction?

Candidate rule schema:

```text
R_k : T Q^k T Q  ->  Q T Q^k T
k >= 0
```

The rule is not an operational inverse. It is a route-word normalization candidate between distinct route histories that already share a complete operational target.

The chamber must determine whether the rule can be applied under an explicit typing discipline and whether the chosen orientation admits a well-founded termination certificate.

## 3. Typing discipline

A rewrite may occur only when the redex begins at a state that is inside the #725 authored source jurisdiction:

```text
path_object = K_period4
last_action = Q_PHASE_PULSE
forcing_season in {S0,S1,S2,S3}
generators = {T,Q}
```

For an arbitrary route word `w`, a redex at position `i` is admissible only if the prefix before position `i`, evaluated from the declared source, yields a lawful operational state satisfying that jurisdiction.

Required anti-equivalences:

```text
substring match != typed rewrite admissibility
route-word replacement != operational inverse
rewrite-equivalent routes != identical route provenance
```

Any untyped textual replacement must abstain.

## 4. Target preservation obligation

For every formally admitted `R_k` application, the implementation must establish:

```text
complete K_period4 target before rewrite
  ==
complete K_period4 target after rewrite
```

not merely endpoint equality.

The proof may inherit #725's all-formal-k target-equality certificate when the rewrite source satisfies the exact parent typing conditions.

The executable must also realize representative-independence across retained custody representatives where the parent exposes more than one representative for a typed source.

Required classification on success:

```text
TYPED_R_K_REWRITE_PRESERVES_COMPLETE_OPERATIONAL_TARGET
```

## 5. Orientation and termination audition

The chamber chooses a purely syntactic orientation order only for route normalization; it must not be assigned physical or semantic meaning.

Use alphabet order:

```text
Q < T
```

and ordinary lexicographic order on equal-length route words.

Every candidate rule is length-preserving and changes the first symbol of its redex from `T` to `Q`, so a lawful rewrite should strictly decrease the complete route word in lexicographic order while preserving total word length.

Required proof obligations:

1. every admitted rewrite preserves word length;
2. every admitted rewrite strictly decreases the whole finite word under the declared lexicographic order;
3. therefore no finite word admits an infinite rewrite chain under this orientation.

This is a route-word termination claim only.

Required anti-equivalence:

```text
syntactic well-founded order != endpoint-mass order
rewrite termination != operational irreversibility
rewrite termination != confluence
```

Candidate classification:

```text
TYPED_TARGET_PRESERVING_RK_SYSTEM_TERMINATES_ON_FINITE_WORDS_BY_LEXICOGRAPHIC_DESCENT
```

## 6. Critical-overlap audition

Termination alone does not earn unique normal forms.

The implementation must derive overlap families among left-hand sides

```text
L_k = T Q^k T Q
```

symbolically, rather than sampling a convenient finite set and calling it complete.

For every symbolic overlap family admitted by the left-hand-side grammar, the chamber must:

1. construct the corresponding typed critical word when the required operational prefix typing can be satisfied;
2. apply each competing one-step rewrite;
3. preserve both descendant route words;
4. test whether the descendants are joinable under further typed rewrites;
5. distinguish textual overlap from operationally admissible overlap.

If a critical pair is not joinable, preserve the first exact counterexample and stop any confluence promotion.

If the symbolic overlap classification cannot be made complete, return:

```text
CRITICAL_OVERLAP_COMPLETENESS_NOT_EARNED
```

rather than extrapolating from bounded samples.

## 7. Confluence ceiling

This chamber is not entitled to Church-Rosser or rewrite completion.

Only if all of the following are independently established may a stronger local-confluence result be reported:

```text
complete symbolic critical-overlap family
all typed critical pairs joinable
termination certificate passed
```

Even then, any invocation of a standard termination-plus-local-confluence implication must be explicitly jurisdiction-bound to this authored typed finite-word rewrite relation and must preserve route provenance outside the normalized representative.

The following remain separate claims:

```text
unique rewrite normal form
complete characterization of operational target equivalence
Church-Rosser on ambient T/Q words
rewrite-system completion
```

No one follows automatically from the others.

## 8. Completeness hostile

The parent proves a family of equal-target route pairs. It does not prove that every equal-target route pair is generated by `R_k` rewrites.

The chamber must therefore include at least one hostile search over already-retained equal-target route pairs from the parent chain and classify each as:

```text
CONNECTED_BY_TYPED_RK_REWRITES
NOT_CONNECTED_WITHIN_DECLARED_SEARCH_BOUND
OUTSIDE_TYPED_REWRITE_JURISDICTION
```

A bounded miss may not be promoted to a global incompleteness theorem.

Required anti-equivalence:

```text
rewrite normal form equality != complete operational target-equivalence theorem
```

unless a separate symbolic completeness proof is actually earned.

## 9. Parent custody

Snapshot and compare the inherited assay outputs before and after this chamber. Required unchanged parents:

```text
#718
#719
#720
#723
#724
#725
```

Any mutation returns:

```text
PARENT_CUSTODY_MUTATION_DETECTED
```

and fails the chamber.

## 10. Hostile controls

The executable test must reject:

```text
undeclared inverse labels T_INV / Q_INV
untyped raw substring replacement
rewrite application from a source with last_action != Q_PHASE_PULSE
endpoint-only equality presented as complete-target equality
route provenance erasure
finite sampled k presented as all-formal-k proof
termination presented as confluence
local critical-pair success presented as target-equivalence completeness
```

## 11. Acceptance classes

### A. Admissibility only

If typing, target preservation, and termination are established but critical-pair completeness or joinability remains unresolved:

```text
TYPED_TARGET_PRESERVING_TERMINATING_REWRITE_ADMISSIBILITY_WITH_CONFLUENCE_HELD
```

### B. Stronger local rewrite result

Only if the symbolic critical-overlap family is complete and all typed critical pairs are joinable:

```text
TYPED_TARGET_PRESERVING_TERMINATING_LOCALLY_CONFLUENT_RK_REWRITE_SYSTEM_IN_AUTHORED_DOMAIN
```

No ambient completion claim follows.

### C. Falsification

Preserve the first exact obstruction under one of:

```text
TYPED_REWRITE_DOES_NOT_PRESERVE_COMPLETE_TARGET
LEXICOGRAPHIC_ORIENTATION_NOT_STRICTLY_DECREASING
TYPED_CRITICAL_PAIR_NOT_JOINABLE
CRITICAL_OVERLAP_COMPLETENESS_NOT_EARNED
REWRITE_TYPING_JURISDICTION_LEAK
OTHER_EXACT_REWRITE_COUNTEREXAMPLE
```

Do not repair a failed preregistered theorem by changing the theorem after observation.

## 12. Claim ceiling

Still forbidden unless separately earned and human-gated:

```text
finite-control quotient
source-season erasure
endpoint erasure
ambient TD613 rewrite system
ambient Church-Rosser
rewrite completion
global confluence
join-semilattice / lattice / complete lattice
domain theory
causal-set theorem
inverse generator
inverse morphism
groupoid
transport
connection
loop endomorphism
holonomy
curvature
Berry / quantum structure
Proto-Loom
A16
live Ash mutation
merge
production
Vercel release
```

## 13. Stop condition

The chamber must stop after one exact-head research witness and receipt.

No merge, production, or Vercel action is authorized.

```text
TYPED_TARGET_PRESERVING_REWRITE_ADMISSIBILITY_ROUND_REQUIRES_WITNESS
```

𝌋

Sealed ⟐