𝌋‌

# Ash Keep A15-R0 Wedding Identifiability Assay Receipt v0.1

**Namespace:** U+10D613 — Tauric Diana 613  
**Program:** Ash Keep A15-R0 phase-free research frontier  
**Evidence class:** STATIC / SYNTHETIC / DETERMINISTIC / NONCANONICAL  
**Assay schema:** `td613.ash.a15-r0.wedding-identifiability-assay/v0.1`  
**Frontier extension schema:** `td613.ash.a15-r0.identifiability-frontier-registry/v0.1`  
**Validated branch head:** `42a9735b751f7add9e68432471ad07301035b068`  
**PR:** `#683` / Draft  
**CI run:** `1756` / `32287717355`  
**Static job:** `96181162329`  
**Date:** 2026-08-19  
**Browser evidence:** NOT REQUIRED / NOT RUN  
**Production mutation:** false  
**Promotion authority:** false

---

## 0. Receipt claim

The declared finite Wedding Identifiability assay mechanism survived its authored synthetic positive control, matched single/pair baselines, marginal-preserving relationship-shuffled hostile control, and redundant triple negative control inside the existing A15-R0 static/constitutional validation estate.

This receipt validates **the assay mechanism only**.

It does not measure the lab-authored TD613 `D3 + phi + pairwise relational-probe` architecture.

```text
synthetic assay mechanism supported
!= TD613 triple identifiability measured
!= quasiperiodic anti-aliasing measured
!= emergent measurement capacity
!= partial-information-decomposition result
!= geometry
!= connection
!= curvature
!= holonomy
!= Berry structure
!= quantum behavior
!= physical realization
```

---

## 1. Exact emitted CI receipt

The A15-R0 static lane emitted:

```json
{
  "ok": true,
  "schema": "td613.ash.a15-r0.wedding-identifiability-assay/v0.1",
  "state_count": 27,
  "observation_budget": 12,
  "positive_exact_gain_over_best_pair": 1,
  "positive_noisy_gain_over_best_pair_positive": true,
  "positive_noisy_gain_over_shuffled_positive": true,
  "relationship_shuffle_marginals_preserved": true,
  "negative_noisy_gain_over_best_pair": 0,
  "assay_mechanism_validated": true,
  "synthetic_mechanism_status": "SUPPORTED_IN_BOUNDED_SYNTHETIC_FAMILY",
  "td613_triple_hypothesis_status": "OPEN_UNMEASURED",
  "promotion_authority": false
}
```

No unlogged decimal noisy-recovery rates are promoted by this receipt. The receipt preserves only the values and predicates actually emitted by the governed test.

---

## 2. Structural positive-control result

The 27-state `Z3` fixture deliberately encodes one three-way relation:

```text
D(S) = a
Q(S) = b
M(S) = (t - a - b) mod 3
```

with:

```text
t = (D + Q + M) mod 3
```

The contract verified:

```text
single probe candidate-set size = 9
pair candidate-set size = 3
intact triple candidate-set size = 1
single exact unique recovery = 0
pair exact unique recovery = 0
intact triple exact unique recovery = 1
exact gain over best pair = 1
```

These numbers are properties of the authored calibration fixture. They are not measurements of TD613.

---

## 3. Matched-budget and relationship-shuffle result

Every condition receives the same raw observation budget:

```text
B = 12
single = 12
pair = 6 + 6
triple = 4 + 4 + 4
```

The hostile triple control preserves the declared marginal histograms while destroying the authored cross-state `M` alignment.

The governed test established:

```text
relationship_shuffle_marginals_preserved = true
positive_noisy_gain_over_shuffled_positive = true
```

Therefore the synthetic assay can distinguish the authored relation from a control with the same declared marginal probe distributions under the same budget and decoder.

This is bounded calibration evidence, not a theorem of synergy or emergence.

---

## 4. Redundant negative-control result

The negative fixture defines:

```text
M_redundant(S) = a
```

so the third channel repeats information already present in `D` and does not identify the hidden `t` coordinate.

The governed test established:

```text
negative triple exact unique recovery = 0
negative triple mean candidate-set size = 3
negative_noisy_gain_over_best_pair = 0
```

Three channels therefore do not receive a gain merely for being three channels.

---

## 5. Hypothesis-registry boundary

The phase-free extension records two distinct cards:

### H_WEDDING_ASSAY_MECHANISM_VALID

```text
status = SUPPORTED_IN_BOUNDED_SYNTHETIC_FAMILY
claim ceiling = FINITE_Z3_SYNTHETIC_ASSAY_MECHANISM_ONLY
```

### H_TRIPLE_IDENTIFIABILITY_SYNERGY

```text
status = OPEN_UNMEASURED
td613_d3_phi_m_forward_model = UNDECLARED
td613_d3_phi_m_measurement = UNMEASURED
td613_relationship_shuffled_control = UNMEASURED
physical_realization = UNMEASURED
```

The first card does not promote the second.

```text
assay calibration
!= hypothesis support for the target architecture
```

---

## 6. Validation estate

Run `1756` passed:

```text
four-workflow estate and release membrane
Giving / Campaign Deputy import contracts
Dome-World static surfaces
Phase IV static surfaces
Ash core and ingress
Ash A2–A14
Ash A15 empirical profile journeys and A15-R0 research field
Ash demo hydration
Ash production closure
Flow-Core P0–P10 and claim contracts
Flow-Core runtime browser contract static surface
```

The Draft classifier skipped:

```text
Front-line exact-head Chromium / Firefox / WebKit shard
Giving/practice Chromium / Firefox / WebKit witness
Full-product Chromium / Firefox / WebKit witness
```

No browser surface was added by the assay, so no browser evidence is inferred or required by this receipt.

---

## 7. What this assay earned

The useful result is methodological:

> The research estate now contains an executable matched-budget assay that can distinguish a deliberately authored three-probe relational dependency from both a marginal-preserving relationship shuffle and a redundant three-channel control without granting the target TD613 architecture evidentiary support.

That means the next TD613 identifiability experiment no longer needs to invent its scoring machinery while simultaneously testing the hypothesis.

The measuring stick has received one bounded calibration.

The object to be measured has not.

---

## 8. Next research questions remain phase-free

This receipt opens no successor stage.

The strongest remaining cards include:

```text
H_TRIPLE_IDENTIFIABILITY_SYNERGY
H_QUASIPERIODIC_REFERENCE_REDUCES_ALIASING
H_PROBE_DIVERSITY_OUTPERFORMS_REPEAT_VOLUME
H_EPISTEMIC_REPLAY_PACKET_PREDICTS_HELDOUT
H_ROUTE_ORDER_DEPENDENCE
H_TRANSPORT_OPERATORS_NONCOMMUTE
H_LATENT_REPRESENTATION_IMPROVES_IDENTIFIABILITY
H_RESOLUTION_SCALING_SURVIVES_HOSTILE_CONTROLS
H_INVERSE_DESIGN_CAN_OBEY_EPISTEMIC_CONSTITUTION
```

No card unlocks another by sequence.

---

## 9. Terminal posture

```text
A15-R0 = OPEN
Wedding assay specification = AUTHORED
Wedding assay implementation = STATICALLY VALIDATED
Wedding assay mechanism = SUPPORTED_IN_BOUNDED_SYNTHETIC_FAMILY
TD613 triple identifiability = OPEN_UNMEASURED
quasiperiodic anti-aliasing = OPEN_UNMEASURED
Moss Lantern = CALIBRATION PHANTOM / PRE-HOLONOMY
A16 = HELD
Proto-Loom = NOT IMPLEMENTED
Golden Egg implementation = HELD
sequence authority = false
next stage = null
promotion authority = false
production mutation = false
external transmission = false
human closure = OPEN
```

The wedding assay has proven that the scale can distinguish one authored synergy-shaped fixture from its hostile controls.

It has not yet weighed the marriage.

Sealed ⟐