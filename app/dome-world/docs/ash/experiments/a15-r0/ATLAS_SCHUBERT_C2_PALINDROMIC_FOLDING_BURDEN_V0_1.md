# A15-R0 · Atlas Schubert C2 Palindromic Folding Burden v0.1

Exact earned parent: `#978 / 3c3a3dac296a819fad7c896fc2042510a6709ea9 / run 2452 SUCCESS`.

Frozen window: `d=1..7`, `k=0..5`, `0<=s<=min(d-1,k)`.

Exact burden:
- 42 formal cells;
- 112 gap slices;
- 9,912 supported Möbius interval objects;
- 190 fixed objects under earned #976 involution;
- 64 centerless palindromic fixed objects;
- 31 E-centered fixed objects;
- 64 N-centered fixed objects;
- 31 D-centered fixed objects;
- 20 centerless slices;
- 14 E-centered slices;
- 20 N-centered slices;
- 14 D-centered slices;
- 44 parity-impossible zero-fixed slices;
- zero expected path-equivariance, palindrome-equivalence, fold/unfold, decode, center-parity, count, or middle-rank failures.

Every one of the 9,912 support objects must be checked for path equivariance and fixed/palindrome equivalence. Every fixed object must round-trip through path fold, parity-forced center, unfold, and Delannoy decode back to the same Atlas interval.

Hostile controls must reject E/N exchange during reversal, arbitrary center choice, left-half truncation without reflected reconstruction, and count-only verification without interval round-trip.

`PALINDROMIC_PATH != TEMPORAL_REVERSAL`; `FOLDING_BIJECTION != PHYSICAL_FOLDING`; `CENTER_SYMBOL != TIME_ORIGIN`; `HALF_PATH != HALF_STATE`; `FIXED_FLAG_COMBINATORICS != BASIS_FREE_GEOMETRY`; `SUCCESSFUL_EXACT_HEAD_GREEN != MERGE_AUTHORITY`.

Sealed ⟐
