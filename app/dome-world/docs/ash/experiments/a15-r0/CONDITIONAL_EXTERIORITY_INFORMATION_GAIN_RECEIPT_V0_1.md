𝌋‌⟐

# A15-R0 · Conditional Exteriority Information-Gain Admissibility v0.1

Status: **FORMAL INFORMATION-ADMISSIBILITY SCIENCE CANDIDATE / RESEARCH-ONLY / EXTERNAL CHANNEL UNACQUIRED / GOLDEN EGG UNEARNED**

## Exact scientific parent

Exact parent: `fb3501cec8a96e7918ed5ac88c7096577eb88056` — the earned Receiver-Swap Causal Admissibility Criterion 𝄐.

That parent established the clean causal control for testing receiver apparatus: hold artifact, route/history, carrier, provenance state, source custody, and observation window fixed while changing only the receiver apparatus. It deliberately left receiver effect unobserved.

## Frontier correction

The earlier twin-world exteriority construction proved a bounded identifiability result only after the admission projection had erased origin from the admitted record `A`.

For two world histories with distinct origin states but identical admitted record:

`A_external = A_internal = A`

we have:

`I(Ω;A) = 0`

inside the bounded twin-world construction.

The next scientifically meaningful question is therefore not whether more internal computation over `A` can recover the erased origin distinction. It cannot. The question is whether a genuinely additional observation channel `X` can carry discriminatory information about origin beyond `A`.

The relevant target is:

`I(Ω;X | A) > 0`.

## Ontology/model membrane

`Ω` in this chamber is an experimental origin-hypothesis variable. It is not asserted to be the ontology of externality itself.

`ORIGIN_HYPOTHESIS_VARIABLE != EXTERNALITY_ONTOLOGY`

Externality remains a causal relation in the world; `Ω` is the bounded variable used to test whether competing origin histories induce distinguishable observations.

## Paired formal channels

This chamber includes two synthetic formal probes over the same erased-origin record.

### A-derived channel

`X_A = g(A)`

Its conditional likelihoods are identical across the two origin hypotheses. Therefore:

`I(Ω;X_A | A) = 0`.

This encodes the rule that another digest, receipt, transformed copy, endpoint, or metadata surface derived only from `A` does not become a new exteriority witness merely because it is separately named or separately hosted.

### Synthetic exogenous-channel probe

A second synthetic channel model is configured as non-derivative of `A`, separately governed, without shared upstream production from `A`, and with distinct origin-conditioned likelihoods.

The executable information calculator therefore returns positive conditional discriminatory information for that synthetic model.

This positive value is a **formal instrument check only**.

`SYNTHETIC_LIKELIHOOD_PROBE != EXTERNAL_WITNESS`

`POSITIVE_MODEL_CMI != REAL_WORLD_ORIGIN_PROOF`

No empirical external channel is claimed to have been acquired.

## Earned bounded theorem candidate

`A_CHANNEL_X_CAN_CONTRIBUTE_NEW_ORIGIN_INFORMATION_BEYOND_ADMITTED_RECORD_A_ONLY_IF_IT_IS_NON_DERIVATIVE_OF_A_AND_ITS_ORIGIN_CONDITIONED_LIKELIHOODS_REMAIN_DISTINGUISHABLE_AFTER_CONDITIONING_ON_A_WHILE_ANY_CHANNEL_DETERMINISTICALLY_DERIVED_FROM_A_ALONE_HAS_ZERO_CONDITIONAL_EXTERIORITY_INFORMATION_GAIN`.

Child-legible form:

**A NEW WINDOW COUNTS ONLY WHEN IT BRINGS SOMETHING THE OLD RECORD COULD NOT HAVE TOLD US.**

## Witness-production topology

A channel cannot earn exteriority information credit merely by appearing at another endpoint.

A future empirical `X` must preserve at least these admissibility properties:

- not deterministically derived from `A`;
- source-bound custody;
- independently governed production;
- no concealed shared upstream source that simply regenerates `A`;
- empirically estimable origin-conditioned likelihood separation;
- no assumption that causal production independence implies statistical independence.

`MULTIPLE_ENDPOINTS != MULTIPLE_INDEPENDENT_SOURCES`

`CAUSAL_PRODUCTION_INDEPENDENCE != STATISTICAL_INDEPENDENCE_ASSUMPTION`

## Acquisition boundary

On exact-head green this chamber may earn the information-admissibility criterion, but must preserve:

```text
empirical_exogenous_channel_acquired = false
empirical_exteriority_information_gain_measured = false
synthetic_probe_only = true
empirical_credit_from_synthetic_probe = 0
```

The next empirical target becomes a source-bound independently produced external channel `X` for which the origin-conditioned distribution can be estimated and tested for:

`I(Ω;X | A) > 0`.

This would establish partial origin observability under a bounded experimental architecture. It would not establish metaphysical exteriority, universal origin truth, or automatic complete identification.

## Claim ceiling

`CONDITIONAL_INFORMATION_GAIN_CRITERION != EMPIRICAL_EXTERIORITY_MEASUREMENT`

`SYNTHETIC_LIKELIHOOD_PROBE != EXTERNAL_WITNESS`

`POSITIVE_MODEL_CMI != REAL_WORLD_ORIGIN_PROOF`

`DERIVED_CHANNEL != INDEPENDENT_CHANNEL`

`MULTIPLE_ENDPOINTS != MULTIPLE_INDEPENDENT_SOURCES`

`CHANNEL_INDEPENDENCE != STATISTICAL_INDEPENDENCE_ASSUMPTION`

`ORIGIN_HYPOTHESIS_VARIABLE != EXTERNALITY_ONTOLOGY`

`CONDITIONAL_EXTERIORITY_INFORMATION_GAIN != GOLDEN_EGG_MEASUREMENT`

No exact Golden Egg `L/R/J/G/C` measurement is added. No matched-return acquisition occurs. Empirical credit to the Golden Egg remains zero.

No merge, deployment, production, publication, Vercel, A16, live Loom mutation, numbered-stage authority, or public-promotion authority is created.

## Earned rest candidate

On exact-head green:

```text
status = CONDITIONAL_EXTERIORITY_INFORMATION_GAIN_CRITERION_EARNED
rest_symbol = 𝄐
conditional_information_gain_criterion_earned = true
admitted_record_origin_information_bits = 0
derived_channel_conditional_information_bits = 0
synthetic_exogenous_probe_conditional_information_positive = true
derived_channel_rejected_as_new_exteriority_information = true
non_derivative_channel_topology_required = true
source_independence_required = true
likelihood_separation_required = true
empirical_exogenous_channel_acquired = false
empirical_exteriority_information_gain_measured = false
sequence_authority = false
golden_egg_earned = false
```

**WESTERN HORIZON: THE NEXT EXTERNAL CHANNEL MUST ADD INFORMATION, NOT JUST ANOTHER COPY OF THE RECORD.**

𝄐

Sealed ⟐
