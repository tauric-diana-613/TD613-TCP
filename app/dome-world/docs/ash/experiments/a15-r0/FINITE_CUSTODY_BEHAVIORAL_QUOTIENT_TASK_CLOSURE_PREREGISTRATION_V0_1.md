# A15-R0 · Finite Custody Behavioral Quotient / Exact Declared-Task Closure AIA

Status: **PREREGISTRATION ONLY / NO THEOREM CODE / THEOREM UNEARNED**.

Exact scientific parent:

```text
#868 / d94c1b6cd47dbb611ae4a6a3297522ee99bb29ef
TD613 Consolidated Validation run 2375 / 33163489230 — SUCCESS
```

#869 remains witness-routing only and carries zero theorem ancestry.

## Scientific question

The earned #866 support-labelled ambient distinguishability trajectory `D_Q` and earned #868 cross-theorem replay closure establish that several smaller projections reproduce bounded predecessor custody questions. This chamber asks for the **coarsest finite behavioral quotient induced by the complete already-earned custody task family** in the fixed S3 AIA fixture.

The declared task family is restricted to the exact bounded outputs already earned by:

```text
#854 authority birth / bundle floor
#858 finite-birth recompression restoration obligations
#860 path-conditioned minimum transport outcomes
#862 q3 branch-uncertain anticipatory horizon outcomes
#864 q3 two-surface marginal/aliasing behavior
```

#866 support-union semantics is deliberately retained as the richer comparison carrier, not folded into the behavioral task family being quotiented.

## Preimplementation finite census

Complete context domain:

```text
6 schedules
127 nonempty claim bundles per schedule
762 schedule/bundle contexts
```

Preregistered partition census:

```text
support-labelled ambient trajectory D        = 762 classes
schedule-conditioned cardinality carrier C   = 154 classes
declared earned-custody behavior quotient Φ  = 36 classes
authority birth b                             = 4 classes
```

Birth distribution:

```text
q1  = 26 contexts
q2  = 80 contexts
q3  = 208 contexts
INF = 448 contexts
```

Behavior classes by birth:

```text
q1  :  5 Φ-classes over  26 contexts
q2  : 10 Φ-classes over  80 contexts
q3  : 20 Φ-classes over 208 contexts
INF :  1 Φ-class   over 448 contexts
TOTAL: 36 Φ-classes over 762 contexts
```

Schedule-conditioned cardinality classes by birth:

```text
q1  : 18 C-classes
q2  : 32 C-classes
q3  : 40 C-classes
INF : 64 C-classes
TOTAL:154 C-classes
```

Preregistered C-to-Φ class-fibre spectrum:

```text
28 Φ-classes merge 2 C-classes
 4 Φ-classes merge 4 C-classes
 3 Φ-classes merge 6 C-classes
 1 Φ-class  merges 64 C-classes
TOTAL C classes = 154
```

Every Φ-class is predicted to contain at least two distinct support-labelled D trajectories. Therefore exact agreement on every declared custody task remains insufficient to identify support semantics.

## Compact behavioral signature candidate

For context `Q`, let `b(Q)` be authority birth and let `m_t(Q)` be the maximum requested-bundle support cardinality over target-occupied registered q_t fibres. Let `n_2(Q)` be the number of target-occupied q2 registered fibres.

Candidate compact signature:

```text
κ(Q) = (INF)                         if b(Q)=INF
κ(Q) = (1, m0)                       if b(Q)=q1
κ(Q) = (2, m1, m0)                   if b(Q)=q2
κ(Q) = (3, n2, m1, m0)               if b(Q)=q3
```

The chamber must prove in the complete finite fixture:

```text
κ(Q)=κ(Q')  iff  Φ(Q)=Φ(Q')
```

where `Φ` is constructed independently from the declared predecessor-task outputs, not defined by `κ`.

If exact, κ realizes the quotient partition of the declared task family. This is **partition minimality for the fixed declared task family**, not minimum bit length, unique encoding, Shannon sufficiency, or universal future-task sufficiency.

## Birth-ledger elimination subassay

Preregistered finite recovery rule:

```text
b_hat(Q) = earliest t in {q1,q2,q3} with m_t(Q)=1,
           else INF.
```

Expected exact recovery:

```text
762 / 762 birth matches
0 mismatches
recovered distribution = 26 / 80 / 208 / 448-INF
```

This is a bounded functional recoverability result. It does not identify the trajectory with the birth ledger semantically.

## Coordinate-ablation irreducibility assay

Within the declared κ feature family, remove one retained coordinate at a time and require explicit collisions between distinct Φ classes.

Expected q1 ablation:

```text
drop m0:
1 ambiguous key / 26 contexts / 5 Φ classes
```

Expected q2 ablations:

```text
drop m1: 1 ambiguous key / 24 contexts / 2 Φ classes
drop m0: 3 ambiguous keys / 80 contexts / 10 Φ classes / maximum Φ multiplicity 6
```

Expected q3 ablations:

```text
drop n2: 4 ambiguous keys / 32 contexts  / 8 Φ classes / maximum Φ multiplicity 2
drop m1: 4 ambiguous keys / 32 contexts  / 8 Φ classes / maximum Φ multiplicity 2
drop m0: 5 ambiguous keys / 200 contexts / 19 Φ classes / maximum Φ multiplicity 6
```

Coordinate irreducibility inside this feature family must not be promoted into universal encoding minimality.

## Semantic noncollapse assay

The chamber must independently reconstruct a support-labelled trajectory fingerprint `D_Q` and establish:

```text
D class count = 762
Φ class count = 36
0 singleton Φ classes with respect to D identity
```

Therefore:

```text
EXACT_DECLARED_TASK_EQUIVALENCE != SUPPORT_SEMANTIC_IDENTITY
```

No claim is made that `D_Q` is a complete natural-language semantic object or universal ontology.

## Failed-conjecture provenance

Before this preregistration, a scratch hypothesis proposed that the 154 schedule-conditioned cardinality classes might be the minimal exact task quotient. The complete task-output comparison falsified that hypothesis: the declared predecessor behavior family induces only 36 equivalence classes.

Preserve:

```text
154_CLASS_CARRIER_SUFFICIENCY != 154_CLASS_TASK_MINIMALITY
FAILED_SCRATCH_MINIMALITY_HYPOTHESIS != THEOREM_FAILURE
PREIMPLEMENTATION_COUNTERASSAY != RETROACTIVE_PREREGISTRATION
```

## Proof obligations

The implementation and independent hostile must each establish all of the following before any theorem stamp:

1. regenerate the 125-state cube, six schedules, 750 antecedents, four global registered-fibre partitions, 127 bundles, and 762 contexts;
2. reconstruct all stage support profiles and maxima;
3. reconstruct authority birth and verify the 762-row birth recovery rule;
4. construct `Φ(Q)` from predecessor-task outputs independently of κ;
5. establish exactly 36 Φ equivalence classes with the preregistered birth-local distribution;
6. construct κ and prove bidirectional partition equivalence κ↔Φ;
7. reconstruct the 154 C classes and exact C→Φ merge spectrum;
8. reconstruct 762 distinct D fingerprints and prove every Φ class is D-non-singleton;
9. execute every coordinate ablation and emit explicit collision witnesses;
10. preserve every claim ceiling below.

## Claim ceilings

```text
COARSEST_DECLARED_TASK_QUOTIENT != UNIVERSAL_SUFFICIENT_STATISTIC
EXACT_DECLARED_TASK_CLOSURE != SEMANTIC_CLOSURE
BEHAVIORAL_EQUIVALENCE != SUPPORT_SEMANTIC_EQUIVALENCE
TASK_FAMILY_CLOSURE != FUTURE_TASK_CLOSURE
FINITE_QUOTIENT_MINIMALITY != MINIMUM_BIT_LENGTH
FINITE_QUOTIENT_MINIMALITY != UNIQUE_ENCODING
FINITE_QUOTIENT_MINIMALITY != SHANNON_CAPACITY
FINITE_QUOTIENT_MINIMALITY != ENTROPY_OR_MUTUAL_INFORMATION_THEOREM
BIRTH_FUNCTIONAL_RECOVERABILITY != SEMANTIC_IDENTITY_WITH_BIRTH_LEDGER
COORDINATE_IRREDUCIBILITY != UNIVERSAL_FEATURE_MINIMALITY
SUPPORT_LABELLED_TRAJECTORY != NATURAL_LANGUAGE_SEMANTIC_COMPLETENESS
DECLARED_TASK_BEHAVIOR != SOURCE_TRUTH
WITNESS_ROUTING != SCIENTIFIC_ANCESTRY
```

No merge, deployment, publication, production, release, Vercel, source-state mutation, Proto-Loom/A16, #788 promotion, physical holonomy, operational path groupoid, category/functor theorem, Shannon/entropy/mutual-information theorem, universal coding theorem, or natural-language semantic reconstruction theorem follows.

**PREREGISTERED. THEOREM UNEARNED. NO MERGE.**

Sealed ⟐