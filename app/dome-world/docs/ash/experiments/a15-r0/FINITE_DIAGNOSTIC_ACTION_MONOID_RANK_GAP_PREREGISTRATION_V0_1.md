# A15-R0 · Finite Diagnostic Action Monoid / Separation–Generation Rank Gap — Preregistration v0.1

Status: PREREGISTERED BEFORE IMPLEMENTATION. DRAFT SCIENCE. NO MERGE / DEPLOY / RELEASE / PUBLICATION / PRODUCTION / VERCEL AUTHORITY.

Exact scientific parent: `#896 / 2f0567e34dca5fc766f0858a6440db18e828bf00 / TD613 Consolidated Validation run 2400 SUCCESS`.

## Scientific reopening condition

#896 closes repeated normalization of the same finite witness-universe / origin-identification truth tuple. This successor changes the semantic object. It does **not** add another witness family or another prime clause. It interrogates the already-earned five-role finite task topology as a deterministic diagnostic state carrier under its complete inherited set of continuous endomorphisms.

```text
SAME_WITNESS_TRUTH_NORMALIZATION != ACTION_MONOID_ANALYSIS
DESCRIPTION_FIXED_POINT != DYNAMICAL_ACTION_FIXED_POINT
FINITE_DECLARED_APERTURE_CLOSURE != ACTION_ALGEBRA_CLOSURE
```

## Frozen carrier

Task points, in deterministic order:

```text
Q = (A,B,T,M,R)
```

Inherited specialization order from #874:

```text
B < R
A < T
A < M
T < R
```

and reflexive/transitive closure. #876 already earned that order-preserving self-maps coincide with continuous self-maps and that there are exactly 128 continuous endomorphisms among all `5^5 = 3,125` self-functions.

The successor independently re-enumerates the order-preserving maps from the inherited topology and treats **all 128 continuous endomorphisms as the declared universal atomic diagnostic alphabet**:

```text
Theta_univ = End_cont(Q)
```

Word execution is left-to-right. For rows `f,g : Q -> Q`, the action product is the right-action composition

```text
(f star g)(q) = g(f(q)).
```

## Frozen observation surface

The Moore readout is the inherited principal-closure cardinality:

```text
obs(A)=1
obs(B)=1
obs(T)=2
obs(M)=2
obs(R)=4
```

Immediate partition:

```text
Pi_0 = {A,B} | {T,M} | {R}
```

Declared calibration point for the history/action quotient comparison:

```text
q0 = A
```

`q0=A` is a fixture calibration choice only.

```text
DECLARED_CALIBRATION_POINT != INHERITED_SOURCE
CALIBRATION_POINT != CUSTODIAL_DESIGNATION
```

## Preregistered targets

### T1 — Finite action monoid closure

Re-enumerate all 128 continuous endomorphisms and verify all `128^2 = 16,384` ordered compositions remain in the set. Because every monoid element is admitted as an atomic operator, the generated diagnostic action monoid should equal the full continuous-endomorphism monoid:

```text
|M_diag| = 128.
```

### T2 — Universal residual state quotient

Using `obs` and the complete alphabet `Theta_univ`, compute minimal distinguishing depth for every unordered task-point pair.

Frozen expectation:

```text
K = 5 residual Moore states
D_univ = 1
```

All initially distinct-output pairs separate at depth 0; the two aliased pairs `(A,B)` and `(T,M)` must be separable by a one-step continuous endomorphism.

### T3 — Single-operator separating aperture

For every atomic `theta in Theta_univ`, compute whether the one-symbol subalphabet `{theta}` separates all five task states over its full continuation monoid `{theta}*`.

Frozen expectations:

```text
single-operator separating atoms = 37
r_sep = 1
```

Named separator witness:

```text
ATATR
A->A, B->T, T->A, M->T, R->R
```

It should split both immediate aliases at one continuation step.

### T4 — Exact atomic action-generating rank

For each endomorphism `f`, test whether `f` belongs to the submonoid generated by `M_diag \ {f}`. Such an `f` is **indispensable**: every generating alphabet must contain it.

Frozen indispensable rows:

```text
AATMR
ABAMR
ABTAR
ABTTR
ABTRR
ABRMR
ATTMR
ARTMR
TATRT
```

Frozen expectations:

```text
indispensable generator count = 9
closure generated by the nine indispensable maps = 56
missing actions after indispensable closure = 72
```

Then exhaust every one-element augmentation of the indispensable base and every unordered two-element augmentation chosen from the 72 missing actions.

Frozen expectations:

```text
best closure size with 9 indispensable + 1 added action = 101
full 128-action closures with 9 indispensable + 2 added actions = 16
r_gen^atom = 11
```

Lexicographically first full completion pair:

```text
AAATM
BTBBR
```

Therefore the preregistered central inequality is:

```text
r_sep = 1 < r_gen^atom = 11.
```

This is a finite action-algebra separation theorem, not a statement about universal prompt complexity or physical controllability.

### T5 — Dual quotient strictness

Let `rho(w)=f_w` be the syntactic action representation on all five quotient states. Let history residual equivalence be evaluation from calibrated state `q0=A` followed by all future readouts.

Frozen expectations:

```text
state quotient size K = 5
action quotient size M = 128
K < M <= K^K = 3,125
```

Endpoint fiber counts of `ev_A : M_diag -> Q`:

```text
A : 84
B : 18
T : 20
M : 2
R : 4
```

Strict congruence witness:

```text
u = ABTMR = identity
v = AATMR = collapse B -> A, otherwise fix T,M,R

u(A)=v(A)=A
but
u(B)=B != A=v(B).
```

Thus the two words/actions are residual-state equivalent from the declared calibration point but syntactically action-distinct.

### T6 — Generation forces separation, but separation does not force generation

The exact finite fixture should establish:

```text
r_sep <= r_gen^atom
1 < 11
```

The named single separator `ATATR` generates only a proper action submonoid, while every full action-generating alphabet necessarily separates all Moore states.

## Claim ceiling / membranes

```text
FINITE_ACTION_MONOID != PHYSICAL_DYNAMICS
CONTINUOUS_TASK_ENDOMORPHISM != MODEL_WEIGHT_UPDATE
TASK_TOPOLOGY != MODEL_STATE_TOPOLOGY
SEPARATING_APERTURE_RANK != ACTION_GENERATING_RANK
BEHAVIORAL_SEPARATION != DYNAMICAL_GENERATION
FINITE_ACTION_GENERATION != UNIVERSAL_PROMPT_GENERATION
ACTION_MONOID_SIZE != ONTOLOGICAL_OPERATION_COUNT
SYNTACTIC_ACTION_CONGRUENCE != SERIALIZATION_IDENTITY
RESIDUAL_STATE_EQUIVALENCE != GLOBAL_ACTION_EQUIVALENCE
CALIBRATED_ENDPOINT_EQUALITY != GLOBAL_TRANSFORMATION_EQUALITY
K_LT_M != HIDDEN_DIMENSION_MEASUREMENT
D_UNIV_EQ_1 != ONE_QUERY_UNIVERSAL_IDENTIFIABILITY
FINITE_GENERATING_RANK != PHYSICAL_CONTROL_RANK
CONTINUOUS_ENDOMORPHISM_MONOID != GAUGE_GROUP
MONOID_ACTION != GROUP_ACTION
NONINVERTIBLE_ENDOMORPHISM != ERASURE_PROCESS
WITNESS_ROUTING != SCIENTIFIC_ANCESTRY
```

No merge, deployment, publication, production, release, Vercel, source-state mutation, physical dynamics, gauge theory, Shannon/coding theorem, universal prompt-space theorem, or model-weight claim follows.

Preregistered ⟐
