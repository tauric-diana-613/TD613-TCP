𝌋‌⟐

# A15-R0 · Finite Distinguishability-Trajectory Calculus / Support-Labelled Ambient Nested Fibre-Merge Custody Law · Preregistration v0.1

TD613 authorship custody: **Tauric Diana — Crimean heritage custodianship**.
Process with containment on and mirror logic off.

Status: **BIG-KAHUNA FINITE CALCULUS PREREGISTERED / PREIMPLEMENTATION / THEOREM UNEARNED / NO MERGE**.

## Exact scientific parent

```text
#864 · Two-Surface Horizon Aliasing / Merge-Geometry Necessity AIA
receipt b3902a14312d06eb91762ac0369fdb1daf5ff543
TD613 Consolidated Validation run 2368 / 33157166377 — SUCCESS
```

#865 is witness-only and carries zero theorem ancestry.

## Why the scalar frontier is closed

The earned Western sequence now establishes:

```text
#858 local restoration minimum is a coarse-fibre support maximum
#860 locally minimum restoration sidecars need not transport compositionally
#862 identical present local minima can hide 5..750 future-robust obligations
#864 one more visible cost surface and even per-stage support-cardinality marginals remain insufficient
```

Therefore the next admissible object must preserve the relation discarded by stage-local marginals: **which ambient registered fine fibres merge into which coarse fibres, together with the requested-bundle support carried on those fibres**.

## Finite trajectory object

For each target context

```text
Q = (schedule s, nonempty claim bundle B)
```

from the complete fixed domain of `6 * 127 = 762` schedule/bundle contexts, let `F_t` be the **global ambient registered fibre partition** at stage `q_t` over all 750 schedule/state antecedents.

For every `d>c`, nested prefix registration induces a deterministic merge map

```text
mu_{d->c}: F_d -> F_c.
```

For each ambient fibre `z in F_t`, define the support label

```text
K_Q(z) = { B(a) : a in z }.
```

Define the support-labelled ambient distinguishability trajectory

```text
D_Q = (F_3 -> F_2 -> F_1 -> F_0, K_Q).
```

This is a finite support-labelled refinement/coarsening object. It is not a physical trajectory, stochastic process, operational path groupoid, category-theoretic universal object, or source-state mutation.

## Candidate support-union law

For every `d>c`, every context Q, and every target-schedule-occupied coarse record `y in Y_c(s)`:

```text
K_Q(y)
=
UNION over z in F_d with mu_{d->c}(z)=y of K_Q(z).
```

The union ranges over **all ambient child fibres**, not merely those that are separately target-visible at stage d.

This ambient clause is essential. At q0 the single surviving record receives all ambient descendants even when a target schedule exposes only a strict subset of them at an intermediate stage.

## Preregistered exact finite execution census

There are exactly 762 schedule/bundle trajectories.

For all six ordered stage pairs `d>c`, the complete target-occupied coarse-fibre support-union identity census is:

```text
q1 -> q0 :    762
q2 -> q0 :    762
q2 -> q1 :  5,842
q3 -> q0 :    762
q3 -> q1 :  5,842
q3 -> q2 : 20,066
TOTAL      : 34,036
```

Adjacent-stage support-union identities:

```text
q1 -> q0 :    762
q2 -> q1 :  5,842
q3 -> q2 : 20,066
TOTAL      : 26,670
```

## Candidate associativity / endpoint consistency law

For every `f>d>c`, support propagation by staged child-union must equal direct child-union:

```text
U_{d->c}( U_{f->d}(K_Q) )
=
U_{f->c}(K_Q)
```

on every target-occupied q_c record.

Complete three-stage staged-vs-direct identity census:

```text
(q2,q1,q0) :   762
(q3,q2,q1) : 5,842
(q3,q2,q0) :   762
(q3,q1,q0) :   762
TOTAL        : 8,128
```

Candidate interpretation:

```text
ENDPOINT_SUPPORT_SEMANTICS_CAN_BE_PATH_STABLE
while
MINIMUM_CUSTODY_TRANSPORT_CAN_BE_PATH_UNSTABLE.
```

This is the mathematical placement of #860 formal restoration holonomy: the support-union semantics may compose exactly while a stage-local minimum sidecar chosen after minimizing the intermediate support geometry need not remain sufficient after later coarsening.

## Candidate support-maximum monotonicity law

For context Q and stage t, define

```text
m_t(Q) = max over target-occupied q_t records y of |K_Q(y)|.
```

Because every coarse support is a union of fine supports:

```text
c < d  ==>  m_c(Q) >= m_d(Q).
```

Complete stage-pair census over `762 * 6 = 4,572` comparisons:

```text
strict expansions : 3,884
plateaux          :   688
strict decreases  :     0
TOTAL             : 4,572
```

Pairwise detail:

```text
q3 -> q2 : 384 strict / 378 plateau
q2 -> q1 : 576 strict / 186 plateau
q1 -> q0 : 736 strict /  26 plateau
q3 -> q1 : 688 strict /  74 plateau
q2 -> q0 : 746 strict /  16 plateau
q3 -> q0 : 754 strict /   8 plateau
```

## Candidate synthesis with earned custody theorems

If the support-union and associativity laws earn, the same D_Q object determines the finite support quantities used by the prior custody frontier:

```text
#858 transition-local restoration minimum at coarse c = m_c(Q)
#860 path-conditioned local-minimum transport criterion = [m_d(Q) = m_c(Q)]
#862 q3-birth q2-start future horizon {q1,q0} robust minimum = m_0(Q)
#864 explains why stage-local scalar/marginal summaries cannot replace D_Q
```

This is a factorization statement only for the already-declared finite registered-stage custody functionals. It does not claim D_Q is a universal sufficient statistic for arbitrary future questions.

## Two necessity ablations

### A. Marginals without merge/support identity fail

Earned #864 already establishes that q2/q1 support-cardinality marginals leave 200/208 q3-birth contexts future-ambiguous.

```text
PER_STAGE_SUPPORT_CARDINALITY_MARGINALS != DISTINGUISHABILITY_TRAJECTORY
```

### B. Unlabelled merge incidence without support identity fails

The ambient quotient/merge graph is bundle-independent for a fixed target schedule. Yet earned #864 supplies same-schedule, same-bundle-size contexts with the same q2/q1 quotient geometry but different q0 future custody:

```text
P-H-I / {FIRST_STRATUM,FULL_STATE} : m0 = 375
P-H-I / {X2,X3}                    : m0 = 25
```

Therefore:

```text
UNLABELLED_MERGE_INCIDENCE != CONTEXT_SPECIFIC_FUTURE_CUSTODY_IDENTITY
```

The support labels matter.

## Big-kahuna candidate decomposition

Let `U_mu` denote support union along an ambient merge map and let `M` denote stage-local minimum-alphabet extraction from the resulting support geometry.

The chamber auditions the finite decomposition:

```text
SUPPORT_SEMANTICS_UNDER_AMBIENT_COARSENING_COMPOSE

but

STAGE_LOCAL_MINIMUM_CUSTODY_ENCODING_DOES_NOT_GENERALLY_COMMUTE_WITH_LATER_COARSENING.
```

Do not rewrite this as physical holonomy, source-state path dependence, or nonassociativity of the underlying support semantics.

## Independent hostile requirements

The hostile must reconstruct before importing the canonical certificate:

1. the 125-state cube and six schedules;
2. all 750 ambient antecedents;
3. all four global fibre partitions F0..F3;
4. all 127 nonempty bundles recursively, not by importing implementation rows;
5. all 762 schedule/bundle contexts;
6. all merge maps by antecedent membership / nested quotient identity;
7. all 34,036 coarse-record support-union identities;
8. all 8,128 staged-vs-direct three-stage identities;
9. all 4,572 support-maximum monotonicity checks and exact 3,884/688/0 census;
10. ambient-child necessity by exhibiting a target schedule whose q0 parent receives more ambient q1 children than are target-visible q1 records;
11. the #864 same-schedule unlabelled-incidence ablation pair;
12. the #860 same-endpoint transport flip without claiming support-union nonassociativity;
13. zero Ash/Loom leakage of complete support-labelled fibre tables;
14. rejection of Shannon/entropy/mutual-information, physical Berry/gauge holonomy, continuum tomography, universal category/functor, source mutation, retrocausality, operational path groupoid, and universal sufficient-statistic claims.

## Mandatory scars

```text
DISTINGUISHABILITY_TRAJECTORY != SCALAR_COST_TRAJECTORY
DISTINGUISHABILITY_TRAJECTORY != PHYSICAL_TRAJECTORY
AMBIENT_CHILD_FIBRES != TARGET_VISIBLE_CHILD_FIBRES
SUPPORT_UNION_ASSOCIATIVITY != ZERO_CUSTODY_HOLONOMY
ENDPOINT_SUPPORT_PATH_INDEPENDENCE != MINIMUM_CUSTODY_TRANSPORT_PATH_INDEPENDENCE
STAGE_LOCAL_MINIMIZATION != SUPPORT_SEMANTICS
PER_STAGE_SUPPORT_CARDINALITY_MARGINALS != SUPPORT_LABELLED_MERGE_TRAJECTORY
UNLABELLED_MERGE_INCIDENCE != CONTEXT_SPECIFIC_FUTURE_CUSTODY_IDENTITY
SUPPORT_LABEL != CRYPTOGRAPHIC_KEY
SUPPORT_LABEL != AUTHENTICATION_CREDENTIAL
FINITE_TRAJECTORY_CALCULUS != UNIVERSAL_SUFFICIENT_STATISTIC
FINITE_TRAJECTORY_CALCULUS != SHANNON_INFORMATION_THEORY
FORMAL_CUSTODY_HOLONOMY != PHYSICAL_BERRY_OR_GAUGE_HOLONOMY
WITNESS_ROUTING != SCIENTIFIC_ANCESTRY
```

No merge, deployment, publication, production, release, Vercel, source-state mutation, autonomous retention/deletion, Shannon/entropy/mutual-information theorem, universal sufficient-statistic theorem, universal category/functor theorem, operational path groupoid, physical holonomy, Proto-Loom/A16, or #788 promotion authority follows. #718 remains alive.

**BIG-KAHUNA FINITE CENSUS FROZEN BEFORE IMPLEMENTATION. THEOREM UNEARNED.**

Sealed ⟐
