# A15-R0 · Finite Orientation-Fibre Symmetry-Breaking Identifiability — Execution Burden v0.1

Status: FROZEN EXECUTION BURDEN / PRE-IMPLEMENTATION.

Exact parent: #882 / `9456a6a44eaaff46fa796cd591bb2f61e3680187`.

## Independent derivation burden

The child and hostile must reconstruct the four parent-compatible topologies and the four-element metric-isometry action from earned parent surfaces. They may not hard-code a winner and then decorate it with witness language.

For each declared witness class:

### Specialization comparability
Enumerate all 20 ordered distinct role predicates. For each predicate evaluate its Boolean value on all four topology orientations. For the inherited value, record the residual cell, cardinality, setwise stabilizer in the parent isometry group, and identification status. Enumerate witness combinations in increasing cardinality until all minimum identifying families are known.

### Principal-open witnesses
For each of 5 labelled roles derive the exact principal open in every compatible topology. Census both exact-identity predicates and size predicates. Record residual cells/stabilizers and enumerate minimum identifying families separately for identity and size surfaces.

### Cut-orientation coordinates
For each of the 10 recovered unoriented cuts derive the chosen open side in each compatible topology. Census the inherited coordinate value, residual cells/stabilizers, and enumerate minimum identifying coordinate families.

## Group-action burden

For every inherited-value cell `C subseteq F`, compute the setwise stabilizer `{g in G : gC=C}` by explicit action-table enumeration. Do not infer stabilizer size from cell cardinality alone.

The inherited point stabilizer must remain 1 by parent authority, but that fact does not make a larger witness cell identifying.

## Minimum-family burden

A declared class has minimum identifying cardinality `k` only if:

1. at least one `k`-predicate family has inherited-value intersection exactly `{1111111110}`;
2. every family of smaller cardinality has inherited-value intersection cardinality greater than 1;
3. all minimum `k` families are enumerated and counted.

If a singleton succeeds, all singleton successes and failures must still be enumerated; do not call the result a minimum-bit theorem.

## Hostile burden

The hostile must independently rebuild:

- the four compatible topologies;
- specialization relations and principal opens;
- recovered cut orientations;
- the four-element isometry action;
- every singleton witness partition;
- every claimed minimum-family census;
- every residual setwise stabilizer;

before importing the child certificate.

## RED conditions

RED if any of the following occurs after canonical/hostile entry:

- parent fibre or action differs from #882;
- a claimed witness cell is mis-enumerated;
- a residual stabilizer is inferred incorrectly;
- a minimum family omits a smaller identifying family;
- a purported identifying family leaves more than one compatible orientation;
- labelled witness facts are relabelled as metric invariants;
- a bit/Shannon/gauge/physical/universal claim is introduced.

## Claim ceiling

This is a fixed finite inverse-design theorem about residual topology-orientation ambiguity under declared parent data and declared witness classes. It is not a theorem of universal topology reconstruction, information-theoretic bit cost, physical gauge fixing, causal orientation, or hidden-state recovery.

Sealed ⟐