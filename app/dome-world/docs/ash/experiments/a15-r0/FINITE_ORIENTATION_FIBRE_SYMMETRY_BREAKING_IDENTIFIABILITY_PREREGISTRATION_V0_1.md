# A15-R0 · Finite Orientation-Fibre Symmetry-Breaking Identifiability — Preregistration v0.1

Status: PREREGISTRATION ONLY / NO THEOREM AUTHORITY / NO MERGE.

## Exact scientific parent

#882 / `9456a6a44eaaff46fa796cd591bb2f61e3680187` — EARNED, exact-head TD613 Consolidated Validation run 2389 / 33285941688 SUCCESS.

Witness #883 is routing only and carries zero theorem ancestry.

## Parent-frozen object

The parent earns a four-element fibre `F` of compatible finite topological orientations of the uniquely recovered ten-distinct-unit unoriented cut skeleton:

- `0000000001`
- `0000000010`
- `1111111101`
- `1111111110` (inherited)

The parent also earns the metric-isometry group

`G={id,(B M),(A R),(A R)(B M)}`

acting freely and transitively on `F`.

## Question

Given the parent metric, inherited ten-distinct-unit probe prior, recovered unoriented cut skeleton, and the four-point compatible topology fibre, what additional finite witness predicates break the residual `G`-symmetry enough to identify the inherited orientation?

This chamber distinguishes labelled naming from structural witness information. A predicate may mention the fixed task-role labels because those labels are already present in the earned parent fixture, but the theorem must report its stabilizer/residual fibre rather than calling a label a metric invariant.

## Frozen witness classes

The implementation MUST exhaustively derive and census at least these three finite classes from the four parent-compatible topologies:

1. specialization comparability predicates `x<y` for ordered distinct roles;
2. principal-open predicates, including exact principal-open identity and size for each labelled role;
3. recovered-cut orientation coordinates for each of the ten parent-earned unoriented cuts.

For every predicate/value observed at the inherited topology, compute:

- surviving compatible orientations;
- residual fibre cardinality;
- setwise stabilizer subgroup inside `G`;
- point stabilizer of the inherited orientation (parent-fixed as trivial);
- whether the witness uniquely identifies the inherited orientation.

For finite witness families, enumerate combinations within each declared class until the minimum cardinality that uniquely selects the inherited orientation is proved, or exhaust the class if no family succeeds.

## Mandatory controls

- Empty witness family leaves all 4 orientations.
- Metric-only data leaves all 4 orientations by #882.
- A witness that is constant across the fibre MUST leave all 4.
- A witness whose inherited-value cell has size 2 MUST NOT be reported as identifying.
- A singleton inherited-value cell identifies the inherited orientation only relative to this fixed four-topology fibre and declared parent labels/prior.
- Any claimed minimum must be a finite census minimum in the declared witness class, not a bit-length, Shannon, coding, or universal inverse-problem minimum.

## Candidate theorem shape — NOT YET EARNED

The desired result is a complete symmetry-breaking spectrum, not a predetermined claim that one witness suffices. The implementation must accept whichever of the following the census yields:

- some single structural predicate selects the inherited orientation;
- every single predicate leaves residual symmetry but a minimum family of size `k>1` selects it;
- a declared witness class cannot select it at any finite family size.

If single predicates succeed, the theorem must still report which single predicates fail, their residual fibre sizes/stabilizers, and the complete minimum-singleton census rather than collapsing the result to “one bit”.

## Mandatory scars

`METRIC_ORBIT != TOPOLOGY_IDENTITY`

`FREE_TRANSITIVE_ORIENTATION_ACTION != PHYSICAL_GAUGE_SYMMETRY`

`SYMMETRY_BREAKING_WITNESS != METRIC_INVARIANT`

`LABELLED_WITNESS != LABEL_FREE_INTRINSIC_INVARIANT`

`SINGLETON_FIBRE_SELECTION != UNIVERSAL_TOPOLOGY_RECONSTRUCTION`

`FINITE_WITNESS_CARDINALITY_MINIMUM != MINIMUM_BIT_LENGTH`

`FINITE_WITNESS_CARDINALITY_MINIMUM != SHANNON_INFORMATION`

`CUT_ORIENTATION_COORDINATE != PHYSICAL_ORIENTATION`

`SPECIALIZATION_COMPARABILITY != CAUSAL_ORDER`

`PRINCIPAL_OPEN != PHYSICAL_NEIGHBORHOOD`

`ORIENTATION_FIBRE != HIDDEN_STATE SPACE`

`WITNESS_ROUTING != SCIENTIFIC_ANCESTRY`

No merge, deployment, publication, production, release, Vercel, source-state mutation, Proto-Loom/A16, #788 promotion, physical gauge theory, physical geometry, continuum topology, Shannon/channel coding, or universal inverse-problem theorem follows.

Sealed ⟐