𝌋‌⟐

# A15-R0 · Finite Prime-Dual Fixed-Point Rest Normalizer · Preregistration v0.1

## Custody

Exact earned parent: **#894 / `961d6eae8491ca1c72da23c5f23c2b573dc8e8ce` / TD613 Consolidated Validation run 2399 SUCCESS**.

This chamber is downstream only of the earned #894 declared-aperture closure lineage. Failed #890/#891 are quarantined and contribute no scientific ancestry.

No merge, deployment, publication, release, Vercel, A16/Proto-Loom, physical gauge theory, Shannon/channel theorem, or universal inverse-problem claim is authorized.

## Fixed scientific tuple

For each declared finite witness class hold fixed:

```text
(orientation fibre,
 metric-isometry action,
 inherited custody point tau_*,
 declared finite witness universe U,
 pointed origin-identification predicate I)
```

No theorem in this chamber survives a change to that tuple without a new experiment.

## Pre-registered construction

For a finite upward-monotone predicate `I: 2^U -> {0,1}`, define the prime-dual normalizer

```text
M(I) = inclusion-minimal W subseteq U with I(W)=1
Fmax(I) = inclusion-maximal W subseteq U with I(W)=0
E(I) = { U \ F : F in Fmax(I) }
N(I) = (M(I), E(I))
```

and the two reconstruction maps

```text
R_DNF(M)(W) = OR_{M0 in M} [M0 subseteq W]
R_CNF(E)(W) = AND_{E0 in E} [W intersects E0].
```

#894 already earned that its inherited minimal-success and minimal-obstruction antichains reconstruct the complete declared truth surface. This chamber does **not** re-label that sufficiency as a new theorem.

## Candidate terminal theorem

The new target is **canonical fixed-point closure**:

```text
N(I) = (M_parent, E_parent)
R_DNF(M(I)) = I = R_CNF(E(I))
N(R_DNF(M(I))) = N(I)
N(R_CNF(E(I))) = N(I)
```

extensionally, on every declared finite class.

Equivalently, repeated prime-dual normalization cannot create a new prime success term or prime obstruction clause under the fixed tuple.

The intended abstract consequence, if the finite certificate passes, is:

```text
R o N = Id
N o R o N = N
```

on the declared finite monotone origin-identification predicates, with `R` represented by either prime normal form.

## Canonicality claim

If earned, `M(I)` is the unique antichain of minimal true points and `E(I)` is the unique antichain obtained as complements of maximal false points. Therefore any exact monotone success DNF or obstruction CNF for the same `I` reduces by absorption to the same prime antichains.

This is stronger than mere irredundancy: the certificate is not one convenient exact representation among several prime choices; its prime antichains are determined by the truth surface itself.

## Required finite falsifiers

The chamber fails if **any** declared class exhibits any of the following:

1. a directly extracted minimal true family absent from the earned parent blocker;
2. an earned parent blocker absent from the directly extracted minimal true families;
3. a complement of a directly extracted maximal false family absent from the earned minimal obstruction clutter;
4. an earned minimal obstruction edge absent from the directly extracted maximal-false complements;
5. DNF reconstruction mismatch against direct transport truth;
6. CNF reconstruction mismatch against direct transport truth;
7. second normalization changes either prime antichain;
8. hostile reconstruction disagrees with the canonical child on any class ledger or prime family.

## Pre-registered exact finite expectations

Declared universes and first-normalization prime counts:

```text
specialization comparability : |U|=20, |2^U|=1,048,576, |M|=22, |E|=3
principal-open identity      : |U|=5,  |2^U|=32,        |M|=4,  |E|=2
principal-open size          : |U|=5,  |2^U|=32,        |M|=4,  |E|=2
cut orientation              : |U|=10, |2^U|=1,024,     |M|=16, |E|=2
TOTAL                         : families=1,049,664, |M|=46, |E|=9
```

These expectations are frozen before implementation.

## Mandatory membranes

```text
CANONICAL_PRIME_DUAL_FIXED_POINT != UNIVERSAL_SCIENTIFIC_COMPLETENESS
NORMAL_FORM_CANONICALITY != COMPLETE_WITNESS_SEMANTICS
IDEMPOTENT_NORMALIZATION != PHYSICAL_EQUILIBRIUM
FIXED_POINT_OF_DESCRIPTION != FIXED_POINT_OF_DYNAMICS
MONOTONE_BOOLEAN_PRIME != SHANNON_INFORMATION_UNIT
PRIME_ANTICHAIN_UNIQUENESS != MINIMUM_BIT_LENGTH
BLOCKER_DUALITY != TRANSPORT_LABEL_RECOVERY
STRUCTURAL_IDENTIFICATION != CUSTODIAL_DESIGNATION
IDENTIFIABILITY != INHERITANCE
REFINED_IDENTIFIABILITY != ANTERIOR_IDENTIFIABILITY
WITNESS_ROUTING != SCIENTIFIC_ANCESTRY
```

If the exact-head witness is green, this is the terminal same-tuple theorem. If red, rest remains at earned #894 and this chamber receives no authority.

Sealed ⟐