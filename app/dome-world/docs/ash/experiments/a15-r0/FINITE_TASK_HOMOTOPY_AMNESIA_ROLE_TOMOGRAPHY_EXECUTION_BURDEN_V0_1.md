# A15-R0 · Finite Task Homotopy-Amnesia / Role-Tomography Execution Burden v0.1

Status: PREREGISTERED BEFORE THEOREM IMPLEMENTATION.

Exact scientific parent:

```text
#874 / 7c4cef95d4f704f05615d663e252d5a53775bdbe
TD613 Consolidated Validation run 2384 / 33278954289 — SUCCESS
```

This successor is a complete finite census. Sampling is forbidden.

## Parent reconstruction burden

Reconstruct from #874's earned certificate:

```text
5 task points
12 open sets
12 closed sets
5 principal closures
5 minimal open neighborhoods
specialization covers B<R, A<T, A<M, T<R
```

Do not hard-code child outcomes from preregistration.

## Local role-tomography burden

```text
5 principal-closure cardinalities
5 minimal-open cardinalities
5 joint fingerprints
2 scalar role partitions
1 joint five-role injectivity test
```

Both scalar projections must be shown lossy before their pair may be called role-identifying in the fixed fixture.

## Complete self-function burden

```text
5^5 = 3,125 self-functions
```

For every function:

```text
25 ordered point-pair order-preservation opportunities
12 open-set inverse-image continuity checks
```

Required exact agreement:

```text
3,125 order-vs-continuity classifications
0 mismatches
128 admitted continuous endomaps
```

The implementation must separately classify image size, bijectivity, and idempotence.

## Continuous-endomap graph burden

Among 128 continuous endomaps:

```text
C(128,2)=8,128 unordered map pairs
5 pointwise comparisons per direction as needed
1,528 expected comparability edges
128 breadth-first shortest-path searches
8,128 unordered shortest-path distances
```

Required graph outputs:

```text
1 connected component
diameter 3
distance spectrum 1,528 / 5,435 / 1,165
identity distance spectrum 1 / 6 / 49 / 72
five identity-to-constant distances
```

## Beat-collapse burden

At every reachable induced subspace, beat status must be recomputed dynamically from the current strict upper/lower sets.

Expected complete recursion:

```text
19 distinct reachable induced subspaces
36 complete deletion sequences
all 5 singleton endpoints reached
```

Every deletion step must carry an explicit beat witness: upper minimum or lower maximum in the current subspace.

## Order-complex / F2 homology burden

Full-space strict chains:

```text
5 vertices
5 comparable-pair edges
1 strict three-chain simplex
```

For the full space and each of five single-point deletions:

```text
construct all strict chains
construct F2 simplicial boundary matrices
row-reduce boundary matrices over F2
compute Betti ranks independently from f-vector arithmetic
```

Expected deletion role partitions:

```text
f-vector : 3 classes
F2 Betti : 2 classes
```

## Aperture-ladder burden

The implementation must explicitly construct the equivalence partitions induced by:

```text
joint local fingerprint
single-deletion f-vector
single-deletion F2 Betti profile
global homotopy/homology aperture
```

and verify exact class counts:

```text
5 -> 3 -> 2 -> 1
```

No numeric class count may be substituted for partition identity.

## Independent hostile requirements

Before consulting the child certificate, the hostile must independently rebuild #874's five-point topology and attack at least:

1. any self-function where order preservation and open-preimage continuity disagree;
2. any second bijective continuous self-map beyond identity;
3. any disconnected continuous-endomap comparability graph;
4. any wrong shortest-path spectrum;
5. any invalid beat deletion or omitted beat sequence;
6. any claim that beat collapse selects a unique structural role;
7. any F2 boundary-rank or Betti mismatch;
8. any false injectivity of either local scalar aperture alone;
9. any failure of the joint local fingerprint to recover all five inherited roles;
10. any mismatch in the exact 5→3→2→1 partition ladder;
11. any inflation of finite combinatorial topology into physical geometry, information entropy, semantic identity, natural-language reconstruction, or universal homotopy/dynamics.

## Authority membrane

No merge, deployment, publication, production, release, Vercel, source-state mutation, Proto-Loom/A16, #788 promotion, Shannon/entropy/mutual-information theorem, category/functor theorem, physical topology, continuum topology, physical homotopy, or natural-language semantic reconstruction theorem follows.

Sealed ⟐