# A15-R0 · Finite Topological Distinguishability Metric / Non-Liftable Isometry Amnesia · Execution Burden

Status: **BURDEN FROZEN BEFORE IMPLEMENTATION / THEOREM UNEARNED**.

Exact scientific parent:

```text
#878 / 4ba3542aea8784586562032c57096248dc961db9
TD613 Consolidated Validation run 2387 / 33282971967 — SUCCESS
```

Preregistration receipts preceding this file:

```text
human preregistration 48ebb72b0e5be822bfdee9e7e067c8e6837cda96
machine expectations  9555205ec1e1933c25ebfac88aaa1a8a6d767a49
```

No theorem implementation may precede this burden.

## Required implementation burden

The implementation must derive the five-role task topology and ten-probe universe from inherited earned parents rather than instantiate a free-standing metric toy model.

It must:

1. import the earned #878 probe-separation certificate and the earned topology parent used by #878;
2. verify exact parent receipt `4ba3542aea8784586562032c57096248dc961db9`;
3. reconstruct the inherited ten nontrivial open-set probes and compare the universe setwise to the preregistered presentation order;
4. enumerate all `2^10=1,024` selected probe families;
5. derive all five membership signatures for every family;
6. derive all ten unordered pair separator counts for every family;
7. build `d_F` from those separator counts;
8. directly verify pseudometric axioms for every family without assuming injectivity;
9. prove family-by-family that exact role identification, signature injectivity, and positive pairwise distance are equivalent;
10. enumerate all `5!=120` role permutations for every exact family and derive each metric-isometry group size;
11. independently derive selected-family labelled-incidence automorphisms by acting on role membership subsets and requiring selected-family preservation;
12. derive the complete metric-isometry size spectrum and the complete joint `(metric isometries, incidence automorphisms)` spectrum;
13. reconstruct the full ten-probe distance matrix, point-distance profiles, triangle slack spectrum, diameter, minimum positive distance, and all full metric isometries;
14. reconstruct the full ten-probe labelled-incidence automorphism group and prove the three nonidentity metric isometries do not lift;
15. locate the unique 24-isometry exact family and derive its signatures, star metric, 24 metric isometries, four incidence automorphisms, and 20 non-liftable metric isometries;
16. independently replay all `46,464` exact erasure deletions for `e=0..4` and compare direct metric survival against `min d_F >= e+1` for all `4,876` family/order comparisons;
17. preserve inherited zero-authority membranes and expose no operational mutation authority.

## Frozen exact targets

```text
roles = 5
nontrivial probes = 10
probe families = 1,024
exact metric families = 795
role permutations per exact family = 120
```

Full distance matrix:

```text
        A  B  T  M  R
A       0  5  4  5  8
B       5  0  5  6  5
T       4  5  0  5  4
M       5  6  5  0  5
R       8  5  4  5  0
```

Full metric:

```text
min positive distance = 4
diameter = 8
metric isometries = 4
labelled-incidence automorphisms = 1
non-liftable metric isometries = 3
```

Metric-isometry spectrum over the 795 exact families:

```text
1 -> 372
2 -> 360
4 -> 40
6 -> 10
8 -> 8
12 -> 4
24 -> 1
```

Joint spectrum:

```text
(1,1)   372
(2,1)   192
(2,2)   168
(4,1)     9
(4,2)    21
(4,4)    10
(6,2)     2
(6,6)     8
(8,1)     2
(8,2)     5
(8,4)     1
(12,6)    4
(24,4)    1
```

Derived:

```text
558 exact families have metric-isometry count equal to incidence-automorphism count
237 exact families have extra non-liftable metric symmetries
```

Maximum-metric-symmetry control:

```text
F_star={RTAM,BRTM,M,R}
A 1000
B 0100
T 1100
M 1110
R 1101
metric group size 24
incidence group size 4
non-liftable metric symmetries 20
T center / all center-leaf distances 1 / all leaf-leaf distances 2
```

Triangle burden:

```text
60 ordered triples of distinct roles
slack spectrum 0:2 / 2:4 / 4:22 / 6:20 / 8:12
zero-slack triples exactly A-T-R and R-T-A
```

Erasure burden:

```text
46,464 exact deletion cases
4,876 family/order comparisons
0 metric-criterion mismatches
```

## Independent hostile burden

The hostile may import the earned parent topology/certificates and completed child certificate only for final comparison. Before reading child-derived metric census fields it must independently reconstruct:

- the ten-probe universe setwise;
- all 1,024 probe families;
- all membership signatures;
- all pair separator counts and all family pseudometrics;
- all 795 exact metric families;
- all 95,400 exact-family role-permutation metric-isometry checks;
- all selected-family labelled-incidence automorphism checks;
- the full distance matrix and 120 full-family permutation checks;
- all point-distance profiles and triangle slacks;
- the complete metric-isometry and metric/incidence joint spectra;
- the unique 24-isometry star control;
- all 46,464 exact erasure deletion cases and 4,876 direct-vs-distance criterion comparisons.

The hostile must fail on any mismatch before acknowledging the child `passed` flag.

## No-fit rule

After implementation begins, do not change:

- the probe universe;
- the role universe;
- the metric definition;
- the exact-family definition;
- any matrix entry or spectrum count;
- the 237/558 lift totals;
- the `F_star` identity or its signatures;
- the erasure semantics;
- the claim ceiling;
- the topology/incidence/metric distinction.

A theorem mismatch is a red specimen. It is not authorization to edit preregistration.

## Hardening requirements

Final hardening must prove:

```text
exact parent = #878 / 4ba3542aea8784586562032c57096248dc961db9
merge base = exact parent
successor commit count = actual frozen chronology
exact declared live paths only
zero inherited A15-R0 theorem-source mutation
```

Any legitimate pre-hostile interface or canonicalization repair must be preserved in chronology rather than rewritten.

## Claim ceiling

If all targets survive, the theorem remains bounded to the fixed five-role / ten-probe finite task topology plus the elementary finite pseudometric identity proved by construction.

It does not establish Shannon capacity, channel coding optimality, probabilistic reliability, physical geometry, continuum topology, semantic identity, universal task identity, or source-state reconstruction.

## Mandatory scars

```text
SEPARATOR_COUNT_METRIC != PHYSICAL_GEOMETRY
FINITE_HAMMING_FORM != CHANNEL_CODING_THEOREM
PAIRWISE_DISTANCE_MATRIX != LABELLED_PROBE_INCIDENCE
METRIC_ISOMETRY != TOPOLOGICAL_AUTOMORPHISM
METRIC_ISOMETRY != SEMANTIC_ROLE_IDENTITY
EXACT_DISTINGUISHABILITY != STRUCTURAL_IDENTITY_RECOVERY
DISTANCE_PROFILE_ALIASING != ROLE_IDENTITY
NON_LIFTABLE_METRIC_SYMMETRY != HIDDEN_PHYSICAL_SYMMETRY
PSEUDOMETRIC_COLLAPSE != SOURCE_STATE_COLLAPSE
MINIMUM_SEPARATOR_DISTANCE != SHANNON_CAPACITY
MINIMUM_SEPARATOR_DISTANCE != MINIMUM_BIT_LENGTH
FINITE_ERASURE_METRIC_EQUIVALENCE != ERROR_CORRECTION_CAPACITY
LABELLED_INCIDENCE_AUTOMORPHISM != SCIENTIFIC_ANCESTRY
WITNESS_ROUTING != SCIENTIFIC_ANCESTRY
```

No merge. No deployment. No publication. No production. No release. No Vercel. No source-state mutation. No Proto-Loom/A16. No #788 promotion.

**BURDEN FROZEN. THEOREM UNEARNED.**

Sealed ⟐