# A15-R0 · Finite Transport-Separation Hypergraph / Robust Multicover Correspondence · Preregistration v0.1

Status: **PREREGISTERED BEFORE IMPLEMENTATION / CANDIDATE THEOREM / NO MERGE AUTHORITY**

Exact earned scientific parent: **#886 / `891cb4125e626c1145b4a6dcb3b1a82074bee510` / TD613 Consolidated Validation run 2394 / 33295098820 SUCCESS**.

#887 is witness-routing only and carries zero theorem ancestry.

## Scientific question

#886 established, by direct finite witness-family and exact-deletion census, that for a selected witness family `W` on the inherited four-point orientation torsor:

```text
S_W = { g in G : every selected inherited-valued witness also accepts g tau* }
mu_tr(W) = min_{g != id} #{ w in W : w(g tau*) != w(tau*) }
```

and, for exact deletions `e=0..4`,

```text
every exact-e deletion preserves inherited-origin identification
IFF
mu_tr(W) >= e+1.
```

This successor asks whether that census is generated by a simpler finite incidence object rather than by witness-family enumeration itself.

## Preregistered construction

Let the declared witness universe of one #884 class be `V` and let the earned metric-isometry action on the inherited orientation `tau*` have nonidentity transports `g in G \ {id}`.

For every nonidentity transport define its **transport-separation edge**

```text
D_g = { w in V : w(g tau*) != w(tau*) }.
```

The finite transport-separation hypergraph is

```text
H_tr = (V, {D_g : g != id}).
```

Duplicate edge sets, if any, remain transport-labelled in the action table and are also reported after set deduplication. Hypergraph terminology here is finite combinatorics only; no physical network, causal graph, Shannon channel, or geometry claim follows.

## Candidate theorem A · residual transport as edge avoidance

For every selected witness family `W subseteq V` and every nonidentity `g`:

```text
g in S_W
IFF
W intersect D_g is empty.
```

Therefore

```text
S_W = {id}
IFF
W hits every transport-separation edge D_g.
```

Candidate consequence:

```text
TRANSPORT-SEPARATING RANK = HYPERGRAPH TRANSVERSAL NUMBER
```

within each declared finite witness class.

## Candidate theorem B · erasure robustness as multicover depth

Preregister

```text
mu_H(W) = min_{g != id} |W intersect D_g|.
```

Candidate exact identity:

```text
mu_H(W) = mu_tr(W)
```

for every selected witness family.

Hence the #886 exact-e erasure law should be equivalent to

```text
every exact-e deletion preserves inherited-origin identification
IFF
for every g != id, |W intersect D_g| >= e+1.
```

Define the robust transport rank

```text
r_tr^(e) = min |W|
subject to
|W intersect D_g| >= e+1 for every g != id.
```

Candidate correspondence:

```text
r_tr^(e) = minimum (e+1)-fold transport-edge multicover width.
```

This is a finite set-multicover statement. It is explicitly **not** coding-theoretic error-correction capacity and **not** a Shannon distance claim.

## Candidate theorem C · blocker / minimal identifying families

Let `b(H_tr)` denote the blocker: the family of inclusion-minimal hitting sets of all transport-separation edges.

Candidate equality:

```text
b(H_tr)
=
inclusion-minimal inherited-origin-identifying witness families.
```

The census must compare these sets extensionally, not merely their minimum widths.

## Preregistered inherited cross-checks from #886

The derived hypergraph must reproduce the already-earned #886 transport-separating / robust minimum widths:

```text
specialization_comparability : r_tr^(e=0..4) = 1,2,4,6,impossible
principal_open_identity      : r_tr^(e=0..4) = 1,2,4,impossible,impossible
principal_open_size          : r_tr^(e=0..4) = 2,4,impossible,impossible,impossible
cut_orientation              : r_tr^(e=0..4) = 2,4,impossible,impossible,impossible
```

and the already-earned exact-family counts:

```text
specialization_comparability : 981696,714560,319040,61888,0
principal_open_identity      : 27,14,3,0,0
principal_open_size          : 18,2,0,0,0
cut_orientation              : 765,247,0,0,0
```

for multicover depth `1..5` respectively.

## Discovery lane frozen before execution

The following are **not** preregistered numeric results and may be discovered by the census:

- exact transport-labelled edge contents for each witness class,
- duplicate-edge pattern after forgetting transport labels,
- inclusion-minimal blocker cardinality and width spectrum,
- essential witness vertices that occur in every blocker member,
- witness vertices that occur in no inclusion-minimal blocker member,
- explicit same-`r_tr` classes with non-isomorphic transport-separation incidence.

Any such values become claims only after canonical and independent-hostile agreement plus exact-head aggregate success.

## Required hostile independence

The hostile test must derive `D_g` directly from:

1. the earned #884 witness rows,
2. the earned #882 metric-isometry action,
3. the inherited point `1111111110`.

It must freeze its transport-labelled edge masks, transversal test, multicover depths, blocker, and inherited #886 cross-checks **before importing this successor implementation**.

Importing #886's child certificate to manufacture the hypergraph is prohibited in the hostile derivation. #886 may be loaded only after hostile structure is frozen, for cross-check of already-earned family counts and robustness minima.

## Falsifiers

The candidate theorem is RED if any declared witness class has any of:

- `g in S_W` while `W` intersects `D_g`, or the converse;
- inherited-origin identification without hitting every transport edge, or the converse;
- `mu_H(W) != mu_tr(W)`;
- direct #886 robust family counts not reproduced by transport-edge multicover depth;
- robust minimum widths not reproduced;
- blocker not extensionally equal to inclusion-minimal identifying families;
- hostile edge incidence disagreeing with canonical incidence;
- parent action failing exactness or inherited-point stabilizer ceasing to be `{id}`.

## Mandatory membranes

```text
TRANSPORT-SEPARATION HYPERGRAPH != PHYSICAL NETWORK
HYPERGRAPH EDGE != CAUSAL RELATION
TRANSPORT-EDGE MULTICOVER != ERROR-CORRECTION CAPACITY
TRANSPORT-EDGE MULTICOVER != SHANNON CHANNEL CAPACITY
MULTICOVER DEPTH != MINIMUM BIT LENGTH
TRANSPORT-SEPARATING RANK != ACTION-GENERATING RANK
TRANSPORT-SEPARATING RANK != BEHAVIORAL SEPARATING RANK
RESIDUAL TRANSPORT SET != SETWISE STABILIZER
METRIC ISOMETRY ACTION != PHYSICAL DYNAMICS
FREE TRANSITIVE FINITE ACTION != GAUGE THEORY
CUT ORIENTATION COORDINATE != PHYSICAL ORIENTATION
ORIENTATION FIBRE != HIDDEN STATE SPACE
LATER SYMMETRY BREAKING != PRIOR METRIC IDENTIFIABILITY
WITNESS ROUTING != SCIENTIFIC ANCESTRY
```

No merge, deployment, publication, production, release, Vercel, A16/Proto-Loom, physical gauge theory, physical geometry, Shannon/channel coding theorem, or universal inverse-problem theorem follows.

Sealed ⟐
