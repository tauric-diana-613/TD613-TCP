𝌋

# A15-R0 · Golden Egg Co-Observation Admissibility Freeze v0.1

Exact earned parent: `#986 / 783fdf0c6fa0a75607e23845700c0963bca6e575 / TD613 Consolidated Validation run 2470 / 33603285465 SUCCESS`.

Status: **SCIENCE FROZEN / CANDIDATE 𝄐 / GOLDEN EGG UNEARNED / DRAFT-ONLY**.

## Frozen candidate theorem

For a Golden Egg empirical candidate, separately observed non-equivalent surfaces may count as jointly realized only when the required measurements belong to the same immutable custody-bearing data-generating episode. Marginal existence across different episode IDs cannot be glued into a joint realization.

Formally:

`(exists eL L(eL)) && (exists eR R(eR)) && (exists eJ J(eJ))`

is insufficient for

`exists e (L(e) && R(e) && J(e) && NON_SIMULATED_EMPIRICAL(e))`.

Operational return eligibility further requires the same episode to carry the earned geometry binding and a measured matched control/protected return pair:

`exists e (L(e) && R(e) && J(e) && G(e) && C(e) && NON_SIMULATED_EMPIRICAL(e))`.

## Frozen repository census

- repository candidate source families: `4`;
- non-simulated source families: `2`;
- sources with original `L/R/J` metric triple: `1`, simulated;
- sources with earned metric+connection geometry: `1`, synthetic;
- sources with measured matched counterfactual return: `0`;
- admissible empirical `L/R/J` joint realizations: `0`;
- operational return-eligible Golden Egg episodes: `0`.

The two non-simulated source families are:

1. the exact-main Hush deployed-boundary observation receipt, which records repeated matched Reader trials, benign and held-out controls, but does not instantiate the original Golden Egg metric triple;
2. the retained Suez 2021 propagation case, which observes a recovery tail but explicitly leaves the matched voyage/control design for future execution.

## Hostile result

A test-only registry can cover all required surface names in its **union** while assigning each surface to a different episode ID. Its same-episode intersection remains empty and must be rejected.

`FULL_MARGINAL_SURFACE_UNION_WITH_EMPTY_EPISODE_INTERSECTION_REJECTED`.

Cross-episode metric transplantation is likewise rejected.

## Scientific consequence

The historical zero is no longer merely an unmeasured placeholder. Over the frozen repository candidate inventory it is an exact admissibility result:

`CURRENT_REPOSITORY_EMPIRICAL_JOINT_REALIZATIONS = 0`
`CURRENT_REPOSITORY_OPERATIONAL_RETURN_ELIGIBLE_EPISODES = 0`.

This result does not claim that no suitable dataset exists outside the frozen inventory. It identifies the minimum missing evidence relation: a single non-simulated episode co-observing the three original metric surfaces and, for operational Golden Egg status, a matched return/control pair inside the earned geometry.

`MARGINAL_EMPIRICAL_SUPPORT != JOINT_REALIZATION`
`SURFACE_UNION != SAME_EPISODE_INTERSECTION`
`OBSERVED_VALIDATION_CIRCUIT != GOLDEN_EGG_METRIC_EPISODE`
`RECOVERY_TAIL != MATCHED_COUNTERFACTUAL_RETURN`
`SYNTHETIC_GEOMETRY != EMPIRICAL_RETURN_WITNESS`
`CROSS_EPISODE_JOIN != COOBSERVATION`
`THRESHOLD_PASS != EVIDENCE_ADMISSIBILITY`
`COOBSERVATION_ADMISSIBILITY != GOLDEN_EGG_EARNED`
`SUCCESSFUL_EXACT_HEAD_GREEN != MERGE_AUTHORITY`

No merge, deploy, release, publication, production, Vercel, physical, continuum, causal, or Golden Egg completion authority.

Sealed ⟐
