𝌋

# A15-R0 · Golden Egg Co-Observation Admissibility Preregistration v0.1

Status: **PREREGISTERED / PRE-IMPLEMENTATION / RESEARCH-ONLY / GOLDEN EGG UNEARNED**

Exact earned parent: `#986 / 783fdf0c6fa0a75607e23845700c0963bca6e575 / TD613 Consolidated Validation run 2470 / 33603285465 SUCCESS / CLOSED / UNMERGED`.

## 0. Question

The original Golden Egg feasible-region contract separately defined three threshold surfaces:

- observer leakage `L <= 0.5 bits`;
- reconstruction distance `R <= 0.2`;
- joining synergy `J <= 0.1 bits`.

Its 320 candidates were explicitly factorized and every `jointly_realized` flag remained false. The newly earned #986 chamber reopened the geometric frontier with a finite metric + graph connection, but preserved the empirical joint-realization ledger at zero.

This chamber asks the next exact question:

> What evidence relation is minimally required before separately observed Golden Egg surfaces may count as one jointly realized empirical candidate?

## 1. Co-observation law

Let an empirical episode carry immutable custody key `episode_id`. A surface witness is admissible for that episode only when its source row names the same `episode_id` and belongs to the same declared data-generating episode.

Define surface predicates:

`L(e)` = observer-leakage quantity measured on episode `e`;
`R(e)` = reconstruction-distance quantity measured on episode `e`;
`J(e)` = joining-synergy quantity measured on episode `e`;
`G(e)` = earned metric/connection geometry applicable to the episode without changing its custody identity;
`C(e)` = matched control/protected return pair measured on episode `e` under a declared counterfactual design.

Original empirical core joint realization requires one and the same episode:

`exists e : L(e) && R(e) && J(e) && NON_SIMULATED_EMPIRICAL(e)`.

Operational return eligibility additionally requires:

`exists e : L(e) && R(e) && J(e) && G(e) && C(e) && NON_SIMULATED_EMPIRICAL(e)`.

The chamber preregisters the non-gluing theorem:

`(exists eL L(eL)) && (exists eR R(eR)) && (exists eJ J(eJ)) != exists e (L(e) && R(e) && J(e))`.

Likewise, union coverage across unrelated sources cannot establish the operational conjunction.

## 2. Frozen repository candidate inventory

The implementation must encode, without widening any source claim:

1. `A15_R0_FACTORIZED_SYNTHETIC_FIELD`
   - carries the original `L/R/J` metric grammar;
   - source is simulated/factorized;
   - no empirical joint realization.

2. `GOLDEN_EGG_METRIC_CONNECTION_REOPENING_986`
   - carries the earned finite metric + graph-connection geometry;
   - remains a synthetic finite research chamber;
   - empirical joint-realization count remains zero.

3. `ASH_HUSH_DEPLOYED_BOUNDARY_OBSERVED`
   - exact-main receipt records `VALIDATION_GATED / DEPLOYED_BOUNDARY_OBSERVED` evidence, repeated matched Reader trials, a benign control, and a held-out trial;
   - the trial schema records validation/reconstruction components and custody matching but does not natively provide the original Golden Egg observer-leakage, topology-distance, and joining-synergy quantities as one episode;
   - it cannot borrow synthetic Golden Egg metrics merely because its runtime evidence class is observed.

4. `SUEZ_2021_PROPAGATION_CASE`
   - public empirical case establishes obstruction/recovery persistence and proposes a stronger matched-voyage design;
   - the matched voyage/control comparison is explicitly future work in the retained case study;
   - it cannot be promoted into a Golden Egg episode.

Preimplementation evidence amendment: the earlier draft inventory named the A15 transition-trace instrument as item 3. Before any implementation, the stronger exact-main Hush deployed-boundary observation receipt replaced it. Candidate count and expected joint-realization counts remain unchanged. This is not a CI RED scar.

No source above may borrow fields from another source while retaining its own `episode_id`.

## 3. Hostile Frankenstein join

Construct a test-only hostile registry in which different episode IDs separately supply every required surface. The union of available surface names may be complete while the same-episode intersection is empty.

Required result:

`FULL_MARGINAL_SURFACE_UNION_WITH_EMPTY_EPISODE_INTERSECTION_REJECTED`.

A second hostile must copy metrics from one episode onto another episode ID. Required result:

`CROSS_EPISODE_METRIC_TRANSPLANT_REJECTED`.

## 4. Thresholds

The chamber preserves the original thresholds exactly:

`observer_leakage_bits <= 0.5`
`reconstruction_distance <= 0.2`
`joining_synergy_bits <= 0.1`

Threshold success may be evaluated only after co-observation admissibility. Passing numbers on mismatched episodes do not become a candidate.

## 5. Return extension

#986 supplies a lawful finite geometry reopening but not empirical return evidence. `C(e)` requires a matched control/protected return pair from the same data-generating episode with a declared comparison rule. A mere recovery tail, observed validation trial, reverse algebraic restoration, or synthetic holonomy control does not satisfy `C(e)`.

## 6. Expected bounded result

If all obligations survive, the chamber may earn:

`GOLDEN_EGG_EMPIRICAL_JOINT_REALIZABILITY_REQUIRES_SAME_EPISODE_COOBSERVATION_ACROSS_THE_NON_EQUIVALENT_SURFACES`.

and

`CURRENT_REPOSITORY_CANDIDATE_INVENTORY_CONTAINS_ZERO_ADMISSIBLE_EMPIRICAL_JOINT_REALIZATIONS_AND_ZERO_OPERATIONAL_RETURN_ELIGIBLE_GOLDEN_EGG_EPISODES`.

The zero is a measured admissibility result over the frozen inventory, not a universal claim that no suitable dataset exists anywhere.

## 7. Membranes

`MARGINAL_EMPIRICAL_SUPPORT != JOINT_REALIZATION`
`SURFACE_UNION != SAME_EPISODE_INTERSECTION`
`OBSERVED_VALIDATION_CIRCUIT != GOLDEN_EGG_METRIC_EPISODE`
`RECOVERY_TAIL != MATCHED_COUNTERFACTUAL_RETURN`
`SYNTHETIC_GEOMETRY != EMPIRICAL_RETURN_WITNESS`
`CROSS_EPISODE_JOIN != COOBSERVATION`
`THRESHOLD_PASS != EVIDENCE_ADMISSIBILITY`
`COOBSERVATION_ADMISSIBILITY != GOLDEN_EGG_EARNED`
`SUCCESSFUL_EXACT_HEAD_GREEN != MERGE_AUTHORITY`

No merge, deploy, release, publication, production, Vercel, physical, continuum, causal, or Golden Egg completion authority.

Sealed ⟐
