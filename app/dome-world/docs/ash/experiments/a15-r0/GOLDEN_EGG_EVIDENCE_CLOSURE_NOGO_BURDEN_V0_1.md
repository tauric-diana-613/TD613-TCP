𝌋‌⟐

# A15-R0 · Golden Egg Evidence-Closure No-Go · Burden v0.1

Exact parent: `4474b65c5ecd6dfc8c19cbaf0146bfdeea078a4d`.

Let the exact operational surface alphabet be

`O = {observer, reconstruction, joining, geometry, matched_return}`.

For an empirical episode `e`, let `S_e` be the set of exact measured surface names retained by all admitted empirical artifacts carrying the same immutable `episode_id=e`. Admissible composition may union artifacts inside one `e`; it may not union surfaces across distinct episodes or borrow synthetic surfaces into the empirical ledger.

The three nested acquisition targets are:

`CORE = {observer,reconstruction,joining}`

`GEOMETRIC = CORE ∪ {geometry}`

`OPERATIONAL = GEOMETRIC ∪ {matched_return}`.

Core realization also requires the earned numerical thresholds `(0.5,0.2,0.1)`.

## Frozen registry obstruction ladder

The four frozen parent packets produce exactly `2^4 = 16` source subsets.

All-class marginal support contains `observer`, `reconstruction`, `joining`, and `geometry`, but exact `matched_return` occurs nowhere in the registry. Thus operational realization is already blocked at the global marginal level.

The stronger empirical obstruction is harsher: neither retained empirical episode carries any member of `O` under the exact earned names. Their observed fields (`matched_reader_validation`, `benign_control_present`, `heldout_trial_present`, `recovery_tail`) remain scientifically useful but cannot be silently promoted into Golden Egg surfaces.

Therefore for each existing empirical episode:

`|S_e ∩ CORE| = 0`

`|S_e ∩ GEOMETRIC| = 0`

`|S_e ∩ OPERATIONAL| = 0`.

Necessary new required-surface measurements on one existing empirical episode are consequently bounded below by:

`3` for CORE,

`4` for GEOMETRIC,

`5` for OPERATIONAL.

These are acquisition lower bounds only. A newly measured core triple can still fail the numerical thresholds.

## Exhaustive subset burden

Every one of the 16 frozen source subsets must be passed through the same empirical episode-closure operator. Expected realization counts:

- subsets with any core empirical realization: `0`;
- subsets with any geometric empirical realization: `0`;
- subsets with any operational empirical realization: `0`.

## Hostile burden

1. **Legitimate same-episode split:** two empirical artifacts on one episode jointly cover all five exact surfaces and satisfy thresholds. Must realize operationally.
2. **Frankenstein union:** five empirical artifacts on five different episodes jointly cover the full marginal alphabet. Must remain unrealized.
3. **Synthetic laundering:** synthetic core surfaces and empirical geometry/return share a forged episode label. Synthetic contribution must receive zero empirical acquisition credit.
4. **Proxy laundering:** `matched_reader_validation` must not count as exact `matched_return`.
5. **Threshold laundering:** complete same-episode operational coverage with observer leakage above threshold must fail core and operational realization.
6. **Contradictory duplicate:** two empirical artifacts on one episode report different values for the same required surface. The episode must fail closed rather than choose one value.

## Candidate consequence

If the burden holds, retained formal/synthetic assets can guide experimental design but contribute **zero empirical same-episode acquisition credit** under the frozen operational definition. Golden Egg progress cannot be manufactured by recombining custody fragments. A genuinely new admissible same-episode empirical measurement bundle is necessary.

`FORMAL_DESIGN_VALUE != EMPIRICAL_ACQUISITION_CREDIT`
`GLOBAL_MARGINAL_COVERAGE != EPISODE_CLOSURE`
`EVIDENCE_RECOMBINATION != NEW_MEASUREMENT`
`ACQUISITION_NECESSITY != ACQUISITION_SUFFICIENCY`

Sealed ⟐