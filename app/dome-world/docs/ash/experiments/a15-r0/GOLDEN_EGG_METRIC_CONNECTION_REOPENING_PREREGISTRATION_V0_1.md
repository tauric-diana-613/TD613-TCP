# A15-R0 · Golden Egg Metric–Connection Reopening Preregistration v0.1

Status: **PREREGISTERED / PREIMPLEMENTATION / GOLDEN EGG UNEARNED / RESEARCH-ONLY**

Exact earned parent: `#984 / c0ef84c5c48af37a8f79d89c80d2e055da707836 / run 2469 / 33600136285 SUCCESS`.

Historical rest gate: `ASH_KEEP_A15_R0_OPEN_RESEARCH_FIELD_RECEIPT_V0_4.md` records `320` formal Golden Egg combinations, `24` threshold-feasible combinations, `18` Pareto-minimal combinations, `0` empirical candidates / joint realizations, `golden_egg_earned=false`, and leaves `H_INFORMATION_CURVATURE_GEOMETRIC` open. Its lawful reopening condition requires either `(a)` a newly declared candidate geometry with a metric and connection, or `(b)` an empirical joint-realizability experiment with discriminating data.

This chamber attempts **only route (a)**. It may reopen the geometric frontier. It may not increment the empirical joint-realization count and may not earn the Golden Egg.

## 1. New candidate geometry

Declare the finite filled triangle

```text
K = {A,B,C; AB,BC,CA; F_ABC}
```

with unit edge lengths and shortest-path base metric

```text
d_G(u,v)=0 if u=v, otherwise 1.
```

The filled face has declared combinatorial area `1`. This is a finite synthetic cell complex, not a continuum manifold.

## 2. Connection

Import the already-authored bidirectional `GL(2,F_31)` transport assay without mutation. Every oriented edge must be independently reconstructed from its own scalar observations, reverse orientations must equal independently reconstructed inverses, the reconstructible-but-orientation-inconsistent `BA_bad` hostile must be rejected, and gauge-frame change must preserve orientation consistency.

Only if that assay passes may this chamber call the assignment a **finite graph connection candidate**.

The face loop is

```text
H_F = T_CA T_BC T_AB.
```

The imported authored target is `[[3,5],[1,2]]`, with determinant `1`, trace `5`, and nonidentity.

## 3. Observation-partition metric

For partitions `P,Q` of the same finite named set `X`, ignore raw bucket labels and optimally match unlabeled blocks. Pad the smaller block family with empty blocks. Define

```text
R(P,Q)=max_sigma sum_i |B_i intersect C_sigma(i)|
d_Pi(P,Q)=|X|-R(P,Q).
```

Before using `d_Pi` geometrically, exhaustively verify symmetry, identity of indiscernibles, and the triangle inequality on **all 15 set partitions of a four-member named set**:

```text
15 partitions
225 ordered pair checks
3375 ordered triple checks
0 expected metric failures.
```

No generic partition metric theorem is imported without this finite witness.

## 4. Connection action on observability

Import the authored holonomy/observability holdout unchanged. It must independently bind to the same loop matrix `H_F` and fixed local readout `q=[1,0]`.

On its four named confirmatory states, freeze these metric obligations:

```text
d_Pi(P_pre,P_post)                 = 2
d_Pi(P_pre,P_flat_post)            = 0
d_Pi(P_pre,P_reverse_restored)     = 0
d_Pi(P_gauge_pre,P_gauge_post)     = 2
d_Pi(P_invariant_before,P_invariant_after) = 0.
```

The positive pre/post partitions both retain bucket-size profile `[2,2]`; therefore a bucket-size-only summary must be shown to miss a genuine distance-2 partition displacement.

The separate nontrivial invariant-readout loop must remain distance zero. Thus any positive result is anisotropic/readout-relative rather than a universal scalar response to nonidentity holonomy.

## 5. Base-metric audit

Exhaustively verify the declared triangle base metric on all `9` ordered pairs and `27` ordered triples with zero failures.

## 6. Gauge and face signature

The connection gauge clone must retain the loop conjugacy relation. Face determinant and trace must agree between the original loop and its gauge conjugate:

```text
det(H_F)=1
tr(H_F)=5 mod 31.
```

This is a finite conjugacy-invariant face signature, not continuum curvature.

## 7. Historical Golden Egg firewall

The emitted certificate must preserve exactly:

```text
formal_combinations             = 320
threshold_feasible              = 24
pareto_minimal                  = 18
empirical_joint_realized        = 0
golden_egg_earned               = false
```

If the candidate metric and connection survive, the strongest allowed status is:

```text
H_INFORMATION_CURVATURE_GEOMETRIC = REOPENED_FOR_EXPERIMENT_NOT_SOLVED
GOLDEN_EGG_REOPENING_TRIGGER = NEWLY_DECLARED_CANDIDATE_GEOMETRY_WITH_METRIC_AND_CONNECTION
```

## 8. Required hostiles

1. orientation-inconsistent but reconstructible reverse edge is rejected;
2. flat loop yields zero partition displacement;
3. forward+reverse restores zero displacement;
4. nontrivial invariant-readout loop yields zero displacement;
5. bucket-size profile equality fails as a substitute for partition distance;
6. forged cross-universe partition comparison abstains/throws rather than silently comparing;
7. an intentionally malformed non-metric distance must fail the triangle audit;
8. old research receipts with `ci_witness=false` cannot themselves count as current exact-head authority.

## 9. Candidate bounded 𝄐

If and only if the exact frozen head passes current TD613 Consolidated Validation including the A15-R0 research field, this chamber may earn:

```text
THE_GOLDEN_EGG_REST_FRONTIER_IS_LAWFULLY_REOPENED_BY_AN_EXACT_FINITE_METRIC_CONNECTION_INFORMATION_GEOMETRY_CANDIDATE_IN_WHICH_AN_INDEPENDENTLY_RECONSTRUCTED_GL2_F31_GRAPH_CONNECTION_HAS_NONTRIVIAL_FACE_HOLONOMY_AND_INDUCES_NONZERO_OBSERVABILITY_PARTITION_METRIC_DISPLACEMENT_FOR_ONE_FIXED_READOUT_WHILE_FLAT_REVERSE_GAUGE_AND_INVARIANT_READOUT_CONTROLS_PRESERVE_THE_REQUIRED_ANISOTROPY_AND_THE_HISTORICAL_EMPIRICAL_JOINT_REALIZATION_COUNT_REMAINS_ZERO
```

## 10. Membranes

```text
GOLDEN_EGG_GEOMETRIC_REOPENING != GOLDEN_EGG_EARNED
REOPENED_FRONTIER != EMPIRICAL_JOINT_REALIZATION
FINITE_PARTITION_METRIC != CONTINUUM_INFORMATION_GEOMETRY
GRAPH_CONNECTION_CANDIDATE != DIFFERENTIAL_CONNECTION
FACE_HOLONOMY != PHYSICAL_HOLONOMY
NONZERO_PARTITION_DISPLACEMENT != CURVATURE_SCALAR
NONTRIVIAL_FACE_HOLONOMY != UNIVERSAL_OBSERVABILITY_DISPLACEMENT
BUCKET_PROFILE_EQUALITY != PARTITION_IDENTITY
CURRENT_EXACT_HEAD_WITNESS != RETROACTIVE_CI_WITNESS_FOR_OLD_RECEIPTS
SUCCESSFUL_EXACT_HEAD_GREEN != MERGE_AUTHORITY
```

No merge, deploy, release, publication, production, Vercel, Proto-Loom, continuum, physical, Berry, quantum, hidden-state, or Golden Egg completion authority.

Sealed ⟐
