𝌋⟐

# A15-R0 · Holonomy Loom / Moss Lantern Discrete Transport Holonomy — Preregistration v0.1

Status: **PREREGISTERED / PREIMPLEMENTATION / RESEARCH-ONLY / NON-RUNTIME / UNMERGED**.

## Exact earned parent

```text
#904 / d840400c37d6ac36b744157c1fbae1bc9451ada1
TD613 Consolidated Validation run 2404 / 33344663913 — SUCCESS
A15-R0 step 19 — SUCCESS
```

#904 earned a bounded procedural-memory/order-defect calibration on the inherited Moss Lantern phantom. It deliberately withheld holonomy terminology because no transport loop had yet been declared.

This chamber declares that missing structure explicitly.

## Declared discrete transport bundle

Base: a synthetic four-edge calibration cycle `C4` with based loop

```text
v0 --A--> v1 --B--> v2 --A^-1--> v3 --B^-1--> v0
```

Fiber over every base vertex:

```text
Xi = F2^2 = {(0,0),(0,1),(1,0),(1,1)}.
```

The base cycle is a research calibration graph only. It is not a live Ash route, product navigation graph, physical trajectory, or geometric manifold.

Declared edge transports inherit the exact #904 affine maps:

```text
A(x,y) = (x xor 1, y)
B(x,y) = (x, y xor x)
```

Both are involutions on `F2^2`, therefore

```text
A^-1 = A
B^-1 = B.
```

With the project’s left-to-right action convention, the based commutator loop is

```text
gamma = A B A^-1 B^-1 = A B A B.
```

## Frozen target holonomy

For every `Xi=(x,y)`:

```text
Hol_gamma(Xi) = (x, y xor 1).
```

Required complete atlas:

```text
00 -> 01
01 -> 00
10 -> 11
11 -> 10
```

Therefore the candidate bounded result requires:

```text
loop basepoint return                         4/4
nonidentity fiber transport                   4/4
unit-Hamming fiber displacement               4/4
delayed y-marker split                        4/4
Hol_gamma^2 = identity                        4/4
```

## Transport-group census

Let `G=<A,B>` be the finite transformation group generated by the declared edge transports on `F2^2`.

Frozen target:

```text
|G| = 8
all 8 elements bijective
all 8 elements possess two-sided inverses
```

Compute all `8*8=64` group commutators. The frozen target commutator image is exactly

```text
[G,G] = {id, Hol_gamma}
|[G,G]| = 2.
```

This is an exact finite transformation-group result inside the declared calibration fixture only.

## Null controls

### Backtrack null

For all four fiber starts:

```text
A A^-1 = id
B B^-1 = id.
```

Required nonidentity backtrack outputs: `0/8`.

### Commuting-square null

Declare

```text
C(x,y) = (x xor 1,y)
D(x,y) = (x,y xor 1).
```

The control commutator

```text
C D C^-1 D^-1
```

must equal identity on all four fiber states.

Required control holonomy divergences: `0/4`.

### Memoryless projection null

Project away `Xi` and retain only the inherited Moss Lantern visible endpoint:

```text
q = returned-practice-capsule.
```

The loop must remain invisible to the state-only endpoint projection:

```text
visible endpoint divergences = 0/4.
```

## Candidate bounded 𝄐

If exact-head GREEN:

```text
THE_DECLARED_MOSS_LANTERN_C4_CALIBRATION_TRANSPORT_ASSIGNMENT_HAS_NONTRIVIAL_FORMAL_DISCRETE_HOLONOMY_BECAUSE_ITS_BASED_COMMUTATOR_LOOP_RETURNS_TO_THE_SAME_BASEPOINT_WHILE_INDUCING_THE_NONIDENTITY_ORDER_TWO_FIBER_AUTOMORPHISM_Y_TRANSLATION_ON_ALL_FOUR_DECLARED_APPARATUS_STATES.
```

and:

```text
THE_GENERATED_EIGHT_ELEMENT_TRANSPORT_GROUP_HAS_A_TWO_ELEMENT_COMMUTATOR_SUBGROUP_EXACTLY_EQUAL_TO_IDENTITY_PLUS_THE_EARNED_LOOP_HOLONOMY_TRANSFORMATION_IN_THE_FIXED_SYNTHETIC_FIXTURE.
```

This chamber may use **formal discrete holonomy** because a base loop, fiber, invertible edge transport, and loop-induced fiber automorphism are all explicitly declared and exhaustively audited.

## Mandatory membranes

```text
FORMAL_DISCRETE_HOLONOMY != GEOMETRIC_HOLONOMY
FORMAL_DISCRETE_HOLONOMY != PHYSICAL_HOLONOMY
DISCRETE_TRANSPORT_ASSIGNMENT != LEVI_CIVITA_OR_GAUGE_CONNECTION
CALIBRATION_BASE_CYCLE != LIVE_ROUTE_GRAPH
DECLARED_INVERSE_EDGE_TRANSPORT != OPERATIONAL_UNDO_OR_REVERSE_WORKFLOW
NONTRIVIAL_COMMUTATOR != PHYSICAL_CURVATURE_MEASUREMENT
FINITE_TRANSPORT_GROUP != MODEL_INTERNAL_SYMMETRY_GROUP
FIBER_AUTOMORPHISM != HIDDEN_MODEL_STATE_TRANSITION
HOLONOMY_LOOP_VISIBILITY != STATE_ONLY_ENDPOINT_VISIBILITY
MOSS_LANTERN_CALIBRATION_PHANTOM != EVIDENCE
HOLONOMY_LOOM_RESEARCH_RECEIPT != LIVE_HOLONOMY_LOOM_RUNTIME
A15_R0_RESEARCH_EXTENSION != PROTO_LOOM_OR_A16_PROMOTION
WITNESS_ROUTING != SCIENTIFIC_ANCESTRY
```

No live Ash/Loom, model-weight, physical memory, geometry, gauge field, Berry phase, continuum connection, physical transport, operational inverse, Proto-Loom, A16, merge, deployment, release, publication, production, or Vercel authority.

Sealed ⟐