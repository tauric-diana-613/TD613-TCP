𝌋‌⟐

# A15-R0 · Loom-Compatible Golden Egg Same-Episode Acquisition Contract · Burden v0.1

Exact earned scientific parent:
`#990 / 28ba14628326db37282d3d78335d6ee707b087b4 / run 2473 / 33607462701 SUCCESS`.

Live Loom source custody is frozen independently:
`d652c5e151471be7e40ff6a08936ba26c0cef1ad:app/dome-world/index.html`
blob `695d22ec77339bc54512fe6a6a7c0203240ff135`.

## Frozen structural burden

A canonical acquisition episode must demonstrate:

- 1 immutable episode;
- 1 departure ID;
- 1 custody ID;
- 1 comparison-frame ID;
- exactly 2 route roles: control + protected;
- 2 distinct route IDs;
- 2 distinct observed return IDs;
- route/departure/custody/frame consistency;
- at least 2 same-episode empirical artifacts in the positive split fixture;
- exact operational surfaces `observer/reconstruction/joining/geometry/matched_return`;
- inherited parent threshold pass;
- valid empirical geometry binding metadata;
- valid matched-return pair metadata;
- output state `CANDIDATE`;
- `golden_egg_earned=false`.

A canonical incomplete episode must remain structurally admissible and return `HELD`.

A canonical complete threshold-miss episode must return `FAILED`.

## Frozen hostile burden

Expected hostile passes: `12/12`.

1. Cross-episode artifact with superficially matching custody is rejected.
2. Cross-episode required-surface transplantation is rejected.
3. Shared origin with collapsed route identity is rejected.
4. Duplicate return identity is rejected.
5. Unobserved return is rejected.
6. `matched_reader_validation` remains a proxy and yields `HELD`.
7. `recovery_tail` remains a proxy and yields `HELD`.
8. Synthetic geometry contributes zero empirical geometry credit and yields `HELD` when empirical `geometry` is absent.
9. Malformed exact empirical geometry fails closed.
10. Malformed exact empirical matched-return fails closed.
11. Contradictory duplicate required measurement fails closed.
12. Five exact admissible surfaces with observer leakage above threshold return `FAILED`.

Cross-episode child-legible message must be exactly:

`THAT THREAD CAME FROM ANOTHER JOURNEY.`

## Loom-source custody burden

The hardening contract must prove:
- #990 remains the exact theorem parent;
- the chamber remains one atomic science commit above #990;
- exactly eight registered A15-R0 paths change;
- #990 hardening/source blobs remain exact;
- the live Loom main commit resolves `app/dome-world/index.html` to frozen blob `695d22ec77339bc54512fe6a6a7c0203240ff135`;
- the science chamber does not change `app/dome-world/index.html`.

## Claim ceiling

Passing this burden earns a route-and-custody acquisition **contract**, not an external observation.

`ACQUISITION_CONTRACT != EMPIRICAL_ACQUISITION`
`CANDIDATE != GOLDEN_EGG_EARNED`
`SYNTHETIC_GEOMETRY_REFERENCE != EMPIRICAL_GEOMETRY_MEASUREMENT`
`LIVE_LOOM_SOURCE_CUSTODY != SCIENTIFIC_ANCESTRY`
`LOOM_COMPATIBILITY != LOOM_MUTATION`

Expected scientific failures: `0`.

Sealed ⟐
