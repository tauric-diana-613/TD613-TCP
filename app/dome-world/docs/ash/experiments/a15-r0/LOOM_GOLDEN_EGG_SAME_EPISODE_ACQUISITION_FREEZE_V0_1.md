𝌋‌⟐

# A15-R0 · Loom-Compatible Golden Egg Same-Episode Acquisition Contract · Freeze v0.1

Status: **SCIENCE FROZEN / CANDIDATE 𝄐 / GOLDEN EGG UNEARNED / RESEARCH-ONLY / NO LIVE LOOM MUTATION / UNMERGED**.

Exact earned parent:
`#990 / 28ba14628326db37282d3d78335d6ee707b087b4 / TD613 Consolidated Validation run 2473 / 33607462701 SUCCESS`.

Separate live Loom source custody:
`main / d652c5e151471be7e40ff6a08936ba26c0cef1ad / app/dome-world/index.html / blob 695d22ec77339bc54512fe6a6a7c0203240ff135`.

## Frozen candidate claim

The first lawful Western-Horizon→Loom convergence object is an immutable same-episode acquisition contract, not a new application surface.

Under the inherited #990 five-surface operational alphabet, a candidate episode must bind one control route and one protected route to the same immutable episode/departure/custody/comparison frame while preserving distinct route and return identities. Empirical L/R/J/G/C artifacts may compose only inside that episode/custody. Exact `geometry` and `matched_return` receive additional metadata gates that prevent synthetic geometry, Reader validation, recovery tails, or route coincidence from silently becoming empirical Golden Egg credit.

The contract emits exactly:
`INADMISSIBLE`, `HELD`, `FAILED`, or `CANDIDATE`.

`CANDIDATE` means only that the acquisition bundle is structurally admissible and passes the inherited L/R/J thresholds. This chamber freezes `golden_egg_earned=false`.

## Frozen compatibility witness

The current Loom remains source-custodied as:
- display `Loom Room`;
- receives `motif-return`;
- preserves `holonomy` and `anti-equivalence-edge`;
- emits `cloth-map` and `route-deformation`;
- holonomy claim ceiling `route-deformation-record-not-truth-proof`.

No live Loom path, product naming, runtime behavior, deployment surface, or production artifact is modified.

## Frozen expected result

- canonical same-episode split acquisition: `CANDIDATE`;
- canonical incomplete acquisition: `HELD`;
- canonical threshold miss: `FAILED`;
- 12 hostile gates: all pass;
- synthetic geometry empirical credit: `0`;
- Golden Egg earned: `false`;
- live Loom mutated: `false`.

## Membranes

`ACQUISITION_CONTRACT != EMPIRICAL_ACQUISITION`
`LOOM_OBSERVATION != GOLDEN_EGG_ADJUDICATION`
`CANDIDATE != GOLDEN_EGG_EARNED`
`FIVE_SURFACES_PRESENT != GOLDEN_EGG_EARNED`
`SYNTHETIC_GEOMETRY != EMPIRICAL_RETURN_WITNESS`
`RECOVERY_TAIL != MATCHED_COUNTERFACTUAL_RETURN`
`OBSERVED_VALIDATION_CIRCUIT != GOLDEN_EGG_METRIC_EPISODE`
`SHARED_ORIGIN != ROUTE_IDENTITY`
`HOLONOMY != TRUTH_PROOF`
`ROUTE_DEFORMATION != CAUSAL_PROOF`
`SCIENCE_ANCESTRY != SOURCE_CUSTODY`
`LOOM_COMPATIBILITY != LOOM_MUTATION`
`LOOM_RENAME_INTENT != CURRENT_REPOSITORY_NAME`
`SUCCESSFUL_EXACT_HEAD_GREEN != MERGE_AUTHORITY`

No merge, deploy, release, publication, production, Vercel, external acquisition, physical, causal, continuum, Golden Egg completion, or Loom rename authority.

Sealed ⟐
