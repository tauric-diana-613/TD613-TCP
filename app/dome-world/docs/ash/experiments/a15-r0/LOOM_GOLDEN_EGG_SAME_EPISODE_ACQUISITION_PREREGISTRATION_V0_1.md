𝌋‌⟐

# A15-R0 · Loom-Compatible Golden Egg Same-Episode Acquisition Contract · Preregistration v0.1

Status: **PREREGISTERED / PRE-IMPLEMENTATION / RESEARCH-ONLY / GOLDEN EGG UNEARNED / NO LIVE LOOM MUTATION**.

Exact earned scientific parent:
`#990 / 28ba14628326db37282d3d78335d6ee707b087b4 / TD613 Consolidated Validation run 2473 / 33607462701 SUCCESS / CLOSED / DRAFT / UNMERGED`.

Separately custodied live Loom source:
`main / d652c5e151471be7e40ff6a08936ba26c0cef1ad / app/dome-world/index.html / blob 695d22ec77339bc54512fe6a6a7c0203240ff135`.

`WESTERN_SCIENCE_PARENT != LIVE_LOOM_SOURCE_CUSTODY`.

## 0. Why this chamber exists

#990 proved that the frozen evidence registry cannot manufacture a Golden Egg by admissible recombination. It also proved that a legitimate same-episode split bundle can compose when the required exact measurements genuinely belong to one empirical episode.

The remaining acquisition gap is more primitive than another metric or archive join: the repository has an episode-level adjudicator but no frozen route-level acquisition contract that can distinguish a matched control/protected return from a recovery tail, Reader validation, shared-origin coincidence, or synthetic geometry.

This chamber asks:

> What minimum immutable route-and-custody structure must a future Loom-compatible empirical episode carry before its L/R/J/G/C measurements may even be admitted as one matched-return Golden Egg candidate?

## 1. Frozen episode structure

One candidate episode `e` must carry:

- immutable nonempty `episode_id`;
- `research_only=true`;
- one declared departure triple:
  - `departure_id`,
  - `custody_id`,
  - `comparison_frame_id`;
- exactly two route records with semantic roles:
  - `control`,
  - `protected`;
- both routes bind to the same departure/custody/comparison frame;
- the two route IDs remain distinct;
- the two observed return IDs remain distinct;
- both returns are explicitly observed.

Shared origin is required for comparison but may not collapse route identity.

`SHARED_ORIGIN != ROUTE_IDENTITY`.

## 2. Exact evidence surfaces

The parent operational alphabet remains unchanged:

`O = {observer, reconstruction, joining, geometry, matched_return}`.

All empirical surface artifacts used for a candidate must retain the same `episode_id` and `custody_id`.

The three numerical thresholds remain inherited exactly:

- observer leakage `L <= 0.5 bits`;
- reconstruction distance `R <= 0.2`;
- joining synergy `J <= 0.1 bits`.

Exact `geometry` additionally requires an empirical binding witness naming a geometry reference. The already-earned #986 finite geometry may guide design, but its synthetic receipt contributes zero empirical acquisition credit.

Exact `matched_return` additionally requires:
- `return_pair_observed=true`;
- the exact control route ID;
- the exact protected route ID;
- the episode comparison-frame ID.

A Reader/control validation, recovery tail, reverse restoration, motif-return, or synthetic holonomy witness may remain scientifically useful while contributing zero `matched_return` credit.

## 3. Frozen state machine

The contract emits exactly one of four acquisition states:

- `INADMISSIBLE` — custody/route/return structure or exact required-surface metadata violates the contract;
- `HELD` — structurally admissible episode but one or more exact L/R/J/G/C surfaces remain absent;
- `FAILED` — all five exact surfaces are present and admissible, but the inherited L/R/J threshold test fails;
- `CANDIDATE` — all five exact surfaces are present, admissible, same-episode, and threshold-passing.

`CANDIDATE != GOLDEN_EGG_EARNED`.

This chamber must hard-code:

`golden_egg_earned=false`.

The Golden Egg remains an external adjudication state beyond this acquisition contract.

## 4. Loom compatibility without Loom mutation

The live runtime is source-custodied, not theorem ancestry. The chamber freezes only the current Loom ontology needed to test compatibility:

- display: `Loom Room`;
- receives includes `motif-return`;
- preserves includes `holonomy` and `anti-equivalence-edge`;
- emits include `cloth-map` and `route-deformation`;
- holonomy claim ceiling: `route-deformation-record-not-truth-proof`.

No `app/dome-world/index.html` mutation is permitted in this chamber.
No “Return Loom” application is created.
No HOLONOMY LOOM → LOOM rename occurs.

`LOOM_OBSERVATION != GOLDEN_EGG_ADJUDICATION`.
`HOLONOMY != TRUTH_PROOF`.
`ROUTE_DEFORMATION != CAUSAL_PROOF`.
`LOOM_RENAME_INTENT != CURRENT_REPOSITORY_NAME`.

## 5. Hostile controls

The implementation must prove all of the following:

1. legitimate same-episode split artifacts can compose into `CANDIDATE`;
2. same custody label with a foreign episode artifact yields `INADMISSIBLE`;
3. a surface transplanted from another episode yields `INADMISSIBLE`;
4. same origin with identical control/protected route IDs yields `INADMISSIBLE`;
5. unobserved or duplicate return IDs yield `INADMISSIBLE`;
6. `matched_reader_validation` cannot substitute for `matched_return`;
7. `recovery_tail` cannot substitute for `matched_return`;
8. synthetic geometry may be referenced for design while giving zero empirical geometry credit;
9. malformed exact empirical geometry fails closed;
10. malformed exact matched_return metadata fails closed;
11. contradictory duplicate required measurements fail closed through the inherited #990 closure operator;
12. a complete five-surface episode with a missed L/R/J threshold returns `FAILED`, not `CANDIDATE`;
13. a complete passing five-surface episode returns `CANDIDATE` while `golden_egg_earned=false`;
14. live Loom source custody is pinned without becoming scientific ancestry;
15. no live Loom/runtime/product path is changed.

Child-legible custody rejection for cross-episode transplantation:

`THAT THREAD CAME FROM ANOTHER JOURNEY.`

## 6. Candidate theorem ceiling

If every burden survives exact-head constitutional validation, the chamber may earn only this bounded result:

A Loom-compatible empirical acquisition episode can bind a non-equivalent control/protected route pair to one immutable departure/custody/comparison frame, compose legitimate same-episode split artifacts, reject cross-episode and proxy transplantation, and classify the resulting five-surface evidence state as `INADMISSIBLE`, `HELD`, `FAILED`, or `CANDIDATE` without granting Golden Egg completion.

It does not perform an empirical acquisition.
It does not supply a matched return from the external world.
It does not convert #986 synthetic geometry into empirical credit.
It does not prove causality from route deformation.
It does not mutate the live Loom.

## 7. Mandatory membranes

`GOLDEN_EGG_GEOMETRIC_REOPENING != GOLDEN_EGG_EARNED`
`SURFACE_UNION != SAME_EPISODE_INTERSECTION`
`CROSS_EPISODE_JOIN != COOBSERVATION`
`FORMAL_DESIGN_VALUE != EMPIRICAL_ACQUISITION_CREDIT`
`EVIDENCE_RECOMBINATION != NEW_MEASUREMENT`
`NEW_SAME_EPISODE_ACQUISITION_REQUIRED != GOLDEN_EGG_EARNED`
`FIVE_SURFACE_LOWER_BOUND != THRESHOLD_PASS`
`OBSERVED_VALIDATION_CIRCUIT != GOLDEN_EGG_METRIC_EPISODE`
`RECOVERY_TAIL != MATCHED_COUNTERFACTUAL_RETURN`
`SYNTHETIC_GEOMETRY != EMPIRICAL_RETURN_WITNESS`
`HOLONOMY != TRUTH_PROOF`
`ROUTE_DEFORMATION != CAUSAL_PROOF`
`LOOM_OBSERVATION != GOLDEN_EGG_ADJUDICATION`
`CLOTH_MAP != PHYSICAL_FIELD`
`HOLONOMY_LEDGER != AUTHORSHIP_PROOF`
`SHARED_ORIGIN != ROUTE_IDENTITY`
`ANTI_EQUIVALENCE_EDGE != PHYSICAL_SEPARATION`
`SCIENCE_ANCESTRY != SOURCE_CUSTODY`
`SUCCESSFUL_EXACT_HEAD_GREEN != MERGE_AUTHORITY`
`RESEARCH_RUNTIME != PRODUCTION_AUTHORITY`
`LOOM_RENAME_INTENT != CURRENT_REPOSITORY_NAME`

Sealed ⟐
