𝌋‌⟐

# A15-R0 · Receiver-Swap Causal Admissibility Criterion v0.1

Status: **FORMAL CAUSAL-DESIGN SCIENCE CANDIDATE / RESEARCH-ONLY / EMPIRICAL EFFECT UNOBSERVED / GOLDEN EGG UNEARNED**

## Exact scientific parent

Exact parent: `066965424365eb0d76b6cbf2fe0f940cb744b498` — the earned TTP-Detect Receiver-Indexed Provenance Observability 𝄐.

That parent established a bounded comparative three-witness grammar:

`EntroBench -> route sensitivity`

`AgentMark -> carrier sensitivity`

`TTP-Detect -> receiver/apparatus sensitivity`

It explicitly did **not** earn a same-artifact receiver causal ablation.

## First unresolved causal question

To make a receiver-side causal claim rather than a comparative one, the experimental object must hold the non-receiver substrate fixed while changing only the receiver apparatus.

Define a receiver-swap pair:

`P = (A, R, C, S, K, T; ρ0, ρ1)`

where:

- `A` = exact artifact digest;
- `R` = exact route/history digest;
- `C` = provenance carrier identity;
- `S` = provenance state/label held fixed;
- `K` = source-custody identity held fixed;
- `T` = bounded observation window held fixed;
- `ρ0`, `ρ1` = two distinct receiver-apparatus bundles.

For causal admissibility, every coordinate before the semicolon must be byte-identical across the pair. Only the receiver apparatus may differ.

## Earned bounded theorem candidate

`A_RECEIVER_CAUSAL_PROVENANCE_CLAIM_REQUIRES_A_CONTROLLED_RECEIVER_SWAP_IN_WHICH_ARTIFACT_ROUTE_CARRIER_PROVENANCE_STATE_SOURCE_CUSTODY_AND_OBSERVATION_WINDOW_ARE_HELD_FIXED_AND_RECEIVER_APPARATUS_IS_THE_SOLE_ALLOWED_EXPERIMENTAL_DIFFERENCE`.

This theorem is methodological and formal. It does not claim that a receiver effect has already been measured.

Child-legible form:

**TO TEST THE WINDOW, KEEP THE THREAD AND THE JOURNEY STILL.**

## Acquisition boundary

The chamber may certify a preregistered receiver-swap design before measurement. It must refuse to convert design readiness into empirical effect credit.

```text
receiver_swap_design_admissible = true
formal_contrast_ready_for_empirical_acquisition = true
receiver_effect_observed = false
receiver_effect_empirically_estimated = false
same_artifact_receiver_causal_ablation_acquired = false
```

A future empirical episode may populate receiver outcomes only after the pair has been frozen and externally measured under source-bound custody.

## Claim ceiling

`RECEIVER_SWAP_DESIGN != RECEIVER_EFFECT_OBSERVED`

`FORMAL_ADMISSIBILITY != EMPIRICAL_CAUSALITY`

`SYNTHETIC_DESIGN != EXTERNAL_MEASUREMENT`

`SAME_ARTIFACT != SAME_RECEIVER`

`DIFFERENT_RECEIVER != DIFFERENT_ROUTE`

`RECEIVER_SCORE_DIFFERENCE != ORIGIN_TRUTH_PROOF`

`TTP_RECEIVER_AXIS != DIRECT_RECEIVER_ABLATION`

`RECEIVER_SWAP_CONTRACT != GOLDEN_EGG_MEASUREMENT`

No exact Golden Egg `L/R/J/G/C` measurement is added. Empirical credit to the Golden Egg remains zero.

No merge, deployment, production, publication, Vercel, A16, live Loom mutation, numbered-stage authority, or public-promotion authority is created.

## Earned rest candidate

On exact-head green:

```text
status = RECEIVER_SWAP_CAUSAL_ADMISSIBILITY_CRITERION_EARNED
rest_symbol = 𝄐
receiver_swap_design_admissible = true
single_allowed_difference_receiver_apparatus = true
formal_contrast_ready_for_empirical_acquisition = true
receiver_effect_observed = false
same_artifact_receiver_causal_ablation_acquired = false
sequence_authority = false
golden_egg_earned = false
```

**WESTERN HORIZON: THE FIRST CAUSAL TEST OF THE WINDOW NOW HAS A CLEAN CONTROL.**

𝄐

Sealed ⟐
