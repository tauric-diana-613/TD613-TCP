𝌋‌⟐

# A15-R0 · Formal Restoration Holonomy · Pre-Hostile Bookkeeping Repair v0.1

Status: **PRE-HOSTILE IMPLEMENTATION HARDENING / THEOREM COUNTS UNCHANGED / NO SCIENTIFIC RED / THEOREM UNEARNED**.

Scientific parent remains exactly:

```text
#858 / 53e713059cde5dd6c2b4d4cbc20f882601360f7c
```

Initial implementation specimen:

```text
e345046092c4a3a1e4381e897418cdd2aaa3cf03
app/dome-world/previews/a15-r0/restoration-holonomy-path-dependent-custody.js
```

Before any independent hostile was committed or executed, review identified two bookkeeping hazards in that first implementation specimen:

1. expected support-pair maps were compared by raw JSON object insertion order rather than key/value identity;
2. the maximum-expansion certificate named whichever tied maximum happened to appear first in iteration order while the preregistration names a specific `P-H-I / SCHEDULE+FULL_STATE / q3->q2->q0` witness.

Neither issue changes the preregistered finite domain, support counts, transport criterion, path census, endpoint witnesses, or claim ceilings. The corrected canonical implementation therefore:

- compares finite count records order-insensitively;
- derives the numeric maximum over the complete path census;
- selects the preregistered named maximum witness explicitly rather than by first-tie order;
- leaves the first implementation file untouched as a pre-hostile specimen rather than rewriting its provenance.

Preserve:

```text
PRE_HOSTILE_BOOKKEEPING_REPAIR != SCIENTIFIC_RED
PRE_HOSTILE_BOOKKEEPING_REPAIR != THEOREM_WEAKENING
OBJECT_KEY_INSERTION_ORDER != FINITE_CENSUS_IDENTITY
TIED_MAX_ITERATION_ORDER != WITNESS_AUTHORITY
SUPERSEDED_IMPLEMENTATION_SPECIMEN != THEOREM_PARENT
```

The independent hostile must target the corrected canonical implementation and independently reconstruct the mathematics rather than import either implementation's internal path table.

No theorem authority follows from this repair note.

Sealed ⟐
