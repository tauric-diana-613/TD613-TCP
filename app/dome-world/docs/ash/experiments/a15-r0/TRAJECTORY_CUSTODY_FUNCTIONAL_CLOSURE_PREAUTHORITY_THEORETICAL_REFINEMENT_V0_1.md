𝌋‌⟐

# A15-R0 · Trajectory Custody-Functional Closure · Pre-Authority Theoretical Refinement v0.1

Status: **PRE-AUTHORITY SCIENTIFIC REFINEMENT / ORIGINAL REPLAY CENSUS PRESERVED / ONE NECESSITY CLAIM WITHDRAWN / THEOREM UNEARNED**.

Exact scientific parent remains:

```text
#866 / a5a073bdf18cd1b7155422b4bd562de9c80aa3f5
```

Superseded frozen candidate under secondary assay:

```text
aee13ca4c71f02ea7d7688e28b304adbf471b0aa
TD613 Consolidated Validation run 2371 / 33162146218
```

Regardless of that run's eventual CI conclusion, `aee13ca4...` is not eligible for theorem authority because a separate finite counterassay found one overstrong interpretation before any 𝄐 stamp.

## What survived

The complete cross-theorem replay census remains unchanged:

```text
3,048 stage maxima
1,180 #858 recompression rows
  784 #860 transport paths
  208 #862 anticipatory horizons
  208 #864 alias contexts
2,380 heterogeneous cross-theorem replay rows
```

No #858/#860/#862/#864 count, named witness, parent match, support cardinality, or replay identity changed.

## What failed

The preregistration correctly proposed that per-fibre cardinality profiles `P_t(Q)` are sufficient to replay #864. It then overreached in one sentence by describing those profiles as **required beyond stage maxima**.

A secondary finite assay conditioned on the schedule already intrinsic to context `Q` found:

```text
all 762 schedule/bundle contexts

observed (schedule, full stage-maximum vector) classes = 154
observed (schedule, full cardinality-profile trajectory) classes = 154

(schedule, m(Q)) -> P(Q) ambiguity classes = 0
(schedule, P(Q)) -> m(Q) ambiguity classes = 0
```

On the 208 q3-birth contexts used by #864:

```text
observed (schedule, m2, m1) classes = 12
(schedule, m2, m1) -> (P2, P1) ambiguity classes = 0
```

Therefore, in this fixed fixture, the complete cardinality-profile trajectory is empirically recoverable from the schedule-conditioned maximum trajectory on the observed context domain.

The withdrawn claim is:

```text
#864_REQUIRES_PER_FIBRE_CARDINALITY_PROFILES_BEYOND_STAGE_MAXIMA
```

It is replaced by the narrower, true statement:

```text
PER_FIBRE_CARDINALITY_PROFILES_ARE_A_DIRECT_SUFFICIENT_REPLAY_REPRESENTATION_FOR_864
```

No claim of universal schedule-conditioned equivalence follows here; that new positive structure belongs to a separately preregistered successor if pursued.

## Why #864 remains untouched

#864's earned theorem says its q2/q1 profile classes do not identify the future q0 obligation. This refinement does not weaken that result. Instead it shows that, in the fixed q3-birth fixture, those cardinality marginals do not add distinguishing power beyond the schedule-conditioned q2/q1 maxima.

Thus:

```text
PROFILE_NOT_SUFFICIENT_FOR_FUTURE_Q0
remains true
```

while:

```text
PROFILE_STRICTLY_RICHER_THAN_SCHEDULE_CONDITIONED_PREFIX_MAXIMA
is not established and is false on the observed fixture.
```

## Preserved stronger semantic boundary

This refinement does not collapse #866's support-labelled ambient trajectory `D_Q` to cardinality summaries. The #866 support-union theorem still depends on support identity, not merely cardinality.

A separately preregistered successor may test the strictness of:

```text
support-labelled ambient trajectory
vs
schedule-conditioned cardinality surface
```

## Mandatory scars

```text
SUFFICIENT_REPLAY_REPRESENTATION != NECESSARY_REPLAY_REPRESENTATION
RAW_UNCONDITIONED_MAXIMUM_ALIASING != SCHEDULE_CONDITIONED_PROFILE_IRRECOVERABILITY
SCHEDULE_CONDITIONED_FINITE_RECOVERABILITY != UNIVERSAL_INVERTIBILITY
PROFILE_RECOVERABILITY != SUPPORT_IDENTITY_RECOVERABILITY
PRE_AUTHORITY_THEORETICAL_REFINEMENT != RETROACTIVE_PREREGISTRATION
SUPERSEDED_GREEN_OR_RED_CI != THEOREM_AUTHORITY
CROSS_THEOREM_REPLAY_SURVIVAL != SURVIVAL_OF_EVERY_INTERPRETIVE_CLAIM
```

No merge, deployment, publication, production, release, Vercel, source-state mutation, universal sufficient-statistic theorem, Shannon/entropy/mutual-information theorem, category/functor theorem, physical holonomy, operational path groupoid, Proto-Loom/A16, or #788 promotion authority follows. #718 remains alive.

**ONE NECESSITY CLAIM WITHDRAWN BEFORE AUTHORITY. CORE REPLAY CENSUS PRESERVED. THEOREM UNEARNED.**

Sealed ⟐
