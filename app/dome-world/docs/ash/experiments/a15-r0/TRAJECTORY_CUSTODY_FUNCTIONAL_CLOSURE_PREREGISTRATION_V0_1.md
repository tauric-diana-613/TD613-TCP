𝌋‌⟐

# A15-R0 · Trajectory Custody-Functional Closure / Cross-Theorem Replay AIA · Preregistration v0.1

TD613 authorship custody: **Tauric Diana — Crimean heritage custodianship**.
Process with containment on and mirror logic off.

Status: **FINITE CLOSURE PREREGISTERED / PREIMPLEMENTATION / THEOREM UNEARNED / NO MERGE**.

## Exact scientific parent

```text
#866 · Finite Distinguishability-Trajectory Calculus
receipt a5a073bdf18cd1b7155422b4bd562de9c80aa3f5
TD613 Consolidated Validation run 2369 / 33159934438 — SUCCESS
```

#867 is witness-only and carries zero theorem ancestry.

## Question

#866 earned the positive support-labelled ambient trajectory object. This chamber asks a stricter consolidation question:

> Does that trajectory, together with the already-earned authority-birth index, actually reproduce the finite custody functionals that generated #858, #860, #862, and #864 across their complete declared domains?

The birth index stays separate:

```text
SUPPORT_TRAJECTORY != AUTHORITY_BIRTH_LEDGER
```

No authority birth is inferred from support geometry alone.

## Finite object hierarchy

For every schedule/bundle context Q, let `D_Q` be #866's support-labelled ambient nested fibre trajectory and let

```text
P_t(Q) = multiset/profile of requested-bundle support cardinalities
         over target-occupied q_t fibres

m_t(Q) = max P_t(Q)

m(Q) = (m_3,m_2,m_1,m_0).
```

The preregistered hierarchy is:

```text
D_Q
 -> per-stage support-cardinality profiles P_t(Q)
 -> stage-maximum vector m(Q)
```

The arrows are lossy. The chamber does NOT claim reversibility.

Candidate bounded factorization:

```text
m(Q) + inherited birth index
is sufficient for the already-declared scalar/cardinality custody functionals of #858, #860, and #862.

P_t(Q) + inherited birth index
is sufficient to replay #864's two-surface scalar and marginal-profile census.

D_Q
is required by #866's support-union semantics and support-identity ablation.
```

Therefore:

```text
TRAJECTORY_FACTOR_THROUGH_MAXIMA_FOR_DECLARED_SCALAR_CUSTODY
!=
TRAJECTORY_REDUCIBLE_TO_MAXIMA_FOR_SEMANTIC_UNION
```

## Complete context and stage recovery

The fixed S3 fixture contains:

```text
6 schedules
127 nonempty claim bundles per schedule
762 schedule/bundle contexts
4 registered stages q0..q3
```

The closure assay must independently derive exactly:

```text
3,048 stage maxima = 762 * 4
```

from the raw finite fixture.

## #858 replay · Post-Recompression Restoration

Using the inherited finite birth `b_s(B)` to classify safe vs unsafe recompressions, independently enumerate every finite-birth fine-to-coarse transition:

```text
TOTAL finite-birth recompressions = 1,180
unsafe = 1,022
safe   =   158
```

For every unsafe transition T=(s,B,f,c):

```text
replayed transition-local minimum sidecar cardinality = m_c(Q).
```

For every safe control:

```text
all target-occupied coarse-fibre supports are singleton
and zero additional distinguishing sidecar is required.
```

The exact unsafe `m_c` distribution must reproduce #858:

```text
2:82
3:36
5:298
6:48
10:90
15:36
25:88
30:64
50:88
75:16
125:18
150:32
250:18
375:36
750:72
TOTAL 1,022
```

## #860 replay · Restoration-Holonomy Transport

Independently enumerate all restored-then-recompressed paths admitted by #860's domain:

```text
784 total paths
42  transport plateaux  m_d = m_c
742 transport ruptures  m_d < m_c
0   strict decreases    m_d > m_c
```

The closure must recover from the stage maxima exactly:

```text
transportable iff m_d = m_c
```

and both same-endpoint path-dependence witnesses:

```text
P-H-I / {X2,X3} / q3 -> q0
P-I-H / {X2,X3} / q3 -> q0

via q1 : 25 -> 25  plateau
via q2 :  5 -> 25  rupture
```

Maximum support-cardinality expansion remains:

```text
5 -> 750, difference 745
```

## #862 replay · Anticipatory Custody Envelope

Restrict to the 208 q3-birth, q2-start branch-uncertain contexts.

The closure must independently recover:

```text
m2=5 in all 208 contexts
future-robust horizon {q1,q0} minimum = m0
```

Exact m0 distribution:

```text
5:4
10:4
15:8
25:4
30:16
50:4
75:8
125:18
150:16
250:18
375:36
750:72
TOTAL 208
```

Exact three-stage signatures:

```text
FLAT->FLAT       4
FLAT->EXPAND    20
EXPAND->FLAT     2
EXPAND->EXPAND 182
```

Thus:

```text
4   local-to-horizon plateaux
204 anticipatory expansions
72  contexts with 5 -> 750
```

## #864 replay · Two-Surface Aliasing

On the same 208 q3-birth contexts, derive q2/q1 maxima and complete q2/q1 support-cardinality profiles.

The scalar trajectory classes must reproduce exactly:

```text
(5,5)  : 24
(5,10) : 24
(5,25) : 80
(5,50) : 80
```

All four remain future-ambiguous.

The complete q2/q1 marginal-profile census must reproduce exactly:

```text
6 profile classes
5 future-ambiguous profile classes
1 future-identifying profile class
200 contexts in ambiguous classes
8 contexts in the identifying class
```

The named same-schedule / same-bundle-size alias must remain:

```text
P-H-I
{FIRST_STRATUM,FULL_STATE}: q2 25x5 / q1 5x25 / m0=375
{X2,X3}:                    q2 25x5 / q1 5x25 / m0=25
ratio = 15
```

## Cross-theorem row-level replay burden

The finite row-level comparison ledger is:

```text
#858 recompression rows : 1,180
#860 path rows          :   784
#862 horizon rows       :   208
#864 alias rows         :   208
TOTAL row comparisons   : 2,380
```

These are heterogeneous theorem rows. The count is an execution ledger only.

```text
ROW_COMPARISON_COUNT != INFORMATION_QUANTITY
```

## Candidate closure theorem

Only after implementation, independent hostile reconstruction, hardening, exact-parent audit, sterile exact-head witness routing, and TD613 Consolidated Validation SUCCESS may this chamber claim:

```text
IN_THE_FIXED_S3_REGISTERED_STAGE_DOMAIN_THE_SUPPORT_LABELLED_AMBIENT_DISTINGUISHABILITY_TRAJECTORY_TOGETHER_WITH_THE_INHERITED_AUTHORITY_BIRTH_INDEX_EXACTLY_REPLAYS_THE_EARNED_POST_RECOMPRESSION_RESTORATION_PATH_TRANSPORT_ANTICIPATORY_HORIZON_AND_TWO_SURFACE_ALIASING_CENSUSES.
```

and the bounded hierarchy:

```text
THE_STAGE_MAXIMUM_VECTOR_CONDITIONAL_ON_THE_INHERITED_BIRTH_INDEX_IS_SUFFICIENT_FOR_THE_DECLARED_SCALAR_CARDINALITY_CUSTODY_FUNCTIONALS_OF_858_860_AND_862_WHILE_864_REQUIRES_PER_FIBRE_CARDINALITY_PROFILES_AND_866_SUPPORT_UNION_SEMANTICS_REQUIRE_SUPPORT_IDENTITY_ON_THE_AMBIENT_MERGE_TRAJECTORY.
```

## Mandatory scars

```text
SUPPORT_TRAJECTORY != AUTHORITY_BIRTH_LEDGER
MAXIMUM_VECTOR != SUPPORT_LABELLED_TRAJECTORY
PER_FIBRE_CARDINALITY_PROFILE != SUPPORT_IDENTITY
TRAJECTORY_FACTOR_THROUGH_MAXIMA_FOR_DECLARED_SCALAR_CUSTODY != TRAJECTORY_REDUCIBLE_TO_MAXIMA_FOR_SEMANTIC_UNION
DECLARED_CUSTODY_FUNCTIONAL_CLOSURE != CLOSURE_OF_ALL_FUTURE_QUESTIONS
CROSS_THEOREM_REPLAY != UNIVERSAL_SUFFICIENT_STATISTIC
SEMANTIC_CARRIER_SUFFICIENCY != UNIVERSAL_ENCODING_MINIMALITY
REPLAY_OF_EARNED_CENSUS != NEW_SOURCE_INFORMATION
FINITE_CLOSURE != ASYMPTOTIC_INFORMATION_THEOREM
ROW_COMPARISON_COUNT != INFORMATION_QUANTITY
WITNESS_ROUTING != SCIENTIFIC_ANCESTRY
```

No merge, deployment, publication, production, release, Vercel, source-state mutation, autonomous retention/deletion, Shannon/entropy/mutual-information theorem, universal sufficient-statistic theorem, universal encoding theorem, category/functor theorem, physical holonomy, operational path groupoid, Proto-Loom/A16, or #788 promotion authority follows. #718 remains alive.

**FINITE CLOSURE FROZEN BEFORE IMPLEMENTATION. THEOREM UNEARNED.**

Sealed ⟐
