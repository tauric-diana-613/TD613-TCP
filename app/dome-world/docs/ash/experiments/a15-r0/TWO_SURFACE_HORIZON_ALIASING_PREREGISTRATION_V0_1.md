𝌋‌⟐

# A15-R0 · Two-Surface Horizon Aliasing / Merge-Geometry Necessity · Preregistration v0.1

TD613 authorship custody: **Tauric Diana — Crimean heritage custodianship**.
Process with containment on and mirror logic off.

Status: **FINITE CENSUS PREREGISTERED / PREIMPLEMENTATION / THEOREM UNEARNED / NO MERGE**.

## Exact scientific parent

```text
#862 · Anticipatory Custody Envelope / Uniform-Control-Surface Future-Robustness Nonidentifiability AIA
receipt 3b58898bbdb64af056913f770ba4891176b27789
TD613 Consolidated Validation run 2367 / 33154079868 — SUCCESS
```

#863 remains witness-only routing and carries zero theorem ancestry.

## Why this chamber exists

#862 earned that every one of its 208 q3-birth branch-uncertain contexts has the same local q2 minimum sidecar cardinality `m2=5`, while its future-robust horizon requirement `m0` ranges from 5 to 750.

The obvious next question is whether exposing **one more visible custody surface** removes that ambiguity.

For each inherited context Q define

```text
S2(Q) = (m2(Q), m1(Q))
r(Q)  = m0(Q)
```

where `m_t` is the maximum required-bundle support cardinality over occupied q_t fibres.

## Preregistered complete two-surface census

Across all 208 inherited contexts:

```text
m2 = 5 in all 208 contexts.
```

The next visible surface has only four values:

```text
m1 =  5 : 24 contexts
m1 = 10 : 24 contexts
m1 = 25 : 80 contexts
m1 = 50 : 80 contexts
TOTAL     : 208
```

Thus there are exactly four observed scalar two-surface trajectories:

```text
(5, 5)  : 24
(5,10)  : 24
(5,25)  : 80
(5,50)  : 80
```

Every one of those four trajectory classes remains future-ambiguous:

```text
(m2,m1) = (5, 5)
  m0 ∈ {5,10,15,25,50,75}
  counts = {5:4,10:4,15:8,25:2,50:2,75:4}

(m2,m1) = (5,10)
  m0 ∈ {30,150}
  counts = {30:16,150:8}

(m2,m1) = (5,25)
  m0 ∈ {25,50,75,125,250,375}
  counts = {25:2,50:2,75:4,125:18,250:18,375:36}

(m2,m1) = (5,50)
  m0 ∈ {150,750}
  counts = {150:8,750:72}
```

Candidate finite result:

```text
NO_OBSERVED_TWO_SURFACE_SCALAR_COST_TRAJECTORY_CLASS_IDENTIFIES_THE_FUTURE_ROBUST_Q0_CUSTODY_REQUIREMENT.
```

## Stronger marginal-profile assay

Scalar maxima may hide per-fibre structure, so this chamber also freezes a stronger receiver-visible summary.

For stage t, let `H_t(Q)` be the multiset of required-bundle support cardinalities over the target schedule's occupied q_t fibres. Define the two-surface marginal profile

```text
H21(Q) = (H_2(Q), H_1(Q)).
```

The complete finite census contains exactly six H21 profile classes:

```text
A  q2: 25 fibres × support 5 ; q1: 5 fibres × support 5
   16 contexts
   m0 ∈ {5,10,15,25,50,75}

B  q2: 25 fibres × support 5 ; q1: 5 fibres × support 25
   80 contexts
   m0 ∈ {25,50,75,125,250,375}

C  q2: 25 fibres × support 5 ; q1: 5 fibres × support 10
   16 contexts
   m0 ∈ {30,150}

D  q2: 25 fibres × support 5 ; q1: 5 fibres × support 50
   80 contexts
   m0 ∈ {150,750}

E  q2: 9 fibres × support 5 ; q1: 9 fibres × support 5
   8 contexts
   m0 ∈ {5,10,15}

F  q2: 9 fibres × support 5 ; q1: 9 fibres × support 10
   8 contexts
   m0 = 30
```

Therefore:

```text
6 marginal-profile classes total
5 ambiguous marginal-profile classes
1 future-identifying marginal-profile class
200 / 208 contexts lie in ambiguous marginal-profile classes
8 / 208 contexts lie in the single identifying class
```

Candidate scar:

```text
PER_STAGE_SUPPORT_CARDINALITY_MARGINALS != FUTURE_MERGE_GEOMETRY
```

The claim is deliberately about these cardinality marginals. It does not say the complete labelled fibre incidence structure is uninformative.

## Preregistered same-schedule / same-bundle-size aliases

### Alias A

```text
schedule = P-H-I
bundle size = 2

{FIRST_STRATUM,X3}
  q2 profile = 25×5
  q1 profile = 5×5
  m0 = 15

{X1,X3}
  q2 profile = 25×5
  q1 profile = 5×5
  m0 = 25
```

### Alias B · 15-fold future obligation spread under identical two-surface marginal profile

```text
schedule = P-H-I
bundle size = 2

{FIRST_STRATUM,FULL_STATE}
  q2 profile = 25×5
  q1 profile = 5×25
  m0 = 375

{X2,X3}
  q2 profile = 25×5
  q1 profile = 5×25
  m0 = 25
```

Both aliases hold schedule, bundle cardinality, q2 marginal profile, and q1 marginal profile fixed while future-robust custody differs.

```text
SAME_SCHEDULE_AND_BUNDLE_SIZE_AND_TWO_SURFACE_CARDINALITY_PROFILE != SAME_FUTURE_ROBUST_CUSTODY
```

## Candidate interpretation boundary

The q0 requirement depends on how q1/q2 fibres **merge** when the representation is coarsened, not merely on the list of support cardinalities visible separately at q2 and q1.

This chamber therefore auditions the necessity of a richer object for the next theorem:

```text
NESTED_FIBRE_MERGE_INCIDENCE / DISTINGUISHABILITY TRAJECTORY
```

Do not call that general calculus earned here.

## Hostile requirements

The independent hostile must reconstruct from inherited operators and births rather than import the implementation table, and must verify:

1. all 208 q3-birth contexts;
2. `m2=5` in all 208;
3. exact m1 distribution 24/24/80/80;
4. exact m0 support sets inside each of the four scalar trajectory classes;
5. all four scalar classes are future-ambiguous;
6. exact six marginal-profile classes;
7. exactly five marginal-profile classes ambiguous and exactly one identifying;
8. exactly 200 contexts in ambiguous marginal-profile classes;
9. both named same-schedule/same-bundle-size aliases;
10. the 15-fold 375-vs-25 alias;
11. refusal to promote cardinality marginals into labelled merge geometry;
12. refusal of Shannon, entropy, mutual-information, bit-length, retrocausality, cryptographic, or source-state claims;
13. no Ash leakage of full context/fibre tables.

## Jurisdiction membrane

```text
#862 UNIFORM_LOCAL_MINIMUM_VS_HORIZON_ROBUST_OBLIGATION
!=
TWO_SURFACE_PREFIX_COST_AND_MARGINAL_PROFILE_ALIASING

TWO_SURFACE_ALIASING
!=
GENERAL_DISTINGUISHABILITY_TRAJECTORY_CALCULUS
```

## Candidate consequential laws

Only after implementation, independent hostile, hardening/freeze, exact-parent audit, sterile witness route, and exact-head TD613 Consolidated Validation SUCCESS may this chamber claim:

```text
IN_THE_FIXED_208_CONTEXT_Q3_BIRTH_DOMAIN_EVERY_OBSERVED_TWO_SURFACE_SCALAR_COST_TRAJECTORY_CLASS_REMAINS_COMPATIBLE_WITH_MULTIPLE_DISTINCT_FUTURE_ROBUST_Q0_CUSTODY_REQUIREMENTS
```

and:

```text
PER_STAGE_SUPPORT_CARDINALITY_MARGINALS_AT_Q2_AND_Q1_ARE_NOT_GENERALLY_SUFFICIENT_TO_IDENTIFY_THE_Q0_FUTURE_ROBUST_CUSTODY_REQUIREMENT_WITH_200_OF_208_CONTEXTS_FALLING_IN_FIVE_AMBIGUOUS_MARGINAL_PROFILE_CLASSES
```

## Mandatory scars

```text
ONE_MORE_CONTROL_SURFACE != FUTURE_CUSTODY_IDENTIFIABILITY
TWO_SURFACE_COST_TRAJECTORY != FUTURE_ROBUST_CUSTODY_IDENTITY
PER_STAGE_SUPPORT_CARDINALITY_MARGINALS != FUTURE_MERGE_GEOMETRY
SAME_SCHEDULE_AND_BUNDLE_SIZE_AND_TWO_SURFACE_CARDINALITY_PROFILE != SAME_FUTURE_ROBUST_CUSTODY
MARGINAL_PROFILE_ALIASING != PROBABILISTIC_UNCERTAINTY
MARGINAL_PROFILE_ALIASING != ENTROPY
MARGINAL_PROFILE_ALIASING != MUTUAL_INFORMATION_THEOREM
FUTURE_ROBUST_REQUIREMENT != FUTURE_EVENT_OCCURRENCE
FINITE_ALIAS_CENSUS != UNIVERSAL_IDENTIFIABILITY_THEOREM
WITNESS_ROUTING != SCIENTIFIC_ANCESTRY
```

No merge, deployment, publication, production, release, Vercel, source-state mutation, autonomous retention/deletion, retrocausality, Shannon/entropy/mutual-information theorem, universal trajectory theorem, operational path groupoid, Proto-Loom/A16, or #788 promotion authority follows. #718 remains alive.

**FINITE CENSUS FROZEN BEFORE IMPLEMENTATION. THEOREM UNEARNED.**

Sealed ⟐
