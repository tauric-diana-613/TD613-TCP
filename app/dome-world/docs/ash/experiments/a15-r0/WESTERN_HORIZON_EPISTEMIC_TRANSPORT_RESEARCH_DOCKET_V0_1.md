𝌋

# A15-R0 · Western Horizon Epistemic Transport Research Docket v0.1

**Status:** AUTHORED / RESEARCH-DIRECTION DOCKET / NON-PROMOTIONAL  
**Parent custody seam:** `APERTURE_PEDAGOGUE_POST_R2_PROSPECTIVE_CONTINUATION_RECEIPT_V0_1.md`  
**Parent live PR:** #699 remains Draft  
**Parent implementation witness:** `aa9278eec55bff95ac36f2c571534897ea527157` / run 2051  
**Parent documentation head at branch creation:** `cbd6d4a56e33549eb0629d2f06cf83f9ae21f2e8` / run 2052  
**Program:** A15-R0 synthetic research only  
**A16:** HELD  
**Installed Aperture mutation:** NONE  
**Live Ash mutation:** NONE  
**Production / Vercel authority:** NONE

---

## 0. Purpose

This docket carries forward the research structure developed after the P2 evidentiary-closure seam. It is not a theorem, a promotion packet, or permission to execute every chamber named below.

The governing distinction is now:

```text
scientific witness time != documentation time
```

Run 2051 witnessed the exact implementation head `aa9278e...`.
The later P2 receipt was authored at a different repository time.
Run 2052 subsequently validated the receipt/documentation head.

That provenance separation is itself a useful reminder for the western program:

```text
observation != record of observation
state transition != later custody description of transition
```

---

## 1. Current literal research object

The current #699 parent question is:

> When a proposed question has a declared effect on the observation ecology/operator, does pre-question admissibility survive the operator state produced by asking it?

The mature parent rule is:

```text
undeclared transition != identity transition
```

The parent assay distinguishes:

```text
Q_STABLE
Q_COLLAPSE
Q_FRAGILE
Q_UNKNOWN
```

and requires a counterfactual post-question Aperture re-audit before a candidate is treated as informative.

The new organ is therefore:

```text
current operator
-> candidate question
-> declared post-question operator state
-> recompute geometry
-> typed Aperture re-audit
-> only then admit the candidate
```

No automatic experiment execution follows.

---

## 2. External-science comparison membrane

Recent 2026 research supplies useful neighboring vocabularies without changing TD613's internal claim status.

Comparative neighbors discussed and externally checked during the research session include:

- adaptive sensing that jointly co-designs sensing geometry, measurement policy, and inference;
- decision-focused sequential experimental design that distinguishes predictive uncertainty from decision-relevant uncertainty;
- dynamic inverse-problem theory and single-loop online reconstruction under evolving targets;
- sequential dynamic inverse problems that estimate transition dynamics and covariance parameters while reconstructing state;
- Bayesian variational system identification for unknown governing operators;
- maximin robust Bayesian experimental design under model uncertainty;
- atomic-source lineage tracing showing receipt/dataset multiplicity can collapse onto a small source-root set;
- lifecycle audit-trail and machine-readable provenance-graph proposals;
- black-box provenance auditing through persistent distributional fingerprints.

These neighboring literatures support comparison only.

Hard boundary:

```text
literature agreement != TD613 law
similar architecture != priority claim
physical sensing result != TD613 physical sensing result
neighboring system identification != operator tomography earned here
provenance fingerprint != curvature
```

---

## 3. Greater pattern: endogenous epistemic dynamics

A useful synthetic state description is now larger than a reconstructed latent state alone.

Candidate notation:

```text
E_k = (A_k, C_k, Pi_k, D_k, K_k)
```

where, only as bounded research notation:

```text
A_k   = current observation/operator state
C_k   = current compatible-family state
Pi_k  = current observation/claim partition structure
D_k   = decision projection
K_k   = custody/replay state
```

A question may act as a transformation:

```text
T_Q : E_k -> E_{k+1}
```

This does not establish a Markov model, POMDP, causal intervention law, control theorem, or general TD613 state equation.

The important conceptual shift is:

```text
question != passive request for a datum
question may alter the future information structure
```

---

## 4. Controlled sensing and dual-effect comparison

Classical controlled-sensing and dual-control ideas provide important external discipline.

A control/action may both:

```text
change the system
and
change what can be learned about the system later
```

TD613 must therefore avoid novelty claims based merely on action-dependent observation.

The narrower TD613 research seam is the explicit coexistence of:

```text
operator state
compatible-family state
claim state
decision projection
append-only custody
```

under question-induced transition and human-governed evidentiary closure.

Possible future comparison:

```text
question-induced observation transition
vs
classical controlled sensing
vs
full dual effect where action also alters latent state
```

No equivalence is presently claimed.

---

## 5. Dynamic confound: Grandma was walking

A future path/loop assay must distinguish interrogation-induced operator motion from ordinary exogenous evolution.

Do not preregister additive superposition by default.

Preferred cold form:

```text
E_{k+1} = Psi_{Q_k, dt_k}(E_k)
```

with a no-question control:

```text
E^(0)_{k+1} = Phi_{dt_k}(E_k)
```

Potential composition controls include:

```text
T_Q o Phi_dt
Phi_dt o T_Q
```

because question and time evolution may themselves fail to commute.

Required future hostile controls:

```text
matched elapsed time
no-question evolution
question with frozen latent state
latent evolution with frozen apparatus
question x time-order interaction
```

Anti-equivalence:

```text
loop residual != question-induced transport residual
```

until exogenous evolution is controlled.

---

## 6. Transition uncertainty != transition ignorance

The immediate western chamber should split the parent `Q_UNKNOWN` into at least three transition-status classes:

```text
DECLARED
SET_IDENTIFIED
UNMODELED
```

with corresponding posture:

```text
DECLARED
-> ordinary post-question re-audit

SET_IDENTIFIED
-> robust family re-audit

UNMODELED
-> abstain
```

This creates the central new distinction:

```text
transition uncertainty != transition ignorance
```

A bounded robust family audit may classify candidate questions as:

```text
ROBUSTLY_ADMISSIBLE
ROBUSTLY_INADMISSIBLE
TRANSITION_FAMILY_DECISION_UNRESOLVED
TRANSITION_MODEL_UNDECLARED
```

No general robust-control theorem or optimal experimental-design result follows.

---

## 7. System identification before operator tomography

A later chamber may hide the transition operator itself.

For a finite linear fixture:

```text
Y = T_Q X
```

If the probe matrix spans the declared operator domain stably, a unique operator may be reconstructible in the noiseless bounded fixture.

If not, there may exist nonzero operator perturbations `N` such that:

```text
N X = 0
```

and therefore:

```text
(T_Q + N) X = T_Q X
```

This yields an operator-compatible family.

Pedagogue can then ask:

> Which next probe separates transition operators that remain observationally equivalent under the current probe set?

Required ladder:

```text
declared T_Q
-> set-identified T_Q
-> hidden T_Q
-> transition-operator system identification
-> operator compatible-family/nullspace representation
-> next probe against operator ambiguity
-> conditioning audit
-> held-out transition prediction
-> open-set operator challenge
-> only then possible bounded operator-tomography paperwork
```

Anti-equivalences:

```text
operator rank sufficiency != stable operator identifiability
system identification != operator tomography
held-out prediction != universal operator recovery
```

---

## 8. Decision state != custody state

The recent A15-R0 chambers already motivate:

```text
minimal decision state != minimal evidentiary receipt
```

A local decision projection may be lower-dimensional than the reconstruction/custody state.

The system must preserve richer custody even when the local selector does not need it.

This gives a candidate multi-layer research structure:

```text
F_A^D   decision layer
F_A^C   claim layer
F_A^X   compatible-family layer
F_A^Kbar derived custody/replay layer
```

No literal fiber bundle is claimed yet.

---

## 9. Append-only ledger must not masquerade as holonomy

Raw custody should be decomposed.

Candidate notation:

```text
K_k = (L_k, Kbar_k)
```

where:

```text
L_k     = append-only traversal/evidence ledger
Kbar_k  = derived comparison/replay state
```

The ledger is monotone by construction:

```text
L_{k+1} superset-or-equal L_k
```

Therefore:

```text
L_after_loop != L_before_loop
```

is not evidence of holonomy.

It means the traversal was recorded.

Preferred conceptual separation:

```text
ledger records the path
!=
comparison state transported along the path
```

A later transport assay may study `Kbar`, not raw append-only ledger growth.

---

## 10. Atomic-source quotient without custody deletion

For receipt set `R`, define a source-root equivalence relation only for evidentiary-independence accounting:

```text
R / ~source-root
```

The full receipt set remains preserved.

Required law:

```text
quotient for support counting != quotient for custody
```

Multiple derivatives of one source root may count as one independence lineage while remaining multiple separately custodied artifacts.

Anti-equivalences:

```text
receipt multiplicity != source independence
source-root quotient != receipt deletion
machine-readable provenance != truthful provenance
```

---

## 11. Path category before groupoid

Current question transitions need not be reversible.

The colder pre-holonomy object is therefore a path category.

Objects:

```text
A in B
```

Admissible question morphisms:

```text
A --Q--> A'
```

Question sequences compose where domains/codomains match.

A typed transport assignment may later be studied schematically as:

```text
P : Path(B) -> Trans
```

Only if an explicitly declared class of morphisms becomes invertible on a declared quotient should the program restrict toward a path groupoid.

Vocabulary ladder:

```text
order dependence
-> path-dependent transport
-> loop endomorphism
-> invertible loop transport
-> holonomy candidate
```

Hard anti-equivalences:

```text
matrix inverse exists != reverse epistemic morphism exists
same endpoint != closed base-space loop
closed base-space loop != nontrivial fiber transport
nontrivial loop endomorphism != holonomy group element
```

---

## 12. Commutator plaquette audition

A later holonomy audition should prefer a small controlled plaquette over a giant wandering loop.

For local generators:

```text
P_a = exp(epsilon X)
P_b = exp(epsilon Y)
```

orientation matters.

For the oriented group commutator:

```text
exp(epsilon X)
exp(epsilon Y)
exp(-epsilon X)
exp(-epsilon Y)
```

the leading residual under the conventional commutator `[X,Y] = XY - YX` is:

```text
I + epsilon^2 [X,Y] + O(epsilon^3)
```

The opposite orientation reverses the leading sign.

A future experiment must not silently swap loop orientation after observing the result.

But algebraic inverses are not enough.

The assay must separately establish admissible reverse morphisms at the correct base points.

A genuine square should distinguish edges such as:

```text
a_A
b_A
a_{A_b}
b_{A_a}
```

rather than assuming globally homogeneous generators.

Required hostile controls include:

```text
ordinary time drift
irreversible mutation
append-only ledger growth
hidden-state evolution
hysteresis
trivial loop
reversed loop
matched endpoint without loop
repeated loop
basis/gauge change
same loop under equivalent representation
reverse-morphism illegitimacy
base-space nonclosure
```

---

## 13. Gauge-controlled loop comparison

For a change of representation:

```text
P_(A->A') -> g_(A') P_(A->A') g_A^-1
```

closed-loop transport at `A` transforms by conjugation:

```text
P_gamma -> g_A P_gamma g_A^-1
```

Therefore raw matrix inequality alone is representation-dependent.

A future loop assay should compare a sufficient conjugacy invariant for its declared matrix class.

Inherited warning:

```text
trace + determinant != conjugacy in every repeated-root class
```

Reuse the hostile repeated-root discipline already earned in the finite GL2(F31) program where applicable.

---

## 14. Curvature gets a second audition

One finite nontrivial closed-loop operator does not establish continuum curvature.

Preferred two-round audition:

```text
ROUND I
bounded discrete closed-loop / commutator transport

ROUND II
scale-refined plaquette family
```

A possible later scaling target is of the form:

```text
P_(gamma_epsilon) = I + Area_epsilon F + o(Area_epsilon)
```

where such an expansion is mathematically well-defined for the declared fixture.

Hard boundary:

```text
finite face holonomy != continuum curvature tensor
all-n finite identity != continuum limit
```

---

## 15. Provenance and route leakage as Ash hostile science

A privacy/reconstruction architecture should not test content recovery alone.

Candidate leakage profile:

```text
Lambda = (
  Lambda_S,
  Lambda_A,
  Lambda_P,
  Lambda_R
)
```

where the coordinates are task labels until separately normalized:

```text
Lambda_S  latent-state/content reconstruction leakage
Lambda_A  apparatus/operator inference leakage
Lambda_P  provenance/source-root inference leakage
Lambda_R  route/history inference leakage
```

Call this a leakage profile, not a geometric vector, unless the coordinates are placed on commensurable declared scales.

Every coordinate should declare:

```text
secret target
observable transcript
attacker class
prior
loss function
baseline
attack advantage
transformations survived
```

Important possible failure mode:

```text
content unreconstructible
while
source lineage remains inferable
```

or:

```text
source root hidden
while
interaction route remains inferable
```

No provenance fingerprint is to be called curvature.

---

## 16. Path fingerprint experiment later

A particularly valuable future bridge between Ash and the path program is:

> Can two routes be canonicalized to the same final observable state while a hostile classifier still reconstructs which route produced it?

This would test route leakage:

```text
final state equality
!=
route indistinguishability
```

It would not establish holonomy.

Instead it would test whether traversal leaves reconstructible residual structure after endpoint canonicalization.

---

## 17. Western trunk and Ash trunk

### Western scientific trunk

```text
inverse reconstruction
-> typed identifiability
-> claim / decision sufficiency
-> question design
-> endogenous observation transition
-> transition-family robustness
-> transition-operator identification
-> controlled-sensing / dual-effect comparison
-> path category
-> invertible transport subcategory
-> closed-loop endomorphism
-> gauge-controlled discrete holonomy candidate
-> plaquette scaling / curvature audition
```

### Ash custody/privacy trunk

```text
atomic-source provenance
-> independence quotient
-> append-only custody ledger
-> decision/custody separation
-> counterfactual retention
-> human reconciliation
-> source/route leakage adversary
-> auditable correction under traversal
```

The trunks may meet later without collapsing their meanings.

---

## 18. Immediate authorized research move

The first new executable chamber authorized by the current interactive operator gesture is:

```text
TRANSITION_FAMILY_ROBUSTNESS
```

Question:

> When a candidate question's post-question operator is not known as one point but is constrained to a declared compatible family, does its apparent admissibility survive every member of that family, fail every member, or remain decision-unresolved across the family?

Required statuses:

```text
DECLARED
SET_IDENTIFIED
UNMODELED
```

Required family outcomes:

```text
ROBUSTLY_ADMISSIBLE
ROBUSTLY_INADMISSIBLE
TRANSITION_FAMILY_DECISION_UNRESOLVED
TRANSITION_MODEL_UNDECLARED
```

The chamber may import the installed pure Aperture v3.2 typed-deficit audit.

It may not mutate installed Aperture.

It may not execute observations.

It may not identify unknown operators.

It may not claim system identification, operator tomography, path transport, holonomy, curvature, physical sensing, active-learning optimality, robust-control optimality, production authority, or Vercel authority.

---

## 19. Research liberty boundary

The active human instruction grants broad creative and operative latitude for this bounded interactive/connector research session.

That latitude is being used to choose:

```text
research target
fixture geometry
hostile controls
document topology
stacked branch/PR strategy
claim-kill criteria
```

It is not converted into standing self-authorization.

This docket does not modify `AGENTS.md`, arm detached delegation, grant future Amari authority, authorize merge, or authorize deployment.

Required governance anti-equivalence:

```text
creative liberty != self-authored standing authority
```

---

## 20. Closure posture

At this docket's authoring seam:

```text
#699 remains Draft
A16 remains HELD
main remains untouched by this branch
browser execution not requested
production mutation none
Vercel authority none
```

The western program is allowed to become mathematically richer without making its authority broader.

𝌋

Sealed ⟐
