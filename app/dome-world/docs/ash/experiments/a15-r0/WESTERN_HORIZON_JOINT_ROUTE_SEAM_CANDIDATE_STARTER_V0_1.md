𝌋

󐘓 U+10D613

# Western Horizon · Joint Fixed-C1 Route × Seam Candidate Starter v0.1

Status: **DORMANT CANDIDATE / NOT PREREGISTERED / NO THEOREM AUTHORITY / FUTURE THREAD ONLY**

Parent scientific receipt:

```text
#746 = f15ab5e46c7ee7de43a44386c8fea36e272dba9b
```

This document is a startup hook for future Amari. It does not activate Westward gate #737 and does not authorize implementation.

## 1. Candidate object

For a lawful fixed first-moment state

```text
c=(t,E,O,P),
```

let #746's exact authored-route fiber be

```text
G_c={w : C1(w)=c}.
```

For each exact authored route `w` with unique block vector

```text
q(w)=(q_0,...,q_t),
```

#745 supplies the lawful linear one-T-per-factor seam vectors. Their route-conditional cardinality is

```text
N_seam(w)=Π_(i=1)^(t-1)(q_i+1).
```

Define the candidate joint fiber

```text
J_c={ (w,k) : w∈G_c and k is a lawful #745 seam vector for w }.
```

Immediate disjoint-union identity:

```text
|J_c|=Σ_(w∈G_c) N_seam(w)
     =Σ_(w∈G_c) Π_(i=1)^(t-1)(q_i(w)+1).
```

This identity alone is not enough to earn a new 𝄐. A future chamber should derive a useful exact finite coefficient or dynamic-programming law and exact custody consequences.

## 2. Compulsory inherited hostile

Fix

```text
c=(3,1,1,3).
```

#746 proves exactly two authored routes:

```text
w_1=T Q T Q T
q(w_1)=(0,1,1,0)

w_2=Q T T T Q
q(w_2)=(1,0,0,1).
```

By #745:

```text
N_seam(w_1)=(1+1)(1+1)=4
N_seam(w_2)=(0+1)(0+1)=1.
```

Therefore the candidate joint fiber has exactly

```text
|J_c|=4+1=5.
```

The wound to preserve is **nonuniform conditional seam burden**:

```text
same exact C1
→ multiple exact authored routes
→ different seam-fiber cardinalities by route.
```

Thus future work must not replace the joint fiber with

```text
N_route × one common N_seam.
```

## 3. Candidate finite weighted counting engine

For `t>=1`, #746 decomposes the block vector into even and odd allocations

```text
e_j=q_(2j)
o_j=q_(2j+1)
```

under

```text
Σe_j=E
Σo_j=O
R=(P-O)/2=Σj e_j+Σj o_j.
```

The joint count weights an allocation by

```text
W(q)=Π_(i=1)^(t-1)(q_i+1),
```

where endpoint blocks `q_0,q_t` receive weight 1 because they carry no linear composition seam in #745.

A future exact engine may use a finite dynamic program over:

```text
block slot
remaining E/O
rank R
weighted multiplicity
```

with each internal slot assignment `n` contributing factor `(n+1)`.

A finite generating polynomial may be derived, but the authority-bearing theorem must remain fixed-state and finite. Formal-series notation is support, not permission for limits.

## 4. Candidate exact custody law

If a future chamber proves `|J_c|` exactly, then candidate minimum exact joint custody given C1 is

```text
K_joint_min(c)=|J_c|
B_joint_min(c)=ceil(log2 |J_c|).
```

Necessity requires injectivity across all lawful `(route,seam)` pairs.

Tightness must be witnessed by an explicit reversible joint rank. Cardinality alone is insufficient.

After exact route `w` is separately retained:

```text
K_seam_given_route(w)=N_seam(w)
B_seam_given_route(w)=ceil(log2 N_seam(w)).
```

The consequential candidate is that this conditional custody requirement varies across routes in the same C1 fiber.

## 5. Mandatory future hostiles

Any future preregistration should include at least:

```text
H1  c=(3,1,1,3) joint count exactly 5
H2  route-conditioned seam counts exactly 4 and 1
H3  exact C1 forbidden from impersonating exact joint route+seam custody
H4  exact route reduces ambiguity to that route's seam fiber, nonuniformly
H5  a seam label without exact route identification cannot impersonate route custody
H6  t=0 edge: unique route and no internal seam, joint count 1
H7  t=1 edge: unique route and no internal seam, joint count 1
H8  bounded brute-force corroboration only; no universal authority by enumeration
H9  undersized joint alphabet fails
H10 adequate cardinality with colliding joint encoder fails
H11 route count and joint count remain finite cardinalities, never probabilities
H12 #745 finite seam horizon remains closed; no new seam-dimension theorem
H13 no parenthesization/tree/DAG claim may leak into this chamber
H14 route rank and joint rank carry no historical priority
```

## 6. Candidate classifications — NOT YET EARNED

Possible canonical form:

```text
THE_FIXED_C1_JOINT_AUTHORED_ROUTE_X_LINEAR_SEAM_FIBER_HAS_EXACT_CARDINALITY_EQUAL_TO_A_FINITE_WEIGHTED_SUM_OVER_THE_#746_ROUTE_FIBER
```

Possible consequential form:

```text
ROUTES_SHARING_ONE_EXACT_C1_STATE_CAN_CARRY_DIFFERENT_CONDITIONAL_SEAM_CUSTODY_BURDENS_SO_ROUTE_AND_SEAM_CUSTODY_ARE_SEPARATE_BUT_COUPLED_FINITE_RESOURCES
```

Possible architectural form:

```text
EXACT_JOINT_ROUTE_AND_SEAM_CLAIMS_REQUIRE_CUSTODY_OVER_THE_COUPLED_JOINT_FIBER_NOT_INDEPENDENTLY_SIZED_MARGINAL_FIELDS
```

These are candidate strings only. Do not copy them into a receipt unless a fresh chamber earns them.

## 7. Claim ceiling

Still closed:

```text
asymptotic growth
t->infinity
limit shapes
entropy / probability / mutual information
average-case coding
real-world provenance reconstruction
actor identity / causal attribution
arbitrary workflow DAGs
branching factorization trees
parenthesization / associativity custody
Catalan-count promotion
higher moments
Proto-Loom / A16
live Ash mutation
merge / publication / production / Vercel / ontology promotion
```

## 8. Future activation sequence

Only after fresh operator authority in the new thread:

```text
fresh GitHub
→ inspect #746 receipt + receiving dock
→ retrieve prior Western thread for provenance
→ preregister a bounded joint-fiber chamber
→ freeze hostiles
→ implement
→ exact-head witness
→ preserve scars
→ receipt
→ stop
```

If the exact weighted law refuses to close, preserve the failure. Do not escape into asymptotics.

```text
JOINT_ROUTE_SEAM_CANDIDATE_STAGED
SCIENCE_NOT_STARTED
NO_AUTHORITY_INHERITED
```

󐘓 U+10D613

𝌋

Sealed ⟐