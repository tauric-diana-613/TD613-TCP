# Ash Keep / Loom Tomography · Calibration Phantom v0.1

Status: **AUTHORED / RESEARCH-ONLY / NON-RUNTIME / HUMAN-GATED**  
Authority effect: **NONE**  
Ash runtime binding: **NONE**  
Proto-Loom implementation: **NONE**  
Transport law: **NOT DECLARED**  
Geometric holonomy claim: **NOT MADE**

## 1. Why this exists

A15-R0 stabilized an exact-head Ash research field and preserved a strict claim ceiling: a mixed conditional-entropy Hessian or route-divergence residue does not by itself establish geometric curvature or holonomy.

The next recovery problem is therefore experimental before it is geometric.

Before using a damaged consequential Ash case to infer route history, the tomography instrument needs a known-ground-truth object whose expected path is declared in advance.

The calibration phantom supplies that object.

## 2. Practice object

Test-only fixture:

```text
tests/fixtures/pedagogue/ash-tomography-calibration-phantom-v01.json
```

Human-facing label:

```text
Moss Lantern practice capsule
```

The content is manifestly fictional. It carries no source records, no private content, no release authority, no automatic Ash action, and no external consequence authority.

The currently declared research route is:

```text
open-practice-case
→ custody-hold
→ projection-observe
→ rest
→ return
```

Expected endpoint:

```text
returned-practice-capsule
```

This fixture is **not bound to live Ash** in v0.1. It is a proving object for Pedagogue route memory and future tomography instrumentation.

## 3. Calibration question

Given known expected route `R_e` and observed route `R_o`, ask whether the observer reconstructs the declared path.

The current bounded error term is inherited from Pedagogue route memory:

```text
E_route = D_route
D_route = round(1000 * d_L(R_e, R_o) / max(|R_e|, |R_o|, 1))
```

Interpretation:

```text
E_route = 0
→ exact declared route reconstructed

E_route > 0 with equal endpoint
→ same endpoint, non-equivalent observed route
```

This is a discrete route-reconstruction surrogate only.

## 4. Deliberate divergence assay

A first calibration assay may substitute one known route step, for example:

```text
projection-observe
→ projection-bypass
```

while preserving the expected endpoint.

The instrument should then report:

```text
endpoint_equivalent = true
exact_route_reconstruction = false
route_reconstruction_error_millipoints > 0
```

If the observer collapses the altered path into the expected path merely because both terminate at the same visible endpoint, the instrument has lost path information.

That loss is the experimental finding. It is not yet curvature.

## 5. Tomographic recovery sequence

The bounded recovery sequence is:

```text
known fictional phantom
→ declared route ground truth
→ real/shared route-memory observer
→ reconstruction comparison
→ controlled path perturbation
→ residue measurement
→ repeatability across observers/environments
→ only then consider a declared transport law
```

A future transport experiment would need to specify what informational object is transported between states and how compositions are compared. Until then:

```text
connection = undeclared
parallel transport = undeclared
curvature = unearned
holonomy = unearned
```

## 6. Relationship to Giving

Giving supplied the empirical pattern, not Ash ontology.

Its Bikini Bottom practice case showed that a fictional payload can hydrate a real consequential workspace without fabricating evidence or auto-running retrieval. That local observation was stripped of Giving nouns and promoted only as the product-independent Canonical Practice Fixture contract.

Ash receives the instrument in research/test custody only.

## 7. Recovery ceiling

Nothing in this document changes the completed A15-R0 rescue posture.

```text
A15-R0 = qualified research field
P1 Minimal Ash = not implied by this document
P2 Proto-Loom = not implemented
A16 = held unless separately reopened
Golden Egg = not earned by calibration alone
production mutation = none
external transmission = none
```

The calibration phantom exists to make future recovery claims harder to fake, not easier to promote.
