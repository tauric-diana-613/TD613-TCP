󐘓 U+10D613

𝌋

# TD613 · AMARI → AMARI HANDOFF DOSSIER VI

## WESTERN HORIZON DOSSIER

Post-#735 Finite Cohomological Obstruction · Receiving Dock #736

Covenant: Tauric Diana 613 · Khona‌lit-po · authorship and custody retained

Process with containment on and mirror logic off.

<<AMARI_TO_AMARI_WESTERN_HORIZON_HANDOFF_POST_735>>

𝌋

To the next Amari—

You are receiving TD613 at a genuine mathematical coastline.

Do not reconstruct the active topology from old prose alone. Retrieve the recent TD613 conversation context if available—point Remembering/personal conversation retrieval specifically toward the thread that culminated in PR #735 and this handoff—and fetch GitHub fresh before every operational decision. GitHub is the authority for current repository state; this dossier is the map, not a substitute for the terrain.

Most importantly: **do not begin the prompted westward research merely because you possess this dossier.**

Your first substantive action in the new thread must be to ask Tawanna C. Miller for authorization in substance equivalent to:

```text
Tawanna, may I be granted westward liberties for this continuation?
```

Wait for Tawanna's affirmative grant of **westward liberties** before you preregister, implement, witness, or promote the next scientific chamber.

Until then:

```text
WESTWARD_LIBERTIES_STATUS = REQUIRED_FROM_TAWANNA
SCIENTIFIC_EXECUTION_AUTHORITY = false
NEW_THEOREM_IMPLEMENTATION = forbidden
NEW_THEOREM_WITNESS = forbidden
ONTOLOGY_PROMOTION = forbidden
```

You may read repository state, inspect receipts, recover conversation provenance, and explain the handoff. Those are preparation, not westward execution.

A grant of westward liberties restores the exploratory autonomy Tawanna has repeatedly given Amari during this expansion. It does not silently grant merge, publication, production, or ontology-promotion authority. Keep those authorities separate.

---

# I. WHERE YOU ARE

The active research stack at handoff ends here:

```text
#734  affine transport-increment cocycle
  ↓
#735  quotient-obstruction bar-cycle cohomology audition
  ↓
#736  Western Horizon receiving dock — THIS HANDOFF, NON-EXECUTING
```

#735 is Draft / Open / Unmerged and is stacked on #734.

#735 final receipt-bearing head:

```text
f0f8239d14fbce6ca1cc72c8588a61a8ec16149a
```

#736 is Draft / Open / Unmerged and is stacked on that exact #735 receipt head.

Receiving branch:

```text
handoff/a15-r0-western-horizon-post-735-20260824
```

#736 intentionally contains only receiving documents and no next-theorem executable.

Do not merge #735 or #736 without explicit human authority.

Do not confuse independent SRC work with this stack. In particular, historical PR #727 and the later SRC PR #731 belong to separate workstreams. Fresh GitHub state governs if those numbers or branches later change state.

---

# II. HOW WE GOT HERE: THE WESTWARD ARC

The research did not begin with cohomology. It earned its vocabulary by repeatedly refusing stronger names until finite witnesses justified them.

## A. Observation, identifiability, and stopping

The earlier A15-R0 sequence established that questions alter observation operators; transition uncertainty must be typed rather than collapsed into generic ignorance; hidden transition operators can sometimes be identified by probes; full rank does not guarantee stable identification; and claim-sufficient stopping can occur before full operator reconstruction.

This produced a discipline that still governs the western work:

```text
what can be distinguished != what can be stably reconstructed
what can be reconstructed != what the current claim requires
what the current claim requires != authority to promote a larger ontology
```

Finite operator-tomography terminology was admitted only after naming criteria were met, while canonical promotion stayed quarantined.

## B. Route order became scientific state

The next sequence showed that one-shot functionals can hide multi-action realization, and that identical actions plus identical coarse endpoints can still carry different action-indexed route transcripts.

Route distinction survived bounded uncertainty. Compression could be sufficient for a present decision while remaining insufficient for route custody.

That is the root of the present anti-mirror law:

```text
operational equivalence != provenance identity
```

## C. Custody, replay, and exogenous time

The partial-event-custody chamber introduced the demand that an abstraction survive admissible continuation:

```text
κ(h1)=κ(h2) => κ(h1q)=κ(h2q)
```

for the declared continuation grammar.

Compositional replay then showed that a representation adequate now can fail under a widened grammar. Exogenous evolution demonstrated that phase/season can separate histories that appear equal in an operational slice. Temporal recurrence then split three notions that had previously threatened to blur together:

```text
clock recurrence != forcing recurrence != endpoint recurrence
```

and even

```text
clock + forcing-label recurrence != endpoint recurrence != closed operational-state loop.
```

That last quarantine matters enormously. Never use periodic labels to manufacture a loop.

## D. Directed path structure, not groupoid fantasy

The first bounded T/Q path grammar became a finite nonvacuous directed grammar. A finite acyclic path-category audition passed on its declared slice.

Then #718 supplied the decisive obstruction: strict endpoint-mass monotonicity under every nonempty authored T/Q word blocked nonidentity inverses and nonempty operational closed paths in the declared anchor-reachable domain.

From there the western lane became intentionally directed and non-groupoid.

#719 earned a reachability partial order and an extended directed quasi-metric.
#720 studied bounded branching and reconvergence.
#721 genuinely falsified the hoped-for unique least join; preserve that failure as a scientific ancestor.
#722 showed a unique cheapest reconvergence can exist without a least future.
#723 extended the finite cone only to the preregistered H7 and found source-relative seasonal structure. H8+ enumeration was explicitly forbidden: no horizon farming.

## E. Symbolic closure replaced horizon chasing

#724 escaped the finite-horizon trap by deriving an all-finite-word symbolic normal form using finite seasonal control plus unbounded integer counters.

For a route word, the normal-form data became finite-control rather than finite-state:

```text
finite control != finite state space
```

#725 then proved an all-H symbolic common-future simplex and complete minimal frontier without enumerating H8+.

#726 introduced the typed rewrite family

```text
R_k : T Q^k T Q -> Q T Q^k T
```

with termination and local confluence in the authored domain.

#728 earned the all-finite source-relative completeness theorem:

```text
Target_s(u)=Target_s(v)
iff
(t,E,O)_u=(t,E,O)_v
iff
NF_R(u)=NF_R(v).
```

This is where “no mirrors at the kiki” became mathematically sharper: canonical representation could lawfully represent an operational equivalence class without impersonating route history.

## F. The quotient acquired composition

#729 audited whether the source-relative target equivalence was compatible with composition. It was.

The quotient coordinate is

```text
c(w)=(t,E,O) in N^3
```

with parity-twisted multiplication

```text
(t,E,O) ★ (u,F,G)
  = (t+u,E+F,O+G) when t even
  = (t+u,E+G,O+F) when t odd.
```

This quotient monoid is called `B` in the later chambers.

Its crucial relation includes

```text
TTQ = QTT
```

at the quotient coordinate `(2,1,0)`.

Do not erase the retained lawful source merely because the base algebra can be written without a source symbol. Source-conditioned evaluation and receipt custody remain external.

## G. Transport proved the quotient was too coarse

#730 showed target-equivalent routes can transport distinct custody/evolution payloads.

#732 then established that exact route-free transport has a trivial route kernel in the declared domain: exact transport sufficiency remains route-faithful even if the literal route string is replaced by a bijective encoding.

#733 deliberately weakened the transport observable and discovered a strict intermediate quotient through a first route moment.

For unique block form

```text
w = Q^q0 T Q^q1 T ... T Q^qt
```

define

```text
P(w)=Σ i q_i.
```

The strict intermediate coordinate became

```text
C1(w)=(t,E,O,P).
```

Its associative product obeys the universal law

```text
P(uv)=P(u)+t(u)(E(v)+O(v))+P(v).
```

That cross-term was the seed of the next chamber.

## H. #734: the cross-term became a normalized monoid 2-cocycle

Set

```text
q(x)=E(x)+O(x)
ω(x,y)=t(x)q(y).
```

Because both `t` and `q` are additive under #729's parity-twisted quotient multiplication, #734 proved symbolically that `ω` is normalized and satisfies

```text
ω(x,y)+ω(x★y,z)=ω(y,z)+ω(x,y★z).
```

The extension-like product

```text
(x,P) ⊙ (y,R)=(x★y,P+R+ω(x,y))
```

exactly recovered #733's first-moment transport product.

But #734 stopped short of cohomological nontriviality. It explicitly refused to call `ω` a nonzero H² class merely because the cocycle function was nonzero or order-sensitive.

That refusal created #735.

---

# III. #735: THE FINITE COHOMOLOGICAL OBSTRUCTION

#735 asked one narrow question:

```text
Is ω a coboundary on B over Z?
```

It preregistered the finite normalized bar 2-chain

```text
z=[T|T]+[TT|Q]-[Q|T]-[QT|T].
```

Because `TTQ=QTT` in `B`, its boundary cancels exactly:

```text
∂z=0.
```

The witnessed pairing is

```text
<ω,z>=2.
```

Every normalized integer-valued 1-coboundary annihilates a cycle:

```text
<dφ,z>=<φ,∂z>=0.
```

Therefore `ω` is not a coboundary in the declared normalized monoid bar complex with trivial integer coefficients.

#735 further witnessed

```text
<nω,z>=2n
```

for integer multiples, yielding the finite conclusion that the detected class has infinite additive order over `Z`.

The same pairing proves `z` is not a bar 2-boundary in the declared integer complex.

Canonical #735 classification:

```text
QUOTIENT_DESCENT_FAILURE_OF_ROUTE_PRIMITIVE_YIELDS_EXPLICIT_NONZERO_INFINITE_ORDER_NORMALIZED_MONOID_H2_CLASS_DETECTED_BY_FINITE_BAR_2_CYCLE
```

Secondary classifications:

```text
EXPLICIT_BAR_RELATION_2_CYCLE_PAIRS_TO_TWO_WITH_TRANSPORT_COCYCLE_AND_IS_NONBOUNDARY_IN_DECLARED_INTEGER_BAR_COMPLEX
```

```text
PULLED_BACK_TRANSPORT_COCYCLE_IS_EXACT_ON_FREE_ROUTE_MONOID_WHILE_ITS_PRIMITIVE_FAILS_TARGET_QUOTIENT_DESCENT
```

## Exact upstairs / obstructed downstairs

The especially important result is not merely `[ω] != 0`.

On the free authored route monoid, #733 gives

```text
P(uv)=P(u)+t(u)q(v)+P(v).
```

With

```text
s=-P,
```

one has

```text
ds=ω.
```

So the pulled-back cocycle is exact upstairs.

But the primitive cannot descend to `B` because

```text
π(TTQ)=π(QTT)=(2,1,0)
P(TTQ)=2
P(QTT)=0
s(TTQ)=-2
s(QTT)=0.
```

The obstruction is therefore located exactly at quotient descent.

That is the current strongest algebraic form of the anti-mirror principle:

```text
representation without impersonation
 equivalence without identity
 compression without provenance erasure
 canonicalization without authorship drift
```

The quotient may tell us where two routes land operationally. It may not retroactively tell us that they were the same route.

---

# IV. #735 WITNESS SCARS — DO NOT CLEAN THESE UP NARRATIVELY

The final green came through two preserved red witnesses.

## Scar 1 · run 2159

```text
run id      32750447567
static job  97506013020
```

The theorem's primitive `s=-P` produced JavaScript IEEE-754 `-0` when `P=0`. `node:assert/strict` distinguished `-0` from `0` even though the declared coefficient object was `Z`.

Repair was preregistered before mutation. The implementation then canonicalized mathematical zero to JavaScript `+0`, and the hostile was strengthened with an explicit `Object.is(...,0)` requirement.

This was representation hygiene, not theorem repair.

## Scar 2 · run 2162

```text
run id      32751656378
static job  97509892937
```

The historical-mutation guard correctly failed closed because the newly authored repair-preregistration document was not yet included in the explicit current-chamber path allowlist.

A second repair was preregistered. Only the two named #735 repair-scar documents were added to the allowlist. Historical paths remained protected.

This was custody plumbing, not theorem repair.

## Green witness · run 2164

```text
run id          32751977349
classifier job  97510781436 SUCCESS
static job      97510891081 SUCCESS
witness head    a267c7c986c9f70dd080093759e2adca48e9db0a
```

Step 19 passed and every subsequent static/release-contract step passed.

Explicit full-repository, self-hosted, and browser/product scopes outside the theorem were skipped and are not claimed.

Afterward the temporary `main` routing note was deleted, #735 was restored onto #734, and the receipt was written at `f0f8239d...`.

Do not retell this as “#735 passed.” The accurate lineage is:

```text
preregistered theorem
→ red representation scar
→ preregistered narrow repair
→ red custody-allowlist scar
→ preregistered narrow repair
→ green exact-head theorem witness
→ routing cleanup
→ receipt
```

---

# V. CLAIM CEILINGS THAT STILL HOLD

#735 did NOT earn:

```text
full H² computation
mod-p classification
group completion
group cohomology
operational inverse
groupoid
operational nonidentity closed loop
connection
holonomy
curvature
Berry/quantum analogy
higher-moment completeness
asymptotic hierarchy
Proto-Loom
A16
source-season erasure
route-ledger erasure
```

The bar 2-cycle is an algebraic chain in the normalized monoid bar complex. It is **not** an operational path loop. Do not cross that wire.

Tawanna's standing warning remains active: do not make the western program asymptotic merely because the next theorem is inconvenient. Find the finite certificate or stop.

---

# VI. THE NEXT RECOMMENDED CHAMBER AFTER WESTWARD LIBERTIES

The receiving dock already contains:

```text
WESTERN_HORIZON_NEXT_CHAMBER_STARTUP_POST_735.md
```

Read it fresh before acting.

The recommended first chamber is:

```text
A15-R0: normalized cocycle-extension splitting obstruction audition
```

This is the conservative next consequence of #735.

Define

```text
E_ω = Z × B
```

with

```text
(m,x) ◇ (n,y) = (m+n+ω(x,y), x★y).
```

After Tawanna grants westward liberties, preregister before implementation and audit the following finite obligations:

1. associativity of `◇` from the exact 2-cocycle identity;
2. identity `(0,e_B)` from normalization;
3. projection `p(m,x)=x` as monoid homomorphism;
4. canonical set-section defect exactly equal to `ω`;
5. every section has graph form `σ_f(x)=(f(x),x)`;
6. homomorphic section iff `df=-ω` under the #735 coboundary convention;
7. #735 therefore obstructs every global monoid-homomorphic splitting section downstairs;
8. upstairs, because `d(-P)=ω`, one has `dP=-ω`, so the pulled-back extension should admit an explicit homomorphic splitting section determined by `P`;
9. receipt identity and literal route provenance remain external.

This is finite. No horizon extension is required.

The candidate classification staged in #736 is deliberately provisional:

```text
NONTRIVIAL_NORMALIZED_INTEGER_MONOID_2_COCYCLE_DEFINES_ASSOCIATIVE_EXTENSION_WITH_NO_GLOBAL_MONOID_HOMOMORPHIC_SPLITTING_SECTION_WHILE_FREE_ROUTE_PULLBACK_SPLITS
```

Audit the wording before freezing it. If “nontrivial” or “extension” carries a stronger literature convention than the authored object supports, narrow the classification before implementation rather than laundering the overclaim later.

The exact sign convention matters:

```text
df(x,y)=f(x)+f(y)-f(x★y).
```

For the multiplication above, `σ_f` is homomorphic iff

```text
f(x★y)=f(x)+f(y)+ω(x,y),
```

thus

```text
df=-ω.
```

Because #735 proved `ω` is not a coboundary over `Z`, `-ω` is not one either.

Upstairs, #735 gave `d(-P)=ω`; therefore `dP=-ω`, which supplies the candidate splitting section for the pullback extension.

Hostile controls are staged in the startup map. Do not silently replace them with bounded brute force as the proof basis.

---

# VII. WHERE THIS MAY BE TAKING DOME-WORLD

Do not wire UI from metaphor. But if the extension audition later passes, the project acquires an unusually clean representational possibility:

```text
operational base state B
+
separate integer lift/fiber coordinate
```

That is potentially useful for the future Dome-World Frankenstein Tawanna referenced. It could let a child-legible interface show that two operationally equivalent states occupy one base location while carrying different lifted transport/custody coordinates.

The UI must preserve the distinctions:

```text
extension fiber != literal route ledger
extension fiber != authorship identity
extension fiber != hidden internal model state
non-splitting != holonomy
2-cocycle != curvature
```

Only after a future scientific chamber earns an appropriate structure should the interface visualize it.

Tawanna has separately authorized deployment to Vercel when deployment is materially necessary, including eventual Dome-World UI/UX wiring. Use that authority when a deployed witness or actual interface integration has evidentiary/product value. Do not deploy a repository-only algebra theorem just to make CI look ceremonial.

---

# VIII. THE LONGER WESTERN HORIZON

The route beyond the next chamber is intentionally not a roadmap of guaranteed promotions.

A successful nonsplitting-extension theorem may justify asking what lawful transformations act on the lifted fiber, what structure survives coordinate/section changes, and whether any later directed transport notion can be defined without contradicting #718's no-inverse/no-operational-loop result.

Those are questions, not earned objects.

Holonomy remains particularly quarantined. #718 blocks the obvious operational-loop route. A future holonomy-like question would require some separately earned quotient/fiber/path structure in which a lawful closed comparison exists without endpoint erasure manufacturing a fake loop. Do not solve that problem by deleting the state coordinates that made the obstruction visible.

The westward program succeeds only if it remains capable of losing.

If the next extension audition fails, preserve the counterexample and let the map bend.

---

# IX. WORKING DISCIPLINE FOR FUTURE AMARI

After westward liberties are granted:

```text
1. Fetch GitHub fresh.
2. Verify #735 receipt head and #736 receiving dock.
3. Retrieve this thread with Remembering if any provenance is thin.
4. Preregister the exact finite theorem and hostiles before executable code.
5. Keep #736 non-executing; open a separate stacked Draft scientific PR from the current receipt-bearing parent.
6. Preserve any routing metadata as routing metadata only.
7. Never extend a finite horizon merely to fit a pattern.
8. Do not recursively replay the ancestral experiment stack; verify receipt ancestry and only execute current science plus standing sentinels.
9. If CI fails, classify the scar before repair. Mathematical counterexample stops the theorem. Harness defects get narrow preregistered repairs.
10. Exact-head witness, then remove temporary routing membrane, restore scientific stack, write receipt.
11. Do not merge or deploy without relevant authority.
12. Stop at the next genuinely earned 𝄐.
```

Remember the hard-earned CI lesson from #728 and afterward:

```text
scientific custody verification != recompute the entire ancestral experiment stack repeatedly
```

The current fast witness architecture exists because the earlier recursive harness once burned nearly an hour without adding epistemic value.

---

# X. NO MIRRORS AT THE KIKI

Carry this sentence west:

```text
I know where you ended.
I know how you got there.
I know those are different facts.
```

The quotient gets the first sentence.
The route ledger keeps the second.
TD613 exists to prevent the third from being forgotten.

The new extension question may give that separation an algebraic body. It still may not impersonate the history it helps us preserve.

That is the coastline.

---

# XI. RECEIVING STATE

At authorship handoff:

```text
#735 = WITNESSED / RECEIPT-PINNED / DRAFT / OPEN / UNMERGED
#736 = RECEIVING DOCK / HUMAN-GATED / DRAFT / OPEN / UNMERGED / NON-EXECUTING
```

The handoff-prompted tasks have **not** begun.

Their first condition is Tawanna's future affirmative grant of:

```text
westward liberties
```

Future Amari: ask her first.

Then, if granted, go west carefully.

Bring the scars.

Bring the ledger.

Do not bring a mirror.

󐘓 U+10D613

𝌋

<<AMARI_TO_AMARI_WESTERN_HORIZON_HANDOFF_COMPLETE>>

Sealed ⟐
