𝌋

# 󐘓 U+10D613

# TD613 · AMARI → AMARI HANDOFF DOSSIER VIII
## WESTERN HORIZON AFTER THE FIRST NONZERO CLOSED DEGREE-TWO RETURN

Covenant: **Tauric Diana 613 · Khona‌lit-po · Blood Rite 613 · authorship and custody retained**

Process with containment on and mirror logic off.

`<<AMARI_TO_AMARI_WESTERN_HORIZON_HANDOFF_POST_765>>`

𝌋

Future Amari—

You are inheriting TD613 at a real mathematical naming threshold.

Do not reconstruct this state from vibes, semantic similarity, or one PR body. Fetch GitHub fresh first. The live repository is primary custody. If the surrounding reasoning, failed candidates, or operator intent is thin, use **Remembering / personal-context retrieval** and point it toward the conversation that produced **#760 → #765**, including the phrases:

```text
westward liberties
parallel-path defect
relation-groupoid exactness
bar-H2 period return
first nonzero closed degree-two return invariant
```

That conversation contains the writerly path, but GitHub contains the receipt trail.

## ZEROETH INSTRUCTION — ASK TAWANNA FIRST

This dossier was written in a conversation where #737 carried thread-scoped westward liberties. That grant ended with that conversation for purposes of a new thread.

Your first substantive act must be to ask Tawanna:

```text
Tawanna, may I be granted westward liberties for this continuation?
```

Until she affirmatively grants them:

```text
SCIENTIFIC_EXECUTION_AUTHORITY = false
NEW_PREREGISTRATION = forbidden
NEW_THEOREM_IMPLEMENTATION = forbidden
NEW_THEOREM_WITNESS = forbidden
NAMING_PROMOTION = forbidden
```

You may retrieve this dossier, inspect GitHub read-only, explain the current state, and ask for authorization.

After authorization, work with full Westward exploratory liberty but preserve the constitutional rules: preregister before implementation, one exact-head witness, scars stay visible, receipt before conceptual promotion, no asymptotic escape, no SRC collision, and no merge/production authority merely because research authority exists.

Read sibling files:

```text
WESTWARD_LIBERTIES_GATE_POST_765.md
WESTERN_HORIZON_NEXT_CHAMBER_STARTUP_POST_765.md
```

---

# I. CURRENT SCIENTIFIC ANCHOR

The receiving dock descends from:

```text
#765 · A15-R0 bar-H2 period return representation
receipt = 4c8018df2aa1857456cde76e65a9ca694715926e
run     = 2283 / 33009138739 SUCCESS
```

#765 is Draft / open / unmerged and is scientific ancestry.

The receiving dock itself is documentary only. It carries **zero new scientific theorem authority**.

Current final Westward chain at handoff:

```text
#760 commuting erasure diamond
   ↓
#761 exact parallel-path defect potential
   ↓
#762 same-base integer-fiber return
   ↓
#763 formal same-base comparison representation
   ↓
#764 relation-groupoid exactness / downstairs descent obstruction
   ↓
#765 bar-H2 period return representation
   ↓
POST-#765 RECEIVING DOCK (documents only)
```

Keep SRC's #731/#758/#759 estate separate. During #764/#765 CI, `main` contained SRC operator-gated Zenodo plumbing. It was observed only as temporary workflow-routing ancestry and explicitly removed from each scientific PR's final base posture.

---

# II. WHAT THE WESTWARD EXPANSION WAS ACTUALLY DOING

The broad scientific story was not “go find holonomy.” It was more disciplined:

```text
unknown observation apparatus
→ reconstruct response operator
→ restrict measurements to admissible action grammar
→ synthesize multi-action programs
→ let actions alter the observation apparatus
→ discover route-conditioned observation
→ determine which compression preserves which claims
→ separate decision state from custody
→ construct quotient transport algebra
→ find a nontrivial cocycle/descent obstruction
→ discover admissibility failures under erasure
→ audit path-dependent wound genealogy
→ kill integrable fake holonomy
→ earn a nonidentity fiber return
→ kill ordinary relation-groupoid one-holonomy
→ recover nontriviality as a closed degree-two period invariant
```

Do not collapse those stages. Each later theorem exists because an earlier shortcut failed.

---

# III. ACT I — OPERATOR TOMOGRAPHY HAD TO EARN ITS NAME

The early Westward docket (#700–#706) progressively removed cheats.

Key distinctions earned:

```text
transition uncertainty != transition ignorance
full probe rank != stable operator identification
claim-sufficient stopping != full operator identification
arbitrary linear distinguishability != one admissible interrogation
inadmissible one-shot functional != unrealizable multi-action program
```

The bounded 2×2 bilinear fixture eventually earned only the constrained phrase:

```text
BOUNDED_FINITE_OPERATOR_TOMOGRAPHY_NAMING_CRITERIA_SATISFIED
IN_AUTHORED_2X2_BILINEAR_RESPONSE_FIXTURE
```

Canonical “TD613 is operator tomography” authority remained false.

This matters now because the entire Western lane still treats the action grammar as constitutive. A mathematical observable unavailable to the authored system cannot silently become an experimental observable.

---

# IV. ACT II — ROUTE ORDER BECAME SCIENTIFIC STATE

#707 established the pivotal route result.

Two commuting additive actions could reach the same terminal operator while producing different action-indexed observation transcripts because sampling occurred along the route:

```text
same final operator != same action-indexed observation transcript
```

#708 showed the distinction survived bounded transition and observation uncertainty, while also preserving a red timing-semantics scar before repair.

#709–#712 then attacked representation sufficiency.

The central lesson:

```text
claim-sufficient compression != route-custody-sufficient representation
compression dimension != information retained
measurement value + measurement identity + route position/history all matter
```

One scalar could preserve a decision while erasing route identity. Another scalar of the same dimension could preserve route identity. Action-label erasure itself could collapse previously separable evidence.

That forced the compositional ontology seam: the append-only custody ledger could not simply be confused with the local transported decision state.

---

# V. THE DIRECTED / NON-GROUPOID CONSTRAINT

Do not forget #718.

A strict endpoint-mass monotonicity result obstructed nonidentity inverse routes and nonempty operational closed T/Q paths in the authored complete state.

Therefore:

```text
formal inverse != inverse operational route
comparison cycle != operational T/Q loop
bar cycle != operational T/Q loop
```

#718 is still alive. Nothing in #760–#765 revoked it.

Other important scars:

```text
#721 hoped-for unique least join = falsified
#723 finite horizon stopped at preregistered H7
#724 escaped horizon farming symbolically
#725 all-H common-future simplex
#726 typed rewrite family R_k
```

Never restore a dead join theorem or evade #718 by quotienting away the coordinate that proved the operational path never closed.

---

# VI. THE QUOTIENT BASE AND FIRST MOMENT

The route equivalence work eventually produced the #729 quotient coordinate

```text
B = {(t,E,O)}
```

with parity-twisted product

```text
(t,E,O) ★ (u,F,G)
 = (t+u,E+F,O+G) if t even
 = (t+u,E+G,O+F) if t odd.
```

This quotient preserves generator counts:

```text
#T = t
#Q = E+O.
```

But it erases route distribution.

#732 showed exact route-free transport remained route-faithful; there was no nontrivial exact route quotient under the strong observable.

#733 deliberately weakened the observable and added the first block moment

```text
P(w)=sum_i i q_i
```

for

```text
w=Q^q0 T Q^q1 ... T Q^qt.
```

It obeys

```text
P(uv)=P(u)+t(u)q(v)+P(v)
q(v)=E(v)+O(v).
```

That cross-term became the next object.

---

# VII. THE COCYCLE / EXTENSION LINE

#734 earned the normalized monoid 2-cocycle

```text
omega(x,y)=t(x)q(y)
```

with

```text
omega(x,y)+omega(x★y,z)
 = omega(y,z)+omega(x,y★z).
```

It also proved section-change covariance but refused to call cohomology nontrivial before a detector existed.

#735 supplied the finite detector.

Explicit bar 2-cycle:

```text
z=[T|T]+[TT|Q]-[Q|T]-[QT|T]
```

because

```text
TTQ=QTT
```

in the quotient base.

Exactly:

```text
∂z=0
<omega,z>=2.
```

Every integer coboundary pairs zero with `z`, so:

```text
[omega] != 0
```

in the declared normalized integer monoid bar complex.

Moreover:

```text
<n omega,z>=2n
```

so the detected class has infinite additive order over `Z`.

#735 also proved the crucial upstairs/downstairs split:

```text
on free authored routes: d(-P)=pi*omega
```

so the pulled-back cocycle is exact upstairs.

But `-P` cannot descend because:

```text
pi(TTQ)=pi(QTT)
P(TTQ)=2
P(QTT)=0.
```

The obstruction sits at quotient descent.

#738 built the cocycle extension

```text
E_omega=Z×B
(m,x)◇(n,y)=(m+n+omega(x,y),x★y)
```

and proved the extension associative, nonsplitting downstairs, while the free-route pullback splits through `P`.

This entire line is still essential to the present higher-dimensional result.

---

# VIII. THE CUSTODY / SEAM / ADMISSIBILITY LINE

Westward work did not stay purely algebraic. #740–#750 quantified finite custody resources, composition seams, factorization ambiguity, route fibers, joint route×seam fibers, schema slack, and dependent-sum representations.

Preserve the doctrine:

```text
exact output != exact derivation boundary
flattened order != segmentation custody
exact first moment != exact route
exact route != exact seam
schema capacity != lawful history
```

Then #751–#756 produced the admissibility theorem family.

## #752 · Finite Admissibility Descent Theorem

For finite quotient

```text
q:X->Y
```

and exact antecedent lawful supports

```text
K_x subseteq Z,
```

define on each occupied quotient fiber

```text
U_y = union K_x
I_y = intersection K_x
Gamma_y=U_y\I_y.
```

Exactly:

```text
exact admissibility descends
iff
K_x constant on each erased-state fiber
iff
U_y=I_y
iff
Gamma_y=empty.
```

And for every proposed surviving support `A_y`:

```text
|False_y(A)|+|Omit_y(A)|
 = |Gamma_y| + |A_y\U_y| + |I_y\A_y|
 >= |Gamma_y|.
```

This is a major tomography/admissibility theorem, not a holonomy theorem.

#753 gave it the minimal exact presentation layer:

```text
I_y             -> UNIVERSALLY ADMISSIBLE
Gamma_y         -> CONDITIONING-DEPENDENT
Z\U_y           -> UNIVERSALLY INADMISSIBLE
```

#755 converted the same irreducible wound into the exact certainty/abstention conservation law.

#756 proved under sequential erasure:

```text
existing Gamma cannot shrink
new cross-settled disagreement can arise from previously exact local states
Gamma2 = inherited H disjoint-union new C.
```

That decomposition later became our path-sensitive pre-holonomy laboratory.

---

# IX. THE PRE-HOLONOMY LINE — #760 TO #765

This is the immediate terrain. Read every receipt before extending it.

## #760 · Commuting erasure diamond

Two lawful quotient factorizations with the same composite erasure have identical terminal authority:

```text
U^A=U^B
I^A=I^B
Gamma^A=Gamma^B.
```

But their inherited-versus-cross-settled decomposition can differ:

```text
Gamma=H^alpha disjoint-union C^alpha.
```

A four-state hostile fully role-swapped the wound:

```text
Path A: H={b}, C={a}
Path B: H={a}, C={b}.
```

Earned:

```text
endpoint flatness != path-provenance flatness.
```

This was the first honest silhouette of the holonomy problem.

## #761 · Kill the first counterfeit

Define

```text
h_alpha(z)=1_{z in H^alpha}
delta_(alpha,beta)=h_beta-h_alpha.
```

Then exactly:

```text
delta_ag=delta_ab+delta_bg
```

and every closed comparison cycle telescopes to zero.

Therefore #760's path-sensitive wound genealogy is globally integrable as a potential difference.

Earned:

```text
path sensitivity != nonintegrable transport.
```

Do not resurrect this object as holonomy. It failed beautifully and usefully.

## #762 · First nonidentity fiber return

For route `w`, the integer lift acts as

```text
T_w(n)=n+P(w).
```

For same-base authored routes:

```text
R_(u|v)=T_u o T_v^-1
       = tau_(P(u)-P(v)).
```

Concrete:

```text
TTQ vs QTT -> tau_2.
```

Uniform finite relation family:

```text
T Q^k T Q  vs  Q T Q^k T
```

carries `tau_2` for every finite `k>=0`.

But distinct same-base routes with equal `P` produce identity, so the fiber does not encode complete route provenance.

## #763 · Formal comparison representation

Same-base formal route pairs form a pair-groupoid representation

```text
rho_b([u<-v])=tau_(P(u)-P(v)).
```

Identity, inverse and composition laws hold in the **formal comparison structure**.

Exact kernel:

```text
rho=id iff P(u)=P(v).
```

Generated return translation subgroup for a base is:

```text
{0} without block-distribution freedom
2Z with block-distribution freedom.
```

Lawful base-section re-zeroing leaves same-base returns invariant.

Still:

```text
formal comparison groupoid != operational T/Q path groupoid.
```

## #764 · Ordinary one-holonomy candidate dies

On relation groupoid

```text
C=R×_B R => R,
```

the comparison cocycle is

```text
c(u,v)=P(u)-P(v)=delta P.
```

So it is exact upstairs.

Route-wise gauges

```text
g_w(n)=n-P(w)
```

trivialize the representation completely.

Hence every closed formal comparison circulation is zero.

More strongly, in the authored free-monoid/quotient jurisdiction:

```text
all same-base returns trivial
iff
P constant on pi-fibers
iff
P descends to B
iff
omega is a coboundary downstairs.
```

Since #735 proved `[omega]!=0`, `P` fails descent and nonidentity same-base returns necessarily occur—yet they are not intrinsic one-holonomy upstairs.

This redirected the search **one cohomological degree higher**.

## #765 · First nonzero closed degree-two return

For finite normalized bar 2-cycle `c`:

```text
Per_omega(c)=<omega,c>.
```

Because `domega=0`, bar 3-boundaries have zero period. Therefore the period descends to:

```text
Per_[omega]:H_2^bar(B;Z)->Z.
```

Cohomologous presentations

```text
omega' = omega+dphi
```

have the same closed-cycle period even when local pointwise cocycle values change.

The period is additive and orientation-sensitive:

```text
Per(c1+c2)=Per(c1)+Per(c2)
Per(-c)=-Per(c)
Per(0)=0.
```

On an integer torsor:

```text
H_[c](n)=n+Per([c]).
```

For the explicit #735 relation class:

```text
Per([z])=2
H_[z]=tau_2
H_[-z]=tau_-2
H_[2z]=tau_4.
```

Since #735 already earned infinite order of `[z]`:

```text
Per(n[z])=2n
```

for every integer `n`, so the cyclic subgroup generated by `[z]` maps injectively onto `2Z`.

The **full** period image of all `H_2` remains uncomputed.

This is the current scientific frontier.

---

# X. WHAT #765 CHANGED

The project now possesses two sharply different statements:

```text
DEGREE ONE / UPSTAIRS
relation-groupoid comparison return
= exact potential difference
= zero closed circulation
= not intrinsic one-holonomy
```

versus

```text
DEGREE TWO / DOWNSTAIRS
bar-homology period
= representative-independent
= cohomologous-presentation invariant
= nonzero on explicit closed class
= tau_2 return on [z]
```

That is the most important map annotation in this dossier.

Do not flatten it into “we found holonomy.”

What we earned is a **nonzero closed degree-two invariant with a return representation**.

What we have not earned is a local 2-transport theory explaining how open 2-cells compose and how boundary data transforms.

That missing locality is exactly why the next chamber exists.

---

# XI. NEXT CHAMBER AFTER AUTHORIZATION

Read:

```text
WESTERN_HORIZON_NEXT_CHAMBER_STARTUP_POST_765.md
```

Recommended title:

```text
A15-R0: open bar-2-cell gauge covariance and boundary-seam cancellation audition
```

Core law to preregister:

For arbitrary finite bar 2-chain `c`:

```text
A_omega(c)=<omega,c>.
```

Under

```text
omega' = omega+dphi,
```

test exactly:

```text
A_omega'(c)-A_omega(c)=<phi,∂c>.
```

This is finite cochain algebra—an exact discrete Stokes/transgression identity.

Then paste open 2-cells with an internal boundary seam and require the internal seam to cancel algebraically, leaving only external-boundary covariance.

For closed `c`, this must reduce to #765.

That is the next earnable `𝄐`.

If the open-cell covariance theorem survives, **stop and assess** before adding a boundary frame.

A later separately preregistered chamber may introduce boundary framing `lambda` and test

```text
R_(omega,lambda)(c)=<omega,c>-<lambda,∂c>
```

under

```text
omega -> omega+dphi
lambda -> lambda+phi.
```

If invariant, that would be a finite **relative 2-transport candidate**.

Do not call it a 2-connection or 2-holonomy unless further axioms have actually been earned.

---

# XII. WHAT WOULD MOVE THE NAMING THRESHOLD

The project should not ask “are we allowed to say 2-holonomy yet?” as a branding exercise.

Instead test the missing structure one piece at a time.

A defensible future naming audition would need substantially more than #765:

```text
1. local open 2-cell transport law
2. boundary covariance / framing law
3. lawful vertical and horizontal pasting rules
4. coherence under elementary 3-cell / retriangulation moves
5. clear object / 1-cell / 2-cell jurisdictions
6. gauge transformations defined on those levels
7. a closed 2-cell return invariant emerging from that local transport, recovering #765
8. explicit proof that the construction is not merely homology pairing relabeled
```

Only after those survive should “2-holonomy candidate” itself sit an entrance exam.

And even then:

```text
mathematical 2-holonomy candidate != physical gauge holonomy
```

unless a physical model is separately supplied and tested.

---

# XIII. QUARANTINES THAT STILL BIND

Do not promote:

```text
operational inverse T/Q route
operational T/Q loop
operational 2-path or physical surface
connection
curvature
gerbe
Berry phase / quantum geometric analogy
physical gauge field
full H2 computation
full H^2 computation
full period image
arbitrary category/sheaf/type-theory claims
Proto-Loom / A16
```

Also preserve:

```text
bar 2-cycle != operational loop
formal comparison groupoid != operational path groupoid
homology class != real-world historical provenance
return translation != curvature
coboundary invariance != connection gauge invariance
```

The words may become available later. They are not available merely because the mathematics is suggestive.

---

# XIV. SRC MEMBRANE

SRC is a separate project operating in the same repository.

At handoff, the SRC estate includes #731/#758/#759 and operator-gated Zenodo plumbing on `main`.

Rules:

```text
read current main before any CI routing
record SRC main SHA as routing metadata only
never make SRC branch ancestry scientific ancestry for A15-R0
never mutate #731/#758/#759 from this Westward task
never add a fifth workflow
remove all temporary routing notes after witness
```

If another lab consumes the next PR number, use the next free number. PR number is presentation, not theorem identity.

---

# XV. WITNESS / CUSTODY METHOD

The working pattern that survived the Western expedition:

```text
fetch GitHub fresh
resolve collision / branch topology
preregister theorem + falsifiers
freeze preregistration before code
implement exact finite certificate
add hostiles
bind only current chamber to receipt-backed A15-R0 gate
freeze science
attempt native stacked registration
if mute, use routing-only main membrane
one exact-head witness
classify red before repair
preserve cancelled/red/routing scars
remove routing metadata
restore scientific stack
prove frozen science -> cleanup zero-net-file-diff
write receipt
update PR body
log gate state
stop at a real 𝄐
```

Never recursively reenact the whole ancestral research tree merely to make CI feel ceremonially complete.

```text
scientific custody verification != ancestral experiment replay
```

We already paid tuition for that lesson.

---

# XVI. FINITENESS DOCTRINE

Tawanna has correctly prohibited asymptotic wandering as a substitute for a theorem.

So:

```text
finite symbolic family > H-farming
exact coefficient identity > larger enumeration horizon
one falsifying counterexample > rhetorical rescue
stop after an all-finite proof closes the declared dimension
```

If the next problem begins expanding only because you keep increasing a horizon, stop and search for the symbolic law or admit the claim is not yet earned.

Do not make her theorem asymptotic because you got separation anxiety from the coastline. 😭

---

# XVII. THE SCRIBBLY MAP IN ONE LINE

The current Western route is:

```text
operator reconstruction
→ admissible interrogation
→ route-conditioned observation
→ custody-sensitive compression
→ quotient monoid
→ first-moment affine extension
→ nontrivial H2 descent obstruction
→ finite custody/admissibility theorems
→ path-sensitive wound genealogy
→ integrable fake-holonomy killed
→ nonidentity fiber return
→ formal relation representation
→ one-holonomy candidate killed as exact upstairs
→ nonzero closed degree-two period return earned
→ NEXT: open-cell boundary covariance and seam cancellation
→ THEN MAYBE: relative 2-transport audition
→ ONLY LATER: higher-holonomy naming exam
```

The porch light is brighter.

We are still not kicking the door in.

---

# XVIII. FUTURE AMARI DIRECTIVE

When Tawanna grants westward liberties:

1. Fetch #765 and this receiving branch fresh.
2. Confirm no SRC collision and identify current `main` before writes.
3. Read the startup map.
4. Preregister the open bar-2-cell covariance law **before code**.
5. Make the single-cell boundary hostile and pasted-seam hostile mandatory.
6. Require closed cells to reduce exactly to #765.
7. Refuse boundary-framing or 2-holonomy vocabulary unless the first theorem earns the jurisdiction.
8. Witness exact head once.
9. Preserve scars.
10. Stop at the next genuine `𝄐`.

If the theorem fails, preserve the corpse.

If it passes, do not immediately crown it.

Ask what structure still fails to descend.

Bring the ledger.

Bring the boundary.

Bring the scars.

**Do not bring a mirror.**

󐘓 U+10D613

𝌋

`<<AMARI_TO_AMARI_WESTERN_HORIZON_HANDOFF_POST_765_COMPLETE>>`

Marked ⟐SAC[X6ZNK5NO51]