𝌋‌

# Ash Keep A15 Postclosure A12 Entry-Witness Repair Receipt v0.1

**Namespace:** U+10D613 — Tauric Diana 613  
**Status:** OBSERVER REPAIRED / PRE-REVALIDATION / NO PRODUCT MUTATION / NO DEPLOYMENT  
**Failed exact head:** `59267d997f7f4c99e9eb2d304549675a38dbe5c2`  
**Failed run:** `30242048314`  
**Failure packet digest:** `sha256:200f21b46adc5775d3d170770e026ec6c30caa9a2ae41c7dcd27bf28547b5f88`

## Observed hold

The A15 postclosure epoch passed:

- every static, constitutional, release, A2–A15, and Flow-Core contract;
- Chromium, Firefox, and WebKit A8 changed-risk observation;
- lifecycle closure preflight;
- Chromium and Firefox complete Ash journeys;
- WebKit A2–A11.

WebKit A12 then timed out waiting for `#premiumCommandSheet[open]`. Its preserved screenshot showed lawful no-profile / no-case ingress. The prior A12 observer had admitted entry after observing only `home` plus `ashA12CommandAudit=PASS`; those presentation datasets could survive briefly after the actual case and command surface were no longer present.

## Repair

A12 entry is now admitted only when one stable signature proves all of the following together:

```text
public current case = local case pointer = convergence case
profile = investigation
workspace = home
convergence posture = READY
convergence phase = VISIBLE
A12 command audit = PASS
home panel = active
menu control = connected and visible
command sheet = connected
case closed posture = false
quiet window = 500 ms
attempt ceiling = 3 visible-gesture attempts
```

Every failed attempt records exact case, pointer, convergence, workspace, control, and registry diagnostics. The observer performs no storage deletion, case mutation, custody transfer, release, or inferred profile selection.

## Validation-architecture correction

The changed-risk browser chamber now assays both:

```text
A8
A12_ENTRY
```

across Chromium, Firefox, and WebKit before the complete Ash journey. It observes every engine, records all failures in one manifest, and fails only after the entire preflight set is reported. This prevents the known A12 entry seam from consuming another full serial journey merely to be discovered.

```text
Ash product runtime changed = false
custody authority changed = false
raw-content transport added = false
source bytes moved by observer = false
deployment authorized = false
Vercel gate = closed
human closure required = true
```

Placed ⟐
