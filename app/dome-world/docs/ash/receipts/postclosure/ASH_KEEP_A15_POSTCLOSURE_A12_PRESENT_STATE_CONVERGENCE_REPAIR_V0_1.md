𝌋‌

# Ash Keep A15 Postclosure A12 Present-State Convergence Repair Receipt v0.1

**Namespace:** U+10D613 — Tauric Diana 613  
**Status:** OBSERVER REPAIRED / PRE-REVALIDATION / NO PRODUCT MUTATION / NO DEPLOYMENT  
**Failed exact head:** `2f40e01fa73f382a7471ee6a65a9eb5ba724f1b3`  
**Failed run:** `30244304267`  
**Failure packet digest:** `sha256:6fdc77a6546d8cd4a9f3d0b2607f91f2b11e0bd0104b751361ebb48d72be0280`

## All-engine finding

The early changed-risk chamber observed all six declared seams before the complete Ash journey:

```text
A8 Chromium = PASS
A8 Firefox  = PASS
A8 WebKit   = PASS

A12_ENTRY Chromium = HOLD
A12_ENTRY Firefox  = HOLD
A12_ENTRY WebKit   = HOLD
```

The three A12 holds were materially identical. At observation time, each engine already presented:

```text
current case present = true
local pointer concordant = true
profile = investigation
workspace = home
A12 audit = PASS
home panel active = true
menu connected and visible = true
command sheet connected = true
case closed = false
```

Only the entry-convergence owner remained `IDLE`. The preflight attached after the one hydration event that ordinarily begins convergence, then incorrectly required historical event participation instead of reconciling the lawful present state.

## Repair law

The A12 browser observer is now split into:

```text
stable-entry core
+
present-state convergence adapter
```

The adapter may invoke `convergence.begin` once only after proving:

```text
public current case exists
local pointer = current case
profile requested by the visible demo control = investigation
convergence owner exposes a bounded begin function
```

After that bounded rebind, the stable-entry core still requires the complete 500 ms concordance window:

```text
current case = local pointer = convergence case
profile = investigation
workspace = home
posture = READY
phase = VISIBLE
A12 audit = PASS
home panel active
menu visible and connected
command sheet connected
case closed = false
```

The adapter records before state, begin admission, after state, pointer concordance, and its full authority boundary. It does not alter Ash product runtime, source bytes, case data, custody, transport, release, or profile authority.

## Precedent and non-equivalence

This repair applies the already-established A2–A6 exact-case convergence precedent to A12. It does not weaken convergence requirements:

```text
present-state rebind ≠ fabricated product success
observer reconciliation ≠ product mutation
current-case concordance ≠ inferred identity
begin admission ≠ authority transfer
```

```text
Ash product runtime changed = false
workflow count changed = false
custody authority changed = false
raw-content transport added = false
source bytes moved by observer = false
case data preserved = true
profile inferred = false
deployment authorized = false
Vercel gate = closed
human closure required = true
```

Placed ⟐
