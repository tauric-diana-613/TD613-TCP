𝌋‌

# Ash Keep A13 Implementation Receipt v0.1

**Namespace:** U+10D613 — Tauric Diana 613  
**Covenant display:** Blood Rite 613  
**Stage:** A13 · Unified six-demo registry and workflow-estate collapse  
**Status:** IMPLEMENTED ON BRANCH / EXACT-HEAD VALIDATION PENDING / HUMAN-GATED  
**Baseline:** A12 relocked production main `d1b57f680551999aad0fd87e5e78caa30fb55b16`  
**Asset epoch:** `20260724-a13-release-v1`  
**Mass-eviction epoch:** retained at `td613.ash.cache-flush/2026-07-24-a11-postclosure-v1`

## 1. Purpose

A13 repairs two forms of fragmented ownership that had begun to imitate architecture:

1. profile hydration was distributed across competing capture-phase `Start a demo` listeners;
2. repository validation was distributed across overlapping workflows that repeatedly installed browsers, started equivalent local runtimes, and re-proved the same source packet.

The stage therefore unifies both the runtime registry and its proof estate.

## 2. Governed demo registry

`app/dome-world/ash-demo-registry.js` becomes the sole runtime owner of:

- explicit profile selection;
- `Start a demo` control state;
- the hydration gesture;
- promoted/held registry posture;
- normalized registry inspection.

Canonical registry order:

1. Investigation — `PROMOTED`;
2. Political Campaign — `PROMOTED`;
3. Fundraiser — `PROMOTED`;
4. Research Project — `PROMOTED`;
5. Legal Matter — `PROMOTED`;
6. Archive — `RESERVED_FOR_A14`.

Archive receives a lawful seat, manifest boundary, claim ceiling, and visible hold. A13 does not fabricate its accession fixture, restriction state, access copy, custody root, or transfer route. Those belong to A14.

The bounded fixture owners remain responsible for their domain material:

```text
registry owns selection and hydration gesture
≠
registry owns fixture truth or domain authority
```

## 3. Normalized entry contract

Every canonical seat exposes or reserves:

- profile fixture;
- pedagogy manifest;
- workspace scenes;
- four explicit AIA route views;
- channel grammar;
- menu/home mapping;
- inspection contract;
- claim ceiling;
- missingness and alternatives;
- deterministic test journey;
- static and reduced-motion parity;
- `automatic_consequential_action = false`.

Other Ash profiles remain available as ordinary contexts without being falsely promoted as canonical demos.

## 4. Workflow-estate collapse

The durable GitHub Actions estate is reduced from eleven workflows to exactly four:

1. `td613-ci.yml` — consolidated static, constitutional, browser, closure, Phase IV, and explicit calibration validation;
2. `vercel-operator-release.yml` — one operator-authorized deployment with exact-source and bounded Chromium production confirmation;
3. `vercel-relock-safety.yml` — independent zero-deployment safety membrane that may only close a stranded Vercel lock;
4. `pages.yml` — explicit manual GitHub Pages publication.

Deleted as superseded:

- `calibration.yml`;
- `tcp-smoke.yml`;
- `ash-flowcore-live-field.yml`;
- `ash-keep-production-closure.yml`;
- `ash-keep-aia3-production-observation.yml`;
- `dome-world-phase4.yml`;
- `vercel-deployment-law.yml`.

Their unique contracts and probes were moved into the consolidated validator. Their duplicated scheduling, browser installation, local-server setup, and artifact repetition were not preserved.

## 5. Evidence placement

```text
before merge
→ one Playwright installation
→ Chromium + Firefox + WebKit
→ current principal journeys
→ closure and convergence

production release
→ exact deployed bytes
→ one Chromium desktop/mobile A13 confirmation
→ one Ash lifecycle observation
→ relock
```

This is an evidence reorganization, not evidentiary weakening. Cross-browser defects stop merge. Production confirms deployment identity without replaying the entire premerge matrix.

No exact energy, water, or emissions saving is claimed because the repository does not measure those quantities. The architectural reduction is directly observable: fewer durable workflows, fewer browser installations, fewer duplicated jobs, shorter artifact retention, and one production browser engine instead of three.

## 6. Cache and deployment posture

A13 uses ordinary monotonic asset-version advancement only:

```text
A13 asset epoch advanced: true
mass eviction executed: false
browser caches cleared: false
service workers unregistered: false
visible canonical URL mutated: false
IndexedDB erased: false
local custodial state erased: false
```

The A12–A15 operator amendment remains controlling: graph-wide mass eviction is reserved for A15 postclosure and occurs once before the final A15 production release.

## 7. Authority receipt

```text
Flow-Core commands Ash: false
Ash custody authority changed: false
raw-content transport added: false
release authority widened: false
automatic case action added: false
Archive fixture fabricated early: false
human closure required: true
receipts may cross stations: true
authority may cross stations: false
```

## 8. Exit gate

A13 may leave draft status only after:

- exact-head static contracts pass;
- the four-workflow estate test passes;
- Chromium, Firefox, and WebKit each pass the consolidated witness;
- all five promoted registry seats become actionable through one owner;
- Archive remains visibly held for A14;
- no automatic release, transport, custody mutation, or mass eviction occurs;
- the exact tested head merges;
- one operator-authorized Vercel deployment passes bounded production confirmation;
- the Vercel deployment lock is restored.

Sealed ⟐
