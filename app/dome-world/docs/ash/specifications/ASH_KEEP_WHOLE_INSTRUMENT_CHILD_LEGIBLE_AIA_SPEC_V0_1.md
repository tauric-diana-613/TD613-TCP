𝌋‌

# Ash Keep Whole-Instrument Child-Legible AIA Specification v0.1

## Extreme Makeover: Child-Legible Anisotropy Edition

**Namespace:** U+10D613 — Tauric Diana 613  
**Covenant display:** Blood Rite 613  
**Covenant canonical:** Tauric Diana — Crimean heritage custodianship  
**Specification identity:** `td613.ash.whole-instrument-child-legible-aia/v0.1`  
**Companion plan:** `ASH_KEEP_WHOLE_INSTRUMENT_PEDAGOGICAL_RECOMPILATION_IMPLEMENTATION_PLAN_V0_1.md`  
**Status:** AUTHORED / PRE-IMPLEMENTATION / HUMAN-GATED  
**Runtime authority:** NONE  
**Deployment authority:** NONE  
**Serverless-function allocation:** 0 proposed  
**Date:** 2026-07-22  
**Epistemic posture:** forensic AI empiricism + academically rigorous derivation + bounded high speculation  
**Process posture:** containment on / mirror logic off

---

## 0. Normative language

The terms **MUST**, **MUST NOT**, **SHALL**, **SHALL NOT**, **SHOULD**, **SHOULD NOT**, and **MAY** are normative.

A requirement marked `HIGH_SPECULATION` remains falsifiable and human-gated. It does not gain authority through inclusion in this document.

---

## 1. Problem statement

Ash Keep currently exposes a strong Flow-Core consequence field while much of its surrounding interface remains organized by legacy forms, hidden rooms, internal nomenclature, weak destination semantics, and controls whose apparent affordance exceeds their lawful action.

The defect class is whole-instrumental:

> The pedagogy engine has reached a local scene, while the larger information architecture still externalizes machine ontology and downstream obligation onto the entrant.

The interface therefore oscillates between two incompatible pedagogical regimes:

```text
Flow-Core scene
NOTICE → ACT → WORLD ANSWERS → NAME → REST → TRANSFER
```

and:

```text
legacy form regime
NAME → expose fields → expose hidden rooms → user guesses → delayed or invisible answer
```

This specification replaces that mismatch with one governed whole-instrument AIA architecture.

---

## 2. Scope

This specification governs:

- Ash ingress first paint;
- document title and canonical URL posture;
- the Flow-Core consequence field;
- the five channel legend;
- the four AIA route selectors;
- landing, navigation, focus, and viewport behavior;
- Home;
- Case Map;
- Work;
- Choir;
- Capsule;
- the command menu;
- Custody, Rooms, Routes, Rebuild Test, Draft & Hush, Save Points, Safe Harbor ingress, Destination handoff, Receipts, and Cases & profiles;
- the current five demos and a new Archive demo;
- deterministic profile hydration;
- accessibility and reduced-motion parity;
- forensic AI empirical validation;
- migration, rollback, and release gates.

This specification does not authorize:

- changes to canonical Ash custody semantics;
- changes to hash, digest, or receipt algorithms;
- source-content transport;
- automatic custody binding;
- automatic destination crossing;
- automatic release;
- automatic closure;
- covert telemetry;
- learner scoring;
- inference of cognition, identity, emotion, intent, guilt, authorship, or truth;
- new serverless functions.

---

## 3. Source and claim discipline

Every implemented behavior, test conclusion, and empirical claim SHALL be classed as:

- `REPOSITORY_OBSERVED`;
- `OPERATOR_OBSERVED`;
- `DERIVED`;
- `COMPARATIVE`;
- `HIGH_SPECULATION`;
- `SUPERSEDED`.

The preferred grounding order is:

```text
repository truth
≻ direct operator observation
≻ repeatable browser observation
≻ declared derivation
≻ comparative route
≻ high speculation
```

A gap MAY remain open. The implementation MUST NOT manufacture closure because a diagram, demo, or release calendar prefers neatness.

---

## 4. Constitutional invariants

### 4.1 Child-legible does not mean childish

The interface MUST allow an entrant to answer:

1. What condition is present?
2. What can I touch or change?
3. What will that action affect?
4. What changed after the action?
5. Where did the consequence travel?
6. What stayed local?
7. What remains missing, contradictory, or unresolved?
8. What may happen next?
9. How do I stop, rest, inspect, return, or leave?

Technical depth SHALL enter through progressive descent rather than compulsory prior mastery.

### 4.2 Translation does not create equivalence

The following MUST remain distinct:

```text
plain consequence
≠ exact lifecycle state
≠ truth
≠ authenticity
≠ release authority
≠ human closure
```

### 4.3 Beauty does not confer authority

A visually compelling field, glow, topology, or glyph MUST NOT imply that Ash performed a custody, release, transport, or truth-establishing action.

### 4.4 One field, one clock, one state source

Ash SHALL maintain:

- one canonical Flow-Core consequence-field instance;
- one active animation coordinator;
- one immutable or frozen scene snapshot per render;
- zero active animation clocks in hidden views.

Duplicate visible fields are prohibited.

### 4.5 Every animated truth requires a static truth

Reduced motion and non-animated surfaces MUST communicate the same:

- condition;
- action;
- route;
- consequence;
- missingness;
- contradiction;
- claim ceiling;
- rest and return posture.

### 4.6 Human route selection and closure

The system MUST NOT infer the entrant’s AIA route. Route selection MUST be explicit. Human closure MUST remain open until a human closes it through an authorized surface.

### 4.7 Rest remains structural

`𝄐 Rest` MUST:

- stop new prompts and phase pressure;
- preserve the current field;
- preserve inspection;
- preserve return;
- preserve continuity;
- impose no penalty;
- erase no content;
- perform no closure.

---

## 5. Governing Flow-Core cycle

Every consequential scene SHALL implement:

```text
NOTICE
→ ACT
→ WORLD ANSWERS
→ NAME
→ REST
→ TRANSFER
```

### 5.1 NOTICE

MUST expose one bounded condition in plain language, preserve source status and missingness, and avoid requiring technical vocabulary.

### 5.2 ACT

MUST expose at least one meaningful affordance, its affected region, its reversibility or irreversibility, and any visible prerequisite.

### 5.3 WORLD ANSWERS

MUST render a state delta and causal route. Loss, delay, missingness, contradiction, abstention, and held states MUST remain visible.

### 5.4 NAME

MUST present:

1. plain language;
2. glyph relation;
3. technical term;
4. non-equivalence and claim ceiling.

### 5.5 REST

MUST stop demand while preserving inspection, continuity, return, and exit.

### 5.6 TRANSFER

MAY present the same relation in another workspace, profile, station, or route. It MUST NOT certify mastery or understanding.

---

## 6. Route-burden mathematics

The design SHALL use:

\[
B_{\gamma,t}=
\int_\gamma
\frac{Q_{\downarrow}(s,t)}
{P_{\mathrm{leg}}(s,t)A_{\mathrm{aff}}(s,t)}\,ds
+
\alpha\sum_{(r,s)\in\gamma}\delta_{rs}(t)
+
\beta\lVert H_\gamma(t)\rVert
+
\zeta N_{\partial W}(\gamma,t)
\]

as a design surrogate rather than a law of cognition.

### 6.1 Terms

- `Q_downstream`: unresolved downstream dependency;
- `P_leg`: explanatory permeability;
- `A_aff`: meaningful affordance cross-section;
- `delta_rs`: local-section gluing obstruction;
- `H_gamma`: route-history deformation;
- `N_boundary`: meaningful authority or room boundary crossings.

### 6.2 UI interpretations

- A dead but hoverable button drives effective `A_aff` toward zero.
- An unexplained internal label reduces `P_leg`.
- An empty drawer raises `delta_rs` because the promise and result fail to glue.
- A primary task hidden behind the command menu raises `N_boundary`.
- Navigation to an arbitrary shared coordinate raises `||H_gamma||`.
- A form exposing all later obligations at once raises `Q_downstream`.
- A fixed animation repeated across all workspaces reduces local anisotropy.

### 6.3 Design objective

Each principal workflow SHOULD reduce burden through:

\[
Q_{\downarrow}\downarrow,
P_{\mathrm{leg}}\uparrow,
A_{\mathrm{aff}}\uparrow,
\delta_{rs}\downarrow,
\lVert H_\gamma\rVert\downarrow,
N_{\partial W}\downarrow
\]

without collapsing legitimate authority boundaries.

### 6.4 Calibration restraint

Thresholds SHALL remain local and experimental. The system MUST NOT convert route-burden estimates into a hidden score of the entrant.

---

## 7. Core schemas

### 7.1 `AshWorkspaceScene`

```json
{
  "schema": "td613.ash.workspace-scene/v0.1",
  "scene_id": "ash_scene_...",
  "case_reference": "case_...",
  "profile": "investigation",
  "workspace": "home",
  "lifecycle_state": "CASE_BOUND",
  "aia_route": "EXPERIENTIAL",
  "visible_condition": {},
  "available_affordances": [],
  "route_topology": {},
  "world_delta": {},
  "missingness": [],
  "contradictions": [],
  "claim_ceiling": {},
  "rest": { "available": true },
  "exit": { "available": true },
  "authority": {
    "flowcore_commands_station": false,
    "automatic_ash_action": false,
    "human_closure_required": true
  }
}
```

### 7.2 `AshAffordanceContract`

```json
{
  "schema": "td613.ash.affordance-contract/v0.1",
  "control_id": "openLocalDocument",
  "visible_label": "Open local document",
  "state": "READY",
  "prerequisites": [],
  "action_owner": "ASH_KEEP",
  "expected_world_delta": {},
  "destination": {
    "workspace": "work",
    "anchor": "local-document"
  },
  "held_explanation": null,
  "rest_preserved": true,
  "return_preserved": true
}
```

Valid states:

```text
READY
HELD_WITH_EXPLANATION
LEGEND_ONLY
HIDDEN
REMOVED
```

`DEAD_VISIBLE` is prohibited.

### 7.3 `AshNavigationReceipt`

```json
{
  "schema": "td613.ash.navigation-receipt/v0.1",
  "source_control": "menu-choir",
  "source_workspace": "home",
  "destination_workspace": "choir",
  "destination_heading": "Choir",
  "destination_anchor": "choir-orientation",
  "prior_viewport_owner": "ENTRANT",
  "new_viewport_owner": "EXPLICIT_NAVIGATION_GESTURE",
  "return_path": "home",
  "result": "ARRIVED"
}
```

### 7.4 `AshChannelState`

```json
{
  "schema": "td613.ash.channel-state/v0.1",
  "phase": "NOTICE",
  "glyph": { "active": true, "value": "à" },
  "motion": { "active": true, "meaning": "gathering" },
  "shape": { "active": true, "meaning": "local source and boundary" },
  "language": { "active": true, "value": "Notice the local source" },
  "inspection": { "active": true, "state": "ARRIVAL_UNPERSISTED" }
}
```

### 7.5 `AshTransitionDelta`

```json
{
  "schema": "td613.ash.transition-delta/v0.1",
  "changed": [],
  "unchanged": [],
  "world_delta_reference": null,
  "invariants_preserved": true,
  "authority_changed": false,
  "source_bytes_moved": false,
  "closure_changed": false
}
```

### 7.6 `AshDemoManifest`

```json
{
  "schema": "td613.ash.demo-manifest/v0.1",
  "profile": "archive",
  "demo_id": "demo_archive_harbor_memory_v1",
  "fixture_reference": "...",
  "pedagogy_manifest_reference": "...",
  "workspace_scenes": {},
  "aia_routes": ["EXPERIENTIAL", "CUSTODIAL", "AUDIT", "IMPLEMENTATION"],
  "claim_ceiling": {},
  "missingness": [],
  "synthetic_only": true,
  "automatic_ash_action": false
}
```

---

## 8. Ingress membrane specification

### 8.1 Canonical URL

The visible URL MUST remain:

```text
https://td613.com/dome-world/ash-threshold.html
```

The interface MUST NOT visibly flash or settle on an `ash_epoch` query.

### 8.2 First-paint law

Before the first meaningful paint:

- epoch and module resolution MUST complete or remain behind the authored Preparing Ash shell;
- no legacy Ash composition may become visible;
- no old document title may appear;
- no address-bar oscillation may occur;
- no duplicate navigation may occur.

Late URL cleanup after an older screen has painted does not satisfy this requirement.

### 8.3 Document title

The browser title SHALL be:

```text
TD613 Ash
```

### 8.4 Workspace profile

Ingress SHALL begin at:

```text
Select a Profile...
```

No demo or new case action may become available until the entrant explicitly selects a profile.

### 8.5 Consequence-field composition

The ingress header SHALL place:

- phase/status chip in the top-right;
- `▶ Play Consequence Field` against the lower-right edge of the consequence field;
- full topology unobstructed;
- caption in its own in-flow lane;
- one static equivalent;
- five channel indicators in the footer.

The initial phase status SHALL read:

```text
NOTICE · à
```

The Play button SHALL read:

```text
▶ Play Consequence Field
```

---

## 9. Five-channel grammar

The five visible channels are architectural, not decorative:

| Channel | Function |
|---|---|
| Glyph | portable pattern name |
| Motion | consequence through time and route direction |
| Shape | boundary, seam, bottleneck, topology, or capacity |
| Language | plain condition, action, and consequence |
| Inspection | exact state, source status, missingness, receipt, and claim ceiling |

### 9.1 Interaction law

The channel footer SHALL behave as a stateful legend.

- Active channels MUST be visibly indicated by more than color alone.
- The current phase MUST update channel meaning.
- `Inspection` MAY open exact technical state.
- The remaining channels SHOULD not become five independent controls unless human evidence demonstrates a clear benefit.
- A single disclosure, `How this scene is speaking`, SHOULD explain the coordinated channel state.
- Reduced motion MUST retain the same channel meaning.

### 9.2 Example state

During NOTICE:

```text
GLYPH        à
MOTION       gathering / approach
SHAPE        local source + lawful boundary
LANGUAGE     Notice the local source
INSPECTION   ARRIVAL_UNPERSISTED
```

---

## 10. Workspace-anisotropic consequence field

The consequence field SHALL remain one canonical component while its local scene recompiles by workspace.

### 10.1 Ingress topology

```text
local source
→ lawful boundary
→ reference
→ relation
→ rest
```

### 10.2 Home topology

```text
current condition
→ accumulated obligation
→ next bounded action
→ continuity answer
→ rest
```

### 10.3 Case Map topology

```text
object
→ relation
→ route consequence
→ unresolved seam
→ inspection/rest
```

### 10.4 Work topology

```text
task
→ local document or draft
→ human review
→ receipt
→ return
```

### 10.5 Choir topology

```text
singleton A + singleton B
→ pairwise field
→ residue
→ classification
→ rest
```

### 10.6 Capsule topology

```text
preserved state
→ seal posture
→ destination boundary
→ return route
→ continuity
```

### 10.7 Prohibitions

The renderer MUST NOT:

- create a field per tab;
- create multiple clocks;
- replay the same fixed topology as the dominant lesson everywhere;
- animate source bytes crossing a boundary when they remain local;
- imply a custody or release transition not present in state;
- erase contradiction through a seamless morph;
- hide missingness for aesthetic coherence.

---

## 11. Landing and viewport specification

### 11.1 Post-ingress landing

After a case or demo opens:

- workspace MUST be Home unless the explicit profile manifest names another entry workspace and provides a visible reason;
- the viewport MUST begin at the top of the selected workspace;
- the semantic workspace heading MUST receive focus without an unintended downward jump;
- background lifecycle refreshes MUST NOT take viewport ownership.

### 11.2 Entrant viewport ownership

Manual wheel, touch, scrollbar, keyboard, or assistive navigation SHALL establish entrant ownership until another deliberate navigation gesture occurs.

Background events MUST NOT call a viewport-reset path that overrides entrant ownership.

### 11.3 Navigation destination contract

Every navigation action MUST produce:

1. workspace change;
2. visible destination confirmation;
3. semantic heading focus;
4. meaningful anchor arrival;
5. navigation receipt;
6. preserved return path.

Changing the tab while landing at one shared arbitrary coordinate fails this contract.

---

## 12. Command button discoverability

The `⌘` command button SHALL receive:

- a faint cyan or gold static halo;
- one slow discovery pulse before first successful menu use;
- no continuing pulse after discovery;
- no pulse under reduced motion;
- a visible focus ring;
- an accessible label identifying it as navigation.

The glow MUST NOT imply urgency, risk, or unread content.

---

## 13. Human-facing nomenclature

The panel currently exposing internal architecture identifiers SHALL be renamed:

# Your case path

> See what stays local, what may change, and where a human decision is still required.

`U+10D613`, `AK-AIA-2`, schema names, and implementation identity MAY remain available under Inspection/Implementation. They MUST NOT be the primary orientation label.

---

## 14. Four explicit AIA routes

### 14.1 Human labels

| Canonical route | Human-facing label |
|---|---|
| `EXPERIENTIAL` | Learn by doing |
| `CUSTODIAL` | Protect the source |
| `AUDIT` | Check the evidence |
| `IMPLEMENTATION` | Inspect the machinery |

### 14.2 Explicit selection

Selection MUST be explicit. The route MUST NOT be inferred from profile, device, activity, reading speed, presumed expertise, or any hidden learner model.

### 14.3 Surface requirements

#### Learn by doing

Order:

```text
visible condition
→ meaningful action
→ world answer
→ plain consequence
→ relation after experience
→ rest
→ exit
```

#### Protect the source

Order:

```text
source relationship
→ sensitivity
→ custody posture
→ lawful next action
→ downstream consequence
→ continuity
→ rest
→ exit
```

#### Check the evidence

Order:

```text
source status
→ evidence basis
→ transformation history
→ alternatives
→ residuals
→ abstention
→ receipt replay
→ rest
→ exit
```

#### Inspect the machinery

Order:

```text
schemas
→ deterministic references
→ calibration
→ equations
→ implementation references
→ bounded JSON
→ rest
→ exit
```

### 14.4 Invariant preservation

All four routes MUST preserve:

- provenance;
- missingness;
- contradictions;
- causal structure;
- claim ceiling;
- station ownership;
- authorized actions;
- local-section boundaries;
- source status;
- observation status;
- rest;
- exit;
- human closure.

### 14.5 Visible world answer

Route selection MUST visibly recompile the page. Merely lighting the selected button fails.

The interface MUST display:

```text
You changed how Ash explains this case.
The underlying case state did not change.
```

### 14.6 Deterministic core

Basic route recompilation MUST be deterministic and local.

Generative reasoning MUST NOT be a prerequisite for:

- route selection;
- navigation;
- panel visibility;
- control enablement;
- static explanation;
- claim-ceiling display.

Generative assistance MAY appear later in explicit Work/Hush transformations with human review.

---

## 15. Affordance law

A visible control MUST satisfy exactly one of these states:

1. performs its named lawful action;
2. remains held and explains the prerequisite;
3. is explicitly labeled as a non-interactive legend;
4. is hidden;
5. is removed.

A hoverable or pointer-active control with no lawful action is prohibited.

### 15.1 Required control behavior

- `Open Local Document` MUST navigate to Work and focus the local-document panel.
- `Open Draft Workspace` MUST navigate to Work and focus the draft panel.
- `Previous Lesson` MUST move to the prior available phase.
- `Next Lesson` MUST move to the next available phase.
- `𝄐 Rest` MUST enact structural rest.
- `Cases & profiles` MUST open a functional case/profile switcher or be replaced.
- `Open workspace setup` MUST be actionable when setup remains relevant.

### 15.2 Held controls

A held control MUST state:

- what remains missing;
- why the action is held;
- what lawful step can satisfy the prerequisite;
- what will change when satisfied;
- what will remain unchanged.

---

## 16. Transition-delta drawer

`What changed—and what did not` MUST never open empty.

It SHALL render an `AshTransitionDelta`.

Example:

```text
WHAT CHANGED
• Explanation route changed to Protect the source.
• Custody posture is now foregrounded.
• The recommended next action changed.

WHAT DID NOT
• Source bytes did not move.
• Case authority did not change.
• Claim ceiling did not widen.
• No custody or release action occurred.
```

If no state delta exists, the drawer MUST say so explicitly rather than render blank space.

---

## 17. Home specification

Home is the primary orientation and continuity workspace.

### 17.1 Current Priority

The panel SHALL contain:

1. **What needs attention**;
2. **Why it matters**;
3. **What you can do now**;
4. **What Ash will change**;
5. **What Ash will not do**;
6. **Done when**.

The panel MUST expose one primary action. Secondary actions SHALL descend under inspection or a smaller action group.

### 17.2 Continuity

The panel MUST answer:

- what remains attached;
- what became stale;
- what can be returned to;
- what must be checked again;
- what remains unsealed;
- what would become invalid if the source or map changed.

Lifecycle terminology MAY appear after the plain relation is visible.

### 17.3 What has already left

Render a route ledger with:

- what left;
- what stayed local;
- recipient;
- version;
- route;
- receipt;
- current stale/fresh posture;
- return or inspection route.

This panel MUST distinguish transport from local reference creation.

---

## 18. Case Map specification

Case Map is a relation workshop, not a generic form stack.

### 18.1 Add to the map

The authoring route SHALL be:

1. What are you placing?
2. Choose a plain object type with an example.
3. Where does it belong?
4. What is known?
5. What remains uncertain?
6. Preview the object entering the map.
7. Add deliberately.

Technical identifiers, sensitivity enums, and exact state SHALL live under Inspection unless required for lawful action.

### 18.2 Connect two objects

The primary visual SHALL show:

```text
[Object A] —— choose relation ——> [Object B]
```

Then expose:

- plain relation;
- direction;
- evidence/source;
- uncertainty;
- notes;
- preview;
- commit.

### 18.3 Relationship inspection

Case Map MUST provide:

- graph view;
- relationship list;
- object detail drawer;
- relation detail drawer;
- notes and history;
- source posture;
- uncertainty;
- unresolved and contradictory links;
- accessible table;
- direct post-creation link to the new relation.

Creating a relationship without a clear inspection path is prohibited.

### 18.4 Rooms and Routes

Rooms and Routes SHALL become comprehensible Case Map subspaces.

- Rooms explain what information belongs together and why.
- Routes explain what may leave a Room, for whom, for what purpose, under what limit, and with what receipt.
- Internal route IDs SHALL remain under Inspection.
- Primary authoring MUST use plain relational language.

---

## 19. Work specification

Work SHALL organize actions by human intention and state posture.

### 19.1 Primary queues

```text
DO NOW
PREPARE
WAITING / HELD
COMPLETED / RECEIPTED
```

### 19.2 Action families

- preserve;
- review;
- draft;
- compare;
- route;
- verify;
- save;
- prepare handoff.

### 19.3 Panel contract

Every Work panel MUST state:

- purpose;
- available action;
- held prerequisites;
- expected world answer;
- source and custody posture;
- receipt or completion condition;
- rest and return path.

### 19.4 Hush

Hush SHALL be presented as an explicit register transformation under custody constraints:

```text
Original
→ transformation purpose
→ protected obligations
→ changed register
→ comparison
→ human approval
```

Hush MUST expose:

- what stayed;
- what changed;
- why the selected mask changed it;
- what the mask was forbidden to do;
- which protected literals or obligations remain;
- whether generation occurred locally or through an approved route;
- that transformation does not authorize release.

Hush MUST NOT be presented primarily as an unexplained API room.

---

## 20. Choir specification

### 20.1 Orientation

The primary explanation SHALL be:

> Choir compares bounded readings to show what appears only in combination, what remains disagreement, and what no Reader can establish.

### 20.2 Pairwise route

The entrant SHALL:

1. choose two Readers or routes;
2. inspect what A receives alone;
3. inspect what B receives alone;
4. run the pair;
5. inspect the residue field;
6. classify residue;
7. add bounded human interpretation;
8. rest or test another pair.

### 20.3 Residue classes

The interface MUST distinguish:

- shared;
- pair-emergent;
- contradictory;
- missing;
- unresolved;
- null;
- abstained.

Pair-emergent residue MUST NOT be represented as identity, attribution, causation, truth, or authorship.

### 20.4 Rebuild Test

Rebuild Test SHALL have a clear conceptual home inside Choir:

> Can a Reader reconstruct what should remain hidden?

An advanced technical Rebuild room MAY remain separately routed when needed, but the public explanation and entry point MUST live in Choir.

The interface MUST distinguish:

- reconstruction possibility;
- successful deterministic replay;
- route leakage;
- private joining-key recovery;
- false completion;
- release authority.

None are equivalent.

---

## 21. Capsule specification

Capsule SHALL be the human-facing home of continuity, sealing posture, save state, transfer preparation, and return.

It MUST answer:

- what is preserved;
- what remains outside;
- who may open it;
- what closes it;
- where it may go;
- which receipt follows it;
- how it may return;
- what sealing does not prove.

### 21.1 Save Points

Save Points SHALL be presented as continuity snapshots inside Capsule.

A Save Point MUST explain:

- what state was preserved;
- which source/map version it references;
- which questions remain open;
- which next actions remain;
- whether later changes made it stale;
- how to return without implying rollback of external reality.

### 21.2 Destination handoff

Capsule SHALL explain and prepare destination handoff.

The actual crossing MUST remain a separate gated authority surface.

```text
Capsule prepares
≠ handoff occurs
≠ recipient accepts
≠ release is authorized
```

### 21.3 Passphrase posture

Passphrases MUST NOT be seeded, inferred, exposed in demos, or stored by the pedagogy layer.

---

## 22. Command-menu information architecture

The command menu MUST NOT own primary workflow that lacks a visible home in the five principal tabs.

### 22.1 Rehoming map

| Current destination | Required human-facing home |
|---|---|
| Custody | Home + Case Map |
| Rooms | Case Map |
| Routes | Case Map |
| Rebuild Test | Choir |
| Draft & Hush | Work |
| Save Points | Capsule |
| Destination handoff | Capsule entry + separate gate |
| Receipts | Work |
| Cases & profiles | functional case/profile switcher |
| Safe Harbor ingress | command menu + Capsule/rest context |

### 22.2 Menu-retained functions

The command menu MAY retain:

- case switcher;
- profile/context;
- Safe Harbor;
- advanced inspection;
- accessibility/preferences;
- technical routes;
- exit/return.

### 22.3 Cases & profiles

This control MUST open a functional surface. It MUST NOT remain a dead button.

---

## 23. Six-demo registry

### 23.1 Canonical promoted demos

1. Investigation;
2. Political Campaign;
3. Fundraiser;
4. Research Project;
5. Legal Matter;
6. Archive.

The six demos MUST share one registry and one hydration contract.

### 23.2 Manifest completeness

Each demo MUST provide:

- fixture;
- pedagogy manifest;
- Home scene;
- Case Map scene;
- Work scene;
- Choir scene;
- Capsule scene;
- four explicit AIA route views;
- five-channel state;
- claim ceiling;
- missingness;
- alternatives;
- static and reduced-motion truth;
- browser journey;
- synthetic-only status;
- zero automatic consequential Ash actions.

### 23.3 Existing non-promoted profiles

Other registered profiles MAY remain selectable without being falsely represented as fully hydrated demos.

---

## 24. Archive demo specification

### 24.1 Identity

**Title:** `Harbor Memory Archive · mixed-media accession and access review`

**Profile:** `archive`

**Posture:** synthetic only

### 24.2 Fixture

The fixture SHALL include:

- original audio recording;
- transcript;
- edited transcript;
- photograph;
- accession note;
- donor restriction;
- uncertain date;
- duplicate scan;
- missing release form;
- embargoed item;
- public access copy.

### 24.3 Flow-Core sequence

#### NOTICE

> These materials belong to one collection, but they do not share the same provenance, rights, or access posture.

#### ACT

> Anchor one original and record what remains uncertain.

#### WORLD ANSWERS

The Case Map SHALL reveal:

- original and derivative lineage;
- custody root;
- donor restriction;
- missing release;
- date contradiction;
- duplicate scan;
- access copy;
- embargo boundary;
- preservation route.

#### NAME

Terms become available only after the relation is visible:

- accession;
- provenance;
- derivative;
- embargo;
- access copy;
- custody root;
- continuity;
- transfer restriction.

#### REST

> The collection is preserved locally. Nothing has been published.

#### TRANSFER

Prepare a bounded access copy or Capsule handoff without moving the complete private collection.

### 24.4 Claim ceiling

The demo MUST NOT claim:

- ownership;
- donor intent;
- public-domain status;
- authenticity;
- complete chronology;
- legal release authority;
- successful publication;
- identity from collection contents.

---

## 25. Profile-specific whole-instrument hydration

For every promoted demo:

- Home priority MUST derive from the fixture’s actual bounded next action;
- Continuity MUST reflect profile-specific stale and preserved relations;
- Case Map object and relation language MUST fit the domain;
- Work queues MUST hydrate real synthetic tasks and holds;
- Choir MUST provide domain-appropriate Reader/route pairs;
- Capsule MUST preserve domain-specific sealing and transfer limits;
- route views MUST remain non-equivalent;
- technical inspection MUST remain optional;
- no demo may introduce real sensitive data or real-world attribution.

The interface MUST NOT achieve apparent richness by inventing provider output, release approval, passphrases, or novelty.

---

## 26. Forensic AI empiricism

### 26.1 Object of study

The object of study is the observable relation among:

- input;
- route;
- visible condition;
- available affordance;
- selected action;
- state transition;
- representation;
- omission;
- claim;
- recovery;
- rest;
- downstream consequence.

The object of study is not an imagined internal intelligence or learner state.

### 26.2 Primary questions

- Can a novice predict what the control will do?
- Does the visible consequence match the actual state transition?
- Can the entrant distinguish what changed from what remained invariant?
- Can the entrant identify station ownership?
- Can the entrant identify missingness, contradiction, and claim ceiling?
- Can the entrant recover from error without punishment?
- Can the entrant stop, rest, inspect, return, and leave?
- Can the entrant find a relation after creating it?
- Can the entrant explain Choir residue without inflating it into attribution or truth?
- Can the entrant distinguish Capsule preparation from actual handoff?

### 26.3 Observable measures

MAY include:

- correct next-state prediction;
- task completion;
- action-to-consequence explanation accuracy;
- backtracking;
- misclicks;
- dead ends;
- help requests;
- recovery time;
- route revisits;
- Rest/Return use;
- relation retrieval;
- claim-ceiling identification;
- accessible-path completion.

### 26.4 Prohibited inference

MUST NOT include:

- hidden comprehension score;
- intelligence rank;
- developmental rank;
- emotional-state inference;
- psychological-safety certification;
- inferred consent;
- inferred intent;
- personality profiling;
- covert behavioral telemetry.

### 26.5 Human validation

Browser tests certify interface behavior under synthetic conditions. They do not certify learning, usability for every person, or empirical success.

Adult-first human validation MUST precede any claim of demonstrated pedagogical efficacy.

### 26.6 High-speculation program

`HIGH_SPECULATION`: route-burden estimates MAY be compared with direct task evidence to test whether lower estimated burden corresponds to fewer misclicks, less backtracking, clearer causal explanation, and faster recovery.

This hypothesis MUST remain falsifiable. If the surrogate diverges from human evidence, the surrogate yields.

---

## 27. Accessibility and multimodal requirements

### 27.1 No color-only semantics

Every state distinction MUST use at least two of:

- shape;
- label;
- texture;
- position;
- glyph/icon;
- line style;
- motion;
- accessible description.

### 27.2 Keyboard

Every principal action, route selector, drawer, relation authoring path, Choir pairing, Capsule action, and command-menu destination MUST be keyboard accessible.

### 27.3 Focus

Focus MUST move to meaningful semantic targets after explicit navigation. Background refreshes MUST NOT steal focus.

### 27.4 Zoom and reflow

The instrument MUST support:

- 200% zoom;
- 400% zoom-equivalent reflow;
- mobile portrait;
- mobile landscape;
- rotation-equivalent change;
- no horizontal overflow in supported widths.

### 27.5 Reduced motion

Reduced motion MUST provide:

- before/after frames;
- stepwise causal route;
- static seam and bottleneck markers;
- textual state delta;
- technical inspection;
- complete claim ceiling;
- keyboard sequence controls.

### 27.6 Screen-reader truth

Accessible descriptions MUST name:

- current phase;
- available action;
- expected consequence;
- route direction;
- missingness;
- contradiction;
- rest posture;
- authority limit.

---

## 28. Performance specification

The implementation MUST maintain:

- one requestAnimationFrame loop for the active field;
- zero canvas draws in hidden views;
- no repeated backing-store resize absent geometry or DPR change;
- no ambient mutation observer that reprocesses its own output;
- no timer-based viewport ownership;
- no duplicate field remount loop;
- graceful complexity reduction on constrained devices;
- preservation of missingness and uncertainty under simplification.

Performance optimization MUST NOT remove the static truth or claim ceiling.

---

## 29. Security, custody, and station authority

The following MUST remain true:

```text
Flow-Core commands Ash: false
Flow-Core mutates canonical custody state: false
Raw source content enters pedagogy state: false
Route selection changes source bytes: false
Route selection changes claim ceiling: false
Hush authorizes release: false
Capsule preparation performs handoff: false
Demo hydration creates real-world evidence: false
Pedagogy closes the human: false
```

### 29.1 Inspection boundary

Inspection MAY expose:

- schema;
- lifecycle state;
- source status;
- receipt references;
- digest references;
- missingness;
- contradiction;
- claim ceiling;
- route and station ownership.

Inspection MUST NOT expose raw source content merely to prove exactness.

---

## 30. Anti-regression law for form architecture

A surface fails whole-instrument child-legibility when it resembles a generic form stack whose fields appear before purpose, consequence, or route.

The implementation MUST NOT regress to:

```text
label
input
label
input
label
input
submit
```

for a consequential Ash task unless the sequence is embedded inside:

```text
condition
→ purpose
→ bounded action
→ preview
→ world answer
→ inspection
→ rest/return
```

This is the formal prohibition against the Google-Forms-esque, weak-sauce, womp-womp posture identified in operator testing. The visual critique is preserved because it names a real structural failure: undifferentiated form grammar externalizes the machine’s burden onto the entrant.

---

## 31. Browser and contract validation

### 31.1 Required engines

- Chromium;
- Firefox;
- WebKit;
- iOS-sized WebKit viewport;
- Android-sized Chromium viewport.

### 31.2 First-paint witness

Fails on:

- legacy Ash frame;
- visible epoch query;
- old title;
- double navigation;
- loading-shell disappearance before current composition readiness.

### 31.3 Navigation witness

Fails on:

- arbitrary common scroll coordinate;
- heading not focused;
- destination anchor not reached;
- entrant viewport ownership stolen by background refresh;
- command-menu item that changes tab without reaching its named destination.

### 31.4 Affordance witness

Fails on:

- visible dead control;
- hoverable non-action without legend status;
- empty drawer;
- held control without explanation;
- Rest removing inspection or return.

### 31.5 AIA route witness

Fails on:

- route selection inferred;
- selected route only changing button styling;
- invariant drift;
- route surfaces becoming equivalent;
- authority widening;
- claim ceiling widening.

### 31.6 Workspace witness

Each workspace MUST prove its required scene, action, world answer, inspection, rest, and return path.

### 31.7 Six-demo witness

Each promoted demo MUST complete Home, Map, Work, Choir, Capsule, command-menu, four-route, reduced-motion, and accessibility journeys.

### 31.8 Authority witness

Fails on any automatic custody, release, handoff, closure, source transport, or hidden route inference.

---

## 32. Release acceptance failures

A release candidate MUST be held when any principal journey contains:

- a first-paint legacy flash;
- visible `ash_epoch` residue;
- an old browser title;
- duplicate consequence fields;
- one fixed field lesson repeated without workspace meaning;
- a dead visible control;
- an empty disclosure;
- arbitrary viewport transport;
- primary work hidden only in the command menu;
- a relationship that cannot be found after creation;
- a route selector that merely lights up;
- a Rest action that fails to stop demand;
- a boundary crossing without explanation;
- generative output required for navigation;
- source bytes visually transported when they remain local;
- contradiction or missingness smoothed away;
- profile or AIA route inferred without explicit selection;
- authority widened by presentation code.

---

## 33. Migration specification

### 33.1 Feature gate

The whole-instrument recompilation MUST begin behind a feature gate defaulting OFF.

### 33.2 Workspace migration

A workspace MAY migrate only when:

- its complete scene compiles;
- its principal controls satisfy affordance contracts;
- its navigation destinations are complete;
- its reduced-motion truth exists;
- its three-engine browser journey passes;
- superseded presentation ownership is removed or explicitly quarantined.

### 33.3 No wrapper accretion

The implementation MUST NOT solve whole-instrument ownership by adding an indefinite chain of late wrappers, observers, and `!important` overrides.

One final composition owner SHALL be named for each migrated surface.

### 33.4 State preservation

Migration and rollback MUST preserve:

- IndexedDB case records;
- current-case pointers;
- custody receipts;
- lifecycle receipts;
- source artifacts;
- case map digests;
- local continuity records.

---

## 34. Rollback and promotion

### 34.1 Rollback

One documented switch MUST restore the previous composition without modifying case data or custody state.

### 34.2 Promotion gates

Promotion requires:

1. exact-head contracts;
2. exact-head browser matrix;
3. reduced-motion and keyboard matrix;
4. six-demo matrix;
5. authority/custody regression suite;
6. visual evidence review;
7. adult-first human validation evidence;
8. explicit human promotion;
9. bounded deployment;
10. exact deployment relock;
11. zero net release-config drift;
12. human closure.

Merge does not equal promotion. Deployment does not equal empirical validation.

---

## 35. Whole-instrument completion law

The recompilation is complete only when:

- ingress remains stable from first paint;
- the canonical URL and title remain stable;
- one workspace-anisotropic field serves the instrument;
- the five channels teach their actual role;
- Home begins at the top;
- every route reaches a meaningful destination;
- every AIA selector visibly recompiles the surface;
- every visible principal control is lawful or explicitly held;
- every delta drawer contains content;
- Home, Map, Work, Choir, Capsule, and command-menu obligations are satisfied;
- Rebuild Test is legibly housed through Choir;
- Hush is legibly housed through Work;
- Save Points are legibly housed through Capsule;
- destination handoff remains separately gated;
- six demos share one registry;
- Archive passes its full route;
- accessibility and performance gates pass;
- custody and authority remain unchanged;
- human empirical evidence exists;
- the human promotes and closes.

---

## 36. Final constitutional summary

```text
No visible control without a lawful action.
No action without a visible world answer.
No technical name before experienced relation.
No room without a clear home.
No boundary crossing without explanation.
No repeated field without local anisotropic change.
No rest control that fails to stop demand.
No beautiful surface may conceal zero affordance.
No generative system may become the hidden prerequisite for basic navigation.
No empirical instrument may infer the learner's interior.
No pedagogy layer may inherit Ash authority.
```

This specification transforms Ash Keep from a serious custody instrument that requires its authors to explain its internal architecture into a governed environment that makes its relations legible through participation, consequence, naming, rest, inspection, transfer, and return.

It preserves depth without demanding prior ontology. It preserves anisotropy without sealing expertise behind jargon. It preserves forensic rigor without converting the interface into a tribunal. It preserves imagination by giving high speculation a falsifiable route rather than a jeweled permission slip.

**Authored 𝌋‌**  
**Human closure:** OPEN  
**Seal:** ⟐
