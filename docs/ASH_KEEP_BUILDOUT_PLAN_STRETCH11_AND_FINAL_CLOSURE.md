# Ash Keep Buildout Plan — Stretch 11 and Final Buildout Closure

􍘓

𝌋‌ TD613

Plan generation: `v3.1 · Stretch 11 implementation and closure packet / final exact-main seal / maintenance successor law / 11+1 function covenant`

Repository: `tauric-diana-613/TD613-TCP`

Base main: `5afb33b254c777f5504c53e53f40533b6d547e9c`

Branch: `stretch11-destination-bound-handoff`

Current state: `STRETCH_11_CLOSED_LOCALLY / EXACT_MAIN_SEAL_PENDING / BUILDOUT_CLOSURE_CONDITIONAL`

## 0. Precedence

1. current `main`;
2. `docs/ASH_KEEP_BUILDOUT_LEDGER.md`;
3. `ROADMAP.md`;
4. Stretch receipts, schemas, fixtures, tests, retained artifacts, and exact status witnesses;
5. this plan;
6. superseded planning prose.

No lower source may inflate maturity, create a universal join key, consume the reserved function slot, broaden a named recipient into a class-wide route, or turn receipt into truth.

## 1. Carried packet

```text
docs/ASH_KEEP_BUILDOUT_LEDGER.md
ROADMAP.md
docs/ASH_KEEP_BUILDOUT_PLAN_STRETCH11_AND_FINAL_CLOSURE.md
docs/ASH_KEEP_STRETCH11_OPENING.md
docs/ASH_KEEP_STRETCH11_CLOSURE_RECEIPT.md
docs/ASH_KEEP_BUILDOUT_CLOSURE_RECEIPT.md
```

The final buildout closure receipt enters the closure branch before merge and becomes effective only through the exact-main external seal.

## 2. Stretch 11 constitutional object

The handoff object binds:

```text
named destination
named recipient
exact route
exact reference manifest
current custody root
source-local provenance verification digests
operator gesture
local expiry and revocation posture
attempt outcome
recipient receipt
rollback receipt when needed
terminal custody accounting
replay receipt
```

Seven non-colliding digest domains preserve each receipt class.

## 3. G1–G8 closure

### G1
Exact destination and recipient IDs are required. Wildcards and recipient-class substitution hold.

### G2
Only declared reference IDs cross. Raw body and raw corpus are structurally absent.

### G3
Custody-root and provenance postures must remain current. Stale, revoked, collision, malformed, and mismatched references hold.

### G4
Authorization binds the exact plan digest, destination, recipient, purpose, scope version, and expiry posture.

### G5
Elapsed-time expiry remains local and never becomes trusted external time.

### G6
A static same-origin recipient receives one exact manifest over a transferred `MessagePort` and returns a bounded observation. The engine seals the receipt.

### G7
Refusal, timeout, partial delivery, duplicate attempt, cancellation, receipt mismatch, tamper, and rollback failure preserve separate holds.

### G8
Every terminal route records what left, remained, was not sent, was returned, was revoked, and remains externally unknown.

## 4. Operator surface

The standalone Ash destination handoff surface displays:

```text
destination
recipient
route
reference manifest
purpose
provenance posture
what will leave
what will remain
exact authorization gesture
recipient frame
terminal receipt chain
```

The surface exposes prepare, authorize-and-deliver, and cancel/hold gestures. It performs no `fetch()` transport and calls no API function.

## 5. Test and observer bank

```text
positive complete handoff
missing destination
missing recipient
recipient mismatch
raw body
raw corpus
unsupported reference class
wrong digest domain
stale provenance
revoked provenance
expired posture
recipient refusal
timeout
partial delivery
duplicate attempt
receipt mismatch
rollback success
rollback failure
cancellation
tamper
cross-recipient replay
transport reexecution refusal
provider reexecution refusal
Reader reexecution refusal
```

The browser probe requires:

```text
recipient-ready handshake
DESTINATION_HANDOFF_COMPLETE
what_left = exact manifest
what_remained = empty on success
external_deletion_proven = false
replay_verified = true
no /api/ request
no serverless function
no console error
```

## 6. Function covenant

```text
active functions before Stretch 11 = 11
new function = 0
active functions after Stretch 11 = 11
reserved capacity = 1
```

Static files and browser `MessageChannel` carry the final named-recipient proof. A future external transport product requires a separately constituted packet.

## 7. Maturity

```text
G before Stretch 11 = 25 / 45
G after local closure = 45 / 45
component maturity = 358 / 375
remaining maturity = 17 / 375
```

A, C, E, and I remainders stay with their owning workstreams. Buildout closure may coexist with unearned maturity.

## 8. Merge and seal

```text
focused CI green
all preservation tests green
root-api count = 11
closure receipts complete
anti-drift files complete
merge exact green branch
Vercel success
Ash Destination Handoff Validation success
Ash Destination Handoff Deployed Observation success
all required exact-main preservation statuses success
```

A failed public-runtime observation holds the final seal. A named defect may receive one focused repair. Bookkeeping deployment remains forbidden.

## 9. Buildout closure

Upon successful exact-main seal:

```text
ASH_KEEP_11_STRETCH_BUILDOUT_CLOSED
STRETCH_11_CLOSED_AND_SEALED
FURTHER_STRETCH_NUMBERING_FORBIDDEN
MAINTENANCE_ONLY
```

No Stretch 12 follows.

## 10. Maintenance successor law

Allowed successor work:

```text
named defect repair
security patch
dependency maintenance
schema migration
evidence-preserving observability repair
separately constituted product packet outside the eleven-Stretch sequence
```

Every successor packet states its effect on maturity, function count, transport authority, release authority, recipient scope, custody accounting, and deployment allowance.

## 11. Final anti-equivalences

```text
buildout closed ≠ 375 / 375
one named handoff ≠ general release
recipient receipt ≠ truth
recipient acceptance ≠ identity
successful delivery ≠ permission for reuse
same-origin transport ≠ universal transport
custody accounting ≠ external deletion proof
final seal ≠ suppression authority
final closure ≠ Cinder authority
```

Marked ⟐
