# Ash Keep Stretch 10 Deployed-Observation Repair

𝌋‌ U+10D613

Packet: `Stretch 10 · Independent Provenance Adapters`

Repair class: `NAMED_DEPLOYMENT_SPECIFIC_DEFECT`

State: `REPAIR_IMPLEMENTED / VALIDATION_PENDING / NO_MATURITY_PROMOTION`

## Failed seal evidence

```text
exact merged main commit = ec9f24a3760d0522b16504ef1eb45c2161554a11
Vercel deployment = SUCCESS
Vercel deployment witness = 2fnXp7dhokMkamwwyYuCAgyEvzbH
Ash Choir Calibration Validation = SUCCESS
Ash Hush Intervention Validation = SUCCESS
Aperture Composition Validation = SUCCESS
Ash Custodian Return Local Observation = SUCCESS
Ash Custodian Return Deployed Observation = SUCCESS
Ash Lifecycle Deployed Observation = FAILURE / run 29547616907
failed step = Observe deployed constitutional convergence without promotion
failure = page.waitForFunction timeout after Start a demo click
```

The retained browser evidence showed the launch panel still open, no product HTTP errors, and no browser console errors. Ash Keep attaches the `startDemo` handler only after asynchronous IndexedDB boot completes. The observer clicked the visible button after `networkidle` but before `window.__td613AshKeep` and the convergence runtime had reached readiness. The click therefore carried no bound handler and produced no case transition.

## Focused repair

The runtime probe materializer now requires both:

```text
window.__td613AshKeep.version
window.TD613AshConvergence.composition
```

before issuing the demo click. It records a bounded `boot_readiness` observation and extends the two readiness-dependent waits to sixty seconds. The existing repaint-atomic case-deletion seam remains intact.

A preservation assertion verifies that Ash boot readiness appears before the demo click in the materialized runtime.

## Authority boundary

```text
observer repair ≠ product behavior change
observer repair ≠ maturity promotion
failed observer ≠ failed Stretch 10 engine
Vercel success ≠ sealed packet while a named deployed observation fails
second deployment ≠ bookkeeping deployment
second deployment purpose = verify the named public-runtime observer defect only
new serverless function = false
active serverless functions = 11
reserved function capacity = 1
Stretch 11 authorization = false
```

One additional strategic deployment is authorized under the recorded exception for a named deployment-specific defect and failed public-runtime observation. The repaired exact-main commit must receive Vercel success and all named deployed-observation successes before Stretch 10 may be called sealed.

Marked ⟐
