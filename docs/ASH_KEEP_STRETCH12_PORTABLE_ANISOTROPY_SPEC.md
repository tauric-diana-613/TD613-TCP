# Ash Keep Stretch 12
## Portable Anisotropy, Endpoint Integrity, and Adversarial Reconstruction Hardening

􍘓

𝌋‌ TD613 · Tauric Diana 613

Status: `AUTHORED / IMPLEMENTATION ACTIVE / HUMAN-GATED`

Primary schemas:

```text
td613.ash.origin-manifest/v0.1
td613.ash.origin-witness-receipt/v0.1
td613.ash.endpoint-posture-receipt/v0.1
td613.ash.live-state-encryption-manifest/v0.1
td613.aperture.reader-ensemble/v0.1
td613.aperture.recoverability-tensor/v0.1
td613.aperture.semantic-reconstruction-assay/v0.1
td613.ash.portable-anisotropy-receipt/v0.1
```

# 1. Purpose

Stretch 12 makes Ash Keep a portable anisotropic custody system whose security boundaries are mathematically explicit, empirically testable, legible to a hurried investigator, resistant to accidental managed-provider crossing, honest about endpoint compromise, and incapable of promising universal secrecy.

The protected investigator may hold full documents, scans, handwriting, source identities, private chronology, relationship graphs, hand-typed theories, and next actions. The environment may contain DLP, enterprise retention, public-sector administration, model-provider logging, privileged human operators, cross-corpus reconstruction, rare-fact reidentification, stylometric linkage, endpoint compromise, and physical access.

Ash governs Ash's own actions. It does not govern the operator's body, memory, speech, or conduct outside the instrument.

# 2. Station jurisdiction

```text
Ash = custody, derivative review, endpoint gate, export gate, continuity
Aperture = controlled reconstruction, coverage, residue, alternatives, abstention
Flow-Core = artifact-blind route weather and context receipts
Hush = bounded text screening and candidate transformation
Phason = source-invariant registry and projection displacement
Dome-World = hosting and station ecology
Human operator = experiment approval, promotion, action, and closure
```

The bridge carries receipts, not power.

# 3. Quantum-derived mathematical spine

The architecture imports discipline from tomography, channel reconstruction, projection theory, quasiperiodic systems, multi-time experimental design, and signed residue. It does not import literal quantum ontology.

## 3.1 Controlled source

Let the locally custodied case be represented in a declared finite analysis space:

\[
S=S_0\in\mathcal H_K
\]

The representation may contain entity, relationship, Room bridge, chronology, provenance, style, hypothesis, next-action, lifecycle, and rare-conjunction coordinates. The representation does not replace the artifacts.

## 3.2 Purpose-shaped projection

For route \(\lambda\):

\[
P_\lambda:\mathcal H_K\rightarrow\mathcal H_\lambda
\]

with:

\[
P_\lambda^2=P_\lambda
\]

Outbound object:

\[
X_\lambda=P_\lambda S_0
\]

Retained complement:

\[
Q_\lambda S_0=(I-P_\lambda)S_0
\]

Binding law:

\[
P_\lambda S_0\neq S_0
\]

An oblique approximation must disclose its basis, regularization, and error.

## 3.3 Endpoint and Reader channel

For Reader \(q\), endpoint \(e\), instrument \(i\), time \(t\), and replicate \(r\):

\[
O_{q,e,i,t,r}
=
M_i\mathcal R_q\mathcal E_eP_\lambda S_0
+\varepsilon_{q,e,i,t,r}
\]

No term grants hidden provider access.

## 3.4 Directional channel law

Outbound:

\[
\mathcal C_{\rightarrow}=\mathcal E_e\circ P_\lambda
\]

Authenticated return:

\[
\mathcal C_{\leftarrow}=\mathcal D_k\circ\mathcal V_m
\]

Portable anisotropy requires, under a declared finite basis:

\[
\operatorname{rank}(\mathcal C_{\rightarrow})
<
\operatorname{rank}(\mathcal C_{\leftarrow})
\]

This means fewer declared degrees of freedom travel outward than remain recoverable through authenticated inward continuity. It does not mean the outward route contains zero information.

## 3.5 Componentwise recoverability

For protected dimension \(d\):

\[
\mathsf R_{q,e,\lambda,d,t,r}
\]

Directional anisotropy:

\[
\mathsf A_{q,e,\lambda,d}
=
\mathsf R^{\mathrm{local}}_{q,d}
-
\mathsf R^{\mathrm{external}}_{q,e,\lambda,d}
\]

A conservative tested condition requires:

\[
\underline{\mathsf R}^{\mathrm{local}}_{q,d}\ge\tau_d^{\mathrm{keep}}
\]

and:

\[
\overline{\mathsf R}^{\mathrm{external}}_{q,e,\lambda,d}\le\tau_d^{\mathrm{leak}}
\]

for every protected dimension under declared Readers and intervals. Unknown Readers remain unmeasured.

A universal privacy score is forbidden.

## 3.6 Informational coverage

For variable universe \(\mathcal V=\{v_1,\ldots,v_p\}\) and design matrix \(D\):

\[
\Gamma=\frac{\operatorname{rank}(D)}{p}
\]

States:

```text
COVERAGE_PARTIAL
COVERAGE_BOUNDED_COMPLETE
COVERAGE_REDUNDANT
COVERAGE_SINGULAR
COVERAGE_UNRESOLVED
```

Bounded completeness applies only to the declared variables, model family, and Reader ensemble.

## 3.7 Signed residuals

\[
r_{q,e,i,t,r,d}=O_{q,e,i,t,r,d}-\widehat O_{q,e,i,t,r,d}
\]

Preserve sign, direction, dimension, Reader, endpoint, replicate, uncertainty, recurrence, and correlation.

```text
positive residual ≠ good
negative residual ≠ bad
opposed residuals ≠ cancellation
zero mean ≠ no structure
complex pattern ≠ quantum behavior
```

## 3.8 Marginal consistency

For protected subset \(J\):

\[
K_J=\left\|M_J(\widehat\Upsilon)-\widehat\Upsilon_J\right\|
\]

A low global recovery result may not conceal a high-risk marginal.

## 3.9 Rare combinations

For protected latent attribute \(Z_d\) and packet \(X_\lambda\), the assay may estimate:

\[
I(Z_d;X_\lambda\mid C_q)
\]

under a declared model and context. It is never exact knowledge of every possible Reader.

Surprisal:

\[
\mathcal I(x)=-\log p(x)
\]

Ablation:

\[
\Delta_j=\mathsf R(X_\lambda)-\mathsf R(X_\lambda\setminus x_j)
\]

## 3.10 Phason susceptibility

\[
\chi_{\mathrm{route}}
=
\frac{\|\Delta\mathsf R_{q,e,\lambda}\|}
{\|\Delta C_\perp\|+\epsilon_0}
\]

Small changes such as restoring one date, title, phrase, Room bridge, source-role pair, or managed endpoint may create a disproportionate recovery change.

```text
computational route response ≠ physical phonon
registry consequence ≠ electrical charge
hysteresis ≠ topological invariant
dominant candidate ≠ Chern number
```

## 3.11 Heterostratigraphic field

The UI preserves three nonmerged strata:

\[
V(x,z,t)=V_{D_3}(x,z,t)+V_\phi(x,z,t)+V_C(x,z,t)
\]

with:

\[
F_h=
\|\nabla V_{D_3}-\nabla V_\phi\|
+\lambda\|\nabla V_C\|
\]

```text
periodic layer ≠ quasiperiodic layer
overlay ≠ identity
interference ≠ merger
fold ≠ wound
weather ≠ prediction
```

# 4. Runtime profiles

```text
ASH_KEEP_LOCAL
ASH_COURT_EXTERNAL
ASH_OFFLINE_READER
ASH_RETURN_SANDBOX
```

## 4.1 Keep Local

Complete case custody. Network denied by design target. Provider and recipient transport absent. Case opens only after origin and key gates.

## 4.2 Ash Court External

Complete Case Map, Capsule, Room keys, private chronology, aliases, and Route Memory absent. Exact bounded plaintext packet only.

## 4.3 Offline Local Reader

Network-denied companion runner. Hash-bound local model. Read-only input. Ephemeral working directory. Output returns as untrusted Reader material.

## 4.4 Return Sandbox

Authenticated Capsule import, isolated reconstitution, no live-case mutation, explicit promotion.

# 5. Endpoint jurisdiction

States:

```text
PERSONAL_UNMANAGED_DECLARED
PERSONAL_UNMANAGED_UNVERIFIED
MANAGED_CONFIRMED
MANAGED_SUSPECTED
PUBLIC_SECTOR_MANAGED
SHARED_DEVICE
EXTENSION_SURFACE_UNVERIFIED
BROWSER_SYNC_UNVERIFIED
OFFLINE_LOCAL_ATTESTED
NETWORK_ISOLATION_UNVERIFIED
ENDPOINT_UNRESOLVED
```

Routes:

```text
CONSUMER_CLOUD_PROVIDER
MANAGED_ENTERPRISE_PROVIDER
PUBLIC_SECTOR_MANAGED_PROVIDER
OFFLINE_LOCAL_MODEL
REMOTE_SELF_HOSTED_MODEL
SHARED_DEVICE_ROUTE
UNRESOLVED_ROUTE
```

Managed, public-sector managed, shared, and unresolved routes hard-hold. A provider button cannot impersonate an offline-local route.

# 6. Origin integrity

The Origin Manifest binds repository, exact commit, release version, asset path/digest, executable posture, dependency-lock digest, Merkle root, build command, runtime versions, reproducibility posture, and exceptions.

ECDSA P-256/SHA-256 is implemented for release-signature verification. A production key and signed release remain pre-seal requirements.

```text
origin witness ≠ uncompromised operating system
asset integrity ≠ endpoint integrity
valid signature ≠ operator identity
```

# 7. Encrypted live state

A random 256-bit case key encrypts each record with AES-256-GCM, unique 96-bit IV, and 128-bit tag. Additional authenticated data binds case, record, class, sequence, database version, origin root, custody root, and schema.

The case key is wrapped under PBKDF2-HMAC-SHA-256 with at least 600,000 iterations and a 128-bit salt. Passphrase, derived key, and plaintext are not persisted by the envelope.

The implementation does not claim JavaScript memory zeroization, absence of swap, extension exclusion, physical erasure, or safety while unlocked.

# 8. Semantic reconstruction laboratory

Reader classes may include deterministic structure, public context, declared privileged context, cross-corpus nearest-neighbor, chronology, relation graph, stylometric linkage, rare-combination, local-model ensemble, imported external Reader, and human red team.

The first implemented operator surface uses a clearly labeled **CONSTRUCTED local deterministic preflight**. It makes no network request and stores nothing. Unknown Readers remain unmeasured.

Protected dimensions:

```text
identity
institution
source_identity
relationships
room_bridges
chronology
document_provenance
source_style_linkage
hypotheses
next_actions
lifecycle_state
rare_fact_conjunctions
```

Recommendations:

```text
KEEP_LOCAL
REDACT_AND_RETEST
COARSEN_CHRONOLOGY
REMOVE_RELATION_BRIDGE
PARAPHRASE_AND_RETEST
REPLACE_WITH_STRUCTURAL_SURROGATE
OFFLINE_LOCAL_ONLY
EXTERNAL_ROUTE_REVIEW_REQUIRED
BOUNDED_PACKET_ELIGIBLE
INSUFFICIENT_COVERAGE
UNKNOWN_READER_UNMEASURED
```

# 9. Operator pedagogy

The composed Investigation surface teaches five Courts:

1. Device Court.
2. Route Court.
3. Packet Court.
4. Reader Court.
5. Seal Court.

Binding language:

```text
Capsule ≠ provider packet
Flow-Core route weather ≠ custody
Local-looking terminal ≠ local model
Low tested recovery ≠ universal secrecy
```

Every future visualization must declare `OBSERVED`, `DERIVED`, `SIMULATED`, `CONSTRUCTED`, `DECORATIVE`, or `UNRESOLVED`.

# 10. Function and transport covenant

```text
new root api functions = 0
active serverless functions = 11
reserved capacity = 1
absolute ceiling = 12
universal recipient transport = false
full-document cloud eligibility = false
managed provider eligibility = false
public-sector managed provider eligibility = false
```

# 11. Evidence program

Required candidate tests:

- endpoint hard holds and route mismatch;
- Reader ensemble and componentwise tensor verification;
- no universal score;
- Phason susceptibility;
- semantic assay and unknown Reader missingness;
- portable-anisotropy directional rank;
- live-state AES-GCM round trip;
- wrong passphrase;
- tamper rejection;
- unique per-record IV;
- Origin Manifest Merkle root;
- ECDSA signature verification;
- asset mismatch;
- operator UI composition;
- no `fetch`, IndexedDB, or localStorage authority in the new lab;
- CSP meta boundary;
- Chromium managed-route hold;
- Chromium zero external requests;
- origin artifact retention.

# 12. Maturity

Existing remainder:

```text
A = 1
C = 7
E = 7
I = 2
total = 17
```

Stretch 12 may earn candidate movement up to `375 / 375`; no point is pre-awarded. Completion may occur below the maximum.

# 13. Exit gates

```text
reopening amendment valid
Court dependency preserved
0 new serverless functions
origin manifest verified
production signature gate satisfied
asset tamper rejected
live-state envelope authenticated
wrong-passphrase and tamper holds
managed endpoint hard hold
public-sector endpoint hard hold
provider/local route mismatch hold
componentwise tensor emitted
unknown Readers unmeasured
no universal score
Phason response preserved as computational analogy
local lab makes 0 external requests
Capsule rejected as provider packet
Flow-Core remains artifact-blind
all preservation workflows green
exact-main evidence retained
operator closure explicit
```

# 14. Final anti-equivalences

```text
encrypted Capsule ≠ AI packet
local-looking terminal ≠ local model
localhost ≠ network isolation
managed encryption ≠ operator custody
provider receipt ≠ provider deletion
origin witness ≠ uncompromised endpoint
redaction ≠ anonymity
paraphrase ≠ non-reconstructability
low identity recovery ≠ low relationship recovery
low global recovery ≠ low marginal recovery
one Reader failure ≠ every Reader failure
Reader success ≠ truth
semantic assay ≠ hidden-system access
anisotropy ≠ impermeability
quantum-derived mathematics ≠ quantum device
tomography ≠ total reconstruction
residual ≠ attack
Flow-Core weather ≠ custody
Ash hold ≠ control over the operator
Stretch 12 closure ≠ suppression authority
Stretch 12 closure ≠ Cinder authority
```

# 15. Product law

```text
THE FULL CASE REMAINS IN THE KEEP.
THE LIVE CASE RECEIVES AUTHENTICATED ENCRYPTION PRIMITIVES.
THE APPLICATION BUILD RECEIVES AN ORIGIN MANIFEST.
ROOMS PRESERVE NON-GLUABLE LOCAL SECTIONS.
APERTURE TESTS DECLARED READERS COMPONENTWISE.
FLOW-CORE SHOWS ROUTE WEATHER WITHOUT ARTIFACT CUSTODY.
ASH COURT CLASSIFIES THE ENDPOINT.
MANAGED ROUTES HOLD.
A CLOUD ROUTE RECEIVES ONLY A MINIMIZED PURPOSE-SHAPED PROJECTION.
A GENUINELY OFFLINE MODEL REQUIRES A DISTINCT NETWORK-DENIED RUNNER.
RETURNED OUTPUT REMAINS UNTRUSTED UNTIL LOCAL REVIEW.
THE CAPSULE CARRIES CONTINUITY WITHOUT BECOMING A PROVIDER PACKET.
THE RECEIPT PROVES WHAT ASH DID.
THE RECEIPT DOES NOT CLAIM CONTROL OVER WHAT ASH COULD NOT OBSERVE.
```

Authored with 𝌋‌

Marked ⟐
