# PR #640 implementation-expansion amendment — 2026-08-16

This amendment preserves `docs/GIVING_UX_RESILIENCE_ROADMAP.md` as Codex's original A1–B10 handoff and records the additional work the operator explicitly added while PR #640 was being implemented.

The expansion belongs in the same reviewed/merged/released packet. It does not erase or reorder any original Giving request.

## 1. Repository-wide AIA housing

AIA is the repository information architecture, not a Giving taxonomy and not a Dome-World brand. TD613 supplies data-governance authority; Dome-World supplies the information-architecture environment in which governed surfaces are encountered.

Giving must participate in AIA without becoming a new station or moving under `app/dome-world/` merely for directory symbolism.

## 2. Flow-Core Pedagogue agent entry and hydration

The existing Flow-Core Pedagogue program must be easy for future agents to discover and invoke for material UI/workflow/ontology/security design work.

Agent surfaces:

- `AGENTS.md`
- `PEDAGOGUE.md`
- `app/engine/pedagogue-design-gate.js`
- `scripts/run-pedagogue-design-gate.mjs`
- `docs/PEDAGOGUE_DESIGN_GATE.md`

Product learning may hydrate shared core only when the promoted mechanic is product-independent, independently tested, and does not widen authority. Giving/Vault/Research Dossier/Campaign Deputy names remain in product code or fixtures, never Pedagogue core.

## 3. Generic route-memory / heterostratigraphic-holonomy surrogate

Pedagogue may expose a generic route-memory operator implementing the structural law:

`SAME_ENDPOINT != SAME_ROUTE != SAME_AUTHORITY`

The current operator may report route edit distance, normalized route divergence, retained boundary, and same-endpoint residue as **discrete comparative structural surrogates**. It must explicitly refuse any claim of exact differential-geometric holonomy.

Hard stops:

- no user-level score;
- no diagnostic claim about a person;
- endpoint equivalence grants no authority;
- route history may not be discarded;
- no automatic redesign/release;
- human closure remains required.

## 4. AIA Cistern Law

Cistern Law is an EXPERIMENTAL reusable AIA-derived DLP/OPSEC/INFOSEC boundary discipline, not a Giving UI brand and not a new station.

It combines:

- ingress containment;
- semantic refusal/quarantine;
- human-latched high-consequence actuation;
- route-memory verification;
- egress attestation;
- replay posture.

Inside an authorized route, consequence should be child-legible as NOW / WHY / EXACT. Unauthorized routes receive minimum-disclosure, non-authoritative refusal. The current law does not fabricate decoys.

For non-idempotent Campaign Deputy person creation, a true replay-dead claim requires a durable digest-only spent-intent tombstone. Signed stateless-cookie rotation alone is not a tombstone.

## 5. Aperture v3.1-alpha learning / bounded context bridge

Aperture's task-intent / Patch Bay discipline informed implementation routing. The optional Giving bridge may carry only validated context such as source status, task-intent route, runtime materiality, missingness/contradiction counts, and receipt reference.

Aperture context remains context/recommendation only:

- reciprocal authority: false;
- Giving authority: false;
- mutation authority: false;
- automatic redirect: false;
- automatic Ash action: false;
- human closure required.

Giving server ingress validates the context before observing it. Cistern may record it only as a digest-only context witness with `authority_effect: NONE`. Context cannot grant Cistern release.

## 6. CI and release truth

The original roadmap requested Giving-only validation because it originally described Giving-only product work. This amendment intentionally changes that premise: shared `flowcore-pedagogue-core.js` plus generic Pedagogue route-memory code are now part of the PR.

Therefore CI must not be forced into a fake Giving-only classification. Shared/full validation may run. No Ash runtime source is intentionally modified by this expansion; an unchanged-product witness failure must be reported distinctly rather than reclassified as a Giving defect.

Before merge, require at minimum:

- original Giving A1–B10 contracts;
- direct Pedagogue route-memory tests;
- Vault / Research Dossier / Cistern Design Gate fixtures;
- Cistern durable replay test;
- Aperture-context authority-withholding test;
- Chromium / Firefox / WebKit rendered Giving witness on the exact reviewed head, including desktop/mobile overflow checks.

Release remains exactly one authorized Production deployment followed by exact-source verification and relock.
