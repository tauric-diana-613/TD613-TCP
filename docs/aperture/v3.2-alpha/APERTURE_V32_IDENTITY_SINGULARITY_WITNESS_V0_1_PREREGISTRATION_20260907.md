# TD613 Aperture v3.2-alpha · Identity Singularity Witness v0.1

**State:** PREREGISTERED / UNIMPLEMENTED — **NO 𝄐 YET**  
**Exact parent:** `d652c5e151471be7e40ff6a08936ba26c0cef1ad`  
**Instrument:** `app/aperture/tool.html`  
**Release / merge / Vercel / production authority:** CLOSED

## 0. Why this chamber exists

The v3.2 repo-guided evolution law requires the current standalone identity to remain singularly:

```text
TD613 Aperture v3.2-alpha
td613-aperture/v3.2-alpha
```

while inherited v3.1/v3.0/v2.9.x contracts retain their historical lineage labels.

A separately supplied standalone witness, `Aperture_v3_2-alpha(10).html`, was observed to contain multiple inherited current-identity writers that can restore v3.1 human-facing identity while other machine-facing surfaces remain v3.2. That attachment is therefore treated as a **quarantined divergent witness**, not as repository source authority.

This chamber does not rewrite that external attachment or retroactively treat its behavior as canonical repository behavior. It asks one bounded repository question:

> Does the exact repository Aperture candidate converge to one current v3.2 identity after boot and remain there after inherited writers have had a bounded opportunity to run?

## 1. Anti-equivalences

```text
SOURCE_DECLARATION != RUNTIME_IDENTITY
MACHINE_META_GREEN != HUMAN_FACING_IDENTITY_GREEN
HISTORICAL_V31_LINEAGE != CURRENT_V31_INSTRUMENT_IDENTITY
DOM_READY_GREEN != DELAYED_IDENTITY_CONVERGENCE
ATTACHMENT_DIVERGENCE != REPOSITORY_REGRESSION
STATIC_ABSENCE_CHECK != THREE_ENGINE_RUNTIME_WITNESS
RUNTIME_WITNESS_GREEN != RELEASE_AUTHORITY
```

## 2. Exact current-identity surfaces

The runtime witness must observe all of the following from the same exact served repository bytes:

```text
document.title
<title> text
<html data-aperture-version>
<body data-aperture-version>
meta[name="aperture-version"]
visible firmware/version readout (mFirmwareVer or firmwareSpineVersion where present)
window.APERTURE_VERSION
window.APERTURE_SCHEMA_VERSION
window.FIRMWARE.VERSION where present
window.FIRMWARE.SCHEMA_VERSION where present
```

Every current instrument identity surface that exists must resolve to v3.2-alpha / `td613-aperture/v3.2-alpha` as appropriate.

Historical module contracts may continue to contain v3.1, v3.0, v2.9.5, and v2.9.4 lineage identifiers where their own schema/version fields require them.

## 3. Temporal witness law

A single DOM-ready read is insufficient.

The observer must sample at least:

```text
T0 = after DOMContentLoaded / initial readiness
T1 = after 350 ms
T2 = after 1000 ms
T3 = after 2200 ms
```

The delayed window is chosen to exceed the inherited 300 ms class of identity writers and the historical 0/220/900/1800 ms stale-writer pattern already recognized by the Aperture sync lane.

At each sample:

```text
current identity surfaces agree with v3.2-alpha
no current title/readout/root/body surface has reverted to v3.1-alpha
no page error or console error occurred
```

## 4. Exact-byte and network posture

The witness must run through the existing local repository server route:

```text
/aperture/tool.html
```

under the existing exact-head three-engine Ready matrix:

```text
chromium
firefox
webkit
```

It must record:

```text
browser engine
source head when supplied by CI
route
samples T0-T3
console errors
page errors
HTTP errors
external requests
non-read requests
```

The witness performs no provider call, no production observation, and no mutation.

## 5. Static hostility

Static tests must hold if any of the following are introduced into current-identity writer code:

```text
TD613 Aperture v3.1-alpha as current document title
current <html> or <body> data-aperture-version = v3.1-alpha
current visible firmware readout = v3.1-alpha
window.APERTURE_VERSION = v3.1-alpha
window.APERTURE_SCHEMA_VERSION = td613-aperture/v3.1-alpha
window.FIRMWARE current version/schema = v3.1-alpha
```

Historical v3.1 contract payloads are not hostile merely because they preserve v3.1 lineage.

The static test must therefore target current-identity writer patterns rather than globally banning the string `v3.1-alpha`.

## 6. Observer receipt ceiling

Every machine receipt must preserve:

```text
schema = td613.aperture.v32-identity-singularity-browser-evidence/v0.1
status = PASS | FAIL
counts_as_human_evidence = false
production_observation = false
release_authority = false
merge_authority = false
vercel_authority = false
provider_call_performed = false
human_closure_required = true
exogenous_witness_credit = 0
golden_egg_credit = 0
```

## 7. Admission sequence

```text
Draft Static admission
→ same exact head Ready
→ Chromium + Firefox + WebKit delayed identity witness
→ exact-head browser convergence
→ 𝄐 only if every required surface remains singularly v3.2
```

A RED is preserved and diagnosed before repair. No same-run relabeling may convert a failed observation into authority.

## 8. Finite stop

If the exact repository candidate passes the three-engine delayed identity witness, this chamber closes only the repository-side **v3.2 current-identity singularity** question.

It does not establish that the quarantined external attachment is repaired, current, or release-authoritative.

It does not authorize merge, Vercel, production release, provider release, external observation, exogenous witness credit, or Golden Egg credit.

Human closure remains required.
