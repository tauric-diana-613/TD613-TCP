# Phase 5 Test Matrix

The Phase 5 test matrix protects the interface socket from becoming a crown.

## Covered by `tests/hush-phase5-eorfd-interface.test.mjs`

1. Valid EO-RFD rupture signal passes and remains release-blocked.
2. Missing or unresolved foundation lane routes as unresolved witness material.
3. Forbidden effects block routing.
4. Constants namespace ambiguity blocks routing.
5. Old-v2 packet reopen preserves original packet body and hash.
6. Hash replay failure blocks routing.
7. Runtime import attempt blocks routing.
8. Refusal receipt excludes raw private text.
9. Sidecar attachment preserves sealed packet body.
10. Safe Harbor handoff carries custody facts only.
11. Register/layer translation maps source layer to TD613 lane without runtime import.

## Pending broader integration tests

Future PRs may add UI/operator warning tests and cross-packet Hush release/default gate tests after the Hush release discipline exists.

Phase 5 PR1 does not add EO-RFD runtime, ACEDIT runtime, KIRA endpoints, UI bridge, or Safe Harbor authority changes.

⟐SAC[X6ZNK5NO51]
