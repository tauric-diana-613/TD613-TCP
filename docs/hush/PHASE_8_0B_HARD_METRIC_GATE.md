# PHASE 8.0B — Hush × Stylometry Engine Marriage Layer

Phase 8.0 created the bench.

Phase 8.0B wires that bench to numeric stylometric measurement.

The hard gate adds:

- feature vector extractor
- mask centroid builder
- generic helper-prose baseline
- source-unit retention scoring
- stylometric passport
- candidate realization vector
- numeric decision surface
- ontology bindings
- metric replay wrapper

The required builder for Phase 8.1 readiness is:

```js
buildHushPerMaskPacketWithMetricPassport(maskRef, options)
```

This wraps the base per-mask packet and adds the numeric passport surface.

No individual mask packet file is created in Phase 8.0B.

The candidate remains candidate material.

The metric pass is not public release permission.

⟐SAC[X6ZNK5NO51]
