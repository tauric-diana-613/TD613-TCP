# Phase 8 Keisha Calibration Hotfix

This patch tightens the Phase 8.K Keisha gate after automated review surfaced two issues.

## Fixes

- Maps `group-chat-soft` directly to the `small circle` passport role so the metric passport and centroid do not inherit the Phase 7 handoff's generic baseline posture.
- Removes the Keisha fixture test's dependency on a `family` field that the Phase 7 handoff does not carry.
- Keeps the test anchored on the packet's stylometric passport role and centroid role instead.
- Lowers Keisha's `generic_ai_baseline_distance_min` to `0.22` so the known-good low-drama custody fixture can pass while hard blockers still catch invented intimacy, fake support-group voice, and social belonging leakage.

## Doctrine

This hotfix does not loosen Keisha's danger gates. It only recalibrates the generic-distance floor for a mask whose human target is intentionally ordinary, relational, and low-drama.

Keisha must still block invented intimacy, social belonging leakage, unsupported care, generic supportive assistant voice, and under-preserved source obligations.
