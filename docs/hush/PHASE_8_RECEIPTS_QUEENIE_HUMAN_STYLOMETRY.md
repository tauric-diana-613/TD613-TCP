# PHASE 8.Q — Receipts Queenie Human-Stylometry Gate

Receipts Queenie is the warm-record gate for `grandma-receipts`.

Her purpose is bounded warmth with receipt custody: the candidate may enter softly, but the file, date, label, mismatch, and reason for keeping them together must remain visible.

## Core rule

Warmth may hold the receipt. Warmth may not become the receipt.

Queenie must not add family scenes, extra backstory, unsupported relationships, gossip, proof posture, or public release authority. The gate measures transformation behavior only.

## Native route

`grandma-receipts` routes to the `warm_receipts` role. The passport supplies a native centroid with mid-length sentence flow, bounded warmth, high custody pressure, low API sheen, and a warm-to-custody ratio.

## Cadence heatmap

Expected contour: `warm-envelope-hard-latch`.

- Warm entry: 0.00–0.35
- Receipt bloom: 0.35–0.78
- Boundary close: 0.62–1.00

The contour must differ from compression, small-circle, quick-handoff, fracture, checklist, and document-distance surfaces.

## Metric families

The Queenie gate adds metrics for:

- warm receipt custody
- receipt, file, date, label, reason, and mismatch visibility
- bounded warmth
- story containment
- cadence heatmap placement
- prior-mask leakage
- invented relationship, family-detail, domestic-prop, and extra-backstory risk
- sentimental overreach
- receipt scold risk

## Fixture matrix

The fixture bank lives at `app/data/hush-phase8-fixtures/receipts-queenie-fixtures.js`.

It covers pass, repair, and block cases for warm receipt custody, warmth without receipt, family-scene invention, memo polish, gossip contamination, small-circle leakage, fracture leakage, receipt scolding, bounded story warmth, and extra backstory.

Every fixture uses explicit source obligations, disables heuristic anchors, excludes raw candidate text, excludes raw sample text, and keeps public-default false.

## Test command

```bash
node tests/hush-phase8-receipts-queenie-fixtures.test.mjs
npm run test:hush:phase8:gate
```

Sealed ⟐
