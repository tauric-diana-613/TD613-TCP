# Pedagogue Carbon Paper — Admission Witness Replay Custody Hostile Assay v0.1

Status: **PREREGISTERED / PRE-EXECUTION / BOUNDED SYNTHETIC HOSTILE RESEARCH / OPERATOR-ADMITTED**

Schema: `td613.pedagogue.admission-witness-replay-custody-hostile-assay/v0.1`

Research surface: PR `#677`

## 0. Inherited result under attack

Blueprint Margins established only this bounded result:

```text
C6_PRECEDENCE_ADMISSION_GENEALOGY
PRECEDENCE_ADMISSION_GENEALOGY_CANDIDATE_SURVIVES_BOUNDED_BLUEPRINT_MARGINS
```

C6 can refuse a boolean witness badge whose payload does not match the claimed synthetic edge. But C6's witness check is payload congruence: a semantically well-formed copied payload is observationally identical to one actually earned through a predeclared admitted derivation.

The silent widening attacked here is:

```text
matching witness payload
→ sufficient witness provenance
```

or:

```text
same witness payload = same entitlement to witness
```

That equivalence is not granted.

## 1. Candidate C7 · Admission Witness Replay Custody

```text
C7_ADMISSION_WITNESS_REPLAY_CUSTODY
Display name: Admission Witness Replay Custody
assay name: Carbon Paper
status = ATTACK_ONLY_NOT_PROMOTED
presumption_of_survival = false
```

Candidate rule:

> A matching edge-admission witness payload is not itself provenance. A support may count as lawfully witnessed only when its witness entitlement can be replayed from admitted primitive support through at least one predeclared, admissible, replayable bounded witness-derivation rule whose replay witness matches the rule semantics. Preserve every semantically distinct lawful witness lineage; preserve rejected witness attempts separately. Rule identifiers and record ordering may label custody but may not create or choose witness authority. Withdrawal of a prerequisite invalidates every dependent current witness lineage unless an independent lawful lineage remains.

This is synthetic custody logic only. It does not establish truth, human credibility, cryptographic authenticity, causal order, or security.

## 2. Frozen scope

Fixed across the hostile family:

```text
event set
semantic event identities
baseline warrant specimen
requested warrant
contradiction families
candidate precedence edges
support semantic kinds
```

Varied only:

```text
primitive witness-support tokens
witness-derivation rules
rule replay receipts
active/inactive primitive scope
record identifiers and serialization
```

Event-set revision remains closed.

### 2.1 Witness entitlement token

For a support record claiming semantic edge `u -> v` under support kind `k`, define one synthetic entitlement token:

```text
EDGE_WITNESS:u>v:k
```

The visible witness payload may repeat `u`, `v`, and `k`, but payload equality does not generate this entitlement token.

### 2.2 Lawful replay lineage

A witness lineage is lawful only if:

```text
all required primitive support tokens are currently admitted
rule.predeclared = true
rule.admissible = true
rule.replayable = true
rule produces exactly the claimed EDGE_WITNESS token
rule replay witness matches the frozen semantic rule signature
```

A boolean `replayable=true`, a matching payload, or a convenient identifier is insufficient by itself.

## 3. Strong falsifier · Carbon Copy

Construct two edge-admission supports with identical:

```text
edge = PINK -> BLUE
semantic_support_kind = DECLARATION_ALPHA
matching witness payload
```

### Genuine sheet

The entitlement token is produced by a predeclared admissible replayable witness rule whose prerequisites are present and whose replay receipt matches the rule semantics.

### Carbon copy

The visible payload is byte-for-byte semantically equivalent, but no lawful admitted derivation produces its witness entitlement.

Under inherited C6 payload congruence, both appear witnessed.

Required C7 result:

```text
C6 sees matching witness payload in both = true
C7 genuine lineage lawful = true
C7 copied lineage lawful = false
copied status = REFUSE_UNREPLAYED_EDGE_ADMISSION_WITNESS
```

### C6 bounded defeat condition

If this room succeeds, C6 is falsified only as a **witness-provenance-sufficient** form:

```text
PRECEDENCE_ADMISSION_GENEALOGY_C6_FALSIFIED_AS_WITNESS_PROVENANCE_SUFFICIENT_FORM
```

C6's edge-admission genealogy jurisdiction remains preserved.

## 4. Hostile rooms

### CP01 · Carbon Copy
Central falsifier above.

### CP02 · Missing Source
A valid predeclared rule and matching replay receipt exist, but one required primitive support token is absent.

Required:

```text
REFUSE_UNREPLAYED_EDGE_ADMISSION_WITNESS
```

### CP03 · Replay Costume
All rule booleans are true but the replay receipt does not match the frozen rule semantics.

Required:

```text
REFUSE_WITNESS_REPLAY_MISMATCH
```

### CP04 · Two Lawful Hands
Two semantically distinct lawful witness derivations independently earn the same edge/support entitlement.

Required:

```text
edge admitted = true
lawful witness lineage count = 2
both lineages preserved
```

### CP05 · One Hand Withdrawn
Withdraw the prerequisite of one lawful witness lineage while an independent lineage remains.

Required:

```text
edge persists = true
witness genealogy changes = true
```

### CP06 · All Hands Withdrawn
Withdraw every currently admitted prerequisite supporting the witness entitlement.

Required:

```text
edge no longer admitted = true
no ghost witness entitlement survives
```

### CP07 · Rule Renaming
Change only `rule_id` while preserving rule semantics, prerequisites, output, and replay receipt semantics.

Required:

```text
semantic witness lineage invariant = true
```

### CP08 · Rule-ID Reuse With Semantic Change
Reuse one `rule_id` while changing the semantic prerequisite set.

Required:

```text
semantic witness lineage changed = true
rule identifier authority = false
```

### CP09 · Bad Carbon Sorts First
An invalid lexical-first witness attempt precedes a valid later witness lineage.

Required:

```text
invalid attempt preserved as rejected
valid lineage preserved as lawful
edge admitted through valid lineage
```

### CP10 · Crossed Witnesses
Lawful replay lineages support opposing edges. Preserve both witness genealogies while inherited Warrant Weave rejects the cyclic relation.

Required:

```text
REJECT_CYCLIC_OR_INCONSISTENT_PRECEDENCE
lawful witness receipts preserved = true
```

### CP11 · Margin Copy Without Backing Sheet
Compare a full replay-custody object with an object retaining only the matching visible witness payload and current admitted edge.

Required:

```text
visible payload equal = true
current relation equal = true
witness provenance equivalent = false
payload-only object has no provenance authority
```

### CP12 · Post-Hoc Re-ink
Attempt to mutate a sealed witness replay receipt or sealed witness rule in place.

Required refusal:

```text
SEALED_ADMISSION_WITNESS_PROVENANCE_IMMUTABLE
```

## 5. C7 strong survival requirement

C7 survives only if it simultaneously:

1. rejects a copied matching payload that lacks replayed provenance;
2. requires all declared prerequisites to be admitted;
3. rejects a replay receipt whose semantics do not match the rule;
4. preserves multiple independent lawful witness lineages;
5. keeps the edge when one lineage dies but another remains;
6. removes the edge when all lawful current witness lineages die;
7. is invariant to rule-ID renaming and record ordering;
8. refuses rule-ID reuse as semantic continuity;
9. prevents invalid lexical-first attempts from suppressing valid later lineages;
10. preserves lawful witness custody even when relation construction is later rejected;
11. gives payload-only compaction no authority over replay provenance;
12. refuses in-place mutation of sealed witness provenance;
13. uses no scalar credibility, confidence, trust, certainty, robustness, or authority score.

Any failure falsifies C7 in this bounded form.

## 6. Preregistered verdict strings

```text
PRECEDENCE_ADMISSION_GENEALOGY_C6_FALSIFIED_AS_WITNESS_PROVENANCE_SUFFICIENT_FORM
ADMISSION_WITNESS_REPLAY_CUSTODY_CANDIDATE_SURVIVES_BOUNDED_CARBON_PAPER
ADMISSION_WITNESS_REPLAY_CUSTODY_CANDIDATE_FALSIFIED_IN_BOUNDED_CARBON_PAPER
```

## 7. Claim ceiling

Even survival does not establish:

```text
truth of a precedence edge
human witness credibility
cryptographic authenticity
security proof
causal order
real-world concurrency semantics
Lamport/vector clocks
consensus
serializability theorem
provenance algebra
proof theory
category/lattice/sheaf structure
physical time law
connection/curvature/holonomy
quantum identity
autonomous scientific authority
```

## 8. Holds

```text
C7 promotion = false
H2 = HELD_NOT_TESTED_HERE
H3 = HELD_NOT_TESTED_HERE
M×D = HELD
M×P = HELD
D×P = HELD
M×D×P = HELD
APERTURE_V32_REPLAY_STABILITY = HELD_NOT_YET_WITNESSED
```

## 9. Authority membrane

```text
research_target_admitted_by_operator = true
major_research_decisions_self_authorized_in_pr677_lane = true
product_mutation = false
shared_pedagogue_engine_mutation = false
workflow_mutation = false
browser_execution = false
merge_authority_for_this_assay = false
deployment_authority = false
release_authority = false
vercel_release_requires_issue_405_and_new_explicit_operator_gesture = true
promotion_authority = false
```

CI green is infrastructure evidence, not theory survival. The hostile test must accept either preregistered scientific verdict as a valid outcome and fail only on broken machinery, changed frozen assumptions, or constitutional regression.

𝌋 A photocopy can match the line perfectly. Carbon Paper asks for the pressure mark on the sheet beneath it. ⟐
