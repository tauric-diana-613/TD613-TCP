# Pedagogue Cut-and-Paste — Event Membership Revision Custody Hostile Execution Receipt v0.1

Status: **WITNESSED / BOUNDED SYNTHETIC HOSTILE RESEARCH / OPERATOR-ADMITTED / HUMAN-GATED FOR PRODUCTION**

Schema: `td613.pedagogue.event-membership-revision-custody-hostile-execution-receipt/v0.1`

Research surface: PR `#677`

## 0. Exact scientific lineage

```text
prior Carbon Paper receipt head:
28e51233d4b014ed76b02328f6b869559baa031d

preregistration:
3b7a311f2715d4ddc857f52523cb2f292f931c39
  docs(pedagogue): preregister Cut-and-Paste event-membership assay

evaluator:
bc18312b0137b143fbe6fd07369b9798f7a1edf5
  feat(pedagogue): add Cut-and-Paste event-membership evaluator

original hostile-test head:
c3802caae2fb180a389c3568e232ef321aba96cf
  test(pedagogue): execute Cut-and-Paste event-membership family

original workflow:
TD613 Consolidated Validation
run 1894 / 32514998141
result = FAILURE
failure class = TEST_ONLY_DOCUMENTATION_WORDING_MISMATCH
scientific falsification = false

bounded test-only repair:
796f370fbaea82b3388ae3b71f72c8615cb90d15
  test(pedagogue): bind Cut-and-Paste docs assertions to frozen wording

successful exact-head witness:
TD613 Consolidated Validation
run 1895 / 32515583712
result = SUCCESS
```

The preregistration and evaluator were not changed after execution began. Run 1894 failed because two documentation assertions demanded slogans that were semantically present but not literally written in the frozen preregistration. The repair changed only those test regexes. No hypothesis, hostile room, evaluator rule, fixture, authority membrane, or scientific disposition changed.

Run 1895 remained in the cheap lane. The static/constitutional/A15-R0 research job passed; Giving/practice browsers, front-line browsers, explicit full-repository validation, and self-hosted calibration were skipped.

CI green is infrastructure evidence. The scientific verdict below is the literal Cut-and-Paste output emitted by the frozen evaluator/test family during successful run 1895.

---

## 1. Primary result

Carbon Paper's C7 result remains valid inside its fixed-event-set jurisdiction:

```text
matching witness payload != replayed witness provenance
```

But witness replay validity is not sufficient custody for current authority once the semantic event universe changes.

Exact bounded inherited verdict:

```text
ADMISSION_WITNESS_REPLAY_CUSTODY_C7_FALSIFIED_AS_EVENT_MEMBERSHIP_REVISION_SUFFICIENT_FORM
```

This does **not** revoke C7's witness-replay jurisdiction.

Candidate under attack:

```text
C8_EVENT_MEMBERSHIP_REVISION_CUSTODY
status before execution = ATTACK_ONLY_NOT_PROMOTED
presumption_of_survival = false
```

Exact bounded candidate verdict:

```text
EVENT_MEMBERSHIP_REVISION_CUSTODY_CANDIDATE_SURVIVES_BOUNDED_CUT_AND_PASTE
```

with:

```text
c8_defeat_conditions = []
promotion_authority = false
```

C8 is a surviving bounded synthetic research candidate, not a Pedagogue law.

---

## 2. Literal successful witness

Run 1895 emitted:

```text
schema = td613.pedagogue.event-membership-revision-custody-hostile/v0.1
inherited_c7_event_membership_revision_verdict = ADMISSION_WITNESS_REPLAY_CUSTODY_C7_FALSIFIED_AS_EVENT_MEMBERSHIP_REVISION_SUFFICIENT_FORM
c8_verdict = EVENT_MEMBERSHIP_REVISION_CUSTODY_CANDIDATE_SURVIVES_BOUNDED_CUT_AND_PASTE
c8_defeat_conditions = []
CPX01_c7_replay_valid = true
CPX01_current_edge_after_withdrawal = 0
CPX03_continuous_episode_count = 1
CPX04_reintroduced_episode_count = 2
CPX05_current_set_equal = true
CPX05_history_equal = false
CPX09_status = ABSTAIN_CONFLICTING_EVENT_MEMBERSHIP
CPX11_mutation_status = SEALED_EVENT_MEMBERSHIP_LEDGER_IMMUTABLE
deployment_performed = false
release_authority = false
```

Therefore the strong falsifier succeeded: the inherited C7 witness remained internally replay-valid while withdrawal of an endpoint made the current edge inadmissible.

```text
historical replay validity != current event-membership admissibility
```

---

## 3. What Cut-and-Paste established inside its bounded family

The hostile rooms support the following distinctions:

```text
replayable old-universe witness != currently admissible witness
event-ID continuity != semantic event continuity
event-ID rename != semantic event replacement
event deletion != historical witness erasure
event reintroduction != uninterrupted event continuity
same current event set != same event-membership history
split/merge declaration != inherited witness authority
serialization order != event authority
conflicting current raw binding != unique semantic membership
current-set custody != membership-revision custody
sealed event-membership ledger != mutable notebook
```

CPX01 preserved historical witness custody while current edge authority fell.

CPX03 preserved a continuous semantic event through a raw-ID rename with one membership episode.

CPX04 withdrawal plus later reintroduction produced two membership episodes rather than retroactively smoothing the gap away.

CPX05 produced equal current event sets with unequal membership histories.

CPX09 refused incompatible active semantic assignments to one raw event ID:

```text
ABSTAIN_CONFLICTING_EVENT_MEMBERSHIP
```

CPX11 refused post-hoc mutation:

```text
SEALED_EVENT_MEMBERSHIP_LEDGER_IMMUTABLE
```

No scalar confidence, authority, trust, authenticity, or reliability score was introduced.

---

## 4. Important residual insufficiency discovered after the witness

Cut-and-Paste did **not** test every ambiguity permitted by its evaluator.

The frozen evaluator detects conflict when multiple **active** records assign different semantic event keys to the same `(raw_event_id, epoch)`. However, current membership for one semantic event is computed separately by selecting a `latestBySemantic` record. If two valid records share the same semantic event key and the same epoch, the evaluator currently breaks that tie by lexical `membership_id` order:

```text
record.epoch === prior.epoch
&& record.membership_id.localeCompare(prior.membership_id) > 0
```

That implementation path is not covered by CPX09.

Therefore C8's bounded survival does not establish unique current membership under same-semantic, same-epoch contradictory dispositions.

The residual question is:

```text
same semantic event + same epoch + contradictory membership dispositions
!=
uniquely resolved current membership

membership identifier ordering != revision authority
```

This is an earned hostile bearing, not a retroactive defect inserted into the C8 verdict. C8 survives the preregistered Cut-and-Paste family exactly as witnessed; the newly exposed boundary is the next candidate insufficiency.

---

## 5. Next earned hostile bearing

The next chamber should hold C8's event-universe and witness-replay semantics fixed and attack **same-epoch membership arbitration**.

At minimum it should preregister hostile cases for:

```text
same semantic key + same epoch: ADMIT versus WITHDRAW
same semantic key + same epoch: active versus inactive with renamed membership IDs
input-order reversal
membership-ID lexical reversal
semantically duplicate same-epoch records
same-epoch compatible rename versus contradictory disposition
raw-ID conflict already handled by C8 as inherited positive control
no declared precedence between competing same-epoch revisions
```

The expected bounded refusal for an unresolved contradiction should be an explicit ambiguity posture, not a lexical winner. Naming of that posture belongs to the next preregistration before implementation.

No distributed-consensus, causal-clock, serializability, temporal-database, event-sourcing-completeness, or provenance-algebra claim is earned by this bearing.

---

## 6. Westward custody spine

```text
warrant reachability
→ warrant genealogy
→ support episodes
→ partial-order history identifiability
→ precedence-revision custody
→ edge-admission genealogy
→ admission-witness replay custody
→ event-membership revision custody
→ same-epoch membership arbitration [NEXT ATTACK]
```

Each arrow records a narrower insufficiency discovered by hostile assay. It is not a promotion ladder.

---

## 7. Holds and claim ceiling

```text
C8 promotion = false
H2 = HELD_NOT_TESTED_HERE
H3 = HELD_NOT_TESTED_HERE
M×D = HELD
M×P = HELD
D×P = HELD
M×D×P = HELD
APERTURE_V32_REPLAY_STABILITY = HELD_NOT_YET_WITNESSED
```

Not established:

```text
real-world event identity
human testimony reliability
causal order
distributed consensus
distributed-log correctness
serializability theorem
event-sourcing completeness
temporal-database completeness
truth-maintenance completeness
provenance algebra
proof theory
category / lattice / sheaf structure
physical time law
connection / curvature / holonomy
quantum identity
autonomous scientific authority
```

---

## 8. Authority membrane

```text
research_target_admitted_by_operator = true
major_research_decisions_self_authorized_in_pr677_lane = true
product_mutation = false
shared_pedagogue_engine_mutation = false
workflow_mutation = false
browser_execution = false
merge_performed = false
deployment_performed = false
deployment_authority = false
release_authority = false
vercel_release_requires_issue_405_and_new_explicit_operator_gesture = true
promotion_authority = false
```

The renewed operator grant authorizes continued bounded research after this receipt. This receipt is a checkpoint, not a stop condition.

𝌋 Cut-and-Paste learned that the sheet can change beneath a perfectly witnessed hand. The next clerk has two stamps for the same line at the same moment. Alphabetical order does not get to become law. ⟐
