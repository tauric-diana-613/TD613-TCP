# Pedagogue Open Window — Exogenous Anchor Admission Custody Hostile Execution Receipt v0.1

Status: **WITNESSED / BOUNDED SYNTHETIC EXOGENOUS-OBSERVABILITY RESEARCH / HUMAN-OPENED / ATTACK-ONLY / NOT PROMOTED**

Schema: `td613.pedagogue.exogenous-anchor-admission-custody-hostile-execution-receipt/v0.1`

Research surface: PR `#677`

Candidate: `E1_EXOGENOUS_ANCHOR_ADMISSION_CUSTODY`

Assay chamber: **Open Window**

## 0. Exact scientific lineage

```text
terminal C14 execution receipt:
adec571d0e3f6b366824ab2d10941d22e5f34942
  docs(pedagogue): seal No Window provenance ceiling receipt

E1 preregistration:
f241a9bf83a21eb06a1894cca02ddde0e86b7586
  docs(pedagogue): preregister Open Window exogenous anchor assay

E1 evaluator:
1f4bafe7d35d38f724af3ae26538acf9785e3d76
  feat(pedagogue): add Open Window exogenous anchor evaluator

E1 hostile-test / science head:
7ef7498bb3bef3f74b47422e5009928b75d8c7ee
  test(pedagogue): execute Open Window exogenous anchor family
```

C14 closed the prior internal-custody sequence. The operator then supplied a fresh human reopening gesture. E1 therefore begins a deliberately new evidence jurisdiction rather than pretending that the closed C-sequence simply continued as `C15`.

```text
C14 = terminal internal-custody sequence
E1 = first exogenous-anchor admission sequence
automatic_C15 = false
```

The PR branch was verified at the E1 science head before this receipt was written.

Exact-head workflow witness:

```text
workflow = TD613 Consolidated Validation
run = 1909 / 32529522692
science head = 7ef7498bb3bef3f74b47422e5009928b75d8c7ee
classifier = SUCCESS
Static, constitutional, and release contracts = SUCCESS
step 18 = Validate Ash A15 empirical profile journeys and A15-R0 research field = SUCCESS
Giving / practice exact-head browsers = SKIPPED
front-line exact-head browser shard = SKIPPED
full-product exact-head browser witness = SKIPPED
explicit full-repository validation = SKIPPED
explicit self-hosted calibration = SKIPPED
```

The Actions connector returned the literal E1 console block. CI green is infrastructure evidence only; the scientific disposition is the emitted hostile-assay result below.

---

## 1. Boundary inherited from C14

C14 witnessed:

```text
INTERNAL_PROVENANCE_NON_BOOTSTRAP_CLAIM_CEILING_SURVIVES_BOUNDED_NO_WINDOW
```

Its jurisdiction remains intact:

```text
internal ordering != exogenous provenance
internal receipt depth != external provenance depth
self-integrity != exteriority
self-attestation != external observation
```

C14 did not prove that external provenance is impossible. It proved only that a closed evaluator cannot manufacture observation of an exterior source from more internal bookkeeping.

E1 changed the admitted evidence surface by introducing a **human-authorized synthetic exogenous primitive**. It did not connect a live source adapter and did not claim real-world source authenticity.

---

## 2. Candidate under attack

```text
E1_EXOGENOUS_ANCHOR_ADMISSION_CUSTODY
status before execution = ATTACK_ONLY_NOT_PROMOTED
presumption_of_survival = false
```

The candidate law was intentionally two-sided.

### Under-use falsifier

A lawful, current, target-bound exogenous observation of one declared semantic field must not be refused merely because the prior C14 surface had no window.

### Overclaim falsifier

A narrow exogenous observation must not widen into:

```text
global source credibility
source honesty
causal independence
wrong-target authority
stale authority
duplicate-derived confidence
neighbor-field authority
self-attested exteriority
```

---

## 3. Literal exact-head verdict

Run `1909 / 32529522692`, static job `96918626853`, emitted:

```json
{
  "ok": true,
  "schema": "td613.pedagogue.exogenous-anchor-admission-custody-hostile/v0.1",
  "inherited_c14_verdict": "INTERNAL_PROVENANCE_NON_BOOTSTRAP_CLAIM_CEILING_SURVIVES_BOUNDED_NO_WINDOW",
  "e1_verdict": "EXOGENOUS_ANCHOR_ADMISSION_CUSTODY_CANDIDATE_SURVIVES_BOUNDED_OPEN_WINDOW",
  "e1_defeat_conditions": [],
  "OW01_status": "ADMIT_SCOPED_EXOGENOUS_OBSERVATION",
  "OW01_value": "PRE_ENTRY",
  "OW01_global_provenance": false,
  "OW02_status": "REFUSE_INTERNAL_SELF_ATTESTED_EXTERNAL_PROVENANCE",
  "OW05_status": "ABSTAIN_CONFLICTING_EXOGENOUS_ANCHORS",
  "OW08_status": "ABSTAIN_EXOGENOUS_ANCHOR_SCOPE_MISMATCH",
  "OW10_status": "UNIDENTIFIED_NO_EXOGENOUS_ANCHOR",
  "OW11_global_status": "ABSTAIN_EXOGENOUS_ANCHOR_SCOPE_MISMATCH",
  "synthetic_exogenous_fixture": true,
  "live_external_source_adapter": false,
  "deployment_performed": false,
  "release_authority": false
}
```

Therefore:

```text
E1 verdict = EXOGENOUS_ANCHOR_ADMISSION_CUSTODY_CANDIDATE_SURVIVES_BOUNDED_OPEN_WINDOW
E1 defeat conditions = []
```

---

## 4. Hostile-room interpretation

### OW01 — Open Window, Narrow View

Observed:

```text
status = ADMIT_SCOPED_EXOGENOUS_OBSERVATION
observed_value = PRE_ENTRY
global_external_provenance_identified = false
```

This defeats the over-skeptical posture. Once the evidence jurisdiction explicitly admits a lawful synthetic exogenous primitive, the protocol can use the exact field it observes.

### OW02 — Passport Drawn Inside

Observed:

```text
REFUSE_INTERNAL_SELF_ATTESTED_EXTERNAL_PROVENANCE
```

An internal assertion does not become exogenous merely by labeling itself external.

### OW03 — Wrong House

Target mismatch remains refusal. A lawful observation about one semantic target does not leak authority into another target.

### OW04 — Yesterday's Newspaper

Expired observation does not retain current authority. Historical custody and current authority remain distinct.

### OW05 — Two Windows, Two Streets

Observed:

```text
ABSTAIN_CONFLICTING_EXOGENOUS_ANCHORS
```

Conflicting current external observations are preserved as conflict rather than forced to closure by identifier, order, or count.

### OW06 — Brass Nameplate Shuffle

Renaming anchor identifiers and reversing serialization preserved semantic authority. Anchor ID and serialization order did not become provenance authority.

### OW07 — Duplicate Telescope

Duplicating a semantically identical anchor did not widen scope or manufacture scalar confidence.

```text
duplication != confidence
record count != authority
```

### OW08 — Telescope Sees One Star

Observed:

```text
ABSTAIN_EXOGENOUS_ANCHOR_SCOPE_MISMATCH
```

An anchor observing source existence did not silently authorize a neighboring question about source honesty.

### OW09 — Window Closed

An inactive historical anchor could remain in custody without retaining current authority.

### OW10 — Empty Frame

Observed:

```text
UNIDENTIFIED_NO_EXOGENOUS_ANCHOR
```

Removing the exogenous primitive restores the C14 claim ceiling. E1 does not create external observation from absence.

### OW11 — Two Fields Are Still Not the Whole World

Two explicit observed fields remained two explicit observed fields. They did not become `GLOBAL_SOURCE_CREDIBILITY`.

Observed global query:

```text
ABSTAIN_EXOGENOUS_ANCHOR_SCOPE_MISMATCH
```

### OW12 — Fake Panorama

A broad textual scope label did not widen the actual semantic observation set.

```text
label != observation
scope name != scope authority
```

---

## 5. Exact claim ceiling

The strongest warranted statement is:

> In this bounded synthetic family, a human-admitted exogenous observation can earn current field-specific, target-specific observation authority when its semantic target and observed field are explicitly bound and non-conflicted. That authority does not automatically establish source authenticity, honesty, independence, global provenance, neighboring fields, or confidence, and it is withheld when the anchor is stale, inactive, mismatched, absent, internally self-attested, or in current conflict.

This is an **admission-custody result**, not a trust theorem.

E1 does **not** establish:

```text
real-world source authenticity
external source identity
physical exteriority
institutional independence
source honesty
honest acquisition
causal independence
unbiased sampling
cryptographic authenticity
external chronological truth beyond the observed field
live adapter security
network provenance
external attestation infrastructure
distributed consensus
universal provenance algebra
scalar trust score
confidence aggregation
H2
H3
intersection behavior
Aperture installed replay stability
production authority
```

---

## 6. Research consequence

E1 establishes a useful jurisdictional distinction that did not exist in the closed C-sequence:

```text
no exogenous anchor
→ external observation remains unidentified

lawful scoped exogenous anchor
→ exact observed field may become identified for exact target

lawful scoped exogenous anchor
↛ global provenance
```

The architecture therefore now has a witnessed narrow window without pretending that a window is a passport office, a lie detector, or the whole weather service.

The immediate frontier is no longer “can an external observation be used at all?” That question survived.

A scientifically earned next attack should target the **custody of authority across exogenous-anchor revision**, not leap into H2/H3/intersections or live-network trust. Candidate hostile directions include, without automatic authorization to promote any of them:

```text
anchor replacement vs semantic continuity
freshness-window overlap vs supersession
same semantic observation from distinct source lineages
revocation after prior downstream use
historical exogenous observation vs current derived warrant
target re-binding / source-record replay
```

Any E2 chamber must be preregistered before evaluator mutation and must preserve E1's bounded field/target claim ceiling.

---

## 7. Authority membrane

```text
candidate_status = ATTACK_ONLY_NOT_PROMOTED
promotion_authority = false
synthetic_exogenous_fixture = true
live_external_source_adapter = false
real_world_external_provenance_claim = false
product_mutation = false
shared_pedagogue_engine_mutation = false
workflow_mutation = false
browser_execution = false
merge_performed = false
deployment_performed = false
release_authority = false
vercel_release_requires_issue_405_and_new_explicit_operator_gesture = true

H2 = HELD_NOT_TESTED_HERE
H3 = HELD_NOT_TESTED_HERE
intersections = HELD_NOT_OPENED_HERE
APERTURE_V32_REPLAY_STABILITY = HELD_NOT_YET_WITNESSED
```

No merge. No deployment. No release. No Vercel. No scalar crown.

𝌋 The window opened one pane wide. The protocol learned to use the view without pretending it owned the horizon. ⟐
