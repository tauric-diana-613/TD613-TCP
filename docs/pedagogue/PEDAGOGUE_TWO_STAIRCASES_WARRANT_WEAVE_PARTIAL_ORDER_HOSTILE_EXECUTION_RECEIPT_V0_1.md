# Pedagogue Two Staircases — Warrant Weave Partial-Order Hostile Execution Receipt v0.1

Status: **WITNESSED / BOUNDED SYNTHETIC HOSTILE RESEARCH / OPERATOR-ADMITTED / HUMAN-GATED FOR PRODUCTION**

Schema: `td613.pedagogue.warrant-weave-partial-order-hostile-execution-receipt/v0.1`

Research surface: PR `#677`

## 0. Directional-custody audit

This receipt is sealed after an explicit operator-requested trajectory audit following an accidental session stop.

The scientific bearing immediately before this chamber is preserved as:

```text
declared derivational reachability
→ warrant provenance custody
→ temporal episode custody
→ partial-order history identifiability
```

The chamber is therefore not a change of research direction. It attacks the exact insufficiency left by the prior After Midnight receipt:

```text
ATTACK_EPISODE_LEDGER_PARTIAL_ORDER_CONCURRENT_SUPPORT_AND_CUSTODY_COMPACTION_BEFORE_ANY_SHARED_FORMALISM_PROMOTION
```

The inherited scopes remain narrower rather than erased:

```text
C1 declared derivational closure
  retains bounded reachability jurisdiction
  but was falsified as provenance-sufficient custody

C2 Warrant Genealogy
  retains bounded snapshot-provenance jurisdiction
  but was falsified as complete temporal custody

C3 Warrant Episode Ledger
  retains bounded declared-serial-history jurisdiction
  but is attacked here as partial-order-custody sufficient
```

No H2/H3 lane, M/D/P intersection program, installed Aperture promotion, product mutation, distributed-systems theory, causal-order claim, or shared formalism program was opened to get here.

---

## 1. Exact scientific lineage

Prior sealed temporal-custody receipt:

```text
2c083cb5a08ef9bfda179a072201491df51dfa8e
  docs(pedagogue): seal After Midnight temporal custody receipt
```

Preregistration:

```text
2e96eade20c47271fb8d9f8bd0a0b1aa3ed90745
  docs(pedagogue): preregister Warrant Weave partial-order hostile assay
```

Executor:

```text
98a3d4207a81814f7305b4eaa31347c11be5c799
  feat(pedagogue): add Warrant Weave partial-order evaluator
```

First hostile-test head:

```text
b8b81d88a93d22caa1eb3d6741454cb18222bb2b
  test(pedagogue): execute Warrant Weave Two Staircases hostile family
```

First workflow witness:

```text
TD613 Consolidated Validation
run 1884 / 32510513435
result = FAILURE
classification = TEST_HARNESS_WORDING_BRITTLENESS_ONLY
```

Narrow harness repair:

```text
63bdf95f8bcad9171252c96315f5d3877a92e226
  test(pedagogue): align Warrant Weave spec assertion with preregistered wording
```

Repaired exact-head workflow witness:

```text
TD613 Consolidated Validation
run 1885 / 32510719498
result = SUCCESS
```

The repaired witness kept the expensive and unrelated families outside scope:

```text
Giving/practice browser witness = SKIPPED
front-line browser shards = SKIPPED
full-product browser witness = SKIPPED
explicit full-repository validation = SKIPPED
explicit self-hosted calibration = SKIPPED
```

No browser execution was purchased for this chamber.

---

## 2. Why run 1884 is not a scientific reversal

The first run reached the A15-R0 research field and emitted the chamber's deterministic scientific receipt before failing on a documentation assertion.

The failing assertion demanded the literal phrase:

```text
deterministic display order != historical authority
```

The frozen preregistration instead already encoded the same refusal in its actual wording:

```text
rather than selecting a serialization by event identifier,
input order, lexical order, or hidden implementation order
```

The repair changed only the test assertion so that it matched the text that had in fact been preregistered. It did not modify:

```text
candidate definition
strong falsifier
precedence semantics
serialization enumeration
room fixtures
verdict strings
claim ceiling
authority membrane
```

Therefore:

```text
run_1884_red_badge != theory_falsification
run_1884_failure_class = HARNESS_LANGUAGE_MISMATCH
```

The repaired exact-head run 1885 passed without widening the theory.

---

## 3. Inherited C3 result under attack

C3 established a bounded ledger for a **declared total episode order**. It could preserve each sealed episode, distinguish current state from historical state, and refuse retroactive continuity.

That does not establish that a total order is identified when the admitted evidence declares only a partial precedence relation.

The silent widening attacked here is:

```text
declared event set + partial precedence
→ one invented total history
```

Exact bounded verdict:

```text
WARRANT_EPISODE_LEDGER_C3_FALSIFIED_AS_PARTIAL_ORDER_CUSTODY_SUFFICIENT_FORM
```

This does not revoke C3's serial-history jurisdiction.

It narrows it:

```text
serial-history custody != partial-order history identifiability
```

---

## 4. Candidate C4 · Warrant Weave

Preregistered candidate:

```text
C4_WARRANT_WEAVE
Display name: Warrant Weave
status = ATTACK_ONLY_NOT_PROMOTED
presumption_of_survival = false
```

Bounded rule:

> Preserve the declared event set and declared precedence relation. Enumerate every bounded serialization consistent with that relation. Replay each serialization through the same frozen warrant machinery. A final-state or transient-history proposition is identified only when all admitted serializations agree. When admitted serializations disagree, return an explicit ambiguity/abstention rather than selecting a serialization by event identifier, input order, lexical order, or hidden implementation order.

Exact bounded verdict from run 1885:

```text
WARRANT_WEAVE_CANDIDATE_SURVIVES_BOUNDED_TWO_STAIRCASES
```

Defeat conditions triggered:

```text
[]
```

This is candidate survival in one finite synthetic family only. It is not promotion.

---

## 5. TS01 · Two Staircases strong falsifier

Frozen baseline:

```text
A + B -> W
W initially supported

C + D -> W
second lawful lineage initially unavailable
```

Two events with no precedence edge:

```text
PINK = withdraw A
BLUE = add C and D
```

Both admitted serializations were retained:

```text
BLUE → PINK
  continuously_supported = true

PINK → BLUE
  continuously_supported = false
```

Both end with the same requested-warrant presence and same final current support posture.

Observed result:

```text
admissible_serialization_count = 2
final_presence = IDENTIFIED_PRESENT
transient_support_continuity = ABSTAIN_TRANSIENT_HISTORY_NOT_IDENTIFIED_BY_PARTIAL_ORDER
selected_serialization = null
lexical_tiebreak_used = false
```

The chamber therefore distinguishes:

```text
final-state identifiability != transient-history identifiability
```

and refuses to manufacture a historical path merely because deterministic display order is available.

---

## 6. Hostile-room receipts

### TS02 · identifier-renaming invariance

Observed:

```text
semantic_summary_invariant = true
```

Renaming event identifiers did not alter the semantic disposition.

Earned refusal:

```text
event identifier != precedence authority
```

### TS03 · explicit withdraw-before-add precedence

Declared:

```text
PINK -> BLUE
```

Observed:

```text
admissible_serialization_count = 1
transient_support_continuity = IDENTIFIED_SUPPORT_INTERRUPTION
```

### TS04 · explicit add-before-withdraw precedence

Declared:

```text
BLUE -> PINK
```

Observed:

```text
admissible_serialization_count = 1
transient_support_continuity = IDENTIFIED_CONTINUOUS_SUPPORT
```

Thus an explicit precedence edge can resolve an otherwise unidentified transient proposition, and opposite valid precedence can resolve it differently.

### TS05 · precedence cycle

Observed refusal:

```text
REJECT_CYCLIC_OR_INCONSISTENT_PRECEDENCE
```

No edge was silently discarded and no total order was invented.

### TS06 · unrelated concurrent event

An unordered event outside the requested warrant's support ancestry did not manufacture requested-warrant ambiguity where none was semantically present.

### TS07 · contradiction-history ambiguity

Observed:

```text
ABSTAIN_CONTRADICTION_HISTORY_NOT_IDENTIFIED_BY_PARTIAL_ORDER
```

The chamber preserves the difference between:

```text
current conflict posture
!=
whether contradiction was ever entered
```

when admitted serializations disagree on the latter.

### TS08 · replay-support handoff

The candidate retained the ambiguity created by one unordered event withdrawing the currently witnessed lineage while another adds an independent witnessed replacement lineage. Same final support does not counterfeit uninterrupted witnessed support.

### TS09 · semantic no-op

A bounded `NOOP` may create an event boundary but did not alter requested-warrant semantic conclusions.

### TS10 · final-state compaction attack

Observed:

```text
final_state_equal = true
```

but final-state-only compaction was not allowed to inherit transient-history authority.

Earned distinction:

```text
final-state equivalence != transient-history equivalence
```

---

## 7. What the trajectory audit establishes

The operator-requested audit found no research U-turn in the pre-stop chain.

The narrowing sequence is reconstructable:

```text
C1 closure membership
  asks: can W be lawfully reached?

C2 Warrant Genealogy
  asks: through which witnessed semantic lineage(s) is W currently supported?

C3 Warrant Episode Ledger
  asks: how did that support posture change across declared serial episodes?

C4 Warrant Weave
  asks: which historical propositions remain identified when the admitted event relation does not identify one total order?
```

Each chamber attacks a sufficiency claim created by the previous chamber. None substitutes an unrelated research program.

The abstraction increased, but it did so under counterexample pressure rather than vocabulary drift.

The current anti-drift invariant is therefore:

```text
new abstraction is earned only when a bounded falsifier demonstrates
that the previous custody object is insufficient for the proposition being claimed
```

not:

```text
interesting mathematical vocabulary
→ new authority
```

---

## 8. What Pedagogue may learn from this chamber

Within bounded research metabolism, Pedagogue may preserve the following candidate grammar for future hostile assays:

```text
declared serial order != identified total order
all admissible serializations must remain visible when order is underidentified
proposition invariant across all admitted serializations -> identified within assay scope
proposition variant across admitted serializations -> explicit abstention
identifier ordering != precedence authority
input ordering != precedence authority
lexical ordering != precedence authority
hidden implementation ordering != precedence authority
final-state equivalence != transient-history equivalence
resolved current state != identified historical path
```

This does not mutate the shared Pedagogue engine.

---

## 9. Next hostile bearing

The next bounded work should remain on custody identifiability rather than converting the phrase `partial order` into a passport for unrelated formalism.

Earned attack families include:

```text
1. final-state disagreement across admissible serializations
   - final presence itself must abstain when order changes the terminal state

2. three-event partial orders
   - some propositions invariant, others variant under the same admitted precedence

3. transitive-reduction / transitive-closure representation invariance
   - equivalent declared precedence must not change semantic custody

4. partial-order compaction
   - compacted representations may preserve only propositions proven invariant under the compaction

5. multiple support and contradiction lineages under incomparable events
   - current support, contradiction history, and support continuity may have different identifiability postures

6. precedence revision / withdrawal custody
   - changing the admitted precedence relation later must create a new custody episode rather than retroactively rewriting the old receipt
```

These are candidates for further hostile assay design, not automatic laws.

The directional warning is explicit:

```text
DO NOT infer from this chamber:
causal order
real-world concurrency semantics
Lamport-clock sufficiency
vector-clock sufficiency
distributed-systems correctness
serializability theorem
temporal database completeness
event-sourcing completeness
truth-maintenance completeness
provenance algebra
partial-order reduction theorem
category / lattice / sheaf structure
physical time law
connection / curvature / holonomy
quantum identity
```

No Northeast joyride by mathematical noun.

---

## 10. Held programs remain held

```text
C4 promotion = false
H2 = HELD_NOT_TESTED_HERE
H3 = HELD_NOT_TESTED_HERE
M×D = HELD
M×P = HELD
D×P = HELD
M×D×P = HELD
APERTURE_V32_REPLAY_STABILITY = HELD_NOT_YET_WITNESSED
```

No formalism promotion is earned from C4 survival alone.

---

## 11. Authority membrane

```text
research_target_admitted_by_operator = true
major_research_decisions_self_authorized_in_pr677_lane = true
product_mutation = false
shared_pedagogue_engine_mutation = false
workflow_mutation = false
browser_execution = false
merge_performed = false
deployment_performed = false
deployment_authority = false
release_authority = false
vercel_release_requires_issue_405_and_new_explicit_operator_gesture = true
promotion_authority = false
```

CI green remains infrastructure evidence, not theory survival.

This receipt seals the current scientific result; it does not convert C4 into a universal law or authorize product consequences.

𝌋 West held. Reachability became genealogy because membership lost custody; genealogy became episodes because snapshots lost time; episodes became a weave because a ledger cannot testify to an order it was never given. ⟐
