# Pedagogue Unlicensed Electrician · Dependency Edge Admission Provenance Custody · Execution Receipt

**Date:** 2026-08-21  
**Research surface:** Draft PR #677  
**Candidate:** `E5_DEPENDENCY_EDGE_ADMISSION_PROVENANCE_CUSTODY`  
**Assay:** Unlicensed Electrician  
**Status:** BOUNDED CANDIDATE SURVIVED / NOT PROMOTED

## Exact lineage

```text
72e13c85381ca5505eb5958e08c1afa012324152  E4 Relay Lantern sealed receipt
→ 598e9edf77b36962150d12e77a5fbb23803719bc  E5 preregistration
→ 442eeeadb0b494d2132066564a85815d03e0fa80  E5 evaluator
→ 10045c671a0afd711ef2ccc26b7c7c76b871e235  E5 hostile test / science head
```

## Witness

`TD613 Consolidated Validation` run **1918 / 32539421026** executed against exact science head `10045c671a0afd711ef2ccc26b7c7c76b871e235`.

Static job: `96946346309` — **SUCCESS**.  
Step 18, `Validate Ash A15 empirical profile journeys and A15-R0 research field` — **SUCCESS**.

The classifier kept every expensive lane outside the assay:

- explicit full-repository validation — SKIPPED
- explicit self-hosted calibration — SKIPPED
- Giving/practice Chromium/Firefox/WebKit witness — SKIPPED
- front-line browser shards — SKIPPED
- full-product Chromium/Firefox/WebKit witness — SKIPPED

No browser execution was scientifically earned by E5.

## Evidence provenance

A bounded Actions-log recovery attempt did not yield a reliable narrow literal E5 JSON stdout block. This receipt therefore does **not** claim literal console extraction.

The bounded verdict below is established by two jointly stated forms of evidence:

1. successful exact-head execution of the immutable E5 hostile-test family at step 18; and
2. deterministic reconciliation of the exact immutable preregistration, evaluator, and hostile-test bytes at the lineage above.

`successful CI != scientific survival`; the hostile test explicitly accepts either candidate survival or scientific falsification. The survival verdict is recorded from evaluation of the preregistered defeat predicates, not inferred from a green workflow icon.

## Inherited insufficiency established

The strongest control succeeds in the required direction:

```text
E4_DECLARED_DEPENDENCY_EDGE_ADMISSION_INSUFFICIENCY_ESTABLISHED
```

In UE01, E4 admits a syntactically valid W1→W2 dependency edge when both endpoints are known, even though no independent edge-admission record exists. With W1 directly lawful, E4 therefore propagates current transitive authority to W2. E5 refuses that same unadmitted edge and leaves W2 without current lawful support.

E5 does not receive credit for repairing a parent defect unless the parent defect is first exhibited.

## Bounded candidate verdict

```text
DEPENDENCY_EDGE_ADMISSION_PROVENANCE_CUSTODY_CANDIDATE_SURVIVES_BOUNDED_UNLICENSED_ELECTRICIAN
defeat_conditions = []
```

The result remains candidate research. It is not promoted into a shared Pedagogue engine operator, authorization framework, or universal graph law.

## Hostile-room findings

### UE01 · Extension Cord Through the Window
E4 admits the unlicensed declared edge and propagates W2. E5 returns `REFUSE_UNADMITTED_DEPENDENCY_EDGE` and W2 returns to `REFUSE_CURRENT_WARRANT_NO_ACTIVE_LAWFUL_SUPPORT`.

### UE02 · Permit on File
A syntactically valid edge with a unique, active, unrevoked, materially matching `DEPENDENCY_EDGE_ADMISSION` record is admitted as `ADMIT_LEDGER_BOUND_DEPENDENCY_EDGE`; inherited E4 propagation then reaches W2.

### UE03 · Old Permit, New Wiring
Preserving `edge_id` and endpoints while changing `dependency_kind` and `scope_fingerprint` changes material edge identity. E4 still propagates because those fields are outside its classifier. E5 refuses the old permit as `REFUSE_STALE_DEPENDENCY_EDGE_ADMISSION_BINDING`.

### UE04 · Number on the Permit
Renaming `edge_id` and `admission_id` without changing material binding leaves authority unchanged. Identifier strings do not select semantic authority.

### UE05 · Papers Reversed on the Desk
Reversing warrants, edges, admission records, and cited admission-record order leaves current authority and admission-state fingerprints invariant.

### UE06 · Photocopies of the Permit
Multiple materially equivalent valid admission records do not amplify authority, confidence, or consequence. Multiplicity may remain visible as custody data but is not scalar support.

### UE07 · Permit Revoked
A materially matching but revoked record returns `REFUSE_REVOKED_DEPENDENCY_EDGE_ADMISSION`; W2 receives no transitive authority from that edge.

### UE08 · Permit for the Neighbor's House
An admission record materially bound to another edge cannot authorize the candidate edge. Material mismatch is refused rather than treated as endpoint resemblance.

### UE09 · Licensed and Unlicensed Copies
A valid admitted edge remains usable when a same-material fabricated/unadmitted copy is present. The fabricated copy is refused and cannot erase the valid path.

### UE10 · Endpoint Photograph
A snapshot claiming that W2 was transitive-lawful does not substitute for edge-admission provenance. Endpoint status has no edge-admission authority.

### UE11 · Duplicate Admission ID
One `admission_id` assigned to materially incompatible records is conflicted. E5 returns `REFUSE_DUPLICATE_DEPENDENCY_EDGE_ADMISSION_ID`; lexical order and serialization do not choose a winner.

### UE12 · Locked Permit Cabinet
A sealed admission-ledger record is immutable under the bounded mutation request: `SEALED_DEPENDENCY_EDGE_ADMISSION_RECORD_IMMUTABLE`.

### UE13 · Relay Lantern Control
For a properly admitted material edge, E5 preserves Relay Lantern's current-state propagation semantics exactly. Admission custody narrows which edges enter E4; it does not rewrite E4's propagation rule.

## Exact claim ceiling

Unlicensed Electrician establishes only the bounded synthetic proposition:

```text
a dependency edge may contribute current transitive warrant authority
only after >=1 active, unrevoked, materially matching admission record
survives the bounded E5 admission checks
```

and, in the same bounded evaluator:

```text
known endpoints
!= edge-admission authority

edge identifier continuity
!= semantic authorization continuity

same endpoints
!= same dependency meaning

admission multiplicity
!= confidence

endpoint snapshot
!= edge-admission provenance
```

Most importantly:

```text
ledger-bound edge admission
!= admission-ledger provenance
!= witnessed authorization event
!= institutional authorization
!= real-world authority
```

The E5 admission ledger is a bounded synthetic authority surface for this assay, not a self-authenticating institution.

## Newly exposed frontier

E5 currently trusts a materially matching, active, unrevoked admission record **because the record is present in the supplied admission ledger**. It has not established how that record entered the ledger, whether an admission event was independently witnessed, or whether the ledger can bootstrap its own authority.

The next earned attack is therefore:

```text
ledger presence
!= admission authority

material binding
!= admission provenance

admission record
!= witnessed admission event
```

Next learning action:

`ATTACK_EDGE_ADMISSION_LEDGER_PROVENANCE_AND_BOOTSTRAP_AUTHORITY_BEFORE_LARGER_GRAPH_COMPOSITION`

## Held boundaries

```text
synthetic_exogenous_fixture = true
live_external_source_adapter = false
real_world_external_provenance_claim = false
real_world_authorization_claim = false
admission_ledger_provenance = HELD_FOR_NEXT_ATTACK
universal_graph_semantics = false
scalar_aggregation_used = false
H2 = HELD_NOT_TESTED_HERE
H3 = HELD_NOT_TESTED_HERE
intersections = HELD_NOT_OPENED_HERE
APERTURE_V32_REPLAY_STABILITY = HELD_NOT_YET_WITNESSED
promotion_authority = false
product_mutation = false
shared_pedagogue_engine_mutation = false
workflow_mutation = false
browser_execution = false
merge_performed = false
deployment_performed = false
release_authority = false
vercel_release_requires_issue_405_and_new_explicit_operator_gesture = true
```

𝌋 The permit can authorize a wire. The permit still cannot issue itself. ⟐
