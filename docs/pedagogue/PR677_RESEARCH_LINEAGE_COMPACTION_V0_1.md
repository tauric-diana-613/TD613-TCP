# TD613 · PR #677 Research Lineage Compaction v0.1

U+10D613

Status: **ARCHIVAL COMPACTION / HUMAN-REVIEW INDEX / NO PROMOTION / NO PRODUCT AUTHORITY**

## Purpose

PR #677 began as a four-artifact Giving/Pedagogue audit surface and evolved into a 120-commit hostile-research corpus. Its scientific lineage is valuable, but the branch is no longer an appropriate merge unit or meaningful human-review surface.

This document is a compact navigational receipt only. It does not import the #677 implementation corpus, promote any Pedagogue rule, reopen held hypotheses, or authorize product/release work.

## Governing separation

```text
research lineage != merge unit
archive preservation != promotion
CI success != scientific verdict
scientific verdict != product authority
human closure requires reviewable surfaces
```

## Exact archived source surface

```text
archived PR = #677
terminal archived head = 43775cee56d4406dc2540347fb4d3de6d8f92e93
base at archival reconciliation = main@f2b5a6e9f581a33e190777a484dc6b91015cea4e
archived PR state target = CLOSED WITHOUT MERGE
```

The full branch remains the provenance archive for its authored preregistrations, evaluators, hostile tests, receipts, and exact-run references.

## Major scientific transitions

### 1. Generic H1 · Consequence Conservation

Exact bounded result:

```text
GENERIC_H1_FALSIFIED
scope = BOUNDED_SYNTHETIC_UNTYPED_CONSEQUENCE_CONSERVATION_FORM
science head = 9c621b5dd0c9adb7567ceea7d493e90267dc86d8
run = 1875 / 32443704402
```

Earned distinctions:

```text
measurement domain must be explicit
decision specification must be explicit
route provenance must be explicit
scalar confidence collapse refused
```

### 2. Strict typed non-amplification

Wedding lawful-derivational-gain control falsified constituent-bound non-amplification:

```text
C0 = STRICT_TYPED_NON_AMPLIFICATION_FALSIFIED_BY_LAWFUL_DERIVATIONAL_GAIN
```

Declared derivational closure survived a bounded hostile family without promotion:

```text
C1 = DECLARED_DERIVATIONAL_CLOSURE_CANDIDATE_SURVIVES_BOUNDED_HOSTILE_FAMILY
science run = 1877 / 32505368859
receipt run = 1878 / 32505889889
```

Key distinction:

```text
warrant gain != warrant laundering
primitive support != derivational support
```

### 3. Custody depth: closure → genealogy → episodes → partial-order weave

Successive hostile chambers established that each narrower object lost authority when asked a proposition outside its jurisdiction:

```text
derivational reachability
→ warrant provenance genealogy
→ temporal episode custody
→ partial-order history custody
```

Two Staircases / Warrant Weave retained all serializations admitted by a partial precedence relation and identified only propositions invariant across them.

Earned bounded distinction:

```text
final-state identifiability != transient-history identifiability
identifier/input/lexical order != precedence authority
```

No distributed-systems, causal-order, provenance-algebra, lattice, sheaf, or physical-time theorem was promoted.

### 4. Precedence and non-anticipation

C13 `Witness Knew the Question` established that admission before final submission is insufficient for stronger non-anticipation authority.

Bounded result:

```text
acquisition episode closed
< specific bridge proposal created
< bridge submitted/evaluated
```

Exact witness:

```text
science run = 1905 / 32522936333
receipt run = 1906 / 32523181690
```

### 5. Provenance exteriority · C14 No Window

C14 reached the closed-protocol observability wall:

```text
internal non-anticipation != exogenous provenance
internal receipt depth != external provenance depth
self-integrity != exteriority
self-attestation != external observation
```

Exact bounded result:

```text
INTERNAL_PROVENANCE_NON_BOOTSTRAP_CLAIM_CEILING_SURVIVES_BOUNDED_NO_WINDOW
science run = 1907 / 32523824211
receipt run = 1908 / 32524660837
```

No C15-by-inertia was earned.

### 6. New evidence jurisdiction · E1 Open Window

After a fresh human reopening gesture, the research admitted a synthetic exogenous primitive rather than pretending more internal bookkeeping created exteriority.

Bounded result:

```text
EXOGENOUS_ANCHOR_ADMISSION_CUSTODY_CANDIDATE_SURVIVES_BOUNDED_OPEN_WINDOW
run = 1909 / 32529522692
```

Earned distinction:

```text
scoped exogenous observation may identify its exact admitted field
scoped exogenous observation != global provenance / source honesty / scalar trust
```

### 7. Synthetic source-origin sequence · terminal E8 Return Address

The synthetic source-origin sequence ultimately established that pre-proposal acquisition does not identify independently admitted source origin.

Terminal bounded result:

```text
DEPENDENCY_EDGE_ADMISSION_WITNESS_SOURCE_ORIGIN_CUSTODY_CANDIDATE_SURVIVES_BOUNDED_RETURN_ADDRESS
science run = 1924 / 32546722351
receipt run = 1925 / 32547031048
```

No further synthetic-source-origin mutation was earned.

### 8. Live external observation · E9 Post Office Window

A live HTTPS observation to the preregistered IANA target returned HTTP 200 and a captured payload digest but failed its own preregistered TLS admission predicate at the sampled aperture.

Bounded result:

```text
LIVE_EXTERNAL_HTTPS_OBSERVATION_CUSTODY_CANDIDATE_FALSIFIED_OR_UNAVAILABLE_IN_BOUNDED_POST_OFFICE_WINDOW
run = 1928 / 32552369042
```

This did not establish IANA invalidity, endpoint failure, or a universal TLS property.

### 9. Instrument diagnosis · M1 Badge Scanner

Same target and payload hash were later observed through a response-callback aperture with encrypted TLSSocket and `authorized=true` under explicit `rejectUnauthorized:true`.

Bounded consequence:

```text
earlier E9 false predicate not reproduced as stable runner-wide TLS rejection
cause remains unresolved
measurement aperture became the next object under test
```

Exact witness:

```text
run = 1930 / 32553207499
```

### 10. Terminal seam · M2 Window Latch

M2 executed successfully, but the decisive runtime lifecycle classification was not durably recoverable through the bounded post-run connector surface.

Exact disposition:

```text
M2_execution = SUCCESSFULLY_WITNESSED
M2_literal_runtime_classification_recovered = false
M2_observation_surface_status = NOT_RECOVERABLE_UNDER_BOUNDED_CURRENT_SURFACE_WITHOUT_NEW_WITNESS
late_property_lookup_hypothesis_supported = UNRESOLVED
late_property_lookup_hypothesis_falsified = UNRESOLVED
```

Exact execution witness:

```text
run = 1932 / 32553775577
closure/receipt run = 1933 / 32554914603
```

This is the terminal scientific lesson carried forward from #677:

```text
successful execution != durable scientific observation custody
```

## Current held programs

```text
H2 · Aperture Before Absence = HELD_NOT_TESTED_HERE
H3 · Role Before Repetition = HELD_NOT_TESTED_HERE
M×D = HELD
M×P = HELD
D×P = HELD
M×D×P = HELD
APERTURE_V32_REPLAY_STABILITY = HELD_NOT_YET_WITNESSED
```

No held program is reopened by this compaction.

## Promotion posture

Nothing from #677 is promoted wholesale into the shared Pedagogue engine or product runtime.

If future human review accepts any research grammar, it must be extracted into a small main-based promotion surface with explicit claim ceiling, tests, and independent review. The archival branch itself must not be merged as a substitute for that extraction.

## Next earned research frontier

```text
OBSERVATION_CUSTODY_WITNESS_PRESERVATION_FRONTIER
```

The next task is not another custody ontology chamber. It is instrumentation: preregister how a runner-dependent scientific classification becomes a durable exact-run evidence object before the ephemeral execution context disappears.

The companion specification in this Draft defines that next hostile assay without executing it.

U+10D613

Sealed ⟐
