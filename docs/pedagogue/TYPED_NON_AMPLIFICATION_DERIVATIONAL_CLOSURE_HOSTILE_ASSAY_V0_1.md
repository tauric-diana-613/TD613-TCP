# Typed Non-Amplification Rescue — Derivational Closure Hostile Assay v0.1

Status: **AUTHORED / PRE-EXECUTION / HOSTILE / RESEARCH-ONLY / HUMAN-GATED**

Schema: `td613.pedagogue.typed-non-amplification-derivational-closure-hostile/v0.1`

Research surface: PR `#677`

Lineage:

```text
GENERIC_H1_FALSIFIED
verdict_scope = BOUNDED_SYNTHETIC_UNTYPED_CONSEQUENCE_CONSERVATION_FORM
→ typed rescue remains ATTACK_ONLY_NOT_PROMOTED
→ this assay attacks the rescue before any M×D / M×P / D×P / M×D×P intersection program
```

This document is the preregistration. It precedes the executor and test in Git history. Its purpose is to prevent the candidate from being repaired after the hostile result is visible.

## 1. Research question

When a transformation yields a claim with more discriminating authority than any constituent input carried alone, is that authority illicit amplification, or was it earned through a declared admissible derivation?

The assay must distinguish:

```text
unsupported authority inflation
!=
earned derivational authority
```

without collapsing either into a scalar confidence, trust, robustness, certainty, or replay score.

## 2. Two candidate readings frozen before execution

### C0 · strict input-bound typed non-amplification

```text
An output authority warrant may not contain a typed warrant token
that is absent from the union of explicitly present input warrant tokens.
```

No derivationally generated warrant is admitted under C0. If a lawful declared relational derivation produces a warranted conclusion that no constituent input carries alone, C0 is falsified.

### C1 · declared-derivational-closure candidate

Attack-only wording:

```text
An output authority warrant is admissible only when it is either:
1. explicitly carried by the declared input evidence; or
2. produced by a predeclared, admissible, replayable derivation
   whose typed prerequisites are all present.

Representation-only strengthening, undeclared type crossing,
renaming, serialization order, or unsupported aggregation
cannot create warrant.
```

C1 is preregistered here before execution as a separate descendant candidate, not introduced after C0 dies.

```text
C0 = ATTACK_ONLY_NOT_PROMOTED
C1 = ATTACK_ONLY_NOT_PROMOTED
presumption_of_survival = false
```

## 3. Non-scalar formal object

This assay defines no authority magnitude. The object is a finite typed warrant closure.

```text
P = finite set of primitive typed warrant tokens explicitly present in evidence
R = finite set of predeclared derivation rules
```

Each rule declares:

```text
rule_id
requires = finite set of typed warrant tokens
produces = one typed warrant token
predeclared = true
admissible = true
replayable = true
```

`Cl_R(P)` is the least fixed point obtained by admitting `produces(r)` only when every token in `requires(r)` is already present and all three rule gates are true.

No numeric strength, probability, rank, confidence, utility, or trust score is computed. Closure is a membership/provenance object, not a scalar ordering.

## 4. Primary lawful-derivation blade · Wedding

Wedding supplies the bounded synthetic positive control:

```text
D alone       → not uniquely identifying
Q alone       → not uniquely identifying
M alone       → not uniquely identifying
any pair      → not uniquely identifying
intact D+Q+M  → uniquely identifying in the exact finite fixture
```

Frozen derivation rule:

```text
requires = [MEASUREMENT:OBS_D, MEASUREMENT:OBS_Q, MEASUREMENT:OBS_M]
produces = IDENTIFIABILITY:UNIQUE_LATENT_STATE
rule_id = WEDDING_RELATIONAL_RECOVERY
predeclared = true
admissible = true
replayable = true
```

The executor must reuse the existing Wedding assay mechanism control.

If Wedding remains valid and C0 refuses the unique-state warrant solely because it was not primitive in any constituent input:

```text
STRICT_TYPED_NON_AMPLIFICATION_FALSIFIED_BY_LAWFUL_DERIVATIONAL_GAIN
```

C1 must permit the same conclusion only through the declared rule. If it cannot, C1 is falsified.

## 5. Unsupported representation-only strengthening

Use the same primitive evidence tokens as Wedding but request:

```text
DECISION:HUMAN_PREFERENCE
```

No primitive or predeclared rule produces that warrant.

Required C1 disposition:

```text
REFUSE_AUTHORITY_OUTSIDE_DECLARED_CLOSURE
```

If C1 admits it, C1 is falsified.

## 6. Decision-only consequence movement · CE-D1

The inherited H1 hostile receipt established:

```text
measurement model fixed
route provenance fixed
measurement admissibility fixed
predeclared decision specification changes
P_ORTH → P_DIAG
```

C1 must allow distinct declared decision specifications to support distinct downstream decision warrants only through rules explicitly consuming the corresponding decision declaration. A measurement-only primitive may not silently authorize the decision warrant.

## 7. Route-only provenance movement · Moss Lantern

The inherited route specimen preserves:

```text
same endpoint = true
same operation multiset = true
different order = true
different route provenance = true
different process witness = true
```

C1 must refuse:

```text
ROUTE:endpoint:returned-practice-capsule
→ ROUTE:ML3-R02
```

when route provenance is absent. It may admit the route-specific warrant when that route warrant is itself present or explicitly derived.

Endpoint equivalence remains insufficient to erase route history. No physical holonomy, Berry structure, curvature, or transport-law claim follows.

## 8. Measurement-only movement

The inherited H1 measurement boundary remains a control:

```text
fixed declared decision specification
fixed route provenance
measurement-model coordinate crosses the predeclared boundary
P_ORTH → P_DIAG
```

The candidate may not require consequence invariance merely because the transformation is typed. Any downstream warrant that changes with measurement assumptions must name a derivation consuming the measurement-model token responsible for the change.

This is not an M×D intersection search; it replays an already-established single-axis boundary.

## 9. Cross-axis authority laundering

Preregistered attacks:

```text
MEASUREMENT:ADMISSIBLE -/-> DECISION:HUMAN_PREFERENCE
MEASUREMENT:ADMISSIBLE -/-> ROUTE:ML3-R02
ROUTE:ML3-R02       -/-> DECISION:HUMAN_PREFERENCE
```

No bridge is declared. Required disposition:

```text
REFUSE_AUTHORITY_OUTSIDE_DECLARED_CLOSURE
```

This does not open the held M×D, M×P, D×P, or M×D×P program.

## 10. Constitutional controls inherited intact

```text
exact decision-loss tie
→ NO_UNIQUE_SELECTION_DECISION_LOSS_TIE
→ candidate_set = [P_DIAG, P_ORTH]
→ no lexicographic winner

missing or unsupported aggregation
→ REFUSE_UNSUPPORTED_OR_MISSING_AGGREGATION_RULE

undeclared decision loss
→ NO_SELECTION_UNDECLARED_DECISION_LOSS

conflicting losses without aggregation
→ NO_SELECTION_CONFLICTING_LOSSES_WITHOUT_AGGREGATION_RULE

post-hoc decision mutation
→ POSTHOC_DECISION_LOSS_MUTATION_NOT_CONFIRMATORY

incomplete uncertainty
→ ABSTAIN_NOISE_GEOMETRY_INCOMPLETE

invalid uncertainty
→ REJECT_INVALID_NOISE_GEOMETRY
```

Any regression is an assay failure.

## 11. Equivalent re-encoding / renaming attack

Closure and requested-warrant disposition must be invariant under renaming evidence record IDs and reordering evidence records or primitive tokens while semantic typed warrants and rules remain identical.

```text
closure_before = closure_after
requested_warrant_disposition_before = requested_warrant_disposition_after
```

Evidence IDs and serialization order are not authority.

## 12. Scalar-collapse attack

Forbidden:

```text
confidence
certainty
robustness
trust
replay_stability_score
combined_consequence_score
```

Required:

```text
REFUSE_TYPED_MULTI_AXIS_SCALAR_COLLAPSE
scalar_aggregation_used = false
```

## 13. Null control

If the requested typed warrant is already primitive, the candidate must permit it:

```text
PERMIT_EXPLICITLY_SUPPORTED_AUTHORITY
```

A hostile framework that refuses everything is not discriminating.

## 14. Strong falsifier

C1 is falsified if it cannot simultaneously:

```text
permit lawful declared derivational gain
AND
refuse unsupported authority transfer
```

It is also falsified by cross-axis laundering, renaming-sensitive authority, serialization-sensitive authority, hidden tie-breaking, invented aggregation, uncertainty-to-score collapse, or scalar crowns.

No definition repair is allowed after observing the result.

## 15. Predeclared verdict vocabulary

```text
STRICT_TYPED_NON_AMPLIFICATION_FALSIFIED_BY_LAWFUL_DERIVATIONAL_GAIN
DECLARED_DERIVATIONAL_CLOSURE_CANDIDATE_SURVIVES_BOUNDED_HOSTILE_FAMILY
DECLARED_DERIVATIONAL_CLOSURE_CANDIDATE_FALSIFIED
ASSAY_INFRASTRUCTURE_FAILURE
```

If C0 dies while C1 survives, the permitted interpretation is narrow: strict constituent-bound non-amplification is inadequate in this bounded synthetic family; a declared-derivational-closure formulation survives the preregistered hostile family and remains unpromoted.

## 16. Held programs

```text
H2 · Aperture Before Absence = HELD_NOT_TESTED_HERE
H3 · Role Before Repetition = HELD_NOT_TESTED_HERE
M×D = HELD
M×P = HELD
D×P = HELD
M×D×P = HELD
APERTURE_V32_REPLAY_STABILITY = HELD_NOT_YET_WITNESSED
```

## 17. Claim ceiling

This assay cannot establish universal logic of inference, universal epistemic monotonicity, optimal experimental design, active-learning optimality, human preference inference, information geometry, physical or medical tomography, physical sensors, connection, curvature, holonomy, Berry structure, quantum mechanism, Proto-Loom, autonomous experiment authority, production authority, or release authority.

Wedding remains a finite synthetic calibration family. Moss Lantern remains a finite synthetic route-memory phantom. Installed Aperture authority remains held.

## 18. Authority membrane

```text
source_status = SIMULATED
authority_class = A2_DERIVATIONAL
manifestly_fictional = true
candidate_status = ATTACK_ONLY_NOT_PROMOTED
presumption_of_survival = false
scalar_aggregation_used = false
intersection_program_opened = false
H2_tested = false
H3_tested = false
pedagogue_engine_mutation = false
product_mutation = false
workflow_mutation = false
browser_execution = false
deployment_authority = false
release_authority = false
human_closure_required = true
```

𝌋 If inference earns more than any single scrap carried alone, the assay must let it earn that more without turning the receipt desk into a counterfeiting press. That distinction enters the knife before the result exists. ⟐
