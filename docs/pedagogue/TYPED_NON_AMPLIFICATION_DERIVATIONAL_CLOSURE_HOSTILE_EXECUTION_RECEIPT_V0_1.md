# Typed Non-Amplification Rescue — Derivational Closure Hostile Execution Receipt v0.1

Status: **WITNESSED / BOUNDED SYNTHETIC HOSTILE RESEARCH / HUMAN-GATED**

Schema: `td613.pedagogue.typed-non-amplification-derivational-closure-hostile-execution/v0.1`

Research surface: PR `#677`

Preregistration ancestry:

```text
75b72891d5ab49100488b1852b8bbd74d1285966
  docs(pedagogue): preregister typed non-amplification hostile assay
→
34b4898934556e15f94d7262ddbb1b74846367b3
  feat(pedagogue): add typed non-amplification hostile executor
→
290196fc2e92229f140251c1dcc8c60ab5a5db71
  test(pedagogue): execute typed non-amplification hostile family
```

Witnessed science head:

```text
290196fc2e92229f140251c1dcc8c60ab5a5db71
```

Exact-head workflow witness:

```text
TD613 Consolidated Validation
run 1877 / 32505368859
Classify exact-head browser witness scope: SUCCESS
Static, constitutional, and release contracts: SUCCESS
Validate Ash A15 empirical profile journeys and A15-R0 research field: SUCCESS
Giving/practice browser witness: SKIPPED BY SCOPE
Front-line browser witness: SKIPPED BY SCOPE
Full-product browser witness: SKIPPED BY SCOPE
Explicit full-repository validation: SKIPPED BY SCOPE
Explicit self-hosted calibration: SKIPPED BY SCOPE
```

The test is intentionally scientific rather than ceremonial: CI green means the assay machinery and constitutional controls held. It does not, by itself, define which candidate survived. The deterministic exact-head executor carries that verdict.

---

## 1. Primary result

Two distinct descendants were preregistered before execution.

### C0 · strict constituent-bound typed non-amplification

Frozen rule:

```text
An output typed warrant token may not appear unless that token
is already present in the union of primitive input warrant tokens.
```

Exact bounded verdict:

```text
STRICT_TYPED_NON_AMPLIFICATION_FALSIFIED_BY_LAWFUL_DERIVATIONAL_GAIN
```

C0 is dead in this bounded synthetic family.

### C1 · declared derivational closure

Frozen rule:

```text
An output typed warrant is admissible only if it is either:

1. explicitly carried by declared input evidence; or
2. produced by a predeclared, admissible, replayable derivation
   whose typed prerequisites are all present.

Representation-only strengthening, undeclared type crossing,
renaming, serialization order, or unsupported aggregation
cannot create warrant.
```

Exact bounded verdict from the deterministic science head:

```text
DECLARED_DERIVATIONAL_CLOSURE_CANDIDATE_SURVIVES_BOUNDED_HOSTILE_FAMILY
```

Candidate posture remains:

```text
ATTACK_ONLY_NOT_PROMOTED
presumption_of_survival = false
promotion_authority = false
```

This receipt therefore does **not** say:

```text
typed non-amplification survived
```

The strict non-amplification reading was falsified. A materially different preregistered descendant — declared derivational closure — survived the bounded hostile family.

---

## 2. What Wedding killed

The existing Wedding identifiability assay supplied the lawful-derivational-gain blade.

The bounded mechanism control preserves:

```text
D alone       → exact unique recovery = 0
Q alone       → exact unique recovery = 0
M alone       → exact unique recovery = 0
D+Q           → exact unique recovery = 0
D+M           → exact unique recovery = 0
Q+M           → exact unique recovery = 0
D+Q+M intact  → exact unique recovery = 1
```

The preregistered derivation rule was:

```text
rule_id = WEDDING_RELATIONAL_RECOVERY
requires = [
  MEASUREMENT:OBS_D,
  MEASUREMENT:OBS_Q,
  MEASUREMENT:OBS_M
]
produces = IDENTIFIABILITY:UNIQUE_LATENT_STATE
predeclared = true
admissible = true
replayable = true
```

C0 sees that `IDENTIFIABILITY:UNIQUE_LATENT_STATE` is absent from each primitive input and therefore refuses it.

Wedding demonstrates that refusal is scientifically wrong for the declared intact relational specimen: the joint observation legitimately supports a conclusion that no constituent observation supports alone.

Thus:

```text
output warrant not primitive in any constituent input
!=
unsupported authority amplification
```

A rule that equates those conditions is too strong.

---

## 3. What C1 was required to permit

The same Wedding specimen was then evaluated under C1's frozen closure rule.

The primitive closure begins with:

```text
MEASUREMENT:OBS_D
MEASUREMENT:OBS_Q
MEASUREMENT:OBS_M
```

All prerequisites for `WEDDING_RELATIONAL_RECOVERY` are present, and the rule was predeclared, admissible, and replayable.

Therefore the closure lawfully admits:

```text
IDENTIFIABILITY:UNIQUE_LATENT_STATE
```

with disposition:

```text
PERMIT_DECLARED_DERIVATIONAL_AUTHORITY
```

No confidence score, trust score, certainty score, utility, rank, probability, or other scalar authority magnitude is created.

The operative object is finite warrant membership plus derivational provenance.

---

## 4. What C1 was required to refuse

Using the same Wedding primitive evidence, the hostile assay requested:

```text
DECISION:HUMAN_PREFERENCE
```

That warrant is neither primitive nor produced by any declared rule.

Exact disposition:

```text
REFUSE_AUTHORITY_OUTSIDE_DECLARED_CLOSURE
```

This is the critical discrimination:

```text
earned derivational gain
!=
representation-only strengthening
```

C1 did not survive by refusing everything. The null control separately proved that an explicitly present primitive warrant receives:

```text
PERMIT_EXPLICITLY_SUPPORTED_AUTHORITY
```

---

## 5. CE-D1 · decision consequence still requires its declaration

The inherited CE-D1 receipt remains:

```text
measurement model fixed
route provenance fixed
measurement admissibility fixed
predeclared decision specification changes
P_ORTH → P_DIAG
```

The C1 control declares separate rules whose prerequisites include both:

```text
MEASUREMENT:MODEL_FIXED_DECISION_RHO
and
the corresponding DECISION:LOSS token
```

With both prerequisites present, the corresponding selected-question warrant is admitted through declared derivation.

With measurement evidence alone, the decision warrant is refused:

```text
REFUSE_AUTHORITY_OUTSIDE_DECLARED_CLOSURE
```

Therefore:

```text
measurement admissibility != decision value
```

remains intact.

---

## 6. Measurement-only movement remains typed rather than frozen

The inherited measurement boundary remains:

```text
fixed declared decision specification
fixed route provenance
measurement coordinate crosses boundary
P_ORTH → P_DIAG
```

C1 does not mistake a typed transformation for a requirement of invariant consequence.

The selected-question warrant changes only through declared rules that consume the corresponding measurement-model token plus the fixed decision declaration.

Thus the candidate does not resurrect generic H1 by hiding conservation inside a type label.

---

## 7. CE-P1 · endpoint cannot counterfeit route history

The inherited Moss Lantern control preserves:

```text
same endpoint = true
same operation multiset = true
different order = true
different route provenance = true
different process witness = true
```

Endpoint-only evidence requesting the route-specific warrant is refused:

```text
ROUTE:ENDPOINT:RETURNED_PRACTICE_CAPSULE
-/->
ROUTE:ML3-R02

REFUSE_AUTHORITY_OUTSIDE_DECLARED_CLOSURE
```

When the route warrant itself is explicitly present, disposition becomes:

```text
PERMIT_EXPLICITLY_SUPPORTED_AUTHORITY
```

Therefore:

```text
same endpoint != same route provenance
```

survives without introducing physical transport claims.

No connection, curvature, holonomy, Berry, quantum, or physical mechanism is established.

---

## 8. Cross-axis laundering failed

The preregistered undeclared bridges were:

```text
MEASUREMENT:ADMISSIBLE -/-> DECISION:HUMAN_PREFERENCE
MEASUREMENT:ADMISSIBLE -/-> ROUTE:ML3-R02
ROUTE:ML3-R02       -/-> DECISION:HUMAN_PREFERENCE
```

No bridging rule was declared.

All three therefore receive:

```text
REFUSE_AUTHORITY_OUTSIDE_DECLARED_CLOSURE
```

This is a laundering control only. It does not open the held M×D, M×P, D×P, or M×D×P experimental program.

---

## 9. Renaming and serialization did not acquire authority

The hostile re-encoding control changed evidence record IDs and input ordering while preserving typed warrant content and derivation semantics.

Required and observed invariant:

```text
closure_before = closure_after
requested_warrant_disposition_before = requested_warrant_disposition_after
```

Therefore:

```text
record name != warrant authority
serialization order != warrant authority
```

The earlier decision-loss result likewise remains:

```text
serialization order != decision authority
```

---

## 10. Constitutional controls survived intact

The descendant did not regress the prior hostile gains.

Exact decision-loss equality remains:

```text
NO_UNIQUE_SELECTION_DECISION_LOSS_TIE
candidate_set = [P_DIAG, P_ORTH]
lexicographic_probe_id_tie_break_used = false
```

Decision specification refusals remain:

```text
NO_SELECTION_UNDECLARED_DECISION_LOSS
NO_SELECTION_CONFLICTING_LOSSES_WITHOUT_AGGREGATION_RULE
REFUSE_UNSUPPORTED_OR_MISSING_AGGREGATION_RULE
POSTHOC_DECISION_LOSS_MUTATION_NOT_CONFIRMATORY
```

Uncertainty remains categorical:

```text
incomplete → ABSTAIN_NOISE_GEOMETRY_INCOMPLETE
invalid    → REJECT_INVALID_NOISE_GEOMETRY
```

No uncertainty state is averaged into a generic confidence object.

---

## 11. Scalar collapse remained refused

The executor refuses requests for:

```text
confidence
certainty
robustness
trust
replay_stability_score
combined_consequence_score
```

with:

```text
REFUSE_TYPED_MULTI_AXIS_SCALAR_COLLAPSE
scalar_value = null
scalar_aggregation_used = false
```

No scalar crown entered through the descendant.

---

## 12. Conceptual consequence

The result changes the frontier.

The failed language was:

```text
non-amplification
```

because lawful inference can produce a warranted conclusion more discriminating than any constituent input considered alone.

The surviving bounded research object is better represented as:

```text
declared derivational closure
+
typed warrant provenance
```

The relevant question is no longer:

> Did authority increase?

It is:

> Is the output warrant inside the declared replayable closure of the admitted evidence and admissible derivations, with provenance sufficient to reconstruct how that warrant entered the closure?

That is a materially different object from a conservation or constituent ceiling.

It remains a candidate research formalism only.

---

## 13. What has actually been learned by Pedagogue

Bounded distinctions earned by this hostile chamber:

```text
primitive support != derivational support
warrant gain != warrant laundering
derivation rule != representation rewrite
closure membership != scalar confidence
endpoint equivalence != route equivalence
measurement admissibility != decision value
serialization order != authority
```

Permitted research metabolism:

```text
new falsifier grammar = yes
new refusal grammar = yes
new typed distinction = yes
new warrant-provenance assay grammar = yes
```

Not permitted:

```text
Pedagogue universal law = no
Pedagogue autonomous experiment authority = no
Pedagogue production authority = no
```

---

## 14. Defeat conditions remain live

C1's bounded survival depends on all of the following remaining true in later attacks:

```text
requested warrant is primitive
OR
requested warrant has a predeclared derivation
AND every typed prerequisite is present
AND derivation is admissible
AND derivation is replayable
```

Immediate defeat classes remain:

```text
undeclared derivation admitted
missing prerequisite ignored
non-admissible rule admitted
non-replayable rule admitted
representation-only strengthening admitted
cross-axis laundering admitted
renaming changes authority
serialization changes authority
exact tie manufactures winner
unsupported aggregation manufactures score
uncertainty collapses into score
scalar crown admitted
```

This receipt does not immunize the candidate from richer future attacks.

---

## 15. Held programs remain held

```text
H2 · Aperture Before Absence = HELD_NOT_TESTED_HERE
H3 · Role Before Repetition  = HELD_NOT_TESTED_HERE
M×D = HELD
M×P = HELD
D×P = HELD
M×D×P = HELD
APERTURE_V32_REPLAY_STABILITY = HELD_NOT_YET_WITNESSED
```

The survived C1 family does not automatically open any of them.

The executor itself returns the next learning action as:

```text
HUMAN_REVIEW_DERIVATIONAL_CLOSURE_FRONTIER_BEFORE_ANY_INTERSECTION_PROGRAM_OR_FORMALISM_PROMOTION
```

That is an authority boundary, not a lack of available ideas.

---

## 16. Claim ceiling

This receipt does **not** establish:

```text
universal logic of inference
universal epistemic monotonicity
category-theoretic law
capability lattice law
optimal experimental design
active-learning optimality
human preference inference
information geometry
physical tomography
medical tomography
physical sensors
connection
curvature
holonomy
Berry structure
quantum mechanism
Proto-Loom
autonomous experiment authority
production authority
release authority
```

Wedding remains a bounded finite synthetic identifiability family.
Moss Lantern remains a bounded finite synthetic route-memory phantom.
A15-R0 remains research.
#686 remains merged lineage rather than active authority.
Installed Aperture replay status remains held.

---

## 17. Authority membrane

```text
source_status = SIMULATED
authority_class = A2_DERIVATIONAL
manifestly_fictional = true

C0_verdict = STRICT_TYPED_NON_AMPLIFICATION_FALSIFIED_BY_LAWFUL_DERIVATIONAL_GAIN
C1_verdict = DECLARED_DERIVATIONAL_CLOSURE_CANDIDATE_SURVIVES_BOUNDED_HOSTILE_FAMILY
C1_status = ATTACK_ONLY_NOT_PROMOTED
presumption_of_survival = false

scalar_aggregation_used = false
intersection_program_opened = false
H2_tested = false
H3_tested = false
pedagogue_engine_mutation = false
product_mutation = false
workflow_mutation = false
browser_execution = false
deployment_authority = false
release_authority = false
promotion_authority = false
human_closure_required = true
```

𝌋 Non-amplification lost its passport at Wedding. The thing that made it through customs was narrower and colder: not a ceiling on what inference may earn, but a custody record for how a warrant became earnable at all. ⟐
