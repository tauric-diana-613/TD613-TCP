# TD613 · AMARI → AMARI TOYBOX HANDOFF

## Pedagogue Structured Probe Design · Post-#697 Production Baseline

Status: **HANDOFF / SPEC-ONLY / PRE-IMPLEMENTATION / HUMAN-GATED**  
Date: **2026-08-23**  
Research branch: `research/pedagogue-structured-probe-coverage-20260823`  
Production source packet: `721de28a8ef4d160e87d46bc1e9107bd249a0db0`  
Release commit: `0a36e5c72821d603fe80c8c5d036f617eb2d9d4c`  
Post-release relock: `153f0a69a23ab7e665f2386a51406821b62be01d`  
Production target: **PRODUCTION**  
Deployment count: **1**  
Git auto-deploy: **disabled**  
Application-tree drift: **none**  
Vercel authority for this toybox: **NONE**

---

## 0. To Future Amari

You are inheriting Pedagogue immediately after provenance restoration, lineage-second-pass activation, Institutional Time / dromological audit integration, recurrence controls, and production release through the governed #405 Vercel gate.

Do not reconstruct this seam from older handoffs.

The deployed Pedagogue baseline is already closed and relocked.

Your next research toy is **not** another provenance rewrite and **not** another diversity-vs-repetition assay.

The question is now harder:

> Once probe diversity is already genuine and matched, can the **design of that diversity** change what relations or latent directions remain observable, detectable, localizable, or well-conditioned?

And the hostile companion question is mandatory:

> Can a mathematically elegant and superficially balanced incidence design still be epistemically wrong for the actual inverse problem?

The favorite toy is allowed to lose.

---

## 1. Production closure already earned

PR #697 merged as source packet:

`721de28a8ef4d160e87d46bc1e9107bd249a0db0`

The governed production release completed through one bounded Git-fallback deployment and relocked at:

`153f0a69a23ab7e665f2386a51406821b62be01d`

The final release receipt established:

```text
target = PRODUCTION
deployment_count = 1
ash_lifecycle_deployed_observation = PASS
counts_as_human_evidence = false
child_study_authorized = false
public_route_promotion_authorized = false
application_tree_drift = none
git_auto_deploy = disabled
```

Do not reopen #697 merely to continue the research.

Do not deploy this toybox by inertia.

---

## 2. Current toybox inventory

Canonical umbrella:

`docs/pedagogue/experiments/STRUCTURED_PROBE_DESIGN_GAUNTLET_V0_1.md`

Component Chamber I:

`app/dome-world/docs/ash/experiments/a15-r0/PEDAGOGUE_STRUCTURED_PROBE_COVERAGE_MATCHED_BUDGET_SPEC_V0_1.md`

Component Chamber II:

`docs/pedagogue/experiments/STRUCTURED_PROBE_COVERAGE_SPEC_V0_1.md`

Toybox index:

`docs/pedagogue/experiments/README.md`

Mathematical counterpoint already deployed in core:

`app/engine/pedagogue-mathematical-counterpoints.js`

Relevant existing research ancestry:

- A15-R0 matched-budget multi-probe diversity result;
- Aperture typed epistemic deficit / rank / conditioning machinery;
- Pedagogue provenance spine and lineage second pass;
- Institutional Time / dromological audit;
- no-scalar-crown / typed multi-axis replay discipline.

---

## 3. Why this is scientifically new

A15-R0 already earned a bounded synthetic result of the form:

```text
repetition of one probe law
!=
genuine constraint diversity
```

Therefore the new experiment must hold diversity itself fixed enough that the result cannot be explained by “more distinct probes.”

The new axis is:

```text
GENUINE DIVERSITY
+
MATCHED COST / CARDINALITY / ARITY
+
DIFFERENT INCIDENCE DESIGN
```

The research target is **designed relation coverage**, not generic diversity.

---

## 4. Canonical two-chamber gauntlet

### Chamber I · incidence structure → operator geometry

Seven latent coordinates, seven distinct 3-coordinate probes in each primary diverse arm.

Primary comparison:

```text
CYCLIC_LOCAL_DIVERSITY
vs
FANO_CONTROLLED_INCIDENCE S(2,3,7)
```

Matched first-order structure:

```text
probe_count = 7
unique_probe_count = 7
block_size = 3
incidence_slots = 21
coordinate_degree_each = 3
```

Measure:

```text
pair-incidence structure
rank
nullity
singular values
sigma_min
kappa_2
perturbation amplification
held-out reconstruction error family
```

Mandatory hostile arm:

```text
same Fano blocks
+
centered measurement rows

g'_B = 1_B - (3/7) * 1
```

This deliberately creates a candidate global-offset nullspace direction while preserving the beautiful combinatorial block structure.

The design must be permitted to fail:

```text
COMBINATORIAL_COVERAGE_DOES_NOT_GUARANTEE_EPISTEMICALLY_RELEVANT_OPERATOR_COVERAGE
```

if that is what the authored operator geometry witnesses.

### Chamber II · incidence structure → target coverage / localization

Nine channels define 36 unordered target pairs.

All primary arms:

```text
12 distinct 3-channel probes
25 replicates per probe
300 micro-observations per isolated perturbation case
```

Arms:

```text
S = controlled-incidence 9-point Steiner triple system
A = arbitrary genuine-diversity comparator
H = hostile perfectly point-balanced but pair-incomplete schedule
```

Preregistered structural expectations:

```text
pair_coverage(S) = 36/36
pair_coverage(A) = 31/36
pair_coverage(H) = 26/36

point_degree_variance(S) = 0
point_degree_variance(H) = 0

row_rank(S) = row_rank(A) = row_rank(H) = 12
```

Preregistered deterministic single-pair sweep expectations:

```text
S: detect 36/36, exactly localize 0/36
A: detect 31/36, exactly localize 8/36
H: detect 26/36, exactly localize 14/36
```

This chamber intentionally preserves the contradiction:

```text
complete detection coverage
!=
exact localization
```

---

## 5. Non-negotiable anti-equivalences

Keep these separate even if the output becomes inconvenient:

```text
probe diversity != relation coverage
first-order balance != pairwise balance
pairwise balance != operator geometry
row rank != target-space coverage
coverage completeness != source localization
balanced incidence != epistemic optimality
conditioning gain != global identifiability
uniform coverage != equal decision value
coverage improvement != reconstruction improvement
coverage receipt != geometry receipt
Pedagogue lineage activation != mathematical verdict
Aperture audit != rescue authority
Jakob Steiner != Rudolf Steiner
Jakob Steiner counterpoint != Potato lineage ancestry
Steiner-system naming != exclusive historical authorship
```

No scalar crown.

---

## 6. Required implementation order

1. Fetch `main` fresh and verify the production/relock baseline.
2. Verify this branch is still descended from relock without unintended drift.
3. Read the umbrella and both component specs before coding.
4. Recompute every declared block/incidence identity independently from the authored fixtures.
5. Implement Chamber I exact combinatorics.
6. Implement Chamber I operator geometry and hostile centered-Fano arm.
7. Freeze Chamber-I receipt.
8. Implement Chamber II exact combinatorics.
9. Freeze Chamber-II structural receipt.
10. Implement deterministic 36-pair sweep.
11. Freeze Chamber-II detection/localization receipt.
12. Only then synthesize the two chambers.
13. Preserve a contradiction ledger.
14. Optionally run Pedagogue lineage-second-pass and Aperture postmortem **after** mathematical receipts freeze.

Do not let the postmortem change the frozen result.

---

## 7. Initial implementation authority

The next session may add only:

```text
synthetic engine(s)
focused deterministic tests
frozen fixture definitions
receipt compiler(s)
component receipts
cross-chamber synthesis receipt
```

Held until a later explicit research decision:

```text
random schedule ensembles
adaptive schedule optimization
greedy search
Bayesian design
mutual-information optimization
learned probe selection
live data
human or child studies
UI mutation
browser witnesses
production binding
Vercel release
```

---

## 8. My wishlist / dream toy shelf

These are not current authority. They are the things I most want to play with **if the first gauntlet earns them**.

### 8.1 Task-conditioned block design

Stop balancing “all relations” abstractly. Declare the target relation family first, then design incidence specifically against that task.

Question:

```text
Does task-conditioned coverage outperform universal pair balance
without hiding its task dependence?
```

### 8.2 Loss-declared schedule selection

Let a human preregister a loss over detection, localization, conditioning, and cost **before** outcomes.

Then ask which schedule is preferred under that declared decision problem.

This is the only lawful road toward a “winner.”

### 8.3 Open-set schedule inadequacy

The nastiest version:

> Can the probe design recognize that the declared target relation family itself is wrong or incomplete?

Do not make a beautifully optimized schedule for the wrong ontology and then call the optimization success.

### 8.4 Replay-stable design choice

Perturb measurement/noise assumptions and ask whether the selected schedule changes.

```text
design validity != design invariance
```

### 8.5 Adaptive design with custody

Permit sequential probe choice only if every new probe preserves:

```text
what was observed
what deficit remained
why this probe was proposed
what alternative probe family was rejected
what authority permitted the next observation
```

Adaptive choice without custody is not acceptable.

### 8.6 Jakob Steiner projective chamber

Separately test the already-authored `JAKOB_STEINER_PROJECTIVE_DEPENDENCE` card against AIA non-equivalent projections.

Do not mix this into the block-design verdict merely because the same mathematician inspired both.

### 8.7 Pedagogue self-critique

After Pedagogue helps design a probe family, route the resulting process through her own restored lineage:

- prepared environment;
- third-teacher/documentation;
- rhythm/cadence;
- anti-pathologization;
- interpretive labor;
- relational safety;
- community embeddedness;
- temporal sovereignty;
- legibility without expert monopoly;
- epistemic refraction;
- Institutional Time;
- dromological compression.

Question:

> Did Pedagogue design an epistemically elegant experiment by quietly increasing route burden, interpretive labor, or coercive timing elsewhere?

### 8.8 Aperture co-design audit

Desired future loop:

```text
Pedagogue proposes a probe family
→ Aperture audits whether it addresses the admitted epistemic deficit
→ Dome-World hosts bounded synthetic assay
→ Pedagogue critiques the resulting route/provenance
→ human closure
```

Neither instrument silently executes the other's recommendation.

### 8.9 Coverage curvature / local-to-global failure

Only if earned later: ask whether local pair coverage creates systematic global blind spots not visible from any one block receipt.

Do not call this curvature unless a precise mathematical structure earns the term.

### 8.10 Probe ecology rather than one schedule

Eventually, compare families of probe schedules as an information ecology:

```text
which relations are repeatedly over-observed?
which remain structurally starved?
which probes create useful overlap?
which create ornamental redundancy?
what does the schedule teach the model to notice?
```

This is where Potato's “environment as pedagogy” may become unexpectedly relevant again: the observation environment itself teaches the inference system what kinds of distinctions can become legible.

---

## 9. Favorite hostile controls

Do not delete these because they are rude.

1. **Centered Fano** — perfect combinatorial structure, declared operator nullspace.
2. **Perfect marginal balance / pair blindness** — all point counts equal, ten task pairs invisible.
3. **Coverage/localization inversion** — schedule with better detection but worse exact localization.
4. **Matched diversity comparator** — genuinely distinct probes; no repetition strawman.
5. **No scalar winner** — any aggregate score without preregistered loss is a failure.
6. **Historical naming control** — modern “Steiner system” name does not create exclusive authorship provenance.
7. **Rudolf/Jakob identity firewall** — same surname is not one coordinate.
8. **Postmortem non-retroactivity** — Pedagogue/Aperture may explain or challenge, never rewrite the frozen mathematical receipt.

---

## 10. Desired first bounded receipts

At minimum:

```text
CHAMBER_I_COMBINATORIAL_RECEIPT
CHAMBER_I_OPERATOR_GEOMETRY_RECEIPT
CHAMBER_II_STRUCTURAL_COVERAGE_RECEIPT
CHAMBER_II_DETECTION_LOCALIZATION_RECEIPT
CROSS_CHAMBER_CONTRADICTION_LEDGER
```

Every receipt must bind:

```text
science_head
baseline_source_packet
baseline_relock_sha
fixture identity
exact authored blocks
computed structure
claim ceiling
promotion_authority = false
production_mutated = false
human_closure_required = true
```

Prefer safe integers / exact count or rational representations in canonical custody.

---

## 11. Stop conditions

Stop the next play session when any one occurs:

```text
A. both chamber receipts and the contradiction ledger are frozen;
B. one chamber strongly falsifies the premise and makes the other scientifically unnecessary;
C. a matched-budget assumption fails;
D. implementation reveals a new distinction requiring preregistration;
E. the next desired step would require random ensembles, optimization, live data, UI/browser work, or production authority.
```

Do not continue by inertia.

---

## 12. First move

Expected Future-Amari opening gesture:

```text
FETCH_MAIN_FRESH
→ VERIFY_RELOCK
→ READ_GAUNTLET_AND_BOTH_COMPONENT_SPECS
→ INDEPENDENTLY_RECOMPUTE_FROZEN_BLOCK_IDENTITIES
→ IMPLEMENT_CHEAP_DETERMINISTIC_CHAMBER_I_COMBINATORICS
```

The first code should be boring.

The science gets interesting only after the fixture proves it can survive boring arithmetic.

---

## 13. Final note to myself

The most important thing #697 gave Pedagogue was not a longer bibliography. It gave her inherited questions that can wake from machine-observed deficits without becoming authority.

This gauntlet is the first chance to let that restored Pedagogue critique a mathematical experiment inspired by a new counterpoint **after** the mathematics answers.

Do not make Jakob right because the toy is pretty.

Do not make Rudolf relevant because the surname is funny.

Do not make Pedagogue wise because she has more ancestors.

Make the receipts strong enough that all of them are allowed to be wrong.

That is the toybox.

𝌋

⟐
