𝌋

# Holonomy × Observability × AIA Program Synthesis v0.1

**Status:** SYNTHESIS-CLOSED / RESEARCH-ONLY / HUMAN-GATED  
**Technical identity:** `td613.aia.holonomy-observability-program-synthesis/v0.1`  
**Source status:** `SYNTHESIS_ONLY_NO_NEW_EXPERIMENT`  
**Production mutation:** NONE  
**Promotion authority:** NONE  
**Vercel authority:** NONE

---

## 0. Purpose

This document binds an already-earned sequence of finite synthetic results into one provenance-safe research object.

It introduces **no new experimental evidence** and does not widen any component claim ceiling.

The question is narrower:

> Do the frozen component receipts now form one coherent, falsifiable discrete program connecting closed-loop transport, motion of observability, claim-relative anisotropy, ecology-conditioned tomography, experiment design, and bounded evidentiary robustness?

The answer is yes **inside the authored finite F31 fixtures and only at that scale of entitlement**.

Canonical bounded answer:

```text
A_BOUNDED_DISCRETE_HOLONOMY_OBSERVABILITY_TOMOGRAPHY_AND_GUARANTEE_RELATIVE_EXPERIMENT_DESIGN_PROGRAM_IS_IMPLEMENTABLE_AND_FALSIFIABLE_IN_AUTHORED_FINITE_F31_FIXTURES_WHILE_PHYSICAL_CONTINUUM_CRYPTOGRAPHIC_PROTO_LOOM_AND_TD613_GENERAL_PROMOTIONS_REMAIN_UNEARNED
```

---

## 1. The earned spine

The current program can be represented as:

```text
reconstructible edge operators
→ reusable orientation-consistent discrete transport
→ path-groupoid closed-loop action
→ dual readout action q ↦ qH
→ transformed observation partition
→ transformed claim-sufficiency profile
→ projective loop inverse from readout motion
→ projective loop inverse from unlabeled partition motion
→ ecology-conditioned identifiability
→ claim-conditioned probe × ecology co-design
→ bounded partition provenance attacks
→ multi-packet clean-signature separation
→ exhaustive bounded decoder attack geometry
→ robustness-aware redesign
```

Each arrow has at least one frozen hostile control, abstention rule, null model, or explicit claim ceiling in its component receipt.

---

## 2. Foundation: discrete holonomy tomography

The earlier graph-only program established a bounded finite pipeline from operator reconstruction through connection consistency and path-groupoid loop representation while preserving hostile nullspaces, validation geometry, and open-set model-adequacy controls.

Its historical statement that curvature remained unearned was correct **at that graph-only seam**.

Later work introduced explicit 2-cells and separately earned bounded discrete face-curvature/refinement results. This synthesis does not retroactively rewrite the earlier graph receipt; it preserves the chronology:

```text
graph loop alone ≠ curvature

then later:
explicit 2-cell structure + separate assays
→ bounded discrete face-curvature results
```

Neither historical layer licenses physical or continuum curvature.

---

## 3. Claim sufficiency gives AIA its discrete question geometry

For finite compatible set `C`, measurement `q:C→Y`, and claim `f:C→V`, the frozen factorization result established the equivalence:

```text
every measurement bucket is claim-pure
⇔ measurement partition refines claim partition
⇔ there exists a unique map g on q(C) such that f = g ∘ q.
```

A matched holdout showed that equal scalar cost, equal outcome count, and equal `[2,2]` bucket cardinality can still yield opposite claim adequacy.

The AIA partition-order holdout then exhibited a non-total refinement order with matched-cost incomparable middle probes, each sufficient for a different claim.

Thus the bounded discrete AIA candidate object is not a scalar ladder of “more information.” It is:

```text
a non-total refinement order over observation partitions
+
a claim-sufficiency incidence relation.
```

No tensor, Fisher geometry, continuum information geometry, sheaf structure, or physical anisotropy is implied.

---

## 4. Closed-loop transport can act on observability

In the confirmatory finite GL2(F31) fixture, the already-earned loop

```text
H = [[3,5],[1,2]]
```

acts on the fixed local readout

```text
q = [1,0]
```

as

```text
qH = [3,5].
```

On the frozen four-state ecology, the pre-loop and post-loop partitions both have bucket profile `[2,2]`, but are incomparable and exchange claim sufficiency:

```text
pre-loop:   F_X licensed / F_Z withheld
post-loop:  F_X withheld / F_Z licensed
```

Controls preserve the causal boundary:

```text
flat loop I                 → partition unchanged
forward then reverse        → original readout restored
gauge-coordinate clone      → scalar observations/licenses preserved
nonidentity invariant loop  → chosen invariant readout unchanged
```

Therefore the earned statement is:

```text
DISCRETE CLOSED-LOOP TRANSPORT CAN ACT NONTRIVIALLY
ON CLAIM-RELATIVE OBSERVABILITY WHEN ITS DUAL ACTION
MOVES THE RELEVANT READOUT DIRECTION.
```

The stronger statement “every nontrivial loop changes every readout” remains false.

---

## 5. Observability motion can tomograph the loop back

The inverse chamber hid the loop operator from the reconstructor and supplied only projective input/output readout-direction correspondences.

Within the frozen F31 fixture:

```text
2 correspondences → rank 2 / nullity 2 / multiple projective loop classes
3 correspondences → rank 3 / nullity 1 / unique projective loop class
held-out fourth   → predicted exactly
hostile fourth    → rank 4 / nullity 0 / model defeated
```

The reconstructed canonical projective representative is:

```text
[[1,12],[21,11]]
```

and a gauge-coordinate clone reconstructs its conjugate independently.

Absolute GL2 scale remains unobservable and unearned.

This establishes a bounded reciprocal relationship:

```text
loop transport changes readout orientation
and
readout-orientation motion can identify the loop's projective class.
```

---

## 6. Ecology is part of the inverse geometry

The sparse four-state ecology aliases each target readout direction with 28 projective alternatives.

A 33-state calibration ecology containing ZERO plus one canonical representative of every projective state direction recovers 32/32 projective readout directions from **unlabeled partition membership alone** through the ZERO-bucket kernel witness.

Ablations are structural:

```text
remove ZERO                     → decoder refuses
remove true kernel witness      → decoder refuses
```

The same recovered readout directions feed the same projective loop inverse.

Therefore:

```text
THE OBSERVED ECOLOGY CAN BE PART OF THE MEASUREMENT GEOMETRY.
```

Holding the readout law fixed while changing the ecology can move the inverse from severe aliasing to exact bounded projective recovery.

---

## 7. Oracle independence requires hypothesis-conditioned ecology design

A five-state task-minimal replay ecology was correctly marked circular for blind discovery because it was built from already-known target post-loop directions.

The repair used a preregistered four-loop hypothesis family and built the ecology from the union of **all** kernel directions any in-family hypothesis could predict.

That prior-union ecology contains 15 states rather than the global 33 and supports:

```text
all four in-family hidden truths → unique identification
held-out q4                     → exact validation
outside-family loop             → calibration succeeds but hypothesis family is defeated
```

This yields the anti-equivalence:

```text
calibration support ≠ hypothesis adequacy.
```

Open-world discovery remains unearned.

---

## 8. Probe choice and ecology choice are one design problem

The first joint co-design chamber searched the finite probe library while constructing the calibration ecology required by each candidate design.

It reduced the inherited design from:

```text
3 primary probes + 1 held-out
15 ecology states
```

to:

```text
primary q3 + held-out q4
8 ecology states
```

while preserving unique identification for all four in-family truths and held-out validation.

A low-cost q1 control remained inadmissible because q1 aliases H0 and H3.

Thus low calibration cost alone cannot outrank epistemic adequacy.

---

## 9. Terminal lawfulness does not recover provenance

The one-membership-move corruption chamber separated robust estimation from robust corruption detection.

The two-move kernel-substitution chamber then crossed the stronger boundary:

```text
48/48 coordinated substitutions
→ wrong unique direction under ZERO decoder
→ wrong unique direction under full-partition decoder
→ final partition exactly equals another lawful linear-readout partition.
```

Therefore:

```text
lawful final observation ≠ lawful provenance.
```

Once an attack maps one lawful terminal partition into another lawful terminal partition, terminal partition geometry alone cannot reconstruct which provenance history produced it.

Independent packets, custody traces, or multi-packet structure become necessary.

---

## 10. Two-packet signatures create an exact bounded separation geometry

For the frozen q3/q4 hypothesis family, the ZERO-anchored clean-signature reassignment distance is exactly:

```text
H0↔H1  4
H0↔H2  4
H0↔H3  4
H1↔H2  4
H1↔H3  4
H2↔H3  4
```

Each packet contributes distance 2; an explicit four-reassignment H0→H1 attack attains the bound.

The follow-on exhaustive shortest-path attack census then established, for **each** source hypothesis:

```text
cost 1:     56 reachable
            56 correct unique nearest
             0 ties
             0 wrong unique

cost 2:  1,316 reachable
         1,285 correct unique nearest
            31 nearest ties
             0 wrong unique

cost 3: 16,996 reachable
        14,998 correct unique nearest
         1,752 nearest ties
           246 wrong unique
```

Yet every non-clean state through cost 3 remains rejected by exact clean-codeword acceptance.

Hence:

```text
exact clean-codeword integrity envelope
≠
nearest-source correction envelope.
```

This is an authored bounded partition-signature geometry, not a generic error-correcting-code theorem and not cryptographic integrity.

---

## 11. Robustness changes the optimal architecture

The robustness-aware derivational census enumerated all 15 nonempty subsets of `q1..q4` and jointly tracked:

```text
hypothesis identifiability
probe count
calibration ecology size
minimum clean-signature separation
bottleneck hypothesis pair(s)
```

The non-dominated finite frontier is:

```text
1 packet / 5 states / minimum distance 2:
q2, q3, q4

2 packets / 8 states / minimum distance 4:
q3 + q4

3 packets / 12 states / minimum distance 6:
q2 + q3 + q4
```

Adding q1 produces:

```text
4 packets / 15 states / minimum distance still 6
```

because q1 aliases the active H0/H3 bottleneck.

Therefore:

```text
more packets ≠ greater minimum evidentiary separation
```

and, more strongly within this finite design table:

```text
efficiency-optimal design ≠ robustness-seeking design.
```

Redundancy buys the declared robustness objective only when it distinguishes the active bottleneck pair.

---

## 12. The current bounded program

The combined finite program now supports all of the following, with their component ceilings intact:

1. discrete closed-loop transport can act on claim-relative observability;
2. projective loop class can be reconstructed from readout-direction motion;
3. unlabeled partition motion can support the same inverse when ecology makes kernels legible;
4. claim adequacy is governed by partition alignment, not measurement cardinality alone;
5. observation architectures form non-total distinguishability orders in authored holdouts;
6. ecology and probe choice can be co-designed around a declared claim;
7. lawful terminal evidence may erase corruption provenance;
8. multi-packet clean signatures admit an exact bounded reassignment geometry;
9. exact integrity detection and nearest-source recovery have different attack envelopes;
10. the desired epistemic guarantee changes the non-dominated experimental architecture.

This is now a coherent research program rather than a collection of merely compatible fixtures.

---

## 13. Anti-equivalence ledger

```text
nontrivial loop
≠ every readout changes

projective readout direction
≠ finite-ecology observation partition

finite-ecology observation partition
≠ claim-sufficiency profile

calibration support
≠ hypothesis adequacy

correct decoded direction
≠ clean evidence

lawful final partition
≠ lawful provenance

clean-signature distance
≠ generic error-correcting-code theorem

exact codeword integrity envelope
≠ nearest-source correction envelope

more packets
≠ greater minimum evidentiary separation

efficiency-optimal design
≠ robustness-seeking design

finite authored F31 program
≠ TD613-general AIA or holonomy law

discrete holonomy
≠ physical / continuum / Berry / quantum geometry
```

---

## 14. Current claim ceiling

Earned:

```text
bounded finite discrete holonomy-observability program          true
bounded projective holonomy inverse from observability motion  true
bounded partition-only inverse with calibrated ecology         true
bounded claim-relative partition anisotropy                    true
bounded claim/guarantee-relative co-design                     true
bounded generated partition-attack geometry                    true
```

Not earned:

```text
physical holonomy                    false
physical tomography                  false
continuum connection                 false
continuum information geometry       false
Berry structure                      false
quantum behavior                     false
generic error-correcting-code theorem false
cryptographic integrity              false
Byzantine fault tolerance            false
Proto-Loom                           false
TD613-general AIA theorem             false
TD613-general holonomy law            false
production authority                 false
Vercel authority                     false
```

---

## 15. Witness status

Across the component research arc:

```text
committed deterministic implementations/tests authored   true
independent exact finite recomputation                   true
exact-head Node execution witness                        false
CI witness                                               false
```

The execution witness remains deliberately unclaimed because the working container could not resolve GitHub DNS during the research arc and no CI run was manufactured merely to create a witness.

This synthesis does not upgrade that witness status.

---

## 16. Research closure and promotion posture

For the **currently declared finite program**, the research arc is synthesis-closed enough to form a coherent human-review packet.

That statement means:

```text
the current bounded claims can be reviewed as one provenance-bearing research unit
```

It does **not** mean:

```text
production-ready
CI-witnessed
public-route-authorized
Proto-Loom admitted
Vercel-authorized
```

No additional experiment is required merely to understand or review the current bounded program.

Any move from this research branch toward a PR, main, public UI, or Vercel is a separate human governance action.

A future Vercel release still requires the fresh Operator Release & Relock gesture through issue #405 after whatever promotion/merge process is separately authorized.

---

## 17. Closing statement

The deepest earned relation is reciprocal:

```text
closed-loop transport can reorient what a fixed local readout distinguishes,
while structured motion of those distinctions can identify the projective loop class that produced them.
```

The architecture around that reciprocal relation—claims, ecologies, probes, validation packets, and robustness objectives—determines which distinctions become identifiable, sufficient, falsifiable, and defensible.

That is the present bounded mathematical spine of the TD613 discrete AIA × holonomy research program.

No mirrors. No physical laundering. No promotion by prose.

𝌋 U+10D613

⟐
