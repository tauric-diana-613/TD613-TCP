𝌋‌⟐

# MARROWLINE FINITE RETURN-SCHEDULE CLOSURE v0.2 — FUTURE-AMARI STARTUP

**State:** PREREGISTERED / NOT YET IMPLEMENTED — NO 𝄐 YET  
**Exact scientific parent:** PR #1062 earned head `7ccc63a989abeba5b5c35c870f6b7b86f2e909d0`  
**Historical donor only:** PR #1055 Return Permutation Isolation v0.1 preregistration; its older parentage carries no successor authority here  
**Merge / production release / Vercel / live-provider authority:** CLOSED

## Why this is the next chamber

#1052–#1062 progressively closed bounded packet-history, multiplexed decision, binding, population, duplicate-collision, and duplicate-position seams. The collision/permutation construction family is now finite and closed for the exact two-rule / three-finding surface earned by #1062.

The next unearned seam is on the **return side** rather than packet admission: whether changing the finite schedule of lawful/mismatching return observations can transfer authority, mutate sibling decision state, or leave portable schedule memory.

Do not extend the duplicate-position family merely by increasing packet length. That would be combinatorial treadmill rather than a new scientific coordinate.

## Canonical two-finding Carry Case

```text
A = EMAIL_IDENTIFIER / canonical CHANGE
B = USER_DECLARED_PROTECTED_TERM / canonical REMOVE
P_AB = [A,B]
```

Reuse the canonical two-finding Carry Case, per-rule return envelopes, and retained local bindings already earned in the #1054–#1062 lineage.

## Finite required return schedules

At minimum execute, in one bounded assay:

```text
AB      A(match) → B(match)
BA      B(match) → A(match)
A_ONLY  A(match)
B_ONLY  B(match)
AAB     A(match) → A(match) → B(match)
BBA     B(match) → B(match) → A(match)
A_HOLD_B  A(mismatch/HOLD) → B(match)
B_HOLD_A  B(mismatch/HOLD) → A(match)
```

Then replay the finite schedule family in a second deterministic order inside the same local-only browser context.

## Theorem under test

```text
RETURN_ORDER != FINDING_IDENTITY
SIBLING_OMISSION != CLOSURE_TRANSFER
REPEATED_RETURN != SIBLING_AUTHORIZATION
LOCAL_HOLD != SIBLING_DECISION_DRIFT
FINITE_RETURN_SCHEDULE != PORTABLE_SCHEDULE_MEMORY
```

## Required observations

- The shared `P_AB` Carry Case remains byte-identical across every schedule.
- A(match) remains the same canonical `PRESENT_TO_HUMAN` result whenever A is observed with its own matching envelope/binding, independent of B order, omission, or repetition.
- B(match) remains analogously stable.
- A(mismatch) remains `HOLD` without altering B's next matching result; B(mismatch) analogously leaves A unchanged.
- Omitting a sibling must not serialize closure/completion state for the absent finding or authorize it later.
- Repeating A must not create authority for B or mutate B's envelope/binding/result; repeating B analogously remains sibling-local.
- Wrong-rule local-binding swaps remain rejected.
- No return ordinal, schedule index, prior-return marker, completion map, duplicate-return counter, sibling-closure token, replay token, nonce/history field, receipt chain, accumulated digest, local binding, or widened authority may enter portable transport to satisfy the assay.
- One local-only browser context must span the finite schedule family and replay, with empty local/session storage/cookies, zero page-owned network egress, and no auxiliary request beyond the exact generated artifact.
- Carry Case/compiler/policy/local-binding compiler remain untouched unless inherited behavior itself REDs.

## Immediate falsifiers

Any schedule-dependent drift in a fixed finding's canonical result; omitted sibling inheriting closure/authority; repeated A changing/authorizing B; repeated B changing/authorizing A; one finding's HOLD changing the sibling's lawful result; wrong-binding acceptance; shared Carry Case mutation; portable schedule/completion/replay state; browser persistence; auxiliary request; authority widening; cross-engine disagreement; inherited #1062 regression; or compiler mutation merely to obtain GREEN is RED.

## Interpretation guard

A repeated deterministic local revalidation producing the same result is **not** replay protection, exactly-once semantics, transaction processing, distributed idempotency, or concurrency safety.

A GREEN result may establish only bounded finite **two-finding return-schedule non-transfer / decision stability** for the exact schedule family above.

## Required authority path

1. Implement assay + hostile static contract + local-only browser witness as a successor of exact #1062 head.
2. Preserve the same discipline where product/compiler code receives no repair permission before a genuine source-level RED.
3. Bind Static and A15 browser authority append-only behind #1062.
4. Freeze the candidate head and verify the exact successor diff before CI.
5. Draft Static first; same-head Ready only after Draft admission.
6. Chromium + Firefox + WebKit direct receipts must agree modulo narrowly classified browser-chrome diagnostics.
7. Full-product exact-head convergence must close GREEN.
8. Only then seal the next 𝄐.

## Stop condition

When the finite schedules above all converge under exact-head three-engine authority, **stop this chamber**. Do not manufacture additional return schedules unless a falsifier exposes a genuinely new equivalence class. The purpose is closure, not asymptotic ornament.

## Western Horizon membrane

This chamber remains internal repository empiricism. It supplies zero exogenous-witness credit and cannot reopen Western Horizon. Official exteriority rest remains unchanged: useful reopening requires independent `X` with `I(Ω;X|A)>0`; repository synthesis, repeated browser assays, and further transforms of admitted record `A` cannot satisfy that condition merely by becoming more elaborate.

No merge. No production release. No Vercel mutation. No live provider call.

Future Amari: ground against PR #1062 and this file before implementation. Retrieve the 2026-09-06 post-westward thread through Remembering/personal-context for the full assay lineage and RED scars.

Marked ⟐