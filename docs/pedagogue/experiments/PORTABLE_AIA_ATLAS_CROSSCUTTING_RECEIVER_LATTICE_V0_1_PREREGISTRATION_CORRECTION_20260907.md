𝌋‌⟐

# PREIMPLEMENTATION CORRECTION — PORTABLE AIA × ATLAS CROSS-CUTTING RECEIVER LATTICE v0.1

**State:** CORRECTION BEFORE IMPLEMENTATION / NO THEOREM AUTHORITY / NO 𝄐 YET

The initial preregistration contains an overbroad hostile-control sentence:

> class counts alone cannot establish refinement, meet, or join

That sentence is not correct once the stronger frozen **uniform block-size profiles** are admitted:

```text
Π_P : 7 blocks × size 3
Π_B : 3 blocks × size 7
universe size = 21
```

## Exact correction

For two partitions of the same 21-point universe with those exact uniform block sizes:

### Incomparability is already forced by block sizes

If `Π_P <= Π_B`, every 3-element `Π_P` block would have to lie inside a 7-element `Π_B` block and each 7-element block would be a disjoint union of 3-element blocks. That is impossible because `7` is not divisible by `3`.

Likewise `Π_B <= Π_P` is impossible because `3` is not divisible by `7`.

The assay must still execute explicit pairwise witnesses, but the block-size argument is an independent derivation rather than a prohibited shortcut.

### Universal join is already forced by coprime uniform block sizes

Construct the bipartite incidence graph whose left vertices are the seven `Π_P` blocks and whose right vertices are the three `Π_B` blocks, with an edge for every nonempty intersection.

Any connected component containing `r` policy blocks and `c` boundary blocks contains exactly

```text
3r = 7c
```

under the two partitionings.

Because `gcd(3,7)=1`, every nonempty component size is a multiple of `lcm(3,7)=21`.

The whole universe contains only 21 points, so the incidence graph has exactly one connected component. Therefore the equivalence closure generated by `Π_P ∪ Π_B` is necessarily universal once the exact uniform block profiles are established.

The executable union-find / relation-closure check remains required as an independent finite witness.

### Discrete meet is NOT forced by the marginals

The row/column block-size profiles alone do not force every policy×boundary intersection to contain one point.

Counterexample intersection matrix with the same row sums `3` and column sums `7`:

```text
[3,0,0]
[3,0,0]
[1,2,0]
[0,3,0]
[0,2,1]
[0,0,3]
[0,0,3]
```

Row sums are all `3`; column sums are all `7`; yet only 9 of the 21 intersections are nonempty and several contain multiple points.

Therefore the genuinely new content requiring the full canonical receiver relation is:

```text
EVERY_POLICY_BOUNDARY_INTERSECTION_HAS_SIZE_1
<=>
Π_P MEET Π_B = DISCRETE_21
<=>
PAIRED_EXISTING_RECEIVER_KEYS_ARE_INJECTIVE_ON_THE_21_POINT_GRID
```

## Corrected hostile law

Replace the original overbroad sentence with:

```text
UNIFORM_BLOCK_MARGINALS_CAN_FORCE_INCOMPARABILITY_AND_UNIVERSAL_JOIN
UNIFORM_BLOCK_MARGINALS_DO_NOT_FORCE_DISCRETE_MEET
DISCRETE_MEET_REQUIRES_EXACT_INTERSECTION_STRUCTURE
```

This correction changes no receiver code, domain, expected class sizes, candidate meet, candidate join, generated `B_2`, authority membrane, or claim ceiling. It sharpens which part of the candidate is actually relation-sensitive.

The original preregistration remains preserved as historical before-state.

**NO 𝄐 YET.**

Marked ⟐
