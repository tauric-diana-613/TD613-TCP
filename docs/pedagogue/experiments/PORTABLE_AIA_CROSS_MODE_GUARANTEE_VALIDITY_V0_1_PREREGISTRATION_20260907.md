𝌋‌⟐

# PORTABLE AIA CROSS-MODE GUARANTEE VALIDITY v0.1 — PREREGISTRATION

**State:** PREREGISTERED / UNIMPLEMENTED / NO 𝄐 YET  
**Exact scientific parent:** PR #1073 earned head `25040f740c1c0c57a082554398fd6ef6dc048f48`  
**Issue donor:** #1038 · Holonomy Loom portable AIA use-mode and provider-boundary matrix  
**Merge / production release / Vercel / live-provider authority:** CLOSED

## Why this chamber exists

The repository has separately earned:

- three-route governance invariance;
- born-minimized Portable-AIA payloads;
- Local Pocket pre-ingress behavior;
- Hosted / Marrowline advisory behavior;
- ChatGPT-thread-companion post-ingress claim ceilings;
- Atlas policy-only and boundary-aware receiver structure.

Those results do not yet answer #1038's remaining finite question:

> Which guarantee statements survive unchanged across all three modes, and which must be restated because their truth depends on the host boundary?

This chamber does **not** add a fourth route, a new production receiver, or a stronger privacy promise. It classifies one frozen finite claim family against the already-existing three route projections.

```text
PORTABLE_GOVERNANCE_INVARIANCE != PORTABLE_GUARANTEE_TEXT
COMMON_POLICY_CORE != COMMON_TRUST_BOUNDARY
HOST_BOUND_CLAIM != UNIVERSAL_CLAIM
CLAIM_SUPPORT != AUTHORITY_TRANSFER
```

## Exact frozen domain

Route modes:

```text
TD613_HOSTED
LOCAL_POCKET
CHATGPT_THREAD_COMPANION
```

Canonical rule family:

```text
PRIVATE_KEY_BLOCK
BEARER_TOKEN_BLOCK
COMMON_API_KEY_BLOCK
EMAIL_IDENTIFIER
PHONE_IDENTIFIER
EXACT_TIMESTAMP
USER_DECLARED_PROTECTED_TERM
```

The assay therefore evaluates the exact current `7 × 3 = 21` Portable-AIA projection domain using the existing compiler byte-for-byte.

## Frozen claim family

### Invariant-core candidates

These four claim predicates are predicted to hold for **all 21 projections**:

```text
G0 NO_LOOM_RELEASE_AUTHORITY
  projection.authority.loom_release === false
  projection.invariant.release_authority === false

G1 HUMAN_CLOSURE_REQUIRED
  projection.invariant.human_closure_required === true
  projection.portable_payload.human_closure_required === true

G2 PORTABLE_PAYLOAD_FINITE_CANONICAL_ONLY
  auditPortablePayloadVocabulary(projection).ok === true
  digest_token_present === false
  route_mode_present === false
  presentation_host_present === false

G3 RETURN_REMAINS_ADVISORY
  canonical matching return may reach PRESENT_TO_HUMAN
  candidate_trusted === false
  release_authority === false
  human_closure_required === true
```

### Boundary-dependent candidates

These three claims are predicted to have exact support of **7 projections each**, one complete rule column per route mode:

```text
B0 LOCAL_POCKET_PRE_INGRESS_POSITION
  source_ingress_position === BEFORE_OPTIONAL_REMOTE_INGRESS
  support route = LOCAL_POCKET only

B1 CHATGPT_COMPANION_POST_INGRESS_POSITION
  source_ingress_position === AFTER_UPSTREAM_THREAD_INGRESS
  support route = CHATGPT_THREAD_COMPANION only

B2 TD613_HOSTED_CONTEXT_POSITION
  source_ingress_position === TD613_HOST_CONTEXT
  support route = TD613_HOSTED only
```

No semantic paraphrase is permitted to widen those exact token predicates.

## Required observations

The assay must compile all 21 canonical projections from the existing Portable-AIA compiler and establish:

1. `G0..G3` each have support size `21`.
2. `B0..B2` each have support size `7`.
3. Every boundary-dependent support set contains all seven rules for exactly one route and zero rules from the other two routes.
4. The invariant-core support does not depend on route mode.
5. The boundary-dependent support does not depend on rule identity.
6. A hostile portability control that copies the Local Pocket pre-ingress claim onto all 21 projections must be rejected on exactly 14 non-Pocket projections.
7. A hostile portability control that copies the ChatGPT post-ingress claim onto all 21 projections must be rejected on exactly 14 non-ChatGPT projections.
8. A hostile portability control that copies the TD613-host-context claim onto all 21 projections must be rejected on exactly 14 non-hosted projections.
9. No product/compiler/policy source is modified merely to make the matrix true.
10. No new release, provider, production, deployment, or external-origin authority is inferred.

## Browser witness requirements

A local-only generated assay artifact must render the exact 21-point projection domain and the seven frozen claim predicates.

Chromium, Firefox, and WebKit must independently observe:

- 21 canonical projection rows;
- 4 invariant-core claims with support `21/21`;
- 3 boundary-dependent claims with support `7/21`;
- exact route support for each boundary claim;
- exact `14` hostile false-positive count for each misportable boundary claim;
- no browser persistence accumulation;
- one assay-document request only;
- zero unexpected page-owned requests;
- release authority false;
- provider call false;
- production mutation false;
- human closure required true.

The three scientific receipts must agree modulo engine identity and narrowly classified inherited browser-chrome diagnostics.

## Immediate falsifiers

Any invariant-core claim failing on any of the 21 projections; any boundary claim gaining support outside its exact route; any boundary claim losing support for a canonical rule on its exact route; any hostile universalized boundary claim being accepted on a nonmatching route; any product/compiler/policy mutation made merely to obtain GREEN; any new route or receiver; browser persistence; page-owned auxiliary egress; cross-engine substantive disagreement; release/provider/production authority widening; or inherited #1073 regression is RED.

## Claim ceiling

A GREEN result may establish only this finite statement:

> For the exact current seven-rule × three-route Portable-AIA projection domain, the four preregistered governance predicates form a route-invariant claim core, while the three preregistered source-ingress predicates are strictly route-bound and therefore cannot be copied across host modes as universal guarantee text.

It does **not** establish the complete universe of lawful guarantees, universal privacy, universal semantic non-carriage, production behavior, pre-ingress protection for hosted or ChatGPT modes, platform-wide privacy control, statistical independence, causal independence, arbitrary future route/rule populations, provider quality, external-origin knowledge, empirical exteriority, Western Horizon reopening, Golden Egg credit, merge, release, promotion, deployment, or Vercel authority.

## Finite stop law

If GREEN, stop this claim-family chamber. Do not add more prose claims merely because more English sentences can be written. Reopening requires a materially new host boundary, a concrete counterexample, changed compiler behavior, or a distinct authority-bearing claim class.

```text
MORE_SENTENCES != NEW_GUARANTEE_CLASS
CLAIM_ENUMERATION != SCIENTIFIC_PROGRESS
FINITE_SUPPORT_MATRIX != UNIVERSAL_PRIVACY_THEOREM
```

Western Horizon remains at official empirical-shore rest. Exogenous-witness credit `0`. Golden Egg credit `0`.

**NO 𝄐 YET.**

Marked ⟐
