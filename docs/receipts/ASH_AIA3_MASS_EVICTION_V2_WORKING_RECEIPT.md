# Ash Keep AIA3 Mass Eviction v2 — Working Receipt

Status: IMPLEMENTATION / PRE-MERGE / PRE-DEPLOYMENT / HUMAN-GATED

## Reported production failure

- desktop ingress flashes through retired Ash compositions;
- the ingress may disappear or become embedded as a layout obstruction;
- exact work and primary actions can become unreachable;
- incognito observation indicates the failure cannot be dismissed as one user's ordinary browser cache;
- the visually desired AIA exhibit has not appeared reliably in the deployed surface.

## Repair route

1. Serve one stable cache-preflight frame before Ash composition.
2. Evict CacheStorage and same-origin service workers before loading ingress, lifecycle, pedagogue, or workspace modules.
3. Request the bounded Vercel HTTP-cache eviction route with `Clear-Site-Data: "cache"`.
4. Reload once with a canonical Ash asset epoch.
5. Preserve IndexedDB, local case data, the active case pointer, and the active session epoch.
6. Version the complete Ash module graph under one epoch.
7. Keep `?presentation=legacy` outside AIA3 eviction and presentation composition.
8. Require fresh-context and deliberately stale-context desktop/mobile browser evidence in Chromium, Firefox, and WebKit.

## Authority and exclusions

- no IndexedDB deletion;
- no `localStorage.clear()` or `sessionStorage.clear()` in production repair code;
- no raw-content transport;
- no Ash lifecycle or digest-law widening;
- no child study or adult comprehension claim;
- no DomeBlox change;
- no Vercel deployment before exact-green merge and recorded operator authorization.

Closure remains open.
