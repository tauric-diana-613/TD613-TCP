# 𝌋 Hush routing authority after live Gemini observation

## Acquired evidence

The sibling JSON is the exact unmodified sanitized receipt from [manual run 2925 / 34290086133](https://github.com/tauric-diana-613/TD613-TCP/actions/runs/34290086133), attempt 1, source `0381ac21c435360b09d613201f10ca831572e6eb`, Environment `gemini-observation`. Observation completed at `2026-09-08T23:19:17.205Z`: HTTP 200, complete, uncached, one page, 40 generateContent-advertising model IDs. No generateContent call occurred.

Artifact ID `10080991110`, archive SHA-256 `a0b549ac3f952f44bd8245566104069f4ad68bd317d2b20a16ea30c43b6a11e6` matched the downloaded ZIP. This digest covers the sanitized archive, never the credential. The original artifact expires September 15. The archived JSON is historical evidence, never a credential-independent runtime discovery cache. Its ten-minute discovery freshness interval does not extend through preservation.

Environment provisioning is OPERATOR-SUPPLIED; run/job/artifact custody is RETRIEVED. Listing establishes visibility only, not quota, quality, routing superiority, future availability, or release authority.

## Lifecycle comparison — retrieved September 8, 2026

[Google model catalog](https://ai.google.dev/gemini-api/docs/models) and [deprecations](https://ai.google.dev/gemini-api/docs/deprecations) compared against `server/gemini-model-policy.js`:

- All seven pinned catalog IDs were visible.
- Visible stable text additions absent from that catalog: `gemini-3.8-flash`, `gemini-3.7-flash`, `gemini-3.6-flash`, `gemini-3.5-flash-lite`. Their documented lifecycle status makes them candidates, without establishing TD613 task quality.
- CONTRADICTORY: `gemini-3.1-flash-lite-preview` remains visible although Google's catalog labels it shut down. Preserve both records. Listing cannot override lifecycle exclusion.
- Moving aliases and specialized image/audio/music/agent models require separate treatment; generateContent advertisement alone cannot admit them to text routing.

No default order changes in this chamber. Shared lifecycle eligibility and bounded task-quality witnesses remain successor work.

## Bounded repair

The deployed budgeted Hush URL already rewrites to the shared quality handler. Its old server implementation nevertheless retained an independent economy-first ladder, moving alias, and sticky success promotion. Quality imported prompt helpers from that obsolete handler.

Extract the unchanged prompt, sanitizer, literal, speech-act, and quarantine helpers into `hush-provider-contract.js`. Quality imports that module. The legacy module re-exports the exact quality handler and all six historical helpers, preserving imports while eliminating its private routing loop. This introduces no new route, UI, provider call, or promotion; it is a mechanical authority consolidation, not a new Pedagogue interaction design.

Focused semantic tests exercise the preserved helpers. A module identity test establishes that the legacy default and current handler are identical. The static lane now runs these witnesses. Source-location assertions follow the extracted implementation and verify the live quarantine call rather than a removed diagnostic string.

Known adjacent baseline RED remains `tests/gemini-quality-routing-vercel.test.mjs`: it expects a standalone readiness function that has already moved behind `api/khonapolit.js`. This chamber does not erase that archaeology or claim a clean release candidate.

Vercel, Western Horizon reopening, empirical exteriority, and Golden-Egg completion remain outside this receipt.

Sealed ⟐
