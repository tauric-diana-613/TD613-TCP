# Dollhouse Atlas/FADT Operational Candidate · 2026-09-09

Status: **FROZEN CANDIDATE / LOCAL EXECUTION UNAVAILABLE / CI WITNESS REQUIRED / DRAFT / UNMERGED**

Base / relocked production main:

```text
0cbe5fbd7cdcbda6725b8330adddaffabcc77a38
```

Candidate branch:

```text
research/dollhouse-atlas-fadt-operational-agents-20260909
```

## Scope

This candidate repairs the Dollhouse asymmetry in which Pedagogue and Aperture had canonical repository shortcuts and callable contracts while Atlas and FADT appeared only as bounded Loom labels.

The candidate adds:

- `DOLLHOUSE.md` four-role operational map;
- `ATLAS.md` canonical Atlas shortcut;
- `FADT.md` canonical FADT shortcut;
- `app/engine/dollhouse-agent-registry.js`;
- `app/engine/dollhouse-atlas-fadt.js`;
- `scripts/run-dollhouse-agent-audit.mjs`;
- `tests/dollhouse-atlas-fadt.test.mjs`.

No existing product file is mutated in this chamber.

## Atlas candidate

Atlas receives an admitted Loom semantic field and builds a Portable-AIA Flow-Core control envelope containing:

```text
source revision
scene identity
Flow-Core phase
relation keys
glyph trace
version-bound glyph legend
governance state
V/C/P/L coordinates
route state
missingness / contradictions
claim ceilings
return rule
```

It then projects intentionally non-equivalent child and auditor presentations while requiring equality of the machine control plane.

```text
SAME_CONTROL_PLANE != SAME_PRESENTATION
GLYPH_TRACE_ALONE != SUFFICIENT_CONTROL_RECEIPT
REMOTE_RETURN != TRUST_TRANSFER
```

The return contract explicitly requires Loom revalidation.

## FADT candidate

FADT consumes finite occupied quotient fibres and computes:

```text
U = union K_x
I = intersection K_x
Gamma = U \ I
```

It authorizes exact descended admissibility only when lawful support is constant on each occupied fibre and the irreducible gap is empty.

The bounded Loom contradiction fixture is translated into one FADT demonstration fibre only for the already-preserved pair of compared local rule-set surfaces.

## Portable-AIA correction preserved

```text
PACKETIZATION = CARRIER
OPERATIONAL PORTABILITY = PHENOMENON
```

A receiving companion is therefore modeled as receiving a versioned Flow-Core control grammar, not merely serialized prompt text. A future audit-only thread may receive the compact structured return receipt and compare it to origin state before Loom revalidation.

## Phasonic supermoiré tomography relationship

The Atlas control-plane witness may later support receiver/projection comparison assays in the phasonic supermoiré tomography lineage. This candidate grants no physical, continuum, basis-free, empirical-geometry, or exteriority claim.

## Witness posture

The active sandbox could not resolve `github.com`, so no local clone/test execution is claimed. Repository CI must arbitrate syntax, imports, tests, and integration.

No merge, deployment, Vercel mutation, production promotion, release, provider call, external-origin claim, Golden Egg credit, or Western Horizon reopening authority follows.

Sealed ⟐
