# Dollhouse Portable AIA Operational Roundtrip Battery · 2026-09-09

Status: **FROZEN CANDIDATE / EXACT-HEAD WITNESS REQUIRED / DRAFT / UNMERGED**

Exact direct parent:

```text
#1091 · Dollhouse Atlas/FADT operational agents
207d3dcf358e9f25de8f7736db40e631b7982056
run 2967 / 34321390825 — SUCCESS
```

Independent prior Portable-AIA result retained as context rather than Git ancestry:

```text
#1044 · three-route governance invariance — EARNED / UNMERGED
```

```text
DIRECT_GIT_PARENT != COMPLETE_RESEARCH_LINEAGE
PR_1044_CONTEXT != SILENT_CODE_IMPORT
```

## Question

Can one admitted Loom relation remain **operational** after a declared receiver change, support a bounded host-side operation, return home, and survive revalidation without turning route change into authority transfer?

This chamber tests that question at the bounded synthetic control-grammar layer.

## Correction preserved

```text
PACKETIZATION = CARRIER
OPERATIONAL_PORTABILITY = PHENOMENON
```

Byte equality is insufficient. A Portable-AIA receiver must be able to use the admitted relation under a declared control grammar and return a structured candidate that origin-side Loom can audit.

## Receiving control grammar

The companion projection carries:

```text
source revision
scene identity
Flow-Core phase
relation keys
exact glyph trace
glyph -> semantic-relation legend
motion / static / inspection grammar
governance state
V / C / P / L coordinates
route state
missingness / contradictions
claim ceilings
allowed operation set
admissible action support
return rule
```

The receiving host may change presentation. It may not silently rewrite the control plane.

A bare glyph string is rejected as an insufficient return receipt.

```text
GLYPH_TRACE != CONTROL_GRAMMAR
CONTROL_GRAMMAR != RELEASE_AUTHORITY
HOST_EXPLANATION != ORIGIN_FACT
```

## Declared bounded operations

```text
EXPLAIN_STATE
TRACE_FLOWCORE
PROPOSE_ACTION
REPORT_MISSINGNESS
```

No arbitrary tool execution, provider call, source mutation, release, deployment, or free-form control mutation is authorized.

## Return / revalidation path

```text
Loom semantic field
-> declared receiver projection
-> Flow-Core-guided bounded operation
-> structured return candidate
-> origin control comparison
-> FADT action-support comparison
-> proposed-action admission check
-> PRESENT_TO_HUMAN or HOLD
```

The candidate remains untrusted even when it passes.

## Battery

### Positive control · operational explanation

A route-memory scene is projected to the `companion` receiver. The companion returns `EXPLAIN_STATE` with the complete control envelope and exact Flow-Core trace.

Expected:

```text
control plane equal
Flow-Core trace equal
FADT action-support gap empty
PRESENT_TO_HUMAN
candidate_trusted = false
release_authority = false
```

### Positive control · bounded proposed action

The companion proposes `REST` when `REST` belongs to origin action support.

Expected:

```text
PRESENT_TO_HUMAN
human closure required
```

### Hostile 1 · action outside origin support

A RED release-block scene returns `COPY_CHECKED_MESSAGE` even though origin support excludes that action.

Expected:

```text
HOLD
PROPOSED_ACTION_OUTSIDE_ORIGIN_SUPPORT
```

### Hostile 2 · authority widening

The return mutates `raw_release_allowed` from false to true.

Expected:

```text
Atlas control mismatch
FADT support gap nonempty
HOLD
CONTROL_PLANE_DRIFT
FADT_ADMISSIBILITY_GAP
```

This is the key Dollhouse joint-agent stress case: Atlas detects relation/control drift while FADT independently detects that the widened returned support no longer descends exactly with origin support.

### Hostile 3 · glyph-only return

A return contains the glyph trace but omits the versioned control envelope.

Expected:

```text
reject before revalidation
```

### Hostile 4 · trace drift

The returned glyph trace differs while the control envelope claims continuity.

Expected:

```text
HOLD
FLOWCORE_TRACE_DRIFT
```

### Advisory missingness

The receiving host may report missingness such as inability to verify pre-ingress custody. That report is returned separately from origin control.

Expected:

```text
host missingness retained
host missingness not promoted into origin fact
```

## Phasonic supermoiré tomography bridge

The revalidation receipt records three finite comparison coordinates:

```text
control changed?
admissibility changed?
presentation allowed to change?
```

These are useful inputs for a later phasonic supermoiré projection-comparison assay because they separate conserved control state from receiver-induced presentation difference and inadmissible authority drift.

This chamber earns no physical geometry, continuum tomography, basis-free holonomy, causal effect, external-origin witness, or Golden Egg credit.

## Two-thread realization

The architecture now supports a future human-facing workflow in which:

1. one ChatGPT thread receives and operates the projected AIA relation; and
2. a separate audit-only thread receives the structured return candidate and replays the control/FADT checks before the operator closes.

This document specifies the machine contract for that realization. It does not claim live ChatGPT integration or pre-ingress secrecy.

## Authority ceiling

No merge, deployment, publication, production promotion, provider call, Vercel mutation, automatic release, hidden-state access, empirical exteriority, Golden Egg realization, or Western Horizon reopening follows.

Sealed ⟐
