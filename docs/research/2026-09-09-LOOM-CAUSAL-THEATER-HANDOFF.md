# Holonomy Loom causal theater handoff

Status: Draft implementation on `td613/loom-causal-theater-20260909`.

This chamber adds a resettable, fictional practice theater to the existing hosted Holonomy Loom. It does not replace the deterministic message checker, grant provider or deployment authority, or reopen the Western Horizon boundary.

## What is implemented

- `semantic-field.js` compiles eight fixture-closed fictional scenes through the real Loom rule engine and validates provider-neutral portable packets.
- `animation-coordinator.js` owns one finite clock per active view. Render passes receive one immutable snapshot. Hidden views, reduced motion, and `𝄐` structural rest settle without an idle animation loop.
- `theater-geometry.js` projects declared route return, custody strands, modeled weather, missingness seams, contradiction forks, and the fixed claim-ceiling horizon. The geometry is explanatory and not a measurement.
- `theater.js` and `theater.css` add child/auditor receiver views, scene controls, replay/scrub, receipts, portable projection, reset, and explicit loading into the existing checker. Loading a case does not press CHECK, copy, or send.
- The existing checker remains the authority for actual local rule outcomes. Modeled reconstructability pressure remains labeled `MODELED_DEMO`; `L` stays `UNEARNED`; `V ≠ C ≠ P ≠ L` remains visible.

## Validation

The focused suite passes locally:

```text
26 tests passing
```

It covers semantic admission/mutation resistance, the single-clock scheduler, deterministic replay, reduced motion, structural rest, all eight scenes, child/auditor projection, portable projection, and the DOM theater contract.

The existing hosted baseline was inspected at `https://td613.com/dome-world/holonomy-loom.html`. It currently shows the pre-theater hosted checker because this branch has not yet been released.

## Evidence and limits

- The theater source revision is `working-tree` until this branch receives an exact commit receipt.
- Replay checksums are non-cryptographic accidental-change checks, not authentication.
- Demo traversal history is recorded only as local UI gestures; the declared scene order is not an observed traversal.
- No provider call, credential, deployment, production release, empirical exteriority, authorship, attribution, or human-comprehension claim is created by this chamber.

## Next bounded actions

1. Open a Draft PR from this branch and run the existing Consolidated Validation workflow.
2. Inspect the PR/preview build in the browser at desktop and narrow widths, including reduced motion.
3. Only after exact-head validation and explicit operator release authorization, deploy the candidate through the repository's Vercel membrane.
