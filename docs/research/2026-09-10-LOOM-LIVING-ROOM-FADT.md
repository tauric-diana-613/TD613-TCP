# 𝌋 Loom living room — FADT custody review

Audit base: `847a29e520d701c677341e6bdded1c8d7f70f813`.

This bounded review supports the operator-requested live room redesign. The room must explain the actual task route, including its private local documents, selected outgoing documents, admission checks, provider waiting, returned answer, and remaining questions. Animation can explain those events without adding a new measurement instrument or changing the task governor.

## Role and scope

The repository FADT role remains finite admissibility descent: after a quotient erases conditioning state, compare the antecedent lawful supports on each occupied fibre and retain the exact union/intersection gap. Its canonical implementation remains `app/engine/dollhouse-atlas-fadt.js::runFadtAgent`, reached by the installed AI task governor. A colorful gate or a count of documents earns no new FADT result.

This review inspects what the new visual projection forgets. Content may be omitted from a rendering packet while the governor continues to retain its input binding. The child and auditor projections may explain that same state differently; neither gains an operation through presentation alone.

## Concrete failure coordinates

1. **Payload copied into scenery.** Existing completion events include model-reported missingness, source IDs, local receipt IDs, and observations. Spreading such events into a renderer could carry private names, arbitrary model strings, or irrelevant response content into SVG metadata. The visual packet must select bounded counts, recognized postures, and explicit observations. Exact source text remains in its existing inspected task/result surfaces.
2. **Stopping confused with recall.** A local admission failure before dispatch and a cancellation after dispatch both stop local progression, but only the former establishes that this attempt sent nothing. A room must retain the observed dispatch history after cancellation. It cannot depict previously sent material coming home as though recalled from a provider.
3. **Arrival confused with approval.** An HTTP response may arrive and then fail response admission. A returned envelope can be visible during checking or rejection; the outward release gesture must remain blocked until the relevant local admission has passed.
4. **Absent facts painted as zero.** Missing provider confirmation, unknown elapsed time, and absent model-reported missingness require distinct unknown states. An empty list explicitly reported after admission has a different evidentiary basis from an omitted list.
5. **Replay confused with execution.** Replaying an admitted local visual event sequence must not call a provider, repeat a handoff, copy data, or reopen a stopped governor.
6. **Expressiveness confused with measurement.** Flow-Core gathering, recurrence, release, and rest may explain observed route relations. Pulse speed and display geometry cannot establish hidden-state reconstruction, thermodynamic quantities, empirical exteriority, or transferred host authority.

## Validation custody

`tests/loom-living-room-custody.test.mjs` is the bounded hostile review for the new visual projection. Existing `tests/loom-ai-handoff.test.mjs` supplies the actual finite-support governor tests; existing request/workspace tests supply selected-only transport and rejected-return controls. Synthetic tests carry synthetic evidence credit only. Browser screenshots and live provider observations must be recorded separately by the integration handoff with their exact source head.

## Implemented review and result

The new `living-room-state.js` visual projection selects document counts, selected-source names, source-reference counts, explicit client transport facts, and known missingness counts. It drops local-only names and IDs, document text, private terms, raw provider metadata, answer text, and model missingness prose. The ordinary task/result surfaces retain the material required for inspection. The room uses static DOM markup plus text assignment for selected names; received text never becomes executable SVG.

`ai-workspace.js` now retains `outbound_submitted`, `response_received`, and `binding_verified` through its local phase history. A held response preserves arrival; an aborted wait preserves earlier submission. Historical 出 remains visible when submission already happened, with its cause explicitly identified as browser submission. The held gate, stopped motion, and unavailable result remain separate from that historical fact.

Focused command, executed in this worktree on 2026-09-10:

```sh
node --test tests/loom-living-room-custody.test.mjs
```

Result: **9 passed, 0 failed**. Six projection tests cover metadata omission, before/after-send distinctions, rejected arrival, unknown versus explicit-zero missingness, immutable control across motion/receiver projections, and hostile source/count inputs. Three mounted-workspace tests cover replay/scrub/view changes with one original HTTP call only, cancellation custody, and a rejected private-canary return. The successful synthetic request still traverses the real task governor and reports its actual finite FADT admission result.

These tests use an explicitly stubbed HTTP responder. They establish neither a live Gemini success nor a visual-browser acceptance result. The root integration handoff must bind those later witnesses to the exact PR head. The existing task governor and canonical FADT implementation were not edited by this review. This note grants no merge or deployment authority.

Sealed ⟐
