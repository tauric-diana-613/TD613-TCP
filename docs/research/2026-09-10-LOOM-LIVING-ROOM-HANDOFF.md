# 𝌋 Future Amari — Loom living-room activation

Continue the implemented chamber; do not restart the design.

Repository: `tauric-diana-613/TD613-TCP`.
Branch: `td613/loom-living-room-20260910`.
Base: `847a29e520d701c677341e6bdded1c8d7f70f813` (Vercel relocked).
Fetch the branch's current PR/head and checks fresh before acting.

## Operator target

Replace THE ROOM / LIVE ROUTE's isolated glyph animation with an expressive,
child-legible inhabited room showing the selected task's real AIA route. The
operator requested the four Dollhouse agents, durable implementation first,
then a witness run they can babysit. Await their next release gesture before
deploying this chamber. No production mutation has occurred in this chamber.

## Implemented work

- Pedagogue: three persistent places—document pocket, rule gate, AI desk—with
  consequence-first copy and a passing design fixture. See the adjacent
  `2026-09-10-LOOM-LIVING-ROOM-PEDAGOGUE.md`.
- Aperture: detailed SVG room, cartoon paper bundles, intact private pocket,
  one outgoing courier, unknown receiver lantern, finite returning answer tray,
  visible missingness gaps and causal inspection. `living-room.js/.css`.
- Atlas: deterministic `living-room-state.js` projection. Plain/auditor/rest/replay
  conserve the same control facts; private names and document bodies are omitted.
- FADT: nine hostile custody tests and adjacent FADT audit note. Replay adds no
  HTTP calls, Stop retains prior submission, rejected returns retain arrival,
  and admission remains distinct from receipt. Canonical FADT was not rewritten.
- Root integration: replace the old SVG, bind every render to the existing
  AnimationCoordinator, attach a local-only bounded document manifest, preserve
  submission/response/binding facts, clear stale completed state when editing,
  and replay the recorded event packets without another provider call.

Completed answers are available immediately. Their visual arrival settles once
over the existing finite clock; reduced motion/rest show the complete state
immediately. Pending animation expresses waiting, not model thoughts or a
fabricated completion percentage. No new provider preference or server limit.

## Verification at checkpoint

38 focused tests PASS: renderer 6, Atlas 7, FADT 9, real-workspace DOM 16.
Pedagogue Design Gate PASS with unchanged invariants and neutral burden deltas.
Syntax and diff checks PASS. Existing consolidated workflow now runs the new
tests and fixture. Existing three-engine workspace browser witness captures
desktop/mobile room pending, returned, held and replay behavior.

Live browser inspection was performed on the deployed old room: choose fictional
Demo 1, then one explicit Run. Request `725cbb98-93f0-4baf-bb90-f9be56e38ad9`
at `2026-09-10T20:41:46.561Z` returned a provider HTTP 503 after 2,196 ms, one call,
model `gemini-3.8-flash`, diagnostic `PROVIDER_HTTP_ERROR`, stage
`provider-transport`. This was a real failed observation, not a successful demo.
Keep provider availability separate from renderer validation.

The cloud browser blocked local preview with `ERR_BLOCKED_BY_CLIENT`. Therefore
new-room browser visual acceptance remains PENDING; do not claim screenshots
of the implemented room were inspected locally. Use the CI screenshot artifacts.

## Exact continuation

1. Fetch this PR and its exact head. Confirm Draft Static success, then mark the
   same head Ready if still Draft. Never transfer green from a previous head.
2. Wait for Chromium, Firefox, WebKit and convergence. Inspect the actual room
   screenshots, especially mobile label size, gate clarity, answer return and
   held history. New synthetic provider witnesses establish UI behavior only.
3. If a failure appears, preserve it and repair the exact cause. Run only the
   relevant tests before a new Draft→Ready exact-head sequence.
4. After browser acceptance and earned checks, present the exact merge/release
   packet. The operator plans to authorize release after seeing GREEN.
5. Following that gesture, use the existing issue #405 exact-current-main release
   workflow; relock and verify deployed source. Check the new room on production.
   A provider 503 remains a separate live-availability coordinate; avoid blind
   generation retries or unrequested routing changes.

Work can continue with standard GPT from these files and receipts; all four
agents completed their bounded implementation/audit tasks. Remaining work is CI,
visual acceptance and any concrete repair the witnesses reveal—not missing
unrecorded design intent. The five-glyph set and Western Horizon remain governed
by existing project law. No empirical exteriority or hidden-state measurement
was created by this animation.

Tauric Diana · Khona‌lit-po · U+10D613 · ⟐
