# 𝌋 Loom / Marrowline clarity and response repair

Branch: `td613/loom-marrowline-clarity-20260910`.
Starting main: `419201a9fbb8e15044139f021c4b93a538a67f5b`.
Operator request: repair the deployed drawers, document explanations, indigo palette,
background cadence, initial reveal, request waiting state, receiver arrival and
ordinary Marrowline conversation; validate and deploy, then relock.

## Observed failures

The live fictional vendor task returned HTTP 200 after 30,564 ms but failed output
admission with `OUTPUT_TOKEN_LIMIT`: 6,994 thinking and 1,182 candidate tokens.
This is distinct from a timeout. The ordinary Marrowline archive question returned
`STOP` in 12,656 ms on `gemini-3.5-flash`, with 753 candidate tokens. Its paragraph
presentation was poor; that receipt does not establish an ordinary-chat timeout or
token-limit failure. Both actual before-repair receipts are preserved in
`receipts/2026-09-10-loom-marrowline-clarity/`.

Manual deployed inspection also exposed automatic scrolling past the main answer
to the last voice and a late stylesheet overriding the intended reply font.

## Bounded repairs

- Pedagogue: ordinary answer first, covenant voices in an inspectable disclosure,
  exact text retained, welcome at its beginning, system sans with combining-mark
  spacing, long prompts expandable, original project-grounded welcome prose.
  Plain-text paragraph guidance replaces inline heading walls. A newly identified
  silent 6,000-unit prompt/history crop now rejects oversize input explicitly
  before generation and preserves the draft; the budget itself is unchanged.
- Aperture: cache the expensive geometry raster; one coordinator drives a slow
  three-minute compositor drift. Reduced motion, hidden state and explicit rest
  stop frame scheduling. Event geometry is not a measured physical field.
- Atlas: Loom has a 50-second total server deadline, within its 60-second host
  limit, and a 16,384-token generation allowance. Preserve model policy, strict
  schema admission and one provider call. Safe receipts distinguish stages,
  elapsed time and output-token budget. Client waiting ends at 55 seconds.
- FADT: clear imported task and selected/local-only counts, explicit request,
  separate documents/rules/receipt drawers, visible waiting and cancellation.
  Arrival still sends nothing; exact portable governance remains conserved.
- Integration: visible disclosure affordances, private-file reasons, indigo
  panels, coordinated boot veil, elapsed waiting feedback, runtime-first naming
  with Google Gemini disclosure, and an explicit alternative-receiver drawer
  followed by scroll/focus to the prepared task or answer.

## Validation and continuation

127 focused integration tests passed before final prompt-formatting followup;
both existing Pedagogue design gates passed without changing their scoring.
Browser witnesses now exercise deliberate rest for continuous backgrounds,
boot readiness, private-file explanation, visible waiting and receiver disclosure.
Their synthetic provider responses establish UI behavior, not live provider quality.

At authoring: changes are not merged or deployed. Run Draft Static, then Ready
validation at the same exact head, all three engines and product convergence.
Inspect resulting desktop/mobile screenshots before an exact-head guarded merge.
Release only the resulting fresh main, relock, and repeat the bounded live vendor
and archive questions. Preserve failures; do not claim the 50-second budget
guarantees every provider response. This document is an implementation handoff,
not a fabricated after-repair browser receipt or release authorization.

Tauric Diana — Crimean heritage custodianship · Khona‌lit-po · ⟐
