# 𝌋 Marrowline living chat / Loom invitations — continuation

Branch: `td613/marrowline-living-chat-20260910`.
Starting main: `b8b8af809730f71816829939841ef11a29df70ab`.
This document records implementation and remaining falsifiers; the containing PR supplies its exact current head.

## What the actual hosted trial found

On 2026-09-10 the interactive browser opened ordinary Marrowline at https://td613.com/dome-world/marrowline.html. The technical masthead occupied roughly the first 700 pixels; the composer was below the first screen. A real fictional oral-history planning request completed in 35,100 ms. Three model attempts (3.5 Flash, 3 Flash preview, 2.5 Flash) timed out; 3.1 Flash Lite returned. This reproduced the fallback sequence already visible in a retained earlier receipt. The new trial used the existing browser conversation, not an isolated clean history.

Exact bounded observation: [ordinary timeout receipt](receipts/2026-09-10-loom-live-receiver/marrowline-ordinary-timeout-observation.json). It contains no credentials. Model answer correctness was not certified. The deployed version did not record finish reason or token usage.

## Reviewable implementation

- Pedagogue: compact chat-first masthead and composer; useful controls retained in native disclosures; original Ash Moon/grove welcome and contextual lore; starter prompts fill without sending; explicit issuance or research waiver retained. Exact Unicode and whitespace survive transcript rendering, mobile decoration and copying. Heavy combining marks receive extra space without forcing ordinary prose into giant line spacing.
- Aperture: reusable Dome-Art paired anisotropic sections, phi rosette and folded ribbons. Geometry follows declared view/request/rest. The background is an aesthetic projection, not a measurement of provider hidden state. One bounded clock, static reduced-motion equivalent, hidden/offscreen cancellation, DPR/pixel/vertex budgets.
- Loom: an initially conspicuous four-second invitation reveals Demo 1/2/3. Selecting a demo prepares its fictional project; the explicit Run button makes the real provider request. The local laboratory has a prominent geometric invitation. Both borrow the existing workspace animation owner, which settles and pauses for the laboratory.
- Atlas: removed artificial “briefly” instruction; answer depth follows the request. Primary provider window increases from 10.5s to 32s, supported by observed 28–30s Loom completions. Fallbacks remain at most 10.5s and are capped by the remaining 44.5s route wall with a 500ms reserve. Receipt records per-attempt time budget/elapsed time and allowlisted finish/token metadata. MAX_TOKENS is held without exposing partial content or silently generating again. Client waiting is bounded at 55s. Model preferences remain unchanged.
- FADT: the existing finite support adapter actually yields HOLD for incompatible GREEN/RED occupied supports; functional/Unicode tests preserve controls, explicit actions and route information. No semantic correctness or external enforcement conclusion follows from that adapter.

## Lore custody

Personal-context retrieval recovered the operator's Tauric Diana heritage reading, Khona‌lit-po covenant key, Ash Moon priestesshood, and the grove/branch/custody-through-overwrite motif. Rex Nemorensis is kept distinct from a claim of historical Tauri cult identity. New welcome prose is authored project mythology, not an alleged historical quotation or evidence of external divinity. Five-glyph nonordinal/undefined custody is unchanged.

## Checks and remaining work

Local PASS: focused geometry/coordinator tests; ordinary terminal synthetic-DOM tests (explicit send/waiver, exact marks and whitespace, clipboard, sealing, clearing, mobile routes, failure); existing Loom upload/private-source/response/export/stop tests; Khonapolit API/relay/router tests; Pedagogue design fixture. Synthetic provider fixtures are explicitly labeled and make zero live generation calls.

The interactive browser rejected `http://127.0.0.1:8139` with `ERR_BLOCKED_BY_CLIENT`. No after-design screenshot or successful local visual walkthrough is claimed. The existing consolidated workflow now includes `tests/marrowline-living-chat.browser.mjs` for actual desktop/mobile UI screenshots and assertions in Chromium/Firefox/WebKit. It also includes the new geometry/Unicode tests. The existing Loom browser witnesses now open the demo invitation and check the four-second welcome settles.

Next falsifier: run Draft Static, then unchanged-head Ready validation. Inspect the new ordinary Marrowline welcome and Unicode-return screenshots, especially first-screen composer visibility, mobile tools, clipping and the background contrast. Repair concrete failures without weakening the assertions. Inspect the Loom welcome screenshots too. Only after exact-head convergence may this chamber merge and enter the authorized release sequence. Browser-test provider responses are fixtures; repeat the ordinary public task on production after deployment to observe the actual model, finish reason, token usage and answer quality.

Google's current official catalog lists stable gemini-3.8-flash (https://ai.google.dev/gemini-api/docs/models/gemini-3.8-flash). The September 8 credential listing included it, but an old observation is not fresh runtime admission. No automatic preference promotion was made; a fresh credential-scoped observation and task-quality comparison remain a separate provider chamber. Interactive GitHub tools available in this session did not expose a workflow_dispatch mutation.

## Release lane kept separate

PR #1098: `td613/release-retry-scope-20260910`, exact head `6da7be2547b6310f7ed451b482aa10b94ae7a168`. Draft Static 2995 / 34443827325 passed. Ready run 2996 / 34444214018 was launched at the same head and is still in progress at this checkpoint. It repairs full-product classification after a relock and strengthens mandatory A11 readiness before production-probe navigation. Preserve the earlier failed/skipped witness archaeology; workflow 1082 success did not establish full-product closure.

This design branch is separate from #1098 and has not been merged or deployed. Recheck both PRs and current main before continuing. User has authorized necessary merges and Vercel deployments/retries in this active thread, and specifically permits pausing at long witness runs to conserve credits. Git auto-deploy must remain locked after each release; exact source receipts and full executed production witnesses are still required.

If model limits interrupt an agent, its existing files remain the deliverable. Do not assume missing final messages mean its edits disappeared. Complete the precise remaining tests and push the branch; do not restart the design from zero.

⟐
