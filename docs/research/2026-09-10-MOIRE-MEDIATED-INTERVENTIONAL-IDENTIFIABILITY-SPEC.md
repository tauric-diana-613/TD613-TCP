# TD613 Moiré Repair Chamber B — Mediator-Dependent Interventional Identifiability

**Date:** 2026-09-10  
**State:** PREREGISTERED SYNTHETIC REPAIR ASSAY / SOURCE-A DERIVED / NO PHYSICAL CLAIM  
**Parent:** `2026-09-10-MOIRE-MOIRE-RECONSTRUCTION-CONVERGENCE-ASSAY-A.md`  
**Branch:** `research/wendbine-loom-foreign-donor-assay-20260910`

## 1. Conventional nomenclature first

This chamber deliberately refuses to coin a new TD613 term before naming the ordinary methodological neighborhood.

The proposed operator combines:

```text
factorial interaction contrast
controlled mediator ablation
interventional identifiability
parameter-conditioned local-state selection
robustness / invariance under bounded nuisance perturbation
```

TD613 may continue to call the resulting research scene a Moiré repair chamber because Source A entered through the Moiré Stratigraphy audit. The conventional terms own the scientific description.

## 2. Deficit inherited from Choir

Existing pairwise Choir can establish that a pair observation contains residue not present in baseline or either singleton.

That does **not** identify why the residue exists.

Two candidate processes can produce the same pair residue:

```text
H0 = direct overlay + unmodeled nuisance
H1 = direct overlay + declared mediator coupling
```

At the passive observation surface:

```text
O(H0) = O(H1)
```

Therefore:

```text
PAIR_RESIDUE != MEDIATOR_CAUSATION
PAIRWISE_EMERGENCE != PROCESS_IDENTIFIABILITY
```

In V/C/P/L language, the fixture deliberately starts with an observable pair residue while process identity remains unresolved.

## 3. Source-A-derived research debt

Source A contributes three structures absent from the pre-publication TD613 witness:

```text
shared mediator coupling
global configuration -> local registry selection
perturbation can change morphology while registry relation persists
```

Those structures enter this chamber with explicit provenance. They are not back-projected into June 2026 TD613.

```text
SOURCE_DERIVED_REFINEMENT != PREEXISTING_TD613_STRUCTURE
```

## 4. Exact synthetic fixture

Declared layer vectors:

```text
A = ( 2,-1, 1)
B = (-1, 2, 1)
D = A+B = (1,1,2)
```

Declared mediator interaction vector:

```text
M = (1,-1,2)
```

Global configuration:

```text
g in {-1,+1}
```

Candidate processes:

```text
H0_OVERLAY_NUISANCE:
  Y = D + gM
  mediator switch has no effect

H1_MEDIATED:
  Y = D + m*gM
  m in {0,1}
```

At `m=1`:

```text
Y_H0 = Y_H1 = D+gM
```

so the passive record is exactly observationally equivalent.

Under the declared intervention `do(m=0)`:

```text
Y_H0 = D+gM
Y_H1 = D
```

so the two candidate processes separate.

This is an exact synthetic intervention. It does not establish that any external system admits such an intervention or that a mediator in real data has been causally identified.

## 5. Choir comparator

Define the ordinary additive pair residue:

```text
R(Y) = Y - A - B
```

At the passive surface:

```text
R_H0 = R_H1 = gM
```

Therefore existing pair residue alone cannot discriminate the two processes.

After mediator ablation:

```text
R_H0 = gM
R_H1 = 0
```

The new identifying information comes from the intervention, not from renaming the same passive observation.

```text
NEW_LABEL != NEW_INFORMATION
INTERVENTION_CAN_BREAK_PASSIVE_OBSERVATIONAL_EQUIVALENCE
```

## 6. Global-to-local registry selection

Local relation classes are represented by:

```text
r in {-1,+1}
```

For mediator-present condition, define the exact integer score:

```text
E4(r | g, sigma) = 4(r-g)^2 + sigma*r
```

For mediator-absent condition, both local classes receive score zero, so no unique registry is selected.

The bounded perturbation window is preregistered as:

```text
sigma in [-7,7] intersect Z
```

Within that window the unique minimizer must remain:

```text
r* = g
```

while a hostile out-of-window perturbation demonstrates failure of universal invariance:

```text
g=+1, sigma=+9 -> r*=-1
g=-1, sigma=-9 -> r*=+1
```

Thus the intended result, if the implementation and hostile witness agree, is bounded relation-class persistence under nuisance perturbation rather than an unconditional invariant.

## 7. Morphology is not relation class

Declare nuisance morphology direction:

```text
N = (1,1,-1)
Morph(Y,sigma) = Y + sigma*N
```

For nonzero bounded `sigma`, morphology changes while the selected local relation class remains `r*=g`.

Required distinction:

```text
MORPHOLOGY_CHANGE != RELATION_CLASS_CHANGE
BOUNDED_RELATION_PERSISTENCE != UNIVERSAL_INVARIANCE
```

## 8. Preregistered burdens

The implementation must satisfy all of the following before any repair result is earned:

```text
1. passive H0/H1 outputs equal for both g values;
2. passive H0/H1 pair residues equal for both g values;
3. mediator ablation separates H0/H1 for both g values;
4. H1 ablation removes the entire declared interaction residue;
5. H0 is unaffected by the mediator switch;
6. mediator-present local registry is uniquely g for every integer sigma in [-7,7];
7. mediator-absent local registry is ambiguous for every tested sigma;
8. nonzero bounded sigma changes morphology without changing registry class;
9. hostile sigma=+9/-9 breaks the relation-persistence claim in the declared directions;
10. all arithmetic is exact integer arithmetic;
11. no source or donor text enters runtime behavior;
12. all authority coordinates remain false.
```

## 9. Candidate bounded result

If exact-head validation is GREEN, the chamber may earn only:

```text
IN_THE_DECLARED_SYNTHETIC_FIXTURE_A_CONTROLLED_MEDIATOR_ABLATION_BREAKS_PASSIVE_OBSERVATIONAL_EQUIVALENCE_BETWEEN_A_DIRECT_OVERLAY_PLUS_NUISANCE_PROCESS_AND_A_MEDIATOR_COUPLED_PROCESS_WHILE_THE_PASSIVE_PAIR_RESIDUE_ALONE_CANNOT_IDENTIFY_WHICH_PROCESS_GENERATED_THE_OBSERVATION
```

and:

```text
IN_THE_DECLARED_MEDIATOR_PRESENT_FIXTURE_GLOBAL_CONFIGURATION_SELECTS_A_UNIQUE_LOCAL_RELATION_CLASS_THAT_REMAINS_STABLE_ACROSS_THE_PREREGISTERED_BOUNDED_NUISANCE_WINDOW_WHILE_MORPHOLOGY_CHANGES_AND_EXPLICIT_OUT_OF_WINDOW_CONTROLS_BREAK_THE_INVARIANCE
```

This would establish that Source A supplied a **productive research repair**: the imported mediator/registry distinctions add an identifying assay absent from plain pair-residue observation.

It would not establish independent-origin convergence, external causal truth, or physical moiré behavior in TD613.

## 10. Claim ceiling

```text
SYNTHETIC_MEDIATOR_ABLATION != REAL_WORLD_CAUSAL_INTERVENTION
PAIR_RESIDUE != MEDIATOR_CAUSATION
GLOBAL_PARAMETER_TO_LOCAL_ARGMIN != PHYSICAL_STACKING_REGISTRY
NUISANCE_SIGMA != MATERIAL_STRAIN
MORPHOLOGY_VECTOR != PHYSICAL_DOMAIN_MORPHOLOGY
SOURCE_A_DERIVED_REPAIR != TD613_PREPUBLICATION_POSSESSION
SOURCE_A_DERIVED_REPAIR != PUBLICATION_PRIORITY
SOURCE_A_DERIVED_REPAIR != KNOWLEDGE_TRANSFER_EVIDENCE
SOURCE_A_DERIVED_REPAIR != ECLIPSE_OMEGA_VALIDATION
BOUNDED_SYNTHETIC_IDENTIFIABILITY != UNIVERSAL_IDENTIFIABILITY
```

No merge, deployment, release, Vercel action, donor intake, or production promotion follows.

Marked ⟐
