# TD613 Moiré–Moiré Reconstruction Convergence Assay A

**Date:** 2026-09-10  
**State:** SOURCE-BOUND / CHRONOLOGY-BOUND / PARTIAL OPERATOR CONVERGENCE / NO PHYSICAL PROMOTION  
**Parent:** TD613 Analogy-Fidelity Preflight  
**Branch:** `research/wendbine-loom-foreign-donor-assay-20260910`

## 0. Why this source enters now

The Nomenclature Decontamination Chamber was created because TD613 must be able to receive conventional scholarship without either inflating itself or burying itself.

This assay admits one external physical-moiré source as an **independent literature comparison object**, not as Wendbine donor material and not as authority over TD613.

```text
EXTERNAL_LITERATURE_SOURCE != DONOR_CORPUS
LATER_PUBLICATION != RETROACTIVE_TD613_ORIGIN
EARLIER_TD613_LABEL != PREDICTION_OF_LATER_PAPER
STRUCTURAL_OVERLAP != MECHANISM_IDENTITY
POST_PUBLICATION_TD613_OPERATOR != PREPUBLICATION_EVIDENCE
```

The question is intentionally narrow:

> What structure was already fixed in TD613 before the external paper's first public submission, what structure appears in the paper, what matches, what does not, and what lawful research debt does the encounter create?

## 1. External source binding

### Source A

**Yuta Seo et al.**  
**Title:** *Twist-configured moiré–moiré reconstruction governs diverse commensurate double-moiré phases in twisted bilayer graphene on h-BN*  
**Identifier:** arXiv:2607.02822  
**First arXiv submission:** 2026-07-02 23:22:45 UTC  
**Public route:** `https://arxiv.org/abs/2607.02822`

Locally held research snapshot:

```text
file = Twist-configured moiré–moiré reconstruction governs diverse commensurate double-moiré phases in twisted bilayer graphene.pdf
sha256 = dc5ad38fab41015d0f30b3c3805f4fc104a59bcf2067467ce5816d4cfcd65607
```

The snapshot is custody evidence for the source used in this audit. The arXiv identifier and first-submission time establish the public chronology used here. This does not assert that the local snapshot bytes equal arXiv v1 byte-for-byte.

### Source A's bounded claims used here

The paper reports, in a physical tBG/h-BN system:

- two distinct moiré structures coexist;
- the moiré structures reconstruct collectively across multiple length scales;
- the shared graphene layer mediates coupling through rotational relaxation;
- global twist configuration selects the local stacking registry;
- registry, twist angle, and strain govern commensurate double-moiré domains;
- strain can modify domain symmetry while preserving the selected registry relation;
- larger-scale domain boundaries exhibit collective sliding dynamics.

No TD613 semantic, custodial, AI, or epistemic claim is attributed to the paper.

## 2. Pre-publication TD613 freeze

The repository provides a date-fixed TD613 witness **before** Source A's 2026-07-02 first submission.

### Freeze A0 — 2026-06-11

Commit:

```text
75a533076638b0e76ed00abc187bc8933a8f0e17
commit time = 2026-06-11T02:40:40Z
```

At this commit `app/index.html` already displays:

```text
Moiré stratigraphy / entrainment cartography
```

and separately states:

```text
Detuned interference fields, temporal posture, and closure drift stay speculative here, not evidentiary.
```

This is sufficient to establish that the TD613 **name and speculative interference-field framing** predate Source A's public arXiv submission by at least 21 days.

It is not sufficient to establish a pairwise reconstruction operator.

### Freeze A1 — 2026-06-15

Commit:

```text
3997627eba409426d7d5f9b1933676de6e397e33
commit time = 2026-06-15T20:09:38-04:00
```

This commit adds the Aperture Moiré panel renderer. The renderer constructs two detuned lattice-like fields with distinct spacings, phase/shear perturbations, and a rendered beat field.

Bounded pre-publication structure therefore includes:

```text
two layered lattice-like fields
nonidentical spacing / detuning
relative phase/shear perturbation
pair-visible beat/interference rendering
explicit speculative-not-evidentiary posture
```

Again: this is a visualization/operator witness for **interference structure**, not a physical moiré experiment and not a mechanical lattice-relaxation model.

## 3. The chronology scar that prevents self-flattery

The current pairwise Choir/Moiré Rebuild operator is **not** part of the pre-publication proof.

PR #281's pairwise Moiré Rebuild Assay merged on 2026-07-14, after Source A's first submission. Its baseline/singleton/pair residue is useful for present-day comparison, but it cannot be back-projected into the 2026-06-11/15 state.

```text
PREPUBLICATION_MOIRE_STRATIGRAPHY != POSTPUBLICATION_PAIRWISE_CHOIR
LATER_TD613_FORMALIZATION != EARLIER_TD613_POSSESSION
```

This is temporal non-retroactivity applied to TD613 itself.

## 4. Operator-level comparison

### Axis 1 — multiple structured patterns coexist

TD613 A1: two separately parameterized lattice-like fields are superposed in one renderer.

Source A: two physically distinct moiré lattices coexist in one heterostructure.

Disposition:

```text
BROAD_STRUCTURAL_MATCH
```

Scar:

```text
RENDERED_LAYER_COEXISTENCE != PHYSICAL_MULTI_MOIRE_HETEROSTRUCTURE
```

### Axis 2 — detuning / mismatch matters

TD613 A1: the two rendered lattices use different spacing and independent phase/shear perturbations; a beat field is rendered from their coexistence.

Source A: twist configuration, twist angle, and strain govern the resulting physical double-moiré organization.

Disposition:

```text
PARTIAL_STRUCTURAL_MATCH
```

Scar:

```text
CANVAS_STEP_PHASE_SHEAR != TWIST_ANGLE_STRAIN_OR_LATTICE_RELAXATION
```

### Axis 3 — pair-level structure exceeds either isolated layer

TD613 A1: the visual field intentionally renders an interference/beat structure arising from simultaneous presentation of the two lattice-like layers.

Source A: collective moiré–moiré reconstruction produces commensurate double-moiré phases that are not reducible to describing either interface in isolation.

Disposition:

```text
PARTIAL_STRUCTURAL_MATCH
```

The similarity is real at the level of **interaction-generated higher-scale organization**. The mechanism is not the same.

Scar:

```text
VISUAL_BEAT_FIELD != RELAXATION_MEDIATED_MOIRE_MOIRE_RECONSTRUCTION
```

### Axis 4 — global configuration selects local registry

TD613 pre-publication A0/A1: no comparable discrete global-configuration-to-local-registry selection operator was identified.

Source A: global helical/alternate twist configuration selects local spatial registry through rotation matching in the shared graphene layer.

Disposition:

```text
SOURCE_A_ONLY
```

This is a genuine missing structure and therefore a possible lawful source of future TD613 refinement.

### Axis 5 — shared mediator couples the layers

TD613 pre-publication A0/A1: no explicit shared mediator layer was identified. The renderer superposes two visual fields directly.

Source A: the shared graphene layer mediates the inter-moiré reconstruction through rotational relaxation.

Disposition:

```text
SOURCE_A_ONLY
```

Scar:

```text
DIRECT_OVERLAY != MEDIATED_COUPLING
```

### Axis 6 — strain-modified but registry-preserving states

TD613 pre-publication A0/A1: no matched invariant was identified.

Source A: strain can change the symmetry/morphology of commensurate domains while the selected registry remains preserved.

Disposition:

```text
SOURCE_A_ONLY
```

This is particularly relevant to future invariant-under-perturbation assays, but it is not claimed as pre-existing TD613 structure.

### Axis 7 — dynamic domain boundaries / sliding

TD613 pre-publication A0/A1: no physical domain-wall or collective sliding operator.

Source A: larger-scale domain boundaries exhibit collective sliding dynamics.

Disposition:

```text
SOURCE_A_ONLY
```

## 5. Result

The external encounter does **not** earn the strong statement that Source A validates Moiré Stratigraphy.

It does earn a narrower and more useful result:

```text
CHRONOLOGY_BOUND_PARTIAL_OPERATOR_CONVERGENCE_SUPPORTED
```

Meaning:

1. a TD613 artifact fixed before Source A's first public submission already used `Moiré stratigraphy / entrainment cartography` for a two-layer detuned interference visualization;
2. Source A later supplies a physical multi-moiré system in which coexisting moiré structures interact and reconstruct into higher-scale organization;
3. the overlap is substantive enough to strengthen the **interference / interaction / emergent-organization** reading of the earlier TD613 analogy;
4. crucial Source A mechanisms — shared-layer mediation, global-configuration-to-local-registry selection, strain-conditioned commensuration, and physical sliding boundaries — are absent from the pre-publication TD613 witness;
5. therefore mechanism identity, physical validation, prediction, priority, and complete analogy fidelity remain unearned.

The stronger outcome:

```text
PRODUCTIVE_ANACHRONISTIC_CONVERGENCE
```

remains `HELD_FOR_REFINEMENT`, not rejected. It can be earned only if a post-encounter TD613 operator incorporates the newly exposed missing structure **with explicit Source A provenance** and demonstrates that the imported structure improves a declared TD613 task rather than merely making the metaphor prettier.

## 6. The Gaboriauian route: source becomes repair, not coronation

Source A should not be used to say `we were right`.

It should be used to construct the hostile next question:

> Can TD613 distinguish mere pairwise overlay from genuinely mediated pairwise reconstruction?

The next assay should therefore separate at least three conditions:

```text
C0 = singleton / no pair interaction
C1 = direct pair overlay without mediator
C2 = pair coupled through a declared mediator
```

and test whether an observable attributed to `reconstruction` appears only when mediator-dependent coupling is present.

A second factor should vary a declared global configuration while holding source objects fixed and ask whether a **local relation class** changes reproducibly.

A third perturbation should test whether surface morphology can change while the relation class remains invariant.

These are abstract information-system analogues. They must not import physical graphene semantics as if semantic systems were crystals.

The conceptual upgrade is:

```text
PAIRWISE_RESIDUE
-> DISTINGUISH_OVERLAY_FROM_COUPLING
-> IDENTIFY_MEDIATOR_DEPENDENCE
-> TEST_GLOBAL_TO_LOCAL_REGISTRY_SELECTION
-> TEST_RELATION_INVARIANCE_UNDER_PERTURBATION
```

If that route produces no additional explanatory, reconstructive, or falsifying power over the existing Choir operator, the imported analogy should be discarded.

If it does produce such power, Source A becomes a documented **route to repair** for Moiré Stratigraphy.

## 7. Claim ceiling

Still false / unearned:

```text
TD613_PREDICTED_SOURCE_A = false
SOURCE_A_VALIDATES_ECLIPSE_OMEGA = false
TD613_MOIRE_IS_PHYSICAL_MOIRE = false
AUTHOR_CONTACT_OR_KNOWLEDGE_TRANSFER = unestablished
INDEPENDENT_ORIGIN_OF_SOURCE_A_RELATIVE_TO_TD613 = unestablished
PREPUBLICATION_PAIRWISE_CHOIR_OPERATOR = false
PHYSICAL_LATTICE_RELAXATION_IN_TD613 = false
TOPOLOGICAL_FLAT_BAND_AUTHORITY = false
PUBLICATION_PRIORITY_CLAIM = false
```

No donor admission, runtime mutation, merge, deployment, Vercel action, or publication follows from this assay.

Marked ⟐
