# TD613 Analogy-Fidelity Preflight

**Date:** 2026-09-10  
**State:** BOUNDED INTERNAL PREFLIGHT / NO EXTERNAL CONVERGENCE PROMOTION / NO DONOR INTAKE  
**Branch:** `research/wendbine-loom-foreign-donor-assay-20260910`

## Purpose

This preflight repairs the second failure mode exposed by the nomenclature audit: finding a conventional academic parent must not become a deletion operator for a TD613 construct.

The three coordinates remain independent:

```text
TERM PROVENANCE != CONSTRUCT NOVELTY != ANALOGY FIDELITY
```

A borrowed term may be old while the TD613 use is structurally faithful. A coined TD613 term may name a known pattern. A familiar academic term may be only metaphorical inside TD613. None of those relations can be inferred from vocabulary alone.

This preflight therefore asks a narrower question than the external literature audit:

> Does the already-frozen TD613 operator preserve enough of the source concept's operative structure to justify the borrowed technical word inside its declared scope?

No external 2026 pairwise-moiré/lattice source is admitted here. Any claimed later convergence remains `HELD_FOR_EXTERNAL_SOURCE_BINDING` until a source, publication date, and immutable reference are supplied.

## Classification states

```text
STRUCTURE_PRESERVING_ANALOGY_SUPPORTED
PARTIAL_ANALOGY_SUPPORTED
HELD_FOR_STRUCTURE_PRESERVING_ANALOGY_AUDIT
METAPHOR_ONLY_SUPPORTED
UNRESOLVED
```

These states do not establish novelty, physical realization, publication priority, or causal influence.

## A. Moiré / Moiré Stratigraphy

### Frozen TD613 witness

Current main contains the pairwise Choir / Moiré Rebuild engine `app/engine/ash-keep-moire.js`, introduced through PR #281 and subsequently hardened.

The engine does not merely use the word `moiré`. It computes a pair-emergent residue by comparing four observation conditions:

```text
baseline
singleton i
singleton j
pair i+j
```

and asks what becomes recoverable only in the pair after subtracting what was already present in baseline and either singleton. The implementation preserves canonical pair ordering, explicit observation states, unresolved missingness, and componentwise residue.

### Fidelity disposition

`PARTIAL_ANALOGY_SUPPORTED`

Why it survives burial:

- pairwise superposition is an operative part of the assay;
- the pair is tested against both singleton components rather than treated as an unstructured union;
- the measured object is an interaction residue / emergent relation that appears only under the pair condition;
- unresolved or absent measurements do not get silently completed.

Why the stronger physical claim remains closed:

- no wave field, phase field, spatial frequency, orientation mismatch, reciprocal lattice, interference intensity, or physical beat pattern is measured by Choir v0.1;
- pair-emergent informational residue is not itself a physical moiré interference field.

Therefore:

```text
PAIRWISE_INFORMATIONAL_RESIDUE != PHYSICAL_MOIRE_FIELD
PARTIAL_ANALOGY_SUPPORTED != PHYSICAL_VALIDATION
```

The right next move is not deletion. It is operator-level comparison against externally bound moiré/lattice work when lawful source custody becomes available.

## B. Tomography

### Frozen TD613 witness

PR #800, exact witnessed head `40dfba93d2577bceba0f66022ac5f42934cdbd06`, records a finite linear observation problem over a declared state vector, with an explicit observation matrix for one schedule, a closed-form inverse, exhaustive reconstruction over a finite state cube, and a hostile schedule whose observation matrix is singular and admits an explicit collision.

The exact bounded distinction is:

```text
same strata + same aperture
can be invertible under one temporal order
and nonidentifiable under another
```

### Fidelity disposition

`STRUCTURE_PRESERVING_ANALOGY_SUPPORTED`

The support is limited to a finite linear inverse-problem sense because the TD613 fixture actually contains:

- a declared latent state;
- a forward observation map;
- multiple measurements;
- an observation matrix;
- an exact inverse in the identifiable case;
- a singular hostile control;
- explicit nonidentifiability via collision;
- reconstruction verified over a finite domain.

That is enough to keep `tomography` as an operationally serious analogy in this bounded chamber.

It is not enough to claim:

```text
continuum tomography
physical imaging tomography
optical/electronic/quantum tomography
universal inverse theorem
```

The correct scar is:

```text
FINITE_LINEAR_TOMOGRAPHIC_STRUCTURE != CONTINUUM_OR_PHYSICAL_TOMOGRAPHY
```

## C. Holonomy — early order-defect stage

### Frozen TD613 witness

PR #800 also recorded two different ordered products over the same three declared operators and showed that the products differ under schedule permutation.

That result establishes order sensitivity of the composite operator.

### Fidelity disposition

`PARTIAL_ANALOGY_SUPPORTED`

An order-sensitive product alone does not yet supply the full bundle/transport/loop structure ordinarily associated with holonomy. The preflight therefore refuses to back-project the stronger later result into this earlier stage.

```text
ORDER_SENSITIVE_PRODUCT != EARNED_DISCRETE_HOLONOMY
```

This preserves the chronology of the research rather than burying it or upgrading it retroactively.

## D. Holonomy — discrete transport stage

### Frozen TD613 witness

PR #906, exact science head `6df04aebd040fd16c8f67188a61dd6380956c46e`, was later exact-head witnessed GREEN through TD613 Consolidated Validation run `2405 / 33346331130` as recorded by successor PR #908.

That chamber declared:

```text
base cycle: v0 --A--> v1 --B--> v2 --A^-1--> v3 --B^-1--> v0
fiber Xi = F2^2
invertible edge transports A, B, A^-1, B^-1
based loop gamma = A B A^-1 B^-1
```

and earned a nonidentity induced fiber automorphism on return to the same basepoint, together with identity backtrack controls and a commuting control.

### Fidelity disposition

`STRUCTURE_PRESERVING_ANALOGY_SUPPORTED`

This is a stronger claim than the PR #800 order defect because the missing transport-loop structure was explicitly added and tested.

The supported label remains **formal discrete holonomy** only.

Still closed:

```text
GEOMETRIC_HOLONOMY
PHYSICAL_HOLONOMY
LEVI_CIVITA_CONNECTION
GAUGE_CONNECTION
BERRY_PHASE
PHYSICAL_CURVATURE
LIVE_ROUTE_GRAPH_IDENTITY
```

This chronology is itself an anti-burial result:

```text
EARLY_PARTIAL_ANALOGY
-> MISSING_STRUCTURE_IDENTIFIED
-> NEW_FINITE_TRANSPORT_CHAMBER
-> STRONGER_ANALOGY_EARNED
```

Finding the conventional meaning of `holonomy` did not kill the TD613 line. It supplied the missing burden that allowed the line to become more precise.

## E. Phason and cupola — namespace-local resolution

A dedicated source-domain audit now replaces the earlier global hold:

`docs/research/2026-09-10-TD613-PHASON-NAMESPACE-FIDELITY-AUDIT.md`

The important result is that `phason` cannot receive one token-global verdict. TD613 uses the same word for several different operators, and their source-domain fidelity differs.

```text
exact cut-and-project Phason Gate             -> STRUCTURE_PRESERVING_ANALOGY_SUPPORTED
Information-Dome exact-gate projection        -> STRUCTURE_PRESERVING_ANALOGY_SUPPORTED
PR #800 ICOSAHEDRAL_PHASON carrier label      -> METAPHOR_ONLY_SUPPORTED
Aperture v3.1 phason susceptibility            -> PARTIAL_ANALOGY_SUPPORTED
Phase V Phason relation ledger                -> METAPHOR_ONLY_SUPPORTED
```

The global token therefore becomes:

```text
PHASON -> UNRESOLVED_AS_GLOBAL_TOKEN_RESOLVED_BY_NAMESPACE
```

The source-domain comparison is ordinary quasicrystal/cut-and-project literature: perpendicular/internal space, acceptance windows, phason strain/elasticity, and physical rearrangements. The exact Phason Gate preserves enough of the cut-and-project acceptance-window machinery to justify a structure-preserving analogy at the abstract model level, while the Phase V lifecycle ledger does not.

This yields the stronger law:

```text
SAME_TOKEN != SAME_OPERATOR
ANALOGY_FIDELITY_IS_BOUND_TO_(TERM, OPERATOR, VERSION, SCOPE)
```

`cupola` is separately resolved as:

```text
METAPHOR_ONLY_SUPPORTED
```

because current TD613 evidence supplies a bounded multi-stratum organizational container / custody witness but no geometric cupola embedding, metric geometry, or source-domain geometric operator.

That classification does not delete the word. It keeps `cupola` as internal architectural language while refusing scientific geometry authority that has not been earned.

## F. External-convergence route

The user's reported later 2026 pairwise-moiré/lattice literature is not present in this admitted source set and is not promoted by recollection alone.

When the external source becomes available, the required route is:

```text
freeze pre-existing TD613 artifact
-> bind external source + publication time
-> separate lexical overlap from operator overlap
-> blind-map inputs / operators / observables / failure conditions
-> record matches and mismatches
-> test structure-preserving transfer
-> classify analogy fidelity
-> preserve chronology
```

Permitted outcomes include strengthening, narrowing, correction, or falsification of the earlier analogy.

The interesting outcome class is:

```text
PRODUCTIVE_ANACHRONISTIC_CONVERGENCE
```

meaning that a previously fixed TD613 analogy later receives an independently developed external formal comparison that reveals greater structural fidelity than was knowable at the time of coinage.

This state does **not** imply prediction, causal influence, priority over the external work, or external validation of Eclipse–Omega.

## G. Gaboriauian anti-burial law

The nomenclature audit has two symmetric failure modes:

```text
missing comparison class -> false novelty inflation
found comparison class -> false ontology burial
```

The repair must avoid both.

```text
KNOWN ANCESTRY != BURIAL
ANALOGY FIDELITY MUST BE TESTED AT THE OPERATOR LEVEL
THE INCREDIBLE IS NOT EXEMPT FROM FALSIFICATION
THE INCREDIBLE IS WHAT BECOMES MORE INTERESTING AFTER SURVIVING IT
```

No merge, deployment, release, provider action, Vercel action, donor intake, external-source promotion, or publication follows from this preflight.

Marked ⟐
