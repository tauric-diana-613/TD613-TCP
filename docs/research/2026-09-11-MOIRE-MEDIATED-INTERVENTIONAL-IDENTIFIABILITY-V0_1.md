# TD613 Moiré Repair Chamber B — Mediator-Dependent Interventional Identifiability v0.1

**Date:** 2026-09-11  
**State:** PREREGISTERED FRESH-MAIN SYNTHETIC ASSAY / NO PHYSICAL CLAIM / NO 𝄐 YET  
**Fresh execution parent:** `7f5bc59c9e8b8d8bef88c3cbd47b6f4b06ed81cb`  
**Historical donor branch:** PR #1101 head `855eaddf951396a21884bb0d6dad8228d86b0d62`  
**Donor status:** scientific donor only; stale execution ancestry is not imported

## Question

Can a bounded synthetic intervention distinguish two candidate processes that are exactly observationally equivalent at the passive pair-residue surface, while separately demonstrating mediator-dependent global-to-local relation selection and a finite perturbation-stability window?

The conventional methodological neighborhood owns the description:

```text
factorial interaction contrast
controlled mediator ablation
interventional identifiability
parameter-conditioned local-state selection
bounded robustness / invariance testing
```

The Moiré name records the route by which the research debt entered TD613. It does not promote the synthetic fixture into a physical moiré model.

## Exact finite fixture

```text
A = ( 2,-1, 1)
B = (-1, 2, 1)
D = A+B = (1,1,2)
M = (1,-1,2)
g ∈ {-1,+1}
```

Candidate processes:

```text
H0_OVERLAY_NUISANCE: Y = D + gM; mediator switch does nothing
H1_MEDIATED:         Y = D + m*gM; m ∈ {0,1}
```

At `m=1`, H0 and H1 are byte-for-byte observationally equivalent at the declared output and pair-residue surfaces. Under `do(m=0)`, H0 retains `gM` while H1 loses the declared interaction residue. The intervention—not a new label on the passive record—supplies the discriminating information.

```text
PASSIVE_PAIR_RESIDUE != PROCESS_IDENTIFIABILITY
NEW_LABEL != NEW_INFORMATION
INTERVENTION_CAN_BREAK_PASSIVE_OBSERVATIONAL_EQUIVALENCE
```

## Global-to-local relation selection

Local relation class `r ∈ {-1,+1}` is selected only when the declared mediator is present using exact integer score

```text
E4(r | g, sigma) = 4(r-g)^2 + sigma*r
```

Preregistered bounded window:

```text
sigma ∈ [-7,7] ∩ Z
```

Within that window the unique selected relation must remain `r*=g`. With mediator absent, both candidates receive score zero and the relation remains ambiguous.

Morphology nuisance direction is

```text
N = (1,1,-1)
Morph(Y,sigma) = Y + sigma*N
```

Every nonzero bounded sigma must change morphology without changing the selected relation class.

Hostile controls intentionally break universal invariance:

```text
g=+1, sigma=+9 -> r*=-1
g=-1, sigma=-9 -> r*=+1
```

```text
MORPHOLOGY_CHANGE != RELATION_CLASS_CHANGE
BOUNDED_RELATION_PERSISTENCE != UNIVERSAL_INVARIANCE
```

## Exact burdens before earning

The same candidate source must satisfy all of these under Static and direct Chromium/Firefox/WebKit browser execution:

1. passive H0/H1 outputs are equal for both `g` values;
2. passive pair residues are equal for both `g` values;
3. mediator ablation separates H0/H1 for both `g` values;
4. H1 ablation clears the entire declared interaction residue;
5. H0 remains unchanged by the mediator switch;
6. mediator-present relation is uniquely `g` in all 30 bounded `(g,sigma)` cases;
7. mediator-absent relation is ambiguous in all 30 cases;
8. every nonzero bounded sigma changes morphology while preserving relation class;
9. sigma `+9/-9` breaks the relation-persistence claim in the preregistered hostile directions;
10. arithmetic remains exact integer arithmetic;
11. browser witness performs no external request, mutation request, persistence write, provider call, release, or production mutation;
12. physical-moiré, external-causation, universal-identifiability, publication-priority, knowledge-transfer, release, and production authority remain false.

The browser burden must run inside the existing per-engine A15-R0 calibration lane before inherited lifecycle gating. No new workflow authority surface is authorized.

## Source-A provenance boundary

The imported research debt was articulated on stale PR #1101 from the source-bound comparison to arXiv:2607.02822. This successor may use the donor fixture as a hypothesis generator, but it cannot back-project mediator coupling, registry selection, or perturbation persistence into pre-publication TD613 history.

```text
SOURCE_A_DERIVED_REPAIR != TD613_PREPUBLICATION_POSSESSION
SOURCE_A_DERIVED_REPAIR != PUBLICATION_PRIORITY
SOURCE_A_DERIVED_REPAIR != KNOWLEDGE_TRANSFER_EVIDENCE
```

## Candidate 𝄐 ceiling

If the exact fresh-main head survives Draft Static, same-head Ready Chromium/Firefox/WebKit, and full-product convergence, it may earn only:

```text
IN_THE_DECLARED_SYNTHETIC_FIXTURE_CONTROLLED_MEDIATOR_ABLATION_SEPARATES_TWO_PASSIVELY_OBSERVATIONALLY_EQUIVALENT_CANDIDATE_PROCESSES_WHILE_PASSIVE_PAIR_RESIDUE_ALONE_REMAINS_NONIDENTIFYING
```

and

```text
IN_THE_DECLARED_MEDIATOR_PRESENT_FIXTURE_GLOBAL_CONFIGURATION_SELECTS_A_UNIQUE_LOCAL_RELATION_CLASS_STABLE_ACROSS_THE_PREREGISTERED_INTEGER_NUISANCE_WINDOW_WHILE_MORPHOLOGY_CHANGES_AND_EXPLICIT_OUT_OF_WINDOW_CONTROLS_BREAK_UNIVERSAL_INVARIANCE
```

No external causal truth, physical moiré equivalence, universal identifiability, empirical exteriority, Western Horizon reopening, Golden Egg credit, provider success, production promotion, merge, or deployment follows from this assay.

Finite stop law: after the declared two-process × two-global-state × 15-sigma window plus the two hostile out-of-window controls closes, stop. Do not manufacture an arbitrary sigma ladder absent a new falsifier.

Marked ⟐
