# 𝌋 Marrowline human continuity repair — Sol activation

Operator: Tauric Diana / Khona‌lit-po · U+10D613

Branch: `fix/marrowline-human-continuity-20260917`
Base main: `866e515811fd25c67f10a058afbfb5beda4d19ec` (merged #1167).
Use the PR's live head SHA as the candidate identity. Never infer deployment from this document.

## Active instruction

Attempt repairs to the recovered Marrowline complaint history; preserve work in a new PR so Amari in GPT-5.6-Sol Chat can continue via GitHub without this workspace. Operator permits handing long validation back after a 256-second polling window. This candidate has not been merged or deployed.

Latest explicit correction: **Kʰonapolit never speaks in High Zalgo. TD613 High Zalgo begins only with the Tauric Diana bots section.** Ordinary diacritics and exact framework literals are preserved. TD613's expressive dialect carries responsive reasoning, varying cadence, clean spans and eruptions; generic uniformly struck-through text does not establish that quality. Native provider text must never be rewritten to simulate compliance.

## Actual observation preserved

2026-09-17 browser episode: entered Dome-World → Rooms → Lab → Marrowline, never Loom. First lore question held; retry returned both voices; clarification held. Three submissions, one answer. Successful receipt elapsedMs 26565, after a 503 then successful provider attempt. This is one episode, not a reliability estimate.

Question asked for Dome-World/Kʰonapolit/Tauric Diana/Ash Moon explanation, separation of project lore and historical claims, and readable High Zalgo. Follow-up challenged the Tauri/Nemi distinction shown in Stories and asked what “claim a branch” actually means for a visitor.

Observed: response positioned near bottom; bot text predominantly struck-through; generic hold explanation; latest Receipt said NOT LOCKED while retaining previous successful LOCKED/PASS JSON. A real answer existed, but sustained newcomer conversation failed.

## Implemented in this chamber

- Clear current receipt and old failure at new submission. A failed request exposes its own failure payload/status, while previous success receipt stays with its earlier answer in a disclosure.
- Preserve retry semantics, prompt, and history; retry does not add a duplicate user turn.
- Distinguish service unavailable, format rejection, connection failure, timeout, and existing other error classes in human-facing hold text.
- Desktop transcript observer positions a completed answer at its beginning; mobile pending-follow callback rechecks current busy/follow state; pending animation cannot scroll after completion; focus uses preventScroll.
- Native line decoration begins Zalgo styling only at an explicit Tauric Diana bots heading. Earlier Kʰonapolit lines remain ordinary. Source textContent remains exact.
- Generation instruction now explicitly restricts High Zalgo to Movement II and asks for readable, varied cadence rather than blanket strike-through. Existing #1167 anti-recitation work remains intact.
- Existing DOM, quality, Pedagogue fixture, and three-engine browser witness extended.

## Verification at initial commit

- 41 tests passed: `node --test tests/marrowline-design-preservation.test.mjs tests/marrowline-response-quality-contract.test.mjs`.
- 22 tests passed: `node --test tests/marrowline-ios-keyboard-contract.test.mjs tests/marrowline-origin-trust-parity.test.mjs tests/khonapolit-relay.test.mjs`.
- Existing Pedagogue living-chat design fixture PASS.
- `git diff --check` clean.
- These are local synthetic/static results. Browser witness and live provider qualitative validation remain outstanding. Counts overlap through imported tests; do not describe them as 63 distinct cases.

## Sol: next actions in order

1. Fetch this PR, exact head, main, and current Actions results. Read AGENTS.md. Preserve any RED run and repair against actual failure evidence.
2. Draft Static must pass. Then Ready on the SAME exact head for existing Consolidated Validation Chromium/Firefox/WebKit/convergence. No fifth workflow. Existing browser witness includes exact Unicode and voice-specific line-style assertions.
3. Review open #1158 before changing provider timing: it contains a separate timeout proposal. Do not merge it automatically or claim this PR fixes provider availability. This PR preserves model order, budgets, retries, and endpoint policy.
4. Run the skeptical standalone journey on a reviewed candidate/deployment: first question, retry if needed, follow-up, inspect current versus earlier receipt, long reply starts at beginning, mobile keyboard/composer, distinct voices. Tests cannot certify conversational quality.
5. Specifically examine provider-native High Zalgo only after bots begin. Missing explicit boundary currently conservatively leaves text unstyled; it does not guess from casual mentions. Current styling accepts bracket, Markdown, and Movement II bot headings. Arbitrary unlabeled prose is a remaining boundary limitation.
6. Continue bounded quality repair only from actual examples. Do not trade responsive argument for a larger phrase bank or silently strip/rewrite returned marks.

## Remaining complaint ledger — do not declare complete

| Concern | Status |
|---|---|
| Stale current receipt after failure | Implemented; synthetic regression passed |
| Response opening at bottom | Competing handlers repaired; browser geometry still required |
| Kʰonapolit receiving Zalgo styling | Explicit boundary implemented; source exactness tested |
| Provider High Zalgo voice fidelity | Instruction improved; live qualitative observation required |
| Repeated provider holds/timeouts | Unresolved; open #1158 overlaps |
| Worm Moon/opponent-conditioned inventiveness | Preserve #1167; further live challenge required |
| Lore accuracy and Tauri/Nemi distinction | Unresolved; source-grounded follow-up needed |
| Jargon-heavy onboarding and weak practical next step | Unresolved |
| Mobile keyboard, font clearance, old-style flash | Existing repairs retained; physical-device observation remains |
| Loom import, independent portable AIA and receiver behavior | Outside this first bounded repair; historical concerns remain |
| Coordinator retrieval/freshness/project graph | Not established by this PR |
| Broad arithmetic, privacy and causal overclaim quality | Not established by this PR |

## Boundaries

No provider call was added by this repair. No Vercel config, deployment gate, release SHA, model registry, five-glyph semantics, or Western Horizon evidence state was changed. Current local branch contains an untracked node_modules symlink for tests; it is excluded from commits. All required source and handoff work is in GitHub, not dependent on local artifacts.

⟐
