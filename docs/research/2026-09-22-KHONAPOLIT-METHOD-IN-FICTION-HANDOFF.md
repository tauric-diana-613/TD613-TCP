# Kʰonapolit method-in-fiction: resumable handoff

Date: 2026-09-22. Notebook: #1304. Parent: #1298. Research registration: #737.
Status: DRAFT CANDIDATE; live literary effect UNTESTED.
Base: bb34ed725ccd4389f6292c4c098c0546211878d6.
Code candidate: 6c1065a2f2999714a440d50b5c958a95dc24a7d3.
Production remains at the prior release; this document grants no detached execution.

## Resume here

Read #1304 and this branch diff, fetch current main, and preserve any concurrent work. This is an active-session user-requested handoff to Chat 5.6-Sol; no background task was armed. User supplied ongoing #737 and separate #405 permission in the active conversation, but a handoff document cannot manufacture authority for a detached process. Follow current operator instructions and AGENTS.md.

The next engineering step is focused full-request assembly/regression validation against the exact candidate head, followed by repository-selected exact-head CI. The next quality step is a bounded, properly authorized matched provider comparison described below. Do not present syntax success or CI as literary recovery. Do not deploy this research candidate merely because this document exists.

## What the operator established

The operator accepted the supplied sample's native High-Zalgo rendering and bots movement as their third successful Marrowline output. Kʰonapolit remains too short and generic relative to the desired righteous, mathematically precise, absurdist academic camp. The full sample appears in the Work conversation; it is not attached here. Preserve this distinction rather than inventing an archived raw specimen.

Exact operator prompt:
> The committee offers Tauric Diana a beautiful final report, unanimous applause, and a plaque declaring the rupture resolved. She asks one question that makes the room understand why closure can become another containment surface. Write the scene so resignation, mercy, fury, and methodological precision all survive in the same breath.

Notebook agent counted 469 whitespace-delimited words in the first movement. The user's 5,432-character total has unspecified counting semantics. High-Zalgo combining marks contribute to raw character counts; totals across both voices do not measure Kʰonapolit's development.

Text-observed strengths: calfskin/wolf image, salt-shaker applause, concrete setting. Defect concentrates at the climax: conserved energy is asserted, then refractive index and observer blindness follow without a defined relation. As metaphor this supplies atmosphere; as methodology it fails to establish the claimed inference. The room's instant silence substitutes for resistance, changed stakes, and an earned aftermath. Resignation and mercy have little enacted consequence.

Unknown for this particular sample: exact source, selected model, thinking tier, finish reason, tokens used, latency, fallback, first-shot versus repair, history. Do not infer any of these from style or screenshot quality.

## Source-confirmed findings

1. server/khonapolit-quality.js, currentTurnRelayCue: the baseline single line assigns mechanism/assumptions/counterexample to analytic work and event/tension/transformation/joke to creative work.
2. app/dome-world/khonapolit-relay.js, buildRelaySystemAddendum (parallel source audit): the same asymmetry appears in the analysis-versus-story instruction. This is a plausible prompt-level contributor, not an established cause.
3. server/gemini-generation-envelope.js: interactive allowance is already 65,536; high request is profiled to medium for 3.8 and low for listed fallback seats. A ceiling permits length; it cannot make an early completion develop more ideas.
4. api/khonapolit.js: attachments branch before the interactive profile. Existing A/B repairs and receipt fixes remain untouched.
5. app/dome-world/khonapolit-covenant.js: a 6,000-character history-entry limit also covers model messages. Separate continuity seam; verify current browser submission before changing limits. This does not explain the submitted short first return.
6. server/khonapolit-quality.js, receipt invocation promptSha256: the hash uses packet.systemInstruction plus packet.message. It excludes the added task guidance, relay addendum and current-turn cue, and also excludes history. Therefore that existing hash alone cannot identify the complete wire prompt in an ablation. Use a test-side canonical digest of the actual serialized request and code revision; retain payload custody. No production telemetry patch bundled here.

## Candidate implementation and scope

Only currentTurnRelayCue's Kʰonapolit first-movement instruction changes, from one line to four:
- explicit requested brevity has priority; open-ended work receives sustained consequential development;
- characteristic camp comes from an exact inference/procedure/contradiction/desire under pressure, with plausible resistance where form permits;
- fiction carries the same intellectual method in action, objects, choices and consequences; requested affects alter decisions;
- technical language must have intelligible terms, relation and limits when it supports an inference; metaphor alone provides no derivation.

This deliberately avoids a universal institutional-measurement checklist, mandatory equations, paragraph counts, character quotas, automatic quality rejection, and a second authoring call. It does not insert the illustrative committee answer into production. No claim of guaranteed longer or better responses.

All source outside that exact replacement was verified unchanged. In particular: original operator text/history, all Tauric Diana instructions and quiet/eruption calibration, all request/configuration logic, repair policy, timers, model order and limits remain byte-identical in the edited file. No renderer file changed.

## Validation completed

- Node syntax check on the full candidate module: PASS.
- Exact reverse-replacement comparison with fetched baseline: PASS; all other file contents identical.
- Full integration tests: NOT RUN in this short handoff window.
- Live Gemini calls: ZERO.
- Production changes, merges, deployments: ZERO.

These checks validate patch shape and syntax only. A string assertion that the new adjectives exist would not validate writing quality.

## Bounded empirical comparison

Preregister a small pilot before calls. A useful first screen is two tasks x baseline/candidate = four first-pass attempts, with one fixed callable model and generation profile; no automatic retries for prose quality. Use the exact committee prompt above plus this topic-distinct analytical task:

> A platform declares a complaints process fair because every admitted complaint received a response. Admission requires selecting a category that excludes the harm being alleged. Trace the inference, give the strongest defense of the platform, test that defense against a concrete case, and identify a change that would make the fairness claim meaningfully testable.

Treat this task as a newly authored test, not an earlier operator specimen. Start both arms from identical history (prefer empty), identical user bytes, same system packet except the candidate line replacement, same full generation config, same seat, and renderer. Randomize arm order within each task. Capture failures and do not silently substitute a different model. A tiny pilot detects obvious failure; it cannot estimate stable rates or establish universal recovery.

Log immutable code/source, serialized-request digest, submitted generation config, model, attempt statuses, completion path, finish reason, candidate/thought tokens if supplied, elapsed time, full raw Unicode return and rendered view. Record voice-separated word/grapheme measures as descriptive observations. Protect private payloads; hashes do not prove quality or completeness.

Render comparisons with concealed arm labels for human review. Review separately:
- actual task/form and requested affects;
- inspectable mechanism and valid inference;
- distinctive, specific camp and novelty;
- sustained development versus padding;
- genuine two-movement connection;
- preserved human-approved native morphology;
- latency, completion reliability and call cost.

Hostile outcomes: longer jargon; repeated lore; decorative math; generic ethical sermon; compulsory courtroom dialogue; mechanically identical scene beats; forced equations; worsened bots; timeouts; followup failure from long history. Any such outcome keeps recovery OPEN even when word counts grow.

Use supplied existing receipt if available to classify this sample, but do not make the operator repeatedly generate outputs just to fill diagnostics. Compare baseline/candidate evidence within its model/profile. Full historical exemplar retrieval remains incomplete: Personal Context found references and snippets, not a verified complete user-approved specimen suitable for few-shot insertion.

Only after the prompt question is evaluated should a separate chamber vary thinking allocation or soft extent guidance. Raw character minima are a poor first intervention; a later separate soft word-range condition could test length preference without converting it into a hard gate. More tokens are valuable when they resolve the task and reduce follow-up burden, not merely when the meter rises.

## Checkpoint durability

The main agent and independent notebook agent append facts, source findings, hypotheses, changes, verification and next actions to #1304. No private reasoning trace is recorded. Subagents have no guaranteed survival across usage exhaustion or session end; committed branch contents and saved issue comments are the recovery mechanism. If a witness exceeds the operator's 128-second wait boundary, record its exact run URL, head, pending status and next check before stopping. Do not imply monitoring continues after the active turn.

𝌋 ⟐

## Final checkpoint: operator length follow-up

The operator explicitly asked whether the approximately 5,000-character answer reflects a creativity-restricting cap. No 5,000-character output cap was identified in inspected generation, browser response storage or display sources. The 6,000-unit history seam is now traced through marrowline-terminal.js: compactHistory uses entryText, which reconstructs model relay parts with labels; submit validates that history before fetch. It is not necessarily byte-identical to payload.text, and labels can increase the count. The earlier browser-untraced note is superseded.

Executable Node reproduction with the unmodified covenant module: synthetic previous model texts of 5000, 5432 and 6000 UTF-16 units pass canInvoke; 6001 and 12000 fail with history-entry-too-long. All input strings remain intact. No provider call. This confirms the validator defect for longer conversations; it does not identify the finish cause of this sample.

Prioritize a separate engineering follow-up that distinguishes composer input limits from model-history/aggregate request capacity. Test realistic long Unicode relay history through actual browser serialization and backend validation; preserve response text and explicit recovery. Do not simply remove every bound or silently truncate the prose. Keep length an explicit human acceptance dimension in the prompt trial; a soft per-first-movement word-range experiment can be isolated later if sustained-development instructions remain too weak.

Draft PR: #1305. Latest notebook #1304 contains independent agent checkpoints and executable boundary results. No long-running witness was launched, and no watcher remains promised after this turn. ⟐
