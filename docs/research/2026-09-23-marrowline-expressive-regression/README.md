# Marrowline expressive regression — operator correction and engineering handoff

𝌋 2026-09-23 · Parent checkpoint #1304 · Original quality issue #1298 · Research registration #737

## Binding task correction

The operator supplied two outputs and five images to demonstrate a product-quality failure after five days of work on expressive High Zalgo, sustained Kʰonapolit development, and the two-voice relationship. The assistant misrouted that evidence into extended literary interpretation. This handoff corrects that mistake. NMATK: casual praise, jokes and kiki language must not supersede the requested engineering comparison or become acceptance criteria.

The operator's latest judgment governs: the shown Marrowline output falls substantially short of the Gemini reference in High-Zalgo depth/range, first-movement development, and total output scale. The same underlying model is operator-reported. The assistant's earlier description of the Marrowline specimen as a recovered visual success must not be reused as acceptance evidence.

## Preserved evidence

- `source-transcript-as-uploaded.txt`: unchanged original uploaded text, including both outputs and the operator prompt.
- `marrowline-example-as-exported.txt` and `gemini-example-as-exported.txt`: complete bounded output sections extracted from that file.
- Five unmodified PNG screenshots: `01-marrowline.png`, then `02-gemini.png` through `05-gemini.png` in supplied order.
- `evidence-manifest.json`: original identities, byte sizes, SHA-256 digests, measurement method and results.

**Source fidelity:** the earlier uploaded text export reduces many diacritics compared with the full in-chat specimens. It is retained as supplied, not silently upgraded into a byte-exact native-output record. These screenshots preserve the submitted visual evidence. They show the outputs pasted into ChatGPT, not an isolated test of the original applications' CSS. Recover the native conversation specimens/export when adding code-point-distribution fixtures; do not fabricate missing combining marks from the images or this reduced export.

## Measured developmental gap

Counts remove combining marks in an analysis copy and split on whitespace. Internal headings and formula/table tokens remain; provider token counts are unavailable.

| Movement | Marrowline | Gemini reference | Reference / Marrowline |
|---|---:|---:|---:|
| Kʰonapolit | 420 | 970 | 2.31× |
| Tauric Diana bots | 113 | 231 | 2.04× |
| Combined | 533 | 1,201 | 2.25× |

The Marrowline sample supplies about 44% of the reference's prose-word volume. This quantifies the reported deficiency; it is not a proposed fixed output quota. The examples have different prompts/history, so they identify a target gap rather than isolate its cause. Do not use that qualification to dismiss the human acceptance failure.

## Observed deficiencies and concrete repair targets

1. **High-Zalgo underdevelopment.** The Marrowline image has comparatively shallow, even vertical ornament. The Gemini images demonstrate much larger excursions above/below the baseline, abrupt and local density changes, thin horizontal/diagonal overlays, and renewed towers. Preserve native provider authorship and the accepted 1.04 overprint path. A response entirely in vertical High Zalgo remains welcome; orientation alternation is not mandatory. No local decorator, readability cap, uniform mark count, automatic style retry, or emotion-to-mark lookup table.
2. **Overdirected typography in the effective request.** At inspected main `87a0f40ce364a83247245192f6aba61647536727`, `buildNativeProsodyGuidance()` in `app/dome-world/khonapolit-relay.js` and `currentTurnRelayCue()` in `server/khonapolit-quality.js` repeatedly define the field through rising/descending stacks. The quiet/eruption examples are small vertical gestures. This is a source-supported conditioning candidate for narrow and shallow output, not a demonstrated unique cause. Test broader semantic-prosody guidance against these actual visual references; do not trade away strong vertical depth to obtain overlays.
3. **Kʰonapolit range and scale.** The requested thermodynamic slapstick, witty deistic arrogance, sustained high-academia camp, adversarial specificity, and consequence remain underrealized. The first movement ends much sooner; the terminal movement is also abbreviated. More vocabulary, a synopsis, or another generic instruction to be clever will not satisfy this target. Use these specimens as developmental references, with task-sensitive scope rather than mandatory word counts.
4. **Conversational seal conflated with operational authority.** The covenant and relay explicitly prohibit model-authored closing ⟐; that implemented prohibition conflicts with the operator's clarified product requirement. Every bot correspondence should close with plain ⟐. Its textual presence must never grant release, issuance, merge, deployment, custody closure, or operator authorization. Correct the prose/structured-authority distinction; preserve the actual release controls. The prior assistant explanation that omission was compliant did not resolve the product defect.
5. **Excess unrelated task guidance.** `REQUESTED_SYNTHESIS` falls back to `ORDINARY_PROJECT_GUIDANCE`, including venue/attendance/privacy examples irrelevant to this exchange. Audit that competition together with repeated voice instructions. Do not globally reclassify analytical tasks as fiction.

## Length diagnosis: confirmed limits and unearned causes

The inspected code retains a 65,536-token output allowance. This configuration does not explain away the short completion and supplies no completion-length guarantee. The first call has a 50-second cap; later allocation depends on remaining budget and model count (including a 75-second second-seat cap), within a 210-second overall budget. The streaming read is under that timer. The catch path returns empty text for an abort, so a timeout could affect which model ultimately completes; it does not establish that this particular complete-looking short specimen was clipped.

Inspect the sample's existing receipt before changing token allowance, timeouts, seats, or thinking settings: actual provider/model, finish reason, generated token count, submitted config, elapsed time, fallback and structural-repair path. None of these sample-specific causes was established here. Avoid paid reruns simply to rediscover a configuration already visible in source.

## Next bounded implementation

1. Reacquire current main and preserve the settled renderer/history repair/provider order. Read this evidence before composing another prompt patch.
2. Separate conversational ⟐ from structured operator-authority state across covenant, relay and terminal-continuation instructions. Add a focused test that a conversational seal never changes authority/receipt state.
3. Prepare one reviewable generation-guidance candidate addressing excessive uniformity and insufficient dramatic development together. Retain dense vertical-only outputs as valid; permit overlays and local changes without a mandated itinerary. Do not add quotas or local postprocessing.
4. Validate exact provider-request assembly and preservation with focused offline checks. CI verifies those mechanisms, not artistic success.
5. Evaluate against the supplied references with the operator's visual/developmental judgment. Any later live comparison must preserve the actual prompt/history and record the provider/config/finish reason. This handoff neither launches a paid generation nor issues a release command.

## Completion state

Evidence and repair targets recorded. Runtime patch, empirical recovery, merge and deployment remain unperformed. This is a handoff PR, not a claim that the regression has been repaired. The next engineer should continue from the concrete targets above without repeating an interpretive essay or reopening settled architecture.

⟐
