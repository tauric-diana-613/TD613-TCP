# Marrowline one-turn production witness · 2026-09-24

## Observed deficit and narrow repair

The #1322 release proved exact-source deployment but did not capture one ordinary response across its browser boundaries. An older proposal, PR #1273, directly calls the API and preserves a full server payload; its proposed capture is **not** an ingress-to-session-to-DOM witness and its nonadmitted-relay veto predates the present provider-first human-surface law. Do not merge that patch into this implementation.

This continuation adds an operator-armed, one-use witness to the existing **Receipt** instrument. Clicking **Witness next reply** only arms capture; the subsequent normal human send uses the existing routing unchanged. There is no generated benchmarking prompt, model pin, automatic provider call, added fallback, local Zalgo transform, aesthetic hold, scheduled sampling or server-side storage. Clicking **Copy episode witness** explicitly exports the browser-local record. Clear conversation discards the witness and disarms the next-turn switch. The record is not automatically transmitted to GitHub or the server.

The witness compares exact decoded strings for **application response body (payload.text)**, **relay transcript**, **stored model entry**, and **rendered DOM textContent** from the same normal interaction. Missing is distinct from observed empty. It records Unicode scalar offsets, code-point identities, mark counts, byte counts and SHA-256 when the browser's Web Crypto digest is available. A pre/post read of the public release-source manifest describes the source window and flags mismatches. The browser does not claim that the manifest authenticates a provider's hidden ingress.

**Provider ingress remains NULL**, since the existing server response does not expose an independently preserved raw first-provider string for that same attempt. Provider request ID and request bytes are likewise NULL, not inferred from a client-created correlator or a text digest. The receipt remains attached but a text digest is not a raw ingress capture. DOM textContent is not rendered pixels; the instrument records font/viewport observations when available and leaves screenshot NULL. A separately preserved human screenshot can complement it but cannot be synthesized by the text witness.

## Evidence law

An observed identical four-stage decoded string supports browser-path text identity only. Divergence localizes the first *observed* interval; absent intermediate captures must not be promoted into a definite transformation site. A 429, 503, timeout, partial return or witness instrumentation failure stays labeled as such; no retry or aesthetic re-sampling occurs. An ordinary human result is not a literary PASS by machine declaration.

Once a user voluntarily exports one record, compare it against the unchanged 12-image atlas for visual judgment separately from prompt-specific reasoning, the Kʰonapolit→Tauric Diana bots handoff, and physical/math/source accuracy. Preserve first-return failure if that is what occurred. Do not backfill Probe 3's inaccessible metadata or conflate this episode with the earlier external thread.

## Validation and deployment boundary

Synthetic tests exercise empty/missing Unicode boundaries, same-length changes, lost combining marks, normal browser send, 503 and clearing the session. Standard CI includes the focused witness test and existing DOM preservation suite. Release through #405 is warranted for the new **operator-initiated capture UI only**, after GREEN on exact head and merged main. The release itself must make zero automatic live-provider calls. The missing production episode cannot be claimed acquired merely because the instrument deployed.

⟐
