# Originals

Source bytes only. Do not place normalized text, OCR output, embeddings, summaries, renamed convenience copies, or agent-authored notes here.

The designated Codex landing path is:

`02-ORIGINALS/emstd613lineage/`

Preserve the local folder's filenames and subdirectory topology wherever Git permits. Renaming for analytical convenience belongs in manifests, never in the original-byte layer.
