# Codex sync landing zone

Sync the contents of the user's local folder named `emstd613lineage` **into this directory**, preserving relative paths and original filenames.

This placeholder may remain alongside the synced corpus.

After sync, generate a content-hash manifest before any derivative or interpretive pass. See `99-ADMIN/CODEX_SYNC_HANDOFF.md`.
