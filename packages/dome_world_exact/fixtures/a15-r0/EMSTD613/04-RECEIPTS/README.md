# Receipts

Append-only custody and assay receipts.

Receipts should record exact Git head, source-manifest hash, method version, inputs, outputs, unresolved states, and whether execution actually occurred.

A written test plan without an execution witness remains `NOT_EXECUTED`. A source review without a source locator remains `UNANCHORED`.
