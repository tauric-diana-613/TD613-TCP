# Projective Routing Grammar Working Assay

This directory is **post-seal working space** for PR #731.

It is not part of the sealed `2026-08-24-phase2-kiln` receipt set.

Bound query epoch:

```text
atelier_snapshot_id = src-20260824-p2-001
seal_id = src-seal:eaf744ce16b1c8b519ad0f1b0325b44192679ea4a5110b0ad0ca49fbcd816a1a
```

Current committed receipt:

```text
IMPLEMENTATION_RECEIPT.json
```

A corpus-result receipt does not exist merely because the instrument exists. Results are created only by explicit execution of:

```bash
python 99-ADMIN/run-projective-routing-grammar-assay.py --self-test --write
```

Expected generated files:

```text
explore-token-observations.jsonl
signature-observations.jsonl
field-marker-observations.jsonl
summary.json
```

Until those files exist from an executed run, status remains:

```text
INSTRUMENT_INSTALLED_EXECUTION_NOT_RECORDED
```

No result may be inferred from implementation alone.

U+10D613

𝌋

Sealed ⟐
