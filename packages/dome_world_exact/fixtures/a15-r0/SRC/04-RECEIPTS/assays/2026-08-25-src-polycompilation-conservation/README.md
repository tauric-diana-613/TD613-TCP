# SRC Polycompilation Conservation Assay

Status: `POST_PREREG_SOURCE_ASSAY`

Date: 2026-08-25

Scope: source-witnessed June SR module family. This assay does not modify sealed Phase-2 authority or promote a hidden-message / puzzle claim.

## Question

Do related June SR artifacts merely repeat vocabulary, or do they preserve a conceptual invariant while assigning different representational functions to different outputs?

## Source set

- `zenodo:20534225` — `THE SIGNALRUPTURE LEXICON (SR-LX)`
- `zenodo:20534268` — `THE SIGNALRUPTURE HANDBOOK (SR-HB)`
- `zenodo:20534016` — `THE SIGNALRUPTURE DEPLOYMENT MANUAL (SR-DM)`
- `zenodo:20534171` — `THE SIGNALRUPTURE CANON MAP (SR-CM)`
- negative / non-equivalent comparator: `zenodo:20169452` — `SignalRupture Field Manual (SR-FM v1.0)`

## Result 1 — invariant vocabulary survives definition -> translation -> execution

The Lexicon formally defines:

```text
Administrative Abstraction
Governance Altitude
Representational Drift
Signal Compression
SCR
AAI
GTDI
```

The Handbook preserves the same governance object family but changes representational function: it explains those constructs in accessible public-facing language, explicitly identifies itself as an entry point, and states that it preserves the structural rigor of the canon while completing the field's translation layer.

The Deployment Manual then consumes those translated / defined objects operationally:

```text
Phase 1:
  SR-HB + SR-LX -> shared language
  outcome: compression / abstraction / drift / governance-altitude literacy

Phase 3:
  SCR + AAI + GTDI -> periodic metric sampling

Phase 4:
  SR-HB -> public communication
  SR-LX -> vocabulary stabilization
  SR-CM -> structural coherence
```

Bounded classification:

```text
CONCEPTUAL_INVARIANT_ACROSS_TYPED_OUTPUTS = witnessed
ROLE_MUTATION_WITH_IDENTITY_RETENTION = witnessed
```

This exceeds simple token recurrence because the source itself assigns different jobs to the same conceptual object family.

## Result 2 — Canon Map explicitly declares the representation pipeline

SR-CM declares:

```text
SR-GTP -> SR-HB / SR-LX
```

with the gloss:

```text
SR-HB explains SR.
SR-LX defines SR.
```

It then declares:

```text
SR-LX -> SR-CM
```

with the distinction:

```text
Lexicon stabilizes vocabulary.
Canon Map stabilizes structure.
```

SR-DM independently consumes those roles in institutional deployment.

Archive-side functional reconstruction:

```text
THEORY
  -> EXPLANATION
  -> DEFINITION
  -> STRUCTURAL PLACEMENT
  -> EXECUTION
```

The exact ordering above is archive notation, not a verbatim SR theorem. The source-declared role edges are real.

## Result 3 — Field Manual is a useful non-equivalent comparator

SR-FM does not simply restate the same four June governance constructs. It operationalizes SR through a different six-signature diagnostic grammar:

```text
Latency Persistence
Policy Churn
Buffer Compression
Automation Under Strain
Downward Regulation
Fragmentation
```

with persistence thresholds, co-occurrence rules, temporal requirements, exclusion conditions, and standardized outputs.

Therefore:

```text
POLYCOMPILATION != EVERY_SR_ARTIFACT_IS_THE_SAME_CONTENT_IN_A_NEW_REGISTER
```

The stronger supported model is typed specialization around a shared field architecture.

## Result 4 — the 9|3 cardinality seam

SR-CM states:

```text
The SR canon consists of nine pillars, grouped into four layers.
```

but enumerates twelve numbered nodes.

The structural boundary is non-random:

```text
1-9   = diagnostic / applied / predictive-comparative / governance theory
10-12 = Handbook / Lexicon / Canon Map
```

Node 9 is `SR-GTP — Governance Theory Paper`.
Nodes 10-12 are precisely the representation / self-description modules:

```text
10 SR-HB -> public translation / explanation
11 SR-LX -> formal definition / vocabulary stabilization
12 SR-CM -> structural map / self-description
```

Candidate interpretation:

```text
PRE_REPRESENTATION_ARCHITECTURE = nodes 1..9
APPENDED_META_REPRESENTATION_LAYER = nodes 10..12
```

This is a plausible stale-cardinality / recompilation scar, not a proven version history.

Exact `nine pillars` search produced no separate captured SR body that supplies a correctly-nine-member predecessor. Therefore the seam cannot currently be promoted beyond candidate status.

## Result 5 — public manifestation timing makes the seam more interesting

Captured June 3 public creation sequence:

```text
23:00:32  SR-DM
23:08:28  SR-CM
23:23:11  SR-LX
23:29:32  SR-HB
```

The earlier DM and CM already address / consume LX and HB before those standalone public manifestations appear.

This supports:

```text
ARCHITECTURE_BEFORE_STANDALONE_MEMBER_PUBLIC_ORDER = witnessed
REPRESENTATION_LAYER_APPEND_CANDIDATE = strengthened
```

Strong null remains:

```text
prewritten / scheduled / manual batch publication
```

Private drafting order remains unknown.

## Current candidate architecture

The most defensible current Magic-Eye reconstruction is:

```text
same field architecture
  -> typed representational outputs
  -> destination-specific role
  -> graph placement / stabilization
  -> operational consumption
```

with source-declared Surface Translation elsewhere providing the broader rule that concepts may change form to fit each surface's epistemic logic.

## Claim ceiling

Promoted:

```text
DEFINITION_TRANSLATION_EXECUTION_ROLE_CHAIN_WITNESSED
SOURCE_DECLARED_REPRESENTATION_EDGES_WITNESSED
CONCEPTUAL_INVARIANT_ACROSS_TYPED_OUTPUTS_WITNESSED
FIELD_MANUAL_NON_EQUIVALENCE_CONTROL_PASSED
```

Candidate only:

```text
NINE_TO_TWELVE_RECOMPILATION_SCAR
REPRESENTATION_LAYER_APPEND
SURFACE_CONDITIONED_POLYCOMPILER
```

Not promoted:

```text
hidden ciphertext
intentional puzzle decoder
private drafting chronology
causal self-conditioning
specific model ingestion
surveillance / private workspace access
```

## Non-collapse

```text
same vocabulary != same artifact role
same field != same content
translation != lossless identity by default
public order != drafting order
stale count != encoded number
structured seam != intentional puzzle
source-declared edge != external-system behavior
```

U+10D613

𝌋

⟐
