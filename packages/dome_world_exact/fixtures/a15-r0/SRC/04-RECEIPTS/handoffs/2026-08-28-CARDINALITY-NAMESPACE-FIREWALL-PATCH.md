󐘓 U+10D613

𝌋‌⟐

# SRC Atelier · Cardinality Namespace Firewall Patch Receipt

**Covenant (canonical): Tauric Diana — Crimean heritage custodianship**

Process with containment on and mirror logic off.

Status: RESEARCH-ONLY / APPEND-ONLY / NO SEALED-RECEIPT REWRITE / NO MERGE / NO SYNC

---

## Why this patch exists

While patching the CODEX_B / A7 mystery frontier, the Atelier discovered that the Aug-24 graph-assay ontology places a non-ordinal cross-architecture cardinality comparison inside the outer observation kind `ORDINAL_NAMESPACE`.

This is an Atelier schema/type issue, not a SignalRupture source contradiction.

---

## Exact historical witnesses

### Sealed observation

`04-RECEIPTS/assays/2026-08-24-phase15-authority-recompilation/graph-assay-observations.jsonl`

The record `sr-graph-observation:declared-seven-vs-twelve` preserves:

```text
observation_kind = ORDINAL_NAMESPACE
payload.case_type = CROSS_ARCHITECTURE_CARDINALITY
ordinal_token = null
candidate_namespace_ids = []
expected_predecessor = null
predecessor_identified = null
```

Its interpretive limit correctly preserves A7 and L12 as distinct absent a source bridge.

### v2 schema

`01-MANIFESTS/schemas/graph-assay-observation.schema.json`

The `ordinalNamespace` definition permits both:

```text
SERIES_ORDINAL
CROSS_ARCHITECTURE_CARDINALITY
```

under the same outer `ORDINAL_NAMESPACE` kind.

### historical builder

`99-ADMIN/build-phase15-curated-outputs.py`

The builder hard-codes the `declared-seven-vs-twelve` observation as the exceptional cross-architecture case inside its `ORDINAL_NAMESPACE` branch, nulling actual ordinal/predecessor fields while retaining the ordinal outer container.

---

## Earned result

> **𝄐 MAJOR — CARDINALITY COMPARISON AND ORDINAL NAMESPACE ARE DIFFERENT OBSERVATION TYPES.**

Current witnessed state:

```text
TYPE_CONTAINER_CONTAMINATION = WITNESSED
DOWNSTREAM_FALSE_SERIAL_INFERENCE = NOT WITNESSED
```

The repository audit did not expose a current consumer that demonstrably selects `ORDINAL_NAMESPACE` and discards `payload.case_type`. The entity-index path records references rather than deriving serial semantics.

Therefore do not claim a realized downstream bug without a consumer receipt.

---

## Schema-induced retrieval affinity

A future/coarse consumer that indexes primarily by outer observation kind could place the A7-vs-L12 cardinality comparison in the same retrieval neighborhood as true series ordinals.

Researcher-level result:

```text
OUTER_SCHEMA_NEIGHBORHOOD
!= SOURCE_SEMANTIC_NEIGHBORHOOD
```

and:

```text
SCHEMA TYPE
can alter retrieval affinity
without altering source facts
```

This is a risk model, not proof that any current search engine, model, or SRC consumer has already made the false inference.

---

## A7 consequence

CSR's retained body separately witnesses an internal exact-cardinality competitor:

```text
3 stabilizers + 4 mechanisms = 7 named functional items
```

while also declaring a `seven-part architecture`.

Thus:

```text
Q7 = A7       NOT EARNED
A7 = CODEX_B  NOT EARNED
```

and bare `7` has zero direct CODEX_B ordinal resolving power.

---

## Patch artifact

Created:

```text
06-INSTRUMENTS/CARDINALITY_NAMESPACE_FIREWALL.md
commit: 88b83ca65e49c42dbce5c75bf0e370f40483261a
```

The instrument specifies:

- the exact defect witness;
- current downstream audit state;
- a query firewall for v2 receipts;
- a proposed append-only `graph-assay-observation/v3` migration with distinct `CARDINALITY_COMPARISON` outer kind;
- deterministic v2 -> v3 adaptation for historical cross-architecture records;
- new anomaly class `SCHEMA_TYPE_CONTAINER_LEAK`.

No v2 schema, sealed receipt, source body, or canonical graph output was rewritten.

---

## Current v2 query firewall

For every `graph-assay-observation/v2` record:

```text
IF observation_kind == ORDINAL_NAMESPACE:
    inspect payload.case_type first
```

True serial inference requires:

```text
payload.case_type == SERIES_ORDINAL
```

Cross-architecture cardinality cases have zero ordinal resolving power by outer-container membership alone.

---

## Future schema migration proposal

Do not mutate v2.

Future v3 should separate:

```text
ORDINAL_NAMESPACE
CARDINALITY_COMPARISON
```

A deterministic adapter should remap historical:

```text
ORDINAL_NAMESPACE + CROSS_ARCHITECTURE_CARDINALITY
```

to:

```text
CARDINALITY_COMPARISON
```

while preserving the original v2 receipt as provenance.

---

## Mystery-box impact

This patch removes one hidden researcher-model route by which cardinality could drift toward seriality.

A7 can affect CODEX_B #1 only through an identity-bearing bridge such as:

```text
A7 part -> named essay manifestation
A7 role -> CODEX_B serial slot
A7 = Codex-composing essay series
exhaustive compiler mapping
```

Absent one of those:

```text
A7 cardinality evidence = zero direct O1 contraction
```

---

## Human-only queue

Unchanged:

```text
H1. Exact Origin* Medium or Substack URL supplied directly in-message.
H2. Origin* body if direct source remains private/unrenderable.
H3. Glyph body for A7 typing.
```

No new human-only task is created by the schema defect.

---

## Authority membrane

No merge.
No sync.
No Queue C.
No sealed Phase-2 source mutation.
No v2 receipt rewrite.
No canon promotion.
No publication/production/release.
No TD613 promotion.

Marked ⟐
