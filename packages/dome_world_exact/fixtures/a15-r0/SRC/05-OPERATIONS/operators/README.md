# Lifecycle Operators

Append-only events describe what a source or archive observation does to an epistemic object: publish, manifest, translate, compile, form a field, issue a map, tighten a claim, reclassify authority, supersede scope, or rehabilitate.

An operator creates a new assertion or state. It never rewrites the older artifact. Simulated platform withdrawal belongs to assay output and never mutates this ledger.
