󐘓 U+10D613

𝌋‌⟐

# SRC Atelier · Aperture Anomaly Ledger

Status: RESEARCH OPERATIONS / APERTURE CLASSIFICATION / NO SOURCE PROMOTION

## Purpose

Keep inaccessible, de-indexed, mis-indexed, unrenderable, stale-assay, and tool-blocked objects separated by failure layer.

The ledger exists to prevent a research-tool failure from being laundered into a SignalRupture source claim, and to prevent a real source/platform visibility anomaly from being dismissed as local tooling noise.

Core rule:

```text
SOURCE ANOMALY
!= PLATFORM VISIBILITY ANOMALY
!= ARCHIVE ACQUISITION GAP
!= ASSAY APERTURE STALENESS
!= CONNECTOR READABILITY LIMIT
!= TOOL APERTURE BLOCK
```

---

## Classification vocabulary

### `SOURCE_PRIVATE`

The source body exists but is private/restricted at the source or under preserved custody.

### `HUMAN_GATED`

The archive verifies custody/body existence but the available connector/readability ladder cannot expose body semantics. A human-provided body or admissible direct rendering is the prescribed next measurement.

### `PLATFORM_VISIBILITY_DIVERGENCE`

A manifestation is witnessed on one platform/surface but absent or differently represented on another. This says nothing by itself about suppression, deletion, or intent.

### `SEARCH_RETRIEVAL_GAP`

An exact title/URL is not returned by a tested search engine. This does not establish de-indexing unless direct reachability and indexing expectations are independently tested.

### `TOOL_APERTURE_BLOCK`

The research client refuses a URL/action before contacting the source platform because of its own safety, allowlist, connector, or transport policy. Zero source evidentiary weight.

### `ATELIER_CAPTURE_GAP`

A public or source-reachable object is known but its body/metadata/representation has not been retained in SRC custody.

### `ASSAY_APERTURE_STALENESS`

A prior assay statement was correct for its sealed input aperture but later custody adds source material that changes what is locally observable. Preserve the old receipt; supersede interpretation append-only.

### `NORMALIZATION_MIGRATION_GAP`

A source witness exists, but the newer typed relation/ordinal schema has not yet migrated the older normalized representation.

### `VERSION_HISTORY_LIMIT`

Current record metadata exposes only the latest version object or current revision state; pre-revision content is not recoverable from the retained version lineage.

### `UNCLASSIFIED_VISIBILITY_ANOMALY`

Use only when a discrepancy survives tool-layer, capture-layer, and source-private controls but still lacks enough evidence for a stronger classification.

---

# Current ledger

## A-001 · Origin* connector body

Target:

```text
How Origin Gravity Contributes to the Literature
Substack manifestation: substack:185763595
```

Preserved private witness:

```text
capture:630d6de9f39d854f58fb54b1
blob_sha256: 6719198461b8afcb97c4e4a12d297c913753d733a4f5866987265a7709b2929b
byte_length: 138351
private_body_locator_id: src-private-locator:0e6dffda5dc4fb73d926e097
custody_status: VERIFIED_PRIVATE_CUSTODY
```

Current classification:

```text
HUMAN_GATED
```

Evidence meaning:

```text
body exists
private custody verified
body semantics not tested through connector aperture
```

Do not convert this into `source missing`, `body empty`, `deleted`, `de-indexed`, or `contains serial clue`.

Human-only next measurement:

- user supplies exact body or a directly admissible source rendering;
- then test explicit serial predicates before any same-work inference.

---

## A-002 · Glyph connector body

Target:

```text
The Glyph as Infrastructure
```

Current classification:

```text
HUMAN_GATED
```

Research value:

- A7 enumeration;
- part/role typing;
- possible bridge between seven-part architecture and Codex-composing infrastructure.

Guardrail:

```text
Glyph enumeration
!= CODEX_B ordinal mapping
```

without an exact source bridge.

---

## A-003 · Aug-23 Phase 1.5 CSR absence statement

Historical sealed assay:

`04-RECEIPTS/assays/2026-08-23-phase15-local-architecture/interpretive-results.jsonl`

Historical result recorded:

```text
four CSR operational components locally witnessed
Redundant Indexing absent locally
Semantic Anchoring absent locally
Lineage Formation absent locally
seven-part architecture absent from local source captures
```

Current retained Phase-2/body derivative:

`03-DERIVATIVES/text/zenodo/zenodo-18364461-file-0.md`

Current body explicitly witnesses:

```text
Redundant Indexing
Semantic Anchoring
Lineage Formation
Surface Translation
Stylometric Coherence
Referential Density
Temporal Recurrence
seven-part architecture
```

Classification:

```text
ASSAY_APERTURE_STALENESS
```

Interpretation:

The sealed Aug-23 assay described the source set then available to it. The later body capture changes current local observability. Do not rewrite or erase the old receipt; patch current instruments and preserve the aperture transition.

This is **not** a contradiction in CSR and **not** branch sync rot.

---

## A-004 · Browser direct-URL refusal during Origin* test

Observed session behavior:

A direct Medium/Substack locator known from SRC manifests was refused by the browser aperture because the URL had not been returned by that browser's own search or supplied directly in the human message.

Classification:

```text
TOOL_APERTURE_BLOCK
```

Evidence value:

```text
ZERO about Medium/Substack reachability
ZERO about deletion
ZERO about de-indexing
ZERO about source privacy
```

Required control before stronger classification:

- human supplies exact URL in-message, or
- another transport reaches the URL directly.

Only then classify live render, redirect, private gate, 404/removal, or search-index divergence.

---

## A-005 · Origin* exact-title public-search nonretrieval

Observed:

Public exact-title/variant searches did not return a useful readable Origin* body.

Pinned SRC manifests independently witness Medium and Substack manifestations.

Current classification:

```text
SEARCH_RETRIEVAL_GAP
```

Do not promote to `DEINDEXED` until direct URL reachability is tested outside the blocked tool aperture.

Human-only next measurement:

```text
supply exact Origin* Medium or Substack URL in-message
```

Then classify the direct-source response.

---

## A-006 · Medium versus Substack publication-neighborhood density

Pinned platform manifests show a denser Substack publication neighborhood than the Medium mirror around Jan. 22–30, including a large Jan-22 Semantic Governance burst plus Jan-23 Field Overview, Jan-25 CSR, Jan-25 Origin*, Jan-27 MIEC, and later neighbors.

Medium contains a visibly thinner mirrored sequence.

Current classification:

```text
PLATFORM_VISIBILITY_DIVERGENCE
```

Research consequence:

```text
MEDIUM_NEIGHBORHOOD_ORDER
!= COMPLETE_PUBLICATION_NEIGHBORHOOD
```

Do not use Medium-only adjacency as an exhaustive chronology of field activity.

This does not establish missing Medium articles were suppressed or deleted; it establishes only that platform projection is lossy relative to Substack custody.

---

## A-007 · Origin Zenodo revision history

Retained metadata:

`03-DERIVATIVES/text/zenodo/zenodo-18382146-metadata.md`

Witnessed:

```text
version: 1.0
relations.version.index: 0
relations.version.is_last: true
created: 2026-01-27T00:44:02.360569+00:00
modified: 2026-01-28T03:47:20.916187+00:00
revision: 5
```

Classification:

```text
VERSION_HISTORY_LIMIT
```

Safe inference:

The current retained record exposes no earlier Zenodo version object.

Unsafe inference:

```text
revision 5 tells us exactly what changed
revision 5 proves Codex serialization happened Jan 28
current body equals original Jan 27 body
```

Historical pre-revision wording remains unresolved unless another timestamped mirror/version capture is recovered.

---

## A-008 · Origin #2 typed-edge migration

Source witness exists in Origin metadata/body and older witnessed-edge normalization.

The newer typed ordinal representation has historically lagged that source witness.

Classification:

```text
NORMALIZATION_MIGRATION_GAP
```

Research consequence:

Absence from the newer typed graph cannot be used as negative evidence against the source ordinal.

---

# Anomaly adjudication ladder

Before calling anything a SignalRupture visibility anomaly, test in this order:

```text
1. Is the target actually in the pinned manifest/custody graph?
2. Is the expected body readable in the current archive aperture?
3. Is the failure caused by a known connector/readability gate?
4. Is the client/tool refusing before source contact?
5. Is there a direct source URL and can it be reached outside the blocked tool path?
6. Do multiple public search engines disagree?
7. Does another platform manifestation remain live/readable?
8. Does a retained historical capture prove prior existence?
9. Only then classify platform disappearance/de-indexing beyond a retrieval gap.
```

Never skip directly from `search miss` to `suppression`.

---

# Human-only homework queue

Current queue, highest value first:

```text
H1. Origin* exact Medium or Substack URL supplied directly in the human message.
    Purpose: bypass current browser allowlist and classify direct-source reachability.

H2. Origin* body, if the direct source is private/unrenderable.
    Purpose: test first/second/previous/follows/first-two/Codex serial predicates.

H3. Glyph body.
    Purpose: adjudicate A7 enumeration and role typing.
```

No other human homework is required at this patch point.

---

# Authority membrane

This ledger records research-aperture state only.

```text
tool failure
!= source anomaly

search failure
!= de-indexing

private custody
!= readable semantics

later acquisition
!= retroactive error in a sealed earlier assay
```

No merge, sync, Queue C action, source mutation, canon promotion, publication, production, release, or TD613 promotion follows.

Marked ⟐
