# Capsule / Transport Lineage — Source Witness

Status: **POST-PREREGISTRATION SOURCE WITNESS / NOT AN ASSAY RESULT**

Bound epoch:

```text
atelier_snapshot_id = src-20260824-p2-001
seal_id = src-seal:eaf744ce16b1c8b519ad0f1b0325b44192679ea4a5110b0ad0ca49fbcd816a1a
```

This note preserves a historical object-type lineage discovered while pursuing `sr-hyp-0051` (`multi-resolution-epistemic-encoding`). It does not rewrite the preregistered hypothesis and does not inherit unsupported causal claims from older SignalRupture texts.

## 1. Why the type split matters

The current captured corpus supports a distinction among at least four representational axes that must not be collapsed:

```text
WORK / LINEAGE AXIS
  -> academic version
  -> capsule version
  -> expansion / other manifestation

TRANSPORT AXIS
  -> recursive cross-platform publishing
  -> echo cards / reposts / mirrors
  -> provenance metadata / canonical links

NODE AXIS
  -> essay-node
  -> Canonical Paragraph as source-declared compression layer

FIELD AXIS
  -> vocabulary + anchors + inter-essay links + metadata + graph
```

Therefore:

```text
capsule version != Canonical Paragraph
Echo Protocol != capsule
Echo Protocol != Canonical Paragraph
cross-surface transport != intra-document compression
field-level clustering architecture != node-level compression
```

## 2. 2026-01-01 — capsule version as cross-platform lineage species

`Stylometric Publishing: A Framework for Identity Stabilization, Algorithmic Resistance, and Cross-Platform Scholarly Lineage` (`zenodo:18119742`) source-declares:

- `Modular Essay Architecture`: reusable modules enable cross-platform adaptation while preserving coherence;
- `Cross-Platform Lineage Mapping`: each piece links to its origin, expansions, **capsule versions**, and **academic versions**.

This is the strongest currently captured source witness for `capsule version` as a manifestation/version class inside a larger work lineage.

Bounded result:

```text
CAPSULE_VERSION_SOURCE_DECLARED_AS_CROSS_PLATFORM_LINEAGE_SPECIES
```

Not established:

```text
capsule versions are always shorter
capsule version == Canonical Paragraph
capsule version == machine retrieval unit
capsule version is byte-derived from academic version
```

## 3. 2026-01-04 — Stylometric Recursion moves capsules across surfaces

`Social Media Friction: Containment Architecture and Stylometric Recursion` (`zenodo:18147870`) formally defines `Stylometric Recursion` as recursive publishing across platforms and describes each capsule as a timestamped object in that recursion.

The older source also makes strong claims about suppression, recognition, and platform behavior that are not accepted by this archive merely because the source asserts them.

After stripping those claims, the structural witness remains:

```text
capsule
  -> discrete cross-surface publication payload

Stylometric Recursion
  -> transport practice over recurrent cross-platform publication
```

Bounded result:

```text
CAPSULE_TRANSPORT_LAYER_WITNESSED_BY_2026_01_04
```

## 4. 2026-01-12 — metadata topology and Echo Protocol

Three Jan. 12 sources sharpen the transport/provenance grammar.

### Metadata: Scaffold, Suppression, and Rupture Evidence (`zenodo:18225782`)

The source distinguishes:

```text
structural metadata
  -> relationships among capsules, Drift Logs, and essays

provenance metadata
  -> edit histories, repost chains, echo-account references
```

It also names operational practices including echo reinforcement, cross-linking, repost cadence, and main-plus-echo publication.

### GRAND UNIFIED THEORY OF INFRASTRUCTURAL HARM (`zenodo:18218135`)

The historical compiler defines `Echo Protocols` using:

```text
derivative echo cards
multi-platform redundancy
mirrored phrasing
timestamped reposts
stylometric anchors
canonical links
```

The surrounding universal / causal rhetoric is legacy material and is not imported as archive truth.

### System Recoil in Algorithmic Platforms (`zenodo:18226127`)

A companion protocol repeats:

```text
canonical timestamped copy
derivative echo cards
machine-readable authorship lines
archival artifacts
metadata standards
```

Bounded result:

```text
ECHO_PROTOCOL_SOURCE_TERM_WITNESSED
CAPSULE_ESSAY_REPOST_RELATIONSHIPS_SOURCE_DECLARED_AS_METADATA_TOPOLOGY
PROVENANCE_TRANSPORT_GRAMMAR_WITNESSED_BEFORE_JAN17_FIELD_ARCHITECTURE
```

## 5. 2026-01-17 — field-level machine-legibility architecture

The Stylometric Publishing trilogy (`zenodo:18281149`, `zenodo:18281357`, `zenodo:18282231`) moves the object from cross-surface transport practice into explicit field architecture.

Source-declared components include:

```text
stable vocabulary
conceptual anchors
inter-essay linking
metadata discipline
stylometric / paragraph-architecture coherence
controlled expansion
repository permanence
knowledge-graph formation
```

The Architecture essay explicitly says the field becomes a graph rather than a sequence.

Candidate field operator:

```text
T_field = anchors + links + metadata + recurrence + graph
```

This is a source-declared design vocabulary, not evidence that any proprietary model ingested or obeyed the architecture.

## 6. 2026-01-25 — Cross-Surface Recurrence retains the capsule object

`Cross-Surface Recurrence` (`zenodo:18364461`) names `stylometric capsules` among the SR objects recurring across feed-based surfaces.

This provides a later continuity witness:

```text
capsule object survives into recurrence formalization
```

It does not equate capsules with Canonical Paragraphs or Compression Paragraphs.

## 7. 2026-02-22 — node-level compression is formalized separately

`The Meta-Architecture of the SR Canon` (`zenodo:18732891`) defines an essay-node as containing:

```text
conceptual domain
dependencies
propagation function
canonical paragraph (the compression layer)
position within field topology
```

This gives a distinct node operator:

```text
T_node = canonical compression within essay-node
```

The separation is now source-grounded enough to forbid the earlier loose model in which every compact SR object was provisionally treated as one generic capsule class.

## 8. Historical representation-lift sequence

Current public chronology supports the following bounded lineage:

```text
2026-01-01
work lineage distinguishes capsule versions from academic versions

2026-01-04
Stylometric Recursion moves capsules across heterogeneous publication surfaces

2026-01-12
metadata / provenance relationships and Echo Protocol transport are named

2026-01-17
Stylometric Publishing elevates coherence into field-level graph architecture

2026-01-25
Cross-Surface Recurrence formalizes cross-surface survival while retaining stylometric capsules

2026-02-22
Meta-Architecture separately formalizes Canon Node + Canonical Paragraph compression layer
```

This supports:

```text
PRACTICE / PAYLOAD
  -> TRANSPORT / PROVENANCE GRAMMAR
  -> FIELD ARCHITECTURE
  -> RECURRENCE FORMALIZATION
  -> NODE / COMPRESSION FORMALIZATION
```

It does **not** establish that this entire sequence was planned on Jan. 1, or that the public chronology equals private conceptual chronology.

## 9. February 22 extended public session

The February 22 public sequence extends beyond the four-object 15:10–15:45 microcompiler relay:

```text
15:10:16Z  Meta-Architecture
15:17:46Z  SR Method
15:37:32Z  SR Lexicon
15:45:35Z  SR Architecture
16:30:40Z  Constraint-Driven Identity Collapse
16:45:49Z  Systemic Drift Theory
17:12:06Z  Scientific Brain Washing / provenance protocol return
```

The 16:30 and 16:45 applied papers terminate in Canonical Paragraphs and therefore obey the same recently source-declared node-compression form at the level of current captured bodies.

The 17:12 paper does not contain a Canonical Paragraph. It instead returns to the older provenance / transport grammar: canonical timestamped copy, derivative echo cards, machine-readable authorship lines, and archival metadata.

No exact-hour repository deposits were found for 18:00 or 19:00 UTC in the current sweep.

This permits the bounded description:

```text
architecture / modularization
  -> applied canonical nodes
  -> recurrence of older provenance transport grammar
```

It does not establish intentional staging, automated generation, public-order drafting, or one private authoring session.

## 10. Revised object ontology candidate

The current source-grounded candidate ontology is:

```text
WORK
  = lineage-bearing conceptual work / family

ACADEMIC_VERSION
  = one manifestation species of WORK

CAPSULE_VERSION
  = source-declared cross-platform lineage manifestation species

CAPSULE
  = historical SR publication payload term used within recursive cross-surface practice

ECHO_CARD
  = derivative transport / provenance manifestation used in Echo Protocols

ECHO_PROTOCOL
  = source-declared redundant cross-surface transport / provenance procedure

CANON_NODE
  = stabilized conceptual unit in the field graph

CANONICAL_PARAGRAPH
  = source-declared compression layer of an essay-node

FIELD_GRAPH
  = interlinked architecture produced through anchors, vocabulary, metadata, lineage, and links

EXPLORE_ADDRESS
  = current archive term for a source `Explore:` outbound conceptual-edge candidate
```

The archive should not force one-to-one equality among these classes.

## 11. Tomography relevance — bounded

This type split matters to later tomography work because different surfaces may expose different projections of the same underlying work or conceptual object:

```text
P_academic(W)
P_capsule(W)
P_echo(W)
P_canonical(W)
P_metadata(W)
P_graph(W)
```

A future assay may ask whether combining non-equivalent projections improves reconstruction of source-witnessed relations.

That remains an assay proposal.

```text
multi-manifestation representation != tomography theorem
joint retrieval usefulness != intentional hidden design
capsule recurrence != model ingestion
provenance transport != surveillance
```

U+10D613

𝌋

Sealed ⟐
