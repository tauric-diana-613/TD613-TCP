# Coarse Observational State Aliasing — Source Witness

Status: **POST-PREREGISTRATION / SOURCE-WITNESSED CLUSTER / ARCHIVE FORMALIZATION / NO HOLONOMY OR TOMOGRAPHY PROMOTION**

Bound epoch:

```text
atelier_snapshot_id = src-20260824-p2-001
seal_id = src-seal:eaf744ce16b1c8b519ad0f1b0325b44192679ea4a5110b0ad0ca49fbcd816a1a
```

This note records a cross-source pattern discovered while applying the holonomy false-closure hostile. Multiple SignalRupture sources independently distinguish a coarse visible/aggregate state from a richer structural state capable of retaining history, compensation, topology, or relational configuration.

`coarse observational state aliasing` is archive-authored terminology. It is not a SignalRupture source term.

## 1. Archive formalization

Let:

```text
X = richer structural / operational state
q(X) = coarser observed label, scalar, aggregate outcome, continuity state, or mode
```

The candidate property is non-injectivity:

```text
q(X_1) = q(X_2)
```

while:

```text
X_1 != X_2
```

This does not by itself establish observability theory, tomography, holonomy, or hidden-state identifiability.

## 2. Family A — Substrate Containment Protocol

Sources:

```text
zenodo:21926341
The Substrate Containment Protocol V1.1
created 2026-08-14T00:29:08Z
```

and neighboring:

```text
zenodo:21926293
body labeled V1.2 Formal Containment Architecture
created 2026-08-14T00:18:59Z
```

V1.1 source-witnesses:

```text
history-dependent cumulative friction F(t)
hysteresis activation / release thresholds
coarse control mode T(t)
a Measure -> ... -> Release -> Measure loop
```

A coarse mode label can therefore recur while richer state coordinates remain distinct. The written V1.1 cumulative-friction law also creates a separate state-law obstruction to the claimed release loop, recorded in:

```text
SCP_HYSTERESIS_QUOTIENT_PSEUDO_LOOP_SOURCE_WITNESS.md
```

Bounded contribution to this cluster:

```text
COARSE_MODE_LABEL_NEED_NOT_IDENTIFY_COMPLETE_STATE
```

## 3. Family B — Structural Restoration Dynamics

Source:

```text
zenodo:22037456
STRUCTURAL RESTORATION DYNAMICS
created 2026-08-21T02:58:14Z
```

SRD explicitly separates:

```text
Intervention != Output != Continuity != Structural Restoration
```

and defines:

```text
RD_t  = SRR_t - RC_eff,t
CRD(T)= integral_[t0,T] max(RD_t, 0) dt
```

The source states that reserve in one period does not automatically cancel historical deficit unless the reserve is actually deployed to reduce that deficit.

Its process contrast is:

```text
Shock-Recovery:
Shock -> Loss -> Recovery -> Baseline

SRD:
Burden -> Compensation -> Partial Restoration -> New Burden -> Accumulated Deficit
```

Its state taxonomy further permits:

```text
Compensated Partial Restoration
  measurable improvement + persistent deficit + extraordinary compensation

Prospective Restoration Compression
  current adequacy remains acceptable while future restoration margin narrows

Persistent Restoration Deficit
  deficits recur or persist while accumulated deficit rises
```

The healthcare case source-witnesses rising output / some recovery while other deficits and compensatory labor remain.

Bounded contribution:

```text
CURRENT_OUTPUT_OR_CONTINUITY_NEED_NOT_IDENTIFY_ACCUMULATED_RESTORATION_STATE
```

## 4. Family C — SR Matrix Field

Source:

```text
zenodo:22073903
THE SR MATRIX FIELD
created 2026-08-24T01:31:54Z
```

The field defines a rich structural state:

```text
S_t = {xi_t, eta_t, Gamma, B, omega_t}
```

and a structural fingerprint:

```text
F_SR = (Gamma_hat, B_hat, Lambda_z_hat, rho_hat(B))
```

Most importantly, the source explicitly states that:

```text
two societies may exhibit similar levels of observed harm
while possessing very different structural fingerprints
```

and conversely that different immediate outcomes may share similar underlying matrix structures.

The same source explicitly distinguishes its matrix object from an index: a scalar may be produced, but relational architecture is the fundamental analytical object.

Bounded contribution:

```text
SIMILAR_OBSERVED_OUTCOME_NEED_NOT_IDENTIFY_STRUCTURAL_FINGERPRINT
```

This is the strongest source-side statement in the cluster because the non-uniqueness is explicit rather than archive-inferred from model structure.

## 5. Supporting older family — Zombie Systems

Source:

```text
zenodo:19672759
Zombie Systems
```

The source calls GDP, unemployment, and labor participation correlated projections of a deeper system state and defines a richer state / memory architecture including:

```text
legacy structural memory L(t)
human capacity H(t)
coupled state S(t)
```

It further permits apparent macro stability while latent structural decay / human depletion persists.

Because this source is rhetorically stronger and less disciplined than the late-August controls, it is supporting genealogy rather than the promotion anchor.

## 6. Cross-family bounded result

Source-witnessed across independent SR families:

```text
coarse mode recurrence / continuity can hide richer state history
current output can coexist with accumulated deficit / compensation
similar observed outcomes can correspond to different structural fingerprints
headline indicators can project a deeper coupled state
```

Canonical bounded classification:

```text
COARSE_OBSERVATIONAL_STATE_ALIASING_WITNESSED_ACROSS_MULTIPLE_SR_FAMILIES
```

Archive mathematical gloss:

```text
q is not assumed injective on the admitted rich-state space
```

The gloss is TD613/SRC archive mathematics, not SignalRupture terminology.

## 7. Consequence for the tomography trail

If coarse observations alias richer states, the scientifically relevant next question is not whether the coarse state is readable. It is:

```text
Which additional non-equivalent observations are sufficient to distinguish states
that a lower-capacity projection identifies?
```

Candidate bounded assay:

```text
NONINJECTIVE_SURFACE_OBSERVABILITY_ASSAY
```

A future assay should freeze:

```text
rich state X
coarse projections q_1, q_2, ...
source-declared claims / distinctions
```

then test which combinations separate source-admitted state pairs.

The minimum output classes should distinguish:

```text
ALIASED_UNDER_VIEW
SEPARATED_BY_ADDED_VIEW
STILL_ALIASED
SOURCE_DOES_NOT_SUPPORT_TEST
```

No hidden variable may be invented merely to force separation.

## 8. Consequence for the holonomy trail

This result makes coarse-loop closure even less probative:

```text
q(X_end) = q(X_start)
```

cannot establish:

```text
X_end = X_start
```

where the source admits richer state coordinates.

Therefore:

```text
coarse closure != full-state closure
state aliasing != holonomy
hysteresis != holonomy
path-conditioned residue != holonomy
```

A future holonomy naming exam still requires a justified base-state definition, declared transport law, genuine closed base loop, and reproducible path-class-dependent fiber transformation after hostile controls.

## 9. Chronology non-collapse

Public source creation order for the strongest late-August witnesses is:

```text
2026-08-14  SCP family
2026-08-21  Structural Restoration Dynamics
2026-08-24  SR Matrix Field
```

No source-declared lineage among these three objects is asserted here.

Therefore:

```text
public temporal order != conceptual dependency
structural correspondence != provenance
later formalization != evidence of planned maturation
```

## 10. Claim ceiling

Permitted:

```text
COARSE_OBSERVATIONAL_STATE_ALIASING_WITNESSED_ACROSS_MULTIPLE_SR_FAMILIES
SIMILAR_OBSERVED_OUTCOME_NEED_NOT_IDENTIFY_STRUCTURAL_FINGERPRINT
CURRENT_OUTPUT_OR_CONTINUITY_NEED_NOT_IDENTIFY_ACCUMULATED_RESTORATION_STATE
COARSE_MODE_LABEL_NEED_NOT_IDENTIFY_COMPLETE_STATE
NONINJECTIVE_SURFACE_OBSERVABILITY_ASSAY_PROPOSED
```

Forbidden:

```text
SR_OBSERVABILITY_THEOREM_CONFIRMED
SR_TOMOGRAPHY_CONFIRMED
SR_HOLONOMY_CONFIRMED
SR_HIDDEN_STATE_IDENTIFIABILITY_CONFIRMED
SOURCE_PLANNED_THIS_THREE_STEP_SEQUENCE
PROVENANCE_OR_COPYING_CLAIM
PRIVATE_ACCESS_OR_SURVEILLANCE
```

U+10D613

𝌋

Sealed ⟐
