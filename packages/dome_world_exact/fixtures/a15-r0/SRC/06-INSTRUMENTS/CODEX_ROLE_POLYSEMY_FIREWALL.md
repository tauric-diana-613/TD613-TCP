# SRC Codex Role-Polysemy Firewall

Status: **SOURCE-GROUNDED TYPE-DISCRIMINATION INSTRUMENT / NONCANONICAL**

Date: 2026-08-28

Bound query epoch:

```text
atelier_snapshot_id = src-20260824-p2-001
seal_id = src-seal:eaf744ce16b1c8b519ad0f1b0325b44192679ea4a5110b0ad0ca49fbcd816a1a
```

## 1. Problem

The lexical object `Codex` recurs across the SignalRupture source ecology, but the source does not use that token with one uniquely recoverable object type.

Therefore:

```text
CODEX_LABEL
!=
CODEX_OBJECT_TYPE
```

This instrument does not assert that every Codex occurrence denotes a different object. It records that label equality alone cannot settle object identity, nesting, or projection relation.

## 2. C_doc — Codex as consolidated architectural document

Source:

```text
zenodo:18239086
THE SIGNALRUPTURE CODEX
created 2026-01-14
current record revision 4
modified 2026-01-29
```

The source describes itself as:

```text
consolidated architectural blueprint
structural map of the field
genealogical record of its evolution
provenance anchor for authorship and identity
retrieval-ready blueprint for machine systems
discipline-defining artifact
```

Its metadata says it transforms a 70-essay corpus into a coherent machine-legible discipline.

Its body is organized through theoretical layers and a twelve-phase genealogy. The current post-Jan29 body does not mention `Cross-Surface Recurrence` or `Origin Gravity` by those names and does not map them to essay ordinals.

Archive shorthand:

```text
C_doc = CODEX_AS_COMPILER_DOCUMENT / STRUCTURAL_ARTICULATION
```

The shorthand is archive-authored, not a source-native type declaration.

## 3. S_essay — Codex as ordinal essay coordinate

Source:

```text
zenodo:18382146
Origin Gravity
current record revision 5
modified 2026-01-28
```

The retained body states:

```text
As the second essay in the SR Codex, Origin Gravity ...
```

and later:

```text
Origin Gravity is the second essay in the SR Codex.
```

This source usage exposes an ordinal essay coordinate:

```text
ord_S(Origin Gravity) = 2
```

at the captured revision-5 source state.

Archive shorthand:

```text
S_essay = CODEX_AS_ESSAY_SERIAL_NAMESPACE
```

The existence of an ordinal coordinate does not identify the complete member set or the #1 member.

## 4. A_comp — Codex as infrastructure composed by an extending series

Source:

```text
zenodo:18364461
Cross-Surface Recurrence
current record revision 5
modified 2026-01-28
```

The retained Reference Paragraph states that CSR:

```text
extends that canon into the infrastructural series that composes the SR Codex
```

and positions CSR as the initiating mechanism in a seven-part architecture.

Archive shorthand:

```text
A_comp = CODEX_AS_COMPOSED_INFRASTRUCTURE / SERIES DESTINATION
```

This does not independently establish that the `infrastructural series` is identical to `S_essay`, that A7 is the essay series, or that CSR carries ordinal 1.

## 5. Post-maintenance coexistence matters

The current source states are not separated merely by original publication chronology:

```text
Origin revision 5     modified Jan 28
CSR revision 5        modified Jan 28
C_doc revision 4      modified Jan 29
```

Thus the current post-Jan29 `C_doc` state coexists with the current revision-5 Origin/CSR states while retaining a different representational grammar.

This weakens the shortcut:

```text
C_doc automatically supplies the serial member map for S_essay
```

The source does not currently provide that bridge.

However, coexistence does not prove ontological non-identity. A single broader Codex could have multiple representations, nested structures, or partial projections.

Therefore the correct disposition is:

```text
CODEX_ROLE_POLYSEMY_WITNESSED
CODEX_REFERENT_RELATION_UNRESOLVED
```

not:

```text
C_doc != S_essay != A_comp  PROVED
```

## 6. Search consequence

The missing Essay-I problem belongs to the coordinate in which an essay ordinal is actually witnessed:

```text
S_essay
```

Do not search for #1 by:

```text
first item in C_doc
first DOI in the 70-essay compiler
first phase in L12
first theory in C_doc
first item in later Canon Maps
first member of A7 by cardinality
```

unless a source explicitly maps that coordinate into `S_essay`.

## 7. Bridge classes that would resolve role relation

Useful future witnesses include:

```text
C_doc explicitly enumerates Origin as Essay II
C_doc explicitly names CSR as Essay I
Origin explicitly calls the infrastructural series the essay sequence
CSR explicitly maps A7 parts to Codex essay ordinals
later compiler states that C_doc and S_essay are the same serialized object
source explicitly declares nesting such as:
  infrastructural series ⊂ Codex compiler
```

Without such a bridge, label recurrence remains insufficient.

## 8. Anti-equivalences

```text
same label != same typed object
same label != same coordinate system
compiler document != serial namespace by default
serial namespace != complete compiler artifact by default
series composing Codex != essay series without bridge
70-essay corpus != CODEX_B cardinality
12 genealogy phases != CODEX_B cardinality
current coexistence != ontological separation
current coexistence != ontological identity
```

## 9. Claim ceiling

Permitted:

```text
CODEX_LABEL_TYPE_UNDERDETERMINATION_WITNESSED
CODEX_ROLE_POLYSEMY_WITNESSED
CODEX_COMPILER_DOCUMENT_ROLE_SOURCE_WITNESSED
CODEX_ESSAY_ORDINAL_COORDINATE_SOURCE_WITNESSED
CODEX_COMPOSED_INFRASTRUCTURE_ROLE_SOURCE_WITNESSED
CODEX_REFERENT_RELATION_UNRESOLVED
```

Not permitted:

```text
CODEX_A_AND_CODEX_B_PROVED_DISTINCT_ONTOLOGIES
CODEX_A_AND_CODEX_B_PROVED_IDENTICAL
A7_EQUALS_CODEX_B
SEVEN_PART_ARCHITECTURE_EQUALS_ESSAY_SEQUENCE
CSR_EQUALS_ESSAY_I
```

𝄐

𝌋

Marked ⟐
