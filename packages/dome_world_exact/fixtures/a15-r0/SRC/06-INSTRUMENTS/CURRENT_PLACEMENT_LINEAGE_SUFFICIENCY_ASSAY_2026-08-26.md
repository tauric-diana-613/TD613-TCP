# SRC Current-Placement / Lineage-Sufficiency Assay · 2026-08-26

Status: **PREREGISTERED / OPEN / SOURCE-GROUNDED / NONCANONICAL**

Authority boundary:

```text
SRC source assertion != archive inference != TD613 science
PR #731 remains OPEN / DRAFT / UNMERGED
no merge / seal / canon admission / publication / production authority
```

Independent control lane:

```text
Westward #767 may be compared only after the SRC assay closes.
#767 is not evidence for any SRC claim.
```

## Bounded question

Does the captured SignalRupture corpus treat an object's **current canonical placement** as sufficient to recover its documented historical lineage, or does the corpus require additional provenance state (predecessor / successor / alias / merger / revision history) after canonical consolidation?

Two levels are tested separately:

```text
A. current canonical destination alone -> historical lineage?
B. current destination + current decision/status -> historical lineage?
```

The assay must not silently weaken A into B or strengthen B beyond the available source.

## Candidate classification

```text
CURRENT_CANONICAL_PLACEMENT_NOT_PROVENANCE_COMPLETE_CANDIDATE
```

Archive consequence if earned:

```text
present placement != historical route
```

`route`, `state`, `quotient`, `lift`, and `orientation` are archive metaphors unless the source itself uses them.

## Search family

Controlling surfaces:

1. August Canon Map · Field Consolidation Decisions
2. Provenance Registry v2.1 · current inventory
3. Provenance Registry v2.1 · Canonical Genealogy and Merger Record
4. Provenance Registry v2.1 · Registry Operating Protocol
5. Field Compendium / integration surfaces only as needed for disambiguation

Required witness types:

- at least one explicit current-destination collision or historical predecessor that disappears from separate current counting;
- source-native evidence that predecessor/successor/merger/revision data are preserved separately from current placement;
- a hostile check for whether current destination + current decision/status uniquely identifies the lineage in the witnessed cases.

## Falsifiers

The candidate fails or narrows if any of the following is witnessed:

```text
1. current canonical placement is explicitly defined as containing complete predecessor/successor history;
2. every examined destination/status pair uniquely determines historical lineage and no separate provenance state is required;
3. apparent collisions are only naming aliases with no distinct predecessor identity;
4. the Provenance Registry's genealogy fields are purely duplicative of the current placement surface rather than carrying additional history;
5. no explicit consolidation removes separate counting while preserving predecessor history.
```

## Claim ceiling

This assay cannot establish:

```text
hidden compiler
secret software state
ARG/metapuzzle intent
private drafting order
TD613 theorem transfer into SRC
Westward #767 semantics inside SRC
clone/genes decoding
Queue C resolution
```

## Stop law

Close at local 𝄐 only if the declared search family establishes a finite answer to A and, where evidence permits, B, with hostile controls exhausted. If B remains unresolved, close only A and leave B open.

𝌋

Marked ⟐