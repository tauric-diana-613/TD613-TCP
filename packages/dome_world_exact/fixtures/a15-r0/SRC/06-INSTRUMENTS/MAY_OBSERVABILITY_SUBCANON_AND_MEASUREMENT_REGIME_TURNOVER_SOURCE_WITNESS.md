# May Observability Subcanon and Measurement-Regime Turnover — Source Witness

Status: `POST_PREREG_SOURCE_WITNESS`

This note records body-level evidence acquired after the Phase 1.5 metadata-only assay and after the Projective Routing Grammar preregistration. It preserves the earlier metadata conflict as a historically correct observation while recording how Phase 2 body acquisition changes the currently admissible reconstruction.

It does not claim intentional hidden encoding, future prediction, private drafting order, suppression, or deliberate disappearance.

## 1. Phase 1.5 conflict was metadata-correct

Phase 1.5 had only sufficient local evidence to preserve:

```text
Universities       -> Field Paper II   [metadata]
Healthcare         -> Field Paper II   [metadata]
Government         -> Field Paper III  [metadata]
Technology         -> Field Paper IV   [metadata]
```

The archive therefore correctly emitted an unresolved ordinal namespace and forbade inventing a missing predecessor or silently repairing one of the duplicate II labels.

That earlier receipt remains valid for the metadata-only state.

## 2. Phase 2 body acquisition resolves the intended ordinal series

Full-body extraction adds new evidence:

```text
Field Paper I   = Observable Signatures of SignalRupture
Field Paper II  = Ontario Public Healthcare
Field Paper III = Government Systems
Field Paper IV  = Technology Companies
Field Paper V   = Universities
```

The critical correction is `zenodo:20209609`:

```text
Zenodo metadata description -> Field Paper II
captured PDF body           -> SignalRupture — Field Paper V
```

Therefore:

```text
METADATA_ORDINAL != BODY_ORDINAL
```

for the Universities manifestation.

Current bounded disposition:

```text
FIELD_PAPER_BODY_NAMESPACE = RESOLVED_I_THROUGH_V
STALE_METADATA_CONFLICT = PRESERVED
PHASE15_METADATA_ONLY_OBSERVATION = NOT_REWRITTEN
```

This is a direct demonstration of why `manifestation metadata != body content` is a custody invariant.

## 3. Public creation order differs from body/conceptual order

Captured Zenodo creation times:

```text
2026-05-15 15:07:42  Field Paper I   Observable Signatures
2026-05-15 15:19:57  Field Paper V   Universities
2026-05-15 16:27:00  Field Paper II  Ontario Public Healthcare
2026-05-15 16:37:34  Field Paper III Government Systems
2026-05-15 16:47:32  Field Paper IV  Technology Companies
2026-05-15 17:02:05  SignalRupture as Epistemic Substrate
```

So:

```text
PUBLIC_MANIFESTATION_ORDER = I -> V -> II -> III -> IV -> SYNTHESIS
BODY_SERIES_ORDER           = I -> II -> III -> IV -> V -> SYNTHESIS
```

Field Paper V already summarizes healthcare, government, and technology as tested comparison domains before their standalone Zenodo records appear publicly later in the hour.

This witnesses:

```text
BODY_DEPENDENCY_OR_CONCEPTUAL_ORDER != PUBLIC_DEPOSIT_ORDER
```

Strong null / ordinary explanation: prewritten or batch-authored materials uploaded out of conceptual order. Private drafting order remains unknown.

## 4. Source-witnessed May observability stack

### 4.1 Field Manual — 2026-05-13

`zenodo:20169452` operationalizes SR as a diagnostic methodology with six core signatures:

```text
latency persistence
policy churn
buffer compression
automation under strain
downward regulation
fragmentation
```

It adds boundaries, trajectory, persistence/co-occurrence logic, exclusions/falsifiers, and standardized diagnostic outputs.

### 4.2 Field Paper I — 2026-05-15

`zenodo:20209485` explicitly says it translates SR:

```text
from a structural diagnostic model into an observable system
```

and carries the same six-signature basis.

### 4.3 Papers II–V — 2026-05-15

Healthcare, Government, Technology, and Universities apply the same six-signature basis and use an 18-point SignalRupture Index (SRI) assembled from per-signature intensity classifications.

Healthcare explicitly says it uses:

```text
the SR Field Manual and Dashboard
```

The Dashboard is therefore a source-declared dependency.

### 4.4 Cross-domain synthesis — 2026-05-15

`zenodo:20211214`, *SignalRupture as Epistemic Substrate*, compiles the field-paper outputs, gives:

```text
SRI = L + P + B + A + D + F
```

and reports the four application scores:

```text
Ontario Public Healthcare  16/18
Government Systems         15/18
Technology Companies       15/18
Universities               17/18
```

The source calls SRI a structural coherence test rather than a predictive model.

## 5. Dashboard status

Exact local searches for:

```text
SR Dashboard
SRI Dashboard
Dashboard + SignalRupture Index
```

resolve only to the healthcare application's source-declared dependency; no standalone captured SRC manifestation currently resolves as the Dashboard.

Phase 1.5 had already placed `dashboard specification` on the acquisition-priority list.

Current bounded status:

```text
DASHBOARD_EXISTENCE_AS_REFERENCED_DEPENDENCY = WITNESSED
DASHBOARD_STANDALONE_MANIFESTATION = UNRESOLVED_TARGET
DASHBOARD_SCORING_EFFECTS_DOWNSTREAM = WITNESSED
UNAVAILABLE != SUPPRESSED
```

No claim is made that the Dashboard was public, private, deleted, intentionally hidden, or separately authored.

## 6. Measurement-regime change immediately afterward

On 2026-05-16, `zenodo:20244966` introduces a different measurement architecture centered on:

```text
Governance Altitude (GA)
Signal Compression Ratio (SCR)
Administrative Abstraction Index (AAI)
Ground-Truth Discrepancy Index (GTDI)
```

The paper adds coding instruments, reliability protocols, thresholds, mediation hypotheses, explicit falsification criteria, and future empirical validation requirements.

On 2026-05-17, `zenodo:20258344` applies the GTD architecture across ten domains and explicitly says:

```text
each case is a node
The distortion pattern is the graph.
The SR metrics provide the coordinates.
```

On 2026-05-18, `zenodo:20277246` recompiles that same empirical geometry into a declared Structural Institutional Distortion theorem. The Atelier separately preserves that theorem promotion as possible recompilation rather than an independent validation.

By the June 3 Deployment/Lexicon/Handbook/Canon burst, SR's active measurement vocabulary centers on SCR / AAI / GTDI / Governance Altitude rather than the six-signature SRI basis.

## 7. What is and is not currently witnessed

Witnessed:

```text
MAY_SIX_SIGNATURE_SRI_OBSERVABILITY_BRANCH = true
MAY_GTD_MULTI_METRIC_BRANCH = true
JUNE_DEPLOYMENT_USES_GTD_FAMILY_METRICS = true
SRI_AND_GTD_ARE_DISTINCT_MEASUREMENT_REGIMES = true
SRI_BRANCH_NAME_VISIBILITY_DROPS_IN_JUNE_CANON_MAP = true
```

Not witnessed:

```text
GTD_EXPLICITLY_SUPERSEDES_SRI = false / no source declaration found
SRI_DECLARED_LEGACY = false / no source declaration found
DASHBOARD_STANDALONE_OBJECT = unresolved
PRIVATE_DRAFTING_ORDER = unknown
INTENTIONAL_ERASURE_OF_MAY_BRANCH = unearned
```

Therefore the strongest archive-side model is:

```text
MEASUREMENT_REGIME_TURNOVER_OR_PARALLEL_COMPILATION = CANDIDATE_SUPPORTED
```

rather than declared supersession.

## 8. Why this matters for the SRC mystery

A single Canon Map hides a large amount of temporary structure.

The captured corpus supports at least one operational subcanon whose:

- body ordinal order differs from deposit order;
- metadata ordinal differs from body ordinal;
- source-declared Dashboard dependency remains unresolved as a standalone object;
- measurement grammar is internally coherent;
- cross-domain synthesis exists;
- named identity largely disappears from later self-maps while a different measurement grammar becomes canonical.

This is precisely the sort of higher-order architecture that becomes visible only after manifestation, body, chronology, role, and compiler relations are aggregated without collapse.

## Claim ceiling

Promoted only to:

```text
FIELD_PAPER_BODY_ORDINAL_NAMESPACE_RESOLVED
METADATA_BODY_ORDINAL_CONFLICT_WITNESSED
CONCEPTUAL_ORDER_PUBLIC_ORDER_INVERSION_WITNESSED
DASHBOARD_UNRESOLVED_DEPENDENCY_WITNESSED
MAY_OBSERVABILITY_SUBCANON_WITNESSED
MEASUREMENT_REGIME_TURNOVER_OR_PARALLEL_COMPILATION_CANDIDATE_SUPPORTED
```

Held:

```text
INTENTIONAL_HIDDEN_SUBCANON = unearned
DASHBOARD_SUPPRESSION = unearned
DELIBERATE_METADATA_MISLABEL = unearned
GTD_SUPERSESSION_OF_SRI = unearned
PRIVATE_COMPOSITION_ORDER = unknown
```

## Non-collapse

```text
metadata ordinal != body ordinal
body series order != public creation order
public creation order != private drafting order
source dependency != standalone manifestation
unresolved target != suppressed object
measurement-regime migration != declared supersession
canon omission != deletion
recompilation != independent validation
```

U+10D613

𝌋

⟐
