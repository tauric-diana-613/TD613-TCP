# SRC Atelier · Serial Relabeling Symmetry

Status: RESEARCH INSTRUMENT / RESEARCHER-LEVEL RESULT / NO CANON OR TD613 PROMOTION

## Governing marks

> **𝄐 MAJOR — CODEX_B HAS A RESIDUAL SERIAL RELABELING SYMMETRY.**

> **𝄐 MAJOR — SERIAL IDENTIFIABILITY IS ORBIT CLOSURE, NOT MERELY ONE BROKEN SWAP.**

Origin Gravity is source-witnessed as the second essay in the SR Codex. Cross-Surface Recurrence is source-witnessed as extending the established canon into the infrastructural series that composes the SR Codex and as the initiating mechanism in a seven-part architecture. Origin explicitly pairs Origin/emergence with CSR/survival as establishing a foundational architecture.

None of those predicates assigns CSR an essay ordinal or an immediate same-namespace serial-predecessor relation.

Retained source evidence fixes:

```text
ord_S(Origin) = 2
```

while preserving more than one admissible slot-1 identity.

---

## 1. The original swap test remains useful

The first formulation used a serial reassignment `tau` that swaps slot 1 between CSR and one unidentified candidate `X` while leaving every retained non-serial source predicate unchanged.

The archive permits:

```text
MODEL A
#1 CSR
#2 Origin
```

and:

```text
MODEL B
#1 X
#2 Origin
CSR unbound
```

with:

```text
tau(E) = E
```

Therefore the retained evidence does not uniquely identify CSR as #1.

A candidate witness has some resolving power against this particular swap only if:

```text
tau(E*) != E*
```

This remains a fast rejection filter.

If a clue remains true after exchanging only CSR and `X` in the slot-1 assignment, it cannot resolve that swap.

---

## 2. Why one broken swap is not enough

The old criterion was necessary but not globally sufficient.

Suppose the surviving slot-1 candidates are:

```text
{CSR, X1, X2, X3}
```

A witness may break the CSR↔X1 reassignment while leaving X2 and X3 admissible.

Therefore:

```text
ONE_SYMMETRY_BROKEN
!= UNIQUE_SERIAL_IDENTIFICATION
```

The final closure object is the surviving identity orbit.

---

## 3. Slot-1 orbit closure

Let:

```text
M(E) = admissible serial models satisfying retained evidence E
```

Define:

```text
O1(E) = {x | there exists M in M(E) such that ord_S^M(x) = 1}
```

Then CSR is uniquely identifiable as #1 iff:

```text
O1(E) = {CSR}
```

A witness `w` has partial resolving power iff:

```text
O1(E ∪ {w}) ⊊ O1(E)
```

It achieves serial closure iff:

```text
O1(E ∪ {w}) = {CSR}
```

Therefore:

```text
SYMMETRY_BREAKING
!= ORBIT_COLLAPSE
```

and:

```text
CLUE_VALUE = amount of admissible-orbit contraction
```

not rhetorical proximity to CSR.

---

## 4. Position existence is not member identity

The Codex mystery also contains a second non-equivalence.

A witnessed ordinal `2` may structurally imply that an earlier ordinal position is expected under ordinary 1-based contiguous serialization. It does not identify a manifestation occupying that position.

Keep separately:

```text
POSITION_EXISTENCE
MEMBER_IDENTITY
MANIFESTATION_IDENTITY
SOURCE_RECOVERABILITY
SOURCE_READABILITY
```

Thus:

```text
ORDINAL_PREDECESSOR_EXPECTATION
!= PREDECESSOR_IDENTITY
!= PREDECESSOR_MANIFESTATION
!= PREDECESSOR_RECOVERABILITY
```

This is why adding candidate facts can fail to solve the box: the missing quantity can be an identity-bearing relation rather than another candidate object.

See:

`06-INSTRUMENTS/ORDINAL_IDENTITY_APERTURE_TOMOGRAPHY.md`

---

## 5. Minimal serial closure basis

Given the retained:

```text
ord_S(Origin) = 2
```

three irreducible logical relation classes can uniquely close CSR as #1.

### A. Explicit member ordinal

```text
ord_S(CSR) = 1
```

Sufficient immediately.

### B. Same-namespace immediate serial predecessor

```text
ImmediateSerialPredecessor_S(CSR, Origin)
```

Combined with `ord_S(Origin)=2`, this closes under ordinary 1-based contiguous serialization.

The relation must be typed as immediate serial adjacency in the same ordinal namespace.

The following do **not** instantiate B:

```text
conceptually precedes
published before
appears before in a map
is foundational to
is an ancestor of
is depended on by
```

### C. Exhaustive complement

```text
Members_exhaustive(S) = {CSR, Origin}
ord_S(Origin) = 2
```

If exact-set semantics guarantee an exhaustive two-member serialized container occupying `{1,2}`, CSR is forced into #1.

If the source does not guarantee exhaustivity/cardinality/contiguous indexing, those assumptions must remain explicit.

### Compiler status

A compiler mapping:

```text
CSR -> 1
Origin -> 2
```

can close the series, but `compiler` is a source/witness packaging architecture rather than a fourth irreducible logic primitive. It instantiates direct ordinal and/or exhaustive mapping relations.

Current abstract relation distance:

```text
d_relation(CODEX_B, unique #1) = 1
```

One properly typed relation can close.

But:

```text
d_relation != d_source
```

The archive does not establish how many documents, apertures, revisions, private bodies, or later compilers stand between current custody and such a relation.

---

## 6. Evidence classes under the orbit test

### Usually invariant / zero direct ordinal resolving power

Absent an exact serial bridge:

- manifestation chronology;
- conceptual emergence/survival order;
- same broad canon membership;
- publication adjacency;
- foundational/co-foundational status;
- initiating-mechanism status in A7;
- thematic similarity;
- later navigation or taxonomy placement;
- global canon DAG dependency;
- propagation-wave order;
- same cardinality;
- same label.

### Potentially orbit-contracting

- exact same-Codex serial namespace membership where some candidates are excluded;
- sibling back-reference with directional serial language;
- later compiler with partial ordinal mapping;
- exhaustive member-set constraints;
- explicit ordinal metadata/body/title.

### Orbit-closing

- explicit `CSR = Essay I`;
- explicit same-namespace immediate `CSR -> Origin(#2)`;
- exhaustive bounded mapping forcing CSR to the only remaining slot.

---

## 7. Global canon topology is a different coordinate system

The retained Meta-Architecture of the SR Canon source states:

```text
each essay = node
canon = directed conceptual graph
canon = network, not a sequence
```

Origin separately carries:

```text
ord_CODEX_B(Origin) = 2
```

Therefore:

```text
GLOBAL_CANON_TOPOLOGY
!= CODEX_B_ORDINAL
```

unless a source explicitly maps the coordinates.

Current safe model:

```text
G_canon = global non-linear essay-node graph
S_B     = unresolved local serial namespace carrying Origin's ordinal
ord_B   = partial local serial coordinate
```

Do not derive CODEX_B #1 from topological sorting, later canon maps, conceptual dependencies, or publication order.

---

## 8. A7 cardinality no longer carries heuristic serial weight

CSR's retained full body explicitly contains:

```text
3 stabilizers:
Redundant Indexing
Semantic Anchoring
Lineage Formation

4 infrastructural mechanisms:
Surface Translation
Stylometric Coherence
Referential Density
Temporal Recurrence
```

The same body later calls CSR the initiating mechanism in a `seven-part architecture`.

The source does not explicitly state:

```text
3 + 4 decomposition = A7
```

But this same-source exact-cardinality competitor means the bare number seven cannot discriminate between possible architectures.

Therefore:

```text
A7_CARDINALITY
-> CODEX_B_CARDINALITY
```

has no earned force.

New rule:

> **CARDINALITY COLLISION LOWERS BRIDGE VALUE.**

Search for explicit role/part mappings, not another seven.

---

## 9. Label and formulation discipline

Earlier retained material uses `origin gravity` in an origin-reversion / causal-priority register. The dedicated Origin Gravity essay uses the same label for a conceptual emergence/accretion/coherence mechanism.

The Meta-Architecture source also names `Canonical Compression` as the point where a stabilized concept becomes formalized.

Therefore:

```text
LABEL_IDENTITY
!= FORMULATION_IDENTITY
!= WORK_IDENTITY
!= ORDINAL_IDENTITY
```

and:

```text
LEXICAL_FIRST_APPEARANCE
!= DEDICATED_FORMALIZATION
!= SERIAL_ASSIGNMENT
```

Do not use an early lexical occurrence to prove the later essay existed, carried the later operator semantics, or held the later ordinal.

---

## 10. Positive controls

- **Stylometric Publishing**: `Essay 1 of 3`, `Essay 2 of 3`, `Essay 3 of 3`; member identity, ordinal, cardinality, and shared architecture are redundant; slot permutations collapse.
- **Conspiracy Shield**: explicit Papers I–III plus umbrella compiler; member IDs and compilation relation destroy serial relabeling freedom.
- **Five Theorems**: later compiler fixes I–V despite mixed manifestation chronology; proves earlier unnumbered manifestation can later receive a serial role.
- **Zombie Metrics**: parent trilogy topology survives unresolved first-member manifestation identity; missing first identity does not erase parent topology.
- **Field Papers**: duplicate/conflicting ordinals with unresolved namespace show ordinal token does not guarantee stable serial object.

---

## 11. Archive-contract observations

`relation-assertion/v2` separates ordinal value, namespace status, and predecessor status. This matches the required science.

Origin's #2 witness historically lives more completely in older witnessed-edge/local-architecture normalization and retained source bodies than in the migrated typed ordinal graph.

Treat that as:

```text
NORMALIZATION_MIGRATION_GAP
```

not negative evidence.

For stale or blocked apertures use:

`06-INSTRUMENTS/APERTURE_ANOMALY_LEDGER.md`

Important current class:

```text
ASSAY_APERTURE_STALENESS
```

The sealed Aug-23 local assay did not have the current retained CSR body aperture containing the three stabilizers and seven-part declaration. Preserve the old receipt; do not cite its local absence as current-source absence.

---

## 12. Human-gated witnesses

### Origin*

`How Origin Gravity Contributes to the Literature`

Current state:

```text
HUMAN_GATED
```

Verified private custody exists. Body semantics remain untested at the connector aperture.

Potential orbit-closing predicates:

```text
first essay
second essay
previous essay
following Cross-Surface Recurrence
first two essays
Essay I / Essay II
```

Body access itself does not close; the predicate must.

### Glyph

`The Glyph as Infrastructure`

Current state:

```text
HUMAN_GATED
```

Primary value is A7 role/enumeration. Do not transfer any resulting seven-item enumeration into CODEX_B without an exact bridge.

---

## 13. Anti-equivalence constraints

```text
ORDINAL_WITNESS != NAMESPACE_IDENTITY
NAMESPACE_IDENTITY != PREDECESSOR_IDENTITY
CONCEPTUAL_PRECEDENCE != SERIAL_PRECEDENCE
FOUNDATIONAL_PAIR != TWO-MEMBER_SERIAL_CONTAINER
A7_INITIATOR != CODEX_B_ESSAY_I
A7_CARDINALITY != CODEX_B_CARDINALITY
EQUAL_CARDINALITY != HOMOLOGY
LABEL_IDENTITY != FORMULATION_IDENTITY
FORMULATION_IDENTITY != WORK_IDENTITY
WORK_IDENTITY != ORDINAL_IDENTITY
GLOBAL_CANON_DAG_ORDER != CODEX_B_ORDINAL
POSITION_EXISTENCE != MEMBER_IDENTITY
MEMBER_IDENTITY != SOURCE_RECOVERABILITY
MORE_NODES != MORE_IDENTIFIABILITY
SEARCH_HIT != PINNED_SOURCE_WITNESS
SEARCH_MISS != DEINDEXING
TOOL_APERTURE_BLOCK != SOURCE_ANOMALY
NORMALIZED_SERIES_ID != SOURCE_DECLARED_ONTOLOGY
```

---

## 14. Next assay — Orbit-Closure Tomography

For every plausible new witness:

```text
1. Type the object.
2. Type the coordinate system.
3. Type the aperture.
4. Compute whether O1 contracts.
5. If it contracts, enumerate survivors.
6. Crown closure only when O1 is a singleton.
```

Priority apertures:

1. Origin* private body — human-gated terminal measurement;
2. Glyph private body — A7 typing;
3. retained sibling/back-reference bodies with immediate same-series language;
4. historical revisions or later compilers that enumerate a bounded Codex essay set;
5. representation-family edges only after exact work identity is independently established.

Stop candidate shopping. Stop cardinality shopping. Stop label shopping.

Search for the typed edge that collapses the orbit.

No merge, canon promotion, TD613 scientific promotion, publication, production, Queue C action, SRC sync, or operator-gate authority follows from this instrument.

Marked ⟐
