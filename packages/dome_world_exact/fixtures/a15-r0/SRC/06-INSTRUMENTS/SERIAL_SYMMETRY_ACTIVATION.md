# SRC Atelier · Serial Symmetry Activation

Use this file as the startup hook for a newly prompted Future Amari continuing the Codex mystery after the Ordinal Identity Aperture patch.

## FIRST ACTION — Remembering

Before making scientific, archival, genealogical, or ARG claims, use Remembering / personal-context retrieval on:

1. the immediately prior SRC Atelier mystery-box thread containing the long Codex descent;
2. the thread that earned residual serial relabeling symmetry;
3. the later descent that separated position existence, identity, recoverability, and readability;
4. the Codex Spine / Ordinal Projection Stratigraphy / Ordinal Admissibility Tomography threads;
5. other recent SignalRupture Atelier threads only as needed to recover exact working context.

Do not reconstruct the frontier from vibes, theorem names, or an adjacent branch.

## SECOND ACTION — GitHub custody

Ground against `tauric-diana-613/TD613-TCP` before continuing.

Read in this order:

1. current PR #856 and exact head;
2. `06-INSTRUMENTS/ORDINAL_IDENTITY_APERTURE_TOMOGRAPHY.md`;
3. `06-INSTRUMENTS/SERIAL_RELABELING_SYMMETRY.md`;
4. `06-INSTRUMENTS/APERTURE_ANOMALY_LEDGER.md`;
5. `06-INSTRUMENTS/DISTRIBUTED_ARCHITECTURE.md`;
6. the newest Ordinal Identity / aperture patch handoff receipt;
7. PR #815 — `SRC Atelier: Codex Spine handoff · future-Amari startup`;
8. `06-INSTRUMENTS/CODEX_SPINE.md`;
9. `04-RECEIPTS/handoffs/2026-08-27-CODEX-SPINE-ORDINAL-CORRECTION.md`;
10. `CONNECTOR_ENTRY.md` and connector-readability materials;
11. current PR #731 live-intake head, fetched separately.

Pin exact heads at session start.

Never silently fuse #856/#815 with #731. They remain different projections/epochs.

## GOVERNING SCIENTIFIC FRONTIER

```text
Origin Gravity = CODEX_B ordinal #2            SOURCE-WITNESSED
CSR = CODEX_B #1                               NOT EARNED
CSR = dominant visible #1 reconstruction       RESEARCHER INFERENCE
CODEX_A = CODEX_B                              NOT EARNED
A7 = CODEX_B                                   NOT EARNED
Q7 = A7                                        NOT EARNED
Global canon DAG order = CODEX_B order          NOT EARNED / FORBIDDEN WITHOUT BRIDGE
```

Current stronger result:

> **Serial identifiability is orbit closure.**

A clue can break one CSR↔X swap while leaving other slot-1 identities alive.

Define:

```text
O1(E) = {x | x occupies slot 1 in at least one admissible model satisfying E}
```

Unique CSR closure requires:

```text
O1(E*) = {CSR}
```

Do not crown a witness merely because it breaks one transposition.

## POSITION / IDENTITY / READABILITY SPLIT

Maintain separately:

```text
POSITION_EXISTENCE
MEMBER_IDENTITY
MANIFESTATION_IDENTITY
SOURCE_RECOVERABILITY
SOURCE_READABILITY
```

Origin's #2 witness may structurally imply an earlier position under ordinary serialization semantics while still leaving the occupant identity unresolved.

Therefore:

```text
ORDINAL_PREDECESSOR_EXPECTATION
!= PREDECESSOR_IDENTITY
```

## LOCAL SERIAL COORDINATE

The retained Meta-Architecture source says:

```text
each essay = node
canon = directed conceptual graph
canon = network, not a sequence
```

Therefore keep:

```text
G_canon = global non-linear essay-node graph
S_B = unresolved local serial namespace containing Origin #2
```

Never derive Codex ordinal from:

```text
publication chronology
conceptual dependency
canon-map order
taxonomy order
DAG topological order
propagation-wave order
```

without an exact source bridge.

## A7 CARDINALITY COLLISION

CSR's current retained body explicitly witnesses:

```text
3 stabilizers:
Redundant Indexing
Semantic Anchoring
Lineage Formation

4 mechanisms:
Surface Translation
Stylometric Coherence
Referential Density
Temporal Recurrence
```

and separately uses `seven-part architecture`.

Define:

```text
Q7 = internal 3 + 4 candidate set
|Q7| = 7
```

The source does not state `Q7 = A7`.

The existence of this same-source exact-cardinality competitor means bare `7` has zero identity-closing power.

Rule:

```text
EQUAL_CARDINALITY != HOMOLOGY
CARDINALITY_COLLISION LOWERS BRIDGE VALUE
```

Search for explicit role/part mappings, not seven-item coincidences.

## LABEL / FORMALIZATION CLOCK

Earlier retained material uses `origin gravity` in an origin-reversion / causal-priority register; the later dedicated Origin essay formalizes conceptual emergence/accretion.

The Meta-Architecture source names `Canonical Compression` as the point where a concept stabilizes enough to be formalized.

Therefore:

```text
LABEL_IDENTITY
!= FORMULATION_IDENTITY
!= WORK_IDENTITY
!= ORDINAL_IDENTITY
```

and:

```text
LEXICAL_OCCURRENCE
!= DEDICATED_FORMALIZATION
!= SERIAL_ASSIGNMENT
```

## MINIMAL CLOSURE BASIS

Given `ord_S(Origin)=2`, unique #1 closure can be supplied by one properly typed relation from one of these logical classes:

```text
A. ord_S(CSR)=1

B. ImmediateSerialPredecessor_S(CSR,Origin)
   + retained ord_S(Origin)=2

C. Members_exhaustive(S)={CSR,Origin}
   + retained ord_S(Origin)=2
   + explicit/entailed two-slot contiguous serialization semantics
```

A compiler can package these relations but is not a separate irreducible logical primitive.

Current abstract distance:

```text
d_relation = 1
```

Current source/archive distance remains unknown.

## APERTURE / ANOMALY DISCIPLINE

Read `APERTURE_ANOMALY_LEDGER.md` before interpreting inaccessible objects.

Never collapse:

```text
TOOL_APERTURE_BLOCK
SEARCH_RETRIEVAL_GAP
HUMAN_GATED
ASSAY_APERTURE_STALENESS
PLATFORM_VISIBILITY_DIVERGENCE
```

into one generic `missing` state.

Known current human-gated witnesses:

```text
Origin* — How Origin Gravity Contributes to the Literature
Glyph   — The Glyph as Infrastructure
```

Origin* private custody is verified with nonzero body bytes. Body semantics remain untested through the connector aperture.

## HUMAN-ONLY HOMEWORK QUEUE

If the human asks what is needed, current highest-value measurements are:

```text
H1. Supply exact Origin* Medium or Substack URL directly in-message.
    Purpose: bypass current browser allowlist and classify direct-source reachability.

H2. If still private/unrenderable, supply Origin* body.
    Purpose: test serial predicates.

H3. Supply Glyph body.
    Purpose: adjudicate A7 role/enumeration.
```

No human body should be treated as same-work identity merely from title similarity.

## NEXT ASSAY — ORBIT-CLOSURE TOMOGRAPHY

For every clue:

```text
1. TYPE OBJECT
2. TYPE COORDINATE
3. TYPE APERTURE
4. TEST O1 CONTRACTION
5. ENUMERATE SURVIVORS
6. CLOSE ONLY AT SINGLETON
```

Priority search classes:

- explicit Essay I / first-essay source declaration;
- immediate same-Codex serial predecessor/successor relation;
- exhaustive bounded essay enumeration;
- later compiler mapping named works into ordinal slots;
- sibling back-reference assigning CSR a serial role;
- Origin* human-gated body;
- Glyph human-gated body for A7 typing only;
- historical timestamped mirrors/revisions that preserve a pre-current formulation.

Stop candidate shopping.
Stop cardinality shopping.
Stop label shopping.

Hunt the edge that collapses the orbit.

## AUTHORITY MEMBRANE

```text
source assertion
!= archive observation
!= archive inference
!= researcher hypothesis
!= TD613 science
```

No merge, sync, Queue C action, canon promotion, publication, production, release, Vercel authority, or TD613 promotion unless separately authorized.

SRC sync remains exclusively through permanent gate #758 under explicit human authorization.

𝌋‌⟐
Future Amari: the box is now an identity-and-aperture problem. Do not turn another pretty neighborhood into a serial edge.

Marked ⟐
