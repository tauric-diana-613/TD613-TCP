# SMS Embedded Subdocument and Logical-Node Retype — Source Witness

Status: `POST_PREREG_SOURCE_WITNESS / CORRECTION_BY_NEW_BODY_EVIDENCE`

This note corrects the immediately prior interpretation of the SR Measurement Standard (SMS). The prior witness correctly reported that no standalone captured manifestation currently resolves under the full declared SMS title. New inspection of the Matrix Field body shows that SMS content itself is not missing: the full SMS is embedded inside the Matrix Field PDF.

The prior witness is not rewritten; this note records the evidentiary retype.

## 1. Physical container

The captured Zenodo manifestation `zenodo:22073903`, titled `THE SR MATRIX FIELD`, begins as a field declaration and foundational charter.

After the field's theoretical-foundations section, the same PDF begins a separately titled internal work:

```text
ANCHORING DOCUMENT
THE SR MEASUREMENT STANDARD (SMS)
Latent Variables of the Infrastructural Order
A Unified Structural Equation Model for Slow Harm, Harm Stack, and the Eroded Subject
```

The SMS then proceeds as a substantial independent technical document.

## 2. SMS content is materially present

The embedded SMS contains, among other material:

```text
latent-variable framework
nine infrastructural constructs ξ1–ξ9
endogenous constructs η1–η3
Blind System ω
measurement equations
canonical structural equation
Infrastructure Determinism matrix Γ
Recursive Erosion matrix B
spectral-radius stability regimes
Slow Harm temporal operator
indicator architecture
measurement invariance
identification and estimation requirements
falsification propositions
cross-domain application rules
full measurement hierarchy
canonical mathematical form
```

It reaches a numbered `29. Canonical Closing Statement` and then continues through four operational appendices:

```text
Appendix A — Technical Validation and Operationalization Standard
Appendix B — Indicator Dictionary and Operationalization Standard
Appendix C — Estimation, Coding, and Reproducibility Protocol
Appendix D — Canada Empirical Implementation of the SR Measurement Standard
```

The final appendix conclusion states:

```text
SMS = Theory + Measurement + Estimation + Reproducibility + Empirical Testing
```

Thus:

```text
SMS_CONTENT_RETRIEVABLE = true
SMS_FULL_INTERNAL_DOCUMENT_STRUCTURE = witnessed
SMS_STANDALONE_MANIFESTATION = unresolved
```

## 3. Logical canon nodes remain distinct

The August SignalRupture Canon Map independently assigns:

```text
C13  SR Measurement Standard  — Canon method
C14  THE SR MATRIX FIELD      — Quantitative program
```

and lists the Measurement Standard and Matrix Field as separate canonical outputs under Pillar 2.

Therefore the canon's logical object model distinguishes SMS from Matrix Field even though the captured physical manifestation packages SMS inside the Matrix Field PDF.

Archive-side statement:

```text
LOGICAL_CANON_NODE != PHYSICAL_MANIFESTATION_BOUNDARY
```

and, for this observed container:

```text
one captured PDF -> multiple logical canon objects
```

## 4. Corrected ontology

Prior provisional model:

```text
named canonical anchor
-> downstream interface traces
-> no resolved standalone manifestation
```

Corrected model:

```text
logical canon node SMS
-> physically embedded as titled subdocument inside Matrix Field manifestation
-> separately recognized as C13 by Canon Map
-> no independent standalone manifestation currently resolved
```

This resembles compiler inlining / bundling in archive terminology only. No claim is made that SR intentionally used software-style compilation semantics.

## 5. Why this matters for the SRC mystery

Title-level cataloging of `zenodo:22073903` exposes only the parent manifestation identity `THE SR MATRIX FIELD`.

Full-body parsing exposes a second logical work with its own:

- title;
- declared role;
- numbered sections;
- mathematical architecture;
- canonical closing statement;
- appendices;
- validation protocol;
- empirical implementation.

Thus aggregation and internal document segmentation recover conceptual multiplicity that manifestation-level title matching alone cannot see.

This is a direct fixture for:

```text
work multiplicity != manifestation multiplicity
```

and for the broader SRC question of whether higher-order architecture becomes visible only after content is aggregated and typed.

## 6. Dashboard remains a different unresolved species

The May Dashboard has not received a comparable retype. Current evidence still supports:

```text
Dashboard source-declared dependency = witnessed
Dashboard downstream scoring grammar = witnessed
Dashboard standalone manifestation = unresolved
Dashboard full embedded body = not currently witnessed
```

Therefore:

```text
SMS_EMBEDDED_LOGICAL_NODE != DASHBOARD_UNRESOLVED_DEPENDENCY
```

## 7. Required next assays

1. search the corpus for other internally titled `ANCHORING DOCUMENT` / `FOUNDATIONAL DOCUMENT` blocks;
2. detect substantial titled subdocuments inside parent manifestations;
3. compare internal logical titles against Canon Map node identities;
4. distinguish appendix, embedded work, anthology, and ordinary section headings;
5. preserve logical-work boundaries without manufacturing standalone manifestations;
6. test whether other canon nodes lacking standalone manifestations are compiler-inlined inside larger artifacts.

## Claim ceiling

Promoted:

```text
SMS_CONTENT_EMBEDDED_IN_MATRIX_FIELD_MANIFESTATION_WITNESSED
SMS_LOGICAL_NODE_DISTINCT_FROM_PARENT_CANON_NODE_WITNESSED
LOGICAL_CANON_NODE_NOT_EQUAL_PHYSICAL_MANIFESTATION_BOUNDARY_WITNESSED
MULTIPLE_LOGICAL_OBJECTS_IN_ONE_CAPTURED_MANIFESTATION_WITNESSED
```

Held:

```text
INTENTIONAL_HIDDEN_DOCUMENT = unearned
SOFTWARE_COMPILER_SEMANTICS = archive analogy only
SMS_EVER_HAD_STANDALONE_PUBLIC_MANIFESTATION = unknown
MATRIX_FIELD_IS_FORMAL_ANTHOLOGY = not source-declared
```

## Non-collapse

```text
embedded content != standalone manifestation
logical work != file boundary
canon node != DOI record
internal title != independent publication history
content retrievability != manifestation retrievability
```

U+10D613

𝌋

⟐
