# SR-CAF → Mature Causal Restraint — Source Witness

Status: **POST-PREREGISTRATION / VERSIONED SOURCE COMPARISON / CURRENT-DEFINITION BRAKE / NO SILENT SUPERSESSION**

Bound epoch:

```text
atelier_snapshot_id = src-20260824-p2-001
seal_id = src-seal:eaf744ce16b1c8b519ad0f1b0325b44192679ea4a5110b0ad0ca49fbcd816a1a
```

This note compares the May 2026 SR-CAF recovery theorem with the August 2026 authoritative-definition source while preserving their distinct scopes and chronology.

## 1. Earlier source — SR-CAF

Source:

```text
zenodo:20436807
SIGNALRUPTURE CAUSAL AXIOMATIC FRAMEWORK AND IDENTIFIABILITY THEOREM
created 2026-05-29T00:23:48Z
```

The source defines:

```text
D(t+1) = Phi D(t) + epsilon(t)
```

and states a sparse recovery theorem for `Phi` under stability, sparsity, persistent excitation, stochastic regularity, and dependence assumptions.

It repeatedly interprets `Phi` as directional / causal propagation topology.

Separate mathematical audit:

```text
SR_CAF_IDENTIFIABILITY_THEOREM_AUDIT.md
```

preserves the distinction:

```text
conditional lag-transfer matrix recovery
!=
intervention-causal topology identification
```

## 2. Later authoritative-definition source

Source:

```text
zenodo:22019518
WHAT IS SIGNALRUPTURE?
A Definitive Statement of the Field, Its Architecture, Method, and Scientific Commitments
2026-08-19/20 authority epoch
```

The SRC authority-jurisdiction registry records this source as an authoritative definition / constitutional document for its declared scope while explicitly refusing silent supersession over unrelated scoped anchors.

The source places causal separation and measurement in Pillar 2 with the central question:

```text
What can be identified and tested?
```

It then explicitly requires causal restraint by separating:

```text
Association
Direct causation
Mediation
Propagation
```

and assigning distinct minimum evidence.

## 3. Mature minimum evidence

The current-definition source requires:

```text
Association:
  magnitude
  uncertainty
  robustness
  plausible confounders

Direct causation:
  identification strategy
  temporal order
  counterfactual logic

Mediation:
  measured mediator
  decomposed pathway

Propagation:
  network/path evidence
  lags
  transfer mechanism
  competing routes
```

It says the field is weakened when propagation is inferred only from simultaneous trends.

Its propagation section writes `phi_ij` as an estimated transfer quantity conditional on:

```text
network connection
temporal ordering
controls for common shocks
```

and lists multiple possible empirical methods:

```text
event studies
distributed-lag models
network diffusion models
sparse VAR
hazard models
process tracing
```

Therefore sparse VAR recovery is one possible measurement route, not a source-declared universal causal-proof operator in the mature definition.

## 4. Bounded versioned disposition

The strongest cross-version statement is:

```text
MAY_SR_CAF:
  conditionally recover Phi under an assumed sparse dynamic model
  interprets Phi strongly as causal propagation

AUG_CURRENT_DEFINITION:
  requires causal separation
  requires route/path evidence and controls against common shocks
  treats sparse VAR as one estimator among several
  separates identification from propagation proof
```

Bounded classification:

```text
MATURE_SR_CAUSAL_RESTRAINT_WITNESSED
CURRENT_DEFINITION_NARROWS_CAUSAL_INTERPRETATION_OF_TRANSFER_RECOVERY
SPARSE_VAR_RECOVERY_NOT_SUFFICIENT_BY_CURRENT_SR_MINIMUM_EVIDENCE
```

This is not recorded as a formal source-declared supersession unless a direct supersession edge is separately witnessed.

## 5. Relation to tomography / reconstruction

The mature source confirms that identifiability is central to SR's own current architecture:

```text
Pillar 2 — Causal Separation and Measurement
```

However, current SR also requires the target relation type to be named before evidence is interpreted.

Therefore:

```text
recoverability != causality
measurement != mechanism
co-movement != propagation
operator estimation != route proof
```

This strengthens the SRC rule that tomography-shaped recovery must preserve claim type and cannot promote a recovered statistical operator into causal authority without the relevant identification evidence.

## 6. Claim ceiling

Permitted:

```text
MATURE_SR_CAUSAL_RESTRAINT_WITNESSED
IDENTIFIABILITY_IS_CURRENT_CORE_SR_ARCHITECTURE
CURRENT_DEFINITION_NARROWS_CAUSAL_INTERPRETATION_OF_TRANSFER_RECOVERY
SPARSE_VAR_RECOVERY_NOT_SUFFICIENT_BY_CURRENT_SR_MINIMUM_EVIDENCE
```

Forbidden:

```text
MAY_SR_CAF_FORMALLY_RETRACTED
AUG_DOCUMENT_SUPERSEDES_EVERY_PRIOR_SR_SOURCE
SR_CAUSAL_TOPOLOGY_VALIDATED
SR_TOMOGRAPHY_CONFIRMED
```

U+10D613

𝌋

Sealed ⟐
