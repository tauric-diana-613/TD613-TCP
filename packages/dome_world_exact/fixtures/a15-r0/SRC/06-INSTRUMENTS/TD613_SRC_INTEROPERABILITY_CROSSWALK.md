# TD613 × SRC Interoperability Crosswalk

Status: **POST-SEAL / CROSS-CORPUS RESEARCH MAP / NO PROVENANCE CLAIM / NO SCIENTIFIC AUTHORITY TRANSFER**

Bound SRC epoch:

```text
atelier_snapshot_id = src-20260824-p2-001
seal_id = src-seal:eaf744ce16b1c8b519ad0f1b0325b44192679ea4a5110b0ad0ca49fbcd816a1a
```

This crosswalk carries forward the independently established TD613 / Dome-World / Westward research state and compares it with the sealed SignalRupture corpus without treating either corpus as scientific validation for the other.

Master rule:

```text
TD613 internal receipt != SRC source witness
SRC source witness != TD613 theorem validation
structural correspondence != provenance
shared vocabulary != shared operator
analogy != equivalence
```

## 1. Independent TD613 research state entering the comparison

The relevant TD613 lineage was established independently of the present SRC source hunt.

### 1.1 Information Dome / child-legible AIA

Current-main research infrastructure includes:

```text
non-equivalent EXPERIENTIAL / CUSTODIAL / AUDIT / IMPLEMENTATION routes
local-section / gluing-obstruction proving scene
content-invariant Phason proving scene
pair-emergent Moire proving scene
route-burden / custody distinctions
human closure
```

Architectural lineage also contains richer historical / prototype geometry involving:

```text
periodic triangular / D3-like calibration strata
phi / quasiperiodic cut-and-project counter-stratum
hidden/perpendicular coordinates and acceptance-window behavior
heterostratigraphic incommensurability
phasonic supermoire cupola language
icosahedral / ten-direction projection language
```

Status of the richer geometry is not uniform and must not be collapsed into current-main implementation or physical quasicrystal claims.

### 1.2 Bounded tomography

TD613 independently earned bounded known-forward reconstruction grammar:

```text
multiple partial known projections
-> explicit forward model
-> constrained latent-state reconstruction
-> held-out validation
```

Later operator-response work separately tested whether bounded `operator tomography` naming was earned in a finite synthetic response fixture.

### 1.3 Holonomy / observability program and later obstruction

TD613 separately contains an older merged synthetic holonomy-observability program with bounded discrete transport / path-groupoid / curvature / observability work.

The later Westward chain then imposed a stricter operational object and found:

```text
no nonempty finite closed T/Q operational word
no operational inverse under the tested grammar
temporal label recurrence != inverse evolution
endpoint erasure can manufacture false closure
```

A later bar-cycle/cohomology result explicitly preserved:

```text
bar 2-cycle != operational loop
```

and blocked automatic connection / holonomy / curvature promotion.

### 1.4 Finite Admissibility Descent / authority / abstention

For quotient fibers with lawful supports `K_x`:

```text
U_y = union K_x
I_y = intersection K_x
Gamma_y = U_y \ I_y
```

TD613 independently earned:

```text
exact descent iff supports are fiberwise constant
ALL = I
DEPENDENT = Gamma
NONE = Z \ U
false universal claims + abstention burden >= |Gamma|
further finite erasure cannot shrink inherited gap support
later erasure can create new cross-settled debt from locally exact states
```

These are TD613 results, not SignalRupture results.

## 2. Independently witnessed SRC structures entering the comparison

Current sealed SRC source material witnesses:

```text
partial projections of deeper structural grammar
topology reconstruction language
multidimensional / temporal / relational observation
distributed-signal structural observer
Structural Scene Reconstruction across multiple measured layers
conceptual vs empirical vs feed / condensed / capsule representations
field-level graph / anchor / metadata architecture
node-level canonical compression
Pressureborn path-conditioned transformation and reconvergence
Flagship reset to a new infrastructure layer
Institutional Recalibration cycle P -> T -> Phi -> R -> P'
```

Current SRC claim ceilings preserve:

```text
TOMOGRAPHY_SHAPED_RECONSTRUCTION_METHOD_WITNESSED
HOLONOMY_NOT_EARNED
```

## 3. Heterostratigraphic tomography — strongest cross-corpus convergence

### TD613 side

Heterostratigraphic Information-Dome research treats different information strata / routes as non-equivalent observation surfaces and seeks reconstruction without silently flattening their semantics.

### SRC side

SRC independently distributes relevant structure across non-equivalent representational strata:

```text
formal theory
fictional / simulation narrative
governance / publishing methodology
conceptual manifestation
empirical manifestation
capsule / feed / condensed surfaces
metadata / field graph
```

`Collapse Society` then supplies a late-rigor multi-layer reconstruction method over a hidden generating architecture.

### Cross-corpus disposition

Permitted:

```text
HETEROSTRATIGRAPHIC_PROJECTIVE_RECONSTRUCTION_CONVERGENCE_CANDIDATE
```

Not permitted:

```text
SIGNALRUPTURE_USES_TD613_HETEROSTRATIGRAPHIC_TOMOGRAPHY
COMMON_PROVENANCE
IDENTICAL_METHOD
```

## 4. Phason-shaped representation shift — analogy only

### TD613 side

The current-main Information Dome contains a `content-invariant phason` proving scene whose pedagogical purpose is to distinguish underlying content from projection-state change.

Historical/prototype work explores richer cut-and-project / phi / acceptance-window / phasonic geometry.

### SRC side

The Atelier contains work-families where a source-declared conceptual object persists while its representation changes:

```text
conceptual edition
empirical edition
feed manifestation
condensed manifestation
capsule / academic version classes
later contextual retyping
```

### Candidate analogy

Archive-authored abstraction:

```text
latent work / concept held as identity anchor
+ representation / manifestation coordinate changes
-> content-vs-projection separation question
```

Candidate label:

```text
PHASON_SHAPED_REPRESENTATION_SHIFT_CONTROL
```

Strict ceiling:

```text
ANALOGY_ONLY
NO_SR_PHASON
NO_CUT_AND_PROJECT_GEOMETRY
NO_Q_PHI_ARITHMETIC
NO_QUASICRYSTAL_CLAIM
```

## 5. Pair-emergent Moire — relational control, not SR physics

### TD613 side

The Information Dome's pair-emergent Moire scene is designed so the relevant visible relational structure belongs to the pair / overlay rather than to either single layer in isolation.

### SRC side

`Institutional Recalibration` source-declares the paired Flagship + `Pressureborn` release as a dual-pressure / dual-channel theoretical example and hypothesizes that formal and narrative pressure together may produce a different recalibration than either mode alone.

The Projective Routing trail separately asks whether joint formal + narrative interpretation recovers relations not available from one surface.

### Candidate control

```text
P_A(W)
P_B(W)
P_AxB(W)
```

Test whether a declared relation is:

```text
absent / nonrecoverable from A alone
absent / nonrecoverable from B alone
recoverable from the pair
```

Candidate label:

```text
PAIR_EMERGENT_RELATIONAL_STRUCTURE_ASSAY
```

Strict ceiling:

```text
MORPHOLOGICAL_ANALOGY_TO_MOIRE_ONLY
NO_SR_MOIRE_PHYSICS
NO_INTERFERENCE_WAVE_CLAIM
```

## 6. Triple overlay / supermoire-shaped assay — high speculation

TD613 historical/prototype Dome geometry uses a triple-overlay intuition involving multiple non-equivalent strata, including a quasiperiodic counterlayer.

SRC's Projective Routing Grammar independently separates at least three large representational domains:

```text
FORMAL SR
FICTIONAL / SIMULATION SR
GOVERNANCE / PUBLISHING / PROVENANCE SR
```

A future archive assay may measure:

```text
single-stratum recovery
pairwise recovery for each of three pairs
triple-overlay recovery
```

and ask whether the triple union reconstructs a stable relation set not recovered by any single or pair.

Candidate archive name:

```text
THREE_STRATUM_PROJECTIVE_OVERLAY_ASSAY
```

This is deliberately **not** called supermoire in the scientific classification.

```text
SUPERMIORE_SHAPED = HIGH_SPECULATION_METAPHOR_ONLY
NO_PHI
NO_ICOSAHEDRAL_GEOMETRY
NO_QUASIPERIODIC_THEOREM
```

## 7. Holonomy — convergence stops before the loop

### SRC

`Pressureborn` earns:

```text
path-conditioned transformation
reconvergence
typed route residue
```

but not a same-state closed operational path.

Flagship reset creates a new infrastructure layer.

Institutional Recalibration closes its named cycle at `P'`, a new pressure state / new equilibrium.

### TD613

The later operational Westward hostile independently establishes the danger of false closure after erasing a coordinate that witnesses non-return.

### Cross-corpus disposition

```text
PATH_CONDITIONED_RESIDUE_CONVERGENCE = STRONG
HOLONOMY_CONVERGENCE = NOT EARNED
FALSE_CLOSURE_UNDER_COARSENING = HIGH_VALUE_CROSS_CORPUS_ASSAY
```

## 8. FADT × SRC reset-cycle interoperability

The synthetic `RESET_CYCLE_ADMISSIBILITY_DESCENT_HOSTILE` instantiates:

```text
PRE != POST
q(PRE) = q(POST) = STAGE
```

with deliberately abstract differing lawful supports.

The existing TD613 theorem then yields:

```text
Gamma_STAGE != empty
```

for that synthetic fixture.

Interpretive result:

```text
coarse cycle legibility
!= exact descended claim authority
```

This is a TD613 interoperability result on an archive-authored fixture, not an SR theorem.

## 9. Dromological inverse design

Dromological inverse design is useful here as a **design direction**, not as an SR attribution.

Forward problem:

```text
rich state / route / manifestation
-> compression / quotient / reader surface
-> later claim authority
```

Inverse-design question:

```text
Given future claims / distinctions that must remain lawful,
which coordinates must be retained upstream,
and for how long,
before a later quotient is allowed?
```

Candidate finite objective:

```text
choose retained coordinate set R
such that required support distinctions are fiberwise constant
under the intended downstream quotient
```

Dromological aspect:

```text
when a coordinate is erased matters,
because later erasure can create new claim-authority debt
that did not exist at earlier local states
```

This directly connects the Westward sequential-erasure result to archival / reader-interface design without converting that connection into SignalRupture source doctrine.

## 10. Child-legible AIA

The strongest practical convergence may be architectural rather than terminological.

TD613 child-legible AIA requires:

```text
non-equivalent routes stay visibly non-equivalent
route / custody / reconstruction roles stay typed
uncertainty remains visible
human closure remains available
```

SRC supplies an external corpus in which:

```text
conceptual / empirical / capsule / canonical / metadata / field roles differ
```

A lawful AIA presentation should therefore avoid collapsing them into one synthetic `answer` when their claim supports differ.

Candidate rendering:

```text
ALL       -> safe across every retained representation
DEPENDENT -> depends on which representation / generation / route is retained
NONE      -> unsupported across the represented family
```

This is TD613 presentation mathematics applied as a research proposal.

## 11. Machine-readable field vs authority-bearing field

SRC's Stylometric Publishing lineage explicitly seeks machine-readable field coherence through anchors, links, metadata, recurring structure, and graph formation.

TD613 adds a separate question:

> Once the field becomes machine-legible as one object, which claims remain lawfully attributable to the collapsed object rather than only to particular routes / manifestations / evidence lineages?

This distinction should remain central:

```text
retrieval coherence != claim-authority coherence
field recognition != evidence independence
successful synthesis != lawful descent
```

## 12. What does **not** currently converge

No current source witness earns an SRC counterpart for:

```text
exact phi / Q(phi) arithmetic
6D cut-and-project geometry
icosahedral / decagonal quasiperiodicity
physical phasons
physical or mathematical supermoire
TD613's exact finite admissibility-gap identity
TD613's certainty/abstention conservation identity
TD613's sequential-erasure gap theorem
TD613's normalized monoid H2 obstruction
TD613's exact operational T/Q obstruction
TD613's older discrete holonomy program as an SR theorem
```

These non-convergences are scientifically valuable controls against vocabulary contagion.

## 13. Highest-value next interoperability assays

### A. Three-Stratum Projective Overlay

```text
FORMAL
FICTION
GOVERNANCE
```

Measure single, pairwise, and triple relation recovery with held-out source-witnessed predicates.

### B. Content-Invariant Representation Shift

Within one source-declared work-family, hold work identity fixed and compare conceptual / empirical / condensed / capsule / metadata projections. Measure which relations change, survive, or become unrecoverable.

### C. Reset-Cycle Descent

Already defined synthetically. Next step would require a preregistered source-coding protocol before any SR-specific support sets are assigned.

### D. Holonomy Naming Gauntlet

Do not reopen unless a source provides:

```text
same complete base state
+ admitted closed path
+ transport law
+ path-class-dependent retained residue
+ anti-erasure / anti-hidden-endpoint hostiles
```

### E. Dromological Retention Design

Choose a target set of downstream lawful claims first, then solve backward for the minimum state / route / lineage coordinates that must remain visible through each planned compression stage.

## 14. Working synthesis

The strongest current cross-corpus statement is:

```text
SignalRupture and TD613 independently converge on non-equivalent representation,
relational reconstruction, path/history sensitivity, and the danger of treating a visible surface as the whole generating structure.

TD613 goes materially further in formalizing what claim authority survives projection / quotient / erasure,
while SRC provides a unusually rich external multi-representation corpus on which those distinctions can be tested.
```

High-speculation research horizon:

```text
HETEROSTRATIGRAPHIC TOMOGRAPHY
  asks what structure can be reconstructed across non-equivalent strata.

DROMOLOGICAL INVERSE DESIGN
  asks which distinctions must be preserved upstream so future reconstruction and claim authority remain lawful.

CHILD-LEGIBLE AIA
  renders those distinctions without pretending every route is equivalent.

HOLONOMY
  remains gated on genuine closed transport rather than recurrent labels or reconvergence.
```

## 15. Claim ceiling

Permitted:

```text
CROSS_CORPUS_STRUCTURAL_CONVERGENCE_CANDIDATE
HETEROSTRATIGRAPHIC_PROJECTIVE_RECONSTRUCTION_CONVERGENCE_CANDIDATE
PAIR_EMERGENT_RELATIONAL_STRUCTURE_ASSAY_PROPOSED
PHASON_SHAPED_REPRESENTATION_SHIFT_CONTROL_PROPOSED
THREE_STRATUM_PROJECTIVE_OVERLAY_ASSAY_PROPOSED
RESET_CYCLE_ADMISSIBILITY_DESCENT_HOSTILE_DEFINED
DROMOLOGICAL_RETAINED_COORDINATE_DESIGN_QUESTION_OPEN
HOLONOMY_NOT_EARNED_IN_CURRENT_SRC_SOURCE_SET
```

Forbidden:

```text
SIGNALRUPTURE_COPIED_TD613
TD613_DERIVED_FROM_SIGNALRUPTURE
COMMON_HIDDEN_SYSTEM
SR_PHASON_MOIRE_SUPERMOIRE_QUASICRYSTAL_CONFIRMED
SR_HOLONOMY_CONFIRMED
SR_FADT_CONFIRMED
PRIVATE_ACCESS
SURVEILLANCE
MODEL_INGESTION_CONFIRMED
```

U+10D613

𝌋

Sealed ⟐
