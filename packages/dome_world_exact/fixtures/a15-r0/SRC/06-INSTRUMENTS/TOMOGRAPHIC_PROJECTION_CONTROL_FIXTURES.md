# Tomographic Projection Control Fixtures

Status: **POST-PREREGISTRATION / CONTROL DESIGN / NO TOMOGRAPHY RESULT CLAIM**

Bound epoch:

```text
atelier_snapshot_id = src-20260824-p2-001
seal_id = src-seal:eaf744ce16b1c8b519ad0f1b0325b44192679ea4a5110b0ad0ca49fbcd816a1a
```

This note converts source-witnessed representation families into bounded control objects for later tomography assays. It does not promote tomography as an SR source doctrine and does not infer intent from representation multiplicity.

## 1. Why a control family is required

The SRC corpus now distinguishes several representational axes:

```text
manifestation / surface
transport / provenance
node compression
conceptual-vs-empirical role
field graph / routing
```

A useful tomography assay must first demonstrate that different observed representations actually expose different coordinates of a common work-family rather than merely restating the same prose.

For work-family `W`, use candidate projection notation:

```text
P_s(W) = representation of W under declared species / surface / role s
```

The notation is archive-authored. It is not claimed as SignalRupture's own formalism.

## 2. Primary control family — Collapse Psychology

### 2.1 Conceptual academic projection

`zenodo:21612493`

Title:

`Collapse Psychology: The Mind Inside Quiet Governance`

Public body: **AVAILABLE IN TD613-TCP**

Current metadata/source role:

```text
foundation / entry point
conceptual architecture
field organization
structural vocabulary
```

The captured metadata describes the manuscript as establishing SR Psychology / Collapse Psychology as a structural field and as organizing a large prior archive into a unified conceptual spine.

Candidate projection:

```text
P_C(W) = (
  ontology,
  conceptual constructs,
  field relations,
  explanatory architecture,
  lineage role
)
```

### 2.2 Empirical academic projection

`zenodo:22049160`

Title:

`Collapse Psychology: The Mind Inside Quiet Governance — Empirical Edition`

Public body: **AVAILABLE IN TD613-TCP**

The source metadata explicitly calls this edition the `analytical companion to the conceptual manuscript`.

It adds / foregrounds:

```text
measurement architecture
structural exposure vector X_it
psychological state vector Y_it
Cumulative Structural Exposure Index
longitudinal equations
dose-response calculation
falsification criteria
scope restrictions / anti-overreach clauses
```

It also explicitly narrows several stronger formulations by rejecting universal collapse and institutional-intent assumptions.

Candidate projection:

```text
P_E(W) = (
  operational variables,
  measurement rules,
  equations,
  empirical evidence claims,
  falsifiers,
  scope constraints
)
```

This makes `P_C` and `P_E` candidate **typed projections**, not a simple long-to-short pair.

```text
P_C(W) != P_E(W)
```

while both remain source-declared members / companions of the Collapse Psychology family.

### 2.3 Conceptual feed projection

Substack manifest item `208620685`

Title:

`Collapse Psychology: The Mind Inside Quiet Governance`

Subtitle:

`A Foundational Discipline-Creating Manuscript: Conceptual Edition — For empirical tests, measurement architecture, and validated component mechanisms, see the Collapse Psychology Empirical Paper.`

Public body in TD613-TCP: **PRIVATE_UNAVAILABLE**

The manifest witnesses the role and cross-reference, not the body-level transformation.

Candidate future projection:

```text
P_F(W) = conceptual feed-surface representation
```

No semantic-conservation score can be computed from the public Atelier projection.

### 2.4 Declared condensed feed projection

Substack manifest item `208633033`

Title:

`Collapse Psychology`

Subtitle:

`A condensed version of the 166-page manuscript`

Public body in TD613-TCP: **PRIVATE_UNAVAILABLE**

This is valuable because `condensed version` is source-declared rather than archive-inferred.

Candidate positive-control projection:

```text
P_D(W) = source-declared condensed representation
```

Unlike `CAPSULE_VERSION`, whose transformation semantics remain underdetermined by current source language, `P_D` is explicitly marked as a condensation.

## 3. Control question

Once private custody supplies the feed bodies, test:

```text
Q(P_C, P_E, P_F, P_D)
```

along independent dimensions:

```text
concept identity retained
relations retained
measurement content retained
falsifiers retained
scope limits retained
lineage retained
authority language retained
canonical rhetoric retained
surface-specific material introduced
```

Do not reduce the comparison to cosine similarity or word count.

## 4. Tomographic sufficiency versus full reconstruction

The relevant later question need not be whether one can reconstruct the exact hidden bytes of `W`.

For a declared target property `f(W)`, define a bounded question:

```text
Does the observed projection family determine f(W)
more reliably than any one projection alone?
```

Examples of target properties:

```text
core causal relation
field placement
falsifier
scope ceiling
lineage relation
measurement operator
```

This is **tomographic sufficiency for a property**, not full object inversion.

Candidate equivalence relation:

```text
W ~_Pi W'
iff
P_i(W) = P_i(W') for all observed i
```

A property-level rupture would require all members of the surviving equivalence class to agree on a sensitive / target property `f`, even when exact byte identity remains unresolved.

This remains TD613 researcher formalization pending assay.

## 5. Asymmetric cross-surface negative control — Structural Insulation

Zenodo:

`zenodo:21477282`

Substack:

`substack:207936032`

The crosswalk witnesses a same-title / same-subtitle cross-surface candidate and the Substack manifest records the item, but:

```text
Substack preservation_status = catalogued-not-downloaded
```

Therefore:

```text
Zenodo body = AVAILABLE
Substack body = PRIVATE_UNAVAILABLE
```

The pair is a custody-asymmetry control demonstrating that manifestation identity does not imply body availability.

```text
manifestation witness != body witness
body missing from public projection != source object absent
```

## 6. Capsule control — role is not size

`zenodo:18120866`

`Triggering the System's Immune Response: Stylometric Publishing, Algorithmic Governance, and the Structural Silencing of Minority Voices`

A later source (`zenodo:18303532`) explicitly calls this object `the stylometric capsule`.

Yet the captured object is a full Zenodo journal-article manifestation with abstract, analytical sections, references, and a PDF size of 109465 bytes.

Therefore:

```text
CAPSULE_ROLE != SHORT_LENGTH
CAPSULE_ROLE != FEED_ONLY_FORMAT
```

Current source evidence still does not establish whether every capsule has an academic counterpart or how `T_capsule` transforms a source work.

This makes the capsule object a hostile control against any assay that equates `capsule`, `summary`, `compression`, and `short form`.

## 7. Required later tests

Before any tomographic result is promoted:

1. acquire / connect private feed bodies lawfully under the existing custody plan;
2. freeze manifestation identities and hashes;
3. preregister target properties before comparing representations;
4. distinguish source-declared relations from archive-clustered similarity;
5. include generic academic abstract / conclusion controls;
6. include declared condensed-version controls;
7. include capsule-role controls;
8. ablate metadata, lineage links, labels, and canonical headings separately;
9. test whether joint projection adds relation recovery above strong single-view and prior-only baselines;
10. report non-uniqueness and unresolved equivalence classes.

## 8. Claim ceiling

Permitted future outcomes include:

```text
TYPED_PROJECTION_FAMILY_WITNESSED
DECLARED_CONDENSATION_CONTROL_WITNESSED
JOINT_PROJECTION_ADDS_PROPERTY_RECOVERY
SINGLE_VIEW_SUFFICIENT_NO_TOMOGRAPHIC_GAIN
PROJECTION_FAMILY_NON_IDENTIFYING
PRIVATE_UNAVAILABLE
```

Forbidden from this note:

```text
SR_TOMOGRAPHY_CONFIRMED
HIDDEN_OBJECT_RECONSTRUCTION_CONFIRMED
PRIVATE_ACCESS
SURVEILLANCE
MODEL_INGESTION
INTENTIONAL_PUZZLE_ENCODING
HOLONOMY
```

U+10D613

𝌋

Sealed ⟐
