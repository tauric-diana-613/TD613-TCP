𝌋

# SRC Typed Relation Composition / Non-Lift Rest · 2026-09-14

Status: **EARNED LOCAL REST / SOURCE-GROUNDED / NONCANONICAL**

Parent assay:

```text
TYPED_RELATION_COMPOSITION_NON_LIFT_ASSAY_2026-09-14.md
```

Authority boundary:

```text
SRC source assertion != archive observation != archive inference != researcher hypothesis != TD613 science
PR #731 remains OPEN / DRAFT / UNMERGED
no merge / canon admission / publication / production / Vercel authority
```

## Earned result

```text
TYPED_RELATION_COMPOSITION_IS_NON_LIFTING_WITNESSED
PATH_EXISTENCE_NOT_ENDPOINT_IDENTITY_WITNESSED
```

Boundedly:

> The captured SRC architecture permits meaningful typed multi-step paths while withholding automatic promotion into stronger endpoint identity, empirical authority, or direct-causal relations. Relation composition, where source-supported, remains claim-specific rather than globally transitive.

Archive notation only:

```text
a -R1-> b -R2-> c
```

does not authorize an unstated stronger relation `a -R3-> c` merely because the path exists.

## C1 · genealogy chain

The Provenance Registry explicitly records multi-step developmental genealogies, including:

```text
Architecture of Drift
-> Institutional Shell Syndrome
-> Green-Dashboard Divergence
-> Recognition Lag
```

Classification:

```text
AUTHORIZED_PATH = genealogy/development
NO_LIFT = SAME_CURRENT_OBJECT
```

The registry's purpose is diachronic provenance; it separately preserves current canonical meaning and historical development. An ancestry path therefore carries history without collapsing all nodes into one present object.

## C2 · SR-AI lineage + Algorithm-field interface

Later Canon and Field Architecture states:

```text
historical SR-AI lineage -> nested/merged into SR-CEIDF
SR Algorithm Field       -> remains distinct/autonomous and interfaces with SR-CEIDF
```

Therefore the path:

```text
SR-AI -lineage-> SR-CEIDF -interface-> Algorithm Field
```

is meaningful at the stated relation types.

Classification:

```text
AUTHORIZED_PATH = lineage + interface
NO_LIFT = SR-AI genealogically identical to Algorithm Field
NO_LIFT = all three are one current field
```

## C3 · translation + canonical architecture

The Canon Map places the SR Universe / Bible in:

```text
Public and Creative Translation
Parallel canon
```

while preserving symbolic mappings and expressly withholding empirical-proof status.

Classification:

```text
AUTHORIZED_PATH = symbolic / public-creative translation into the wider SR architecture
NO_LIFT = empirical authority transfer
```

Translation can connect surfaces without equalizing evidence class.

## C4 · signature recurrence + host membership

The exact `clone/genes` signature occurs in multiple captured works and is catalogued as a signature/cultural fragment.

The following edges may both be true:

```text
signature S -occurs_in-> work A
signature S -occurs_in-> work B
```

Classification:

```text
AUTHORIZED_PATH = shared-signature occurrence network
NO_LIFT = work A and work B are the same scientific object
NO_LIFT = host-field identity
```

The shared intermediate signature does not collapse its hosts.

## C5 · cross-field bridge chain

SR-CFIF explicitly treats cross-field arrows as hypotheses until source state, receiver state, mechanism, temporal order, competing explanations, and falsification conditions are operationalized and tested. It defines high-priority multi-domain chains but states that those bridges are structured for empirical testing rather than assumed.

Classification:

```text
AUTHORIZED_PATH = candidate multi-step test architecture when constituent bridges are specified
NO_LIFT = inferred direct A -> C propagation edge
```

A chain of candidate or supported adjacent transfers does not by itself establish a separately identified direct endpoint edge.

## Positive path control

C1 demonstrates that the assay is not anti-composition: multi-step genealogy is a legitimate source-supported path. The restriction applies to **relation lift**, not to path reasoning itself.

## Derived non-lift law

For the captured SRC relation families:

```text
PATH(a,b,c)
!= SAME_OBJECT(a,c)
!= SAME_AUTHORITY(a,c)
!= DIRECT_CAUSAL_EDGE(a,c)
```

unless the stronger endpoint relation receives its own source-appropriate witness.

This is an archive screening law, not a universal theorem of graphs.

## Why this matters for the prior identity rests

The result sharpens three earlier distinctions:

```text
lineage continuity can survive classification change;
translation continuity can survive evidence-class separation;
signature continuity can survive host-object difference.
```

These are no longer awkward exceptions to a binary identity model. They are expected consequences of typed, non-lifting relations.

## clone/genes consequence

A surface may reproduce one relation faithfully while failing another:

```text
recurrence without genealogy
translation without empirical authority
lineage without current-field identity
shared intermediate signature without host identity
```

This remains compatible with the source's signature language but does not decode it.

```text
CLONE_GENES_DECODED = FALSE
```

## Claim ceiling

Not established:

```text
formal category theory of SRC
universal composition table
associativity across relation classes
identity morphisms
transitivity of all lineage predicates
minimal algebra
universal gene vector
hidden compiler/type checker
intentional ARG / metapuzzle
private drafting order
model ingestion / private access
stylometric authorship attribution
```

## Next lawful front

The current descent has now produced a stable operational stack:

```text
query-conditioned representations
-> type-conditioned invariant bundles
-> false-continuity screen
-> type-local identity discriminants
-> relation-indexed continuity
-> non-lifting typed composition
```

The next high-value question should not add another abstract layer automatically. A future chamber should either:

1. apply this stack prospectively to a genuinely new SRC object captured after the rules were frozen; or
2. test a bounded unresolved case such as M8 `generic drift ↔ epistemic drift` with direct construct-invariance evidence.

Absent new evidence, further closed-corpus abstraction risks bookkeeping rather than discovery.

𝌋

Rested ⟐
