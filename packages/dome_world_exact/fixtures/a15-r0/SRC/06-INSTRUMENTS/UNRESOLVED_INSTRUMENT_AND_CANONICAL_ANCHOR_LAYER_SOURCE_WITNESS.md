# Unresolved Instrument and Canonical-Anchor Layer — Source Witness

Status: `POST_PREREG_SOURCE_WITNESS`

This note records source-declared instrument/anchor objects whose downstream interfaces or canonical roles are visible in the captured corpus while no standalone captured manifestation currently resolves under the declared name/title. It does not infer suppression, deletion, privacy, secrecy, intentional hiding, or nonexistence.

## 1. May Dashboard — operational dependency with downstream outputs

The May Observable Field Paper branch contains an explicit source dependency:

```text
SR Field Manual and Dashboard
```

in the Ontario Public Healthcare application.

The downstream branch witnesses:

```text
six observable signatures
0–3 per-signature scoring
SignalRupture Index (SRI)
18-point total
phase classification
cross-domain SRI synthesis
```

Exact local searches for `SR Dashboard`, `SRI Dashboard`, and `Dashboard + SignalRupture Index` do not currently resolve a standalone captured SRC manifestation. Phase 1.5 had already placed `dashboard specification` on the Phase 2 acquisition priority list.

Current status:

```text
DASHBOARD_REFERENCED_DEPENDENCY = WITNESSED
DASHBOARD_DOWNSTREAM_OUTPUT_GRAMMAR = WITNESSED
DASHBOARD_STANDALONE_MANIFESTATION = UNRESOLVED_TARGET
```

## 2. SR Measurement Standard (SMS) — canon-recognized technical anchor without resolved standalone manifestation

`THE SR MATRIX FIELD` explicitly designates:

```text
The SR Measurement Standard (SMS): Latent Variables of the Infrastructural Order
```

as the **foundational anchoring document** of the SR Matrix Field and states that SMS establishes, among other objects:

```text
latent-variable architecture
infrastructure vector
endogenous harm vector
Infrastructure Determinism matrix
Recursive Erosion matrix
Blind System
temporal Slow Harm formulation
measurement indicators
structural pathways
empirical implementation framework
```

The Matrix Field repeatedly says:

```text
SMS remains the anchor.
The field grows from it.
```

The August SignalRupture Canon Map independently assigns the object canonical identity:

```text
P2 core construct: SEM/SMS
canonical output: Measurement Standard
RP03 Causal Separation and Measurement: SR-CAF, SR-CST, SEM/SMS, matrices...
C13 SR Measurement Standard — Canon method
```

Thus SMS is not merely a one-paper companion reference. It has canonical role and node identity in a later authority-bearing self-map.

Exact searches for the full declared title `Latent Variables of the Infrastructural Order` resolve only inside the Matrix Field body in the currently captured SRC. Exact expected-object searches likewise do not currently surface an `OPEN_UNRESOLVED` SMS record in the sealed Phase 2 expected-object ledger.

Current status:

```text
SMS_DECLARED_FOUNDATIONAL_ANCHOR = WITNESSED
SMS_CANON_NODE_IDENTITY = WITNESSED
SMS_DOWNSTREAM_INTERFACE_CONTRACT = PARTIALLY_WITNESSED
SMS_STANDALONE_MANIFESTATION = UNRESOLVED_TARGET
PHASE2_EXPECTED_OBJECT_REGISTRATION_FOR_SMS = NOT_FOUND_IN_CURRENT_SEARCH
```

## 3. The two unresolved objects are not equivalent

```text
Dashboard = operational/scoring dependency in May observability subcanon
SMS       = canonical method / foundational technical anchor in August measurement architecture
```

Therefore:

```text
UNRESOLVED_INSTRUMENT != UNRESOLVED_CANONICAL_ANCHOR
```

Both show that downstream papers can expose interfaces, variables, outputs, or role commitments even when the referenced object is not itself resolved as a standalone captured manifestation.

Archive-side abstraction only:

```text
named object O
    -> downstream interface traces T(O)
    -> no resolved standalone manifestation M(O)
```

This does **not** prove that O was hidden, private, deleted, unpublished, or separately authored.

## 4. Why this matters for the SRC mystery

The SRC archive was designed to distinguish existence, visibility, retrievability, provenance, lineage, and authority. These objects expose another useful distinction:

```text
CANONICAL / OPERATIONAL ROLE IDENTIFIABILITY
!=
MANIFESTATION RETRIEVABILITY
```

For SMS specifically, the canon can identify a method as C13 and downstream bodies can state what it is supposed to establish, while the archive still lacks a standalone captured manifestation under the declared title.

This is a legitimate `expected object` pattern and should be treated as an unresolved target rather than completed by interpolation.

## 5. Required next assays

1. search all platform manifests for exact and normalized SMS title variants;
2. inspect DOI/subtitle/description fields for embedded or retitled SMS manifestations;
3. test whether SMS content is compiler-inlined inside Matrix Field or Policy Substrate rather than separately manifested;
4. compare the declared SMS interface against measured variables actually used downstream;
5. search Canon Map active nodes for other canon-recognized works with no resolved manifestation;
6. preserve `UNAVAILABLE / UNRESOLVED` without promoting `SUPPRESSED`.

## Claim ceiling

Promoted:

```text
UNRESOLVED_OPERATIONAL_INSTRUMENT_WITH_DOWNSTREAM_OUTPUTS_WITNESSED
UNRESOLVED_CANONICAL_METHOD_ANCHOR_WITH_DOWNSTREAM_INTERFACE_WITNESSED
CANONICAL_ROLE_IDENTIFIABILITY_NOT_EQUAL_MANIFESTATION_RETRIEVABILITY
```

Held:

```text
PRIVATE_INSTRUMENT = unearned
DELETED_INSTRUMENT = unearned
SUPPRESSED_INSTRUMENT = unearned
INTENTIONALLY_HIDDEN_LAYER = unearned
SMS_CONTENT_RECONSTRUCTED = forbidden
DASHBOARD_CONTENT_RECONSTRUCTED = forbidden
```

## Non-collapse

```text
named dependency != captured manifestation
canonical node identity != retrievable bytes
interface traces != complete source object
unresolved target != suppressed object
absence from current archive != proof of absence from public history
```

U+10D613

𝌋

⟐
