# SRC Visibility-Lag Cross-Field Identity Rest · 2026-09-14

Status: **EARNED LOCAL REST / SOURCE-GROUNDED / NONCANONICAL**

Parent assay:

```text
VISIBILITY_LAG_CROSS_FIELD_IDENTITY_ASSAY_2026-09-14.md
```

## Result

```text
CROSS_FIELD_VISIBILITY_LAG_OPERATOR_INHERITANCE_WITNESSED
classification = SAME_OPERATOR / DOMAIN_BINDING
```

## Why A survived

The older SR corpus defines Visibility Lag as a recognition-delay operator:

```text
structural change -> delayed reliable recognition
VL_i = T_recognition,i - T_structural-change,i
```

The two newly captured domain uses preserve that causal skeleton.

### PSS · zenodo:22552967

```text
trigger state      = deterioration in compensatory burden
recognizer         = conventional indicators
lag semantics      = deterioration precedes later recognition
domain binding     = pandemic / cross-domain burden transfer
```

PSS does not replace the lag with a new causal object. It binds the generic recognition-delay structure to a pandemic-systems measurement surface.

### PDD · zenodo:22725400

```text
trigger state      = measurable buffer erosion / cross-domain deterioration
recognizer         = coordinated institutional recognition
lag semantics      = deterioration precedes recognition
domain binding     = political discontinuity / institutional response
```

PDD's qualifier `Institutional` narrows the recognizer. It does not reverse direction, abolish the temporal interval, or introduce a different causal role.

## Hostile outcome

The lexical-null survived inspection: the decision does not rest on both papers using `visibility lag`. The invariant bundle is structural:

```text
EARLIER_STATE_CHANGE
-> UNRECOGNIZED_INTERVAL
-> LATER_RECOGNITION
```

What changes across domains is the binding:

```text
what state changes
who/what recognizes it
which indicators timestamp recognition
which downstream consequences are studied
```

This matches the source's own operator-validity rule in `THE SIGNALRUPTURE CANON AND FIELD ARCHITECTURE`: cross-field operators require measurable, domain-consistent construct invariance rather than vocabulary reuse.

## What did not earn

```text
PSS visibility lag = new autonomous operator        FALSE / NOT EARNED
PDD IVL = new autonomous operator                   FALSE / NOT EARNED
PSS and PDD mechanisms identical in every variable  FALSE
empirical validity of either field                  UNADJUDICATED
causal effect size                                  UNADJUDICATED
novelty                                             UNADJUDICATED
TD613 theorem transfer                              FORBIDDEN
```

## Architectural consequence

The SR corpus now provides a clean positive example for future cross-surface identity work:

```text
operator identity may survive
while domain-specific variables, observables, and receiver surfaces change
```

That is materially stronger than lexical recurrence and weaker than full semantic identity.

Reopen only if a later controlling source redefines PSS visibility lag, PDD Institutional Visibility Lag, or the generic SR operator so that the recognition-delay skeleton no longer survives.

𝌋

Rested ⟐
