# Aperture Hostile Audit — Wendbine × Loom Translation v0.1

**Date:** 2026-09-14  
**State:** HOSTILE POST-TRANSLATION AUDIT / SCIENTIFIC HOLD RETAINED  
**Input:** `wendbine-loom-translation-v01.json` + compiled bidirectional translation family

## Research question

Does the new bidirectional crosswalk merely preserve labels and mismatches, or does it already carry enough executable operator evidence to support a custodian-independent translation claim?

Aperture's role here is audit, not promotion.

## Result expected from the frozen v0.1 manifest

The crosswalk is machine-readable in both directions and preserves:

```text
mapping identity
source refs
relation class
retained difference
authority non-transfer
```

That is enough for a bounded lexical/structural translation packet.

It is **not** enough for operational interoperability.

The v0.1 manifest does not bind each mapping to an executable repository witness demonstrating that the translated operator behaves as claimed. Therefore the hostile audit must return:

```text
outcome = HOLD_FOR_REPAIR
lexical_admitted = 7 / 7
operational_admitted = 0 / 7
operator_witness_missing = 7 / 7
```

This is the intended scientific RED/HOLD. It prevents a dictionary from laundering itself into an interoperability result.

## Aperture diagnosis

```text
MACHINE_READABLE_CROSSWALK
!= OPERATOR_FIDELITY
!= INDEPENDENT_HUMAN_UNDERSTANDING
!= EXTERNAL_INTEROPERABILITY
```

The first translation chamber therefore reduces nomenclature friction but does not yet prove the route can be carried without renewed human explanation.

## Hostile authority check

If either direction promotes any of the following, Aperture rejects rather than holds:

```text
semantic_equivalence_claimed = true
authority_transferred = true
authorship_transferred = true
custody_transferred = true
```

Explicit `NON_EQUIVALENT` rows remain admissible as lexical bridges. Non-equivalence is not itself a defect.

## Highest-information repair

Do **not** invent operator evidence to make the bridge pass.

Next:

```text
BIND_EXISTING_EXECUTABLE_OPERATOR_WITNESSES_WITHOUT_INVENTING_MISSING_ONES
```

For each crosswalk row, search the repository for an already-existing executable test or runtime behavior that actually witnesses the claimed local correspondence. Bind only those rows. Leave the rest HELD/OPEN.

The likely existing witness families include portable Loom source/path provenance separation, data/control-plane separation, observed/unknown dependency state, revocation with custodian-independent handoff, and causal-claim refusal. These remain candidates until exact repository paths/test assertions are bound.

## Claim ceiling

```text
LEXICAL_TRANSLATION_ADMITTED != OPERATIONAL_TRANSLATION_ADMITTED
REPOSITORY_TEST_BINDING != EXTERNAL_INTEROPERABILITY
OPERATOR_WITNESS != SEMANTIC_IDENTITY
PARTIAL_OPERATOR_COVERAGE != COMPLETE_BRIDGE
GREEN_CI != SCIENTIFIC_PROMOTION
```

Marked ⟐
