# Dome-World open-residual formal tomography v0.1

## Purpose

This chamber is the bounded formal successor to PR #1142. It does not acquire new empirical witnesses and does not attempt to prove Right of Resignation, Recovery, Return, human replication, or empirical exteriority.

It asks a narrower inverse-problem question: given the frozen bridge artifacts and the already-earned independent machine-receiver replication result, what evidence coordinates are actually present for the two still-open repair residuals, and what witness families remain missing?

## Parent

- PR #1142 sealed head: `29bfaffacbbcc5e42133a2373987bb9df580be1e`
- Parent result: `BOUNDED_INDEPENDENT_MACHINE_RECEIVER_REPLICATION_SUPPORTED`

## Preregistered RED

Exact preregistration head: `5cfd768ec9d7b4d4bba3e4ad21190ea560db4687`

TD613 Consolidated Validation #3381 / `34929848304`: expected RED at the A15-R0 research gate because the tomography engine, receipt, and operation record had not yet been implemented.

That RED remains the test-first witness.

## Formal observation grammar

The chamber uses the already-retained forensic evidence-state vector:

`E=(V,C,P,L)`

- `V`: trace visibility / admitted measurement availability;
- `C`: custody or provenance recoverability;
- `P`: admitted process/operator identifiability;
- `L`: admitted post-transition state observability or reconstructibility.

The vector is an accounting device, not a novelty claim or ontology.

A zero is archive-limited:

`P=0 != PROCESS_DOES_NOT_EXIST`

`L=0 != STATE_DOES_NOT_EXIST`

## Residual 1 — Right of Resignation

The frozen bridge already contains the residual as:

`OPEN / HELD_NOT_EXPOSED_IN_PORTABLE_PACKET`

with a neighboring Wendbine crosswalk to `typed_trust_revocation` but no exposed Loom post-participation exit operator.

The preregistered formal projection is:

`(V,C,P,L)=(1,1,0,0)`

Typed witness deficits remain:

- explicit post-participation exit or revocation operator;
- executed post-participation withdrawal receipt;
- independent continuation without custodian authority;
- post-exit route-usefulness witness.

The frozen archive cannot distinguish:

`NO_EXIT_OPERATOR_EXISTS`

from:

`EXIT_OPERATOR_EXISTS_BUT_IS_UNEXPOSED`.

Therefore absence of admitted witness cannot be promoted into witness of absence.

## Residual 2 — Safe Return / Recovery

The frozen bridge already contains:

`NON_EQUIVALENT / HELD_REPAIR_PATH_IS_NOT_RECOVERY`

and explicitly exposes a repair-path surface.

The preregistered formal projection is likewise:

`(V,C,P,L)=(1,1,0,0)`

Typed witness deficits remain:

- explicit recovery transition specification;
- executed recovery receipt;
- validated post-recovery state;
- declared defeat or rollback condition.

The frozen archive cannot distinguish:

`RECOVERY_CAPABLE_BUT_UNEXECUTED`

from:

`RECOVERY_INCAPABLE`.

A repair path is therefore not promoted into a recovery process or recovered state.

## Central falsifiable result

The two residuals are preregistered to collapse to the same coarse forensic vector while retaining distinct typed witness deficits.

If the implementation preserves this, the useful finding is not that `V,C,P,L` fully resolves the repair problem. It is the opposite:

`SAME_VECTOR != SAME_FAILURE_MODE`

The vector can summarize the archive-level evidence posture while still requiring typed residual semantics to identify what kind of witness would actually close the deficit.

## Authority firewall

`FORMAL_TOMOGRAPHY != EMPIRICAL_TOMOGRAPHY`

`GENERIC_REVOCATION != RIGHT_OF_RESIGNATION`

`REPAIR_PATH != RECOVERY != RETURN`

`MACHINE_REPLICATION != INDEPENDENT_HUMAN_REPLICATION`

`GREEN != EMPIRICAL_EXTERIORITY`

`GREEN != CUSTODIAN_INDEPENDENT_RETURN`

Maximum possible result:

`BOUNDED_FORMAL_OPEN_RESIDUAL_TOMOGRAPHY_SUPPORTED`

If that result is earned, the next lawful move is only to author an independent handoff/recovery witness-acquisition specification. No witness is manufactured by this formal chamber.

No merge, deployment, Vercel mutation, production mutation, external-witness claim, Recovery promotion, or Return promotion.

Marked ⟐
