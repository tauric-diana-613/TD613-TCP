# Wendbine Public Relational Continuity Atelier

> **Independent public-surface research fixture.** Wendbine materials remain the work of their stated public poster(s). TD613 and TD613-TCP claim no authorship, ownership, origin, derivation, affiliation, endorsement, or authority over the source posts.

This Atelier is modeled after the **SRC operational research atelier** and the **EMSTD613 Lineage Atelier**, but it is neither a copy of their research question nor a donor-import lane.

Its first job is narrower:

> Can a relational architecture be reconstructed from a bounded stream of serialized public outputs without treating semantic resemblance as private state, motive, hidden memory, or authority?

## Official status

Before this scaffold, `Wendbine Atelier` existed only as a conversational research idea. Current `main` contained no official `WENDBINE/` fixture.

This branch creates the first repository-resident Wendbine Atelier.

## First snapshot

```text
snapshot_id = wendbine-public-reddit-48h-20260911T092700Z-v01
requested window = 2026-09-09T09:27:00Z .. 2026-09-11T09:27:00Z
surface = public Reddit only
community = r/Wendbine
admitted public posts = 33
full Reddit bodies copied = false
private Google-group material admitted = false
coverage = PUBLIC_SEARCH_SNAPSHOT_NOT_EXHAUSTIVE_CENSUS
```

The source registry stores URLs, day-level publication dates, technical headers, bounded paraphrases, concept tags, and typed source observations. It does **not** mirror Reddit post bodies.

The 48-hour membership decision is safe at day precision because every admitted item is publicly indexed to September 10 or September 11, both inside the requested window. The retrieval surface did not provide a trustworthy exhaustive live subreddit census or exact publication times, so this archive must not infer that an unlisted post did not exist.

## Architecture

```text
WENDBINE/
├── 01-MANIFESTS
├── 02-ORIGINALS/public-reddit
├── 03-DERIVATIVES/public-reddit-48h
├── 04-RECEIPTS
├── 05-OPERATIONS
├── 06-INSTRUMENTS
├── 07-ARCHIVE-LEDGER
├── 08-EXTERNAL-CORPORA
└── 99-ADMIN
```

SRC contributes the non-collapse wall, typed route discipline, deterministic query/compiler posture, and source/evidence separation.

EMSTD613 contributes provenance-preserving intake, source/work distinction, explicit human-provenance state, comparison without lineage collapse, and red-team disconfirmation posture.

Wendbine adds a **serialized-public-topology** problem: the same small corpus may contain explicit dependencies, continuity references, corrections, programmatic sequencing, recurring concepts, and source-declared synthesis.

## Core membranes

```text
PUBLIC_POST != AUTHOR_INTENT
PUBLIC_POST_TOPOLOGY != PRIVATE_STATE
SERIALIZED_CONTINUITY != SHARED_HIDDEN_MEMORY
EXPLICIT_CROSS_REFERENCE != MODEL_INFERRED_EDGE
SEMANTIC_RESEMBLANCE != STATE_PERSISTENCE
STATE_PERSISTENCE != EXTERNAL_ORCHESTRATION
SAME_TOKEN != SAME_OPERATOR
CONVENTIONAL_DOMAIN_LANGUAGE != WENDBINE_PROVENANCE
TD613_COMPARABLE != TD613_DERIVED
SOURCE_ASSERTION != ARCHIVE_OBSERVATION != ATELIER_INFERENCE
```

The user-supplied handle binding `u/Upset-Ratio502` is stored as `HUMAN_PROVENANCE`; this Atelier does not adjudicate the poster's civil/legal identity, and no such adjudication is necessary to compile the public architecture.

## Typed topology

The compiler keeps relation families distinct:

```text
G_D = dependency topology
G_A = authority / permission / trust
G_F = information and metadata flow
G_I = identity / continuity / entity alignment
G_T = temporal / program / version relations
G_P = provenance / custody / ancestry
G_R = reconstruction / estimation / recovery
G_O = observation boundary / observability
G_X = cross-relation interaction or source-declared synthesis
```

Therefore:

```text
G_D != G_A != G_F != G_I != G_T != G_P != G_R != G_O != G_X
```

`G_COOCCURRENCE` is generated separately and carries no causal meaning.

## First compiled result

The 33-post snapshot is not merely a bag of repeated vocabulary. The admitted public record contains:

- an explicit security-layer base;
- an explicit authorization/trust corridor;
- an explicit third-party dependency program/table of contents;
- a source-level synthesis saying the relations form one causal topology;
- later posts that expand observability, state estimation, topology drift, provenance, causal reconstruction, recovery, and operational-digital-twin boundaries;
- an explicit EchoCore correction that narrows an earlier “generic drift checker” reading toward nonlinear cross-effects among heterogeneous relation types.

That supports a bounded claim of **serialized public architectural continuity**.

It does not establish hidden state continuity, a private shared memory, author motive, or any institutional/TD613 relationship.

## Compiler

```bash
node packages/dome_world_exact/fixtures/a15-r0/WENDBINE/99-ADMIN/wendbine-compile.mjs
```

The compiler consumes only the normalized public-source manifest and typed relation registry. It emits a deterministic topology containing source nodes, concept nodes, typed source-bound relations, recurrent concepts, and a non-causal co-occurrence overlay.

See:

- `06-INSTRUMENTS/WENDBINE_PUBLIC_CORPUS_COMPILER_V0_1.md`
- `06-INSTRUMENTS/HELD_OUT_RECONSTRUCTION_PROTOCOL_V0_1.md`

## Authority

```text
research_only = true
donor_import = false
private_group_intake = false
author_intent_adjudication = false
real_person_identity_adjudication = false
TD613_scientific_promotion = false
merge_authority = false
deployment_authority = false
human_closure_required = true
```

Marked ⟐
