# Verification package: lambda=2c & Emission-Gap Theorem
# Python 3.12.3. Install: pip install -r requirements.txt
# (mpmath is also a sympy dependency, but is used explicitly here.)

sympy==1.14.0
mpmath==1.3.0
numpy==2.4.4
pytest==9.1.1

# Not pip-installable (documented in README section 9):
#   - TeX Live (pdflatex) to rebuild the papers; lmodern optional.
#   - stdlib only: fractions, math, itertools, functools, random, subprocess.
